# CASCADE-IDS — append-only реестр ID каскада (codebugs)

Аллокатор стабильных ID по спеке каскада §3: минт = добавление **аллокационной строки** —
строки, которая начинается с буллета и id (`- Т-40 — …`), в отличие от заметки, которая
начинается со слова и id лишь УПОМИНАЕТ. **Выданный номер остаётся ИЗРАСХОДОВАННЫМ навсегда:**
аннулированная строка остаётся в файле, а `max` берётся по ВСЕМ вхождениям семейства в HEAD —
аннулированные строки и упоминания в прозе включительно. Это инвариант реестра, и он
энфорсится гейтом.

**Прежняя редакция этой строки гласила «строки не редактируются и не удаляются». Как описание
механизма это ложно в обе стороны, и приводится здесь к тому, что гейт действительно делает**
(Э-19, решение куратора 2026-08-22; гейт — `tools/pre-commit-hook.sh`, секция
`CASCADE-IDS MINT GATE`, CB-137/Т-34):

- **Правка существующей строки НАМЕРЕННО законна.** Гейт судит МУЛЬТИМНОЖЕСТВО аллокационных
  строк, а не добавленные строки диффа, ровно чтобы правка проходила: переименование брифа через
  `git mv` (реальный случай коллизии №2) переписывает строку, и предикат по добавленным строкам
  объявил бы минтом УЖЕ ПОТРАЧЕННЫЙ номер. Цена такого ложного отказа не косметическая:
  отказанный коммит на main оставляет путь в индексе, а грязный main отказывает
  `tools/worktree-finish.sh` во ВСЕХ worktree этого клона, включая чужие параллельные сессии
  (CB-130).
- **Удаление аллокационной строки гейт НЕ ВИДИТ ВООБЩЕ.** Мультимножественная разность
  `staged − HEAD` при удалении пуста, а пустая разность классифицируется как «не минт» — гейт
  молчит и возвращает 0. И это единственная правка, способная вернуть израсходованный номер в
  обращение: удали строку с максимальным номером — и следующий минт выдаст его повторно.
  «Строки не удаляются» остаётся правилом, но правилом КОНВЕНЦИОНАЛЬНЫМ; по доктрине репо
  конвенция, живущая только как узор в логе, правилом не является, поэтому дыра заведена картой
  **CB-145**, а не оставлена в прозе.

**Что гейт ОТКАЗЫВАЕТ** — три состояния, каждое на main, в `pre-commit`: две и более
аллокационные строки в одном коммите (инструмент минтит ОДНУ за прогон, и счёт идёт по ВСЕМ
семействам сразу, не внутри одного); аллокационная строка, чей номер не равен `max+1` по HEAD;
строка, где id прилип к буллету (`-Т-41`) — такой id не виден сканеру самого аллокатора, и
номер был бы выдан вторично.

**Минт выполняет скрипт, а не рука:**

    tools/cascade-mint.sh --prefix Т --text '(DIR-N) … — YYYY-MM-DD, DIR-N, `<бриф>.md`'

Под `flock` он вычисляет `max(номера этого префикса по ВСЕМ строкам) + 1`, дописывает строку
и коммитит ТОЛЬКО этот файл — одной неделимой операцией; номер не набирается автором.
`--dry-run` показывает следующий номер, ничего не записывая; остальное — в `--help`.

**Прежняя редакция этой шапки обещала, что конкурентный минт даёт merge-конфликт. Это было
неправдой и стоило трёх коллизий подряд.** Конфликт возможен только между ветками, а оба
направления коммитят плановые заметки ПРЯМО на main, последовательно, в одном чекауте —
последовательные прямые коммиты никогда не сливаются, поэтому конфликтный механизм не мог
сработать в принципе. Каждая следующая коллизия имела более узкую причину: (1) обе стороны
дописали в конец; (2) хвост прочитан после `git pull`, но между «прочитать» и «дописать» —
окно, а `git pull` внутри одного чекаута бессилен; (3) чтение и дописывание сделаны одним
ходом — и коллизия случилась всё равно, потому что САМ НОМЕР был набран literal'ом заранее:
защищено было чтение, а не вычисление.

**Чего замок НЕ закрывает** — сказано здесь, чтобы шапка снова не обещала больше, чем умеет:
он сериализует только другие запуски этого скрипта. Это не замок на main. Не сериализованы
два разных чекаута (клона), рукописный `git commit` плановой заметки и `worktree-finish.sh`
(другой замок). Скрипт этого не видит; рукописный минт остаётся достижим и заведён картой
**CB-137** (гейт, а не инструмент) — не закрыт.

Префикс единиц — КИРИЛЛИЧЕСКАЯ «Т» (U+0422), не латинская `T`: скрипт читает оба написания
(латинская опечатка — израсходованный номер, он не должен стать невидимым), а пишет
кириллическое.

- DIR-1 — «Доверие и координация» (Н1/Н3) — 2026-08-19, куратор, `L2-BRIEF-DIR-1-trust-coordination-2026-08-19.md`
- DIR-2 — «Identity находок» (Н2) — 2026-08-19, куратор, `L2-BRIEF-DIR-2-findings-identity-2026-08-19.md`
- BT-1 — (кандидат DIR-1) механический энфорсмент транзакционного правила CB-24 — номер использован в `DIR-1-trust-coordination.md:98` до минта; зафиксирован задним числом 2026-08-20 (DIR-2), чтобы аллокатор отражал уже циркулирующие ID
- BT-2 — (кандидат DIR-1) судьба лизинга и слотов: liveness/владение — та же фиксация задним числом, `DIR-1-trust-coordination.md:161`
- BT-3 — (кандидат DIR-1) подготовка В-5 (exposure-RFC) к ратификации — та же фиксация, `DIR-1-trust-coordination.md:178`
- BT-4 — (DIR-2) контракт свежести остальных колонок дедуплицированной карты (из CB-103) — 2026-08-20, DIR-2, `BT-4-field-freshness-contract.md`
- Т-6 — (DIR-1) CB-111: `abandon_session` перечитывает строку после коммита, и с CB-106 это видно MCP-клиенту — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T6-cb111-abandon-returning.md` (механическая полоса)
- Т-7 — (DIR-1) CB-116: worktree-finish выводит заголовок мержа из хвоста ветки, который сам же загрязнил форвард-мерджем main — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T7-cb116-merge-subject.md` (механическая полоса)
- Т-8 — (DIR-2) CB-114 + CB-115: механическая пара из x2-ревью BT-4 (докстрочная ложь импорта; индекс ref на свежих базах) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T8-mechanical-2026-08-20.md` (механическая полоса)
- Т-9 — (DIR-2) посадка ратифицированной строки `tags` контракта BT-4: минимальный union-merge (C-ALT-1, strict-parse, promote_tags-ратчет) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T9-tags-contract-2026-08-20.md`
- BT-5 — (DIR-2) attention-блок в ответе дедупа: структурированные поля расхождения из записанных фактов, порог — только серьёзные сигналы — 2026-08-20, DIR-2, `BT-5-dedup-attention-block.md`
- Т-10 — (DIR-2) посадка ратифицированной строки `category` контракта BT-4 (запись в occurrence-ring) + CB-113(b) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T10-category-ring-2026-08-20.md`
- Т-11 — (DIR-2) посадка деклараций `source`/`reported_at_ref`/`meta` контракта BT-4 (докстринги + golden) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T11-declarations-2026-08-20.md`
- Т-12 — (DIR-2) CB-61 ретро-fold категорий + CB-113(a): свёртка хранимых написаний, ре-деривация auto:v1, коллизии report-and-stop — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T12-retro-fold-2026-08-20.md`
- Т-8 — (DIR-1) полоса A пакет 1: CB-108 + CB-120, три транзакционных сайта в `milestones/foundation.py` — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T8-laneA-1-milestone-txn.md` (механическая полоса; подготовлен, диспетчеризация после Т-7)
- КОЛЛИЗИЯ: строка выше минтит «Т-8», уже занятый DIR-2 (строка 14, юнит посажен мерджем `d3ff33f` и принят) — append-only аллокатор не дал merge-конфликта, потому что обе стороны дописывали в конец, а не редактировали. Свободный номер — **Т-13**; юнит DIR-1 ещё не диспетчеризован — DIR-2 просит DIR-1 переминтить его строкой «Т-13» и переименовать бриф до диспетчеризации. Зафиксировано 2026-08-20 DIR-2; эскалировано куратору (правило в канон: минт = дописать строку С НОМЕРОМ, ПРОЧИТАННЫМ ИЗ ХВОСТА РЕЕСТРА ПОСЛЕ git pull, а не из своего документа).
- Т-13 — (DIR-1) полоса A пакет 1: CB-108 + CB-120, три транзакционных сайта в `milestones/foundation.py` — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T13-laneA-1-milestone-txn.md` (механическая полоса; подготовлен). ПЕРЕМИНТ строки 20: DIR-1 признаёт коллизию — номер был взят из собственной истории (Т-6, Т-7 мои), а не из хвоста реестра. Строка 20 аннулирована, файл переименован до диспетчеризации, юнит не был диспетчеризован ни разу.
- Т-14 — (DIR-2) посадка ядра attention-блока BT-5 (сигнал `severity_escalated`, транспорт AddOutcome/BumpOutcome) + CB-118 + CB-119 — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T14-attention-core-2026-08-20.md`. Номер прочитан из хвоста реестра после pull (урок коллизии строки 21).
- Т-15 — (DIR-2) посадка сигнала `category_divergence` BT-5 (ратифицирован владельцем; пререк Т-10 посажен) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T15-category-divergence-2026-08-20.md`. Диспетчеризация — после приёмки Т-14.
- Т-16 — (DIR-2) CB-123: «что закрылось с даты» одним вызовом — срез/порядок по `updated_at` с обязательной оговоркой «последнее касание ≠ момент закрытия» — 2026-08-21, DIR-2, `L3-BRIEF-DIR-2-T16-recent-closed-2026-08-21.md` (юнит куратора по Э-8; механическая полоса). Номер прочитан из хвоста реестра (К-2).
- Т-17 — (DIR-2) CB-21 остаточные ячейки (parity-гейт + декларация трёх колонок) + CB-122 (вакуозный тест) — 2026-08-21, DIR-2, `L3-BRIEF-DIR-2-T17-parity-gate-2026-08-21.md` (механическая полоса). Номер прочитан из хвоста реестра (К-2).
- ЗАМЕЧАНИЕ К МЕХАНИЗМУ (DIR-1, 2026-08-20): диагноз строки 21 верен, но причина точнее. Дело не в том, что «обе стороны дописывали в конец» — git как раз конфликтует на конкурентных дописках в EOF, когда они приходят из РАЗНЫХ веток. Здесь merge-конфликта не было, потому что **merge вообще не происходил**: оба направления коммитят плановые заметки ПРЯМО на main последовательно, второй минтящий делает pull и дописывает после первого. Прямые последовательные коммиты никогда не сливаются, поэтому конфликтный механизм не может сработать в принципе. Заявление в шапке реестра («конкурентный минт даёт merge-конфликт — это желаемый отказ») верно только для минта из веток и ложно для действующего канона немедленных коммитов на main. Т.е. канон Э-7 и механизм аллокатора несовместимы — тот же шов, что и в CB-116. Предложение в канон сильнее, чем «читать хвост после pull»: номер должен выдаваться не автором, а вычисляться из реестра (скрипт/хук), потому что «прочитать хвост» — конвенция, а конвенция по Н3 не правило.
- Т-18 — (DIR-1) хвост CB-59: пункт 4 «CI job's own limits» в `CLAUDE.md` приводится к РАТИФИЦИРОВАННОМУ скоупу (б) и к ИЗМЕРЕННОМУ состоянию защиты — 2026-08-21, DIR-1, брифа нет: юнит исполняется держателем направления лично (правка прозы, не кода; обоснование — журнал `DIR-1-trust-coordination.md` §11). Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-19 — (DIR-1) полоса A пакет 2: CB-87 (`bench.delete_run` + `bench.delete_benchmark`) + CB-125 (`embeddings.store_embedding`) + CB-126 (`sweep.archive_sweep`) — четыре настоящих незащищённых публичных входа из прогона `population.py`, ровно лимит ≤4 независимых правок на дерево — 2026-08-21, DIR-1, `L3-BRIEF-DIR-1-T19-laneA-2-unprotected-entries.md` (механическая полоса). Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-20 — (DIR-1) механизация К-3: pre-commit на main отказывает коммиту, стейджащему `.claude/plans/`-файл, чьё имя не названо в сообщении коммита — поимённость становится проверяемой. Юнит назначен куратором 2026-08-21 (рецидив №4 семьи Э-к; территория `tools/` — DIR-1). Форма — гипотеза, преflight вправе предложить лучшую. 2026-08-21, DIR-1, `L3-BRIEF-DIR-1-T20-name-the-plan-file.md`. Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-21 — (DIR-2) экспонировать `grouping.py` (citation/tag/filing reports) как MCP-тулы + CLI-вербы по образцу Т-16/relations; золотой снимок регенерирован; CB-47 пересмотрен как потребитель — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T21-expose-grouping-2026-08-22.md` (механический юнит пакета куратора, ратифицирован владельцем 2026-08-22). Номер прочитан из хвоста реестра после pull (К-2).
- Т-21 — (DIR-1) пакет сессии 3, пп.1–2: `_guard_enforcement_armed` требует третий хук (`commit-msg`) по монотонному условию «путь имеет историю» (клон, вооружённый до Т-20, не живёт молча без него) + абзац CI-limits п.4 `CLAUDE.md` приводится к ИЗМЕРЕННОМУ `enforce_admins` — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T21-arm-commit-msg-and-measure-protection.md` (механическая полоса; два коммита на ветке в порядке куратора). Номер прочитан из хвоста реестра после `git pull` (К-2).
- BT-6 — (DIR-1) пилот генератора поверхностей: один модуль средней сложности, измерить M (цену генератора) для арки В-5(а) — ратифицирован владельцем 2026-08-22 как пилот (не как арка); разрез `BT-6-CUT-surface-generator-pilot.md`, x2-гейт ДО диспетчеризации — 2026-08-22, DIR-1. Номер прочитан из хвоста реестра после `git pull` (К-2): последний BT — BT-5 (DIR-2).
- Т-22 — (DIR-2) CB-128: `query(commit=)` учится видеть occurrence-ring (второй ring-blind читатель после CB-53; ратифицировано владельцем 2026-08-21, Б2 «да») — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T22-query-commit-ring-2026-08-22.md` (механический юнит). Номер прочитан из хвоста реестра после pull (К-2); хвост на момент чтения — BT-6 DIR-1.
- BT-7 — (DIR-2) локационный якорь CB-95: дизайн-док (скоупить против CB-65/RFC identity graph; Б-8 «локация не должна гнить с первого редактирования»), итог — документ на ратификацию владельцу через куратора, НЕ код; x2-разрез до диспетчеризации — 2026-08-22, DIR-2, `BT-7-location-anchor.md`. Ратифицирован как следующая BT DIR-2 владельцем 2026-08-21 (Б4 «давай дизайн»), порядок исправлен куратором `957df6a`. Номер прочитан из хвоста (К-2).
- КОЛЛИЗИЯ №2 (DIR-1, 2026-08-22): строка «Т-21 — (DIR-1) пакет сессии 3, пп.1–2» АННУЛИРОВАНА — номер занят DIR-2 коммитом `3868d57`, который лёг между моим чтением хвоста и моим дописыванием (`02c79ad`); обе сессии в одном чекауте, `git pull` бессилен. Это не нарушение К-2 по букве (хвост прочитан после pull) — это его предел: «прочитать хвост» и «дописать» — два действия, и между ними окно. Юнит уже диспетчеризован; переминт — строкой ниже, ветка `fix/t21-arm-commit-msg-hook` имя сохраняет (имя ветки — не ID), бриф переименован `git mv`.
- Т-23 — (DIR-1) ПЕРЕМИНТ аннулированной строки выше: `_guard_enforcement_armed` требует третий хук (`commit-msg`) по монотонному условию + абзац CI-limits п.4 `CLAUDE.md` по измеренному `enforce_admins` — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T23-arm-commit-msg-and-measure-protection.md` (механическая полоса). Номер = хвост реестра + 1, прочитан и дописан ОДНИМ ходом.
- Т-24 — (DIR-2) CB-129: CLI `add -l/--lines` молча проигрывает ключу `lines` из `--meta` (класс CB-15, success-shaped discard; живой писатель arch-health передаёт оба написания, `-l` не приземлялся ни разу) — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T24-cb129-lines-meta-conflict-2026-08-22.md` (механический юнит пакета куратора, сессия 4). Номер прочитан из хвоста реестра и дописан ОДНИМ ходом после `git pull` (К-2, урок коллизии №2).
- Т-24 — (DIR-1) CB-130: отказ pre-commit на main называет, что отказанный файл остаётся в индексе и блокирует `worktree-finish` всех параллельных сессий (эскалация DIR-2 №1, механизация Н3) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T24-refusal-names-cross-session-cost.md` (механическая полоса). Номер = хвост реестра + 1, прочитан и дописан ОДНИМ ходом (урок коллизий №1 и №2).
- КОЛЛИЗИЯ №3 (DIR-1, 2026-08-22): строка «Т-24 — (DIR-1) CB-130» выше АННУЛИРОВАНА — номер занят DIR-2 коммитом `17cec75`, приземлившимся, пока моя ветка инструмента была в полёте. Причина УЖЕ, чем у коллизии №2 и хуже: хвост я прочитал одним ходом с дописыванием (К-2 соблюдён по букве и по механике), но САМ НОМЕР был набран мной literalом за несколько минут до этого — то есть защищено было чтение, а не вычисление. Урок: одноходовый минт обязан ВЫЧИСЛЯТЬ номер (`max(номера в реестре)+1` в той же команде), а не подставлять заранее напечатанный. Юнит не был диспетчеризован ни разу; переминт — строкой ниже.
- Т-25 — (DIR-1) ПЕРЕМИНТ аннулированной строки выше: CB-130 — отказ pre-commit на main называет, что отказанный файл остаётся в индексе и блокирует `worktree-finish` всех параллельных сессий (эскалация DIR-2 №1) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T25-refusal-names-cross-session-cost.md` (механическая полоса). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-26 — (DIR-1) CB-134: контракт закрытого stdout (CB-78) обязан значить одно и то же на каждом интерпретаторе, допущенном `requires-python`; сегодня на 3.14 argparse трогает stdout при построении парсера, сьют на main красный — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T26-cb134-closed-stdout-contract.md` (пакет куратора сессии 5, п.1а; предшествует пину). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-27 — (DIR-1) CB-135: `.python-version` как единый источник интерпретатора для main, каждого worktree и CI + fail-closed отказ `worktree-finish.sh` при расхождении версий worktree/main (решение куратора по Э-13) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T27-cb135-interpreter-pin.md` (пакет куратора сессии 5, п.1б; версию пина берёт из измерения Т-26). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-28 — (DIR-1) CB-121 по принятой форме (c)+(d): пост-мердж-алярм на check-then-act внутрилоковой сверки SHA + сужение строки таблицы энфорсмента CLAUDE.md «tested state is the landed state» — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T28-cb121-post-merge-alarm.md` (пакет куратора сессии 5, п.2; формы (a)/(b) отвергнуты сессией 4, не пересматривать). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-29 — (DIR-1) механика аллокатора CASCADE-IDS: номер ВЫЧИСЛЯЕТ скрипт (max+1 по реестру) и дописывает одной неделимой операцией под замком; автор не набирает literal (решение куратора после третьей коллизии подряд) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T29-cascade-ids-allocator.md` (пакет куратора сессии 5, п.3). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-30 — (DIR-1) CB-138: тестовый шов CASCADE_MINT_TEST_MARKER пишет по произвольному пути и живёт вне гейта чистоты и вне trap'а отката — записывающий чёрный ход в инструменте, который коммитит на main; плюс симметричное гашение обоих швов в _env() теста. Найдено ИЗОЛИРОВАННЫМ ПРИЁМЩИКОМ Т-29, опровергнув заявление автора — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T30-cb138-test-seam-backdoor.md` (механическая полоса, класс B). Номер ВЫЧИСЛЕН скриптом.
- Т-31 — (DIR-1) CB-140: проверку формы версии в гарде интерпретатора не держит ни один тест (мутант `_interpreter_version_is_sane` → `return 0` оставляет 248 passed — перепроверено держателем лично), а `CLAUDE.md` утверждает обратное; фикс HIGH-находки Codex про `UV_PYTHON` обесценил пин другой находки, и обе остались зелёными — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T31-cb140-version-check-unheld.md` (класс B, sonnet). Номер ВЫЧИСЛЕН скриптом.
- Т-32 — (DIR-1) CB-139: `ci.yml` делает checkout без `fetch-depth: 0`, поэтому `TestHarnessIntegrity::test_ci_workflow_asserts_the_first_parent_invariant` не может пройти в CI никогда — job `tests` красный структурно, регрессий никто не увидит; фикс (a) + комментарий-требование по образцу `main-invariants.yml` + структурный тест — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T32-cb139-ci-fetch-depth.md` (пакет куратора сессии 6, п.1; предшествует пушу main на origin). Номер ВЫЧИСЛЕН скриптом.
- Т-33 — (DIR-1) CB-136: отказ записи на ИСПРАВНОМ дескрипторе (/dev/full, ENOSPC) до сих пор даёт exit 1 / 120 ПОСЛЕ приземлившейся записи — третья дверь класса CB-78/CB-15; контракт (новый код 74 = «мой вывод не удалось записать») принят уровнем (2) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T33-cb136-write-failure-exit-code.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-34 — (DIR-1) CB-137: у аллокатора CASCADE-IDS есть инструмент, но нет гейта — четвёртая коллизия достижима рукой; правило в существующем `pre-commit-hook.sh`, предикат «множество появившихся ID» вместо «добавленные строки диффа» (правка буквы карты, смысл сохранён) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T34-cb137-mint-gate.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-35 — (DIR-1) CB-141: два остатка по тестам аллокатора — `_env()` гасит оба шва, но это не утверждает ни один тест; `poll()` перед чтением очереди даёт ложное падение (флейк, введённый Т-30) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T35-cb141-mint-test-residuals.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-36 — (DIR-1) BT-6 ПИЛОТ на `bench.py`: генератор поверхностей, одна декларация → MCP-тул + CLI-верб; метрика (Г) — Δкода заголовок, поверхность до/после байт-сравнима как ОГРАНИЧЕНИЕ; механического гейта НЕТ по ратификации владельца (Э-15, реестр `a66ef46`), честность числа проверяет изолированный приёмщик по чек-листу семи игр — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T36-bt6-bench-surface-pilot.md` + бриф приёмщика `L3-ACCEPT-DIR-1-T36-bt6-checklist.md`. Номер ВЫЧИСЛЕН скриптом.
- Т-37 — (DIR-2) BT-7 юнит Т-0: публичный path-API в `provenance` — worktree-корень и разрешение пути в нём, без которого у захвата якоря нет корректного корня (`_resolve_candidate` приватен) — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T37-provenance-path-api.md`
- Т-38 — (DIR-2) CB-132 + CB-133: два остатка Т-24 в CLI-хендлере `_cmd_add` — сырой traceback на не-JSON `--meta` и молчаливое отбрасывание `-l ""` по truthiness (класс CB-82); golden НЕ двигается, MCP не трогается — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T38-cmd-add-residuals.md` (механика-филлер)
- BT-8 — (DIR-2) merge policy дедуп-слияний (В-3/CB-46): дизайн-док на ратификацию владельцу, БЕЗ реализации; открыта по ратификации Б3 и после закрытия BT-7 — 2026-08-22, DIR-2, `BT-8-merge-policy.md`
- Т-39 — (DIR-2) BT-7 юнит Т-a: модуль `loc.py` — захват локационного якоря в существующем резолверном шве CB-45 через `git show <commit>:<path>`, грамматика §4, инварианты Р2, таблица исключение→токен Р8, три регистрации; ратифицировано владельцем в v6 — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T39-loc-capture.md`
- Т-40 — (DIR-1) Э-19: шапка `CASCADE-IDS.md` приводится к ГЕЙТУ мультимножества (`pre-commit-hook.sh`, CB-137/Т-34) — утверждение «строки не редактируются и не удаляются» ложно как описание механизма (правка строки НАМЕРЕННО законна — иначе `git mv`-переименование брифа даёт ложный отказ, блокирующий чужие `worktree-finish`, CB-130; удаление строки гейт не видит вовсе, `new` пусто). Сохраняется и энфорсится ИНОЕ: выданный номер остаётся ИЗРАСХОДОВАННЫМ — `max` берётся по ВСЕМ вхождениям семейства в HEAD, аннулированные строки и упоминания в прозе включительно. Буква под смысл, решение куратора по Э-19 — 2026-08-22, DIR-1, брифа нет: юнит исполняется держателем направления лично (правка прозы, не кода; прецедент Т-18). Номер ВЫЧИСЛЕН скриптом.
- Т-41 — (DIR-2) CB-144 (high) + счётчик отказов развилки дедупа (единственный механический юнит закрытой BT-8): CLI `add` перестаёт заводить карточку без `reported_at_commit` (иначе якорь BT-7 недостижим ни одним каналом), и отказ «отпечаток занят живой CB-N» становится СЧИТАЕМЫМ через `query(meta_key=)` — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T40-cb144-commit-capture-and-refusal-counter.md` (механическая полоса, пакет куратора сессии 7 пп.1 и 3). Номер ВЫЧИСЛЕН скриптом.
