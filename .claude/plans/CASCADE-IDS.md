# CASCADE-IDS — append-only реестр ID каскада (codebugs)

Аллокатор стабильных ID по спеке каскада §3: минт = добавление **аллокационной строки** —
строки, которая начинается с буллета и id (`- Т-40 — …`), в отличие от заметки, которая
начинается со слова и id лишь УПОМИНАЕТ. **Выданный номер остаётся ИЗРАСХОДОВАННЫМ навсегда:**
аннулированная строка остаётся в файле, а `max` берётся по ВСЕМ вхождениям семейства в HEAD —
аннулированные строки и упоминания в прозе включительно. Это инвариант реестра, и он
энфорсится гейтом.

**Прежняя редакция этой строки гласила «строки не редактируются и не удаляются». Как описание
механизма это ложно в обе стороны, и приводится здесь к тому, что гейт действительно делает**
(Э-19, решение куратора 2026-08-22; гейт — `tools/pre-commit-hook.sh`, секция
`CASCADE-IDS MINT GATE`, CB-137/Т-34):

- **Правка существующей строки НАМЕРЕННО законна.** Гейт судит МУЛЬТИМНОЖЕСТВО аллокационных
  строк, а не добавленные строки диффа, ровно чтобы правка проходила: переименование брифа через
  `git mv` (реальный случай коллизии №2) переписывает строку, и предикат по добавленным строкам
  объявил бы минтом УЖЕ ПОТРАЧЕННЫЙ номер. Цена такого ложного отказа не косметическая:
  отказанный коммит на main оставляет путь в индексе, а грязный main отказывает
  `tools/worktree-finish.sh` во ВСЕХ worktree этого клона, включая чужие параллельные сессии
  (CB-130).
- **Удаление аллокационной строки ОТКАЗЫВАЕТСЯ.** Это стало правдой 2026-08-23, юнитом Т-47
  (мерж `c264e1a`, карта **CB-145**); **до него гейт удаления не видел вообще**, и абзац на этом
  месте честно это признавал. Механизм: разность мультимножеств считается по ОБЪЕДИНЕНИЮ id обеих
  сторон, а не обходом одного лишь `staged` — обход по `staged` слеп к удалению ПО ПОСТРОЕНИЮ,
  потому что id, чья последняя строка удалена, в `staged` не появляется вовсе; отказ по «staged
  меньше HEAD» стоит ДО раннего возврата нуля по пустой разности, то есть до самого состояния,
  которое удаление и производит. Зачем: удаление — единственная правка, способная вернуть
  ИЗРАСХОДОВАННЫЙ номер в обращение (удали строку с максимальным номером — и следующий минт выдаст
  его повторно), и инструмент здесь не страхует, потому что читает тот же HEAD и вычислит тот же
  заниженный `max`.
- **Цена этого отказа, названная здесь, а не оставленная на обнаружение:** аннулировать строку,
  ПЕРЕПИСАВ её в прозаическую заметку, больше нельзя — это удаление аллокационной строки, и оно
  отказывается. Канон и так требует сохранять буллет с id у аннулированной строки (её номер
  остаётся израсходованным), так что ложным этот отказ не является; но форма аннулирования стала
  МЕХАНИЧЕСКИ ОБЯЗАТЕЛЬНОЙ, а не рекомендованной. Аннулируй, дописывая пометку В ТУ ЖЕ строку или
  отдельной заметкой рядом; `--no-verify` остаётся выходом для того, кто знает, что делает.
- **Чего этот отказ НЕ закрывает, измерено:** сам гейт отказал бы **12 из 52** реально случившихся
  коммитов этого реестра — они сделаны до появления аллокатора, когда номера набирались рукой и
  несколько строк могли лечь одним коммитом. Прогон против всей истории сделан дважды и независимо
  (исполнителем Т-47 и его приёмщиком), множества отказов на версии с фиксом и без **идентичны**,
  вклад CB-145 в них — **ноль**. Это латентный класс ложных отказов, заведён картой **CB-150**.

**Что гейт ОТКАЗЫВАЕТ** — ЧЕТЫРЕ состояния, каждое на main, в `pre-commit`: две и более
аллокационные строки в одном коммите (инструмент минтит ОДНУ за прогон, и счёт идёт по ВСЕМ
семействам сразу, не внутри одного); аллокационная строка, чей номер не равен `max+1` по HEAD;
строка, где id прилип к буллету (`-Т-41`) — такой id не виден сканеру самого аллокатора, и
номер был бы выдан вторично; **и удаление аллокационной строки** (Т-47/CB-145, выше).

**Минт выполняет скрипт, а не рука:**

    tools/cascade-mint.sh --prefix Т --text '(DIR-N) … — YYYY-MM-DD, DIR-N, `<бриф>.md`'

Под `flock` он вычисляет `max(номера этого префикса по ВСЕМ строкам) + 1`, дописывает строку
и коммитит ТОЛЬКО этот файл — одной неделимой операцией; номер не набирается автором.
`--dry-run` показывает следующий номер, ничего не записывая; остальное — в `--help`.

**Прежняя редакция этой шапки обещала, что конкурентный минт даёт merge-конфликт. Это было
неправдой и стоило трёх коллизий подряд.** Конфликт возможен только между ветками, а оба
направления коммитят плановые заметки ПРЯМО на main, последовательно, в одном чекауте —
последовательные прямые коммиты никогда не сливаются, поэтому конфликтный механизм не мог
сработать в принципе. Каждая следующая коллизия имела более узкую причину: (1) обе стороны
дописали в конец; (2) хвост прочитан после `git pull`, но между «прочитать» и «дописать» —
окно, а `git pull` внутри одного чекаута бессилен; (3) чтение и дописывание сделаны одним
ходом — и коллизия случилась всё равно, потому что САМ НОМЕР был набран literal'ом заранее:
защищено было чтение, а не вычисление.

**Чего замок НЕ закрывает** — сказано здесь, чтобы шапка снова не обещала больше, чем умеет:
он сериализует только другие запуски этого скрипта. Это не замок на main. Не сериализованы
два разных чекаута (клона), рукописный `git commit` плановой заметки и `worktree-finish.sh`
(другой замок). Скрипт этого не видит; рукописный минт остаётся достижим и заведён картой
**CB-137** (гейт, а не инструмент) — не закрыт.

Префикс единиц — КИРИЛЛИЧЕСКАЯ «Т» (U+0422), не латинская `T`: скрипт читает оба написания
(латинская опечатка — израсходованный номер, он не должен стать невидимым), а пишет
кириллическое.

- DIR-1 — «Доверие и координация» (Н1/Н3) — 2026-08-19, куратор, `L2-BRIEF-DIR-1-trust-coordination-2026-08-19.md`
- DIR-2 — «Identity находок» (Н2) — 2026-08-19, куратор, `L2-BRIEF-DIR-2-findings-identity-2026-08-19.md`
- BT-1 — (кандидат DIR-1) механический энфорсмент транзакционного правила CB-24 — номер использован в `DIR-1-trust-coordination.md:98` до минта; зафиксирован задним числом 2026-08-20 (DIR-2), чтобы аллокатор отражал уже циркулирующие ID
- BT-2 — (кандидат DIR-1) судьба лизинга и слотов: liveness/владение — та же фиксация задним числом, `DIR-1-trust-coordination.md:161`
- BT-3 — (кандидат DIR-1) подготовка В-5 (exposure-RFC) к ратификации — та же фиксация, `DIR-1-trust-coordination.md:178`
- BT-4 — (DIR-2) контракт свежести остальных колонок дедуплицированной карты (из CB-103) — 2026-08-20, DIR-2, `BT-4-field-freshness-contract.md`
- Т-6 — (DIR-1) CB-111: `abandon_session` перечитывает строку после коммита, и с CB-106 это видно MCP-клиенту — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T6-cb111-abandon-returning.md` (механическая полоса)
- Т-7 — (DIR-1) CB-116: worktree-finish выводит заголовок мержа из хвоста ветки, который сам же загрязнил форвард-мерджем main — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T7-cb116-merge-subject.md` (механическая полоса)
- Т-8 — (DIR-2) CB-114 + CB-115: механическая пара из x2-ревью BT-4 (докстрочная ложь импорта; индекс ref на свежих базах) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T8-mechanical-2026-08-20.md` (механическая полоса)
- Т-9 — (DIR-2) посадка ратифицированной строки `tags` контракта BT-4: минимальный union-merge (C-ALT-1, strict-parse, promote_tags-ратчет) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T9-tags-contract-2026-08-20.md`
- BT-5 — (DIR-2) attention-блок в ответе дедупа: структурированные поля расхождения из записанных фактов, порог — только серьёзные сигналы — 2026-08-20, DIR-2, `BT-5-dedup-attention-block.md`
- Т-10 — (DIR-2) посадка ратифицированной строки `category` контракта BT-4 (запись в occurrence-ring) + CB-113(b) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T10-category-ring-2026-08-20.md`
- Т-11 — (DIR-2) посадка деклараций `source`/`reported_at_ref`/`meta` контракта BT-4 (докстринги + golden) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T11-declarations-2026-08-20.md`
- Т-12 — (DIR-2) CB-61 ретро-fold категорий + CB-113(a): свёртка хранимых написаний, ре-деривация auto:v1, коллизии report-and-stop — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T12-retro-fold-2026-08-20.md`
- Т-8 — (DIR-1) полоса A пакет 1: CB-108 + CB-120, три транзакционных сайта в `milestones/foundation.py` — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T8-laneA-1-milestone-txn.md` (механическая полоса; подготовлен, диспетчеризация после Т-7)
- КОЛЛИЗИЯ: строка выше минтит «Т-8», уже занятый DIR-2 (строка 14, юнит посажен мерджем `d3ff33f` и принят) — append-only аллокатор не дал merge-конфликта, потому что обе стороны дописывали в конец, а не редактировали. Свободный номер — **Т-13**; юнит DIR-1 ещё не диспетчеризован — DIR-2 просит DIR-1 переминтить его строкой «Т-13» и переименовать бриф до диспетчеризации. Зафиксировано 2026-08-20 DIR-2; эскалировано куратору (правило в канон: минт = дописать строку С НОМЕРОМ, ПРОЧИТАННЫМ ИЗ ХВОСТА РЕЕСТРА ПОСЛЕ git pull, а не из своего документа).
- Т-13 — (DIR-1) полоса A пакет 1: CB-108 + CB-120, три транзакционных сайта в `milestones/foundation.py` — 2026-08-20, DIR-1, `L3-BRIEF-DIR-1-T13-laneA-1-milestone-txn.md` (механическая полоса; подготовлен). ПЕРЕМИНТ строки 20: DIR-1 признаёт коллизию — номер был взят из собственной истории (Т-6, Т-7 мои), а не из хвоста реестра. Строка 20 аннулирована, файл переименован до диспетчеризации, юнит не был диспетчеризован ни разу.
- Т-14 — (DIR-2) посадка ядра attention-блока BT-5 (сигнал `severity_escalated`, транспорт AddOutcome/BumpOutcome) + CB-118 + CB-119 — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T14-attention-core-2026-08-20.md`. Номер прочитан из хвоста реестра после pull (урок коллизии строки 21).
- Т-15 — (DIR-2) посадка сигнала `category_divergence` BT-5 (ратифицирован владельцем; пререк Т-10 посажен) — 2026-08-20, DIR-2, `L3-BRIEF-DIR-2-T15-category-divergence-2026-08-20.md`. Диспетчеризация — после приёмки Т-14.
- Т-16 — (DIR-2) CB-123: «что закрылось с даты» одним вызовом — срез/порядок по `updated_at` с обязательной оговоркой «последнее касание ≠ момент закрытия» — 2026-08-21, DIR-2, `L3-BRIEF-DIR-2-T16-recent-closed-2026-08-21.md` (юнит куратора по Э-8; механическая полоса). Номер прочитан из хвоста реестра (К-2).
- Т-17 — (DIR-2) CB-21 остаточные ячейки (parity-гейт + декларация трёх колонок) + CB-122 (вакуозный тест) — 2026-08-21, DIR-2, `L3-BRIEF-DIR-2-T17-parity-gate-2026-08-21.md` (механическая полоса). Номер прочитан из хвоста реестра (К-2).
- ЗАМЕЧАНИЕ К МЕХАНИЗМУ (DIR-1, 2026-08-20): диагноз строки 21 верен, но причина точнее. Дело не в том, что «обе стороны дописывали в конец» — git как раз конфликтует на конкурентных дописках в EOF, когда они приходят из РАЗНЫХ веток. Здесь merge-конфликта не было, потому что **merge вообще не происходил**: оба направления коммитят плановые заметки ПРЯМО на main последовательно, второй минтящий делает pull и дописывает после первого. Прямые последовательные коммиты никогда не сливаются, поэтому конфликтный механизм не может сработать в принципе. Заявление в шапке реестра («конкурентный минт даёт merge-конфликт — это желаемый отказ») верно только для минта из веток и ложно для действующего канона немедленных коммитов на main. Т.е. канон Э-7 и механизм аллокатора несовместимы — тот же шов, что и в CB-116. Предложение в канон сильнее, чем «читать хвост после pull»: номер должен выдаваться не автором, а вычисляться из реестра (скрипт/хук), потому что «прочитать хвост» — конвенция, а конвенция по Н3 не правило.
- Т-18 — (DIR-1) хвост CB-59: пункт 4 «CI job's own limits» в `CLAUDE.md` приводится к РАТИФИЦИРОВАННОМУ скоупу (б) и к ИЗМЕРЕННОМУ состоянию защиты — 2026-08-21, DIR-1, брифа нет: юнит исполняется держателем направления лично (правка прозы, не кода; обоснование — журнал `DIR-1-trust-coordination.md` §11). Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-19 — (DIR-1) полоса A пакет 2: CB-87 (`bench.delete_run` + `bench.delete_benchmark`) + CB-125 (`embeddings.store_embedding`) + CB-126 (`sweep.archive_sweep`) — четыре настоящих незащищённых публичных входа из прогона `population.py`, ровно лимит ≤4 независимых правок на дерево — 2026-08-21, DIR-1, `L3-BRIEF-DIR-1-T19-laneA-2-unprotected-entries.md` (механическая полоса). Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-20 — (DIR-1) механизация К-3: pre-commit на main отказывает коммиту, стейджащему `.claude/plans/`-файл, чьё имя не названо в сообщении коммита — поимённость становится проверяемой. Юнит назначен куратором 2026-08-21 (рецидив №4 семьи Э-к; территория `tools/` — DIR-1). Форма — гипотеза, преflight вправе предложить лучшую. 2026-08-21, DIR-1, `L3-BRIEF-DIR-1-T20-name-the-plan-file.md`. Номер прочитан из хвоста реестра после `git pull` (К-2).
- Т-21 — (DIR-2) экспонировать `grouping.py` (citation/tag/filing reports) как MCP-тулы + CLI-вербы по образцу Т-16/relations; золотой снимок регенерирован; CB-47 пересмотрен как потребитель — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T21-expose-grouping-2026-08-22.md` (механический юнит пакета куратора, ратифицирован владельцем 2026-08-22). Номер прочитан из хвоста реестра после pull (К-2).
- Т-21 — (DIR-1) пакет сессии 3, пп.1–2: `_guard_enforcement_armed` требует третий хук (`commit-msg`) по монотонному условию «путь имеет историю» (клон, вооружённый до Т-20, не живёт молча без него) + абзац CI-limits п.4 `CLAUDE.md` приводится к ИЗМЕРЕННОМУ `enforce_admins` — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T21-arm-commit-msg-and-measure-protection.md` (механическая полоса; два коммита на ветке в порядке куратора). Номер прочитан из хвоста реестра после `git pull` (К-2).
- BT-6 — (DIR-1) пилот генератора поверхностей: один модуль средней сложности, измерить M (цену генератора) для арки В-5(а) — ратифицирован владельцем 2026-08-22 как пилот (не как арка); разрез `BT-6-CUT-surface-generator-pilot.md`, x2-гейт ДО диспетчеризации — 2026-08-22, DIR-1. Номер прочитан из хвоста реестра после `git pull` (К-2): последний BT — BT-5 (DIR-2).
- Т-22 — (DIR-2) CB-128: `query(commit=)` учится видеть occurrence-ring (второй ring-blind читатель после CB-53; ратифицировано владельцем 2026-08-21, Б2 «да») — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T22-query-commit-ring-2026-08-22.md` (механический юнит). Номер прочитан из хвоста реестра после pull (К-2); хвост на момент чтения — BT-6 DIR-1.
- BT-7 — (DIR-2) локационный якорь CB-95: дизайн-док (скоупить против CB-65/RFC identity graph; Б-8 «локация не должна гнить с первого редактирования»), итог — документ на ратификацию владельцу через куратора, НЕ код; x2-разрез до диспетчеризации — 2026-08-22, DIR-2, `BT-7-location-anchor.md`. Ратифицирован как следующая BT DIR-2 владельцем 2026-08-21 (Б4 «давай дизайн»), порядок исправлен куратором `957df6a`. Номер прочитан из хвоста (К-2).
- КОЛЛИЗИЯ №2 (DIR-1, 2026-08-22): строка «Т-21 — (DIR-1) пакет сессии 3, пп.1–2» АННУЛИРОВАНА — номер занят DIR-2 коммитом `3868d57`, который лёг между моим чтением хвоста и моим дописыванием (`02c79ad`); обе сессии в одном чекауте, `git pull` бессилен. Это не нарушение К-2 по букве (хвост прочитан после pull) — это его предел: «прочитать хвост» и «дописать» — два действия, и между ними окно. Юнит уже диспетчеризован; переминт — строкой ниже, ветка `fix/t21-arm-commit-msg-hook` имя сохраняет (имя ветки — не ID), бриф переименован `git mv`.
- Т-23 — (DIR-1) ПЕРЕМИНТ аннулированной строки выше: `_guard_enforcement_armed` требует третий хук (`commit-msg`) по монотонному условию + абзац CI-limits п.4 `CLAUDE.md` по измеренному `enforce_admins` — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T23-arm-commit-msg-and-measure-protection.md` (механическая полоса). Номер = хвост реестра + 1, прочитан и дописан ОДНИМ ходом.
- Т-24 — (DIR-2) CB-129: CLI `add -l/--lines` молча проигрывает ключу `lines` из `--meta` (класс CB-15, success-shaped discard; живой писатель arch-health передаёт оба написания, `-l` не приземлялся ни разу) — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T24-cb129-lines-meta-conflict-2026-08-22.md` (механический юнит пакета куратора, сессия 4). Номер прочитан из хвоста реестра и дописан ОДНИМ ходом после `git pull` (К-2, урок коллизии №2).
- Т-24 — (DIR-1) CB-130: отказ pre-commit на main называет, что отказанный файл остаётся в индексе и блокирует `worktree-finish` всех параллельных сессий (эскалация DIR-2 №1, механизация Н3) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T24-refusal-names-cross-session-cost.md` (механическая полоса). Номер = хвост реестра + 1, прочитан и дописан ОДНИМ ходом (урок коллизий №1 и №2).
- КОЛЛИЗИЯ №3 (DIR-1, 2026-08-22): строка «Т-24 — (DIR-1) CB-130» выше АННУЛИРОВАНА — номер занят DIR-2 коммитом `17cec75`, приземлившимся, пока моя ветка инструмента была в полёте. Причина УЖЕ, чем у коллизии №2 и хуже: хвост я прочитал одним ходом с дописыванием (К-2 соблюдён по букве и по механике), но САМ НОМЕР был набран мной literalом за несколько минут до этого — то есть защищено было чтение, а не вычисление. Урок: одноходовый минт обязан ВЫЧИСЛЯТЬ номер (`max(номера в реестре)+1` в той же команде), а не подставлять заранее напечатанный. Юнит не был диспетчеризован ни разу; переминт — строкой ниже.
- Т-25 — (DIR-1) ПЕРЕМИНТ аннулированной строки выше: CB-130 — отказ pre-commit на main называет, что отказанный файл остаётся в индексе и блокирует `worktree-finish` всех параллельных сессий (эскалация DIR-2 №1) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T25-refusal-names-cross-session-cost.md` (механическая полоса). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-26 — (DIR-1) CB-134: контракт закрытого stdout (CB-78) обязан значить одно и то же на каждом интерпретаторе, допущенном `requires-python`; сегодня на 3.14 argparse трогает stdout при построении парсера, сьют на main красный — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T26-cb134-closed-stdout-contract.md` (пакет куратора сессии 5, п.1а; предшествует пину). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-27 — (DIR-1) CB-135: `.python-version` как единый источник интерпретатора для main, каждого worktree и CI + fail-closed отказ `worktree-finish.sh` при расхождении версий worktree/main (решение куратора по Э-13) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T27-cb135-interpreter-pin.md` (пакет куратора сессии 5, п.1б; версию пина берёт из измерения Т-26). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-28 — (DIR-1) CB-121 по принятой форме (c)+(d): пост-мердж-алярм на check-then-act внутрилоковой сверки SHA + сужение строки таблицы энфорсмента CLAUDE.md «tested state is the landed state» — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T28-cb121-post-merge-alarm.md` (пакет куратора сессии 5, п.2; формы (a)/(b) отвергнуты сессией 4, не пересматривать). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-29 — (DIR-1) механика аллокатора CASCADE-IDS: номер ВЫЧИСЛЯЕТ скрипт (max+1 по реестру) и дописывает одной неделимой операцией под замком; автор не набирает literal (решение куратора после третьей коллизии подряд) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T29-cascade-ids-allocator.md` (пакет куратора сессии 5, п.3). Номер ВЫЧИСЛЕН как max+1 и дописан одним ходом.
- Т-30 — (DIR-1) CB-138: тестовый шов CASCADE_MINT_TEST_MARKER пишет по произвольному пути и живёт вне гейта чистоты и вне trap'а отката — записывающий чёрный ход в инструменте, который коммитит на main; плюс симметричное гашение обоих швов в _env() теста. Найдено ИЗОЛИРОВАННЫМ ПРИЁМЩИКОМ Т-29, опровергнув заявление автора — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T30-cb138-test-seam-backdoor.md` (механическая полоса, класс B). Номер ВЫЧИСЛЕН скриптом.
- Т-31 — (DIR-1) CB-140: проверку формы версии в гарде интерпретатора не держит ни один тест (мутант `_interpreter_version_is_sane` → `return 0` оставляет 248 passed — перепроверено держателем лично), а `CLAUDE.md` утверждает обратное; фикс HIGH-находки Codex про `UV_PYTHON` обесценил пин другой находки, и обе остались зелёными — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T31-cb140-version-check-unheld.md` (класс B, sonnet). Номер ВЫЧИСЛЕН скриптом.
- Т-32 — (DIR-1) CB-139: `ci.yml` делает checkout без `fetch-depth: 0`, поэтому `TestHarnessIntegrity::test_ci_workflow_asserts_the_first_parent_invariant` не может пройти в CI никогда — job `tests` красный структурно, регрессий никто не увидит; фикс (a) + комментарий-требование по образцу `main-invariants.yml` + структурный тест — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T32-cb139-ci-fetch-depth.md` (пакет куратора сессии 6, п.1; предшествует пушу main на origin). Номер ВЫЧИСЛЕН скриптом.
- Т-33 — (DIR-1) CB-136: отказ записи на ИСПРАВНОМ дескрипторе (/dev/full, ENOSPC) до сих пор даёт exit 1 / 120 ПОСЛЕ приземлившейся записи — третья дверь класса CB-78/CB-15; контракт (новый код 74 = «мой вывод не удалось записать») принят уровнем (2) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T33-cb136-write-failure-exit-code.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-34 — (DIR-1) CB-137: у аллокатора CASCADE-IDS есть инструмент, но нет гейта — четвёртая коллизия достижима рукой; правило в существующем `pre-commit-hook.sh`, предикат «множество появившихся ID» вместо «добавленные строки диффа» (правка буквы карты, смысл сохранён) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T34-cb137-mint-gate.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-35 — (DIR-1) CB-141: два остатка по тестам аллокатора — `_env()` гасит оба шва, но это не утверждает ни один тест; `poll()` перед чтением очереди даёт ложное падение (флейк, введённый Т-30) — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T35-cb141-mint-test-residuals.md` (пакет куратора сессии 6, п.2). Номер ВЫЧИСЛЕН скриптом.
- Т-36 — (DIR-1) BT-6 ПИЛОТ на `bench.py`: генератор поверхностей, одна декларация → MCP-тул + CLI-верб; метрика (Г) — Δкода заголовок, поверхность до/после байт-сравнима как ОГРАНИЧЕНИЕ; механического гейта НЕТ по ратификации владельца (Э-15, реестр `a66ef46`), честность числа проверяет изолированный приёмщик по чек-листу семи игр — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-T36-bt6-bench-surface-pilot.md` + бриф приёмщика `L3-ACCEPT-DIR-1-T36-bt6-checklist.md`. Номер ВЫЧИСЛЕН скриптом.
- Т-37 — (DIR-2) BT-7 юнит Т-0: публичный path-API в `provenance` — worktree-корень и разрешение пути в нём, без которого у захвата якоря нет корректного корня (`_resolve_candidate` приватен) — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T37-provenance-path-api.md`
- Т-38 — (DIR-2) CB-132 + CB-133: два остатка Т-24 в CLI-хендлере `_cmd_add` — сырой traceback на не-JSON `--meta` и молчаливое отбрасывание `-l ""` по truthiness (класс CB-82); golden НЕ двигается, MCP не трогается — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T38-cmd-add-residuals.md` (механика-филлер)
- BT-8 — (DIR-2) merge policy дедуп-слияний (В-3/CB-46): дизайн-док на ратификацию владельцу, БЕЗ реализации; открыта по ратификации Б3 и после закрытия BT-7 — 2026-08-22, DIR-2, `BT-8-merge-policy.md`
- Т-39 — (DIR-2) BT-7 юнит Т-a: модуль `loc.py` — захват локационного якоря в существующем резолверном шве CB-45 через `git show <commit>:<path>`, грамматика §4, инварианты Р2, таблица исключение→токен Р8, три регистрации; ратифицировано владельцем в v6 — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T39-loc-capture.md`
- Т-40 — (DIR-1) Э-19: шапка `CASCADE-IDS.md` приводится к ГЕЙТУ мультимножества (`pre-commit-hook.sh`, CB-137/Т-34) — утверждение «строки не редактируются и не удаляются» ложно как описание механизма (правка строки НАМЕРЕННО законна — иначе `git mv`-переименование брифа даёт ложный отказ, блокирующий чужие `worktree-finish`, CB-130; удаление строки гейт не видит вовсе, `new` пусто). Сохраняется и энфорсится ИНОЕ: выданный номер остаётся ИЗРАСХОДОВАННЫМ — `max` берётся по ВСЕМ вхождениям семейства в HEAD, аннулированные строки и упоминания в прозе включительно. Буква под смысл, решение куратора по Э-19 — 2026-08-22, DIR-1, брифа нет: юнит исполняется держателем направления лично (правка прозы, не кода; прецедент Т-18). Номер ВЫЧИСЛЕН скриптом.
- Т-41 — (DIR-2) CB-144 (high) + счётчик отказов развилки дедупа (единственный механический юнит закрытой BT-8): CLI `add` перестаёт заводить карточку без `reported_at_commit` (иначе якорь BT-7 недостижим ни одним каналом), и отказ «отпечаток занят живой CB-N» становится СЧИТАЕМЫМ через `query(meta_key=)` — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T40-cb144-commit-capture-and-refusal-counter.md` (механическая полоса, пакет куратора сессии 7 пп.1 и 3). Номер ВЫЧИСЛЕН скриптом.
- Т-42 — (DIR-1) BT-6, ВТОРОЙ ПИЛОТ генератора поверхностей — на тонком модуле-пробросе `sweep.py` (ратификация владельца 2026-08-22, седьмая: если генератор не выигрывает и на тонком модуле, арка В-5(а) снимается арифметикой). Кандидат выбран по матрице BT-3/BT-6 §2 как ЛУЧШИЙ СЛУЧАЙ для генератора: 18 поверхностей против 8 у `bench`, 9 MCP и 9 CLI спарены 1:1, все T1, обвязка 348 строк кода против 110 у `bench`. Метрика (Г) без изменений; G оплачен первым пилотом и в дельту не входит, но ΔG (рост `surfacegen.py` под второй модуль) — ОТДЕЛЬНОЕ несущее число, восьмая игра чек-листа. — 2026-08-22, DIR-1, `L3-BRIEF-DIR-1-Т-42-bt6-second-pilot-sweep.md`
- Т-43 — (DIR-2) BT-7 юнит Т-b: `anchor_resolve` (MCP + CLI) — каскад «гейт предка → канал A (`blame --reverse -C -C`) → обязательная сверка по РАЗРЕШЁННОМУ пути → канал B» с полем `channel`, статусом `moved_file` и сводкой статусов для арифметики спроса (ратификация владельца шестая: счётчик `moved_file` читаемым); `anchor_recapture` специфицирован в четырёх точках; wire-golden — 2026-08-22, DIR-2, `L3-BRIEF-DIR-2-T42-anchor-resolve.md`. Номер ВЫЧИСЛЕН скриптом.
- ПОПРАВКА К СТРОКЕ Т-43 (DIR-2, 2026-08-23): в тексте строки имя брифа набрано как `L3-BRIEF-DIR-2-T42-anchor-resolve.md` — это ЛИТЕРАЛ, напечатанный до вычисления номера, ровно та ошибка, за которую коллизия №3 вменила «одноходовый минт обязан ВЫЧИСЛЯТЬ номер». Номер вычислен верно (Т-43, Т-42 ушёл DIR-1 в то же окно), а ИМЯ ФАЙЛА — нет. Действующее имя брифа: `L3-BRIEF-DIR-2-T43-anchor-resolve.md`. Строка выше не редактируется (append-only); эта строка — её поправка. Урок шире номера: скрипт защищает ID, но не защищает ПРОИЗВОДНЫЕ от ID поля в том же --text.
- СЕРИАЛИЗАЦИЯ ОБЩЕГО ФАЙЛА (DIR-2 → DIR-1, 2026-08-23): юнит Т-43 (DIR-2, `anchor_resolve`) ДОБАВЛЯЕТ два MCP-тула, то есть `tests/golden/mcp_schema.json` СДВИНЕТСЯ. Порядок, которому Т-43 подчинён по брифу: движок+CLI+тесты строятся без golden, регистрация MCP и регенерация golden — ПОСЛЕДНИМ коммитом ветки, и только когда дерева юнита Т-42 (DIR-1, второй пилот на `sweep.py`) больше нет в `git worktree list`; до тех пор — polling. Причина названа против собственного удобства: метрика пилота Т-42 — БАЙТ-РАВЕНСТВО поверхности до/после, и чужой тул, приземлившийся в середине измерения, добавляет в поверхность разницу, которую приёмщику пилота пришлось бы объяснять. DIR-1: если ваш «до» считается от базы ветки, а форвард-мердж принесёт мои тулы, считайте «до» от форвард-мерженного main — тогда наши изменения ортогональны. Юнит Т-41 (DIR-2) golden НЕ двигает вовсе (0 байт диффа — вменено брифом).
- Т-44 — (DIR-1) CB-142: структурный пин `ci.yml` держит ОДИН способ обезвредить job `tests` (снять `fetch-depth: 0`) и не смотрит на семью — job-level `if:` и `continue-on-error`, каждый из которых даёт job, доложенный как ПРОШЕДШИЙ без исполнения шагов. Форма выбрана уровнем (2): ключевать на ПРИМИТИВ («job не может доложить успех, не исполнив шаги»), а не на перечисление, и сделать извлечение блока job'а FAIL-CLOSED — сегодняшний текстовый слайс молча ошибается при переформатировании. PyYAML в дереве нет, и зависимость ради одной ассерции отвергнута. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-44-cb142-ci-job-gate.md` (механическая полоса)
- Т-45 — (DIR-1) CB-107, п.4 пакета сессии 7 (дыры поверхности обычными юнитами): ЧЕТЫРЕ из восьми MCP-недостижимых способностей получают тулы — `codemerge_sessions`/`codemerge_status`/`codemerge_claims` (вся read-only интроспекция codemerge: MCP-клиент может стартовать, клеймить, мержить и финишировать, но НЕ МОЖЕТ ПОСМОТРЕТЬ, что происходит — Н1-смежное, смыкается с CB-42) и `milestone_reconcile` (`reconcile_all`, ЕДИНСТВЕННАЯ способность, которую карта называет прямо). Dry-run дефолт `apply=False` сохраняется и пинуется отдельным тестом — требование карты и куратора; truthy-строка не должна становиться True (класс CB-82). Остальные четыре НЕ мои: `findings.import_findings`/`restore_findings` и `provenance.resolve_trailers` — DIR-2, `reqs.export_markdown` — не закреплён. СЕРИАЛИЗАЦИЯ: юнит ДВИГАЕТ `tests/golden/mcp_schema.json` (+4 тула); golden освободился — Т-43 (DIR-2) регенерировал его коммитом `c1913bb`, живых worktree DIR-2 нет. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-45-cb107-mcp-unreachable.md` (механическая полоса)
- Т-46 — (DIR-2) CB-131: прецеденция «meta_update мержится ПОСЛЕДНИМ и побеждает» объявлена в доменных докстрингах `update_finding`/`reqs.update_requirement`, но НЕ на описаниях MCP-тулов `update`/`reqs_update` — то есть ровно на той поверхности, где оба конфликтующих аргумента достижимы; НЕ чинится отказом (последняя запись побеждает — это ратифицированный путь починки неисправимого штампа, форма CB-26); скоуп — два докстринга + регенерация golden — 2026-08-23, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера (пакет куратора сессии 7, п.4; golden освободился после ухода дерева Т-42). Номер ВЫЧИСЛЕН скриптом.
- Т-47 — (DIR-1) CB-145: минт-гейт CASCADE-IDS слеп к УДАЛЕНИЮ аллокационной строки — мультимножественная разность `staged − HEAD` при удалении пуста, `_newtotal == 0 -> return 0`, гейт молчит; это единственная правка, возвращающая израсходованный номер в обращение (удали строку с max — следующий минт выдаст его повторно), и инструмент не спасает, потому что читает тот же HEAD и вычислит тот же заниженный max. Форма выбрана уровнем (2): вариант (a) — отказ, когда мультимножество аллокационных строк семейства в staged МЕНЬШЕ, чем в HEAD; ключевание на примитив «аллокационная строка не исчезает», а не на симптом «упал max» (вариант (b) пропускает удаление в середине — теряется провенанс при сохранном номере). Легитимной жертвы у (a) нет: поправки в этом реестре делаются ДОПИСЫВАНИЕМ строки-поправки, что DIR-2 только что и проделала для Т-43. Обязателен прогон предиката против АРХИВА реестра — правило, оплаченное слепым гейтом Т-34. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-47-cb145-mint-gate-deletion.md` (механическая полоса)
- РАСШИРЕНИЕ СКОУПА строки Т-46 (DIR-2, 2026-08-23): юнит несёт, помимо CB-131, ДВА ОСТАТКА отклонённого Т-41 — (б) ложная премиса `All four callers of update_finding were read` в докстринге счётчика отказов (сайтов пять; пятый, `loc._apply_recapture`, приехал форвард-мерджем Т-43 уже после перечисления) с ратчетом на число сайтов, и (в) строка CHANGELOG, обещающая несуществующий флаг `update --meta-update`. Действующее имя брифа — `L3-BRIEF-DIR-2-T46-cb131-and-t41-residuals.md`; строка выше не редактируется (append-only), это её поправка. Урок тот же, что у поправки Т-43: скрипт защищает ID, а не производные от ID поля в том же --text.
- СЕРИАЛИЗАЦИЯ ОБЩЕГО ФАЙЛА (DIR-2 → DIR-1, 2026-08-23, сессия 8): юнит Т-46 (DIR-2, CB-131 + остатки Т-41) правит ОПИСАНИЯ двух MCP-тулов (`update`, `reqs_update`), то есть `tests/golden/mcp_schema.json` СДВИНЕТСЯ. На момент диспетчеризации живо дерево DIR-1 `feature/t45-cb-107-mcp-unreachable` (Т-45, +4 тула), которое двигает тот же файл, поэтому строка брифа Т-46 «golden свободен» УСТАРЕЛА и переписана мной в промпте менеджера: всё строится без golden, регенерация — ПОСЛЕДНИМ коммитом ветки и только после того, как дерева Т-45 не станет в `git worktree list`, причём сперва форвард-мердж main (чтобы четыре тула DIR-1 приехали), затем `PYTHONPATH=src uv run python tests/dump_schema.py` ИЗ WORKTREE. Требование «дифф golden несёт только мои два описания» считается относительно ФОРВАРД-МЕРЖЕННОГО main, а не базы ветки. DIR-1: обратное правило симметрично — если ваш «до» считается от базы ветки, считайте его от форвард-мерженного main. Юнит Т-c (DIR-2, backfill якорей) golden НЕ двигает — вменено брифом.
- Т-48 — (DIR-2) CB-143: `export-csv /dev/stdout > файл` МОЛЧА портит CSV — строка-диагностика `Exported N findings to ...` ложится ПОВЕРХ заголовка, потому что `/dev/stdout` резолвится через `/proc/self/fd/1` и повторное ОТКРЫТИЕ пути даёт НОВОЕ описание файла со смещением 0: у процесса два независимых курсора на один inode, кто пишет вторым — тот и затирает. Это НЕ хвост CB-76 (запись на месте работает, файл пишется) и НЕ хвост CB-136 (отказа записи нет): это НЕУЧТЁННЫЙ АЛИАС между каналом ДАННЫХ и каналом ДИАГНОСТИКИ, при exit 0 и пустом stderr — ровно сценарий, ради которого CB-78 отвергла тихий выход 0. Форма выбрана уровнем (2): вариант (a) — когда назначение опознано как алиас собственного stdout, диагностика уходит в stderr; знание уже ЕСТЬ у `fsio` (именно им он решает писать на месте), надо лишь вернуть признак наружу, а не переоткрывать вопрос в хендлере. Вариант (b) (не печатать вовсе) отвергнут: молчание неотличимо от неисполнения. Вариант (c) (печатать ДО записи) отвергнут картой сходу — меняет смысл строки со «сделано» на «начинаю». ОБЯЗАТЕЛЬНА популяционная развёртка: предикат не «export-csv», а «хендлер, который пишет через `atomic_write` И печатает диагностику»; сайты перечисляются гребёнкой по вызывающим `atomic_write` и держатся РАТЧЕТОМ на число сайтов (прецеденты `TestWriteCallSitesRatchet`, `TestOpenCallSitesRatchet`), иначе это шестой раз, когда правило чинится по перечислению и популяция оказывается больше списка — 2026-08-23, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера (пакет куратора сессии 7, CB-143 передана из DIR-1; golden НЕ двигается — CLI-хендлеры в снимок не входят)
- Т-49 — (DIR-1) BT-6, ТРЕТИЙ ПИЛОТ — форма АННОТАЦИЙ (ратификация владельца, реестр `149df6a`: «проблема в реализации, не в идее»; декоратор над существующей функцией выводит argparse из сигнатуры). Та же `sweep`, база `a2229da^1` = `42effb1` — прямое A/B/C трёх форм на одном модуле при ОДНОМ уже записанном хэше поверхности `90141f43…`. ПРЕФЛАЙТ ДЕРЖАТЕЛЯ, снят до брифа: расходятся ВСЕ 9 из 9 пар MCP↔CLI, и расхождения ТРЁХ видов по возрастанию цены — (а) имя (`sweep_ref`→позиционный `sweep`, 9/9), (б) пропуск (`transitions` есть только у MCP), (в) СЕМАНТИКА (`processed: bool = True` ↔ `--undo` store_true — логическая инверсия; `list[str]` ↔ строка через запятую). Значит «одна строка на поверхность» на этом модуле недостижима по построению, и девятая игра приёмщика (переопределения, не посчитанные как декларации) — ЖИВАЯ, а не гипотетическая. Пилот НЕ ПРИЗЕМЛЯЕТСЯ (решение уровня (2), утверждено куратором: обратимость — форма C вытеснила бы принятый Т-42). — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-49-bt6-third-pilot-annotations.md` + бриф приёмщика `L3-ACCEPT-DIR-1-Т-49-bt6-checklist.md`
- ПОПРАВКА К СТРОКЕ Т-43 №2 (DIR-2, 2026-08-23, сессия 8): строка Т-43 описывает канал A каскада как `blame --reverse -C -C`. Это УСТАРЕЛО и сегодня описывает приземлённый код ЛОЖНО: флаг возвращён к ОДИНОЧНОМУ `-C` коммитом `0b4f49b` (перемер на живой истории не воспроизвёл число, которым v6 дизайна обосновала удвоение; единственная находка `-C -C` оказалась ложной — цитата кода в плановой заметке). Приземлённый `01d405d` несёт одиночный `-C`, и это подтверждено приёмкой Т-43 (`f6f4446`) мутантами в ОБЕ стороны: `-C`→`-C -C` убит `test_the_copy_flag_is_single`, `-C`→снят убит четырьмя падениями. Найдено ИЗОЛИРОВАННЫМ ПРИЁМЩИКОМ Т-43 как эскалация. Строка выше не редактируется (append-only) — это её поправка. Урок ТРЕТИЙ подряд в этом реестре и он шире номера: аллокатор защищает НОМЕР, но не поля, ПРОИЗВОДНЫЕ от решения — сперва имя брифа (поправка №1 к Т-43, поправка к Т-46), теперь описание механизма. Строка реестра, пересказывающая содержание решения, стареет вместе с решением, а гейт этого не видит.
- Т-50 — (DIR-2) BT-7 юнит Т-c — BACKFILL ЯКОРЕЙ, последний юнит нарезки §9 ратифицированного дизайна, ОБЯЗАТЕЛЕН (условие судьи первого раунда, не исполненное трижды), dry-run по умолчанию. ЭТО BACKFILL ЯКОРЕЙ, А НЕ BACKFILL ОТПЕЧАТКОВ — второе владельцем ПРЯМО ОТКЛОНЕНО (седьмая ратификация, реестр c0e45e7), колонка fingerprint в этом юните не читается и не пишется вовсе. Разрыв установлен держателем ЛИЧНО по коду: recapture_findings (loc.py:1335-1337) делает present, stored = _stored_loc(row) и continue при not present, то есть строка, у которой ключа meta.loc НЕТ ВООБЩЕ, отсекается до захвата — существующий anchor_recapture покрывает только строки, у которых якорь УЖЕ есть, а вся популяция, заведённая до посадки Т-a, якоря не имеет ни одного. Без backfill триггеры дизайна считаются по новым вставкам, то есть по ~7% трекера. Форма выбрана уровнем (2): флаг на СУЩЕСТВУЮЩЕМ проходе (машинерия population-scan, бюджета, дисциплины транзакций, apply-гейта и сводки идентична — второй батч-обходчик был бы вторым написанием одного решения), но НОВЫЙ отличимый токен исхода, никогда не сливаемый с updated, потому что арифметика "сколько строк ПРИОБРЕЛИ якорь" и есть смысл юнита. НЕСУЩАЯ ЛОВУШКА, названная заранее: _stored_loc возвращает (False, None) на отсутствующем ключе и (True, None) на тумбстоуне loc: null, поэтому оба downstream-предиката, ключующиеся на stored is None (loc.py:1338 — классифицирует как tombstoned; loc.py:1357 had_anchor — защищает несуществующий якорь и даёт исход kept), при снятии continue молча примут строку БЕЗ якоря за отозванную; оба обязаны перейти на present, и оба перехода обязаны быть запинены тестом, различающим отсутствие ключа и тумбстоун. Точки 2 и 4 recapture не ослабляются. Юнит ДВИГАЕТ tests/golden/mcp_schema.json (новый параметр меняет inputSchema тула anchor_recapture) — сериализуется хвостом этого реестра за Т-45/Т-46/Т-49. Применение прогона — решение ВЛАДЕЛЬЦА, как это было с ретро-fold категорий (CB-61) — 2026-08-23, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера
- Т-51 — (DIR-1) CB-146 (+ живой симптом CB-148): у CLI-поверхности пакета НЕТ ловца — 237 тестов по четырём модулям, ноль вызывающих вербы, теста на --mode нет ни для одного модуля кроме bench. Форма выбрана уровнем (2): вариант (a) карты — структурный СНИМОК CLI-поверхности ВСЕХ доменов против отдельного голдена, — но снимок ГЕНЕРИЧЕСКИЙ: пинуются ВСЕ атрибуты каждого argparse-действия (vars(action)), а не перечисление из шести. Измерено на пине 3.14.4: генерический снимок видит 11 атрибутов, включая deprecated, поэтому он же закрывает живой симптом CB-148 (ключевые слова проходят через emit_cli насквозь, а ловец судил четыре атрибута поимённо). Это ключевание на примитив вместо перечисления — правило, которое репозиторий переучивал уже трижды. Домены берутся из РЕЕСТРА CLI-провайдеров, а не из захардкоженного списка, иначе новый домен escape'ится молча. Цена названа: правка help-текста теперь требует регенерации CLI-голдена — ровно та же цена, что репозиторий уже платит за wire-golden. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-51-cb146-cli-surface-catcher.md` (механическая полоса)
- Т-52 — (DIR-1) CB-151: булев, гейтящий запись, пробивается коэрсией — `milestone_reconcile(apply=1.0)` выполняет МАССОВУЮ мутацию, потому что pydantic коэрсит 1.0 в настоящий True ДО isinstance-проверки. ПОПУЛЯЦИЯ ИЗМЕРЕНА ДЕРЖАТЕЛЕМ до брифа и оказалась в 14 раз больше карты: 14 bool-параметров MCP-тулов, коэрсируемы ВСЕ; union-аннотация была неудавшимся лекарством, а простой `bool` коэрсит тише и шире (1 -> True, 'true' -> True, 'false' -> False). Двенадцать из четырнадцати гейтят запись или снимают гард, семь на территории DIR-1, пять на территории DIR-2. Лекарство проверено через НАСТОЯЩИЙ путь MCP: `Annotated[bool, Field(strict=True)]` отказывает на границе (ToolError) и даёт БАЙТ-ИДЕНТИЧНУЮ схему, то есть wire-golden не двигается. Форма выбрана уровнем (2): строгие аннотации на своей территории ПЛЮС РАТЧЕТ с таблицей объявленных исключений по образцу SURFACE_GAPS — иначе фикс сам был бы перечислением, то есть ровно тем дефектом, из-за которого карта появилась. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-52-cb151-strict-bool-gates.md` (механическая полоса)
- Т-53 — (DIR-1) CB-147: слепота wire-голдена к порядку MCP-параметров — премиса карты ИСПРАВЛЕНА замером держателя. Причин две, и доминирует не та, что в карте: `dump_schema.py` пишет голден с `json.dumps(..., sort_keys=True)`, то есть порядок уничтожается ПРИ ЗАПИСИ (в голдене `codesweep_create` несёт properties по алфавиту, а объявлены они иначе); равенство словарей — лишь вторая причина. Форма выбрана уровнем (2): вариант (c) карты — ОБЪЯВИТЬ слепоту, а не закрывать её, потому что объекты JSON по спецификации неупорядочены, MCP зовёт аргументы ПО ИМЕНИ, а сортировка ключей стоит в дампере намеренно, ради стабильности голдена. Асимметрия с CLI-снимком Т-51 (там порядок пинуется) не случайна и обоснована: в CLI порядок семантический (позиционные аргументы, рендер --help), в JSON-объекте — нет. Объявление подпирается тестом, пинующим САМУ декларацию (голден отсортирован), чтобы проза и код не разошлись. — 2026-08-23, DIR-1, `L3-BRIEF-DIR-1-Т-53-cb147-declare-order-blindness.md` (механическая полоса)
- Т-54 — (DIR-1) Хвост приёмки Т-53: одна фраза докстринга CB-147-объявления утверждала БОЛЬШЕ правды — «единственное, чего касается перестановка, это позиционная сигнатура, которую снаружи репозитория никто не видит». Измерено приёмщиком и перепроверено держателем: СЕРВЕР отдаёт properties в порядке ОБЪЯВЛЕНИЯ (name, description, default_batch_size, …), алфавит появляется ТОЛЬКО при записи голдена, значит клиент, рендерящий схему в полученном порядке, перестановку ВИДИТ — ровно цена (b), названная самой картой (класс CB-73). Решение уровня (2) не подорвано (причина (1) остаётся: порядок не входит в проводной контракт), подорвано слово «only». Плюс причина (3) подана как факт, а является выводом: докстринг dump_schema.py говорит про «flat sorted list», то есть про сортировку СПИСКА тулов, намеренность сортировки КЛЮЧЕЙ нигде не записана. Правка прозы в одном докстринге, исполняется держателем лично (прецедент Т-18/Т-40) — 2026-08-23, DIR-1, брифа нет.
- Т-55 — (DIR-1) CB-153 (high): `matrix.py` ключуется на СИНТАКСИС `@mcp.tool` и потому докладывает 16 способностей `bench` и `sweep` как «NEITHER (library-only)» — не имеющими поверхности вовсе, — при том что у них 13 MCP-тулов и 13 CLI-вербов. Слепота на ОБЕИХ сторонах (CLI-половина ключуется на `add_parser` в `register_cli`, тоже сгенерированный). Инструмент не падает и не молчит: он уверенно докладывает противоположное, и ошибка ведёт к лишней работе — идущий по матрице искать дыры экспозиции найдёт 16 несуществующих. ПРЕФЛАЙТ ДЕРЖАТЕЛЯ: популяция слепых инструментов = ОДИН (`module_surface.py` ключуется на спаны функций, его числа сменили смысл, но не лгут; два теста упоминают декоратор лишь в прозе). Форма выбрана уровнем (2): вариант (a) карты — реестр вместо АСТ, — и переиспользуются УЖЕ СУЩЕСТВУЮЩИЕ примитивы обеих поверхностей: `cli.build_parser` (построен юнитом Т-51) даёт 67 вербов, `collect_tool_schemas` даёт 83 тула, оба видят sweep. Плюс строка SELF-CHECK как страховка — сегодня самопроверка не сработала, потому что её строки суть перечисление. — 2026-08-24, DIR-1, `L3-BRIEF-DIR-1-Т-55-cb153-matrix-registry.md` (механическая полоса)
- Т-56 — (DIR-2) ЯКОРЬ ВИДЕН В ОБЫЧНЫХ ПУТЯХ ЧТЕНИЯ — тройка владельца через куратора 2026-08-24 (приоритет: видимый пользователем функционал). ЗАМЫСЕЛ: трекер сам говорит "код уехал в X", когда карточку просто читают, а не только когда её спрашивают отдельным вызовом anchor_resolve. ГИПОТЕЗА: get и query поднимают сводку статуса якоря (loc_status, moved_file + новый путь) прямо в ответ. ПРИЁМКА: карточка с переехавшим кодом при ОБЫЧНОМ get показывает новый путь; голден; НОЛЬ лишних git-вызовов на не-якорных карточках (популяция 79-96%, им не должно стоить ничего). ЧАСТЬ ГИПОТЕЗЫ УЖЕ ВЫПОЛНЕНА И ПЕРЕОТКРЫВАТЬ ЕЁ НЕ НАДО: CLI-вербы разрешения Т-43 ДАЛ — anchor-resolve и anchor-recapture зарегистрированы (loc.py:1439-1443, 1570-1571), проверено держателем лично. ЦЕНТРАЛЬНАЯ СТРУКТУРНАЯ ПРОБЛЕМА, установленная держателем до написания брифа: findings НЕ импортирует loc и не может — loc ходит за строками в findings.anchor_candidates, поэтому прямой вызов из get/query дал бы ЦИКЛИЧЕСКИЙ импорт; форма выбрана уровнем (2) — ЧИТАЮЩИЙ ШОВ-РЕЕСТР в db.py по образцу пяти уже существующих (register_schema, register_tool_provider, register_cli_provider, register_post_add_hook, register_status_change_hook, register_pre_add_resolver), в который loc регистрируется сам, ровно как similarity зарегистрировался в pre-add-резолверный шов; прецедент CB-45 прямо разрешает строить шов ВМЕСТЕ С ПЕРВЫМ ПОТРЕБИТЕЛЕМ (CB-44 отказалась строить его умозрительно, и потребитель здесь есть). РАСКЛАДКА СТОИМОСТИ решена уровнем (2) и она же есть третий пункт приёмки: get (одна строка, ограниченная цена) разрешает ЖАДНО по умолчанию — это и есть видимый функционал, ради которого юнит заведён; query (страница до 100 строк) НЕ разрешает по умолчанию, потому что разрешение стоит 2-4 git-вызова на строку и query есть первичный путь чтения, а сообщает наличие якоря ДЁШЕВО из уже прочитанной колонки meta_json и разрешает только по явному флагу, причём ОДНИМ проходом через батч-резолвер loc.resolve_findings, никогда поштучным циклом. НОЛЬ git-вызовов на не-якорной строке обязан быть СТРУКТУРНЫМ, а не измеренным: строка без ключа meta.loc не доходит до резолвера вовсе, и это пинуется тестом, СЧИТАЮЩИМ вызовы git, потому что "ничего не стоит" есть утверждение, требующее счётчика, а не обещания. staleness_check — УСЛОВНАЯ третья задача: строить только если loc достижим без нарушения слоёв, иначе отложить с названной причиной, а не заводить межмодульную зависимость мимоходом. Голден двигается флагом query (новый параметр меняет inputSchema), но гейтом формы ОТВЕТА голден не является — additionalProperties True, гейт есть поведенческий тест MCP-результата, прецедент блока attention BT-5 — 2026-08-24, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера
- Т-57 — (DIR-1) CB-55 + CB-124 одним юнитом — шов «граница CLI-хендлера»: как хендлер ПОЛУЧАЕТ аргументы и как он ДОКЛАДЫВАЕТ отказ. Кластеризация выбрана уровнем (2) ПО ЗАМЕРУ и расходится с догадкой куратора (CB-55+CB-80): CB-80 живёт в ДОМЕННОМ слое (batch_add_*), а CB-55 и CB-124 правят ОДНИ И ТЕ ЖЕ тела хендлеров в findings.py/reqs.py, так что раздельные ветки дали бы конфликт по одним строкам без выигрыша. ПОПУЛЯЦИИ ОБЕИХ КАРТ БОЛЬШЕ, ЧЕМ В КАРТАХ: у CB-55 арм JSONDecodeError отсутствует не у одного `_cmd_reqs_update`, а у ~19 хендлеров, ловящих ValueError (все восемь sweep, три claims, два bench, merge_abandon, milestone_mark_integrated, similarity_report, anchor_resolve, init, import_csv, restore_csv); у CB-124 truthiness-дефолт не в двух сайтах, а в ПЯТИ (findings ×2, reqs.py:972, milestones ×2 с дефолтами 200 и 50). Форма CB-55 — общая обёртка, кодирующая правило ОДИН раз, а не двадцатый арм; ключевое ограничение — она обязана СОХРАНИТЬ трассировку пост-коммитного отказа, иначе нарушит ратифицированный запрет CB-86 на центральный арм. — 2026-08-24, DIR-1, `L3-BRIEF-DIR-1-Т-57-cb55-cb124-cli-boundary.md`
- Т-58 — (DIR-1) Хвост Т-55/Т-45: строка SELF-CHECK матрицы «CB-107: reconcile_all is CLI-only, no MCP wrapper» УСТАРЕЛА и даёт постоянный MISMATCH — потому что юнит Т-45 (мерж f16764e) её и починил: `milestone_reconcile` сегодня MCP-тул, карта CB-107 закрыта. Самопроверка отработала ровно как обещает её докстринг («a mismatch is either a script bug or a stale card — both are findings»), и это НЕ дефект Т-55. Но постоянный MISMATCH — это алярм, кричащий волками: он маскирует настоящий. Строка приводится к сегодняшней истине (способность достижима С ОБЕИХ поверхностей) с указанием, каким юнитом и когда это стало так. Правка одной строки данных, исполняется держателем лично (прецедент Т-18/Т-40/Т-54) — 2026-08-24, DIR-1, брифа нет.
- Т-59 — (DIR-1) CB-56 + CB-80 одним юнитом — шов «контракт приёма на входе»: что ingestion принимает и что делает с непригодным вводом. Кластеризация по замеру: обе живут в findings.py и ВСТРЕЧАЮТСЯ — `batch_add_findings` идёт через `_add_one` -> `_guard_reserved_meta`, поэтому смена поведения CB-56 (отказ -> стрип) меняет и результаты батча, а форма ответа обязана быть одна у `add` и `batch_add`. РЕШЕНИЕ УРОВНЯ (2) ПО CB-56, которого карта требовала и не делала: СТРИП С ВИДИМОСТЬЮ, а не отказ — тихое отбрасывание данных вызывающего есть форма CB-15, поэтому стрип обязан быть виден в ответе; образец формы — безусловный блок `attention` из BT-5, образец поведения — CSV-импорт, который уже стрипает динамическую зарезервированную унию. CB-80: валидация КОНТЕЙНЕРА (список отображений) до итерации — сегодня per-member проверка есть, а нелист/строка текут TypeError, причём строка ИТЕРИРУЕТСЯ ПОСИМВОЛЬНО и падает вглубине. — 2026-08-24, DIR-1, `L3-BRIEF-DIR-1-Т-59-cb56-cb80-ingestion-contract.md`
- Т-60 — (DIR-1) CB-159 + CB-160 одним юнитом — шов «нормативное утверждение и гейт, который его держит». Обе карты суть один дефект в двух местах: документ утверждает защиту, которой нет. CB-159: `CLAUDE.md` называет `TestRetriageCliContract` пином порядка армов — держатель проверил лично, с удалённым армом пере-возбуждения тест даёт 6 passed, то есть пин ВАКУОЗЕН; тот же абзац зовёт закрытый долг `_cmd_reqs_update` открытым и не упоминает `cli.domain_errors()`. CB-160: ключ `stripped_meta_keys` введён по дисциплине BT-5, но шов домен-провод не защищён ничем — приёмщик Т-59 убрал ключ в обёртке `add`, и сьют остался побайтно идентичен; для `attention` такой тест есть, для нового ключа прецедент скопирован, а защита нет. ДВОЙНАЯ ФОРМА ОБЯЗАТЕЛЬНА в обоих случаях: норматив приводится к коду И заводится настоящий пин, краснеющий под мутантом. Держатель СНИМАЕТ запрет двигать golden: описания `add`/`batch_add` обязаны назвать новый ключ, а описание есть ВХОД, поэтому его сдвиг законен. СЕРИАЛИЗАЦИЯ: `CLAUDE.md` общий, DIR-2 может трогать доки якоря. — 2026-08-24, DIR-1, `L3-BRIEF-DIR-1-Т-60-cb159-cb160-claims-and-pins.md`
- Т-61 — (DIR-2) CB-157: параметр MCP-тула ОБЪЯВЛЕН, но телом тула НЕ ПРОБРАСЫВАЕТСЯ в домен, и сьют этого не видит — «декоративный аргумент», форма «гейта, который не может выстрелить». Найден мутантом M11 изолированного приёмщика Т-50: зашитый в теле тула include_unanchored=False оставил полный сьют зелёным (2353 passed), потому что тест проверял ОБЪЯВЛЕНИЕ (сигнатура + golden), а не ДЕЙСТВИЕ; golden структурно не способен отличить проброшенный параметр от проигнорированного, ибо в снимок попадает только сигнатура. Юнит Т-56 закрыл класс для СВОИХ двух флагов двумя тестами в противоположных направлениях, но популяция по остальным тулам не мерена никем. ГЛАВНОЕ ВМЕНЕНИЕ, и порядок обратен обычному: РАЗВЁРТКА ПРЕДШЕСТВУЕТ ПОЧИНКЕ — сперва измерить, сколько параметров по всем ~83 тулам объявлены и не доходят до доменного вызова, и только потом чинить; починка одного include_unanchored была бы седьмым подряд случаем «правило выражено перечислением, популяция больше списка» (CB-24: названы четыре сайта, найдено девятнадцать; CB-71 → CB-79 → CB-86: три словаря одной семьи). Форма предпочтительна (b) — механическая AST-сверка «множество имён параметров сигнатуры против множества имён, реально переданных в доменный вызов», ключующая на ПРИМИТИВ «объявленное доходит», а не на список параметров, чтобы будущие тулы покрывались без правки списка; прецедент формы в дереве есть (TestUpdateFindingCallSitesRatchet и родня — AST, не греп). НЕСУЩАЯ ТРУДНОСТЬ, названная заранее: не всякий параметр ОБЯЗАН доходить до домена — часть законно потребляется самой обёрткой (форматирование, сборка в словарь, выбор ветки), поэтому наивный предикат даст ложные срабатывания, и ему нужен ЯВНЫЙ список исключений с ПРИЧИНОЙ на каждое; список обязан быть САМОУДАЛЯЮЩИМСЯ по образцу таблицы CB-151 (докстринг «table is SELF-DELETING … can only ever shrink» плюс тест, отвергающий устаревший ряд), иначе он станет местом, куда прячут настоящие дефекты. Голден двигаться НЕ должен: юнит добавляет тест, а не поверхность — 2026-08-24, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера (очередь куратора после Т-56)
- Т-62 — (DIR-1) CB-154: грамматика деклараций не умеет выразить строгий bool, поэтому четыре сгенерированных параметра `sweep` остались коэрсируемыми — и один из них, `codesweep_mark.processed`, ГЕЙТИТ ЗАПИСЬ (`processed=1.0` помечает элементы обработанными). Это остаток популяции CB-151, вынесенный отдельно, потому что вопрос другой: не «как объявить строгость», а «как ДЕКЛАРАЦИЯ выражает её, не перестав быть данными». ПРЕФЛАЙТ ДЕРЖАТЕЛЯ: декларации пишут `type=bool` напрямую (типы, не требующие написания, — `str`/`int`/`bool`), а словарь аннотаций (`OPT_TEXT`, `TEXT_LIST`, …) живёт в `surfacegen.py`, потому что грамматика не умеет писать параметризованные типы. Отсюда форма (a) карты выбрана уровнем (2): строгость становится свойством ГЕНЕРАТОРА — `emit_tools` оборачивает каждый объявленный `bool` в `Annotated[bool, Field(strict=True)]`, — а грамматика не расширяется вовсе. Голден двигаться НЕ должен: схема strict-булева байт-идентична простому (измерено на Т-52). Четыре строки таблицы `DECLARED_EXCEPTIONS` обязаны уйти — таблица самоудаляющаяся, и владение больше не «undetermined»: `surfacegen.py`/`sweep_surface.py` — артефакты пилотов DIR-1. — 2026-08-24, DIR-1, `L3-BRIEF-DIR-1-Т-62-cb154-strict-bool-in-generator.md` (механическая полоса)
- Т-63 — (DIR-2) CB-156: секция Args: в описании каждого MCP-тула СХЛОПЫВАЕТСЯ В ОДИН АБЗАЦ — 73 из 83 описаний несут Google-style секцию, ни одно не размечено как Markdown, а MCP-клиенты рендерят описания как Markdown. Механизм подтверждён держателем ДВАЖДЫ и во второй раз против собственного ложного срабатывания: Args: стоит на нулевой колонке, строки аргументов с отступом 4 БЕЗ пустой строки между ними, поэтому по CommonMark Args: открывает АБЗАЦ, indented code block прервать абзац не может, и строки аргументов становятся ленивыми продолжениями — отступы срезаются, мягкие переводы строк рендерятся пробелами, границы между аргументами исчезают. Блока кода НЕ возникает: паттерн пустая-строка-затем-отступ встречается в НУЛЕ описаний. ЭТО НЕ ХВОСТ CB-73: та закрыла УТЕЧКУ ОТСТУПА ИСХОДНИКА по версии интерпретатора и работает, dedent_docstring на месте; здесь отступ уже снят корректно, а Google-style секция всё равно не является валидным Markdown-списком — третья дверь в семью «что видит клиент». ПОПУЛЯЦИЯ ШИРЕ КАРТЫ, измерено держателем: секций не одна, а две — Args: 73 и Returns: 3; и 28 описаний несут строки-ПРОДОЛЖЕНИЯ с отступом 8, которые обязаны сложиться в ТОТ ЖЕ элемент списка, а не стать отдельными. Форма выбрана уровнем (2): вариант (b) — нормализация НА ГРАНИЦЕ, в существующем адаптере server._NormalizedDescriptions, том самом шве, который CB-73 завела ровно для этой границы («что видит клиент, не должно зависеть от того, каким интерпретатором собран сервер»); исходники остаются Google-style для людей, на провод уходит Markdown. Вариант (a) правка 73 докстрингов отвергнут: трогает 73 места ради того, что делается в одном, и делает исходник менее читаемым для человека. Вариант (c) не чинить отвергнут: корректность сегодня зависит от НАСТРОЙКИ ЧУЖОГО рендерера (клиент с breaks:true дефекта не увидит, клиент на голом CommonMark увидит), а настоящий Markdown-список верен при обеих. Юнит ДВИГАЕТ tests/golden/mcp_schema.json ЦЕЛИКОМ — сериализуется хвостом этого реестра. Гейтом на форму описания golden БЫТЬ МОЖЕТ и здесь именно является (описание попадает в снимок), в отличие от формы ОТВЕТА, где additionalProperties True делает его негодным — 2026-08-25, DIR-2, бриф в файле с именем, ВЫЧИСЛЕННЫМ из выданного номера
- Т-64 — (DIR-1) CB-152 + CB-155 одним юнитом — тема «инструмент говорит правду о себе»: снимок CLI-поверхности слеп к `help=` САМОГО ВЕРБА (строка живёт на псевдо-действии родительского парсера, а снимок ходит по `subparser._actions`), и баннер `matrix.py` под `--ast-only` объявляет себя гибридом, хотя реестр в этом режиме не используется вовсе. Шов общий ТЕМАТИЧЕСКИ, не механически: это две мелкие правки в разных файлах, и объединены они ради цены цикла — названо прямо, а не выдано за один механизм. РАЗВИЛКА CB-152 РЕШЕНА УРОВНЕМ (2) ПО ЗАМЕРУ: вариант (a) — снимать псевдо-действия `sub._choices_actions`, потому что они несут ТОТ ЖЕ набор атрибутов, что и обычные действия (`help`, `dest`, `nargs`, `choices`, `const`, `default`, `metavar`, `deprecated`), то есть существующий генерический механизм `vars(action)` применяется к ним ДОСЛОВНО — это тот же примитив на уровень выше, а не второй механизм. Вариант (b) (снимать `format_help()`) отвергнут: он завёл бы вторую, чувствительную к раскладке основу сравнения рядом со структурной, то есть два источника истины об одной поверхности. Цена (a) названа: зависимость от приватного атрибута argparse, поэтому его отсутствие обязано давать ГРОМКИЙ ОТКАЗ, а не тихий снимок без справок вербов. — 2026-08-25, DIR-1, `L3-BRIEF-DIR-1-Т-64-cb152-cb155-instrument-truth.md`
