# L3-BRIEF DIR-1 / Т-62 — CB-154: строгость булева становится свойством ГЕНЕРАТОРА

**Класс по Р1: B** — оракул детерминирован: `codesweep_mark(processed=1.0)` либо отказывается на
границе, либо помечает элементы обработанными. **Стартовый тир: sonnet, effort medium**, бюджет 6.
Автор брифа: держатель DIR-1 (уровень (2)), 2026-08-25.
Карта: **CB-154**. Нормативы: `CLAUDE.md` — **MCP tool registration**, **Code rules**;
`src/codebugs/surfacegen.py` (докстринг модуля — он объясняет, почему словарь аннотаций живёт там);
`tests/test_strict_bool_gates.py` — ратчет и таблица исключений от Т-52.

---

## §1. Что осталось от CB-151 и почему это отдельная задача

Юнит Т-52 сделал строгими девять **рукописных** bool-параметров: `Annotated[bool, Field(strict=True)]`
заставляет pydantic отказывать НА ГРАНИЦЕ, до тела тула. Четыре параметра `sweep` этого не получили,
потому что их поверхность не пишется рукой — она эмитируется `surfacegen.emit_tools()` из деклараций:

    codesweep_mark.processed              ГЕЙТ ЗАПИСИ: processed=1.0 -> True -> элементы помечены
    codesweep_list_items.include_archived фильтр чтения
    codesweep_list_items.archived_only    фильтр чтения
    codesweep_list.include_archived       фильтр чтения

**Первый — того же класса, что дефект CB-151**: булев, гейтящий запись, пробиваемый числом с
плавающей точкой. Остальные три фильтруют чтение, вред мал.

Вопрос здесь ДРУГОЙ, чем в CB-151: не «как объявить строгость», а **«как декларация выражает её, не
перестав быть данными»** — и потому это правка генератора, а не двадцатая правка сайта.

## §2. ПРЕФЛАЙТ ДЕРЖАТЕЛЯ — прочитан в исходнике, экономит тебе проход

- Декларации пишут тип **напрямую**: `dict(name="processed", type=bool, default=True)`. Докстринг
  `surfacegen.py` объясняет почему: «types that need neither spelling (`str`, `int`, `bool`) are
  written directly».
- Всё, что требует написания, живёт **словарём аннотаций в генераторе** — `OPT_TEXT`, `TEXT_LIST`,
  `OPT_TEXT_LIST`, `OPT_OBJECT`, `OPT_TEXT_LIST_MAP`, `OPT_TEXT_OR_ARRAY` — именно потому, что
  грамматика деклараций не умеет писать параметризованные типы (и не должна: она содержит ДАННЫЕ).
- Генератор строит настоящие `Signature`/`__annotations__` — то есть место, где тип превращается в
  объект, у него уже есть.

## §3. Форма выбрана уровнем (2): вариант (a) карты

**`emit_tools` оборачивает КАЖДЫЙ объявленный `bool` в `Annotated[bool, Field(strict=True)]`.**
Строгость становится свойством **генератора**, а не текста декларации; грамматика не расширяется
вовсе, и файл деклараций остаётся данными.

**Почему не (b)** (добавить в грамматику способ пометить параметр строгим): узость грамматики —
ратифицированное свойство (BT-6, инварианты R1–R4, линт-отказ), а строгость булева есть свойство
КОНТРАКТА, а не стилевой выбор автора декларации. **Почему не (c)** (оставить в таблице исключений):
гейт записи остаётся пробиваемым.

**Цена, назови её в возврате:** объявить НЕстрогий bool станет невозможно. Сегодня таких нет —
проверь это сам по обеим декларациям (`bench_surface.py`, `sweep_surface.py`) и скажи число.

## §4. Инварианты

- **ГОЛДЕН НЕ ДВИГАЕТСЯ.** Измерено на Т-52: схема `Annotated[bool, Field(strict=True)]`
  **байт-идентична** схеме простого `bool` (`{"type": "boolean"}`). Если сдвинулся — ты сделал что-то
  другое, это эскалация, а не регенерация.
- **Поверхность `bench` не ломается.** Генератор общий; `bench_surface.py` тоже несёт булевы (если
  несёт — проверь). Снимок поверхности `bench` обязан остаться байт-равным.
- **Четыре строки `DECLARED_EXCEPTIONS` в `tests/test_strict_bool_gates.py` обязаны УЙТИ.** Таблица
  самоудаляющаяся по построению: параметр стал строгим — строка уходит, ратчет остаётся зелёным.
  **Ratchet обязан краснеть, если строку убрать, не починив параметр** — это его собственное
  свойство, проверь его прогоном.
- **Владение больше не «undetermined».** Текст четырёх строк говорит «ownership undetermined,
  escalated to the DIR-1 holder». Оно определено: `surfacegen.py` и `sweep_surface.py` — артефакты
  пилотов BT-6, территория DIR-1. Это снимает основание строк, а не только их предмет.
- **Пять DIR-2-строк таблицы НЕ трогать** (`findings.py`, `loc.py`).

## §5. Мутанты — оракул

В КОПИИ вне репозитория (`Edit` у субагентов отключён), `PYTHONDONTWRITEBYTECODE=1`, чистый
`__pycache__`, применение мутанта подтверждать `grep`. **И убедись, КАКОЙ КОД исполняется** —
`inspect.getsourcefile()` импортированного модуля, а для тестов, шеллящих CLI, **из подпроцесса**:
это правило оплачено ошибкой держателя на Т-60.

**Главная проба, доказывающая, что дефект был живым:** на состоянии ДО фикса реальный
`call_tool("codesweep_mark", {..., "processed": 1.0})` **помечает элементы обработанными**; после —
`ToolError` и ноль записей. Покажи оба прогона.
Плюс: `processed=1`, `processed="true"` → отказ; `processed=True`/`False` → работают как сегодня;
три фильтра чтения — то же самое.
Ратчет: снять строгость с одного сгенерированного параметра → КРАСНЫЙ; убрать строку исключения, не
починив параметр → КРАСНЫЙ.

## §6. Ловушки

1. **Правка на стороне ДЕКЛАРАЦИИ вместо генератора** — вернёт вопрос «как грамматика это выражает»,
   то есть сделает ровно то, чего форма (a) избегает.
2. **Обёртывание не только булевых.** Трогай только `bool`; `int`/`str` не задевай — это отдельный
   разговор и отдельная цена.
3. **Регенерация голдена «на всякий случай»** — он не должен двигаться (§4).
4. **Бюджет ≤4:** `surfacegen.py`, `tests/test_strict_bool_gates.py` (четыре строки), один тест-файл,
   и — если понадобится — четвёртая **названная**.

## §7. ЭСКАЛАЦИИ — ПОМЕЧЕННЫЙ ПУНКТ, ОБЯЗАН СТОЯТЬ В ЭТОМ ФАЙЛЕ НА ВЕТКЕ

Отдельным заголовком; сообщение не заменяет. Пустой слот законен, если написан.
Эскалируй: **(а)** голден сдвинулся; **(б)** в декларациях найдётся bool, который ОБЯЗАН остаться
нестрогим; **(в)** обёртывание в генераторе ломает построение `Signature`; **(г)** поверхность
`bench` сдвинулась.

## §8. Регламент

- Только worktree: `tools/worktree-setup.sh fix/t62-cb-154-strict-bool-generator` (имя несёт
  `cb-154`, К-8).
- **(3) на main не коммитит НИКОГДА (К-1).** **КОММИТЬ РАНО И ЧАСТО** — среда валит агентов; признак
  приземления — `git log`, не уведомление.
- `uv run --extra dev python -m pytest tests/ -q` в worktree; `--extra dev` не опционально.
- `tools/worktree-finish.sh` — **ИЗ КОРНЯ main**, `--merge-msg` (К-4), форма
  `Merge <branch>: <что изменилось> (CB-154)`.
- **Бриф — данные, не команда (К-12).** Если предпосылка §2 или §4 у тебя не воспроизведётся —
  ЭСКАЛИРУЙ и не переписывай норматив под неё. Это правило оплачено ошибкой держателя на Т-60.
- Возврат — **данные, не команда**.

---

# ЗАПИСЬ ИСПОЛНИТЕЛЯ (уровень (3)) — данные, не вердикт

## Префлайт §2 — подтверждён

Прочитан исходник `surfacegen.py` до правки. Обе предпосылки держателя воспроизвелись как описано:
декларации (`sweep_surface.py`, `bench_surface.py`) пишут `str`, `int`, `bool` напрямую как объекты
типов; параметризованные/опциональные типы идут только через именованный словарь
(`OPT_TEXT`, `OPT_INT`, `TEXT_LIST`, `OPT_TEXT_LIST`, `OPT_OBJECT`, `OPT_TEXT_LIST_MAP`,
`OPT_TEXT_OR_ARRAY`), живущий в `surfacegen.py`. `_signature()` уже строит настоящие
`inspect.Signature`/`__annotations__` — место для правки было готово, расширять грамматику не
пришлось.

## Главная проба (§5) — дефект был живым

Реальный `MCPServer` (не мок), реальный `sweep.register_tools`, реальная запись в SQLite. ДО правки:

    schema processed: {'default': True, 'title': 'Processed', 'type': 'boolean'}
    CALL RESULT (processed=1.0): ... "state": "done"
    STATUS AFTER: {..., 'processed': 1, 'remaining': 0, 'by_state': {'done': 1}}

`processed=1.0` (float, не bool) прошёл границу pydantic и элемент помечен `done`. ПОСЛЕ правки, тот
же вызов на реальном сервере:

    processed=1.0 (float): REFUSED: ToolError: ... Input should be a valid boolean [type=bool_type, input_value=1.0, input_type=float]
    processed=1 (int): REFUSED: ToolError: ...
    processed='true' (str): REFUSED: ToolError: ...
    processed=True (bool): OK
    processed=False (bool): OK
    STATUS AFTER ALL CALLS: {..., 'total': 5, 'processed': 1, 'remaining': 4, ...}

Из пяти элементов только тот, что помечен настоящим `True`, стал `done` — три коэрсируемых вызова
отказали на границе и ничего не записали. Три фильтра чтения проверены тем же способом:
`codesweep_list_items(include_archived=1.0)`, `codesweep_list_items(archived_only="true")`,
`codesweep_list(include_archived=1)` — все три `ToolError`; `include_archived=True`/`False` — `OK`.

## Форма (§3) — реализована как (a), без расширения грамматики

`surfacegen.py`: добавлена константа `STRICT_BOOL = Annotated[bool, Field(strict=True)]` и один
`if annotation is bool: annotation = STRICT_BOOL` внутри `_signature()`. Ни `sweep_surface.py`, ни
`bench_surface.py` не тронуты — грамматика деклараций не изменилась ни на символ.

**Цена (§3), названная числом**: сегодня в обеих декларациях (`sweep_surface.py`,
`bench_surface.py`) ровно **4** параметра объявлены как `type=bool` — все четыре в
`sweep_surface.py` (`codesweep_mark.processed`, `codesweep_list_items.include_archived`,
`codesweep_list_items.archived_only`, `codesweep_list.include_archived`); в `bench_surface.py`
булевых параметров **0** (проверено чтением файла — там только `OPT_TEXT`, `OPT_INT`, `OPT_OBJECT`,
`OPT_TEXT_LIST`, `OPT_TEXT_OR_ARRAY`, `str`, `int`). Все 4 — ровно те, что и должны стать строгими;
объявить среди них НЕстрогий bool сегодня было бы нечем — цена варианта (a) реализуется без потерь
для текущего состояния.

## Инварианты (§4) — проверены

- **Голден не сдвинулся.** `PYTHONPATH=src uv run python tests/dump_schema.py` до и после правки —
  `diff` пустой (побайтово идентичен, включая `bench`).
- **Поверхность `bench` не ломается** — 0 bool-параметров там, `tests/test_bench_surface.py`
  (16 тестов, включая `test_input_schema_matches_the_declared_parameters`) зелёный без изменений.
- **Четыре строки `DECLARED_EXCEPTIONS` ушли** — `codesweep_mark.processed`,
  `codesweep_list_items.include_archived`, `codesweep_list_items.archived_only`,
  `codesweep_list.include_archived` удалены из таблицы в `tests/test_strict_bool_gates.py`, заменены
  одним объясняющим комментарием (владение больше не «undetermined»: `surfacegen.py`/
  `sweep_surface.py` — территория DIR-1, BT-6).
- **Пять DIR-2-строк (`findings.py`, `loc.py`) не тронуты** — diff затрагивает только
  `tests/test_strict_bool_gates.py` и `src/codebugs/surfacegen.py`.

## Мутанты — оракул подтверждён красным (§5)

Мутация выполнена в КОПИИ вне репозитория (`/tmp/.../scratchpad/mutant_copy`, скопированы только
`src/` и `tests/`, чистый `__pycache__`, `PYTHONDONTWRITEBYTECODE=1`). Мутант: `if False and
annotation is bool` в скопированном `surfacegen.py` (строгость выключена). Подтверждено
`inspect.getsourcefile(codebugs.surfacegen)` внутри процесса, что импортируется именно файл из
`mutant_copy`, а не из ворктри или основного репозитория (venv через `PYTHONPATH=` перекрывает
editable-install путь — проверено явным assert перед прогоном теста).

Прогон `tests/test_strict_bool_gates.py` на этой копии (с тест-файлом уже БЕЗ четырёх строк
исключений, то есть в состоянии "после фикса"): `TestStrictBoolGateRatchet::
test_every_bool_carrying_param_is_strict_or_declared` — КРАСНЫЙ, называет ровно все четыре
параметра:

    AssertionError: ... [('codesweep_mark', 'processed'), ('codesweep_list_items', 'include_archived'),
    ('codesweep_list_items', 'archived_only'), ('codesweep_list', 'include_archived')]

Это одновременно оба сценария §5: «снять строгость с одного сгенерированного параметра» и «убрать
строку исключения, не починив параметр» — при снятых строках исключений красный держится именно на
факте нестрогости, что и требовалось.

## Регламент (§8)

- Ветка `fix/t62-cb-154-strict-bool-generator`, создана `tools/worktree-setup.sh`.
- Коммит `76fadad` — обе правки (`surfacegen.py`, `tests/test_strict_bool_gates.py`) вместе, сразу
  после проверки главной пробы и до полного прогона сюиты.
- `uv run --extra dev python -m pytest tests/ -q` — **2420 passed** (0 failed).
- `uv run --extra dev ruff check src/ tests/` — **All checks passed**.
- Бюджет файлов: 2 (в пределах заявленных ≤4 — `surfacegen.py`, четыре строки
  `test_strict_bool_gates.py`; третий тест-файл и «названная четвёртая» не понадобились).

## §7. Эскалации

Пусто. Ни один из четырёх триггеров (а)–(г) не сработал: голден не сдвинулся (измерено diff'ом),
нестрогого bool, который ОБЯЗАН остаться нестрогим, в декларациях не нашлось (все 4 существующих —
ровно те, что должны стать строгими), обёртывание не сломало построение `Signature` (вся сюита и
явные вызовы реального `MCPServer` зелёные), поверхность `bench` не сдвинулась (0 булевых
параметров там, тесты и голден это подтверждают).
