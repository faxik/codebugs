# L3-BRIEF DIR-1 / Т-19 — полоса A, пакет 2: четыре незащищённых публичных входа

Бриф уровня (2) → уровню (3). Написан 2026-08-21 держателем направления DIR-1
(«Доверие и координация», ценности Н1/Н3). **Ты приходишь холодным — всё нужное здесь.**

Карты: **CB-87** (два сайта в `bench.py`), **CB-125** (`embeddings.py`), **CB-126** (`sweep.py`).
Ветка: `fix/cb-87-cb-125-cb-126-lane-a-2`. Все три карты названы в имени ветки, поэтому
`worktree-setup.sh` заклеймит каждую сам — **пре-клеймить ничего не надо** (канон Р-2).

---

## §1. Замысел — то, против чего будут принимать. Не задание.

**Функция не должна отчитываться успехом о записи, которая могла не произойти.**

Это ценность Н1 направления дословно (*success-shaped lie*). Все четыре сайта — один и тот же
отказ: между чтением, которое решает «делать ли запись», и самой записью **нет транзакции**,
поэтому в разрыв помещается конкурентный писатель, запись матчит ноль строк, а возвращаемое
значение всё равно утверждает, что всё случилось.

Приёмка будет читать дифф **против этого предложения**, а не против списка правок ниже.

## §2. Правило, которое здесь применяется, — уже РЕШЁННЫЙ закон, не предмет обсуждения

`CLAUDE.md`, раздел **Database**, правило CB-24: значение, вычисленное в Python из только что
прочитанной строки, записывается обратно **внутри ОДНОЙ транзакции**, и эта транзакция владеет
коммитом. Механизм — `db.txn(conn)`, который выдаёт `BEGIN IMMEDIATE`, т.е. берёт **write-lock
ДО чтения**. `busy_timeout` здесь не спасает: он сериализует записи и никогда не трогает
чтение, которое им предшествовало.

**Три следствия CB-24, каждое из которых стоило репозиторию реального отказа** — прочитай их в
`CLAUDE.md` целиком, здесь конспект:
1. **Удали собственный `conn.commit()` функции.** `db.txn` выдаёт `False` при уже открытой
   транзакции вызывающего, и коммит тогда закоммитит **чужую** работу.
2. **Конвертируй/сериализуй возвращаемую строку ПОСЛЕ блока.** Внутри блока `json.loads` на
   битом `meta` откатит запись, которую контракт обещал приземлить.
3. **Однооператорный read-modify-write — не этот случай** (`SET n = n + 1` уже атомарен).

И **следствие (4)**: читай строку **сырой** внутри блока, если хелпер парсит JSON, иначе
следствие (2) невыполнимо.

**Правило `RETURNING`** (`CLAUDE.md`, Database): оператор либо несёт `RETURNING` и читается
`fetch`-ем, либо не несёт и читается `cursor.rowcount`. **Никогда оба** — на `RETURNING`-операторе
`rowcount` равен `0`, пока курсор не исчерпан, т.е. отчитается «ничего не произошло» о
приземлившейся записи. Это строго хуже no-op.

## §3. ЧТО ИМЕННО ЧИНИТЬ — четыре сайта, ровно четыре

Список получен **прогоном** `python3 .claude/plans/bt1-scripts/population.py`, не чтением кода
и не из таблицы в документе. Все четыре в нём — `family=F1`, `dependence=proven`,
`with db.txn: NONE`. Я спот-чекнул каждый лично по исходнику.

**Цитирую ГРЕПАЕМОЕ СОДЕРЖАНИЕ, а не номера строк — номера не переживают форвард-мердж.**

### (1) `bench.delete_run` — `src/codebugs/bench.py`
Читает `SELECT run_id, benchmark FROM codebench_runs WHERE run_id = ?`, затем
`SELECT COUNT(*) as c FROM codebench_results WHERE run_id = ?`, затем удаляет
`DELETE FROM codebench_results WHERE run_id = ?` и `DELETE FROM codebench_runs WHERE run_id = ?`,
закрывает голым `conn.commit()`.
**Вред:** `result_count` прочитан ДО `DELETE` и возвращается уже неверным.

### (2) `bench.delete_benchmark` — `src/codebugs/bench.py`
Читает `SELECT run_id FROM codebench_runs WHERE benchmark = ?`, затем f-string
`SELECT COUNT(*) as c FROM codebench_results WHERE run_id IN (…)`, удаляет f-string
`DELETE FROM codebench_results WHERE run_id IN ( {} )` и
`DELETE FROM codebench_runs WHERE benchmark = ?`, голый `conn.commit()`.
**Вред — ХУЖЕ остальных, это порча данных, а не TOCTOU:** результаты удаляются по **снятому
списку** `run_ids`, а сами прогоны — **безусловно** `WHERE benchmark = ?`. Прогон, вставленный в
разрыв, теряет свою строку и **оставляет осиротевшие результаты**. Ставь мутационную пробу
именно сюда.
*(f-string в SQL здесь — существующая форма для `IN (...)`; она параметризована плейсхолдерами.
Не переписывай её и не трогай `# noqa`, если он там есть — это не твой скоуп.)*

### (3) `embeddings.store_embedding` — `src/codebugs/embeddings.py` (CB-125)
`SELECT * FROM requirements WHERE id = ?` → `KeyError` если нет →
`UPDATE requirements SET embedding = ?, updated_at = ? WHERE id = ?` → голый `conn.commit()` →
`return {..., "stored": True}` **безусловно**.
**Вред:** требование удалено в разрыве → UPDATE матчит 0 строк → `"stored": True`.

### (4) `sweep.archive_sweep` — `src/codebugs/sweep.py` (CB-126)
`sweep_id = _resolve_sweep(conn, sweep_ref)` →
`UPDATE codesweep_sweeps SET status = 'archived', updated_at = ? WHERE sweep_id = ?` → голый
`conn.commit()` → `return {..., "status": "archived"}` **безусловно**.
**Вред:** тот же. Чтение спрятано **за хелпером** — поэтому grep по `SELECT` этот сайт не видит;
это один из пяти helper-hidden чтений, найденных работой по популяции. Помни следствие (4).

## §4. Форма фикса

Для каждого: обернуть тело в `with db.txn(conn):`, **удалить собственный `conn.commit()`**, и
**сделать возвращаемое значение отражением того, что запись реально сделала**, а не утверждением.
Последнее — не косметика: именно оно закрывает Н1. Как именно (rowcount / `RETURNING` /
поднять `KeyError`) — **твоё решение по каждому сайту**, но оно должно быть одинаково
обосновано и не нарушать правило `RETURNING`.

**Изменение наблюдаемого контракта — назови его вслух в возврате.** Если `store_embedding`
начнёт поднимать `KeyError` там, где раньше возвращала `"stored": True`, это изменение
поведения: оно правильное, но оно должно быть в `CHANGELOG.md` и в возврате, а не всплыть у
приёмщика.

## §5. Чего НЕ делать — читай как запреты, они стоили времени

- **НЕ изобретать энфорсмент попутно.** Стоячее указание куратора для всей полосы A: ратчет,
  AST-предикат, обёртка над `commit()`, линт-правило — **ничего из этого**. Вопрос «чем
  механически энфорсить CB-24» — это CB-37, он у владельца **отложен** и решается не здесь.
  Юнит, который приволочёт энфорсмент, будет возвращён.
- **НЕ расширять пакет.** Популяция — 22 сайта; в этом дереве ровно **четыре**. Лимит ≤4
  независимых правок на дерево — это правило приёмки, а не пожелание. Увидишь пятый — **заведи
  наблюдение в возврат, не чини.**
- **НЕ трогать `findings.py`, `similarity.py`, `provenance.py`** — территория DIR-2, и они
  **прямо сейчас** в этих файлах двумя живыми ветками. `findings._add_one` и
  `findings._migrate_statuses` тоже в популяции — **это не твоё**.
- **НЕ трогать `reqs.py`.** `reqs.add_requirement` (CB-117) и `reqs.batch_add_requirements` в
  популяции, но скоуп `reqs.py` не назначен ни одному направлению — отдельный вопрос куратору.
- **Приватные (`_`-префикс) сайты популяции не трогать.** Урок BT-1, купленный дорого: ~три
  четверти «нарушителей» — **паттерн соблюдения**, core-слой, намеренно работающий внутри
  транзакции вызывающего (`db.txn` реентрантен по замыслу; `reconcile.py` документирует это
  дословно). Все четыре твоих сайта — публичные, и это не совпадение.

## §6. Точки сериализации и wire golden

Общие с DIR-2: `types.py`, `tests/golden/mcp_schema.json`, `CLAUDE.md`. **Ничего из этого твой
фикс трогать не обязан.** Но:

- **Если ты правишь докстринг функции, экспонированной как MCP-тул** (`codebench_delete`,
  `codesweep_archive` и т.п.), golden **сдвинется** — SDK берёт `description` из `__doc__`.
  Тогда регенерируй **из worktree с `PYTHONPATH=src`**:
  `PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json`.
  Без `PYTHONPATH=src` голый `python` резолвит `codebugs` через editable-install, смотрящий на
  **main**, и ты снимешь снимок чужого дерева.
- Возвращаемая форма (`dict`) golden **не** двигает: `outputSchema` не снапшотится.
- Перед касанием `CLAUDE.md` — **проверь `claims_list` и ветки DIR-2**. Скорее всего тебе туда
  не надо.

## §7. Тесты — и правило, из-за которого этот раздел длинный

Прогоны **в worktree**, `--extra dev` **не опционален**:
```
uv run --extra dev ruff check src/ tests/
uv run --extra dev python -m pytest tests/ -q
```
**Никогда не проверяй правки worktree прогоном сьюта с main** — `pythonpath = ["src"]`
резолвится относительно того дерева, в котором ты запускаешь, и ты протестируешь чужой исходник.

**Мутационная проба обязательна для каждого из четырёх сайтов, и она ставится В ТОЧКУ
дефекта.** Урок Т-6, стоивший приёмке отдельного разбора: первый мутант был **эквивалентным**
(чтение `rowcount` стояло после `fetchone()`, где значение уже `1`) — проба не могла упасть по
построению и была **неотличима от пройденной**. Проба не в той точке = зелёная по построению.

**Закоммить перед мутационной пробой.** Урок Т-7: `git checkout -- <file>` в пробе снёс
несохранённые правки исполнителя.

**Тест обязан дискриминировать.** Если тест зелёный по обе стороны фикса — он пин сохранённого
поведения, и тогда **подпиши его таковым** в имени или докстринге (правило репозитория). Молчащий
недискриминирующий тест приёмка засчитает как вакуозный.

Для конкурентных тестов прочитай в `CLAUDE.md` раздел **Testing** целиком — там три правила,
каждое куплено провалом: проверяй, что финальное СОСТОЯНИЕ различает; никогда не жди
проигравшего писателя без границы (после фикса он встаёт на `BEGIN IMMEDIATE` навсегда);
чтобы пробить коммит-шов, цепляйся за **оба** (`conn.commit()` и `conn.execute("COMMIT")`).

## §8. Кросс-модельное ревью — форма обязательна, иначе виснет

Цена несоблюдения **измерена: три часа зависшей сессии** на Т-7.

```
timeout 420 codex exec --sandbox read-only '…' < /dev/null
```
- `< /dev/null` и `timeout` — **оба обязательны**, никогда не в фоне.
- В промпте — **явный запрет исследовать репозиторий**; дифф подаётся **ТЕКСТОМ в промпт**, не
  путём; задай лимит объёма ответа.
- **Имя модели из каскадных документов не бери.** `gpt-5.6-codex-sol` отвергается на
  ChatGPT-аккаунте. **Фолбэк на модель по умолчанию разрешён явно.**
- **После ДВУХ таймаутов ты вправе отказаться от кросс-модельной половины — назвав это вслух в
  возврате.** Молчаливый пропуск не принимается; названный принимается.

## §9. Каноны каскада — нарушение проваливает приёмку

- **К-1. На `main` ты не коммитишь НИКОГДА.** Факт своей приёмки пишешь **в этот бриф, на
  ветке**. Строку в документе направления пишу только я и только после своего вердикта.
- **К-3.** Файлы в git добавляешь **поимённо**, никогда каталогом (`git add .claude/plans/`
  однажды затянул чужой артефакт в чужой коммит).
- **К-4. Если ветка начинается с красных тестов (TDD) — `--merge-msg` ОБЯЗАТЕЛЕН**, иначе
  деривация субъекта возьмёт первый коммит первопарентной линии, т.е. «red tests».
- **Факт приёмки пиши НА ВЕТКЕ ДО `worktree-finish.sh`** — чтобы итерация карты осталась **одним**
  мерджем. Два мерджа одной ветки делают неоднозначным вопрос «каким мерджем приземлилась карта»
  и ломают `revert -m 1`.
- Цитируй **грепаемое содержание, а не номер строки.**

## §10. Харнес

```
tools/worktree-setup.sh fix/cb-87-cb-125-cb-126-lane-a-2
# … работа в .worktrees/fix-cb-87-cb-125-cb-126-lane-a-2 …
tools/worktree-finish.sh fix-cb-87-cb-125-cb-126-lane-a-2 'commit msg' \
  --merge-msg 'Merge fix/cb-87-cb-125-cb-126-lane-a-2: четыре незащищённых входа обёрнуты в db.txn (CB-87, CB-125, CB-126)'
```
`worktree-setup.sh` заклеймит CB-87/CB-125/CB-126 из имени ветки. Коды возврата — API:
`3` держит кто-то другой (**фатально**, скажи мне), `4` уже терминальна (предупреждение, идём
дальше), `5` не определено (**повтори тот же вызов один раз** — примитив идемпотентен).

## §11. Критерий приёмки — семь пунктов §13 спеки, плюс замысел

Тебя примут, если: дифф отвечает **замыслу §1** (а не списку §3); каждый из четырёх сайтов несёт
пробу, которая **падает без фикса и проходит с ним**; недискриминирующие тесты **подписаны**
таковыми; собственные `conn.commit()` четырёх функций **исчезли**; возврат каждой функции
отражает произошедшее; правило `RETURNING` не нарушено; попутный энфорсмент **не изобретён**;
пакет **не расширен**; слот эскалаций в возврате **заполнен явно** (пустой — явно пустой).

## §12. Возврат мне

Тройка: **(результат, доказательства, эскалации)**. Слот эскалаций обязателен и может быть явно
пуст. В доказательства включи: вывод `ruff` и `pytest` из **worktree**, по мутанту на сайт с
указанием, **куда** он поставлен и **сколько** тестов уронил, SHA рабочих коммитов и мерджа, и
— если кросс-модельная половина была пропущена — **это словами**.

Отдельной строкой: любой **пятый** сайт, который ты увидел и не стал чинить.
