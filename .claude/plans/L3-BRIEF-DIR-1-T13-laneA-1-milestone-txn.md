# L3-BRIEF Т-13 — полоса A, пакет 1: три транзакционных сайта в `milestones/foundation.py`

Уровень (2) → (3). 2026-08-20. Направление **DIR-1**. **Полоса: МЕХАНИЧЕСКАЯ.**
**СТАТУС: ПОДГОТОВЛЕН, НЕ ДИСПЕТЧЕРИЗОВАН** — WIP-лимит (3) допускает один
диспетчеризованный юнит плюс один подготовленный; сейчас диспетчеризован Т-7 (CB-116).
Диспетчеризуется, как только освободится слот.

## §1. Юнит и почему именно эта тройка

**Карты: CB-108 + CB-120.** Три функции в ОДНОМ файле `src/codebugs/milestones/foundation.py`:

| Функция | commit | Семьи дефекта | Карта |
|---|---|---|---|
| `create_milestone` | `:57` | F1 check-then-act + F2 пост-коммитное перечитывание (`:58`) | CB-120 |
| `update_milestone` | `:92` | F1 (устаревший `from_state` в audit-INSERT) + F2 (`:93`) | CB-108 |
| `add_milestone_item` | `:282` | F1 + F2 (`:283`), и F2 здесь возвращает **не ту строку** | CB-120 |

**Три правки — в пределах лимита ≤4 независимых правок на дерево.** Разносить их по разным
деревьям нельзя: один файл, они сериализуются друг о друга и дадут конфликты на ровном месте.

**Ветка: `fix/cb-108-cb-120-milestone-txn`.** Имя обязано нести **ОБА** id. Харнесс выводит
карты для клейма из имени ветки (`tools/worktree-setup.sh:70-74`, `grep -oiE` + `sort -u`,
берёт ВСЕ совпадения), `--items` в этом порту нет — это CB-112. Сократишь имя — вторая карта
молча останется незаклеймлённой.

## §2. Тройка (П-Б)

**Замысел.** Вызов, записавший строку, обязан вернуть ту строку, которую записал, и решение о
записи обязано приниматься под тем же замком, что и запись. Сегодня ни то, ни другое: три
функции коммитят вне транзакции и затем перечитывают результат отдельным запросом.

**Задание-гипотеза.** Единая трансформация на все три: тело в `db.txn(conn)`, собственный
`conn.commit()` удалить, возвращаемое значение строить из `INSERT/UPDATE … RETURNING *`, а не
из перечитывания.

**Критерий приёмки.**
1. Возвращаемое значение каждой из трёх функций — строка, которую записал **этот** вызов.
2. Ни одного `conn.commit()` в этих трёх функциях не осталось.
3. Внешняя транзакция вызывающего переживает свой `ROLLBACK` (функции перестают её коммитить).
4. `update_milestone`: строка `milestone_audit` описывает **фактический** переход, а не
   прочитанный до записи.
5. `add_milestone_item`: возвращается вложение, созданное этим вызовом (см. §3.2).
6. Полный сьют и `ruff` зелёные; мутационная проба на каждый новый/изменённый тест.

## §3. Ловушки — четыре, и три из них уже стоили времени на предыдущих юнитах

**3.1 `RETURNING` и `rowcount` несовместимы.** Как только оператор несёт `RETURNING`, его
`rowcount` равен 0, пока курсор не исчерпан. Любая существующая проверка `rowcount` на этих
операторах обязана стать «курсор не выдал строки». Измерено на CB-111: наивный вариант даёт
отказ на **каждом успешном** вызове.

**3.2 `add_milestone_item` — F2 здесь возвращает не ту строку, и `RETURNING` чинит это даром.**
INSERT ключуется на тройке `(milestone_id, item_kind, item_ref)`, а перечитывание идёт через
`_get_item_by_ref` → `_spine._get_item_row_by_ref`, чей запрос —
`SELECT * FROM milestone_items WHERE item_ref = ? ORDER BY id DESC LIMIT 1` (`_spine.py:100-103`),
то есть по `item_ref` **одному** с разрешением неоднозначности «самый новый id». Множественные
вложения на один `item_ref` легальны по схеме (это CB-33), поэтому функция может вернуть
вложение к ДРУГОЙ вехе. `RETURNING` отдаёт вставленную строку и запрос по `item_ref` просто
исчезает.
**НЕ чинить `_get_item_row_by_ref`, перекладывая его на тройку** — у хелпера есть другие
вызывающие, и его семантика — решение CB-33, не этого юнита.

**3.3 `_audit` обязан оказаться в ТОЙ ЖЕ транзакции**, что и строка, которую он описывает
(`_spine.py:64-79`). Обёртка это даёт сама. Не давать `_audit` собственную транзакцию.

**3.4 `fetchone()` — ВНУТРИ блока `db.txn`.** Вынесешь наружу — умрёт с
`cannot commit transaction - SQL statements in progress` (измерено на CB-111).

И ещё одно, не ловушка, а факт для теста: у `update_milestone` есть no-op-путь
(`:78-80`, `if not updates: return current`), возвращающий значение, прочитанное ДО любой
записи. Под `db.txn` он будет держать write-lock ради одного SELECT — CB-24 эту цену уже
ратифицировала, менять не нужно, но тест не должен на этом спотыкаться.

## §4. Что НЕ делать

- **Не изобретать энфорсмент транзакционного правила попутно.** Прямой запрет куратора для
  всей полосы A. Механизм — предмет BT-1, она сейчас на кросс-модельном гейте.
- **Не трогать остальные сайты популяции** (`bench.py`, `blockers.py`, `sweep.py`,
  `embeddings.py`, `reqs.py`) — они в других пакетах полосы A.
- **Не трогать `reqs.py` вообще**: файл не входит в скоуп ни одного направления, владелец не
  назначен (см. CB-117).
- Не трогать `findings.py` / `similarity.py` / `provenance.py` — DIR-2.

## §5. Сериализация и приёмка

`milestones/` — мой скоуп. **Wire golden трогать не должно** (сигнатуры и докстринги тулов не
меняются) — сдвинулся, значит изменилось видимое клиенту, стоп и доклад. Перед финишем сверься
с `claims_list` и `git worktree list`: DIR-2 работает в `fix/bt4-*`, пересечений по файлам быть
не должно, но проверь.

**`--merge-msg` обязателен** при запуске `worktree-finish.sh`, особенно на ретрае после
exit 13 — интерим-конвенция куратора до посадки CB-116.

Приёмка (3)←(4) — семь пунктов §13, **п.6 n/a с обоснованием**. **П.7 требует терминального
статуса ОБЕИХ карт**, не только той, что первой в имени ветки. Возврат — тройка с обязательным
слотом эскалаций.

---

## §6. Факт приёмки (3)←(4) — записан менеджером Т-13, 2026-08-20

**СТАТУС: ПРИЗЕМЛЁН.** Мердж `53680a6` на main, ветка `fix/cb-108-cb-120-milestone-txn`
(не удалена). Изменены ровно два файла: `src/codebugs/milestones/foundation.py` и
`tests/test_milestones.py`. **Wire golden не сдвинулся** — сигнатуры и докстринги тулов
не менялись, `tests/golden/mcp_schema.json` не в диффе.

Это факт приёмки менеджера уровня (3) над исполнением. **Строку в документе направления
пишет DIR-1 после своего вердикта** (§8.2: самоотчёт «принят 7/7» от менеджера запрещён).

### Семь пунктов §13

1. **Заявленное сделано.** Все три функции — `create_milestone`, `update_milestone`,
   `add_milestone_item` — в `db.txn(conn)`; собственных `conn.commit()` в них не осталось;
   возвращаемое значение строится из `INSERT/UPDATE … RETURNING *`, `fetchone()` — внутри
   блока. Критерии §2.1–§2.5 выполнены, §2.6 — ниже.
2. **Ничего сверх задания.** Энфорсмент транзакционного правила не изобретался (запрет
   куратора, предмет BT-1/CB-37). `_get_item_row_by_ref` не тронут — его правило выборки
   это решение CB-33. Остальные сайты популяции (`bench.py`, `blockers.py`, `sweep.py`,
   `embeddings.py`, `reqs.py`) не тронуты. `findings.py`/`similarity.py`/`provenance.py`
   (скоуп DIR-2) не тронуты.
3. **Тесты и линт.** Полный сьют `1794 passed` в worktree (`uv run --extra dev python -m
   pytest tests/ -q`), `ruff check src/ tests/` — «All checks passed!». Прогон повторён
   харнессом на объединённом дереве в фазе [6/7] финиша.
4. **Мутационная проба — пять мутантов, все в ТОЧКЕ дефекта, все убиты:**
   - **М1** — гард `_milestone_exists` в `create_milestone` вынесен обратно ЗА `db.txn`
     → падает `test_create_milestone_checks_for_a_duplicate_under_the_write_lock`.
   - **М2** — чтение строки в `update_milestone` вынесено обратно ЗА `db.txn` (это в
     точности источник `from_state` для audit-INSERT) → падают ДВА теста: пин на замок
     и оракул аудита.
   - **М3** — возврат заменён обратно на `_get_item_by_ref(conn, item_ref)` (перечитывание
     по `item_ref` одному) → падает `test_add_milestone_item_returns_the_attachment_it_created`.
   - **М4** — восстановлен собственный `conn.commit()` в `create_milestone`.
   - **М5** — `update_milestone` откачена ДОСЛОВНО к пред-фиксному телу → падают пин на
     замок, оракул аудита и тест внешней транзакции. М5 существует именно затем, чтобы
     тест внешней транзакции не оказался вакуумным: он ловит возвращённый `conn.commit()`
     адресно.
5. **Кросс-модельное ревью — БЫЛО.** `timeout 420 codex exec --sandbox read-only … <
   /dev/null`, дифф передан ТЕКСТОМ, в промпте прямой запрет исследовать репозиторий,
   лимит 400 слов. Модель — по умолчанию (имя из документов не бралось). Вердикт:
   «No serious correctness bug or rule violation found». Две пометки «low», обе
   намеренные и обе названы в диффе: (а) разбор `meta_json` теперь ПОСЛЕ блока, поэтому
   `JSONDecodeError` после успешной записи стал достижим — это CB-24 следствие (2),
   ставшее выполнимым, а не регресс; (б) `BEGIN IMMEDIATE` теперь предшествует гардам,
   поэтому под контенцией `OperationalError` может опередить `KeyError`/`ValueError`.
   Порядок исключений в неконтендированном случае сохранён дословно: гарды не поднимались
   выше и не переставлялись.
6. **Проверка на живой системе — n/a, с обоснованием.** Правка не меняет ни одной
   наблюдаемой снаружи поверхности: MCP-обёртки, их сигнатуры и докстринги не тронуты,
   wire golden не сдвинулся, возвращаемые словари содержат те же ключи. Наблюдаемое
   отличие проявляется только под конкуренцией и в чужой открытой транзакции — то есть
   ровно там, где оракулом может быть тест, а не ручной вызов. Дополнительно проверено
   чтением: `create_milestone`/`update_milestone`/`add_milestone_item` достижимы ровно из
   трёх MCP-обёрток в `milestones/__init__.py`, каждая со свежим соединением и без внешней
   транзакции, поэтому на удалённый `conn.commit()` не опирался ни один вызывающий.
7. **Терминальный статус ОБЕИХ карт.** `CB-108` → `fixed`, `CB-120` → `fixed`
   (`mcp__codebugs__update`, каждая с разбором улик в заметке). Клеймы ветки сняты:
   финиш освободил оба, терминальный статус освобождает их же вторым путём.
   `claims_list` перед финишем показывал ровно эти два клейма и ничьих чужих;
   `git worktree list` — параллельное дерево DIR-2 `fix/bt5-category-divergence`,
   пересечений по файлам нет.

### Порядок работы

TDD: красные тесты закоммичены отдельно (`99b5f60`, 6 из 7 падали по нужным причинам —
седьмой намеренно проходит на обеих сторонах и помечен как таковой в докстринге), фикс
вторым коммитом (`c650118`). Мутационная проба запускалась ПОСЛЕ коммита фикса, поэтому
`git checkout --` в пробе не мог снести несохранённые правки. `--merge-msg` передан явно
(канон К-4).

### Эскалации

**Э-1 (процессная, к DIR-1).** Факт приёмки записан в бриф ПОСЛЕ посадки `53680a6`, а не
до неё. Прецедент Т-14 (`9e60520`) кладёт эту запись на ветку ДО финиша, и мердж вносит её
в main одним движением; я landed раньше, поэтому запись приходит вторым мерджем той же
ветки. Канон К-1 при этом не нарушен — на main я не коммичу, — но порядок стоит зафиксировать
в шаблоне брифа явно: «§6 пишется до `worktree-finish.sh`, не после».
