# Бриф юнита Т-144 — CB-318: версионно-честное утверждение о тексте аварии

```
class:   исполнение по решённой форме (уровень (3) каскада планирования)
model:   opus
effort:  medium
branch:  fix/cb-318-version-honest-crash-text
worktree: /home/faxik/w/codebugs/.worktrees/fix-cb-318-version-honest-crash-text
card:    CB-318 (medium, test_fragility) — клеймо держит эта ветка
```

Малый юнит, поручен уровнем (1) отдельно от идущего параллельно Т-143 — предмет другой, а на одну
ветку берётся одна забота. Ветка Т-143 трогает `src/codebugs/reqs.py` и свои тесты; твоя ветка
трогает `tests/test_hand_built_surfaces.py`. Пересечения нет, но перед возвратом убедись сам.

## 1. Что сломано — замерено держателем на `main`, воспроизведи первым делом

Вершина `main` `acb010f`, исходник без единой правки какой-либо ветки:

```
uv run --extra dev python -m pytest tests/test_hand_built_surfaces.py -q                  → 14 passed
uv run --with 'mcp==2.1.1' --extra dev python -m pytest tests/test_hand_built_surfaces.py -q → 3 failed, 11 passed
```

Падают три:

- `TestTheGuardFires::test_one_hand_added_tool_on_an_OTHERWISE_correct_server_is_caught`
- `TestTheGuardFires::test_a_production_built_surface_is_not_refused`
- `TestTheAllowanceTableIsDisciplined::test_a_row_actually_licenses_the_place_it_names`

**Механизм целиком, и его надо понять прежде, чем править.** Каждая из трёх проверок вызывает
инструмент на поверхности, собранной тем или иным способом, ловит исключение и различает две
возможности парой утверждений: `assert "CB-311" not in str(raised.value)` — то есть «это НЕ отказ
сторожа» — и `assert "no connection is ever needed" in str(raised.value)` — то есть «это собственная
авария тела инструмента, дошедшая ко мне текстом». Вторым утверждением проверка и доказывает, что
сторож НЕ сработал там, где не должен.

Под библиотекой `mcp` 2.0.0 SDK приписывает текст любого исключения к своему сообщению, поэтому
текст доходит и утверждение выполняется. Под 2.1.1 SDK показывает клиенту текст только исключений
собственного класса `ToolError`, а всякую иную аварию отдаёт как обезличенное
`Error executing tool query`. Тогда текста нет ни у отказа сторожа, ни у аварии тела, **оба
утверждения перестают что-либо различать**: отрицательное выполняется тривиально, положительное
падает.

**Почему это дорого, а не косметика.** Нога непрерывной сборки `newest-sdk`
(`.github/workflows/ci.yml`, строки 152–176) прогоняет **весь** набор на новейшей версии, какую
допускает диапазон зависимости, — сегодня это 2.1.1. Мерж юнита Т-140 (`6202da4`) уже в
`origin/main`, значит нога красна на `main` прямо сейчас. Пока она красна, ни одно новое падение под
2.1.1 не отличимо от известного: гейт, который не может пройти, прячет те регрессы, ради которых
заведён. Это тот же класс, что карта CB-139.

**Происхождение — предыдущий юнит этого же направления.** Файл создан коммитом `359fe5c` работы по
карте CB-311 (юнит Т-140), утверждения о тексте добавлены коммитами `1de9bff`, `51ba915`, `3c3eec9`
той же работы. Приёмка Т-140 перемеряла набор в собственной копии и получила ноль падений — но под
версией по умолчанию, то есть под 2.0.0. Оракул существовал и громко краснел; его просто не
прогнали на той версии, которая стоит у владельца. Это буквально диагноз, который направление
поставило проекту тремя сменами раньше, воспроизведённый внутри работы, которая его закрывала.

## 2. Форма решения РЕШЕНА уровнем (1) и не является меню

**Три утверждения приводятся к версионно-честной форме: ожидание зависит от установленной версии
библиотеки и ГОВОРИТ ЭТО ВСЛУХ.**

**Чего делать нельзя, и это главная ловушка юнита.** Нельзя ослабить утверждение до «пришло хоть
какое-нибудь исключение» или просто снять положительную половину. Обе половины пары существуют
затем, чтобы отличить «сторож сработал» от «сторож не сработал»; проверка, которая зелена под обеими
версиями потому, что больше ничего не различает, — это гейт, который не может сработать, то есть
ровно тот дефект, ради закрытия которого написан весь этот файл. Приёмка такое отклонит.

**Как именно сделать честно — решаешь ты, но выбор запиши вслух с ценой.** Держателю видны два
пути, и они не равноценны:

1. **Различитель, переживающий обе версии.** Спросить не текст, а что-то, что SDK не стирает: класс
   исключения, цепочку через `__cause__`, поведение на стороне сервера до перевода в ответ клиенту.
   Если такой различитель существует и он честный — это лучший исход, потому что проверка перестаёт
   зависеть от версии вовсе. Проверь это раньше пути 2.
2. **Ожидание, ветвящееся по установленной версии.** Проверка читает версию установленной библиотеки
   и требует текст там, где он обязан быть, и его отсутствие там, где его нет по устройству SDK.
   Это законно, но обязано быть написано так, чтобы читатель понял, ПОЧЕМУ ожидание разное, а не
   принял ветвление за уступку. **Образец уже есть в дереве** — `tests/test_cb310_refusal_text.py`,
   где прямо записано, какие проверки краснеют только под 2.1.1 и почему проверка, зелёная под
   обеими версиями, измеряла бы SDK, а не починку. Прочитай его прежде, чем строить своё.

В обоих случаях причина различия версий пишется словами в коде: SDK показывает клиенту текст только
исключений собственного класса, всё прочее приходит обезличенным. Это не пояснение к коду, а
предмет самой проверки.

## 3. Критерий приёмки

1. **Тест зелён под ОБЕИМИ версиями.** Команды и ожидаемый вывод:
   - `uv run --extra dev python -m pytest tests/test_hand_built_surfaces.py -q` → «14 passed»
     (или больше, если добавишь проверки).
   - под 2.1.1 — тот же файл зелён; способ прогона см. §4.
2. **Полный набор зелён под обеими версиями**, за вычетом известного: под 2.1.1 на `main` сегодня
   падают ровно эти три и больше ничего в этом файле. Если после твоей правки под 2.1.1 падает
   что-то ещё — это твоё, разбирайся.
3. **Сторож по-прежнему различает.** Мутант обязателен и он — сердце приёмки: сломай сторож так,
   чтобы он перестал срабатывать (или срабатывал там, где не должен), и покажи, что твоя
   переписанная проверка краснеет **под обеими версиями**. Проверка, которая после правки краснеет
   только под одной, различает вполовину, и это надо либо починить, либо назвать вслух с причиной.
4. **Причина версионного различия записана словами** в коде проверки.
5. **После посадки** — зелёный прогон ноги `newest-sdk` на GitHub, подтверждённый **журналом шага**,
   а не сводкой «success». Это делает держатель, не ты; но твой отчёт должен дать ему то, с чем
   сверять: состав и число проверок под обеими версиями.

## 4. Как прогнать под версией 2.1.1, не сломав чужую работу

**`uv.lock` и `pyproject.toml` менять НЕЛЬЗЯ** — это файлы направления DIR-3, которое прямо сейчас
ведёт по ним юнит; правка отсюда даст конфликт на посадке.

Два способа, оба годятся, и лучше показать оба:

- **Быстрый, ничего не трогает:** `uv run --with 'mcp==2.1.1' --extra dev python -m pytest … -q`.
- **Как делает нога сборки:** скопируй дерево во временный каталог, в КОПИИ выполни
  `uv lock --upgrade-package mcp`, убедись выводом
  `uv run --extra dev python -c "import importlib.metadata as m; print('mcp', m.version('mcp'))"`,
  какая версия разрешилась, и прогони набор там. Копия выбрасывается. Этот способ ближе к тому, что
  реально делает нога, и потому он — доказательство, а первый — удобство.

Если два способа дадут разный результат — это находка, и она важнее самого юнита; сообщи её сразу.

## 5. Порядок работы

1. Воспроизведи замер §1 прежде всякой правки.
2. Сперва проверь путь 1 из §2 (различитель, не зависящий от версии). Отрицательный результат тоже
   результат — запиши его.
3. Тесты вперёд кода в той мере, в какой это применимо к правке самих тестов: сначала покажи, что
   новая форма проверки краснеет на сломанном стороже, потом убеждайся, что она зелена на целом.
4. Прогоны — только в своём рабочем дереве, `--extra dev` не опционален.
5. Коммить рано и часто; журнал веди в этом файле по ходу.
6. `CHANGELOG.md` — только если меняется наблюдаемое пользователем поведение. Здесь меняются тесты,
   так что, скорее всего, записи не нужно; реши и скажи, что решил.
7. Проход `/simplify-traced` перед возвратом, результат запиши сюда.
8. Ветку НЕ вливай, `worktree-finish.sh` НЕ запускай — посадку делает держатель.

## 6. Чего в этом юните НЕТ

- Правок `src/codebugs/` вообще: предмет — утверждения тестов, а не поведение сервера. Если по ходу
  окажется, что честная проверка требует правки исходника, **остановись и напиши держателю**, не
  расширяй область молча.
- `uv.lock`, `pyproject.toml`, `.github/workflows/`, `README.md`, `tests/test_claude_md_*` — чужие.
- `src/codebugs/reqs.py` и `tests/test_cb316_*` — их ведёт параллельная ветка Т-143.

---

## Журнал исполнения

### Шаг 1. Замер §1 воспроизведён до всякой правки

Рабочее дерево на `033574b`, ни одной правки исходника.

```
uv run --extra dev python -m pytest tests/test_hand_built_surfaces.py -q
    → 14 passed in 0.50s          (установленная версия библиотеки mcp — 2.0.0)

uv run --with 'mcp==2.1.1' --extra dev python -m pytest tests/test_hand_built_surfaces.py -q
    → 3 failed, 11 passed in 0.67s
```

Падают ровно те три проверки, что названы в §1, и падают обе на одном и том же
утверждении — положительной половине пары:

```
E  AssertionError: assert 'no connection is ever needed' in 'Error executing tool query'
E   + where 'Error executing tool query' = str(UnexpectedToolError('Error executing tool query'))
```

### Шаг 2. Путь 1 из §2 проверен ПЕРВЫМ и дал ПОЛОЖИТЕЛЬНЫЙ результат

Различитель, переживающий обе версии, существует, и его нашёл прямой замер, а не
рассуждение. Пробник (`scratchpad/probe_sdk.py`) поднимал исключение внутри тела
инструмента и печатал под каждой версией класс, текст и всю цепочку связанных
исключений (`__cause__` и `__context__`).

| | `mcp` 2.0.0 | `mcp` 2.1.1 |
|---|---|---|
| класс, дошедший до вызывающего | `ToolError` | `UnexpectedToolError` (подкласс `ToolError`) |
| `str(исключение)` | `Error executing tool query: …текст тела…` | `Error executing tool query` |
| `__cause__` | `AssertionError('…no connection is ever needed')` | `AssertionError('…no connection is ever needed')` |
| `__context__` | то же самое | то же самое |

**Обе версии сохраняют исходное исключение тела в цепочке — стирается только текст
у внешней обёртки.** Значит ветвить ожидание по версии не нужно вовсе: достаточно
спрашивать не `str()` внешнего исключения, а всю цепочку. Это лучший из двух исходов,
названных в §2, и путь 2 (ветвление по версии) не понадобился.

**Почему вторая половина пары вообще работала под 2.1.1** — деталь, без которой
механизм не понять. Сторож подменяет `MCPServer.call_tool` целиком и поднимает свой
отказ ДО обращения к настоящему методу SDK (`tests/conftest.py:841` и `:856`, вызов
`_REAL_CALL_TOOL` идёт только строкой ниже). Поэтому отказ сторожа никогда не проходит
через обёртывание SDK и доходит до вызывающего как есть, с меткой `CB-311` в тексте.
Обёртывается только авария ТЕЛА инструмента. Отсюда и наблюдаемая картина: одиннадцать
проверок, которым нужен только отказ сторожа, зелены под обеими версиями, а красны ровно
те три, которым нужен текст аварии тела.

### Шаг 3. Что сделано

Добавлен помощник `_why_the_call_failed(raised)` — он собирает в одну строку тексты
всего ряда связанных исключений: само исключение, затем его `__cause__`, при отсутствии
такового `__context__`, и так далее, с защитой от кольца. Три пары утверждений теперь
спрашивают эту строку вместо `str(внешнее исключение)`. Ветвления по версии нет.

**Ни одна половина пары не снята и не ослаблена.** Отрицательная («метки `CB-311` тут
нет», то есть это не отказ сторожа) и положительная («текст аварии тела дошёл», то есть
вызов добрался до тела) обе на месте; изменился только источник, у которого они
спрашивают, — и он стал ШИРЕ прежнего, а не уже: цепочка содержит текст внешней обёртки
тоже, первым своим звеном.

Причина, по которой читается цепочка, записана словами в объяснении к помощнику: там
названы обе версии, сказано, что именно каждая делает с текстом, и отдельно оговорена
граница — здесь спрашивают «дошёл ли вызов до тела инструмента», а не «сможет ли клиент
прочитать причину»; второй вопрос принадлежит `tests/test_cb310_refusal_text.py`, и
смешение этих двух вопросов и есть то, из-за чего проверка вообще оказалась зависима от
подачи SDK.

В сообщениях обоих утверждений теперь печатается сама цепочка, и в положительном отдельно
названа единственная другая вещь, которую его падение может значить, — SDK, переставший
связывать исключение тела. Без этого будущее падение по такой причине читалось бы как
«вызов не дошёл до тела», то есть как ложный диагноз.

### Шаг 4. Мутанты — по одному на каждую половину, оба направления поломки

Сторож ломался в `tests/conftest.py`, каждый мутант накладывался на чистую копию, дерево
восстанавливалось после каждого прогона (`scratchpad/mutants.sh`). Сокращения:
Т1 — `test_one_hand_added_tool_on_an_OTHERWISE_correct_server_is_caught`,
Т2 — `test_a_production_built_surface_is_not_refused`,
Т3 — `test_a_row_actually_licenses_the_place_it_names`.

| Состояние сторожа | Т1 | Т2 | Т3 |
|---|---|---|---|
| **здоровый** | зелено / зелено | зелено / зелено | зелено / зелено |
| **М1** — сторож срабатывает там, где не должен (отказывает всему) | **КРАСНО / КРАСНО** | **КРАСНО / КРАСНО** | зелено / зелено |
| **М2** — сторож перестал читать таблицу разрешений | зелено / зелено | зелено / зелено | **КРАСНО / КРАСНО** |
| **М3** — сторож обрывает вызов до тела, НЕ ставя своей метки | **КРАСНО / КРАСНО** | **КРАСНО / КРАСНО** | **КРАСНО / КРАСНО** |
| **М4** — снята потельная половина сторожа (перестал срабатывать) | **КРАСНО / КРАСНО** | зелено / зелено | зелено / зелено |

В каждой клетке слева итог под версией 2.0.0, справа — под 2.1.1. **У каждой из трёх
проверок есть мутант, который красит её под ОБЕИМИ версиями**, — значит ни одна не
«различает вполовину», чего требовал п.3 критерия.

Зелёные клетки при мутанте — не пробел, а разделение обязанностей, и оно правильное:
Т3 не зависит от того, срабатывает ли сторож вообще (её предмет — что строка таблицы
ВЫКЛЮЧАЕТ отказ), а Т2 не зависит от потельной половины. Мутант М1 ловится Т3 не потому,
что Т3 слепа, а потому что разрешающая строка в этой проверке стоит раньше и законно
гасит отказ; за «сторож вообще перестал срабатывать» отвечают десять остальных проверок
файла, начиная с `test_a_hand_registered_tool_is_refused_at_the_call`.

**М3 — главный мутант юнита**, потому что он изолирует именно ПОЛОЖИТЕЛЬНУЮ половину,
ту, которую бриф запрещал ослаблять: сторож обрывает вызов, не ставя метки `CB-311`,
поэтому отрицательная половина проходит, и красит проверку только положительная. Она
красит под обеими версиями.

### Шаг 5. Прямое доказательство, что прежняя проверка под 2.1.1 не различала НИЧЕГО

Это сильнее, чем «прежде было красно»: прежний файл проверок прогнан в двух
противоположных мирах — при здоровом стороже и при мутанте М1, который ломает ровно то,
что эти проверки стерегут.

| | 2.0.0 | 2.1.1 |
|---|---|---|
| прежние проверки, сторож ЗДОРОВ | 3 зелёных | **3 красных** |
| прежние проверки, сторож СЛОМАН (М1) | 2 красных, 1 зелёная | **3 красных** |

Под 2.0.0 прежние проверки отвечали по-разному в двух мирах, то есть работали. Под 2.1.1
они выдавали **один и тот же вердикт в обоих мирах** — то есть не несли никакой
информации о стороже вовсе. Новые проверки под 2.1.1 отвечают так же по-разному, как
прежние под 2.0.0 (см. таблицу шага 4).

### Шаг 6. Оба способа прогона под 2.1.1 из §4 — выполнены, результаты совпали

**Способ первый, быстрый** (`uv run --with 'mcp==2.1.1' …`), файл под проверкой:
`14 passed in 0.68s`.

**Способ второй, как делает нога сборки.** Вместо простого копирования дерева сделан
самостоятельный клон репозитория на этой ветке. Причина: рабочее дерево — это
присоединённый worktree, где `.git` не каталог, а файл-указатель на служебный каталог
основного клона; сырая копия такого дерева продолжала бы ссылаться на ЧУЖОЙ служебный
каталог, и любая git-операция в копии била бы по оригиналу. Клон, кроме того, точнее
воспроизводит ногу сборки: та делает `actions/checkout` с `fetch-depth: 0`, потому что
одна проверка набора читает настоящую историю репозитория.

Шаги клона повторяют задание `newest-sdk` (`.github/workflows/ci.yml`, строки 152–176)
дословно:

```
git clone --branch fix/cb-318-version-honest-crash-text …   (клон на 0a41fdf)
uv lock --upgrade-package mcp     → Updated mcp v2.0.0 -> v2.1.1
                                    Updated mcp-types v2.0.0 -> v2.1.1
uv run --extra dev python -c "…m.version('mcp')"  → mcp 2.1.1
uv run --extra dev python -m pytest tests/test_hand_built_surfaces.py -q → 14 passed in 0.65s
uv run --extra dev python -m pytest tests/ -q     → 3452 passed in 279.34s (0:04:39)
```

**Способы совпали.** Оба разрешили ровно версию 2.1.1 и оба дали по файлу `14 passed`.
Расхождения, о котором бриф просил сообщить немедленно, нет. Сверх того второй способ
дал то, ради чего заведена нога сборки: **весь набор под 2.1.1 зелён целиком —
3452 пройденных, ни одного падения**. Это и есть предмет п.2 критерия и прямое
основание ожидать, что нога `newest-sdk` после посадки станет зелёной.

### Шаг 7. Проход `/simplify-traced` — что он нашёл и что изменено

Пройдены обе применимые части: разбор качества четырьмя независимыми проверяющими
(повторное использование, упрощение, эффективность, высота решения) и сверка сделанного
с исходным поручением. Третья часть — рефлексия — в этом репозитории неприменима: она
опирается на `.claude/skills/reflect/process-rules.md`, `tools/fold_process_rules.py` и
каталог `.claude/retro/`, которых здесь нет вовсе (они живут в соседнем проекте
autosorter), да и собственная памятка навыка ставит эту часть в зависимость от
инструмента, отсутствующего в этом дереве. Пропуск назван, а не сделан молча.

**Повторное использование:** готового помощника в дереве нет. Найден, однако, брат по
приёму — `tests/test_refusal_classification.py::_causes` (строки 263–269) обходит ровно те
же два звена, `__cause__` затем `__context__`. Слить их нельзя: тот возвращает объекты
исключений, потому что его вопрос — КЛАСС звена, а здесь нужен ТЕКСТ. Проверено мной
лично чтением файла, а не принято со слов. Второй экземпляр приёма теперь назван прямо в
объяснении к помощнику — в этом репозитории принято называть повтор, а не прятать его.

**Эффективность:** ничего. Отдельно проверено, что сообщения при `assert` не вычисляются
на зелёном прогоне.

**Упрощение — три правки, каждая проверена замером, а не принята на веру:**

1. Объяснение обхода `__context__` было **перевёрнуто**: я написал, будто эта ветка даёт
   «громкий отказ вместо тихого», тогда как она ровно наоборот спасает от ЛОЖНОГО отказа
   на здоровом стороже. Переписано; заодно опора переведена с наблюдения на документ:
   в mcp 2.1.1 объяснение к `Tool.run` (`tools/base.py`, строки 137–140) прямо говорит,
   что всякая обёрнутая авария поднимается `from` исходной и что текст аварии намеренно
   не доводится до клиента. То есть цепочка — это заявленный SDK договор, а не случайность.
2. Механизм версий излагался в четырёх местах. Полное изложение оставлено одно, три
   остальных заменены указателем на него: меньше мест, которые разойдутся при смене SDK.
3. Из трёх ПОЛОЖИТЕЛЬНЫХ утверждений убран `{reason!r}`. Замерено: при `assert X in Y`
   pytest печатает всю строку сам, а при `assert X not in Y` — обрезает её. Поэтому в
   отрицательных утверждениях он оставлен: там он добавляет то, что срезано.

**Высота решения — вердикт «высота верная, пластыря нет», две находки.**

Первая — **фактическая ошибка в моём объяснении, и она же поправка к посылке брифа.**
Бриф (и вслед за ним первая редакция моего текста) утверждает, что под 2.1.1 «текста нет
ни у отказа сторожа, ни у аварии тела», отчего перестают различать ОБЕ половины пары.
Замер говорит иначе: отказ сторожа не проходит через SDK вовсе, поэтому его текст несёт
метку `CB-311` под обеими версиями, и отрицательная половина исправно краснела на
сломанном стороже. **Ломалась только положительная половина — но ломалась безусловно, и
потому тест был красен при любом состоянии сторожа; вот это и есть «проверка, не несущая
различия».** Прямое доказательство лежит в том же файле: проверки отказа утверждают
НАЛИЧИЕ метки в `str()` и зелены под 2.1.1 сегодня. Исправлено; на существо починки
поправка не влияет — чинить надо было то же самое.

Вторая — **доступна ступень глубже, и я её сознательно НЕ сделал; решение за держателем.**
Об этом отдельный раздел ниже.

**Сверка с поручением.** Сделано ровно то, что поручено: затронуты два файла — тот, что
назван предметом, и этот журнал. Ни `src/codebugs/`, ни `uv.lock`, ни `pyproject.toml`,
ни `.github/`, ни `README.md`, ни `tests/test_claude_md_*`, ни файлы соседней ветки Т-143
не тронуты. Ни одна проверка не снята и не ослаблена, число и состав проверок в файле
прежние (14). Записи в `CHANGELOG.md` не делаю — наблюдаемое пользователем поведение не
менялось, менялись утверждения тестов; п.6 порядка работы прямо оставляет это решение
исполнителю, и я решил так.

### Шаг 8. Ступень глубже, которую я не сделал, и почему решать держателю

Проверяющий на высоте предложил строго более сильный различитель и подкрепил его
замером: пусть подставная фабрика соединения (`_factory()`) не только бросает исключение
с текстом-меткой, но и ОТМЕЧАЕТ факт своего вызова, а проверка спрашивает саму эту
отметку. Тогда вопрос «дошёл ли вызов до тела инструмента» задаётся напрямую, а не через
текст, который до нас доводит третья сторона.

Что это даёт, по замеру проверяющего (столбцы: метка нашлась в цепочке / фабрика
отметилась / здоровое ли это дерево):

| случай | цепочка | отметка | дерево здорово? |
|---|---|---|---|
| здоровая продакшн-поверхность | да | да | да |
| сторож отверг корректную поверхность | нет | нет | нет |
| вызов погиб до тела инструмента | нет | нет | нет |
| **SDK перестал сцеплять исключения** | **нет** | **да** | **да** |

Последняя строка — цена нынешнего решения, названная вслух: будущий SDK, переставший
класть исходное исключение в `__cause__`/`__context__`, покрасит эти три проверки на
ЗДОРОВОМ дереве. Отметка такого режима не имеет вовсе, и вдобавок делает помощника
ненужным, то есть решение стало бы и сильнее, и короче.

**Почему я всё же не стал этого делать, а выношу решение наверх.** Четыре довода, и они
о риске, а не о лени:

1. Все пункты критерия приёмки уже выполнены и проверены мутантами под обеими версиями.
2. Правка задела бы общую фикстуру, которой пользуются все проверки файла, — и попала бы
   на ветку поздно, пройдя проверку только мною, тогда как нынешняя редакция прошла
   разбор с четырёх сторон. Ветка садится рядом с соседней, и цена ошибки не только моя.
3. Остаточный риск, который отметка закрывает, принадлежит **менее опасному виду**: это
   ЛОЖНЫЙ КРАСНЫЙ — громкий, диагностируемый, и в сообщении об ошибке прямо названный
   как второе возможное значение. Юнит же чинит противоположное — проверку, которая не
   может ничего показать. Меняться на первое ради второго не нужно.
4. Вероятность самого события снижена тем, что сцепление в 2.1.1 **документировано** как
   договор SDK, а не наблюдено как совпадение.

Сам проверяющий оценивает эту ступень как улучшение, а не условие приёмки, и отдельно
отмечает, что случай «SDK перестал сцеплять» никем не наблюдался. **Считаю правильным
завести это отдельной карточкой, если держатель согласится, — но не расширять область
юнита молча.**

### Шаг 9. Итоговые прогоны на неподвижном дереве

Все числа сняты на `3a3ebf931a4d57728ae05b6f349882f17461a88f`. Неподвижность дерева
подтверждена самим прогоном: `git status --porcelain` дал **0 изменённых файлов и до, и
после**, вершина ветки та же самая.

| прогон | версия | где | итог |
|---|---|---|---|
| весь набор | 2.0.0 (закреплённая) | рабочее дерево | **3452 пройдено, 196,58 с** |
| весь набор | 2.1.1 (способ 1, `--with`) | рабочее дерево | **3452 пройдено, 225,79 с** |
| весь набор | 2.1.1 (способ 2, как нога сборки) | клон | **3452 пройдено, 211,82 с** |
| файл юнита | 2.0.0 | рабочее дерево | 14 пройдено |
| файл юнита | 2.1.1 | рабочее дерево и клон | 14 пройдено |
| `ruff check src/ tests/` | — | рабочее дерево | без замечаний |

**Ни одного падения нигде.** Число проверок совпадает под обеими версиями — 3452 в наборе
и 14 в файле юнита, — а совпадение чисел здесь содержательно: оно означает, что ни одна
проверка не пропускается по версии, то есть починка сделана не отключением.

Мутанты перепроверены на ОКОНЧАТЕЛЬНОМ коде под 2.1.1 (в клоне, чтобы не трогать
рабочее дерево во время прогона) — таблица шага 4 воспроизвелась в точности.

**Состав файла для сверки держателем** (для п.5 критерия): 14 проверок, состав прежний,
ни одна не добавлена и не удалена — `TestTheGuardFires` содержит одиннадцать,
`TestTheAllowanceTableIsDisciplined` — три.
