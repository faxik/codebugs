# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-09-06, смена 24

(вытесняет `HANDOFF-DIR-1-2026-08-31-session22.md`; смена 23 своего handoff'а не оставила и
не могла — см. §1. Следующий вытесняет этот.)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

**Писался при живых фоновых работниках.** На момент последней правки шли два исполнителя и один
приёмщик; §5 говорит, что именно, и это первое, что перемеряется.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов**, называй число явно в каждом промпте. Пункт 9 ссылается на
  `_INSURANCE_PATHS`, которого здесь НЕТ (эквивалент — `uv run --extra dev ruff check src/ tests/`
  плюс полный набор); **пункт 7 умеет быть закрытым НЕВЫПОЛНЕНИЕМ**; **пункт 5 закрывается записью
  вызывающего ПОСЛЕ вердикта**.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md` — слой правил; обоснования в
  `docs/claude-md-rationale/`. Подсистемный слой — `src/codebugs/CLAUDE.md`, и он грузится по
  требованию: **чтение любого файла в `src/codebugs/` втягивает его целиком в контекст.** Знай это
  до того, как решишь «посмотреть одну строчку в `server.py`» — файл большой.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §9 неизменяемые факты
  приёмки, §10 эскалации, §11 журнал (записи смены 24 в конце), §13 шаблон брифа.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`.
- **Реестр болей куратора:** `.claude/plans/PAIN-REGISTRY-codebugs.md` — §1 приоритеты, §2 боли,
  §3 пакеты вниз, §5 ратификации владельца. **Аллокатор номеров:** `tools/cascade-mint.sh --help`;
  реестр `.claude/plans/CASCADE-IDS.md`. **Аллокатором пользоваться ВСЕГДА** — почему, см. §7 п. 1.

## 1. Что сделано за смену

| что | состояние | улика |
|---|---|---|
| Разбор наследства смены 23 | **закрыт** | транскрипт прочитан дешёвым агентом |
| **Т-138** (карта CB-284, задача владельца, `high`) — перевыпуск номера, посадка брифа, запуск | **исполнитель работает** | минт `eacc6a8`, бриф `c53a7df` |
| **Т-135** (карта CB-285) — приёмка | **приёмщик работает** | долг смены 23, оставлен намеренно |
| **Б-12** — карта, разложение пакета, бриф, запуск головного юнита | **исполнитель работает** | карта `CB-310`, минт Т-139, бриф `c7a14ac` |
| Журнал направления за смену | **закрыт** | `e456d01` |

**Смена 23 не оставила handoff'а и не могла.** Транскрипт
(`~/.claude/projects/-home-faxik-w-codebugs/174f6caf-e1f1-40a5-b4d8-720c61bf6403.jsonl`) показал, что
умерла сессия КУРАТОРА, а держатель DIR-1 был фоновым агентом внутри неё; материнская сессия
исчезла через девятнадцать секунд после того, как отправила ему указание ротироваться.

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
   **Попадание в `main` проверяй `git merge-base --is-ancestor`, никогда текстом и никогда порядком
   коммитов.** При сравнении «до и после» приземлённой ветки базой бери ПЕРВОГО РОДИТЕЛЯ МЕРЖА.
2. **Выясни судьбу трёх фоновых работников (§5).** Они могли умереть вместе с сессией. Умерший
   возобновляется сообщением по его идентификатору — не заводи вторых и не создавай вторых деревьев.
3. **Ни одна ветка ещё не садилась.** Перед всякой посадкой: прочти НЕЗАКОММИЧЕННЫЙ ДИФФ ветки
   (канон К-32 — осиротевшая сессия оставляет в дереве и готовую работу, и свои недоделанные пробы),
   сверь ОРАКУЛ БРИФА поимённо, а не зелень набора, и проси окно тишины у куратора.

## 3. Что преемник обязан знать до того, как начнёт

1. **ГЛАВНЫЙ ЗАМЕР СМЕНЫ, И ОН ОПРОВЕРГАЕТ РЕЕСТР БОЛЕЙ.** Боль Б-12 утверждает, что тестов через
   настоящую границу SDK нет. Неверно: полный набор под `mcp` 2.1.1 даёт **десять упавших из 3378**
   (команда `uv run --with 'mcp==2.1.1' --extra dev python -m pytest tests/ -q`). Значит оракул
   существует и громко краснеет, а дыра в том, что **его никто не прогоняет на версии, которая
   реально установлена**. Это меняет вес работ: нога непрерывной сборки — половина починки, а не
   её оформление. **Строка «Почему набор не увидел» в реестре подлежит правке куратором.**
2. **У регресса Б-12 есть точная минута рождения:** установка пересобрана 1 сентября в 13:16, минутой
   позже посадки выпуска 0.3.0, и версия 2.1.1 пришла с ней. То есть дефект вносится КАЖДОЙ
   установкой заново, пока установленное собирается не из файла фиксации версий.
3. **Классификация «ожидаемый отказ против аварии» в проекте РАЗДЕЛЕНА НАДВОЕ**, и буквальное
   переиспользование одной половины восстановило бы текст всех отказов, кроме главного:
   `cli.domain_errors` знает только `ValueError`/`KeyError`, а «нет трекера в этом каталоге»
   поднимается наследником `RuntimeError` (`src/codebugs/db.py:912`).
4. **Единая точка правки поверхности MCP — обёртка регистрации, а не промежуточный слой.**
   Промежуточный слой не годится: SDK перехватывает доменное исключение ДО него, и это записано в
   самом дереве (`src/codebugs/server.py:288-297`). Через обёртку `_NormalizedDescriptions`
   (`:171-221`) проходят все восемьдесят три инструмента, включая тринадцать порождённых генератором.
5. **Долг приёмки Т-135 был оставлен НАМЕРЕННО, а не забыт** — прямое указание куратора смены 23 с
   доводом «заказанная и неразобранная приёмка хуже незаказанной». Знать это надо, чтобы не счесть
   предшественника небрежным и не начать искать несуществующую причину.
6. **Одно поручение смены 23 восстановить НЕЛЬЗЯ, и это сказано вслух.** Счёт «тринадцать случаев
   класса „прибор смотрит на факт записи, а не на содержание“» в транскрипте не расшифрован: списка
   нет, а само число внутренне противоречиво (разбивка по адресатам даёт четырнадцать, и автор в той
   же фразе поправляет себя насчёт числа случаев против себя). Списка за автора не достраивал.

## 4. Очередь преемнику

**1. Разобрать возвраты трёх работников (§5) и принять решения по ним.** Головное.

**2. Посадка Т-138 и Т-139**, каждая — с окном тишины, чтением незакоммиченного диффа и поимённой
сверкой оракула брифа.

**3. Приёмка Т-138 и Т-139** после посадки, девять пунктов.

**4. Два оставшихся юнита пакета Б-12**, разложенных сменой 24 и пока не заведённых:
   **(а)** аналог глагола `where` на поверхности MCP — сегодня узнать привязку трекера через MCP
   нельзя вовсе, диагностика пишется только в поток при старте (`src/codebugs/server.py:458-475`);
   принцип CB-11 «привязку, которую нельзя увидеть, нельзя отладить».
   **(б)** сборка установленного сервера из файла фиксации версий — по пункту 3 §3 это то, без чего
   дефект возвращается при следующей же установке.

**5. Хвост очереди смены 22, не тронутый:** CB-283 (`medium`, продуктовый), CB-282 (`low`,
текстовый). **Отложенное осью приоритета:** CB-272, CB-273, CB-275, CB-270, CB-271, CB-279.
**Хвост (`low`):** CB-253, CB-252, CB-250, CB-240.

**6. Карты, заведённые вокруг Т-135 и не разобранные:** CB-287 (таблица механически принуждаемого в
корневом `CLAUDE.md` не называет коды 16 и 17 — обе половины починки CB-187) и CB-309
(`_worktree_is_provably_gone` лежит в `worktree-finish.sh`, а не в `tools/_guards.sh`, и потому
недостижима для механизма, прогоняющего сторожей по отдельности; держатель смены 23 назвал это общим
корнем трёх практических неудобств диффа Т-135).

## 5. Скоропортящееся — перепроверить на месте

На момент последней правки `main` стоял на коммите брифа Т-139 (`c7a14ac`), главный чекаут чист
кроме файлов настроек владельца. **Рабочих деревьев ДВА**: `fix-cb-284-show-what-is-committed`
(юнит Т-138) и дерево юнита Т-139, если исполнитель успел его создать.

**Три фоновых работника были живы:** исполнитель Т-138, исполнитель Т-139, приёмщик Т-135. **Все
трое переживали обрыв сессии в эту смену и были возобновлены сообщением** — то есть механизм
возобновления здесь работает, но проверять живость надо каждый раз заново.

**Главный чекаут делят параллельные направления** (DIR-2, и в трекере появились следы DIR-3).
В твоём индексе могут оказаться чужие файлы: лечится перечислением своих путей после `--` в команде
коммита, а не `--no-verify`. Файл настроек владельца `.claude/settings.json` и его копия — **не
трогать**. **Ни одна ветка никогда не удаляется.**

**ГРЯЗЬ В ГЛАВНОМ ЧЕКАУТЕ ОПАСНА И ВНЕ ОКОН.** Неотслеживаемый файл финишу — «note», а изменённый
ОТСЛЕЖИВАЕМЫЙ — `exit 11`, и повтором он не лечится.

## 6. Инструментальное — ловушки этой смены

- **`pytest` в этом репозитории НЕ несёт плагина таймаутов**: `--timeout=600` роняет запуск с
  «unrecognized arguments» ещё до сбора тестов.
- **`команда | tail -N` возвращает код возврата `tail`, а не команды.** Фоновый прогон, отчитавшийся
  «exit code 0», на деле упал; я это чуть не принял за зелёный набор. Читай ВЫВОД, а не код.
- **Чтение любого файла в `src/codebugs/` втягивает в контекст весь подсистемный `CLAUDE.md`.**
  Планируй это как трату, а не как побочный эффект.
- **`uv run --with 'mcp==2.1.1' …` — рабочий способ построить состояние другой версии библиотеки**
  без порчи окружения: накладывается поверх и `uv.lock` не трогает. Проверено.
- **Полный прогон набора из главного чекаута безопасен только на НЕПОДВИЖНОМ дереве.** Смотри §7
  п. 2: я нарушил это сам.
- Из прошлых смен, всё ещё живое: **`./.venv/bin/ruff` НЕ СУЩЕСТВУЕТ** (только
  `uv run --extra dev ruff`, `0.15.7`); **`git commit -- <путь> -m '<текст>'` ОТКАЗЫВАЕТ** —
  сообщение стоит ДО разделителя; **`git commit -- <путь>` не видит НЕОТСЛЕЖИВАЕМЫЙ файл**, сперва
  `git add -- <путь>`; **прогон по именам ФАЙЛОВ вместо КЛАССОВ** даёт «no tests ran»; **приёмщиков
  гоняй в их СОБСТВЕННЫХ копиях**; **прямой вызов клиента базы данных запрещён**.

## 7. Записи против меня

**Их три, и первая — самая неприятная, потому что это тот же дефект, который я в эту смену чинил
у предшественника.**

1. **Я написал бриф с номером карточки, которого ещё не существовало.** Файл брифа Т-139 был
   написан с номером `CB-311` — вычисленным «по порядку», до того как трекер выдал настоящий, —
   и настоящим оказался `CB-310`. Пришлось править файл заменой по всему тексту. Это буквально то,
   за что я в этой же смене перевыпускал номер юнита смены 23: **идентификатор, вычисленный
   собственной головой вместо аллокатора.** Разница только в том, что мою ошибку поймало филирование
   карточки минутой позже, а ошибку смены 23 никто не ловил пять дней. Вывод не «быть внимательнее»,
   а процедурный: **сначала аллокатор, потом текст, всегда — и для карточек, и для номеров юнитов.**

2. **Я прогнал полный набор из главного чекаута, коммитя в него заметки во время прогона.**
   Встроенный сторож набора об этом честно предупредил («дерево двигалось под прогоном»). Замер
   уцелел — все десять падений поведенческие и однородные, и одно я воспроизвёл отдельным прицельным
   прогоном, — но я создал ровно то состояние, от которого сторож защищает, и сделал это в ту же
   смену, когда запрещал исполнителям пачкать главный чекаут. **Прогон, который решает вопрос,
   делается на неподвижном дереве, иначе его придётся защищать вместо того, чтобы на него
   опираться.**

3. **Я потратил круг на запуск с несуществующим флагом `--timeout` и не заметил этого сразу**,
   потому что конвейер с `tail` вернул нулевой код возврата. Мелочь по цене, но по форме — то же
   самое, что записи 1 и 2: **я поверил показанию прибора, не посмотрев на предмет.** Три записи
   из трёх — один класс, и класс этот в точности тот, что смена 23 назвала своим главным:
   «прибор смотрит на написание и находит написание, а не предмет».

**Что из этого следует для преемника.** Правило смены 21 остаётся первым: находка есть ГИПОТЕЗА,
пока её не воспроизвёл второй независимый способ. Моя смена добавляет уточнение к нему: **второй
способ нужен не только находке, но и УСПЕХУ** — «набор прошёл», «команда вернула ноль», «номер
следующий по порядку» суть такие же показания прибора, как и красный тест, и все три раза меня
подвело именно доверие к успеху, а не к отказу.
