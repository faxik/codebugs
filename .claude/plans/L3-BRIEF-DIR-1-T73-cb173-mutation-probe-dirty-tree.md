# Бриф юнита Т-73 (DIR-1) — CB-173: мутационная проба обязана отказывать на грязном дереве

**Класс по Р1: B** (оракул детерминирован — автотест: на грязном дереве проба отказывает, на чистом
идёт). **Стартовый тир: `sonnet`, effort: medium.** Эскалация до `opus` разрешена по признаку Р2
(два подряд неудачных подхода к одному месту, или обнаруженное расхождение брифа с кодом) — назвать
эскалацию вслух в отчёте, пункт 4 ниже.

**Ветка:** `fix/cb-173-mutation-probe-refuses-dirty-tree`. Создаётся
`tools/worktree-setup.sh fix/cb-173-mutation-probe-refuses-dirty-tree`.
**Карта юнита: CB-173** (одна). **Территория: `tests/manual/`** плюс новый файл теста в `tests/`.
**Чужая живая территория, не заходить:** `src/codebugs/findings.py`, `tests/test_dedup.py`,
`tests/golden/mcp_schema.json` — в них работает ветка DIR-2 `fix/cb-90-description-write-guard`.
`CHANGELOG.md` — файл общий для всех направлений; см. пункт 6.

---

## 1. Что болит и почему это высокая тяжесть

Мутационная проба — это когда, чтобы доказать, что тест не декоративный, в исходник намеренно
вносят дефект и убеждаются, что тест краснеет. В этом репозитории проба делается так: скрипт
читает файл, записывает поверх испорченную версию, прогоняет тесты и в `finally` возвращает
оригинал.

**Пять раз это уничтожило незакоммиченную работу агента.** Инциденты выписаны в карте CB-173 с
цитатами из `.claude/plans/BUGFIX-LOOP-LEDGER.md` (2026-08-13 — четыре подряд, 2026-08-17 — пятый).
Сам леджер дважды вывел из них предписание, дословно: **«the mutation runner should refuse to run on
a dirty tree»**. Этого отказа **нет нигде** — ни в двух существующих скриптах, ни в `tools/`, ни в
инструкции скилла `bugfix-loop`, где живёт только текст «Commit first, mutate second». Конвенция,
нарушенная четырежды после принятия, не есть правило: это доктрина `CLAUDE.md` слово в слово, и
именно поэтому карта заведена как high.

## 2. Что делать — форма выбрана уровнем (2), не выбирай другую

**Один общий охранник, вызываемый обоими скриптами до первой записи в файл.**

Карта перечисляет три возможные формы. Уровень (2) выбрал **(a) fail-closed отказ на грязном
дереве**, потому что режим владельца на эту смену — минимальная нарезка: видимая польза плюс
честный тест, без переделки механизма. Формы (b) (мутация через одноразовую копию дерева) и (c)
(общий runner вместо двух копипаст) **в скоуп не входят** и не начинаются.

Конкретно:

1. Новый модуль `tests/manual/mutation_guard.py` с одной публичной функцией — например
   `require_clean_tree(paths, *, allow_dirty=False)`. Она спрашивает у git состояние **тех файлов,
   которые проба собирается портить** (`git status --porcelain -- <path>...`), и **поднимает
   исключение с внятным текстом**, если ответ непуст. Текст обязан назвать сами файлы и назвать
   выход: закоммитить их, либо передать явный флаг.
2. Оба существующих скрипта — `tests/manual/mutate_cb69.py` и `tests/manual/mutate_cb31.py` —
   зовут её **первой строкой работы**, до `read_text()`. Скрипт, который уже начал портить файл,
   охранником не спасён.
3. **Явный аварийный выход обязателен**: флаг командной строки (`--allow-dirty`) или переменная
   окружения. Отказ без выхода — стена, а не диагностика; это правило репозитория, и в `CLAUDE.md`
   оно записано на примере `_guard_interpreter_matches_main`.
4. **Fail-closed по-настоящему.** Если git недоступен, вернул ненулевой код или ответил тем, чего
   охранник не понимает, — это **отказ**, а не разрешение. «Не смог посмотреть» обязан читаться как
   «грязно», иначе получится ровно тот класс «guard reporting clean because it could not look»,
   который этот репозиторий ловит у себя четвёртый раз. Поймай `OSError` **и**
   `subprocess.SubprocessError`: второе не является подклассом первого (правило CB-79 в `CLAUDE.md`).

## 3. Честный тест — и он не в `tests/manual/`

`tests/manual/` не собирается сьютом. Тест охранника обязан лежать в обычном месте — новый файл
`tests/test_mutation_guard.py` — и обязан работать **на настоящем временном git-репозитории**
(`tmp_path` + `git init` + один коммит), а не на подделанном выводе `git`. Подделав вывод, ты
проверишь свою же строку формата, а не поведение.

Минимум, который обязан покраснеть на дефекте:

- чистое дерево → охранник пропускает;
- файл, который проба собирается портить, изменён → **отказ**, и в тексте отказа стоит имя файла;
- флаг явного выхода → проходит и на грязном;
- **git недоступен или ответил ошибкой → отказ** (это и есть пункт fail-closed; смоделируй, подсунув
  пустой `PATH` или неисполняемый git — измерено в `CLAUDE.md`, что неисполняемый git должен быть
  ЕДИНСТВЕННЫМ в `PATH`, иначе CPython просто найдёт настоящий и продолжит).

**Композиция, отдельным пунктом, потому что проверка элементов не проверяет их сочетание.** Не
ограничивайся отдельными случаями: собери цепочку **«грязный файл → запуск скрипта целиком»** и
убедись, что скрипт **не тронул файл вообще** — то есть после отказа содержимое на диске
байт-в-байт то же, что до. Отказ, случившийся уже после первой записи, формально «отказ», а работу
уже съел. Этот пункт — предмет приёмки наравне с остальными.

**Мутационная проба над собственным тестом (пункт 2 приёмки).** Сделай её **во временном worktree,
никогда в рабочем дереве** (канон К-5; в этом репозитории пробы делаются `git archive` в scratchpad
— скрипта `tools/worktree-probe.sh` здесь **нет**, это инструмент другого проекта, за ним ходили уже
три приёмщика). Поставь мутант **в точку дефекта** — например, заставь охранника возвращать «чисто»
при непустом ответе git — и покажи, что тест краснеет. Обязательно: `PYTHONDONTWRITEBYTECODE=1`,
чистый `__pycache__`, и **подтверди `grep -c`, что мутант реально лёг в файл**: без этого «мутант
выжил» и «мутант не применён» неразличимы.

## 4. Эскалации (ПОМЕЧЕННЫЙ ПУНКТ — обязан стоять отдельным заголовком в отчёте НА ВЕТКЕ)

Отчёт исполнителя в этом файле, на ветке, обязан содержать раздел с заголовком **«Эскалации»**.
Пустой — тоже явный ответ, так и напиши: «эскалаций нет». Сообщение в чат его не заменяет: приёмщик
судит артефакт, а не переписку.

Эскалируй немедленно, не решая сам:
- если предпосылка этого брифа не сошлась с кодом (см. пункт 8 — она объявлена гипотезой);
- если охранник ломает существующие пробы способом, которого бриф не предвидел;
- если территория пересеклась с чужой веткой.

## 5. Названный остаток — и его ОЦЕНКА тоже предмет приёмки

Путь (B) из карты — обрыв процесса (`SIGKILL`, таймаут, закрытие сессии) **до** `finally` — этот
юнит **не закрывает**, и закрывать не должен. Назови его в отчёте и **оцени честно**: что именно
останется достижимым и насколько это дорого. Дважды подряд в этом направлении исполнитель честно
называл остаток и **занижал** его; занижение — такой же дефект, как умолчание.

## 6. `CHANGELOG.md` — часть критерия приёмки

Ратификация владельца 2026-08-25: **каждый принятый юнит оставляет запись в `CHANGELOG.md`
пользовательским языком.** Требования:

- В секции `## [Unreleased]`, подсекция по смыслу (здесь — `### Fixed`).
- **Коротко: 4–8 строк.** Существующие записи в файле длинные; новый режим владельца — минимальная
  нарезка, длинные записи больше не образец.
- Языком того, кто пользуется, а не того, кто чинил: что изменилось и почему это важно. Здесь
  аудитория — тот, кто работает с репозиторием, поэтому скажи прямо: пробы больше не могут стереть
  незакоммиченную работу, и как из отказа выйти.
- **Стилем и языком файла** (он английский) — не смешивай.
- **Отдельным, ПОСЛЕДНИМ коммитом ветки.** Файл трогают все три направления одновременно; запись,
  сделанная последней, даёт наименьшую площадь конфликта при форвард-мердже.

## 7. Регламент

- **Ты работаешь ТОЛЬКО в своём worktree.** Правка файла в основном чекауте — редактирование main.
- **Уровень (3) на `main` не коммитит никогда** (канон К-1). Факт своей работы пиши **в этот бриф на
  ветке**. Строку в документе направления пишу я, после вердикта приёмщика.
- Тесты и линт — в worktree и **обязательно с `--extra dev`**:
  `uv run --extra dev python -m pytest tests/ -q`, `uv run --extra dev ruff check src/ tests/`.
  Прогон сьюта из main тестирует чужое дерево и проходит на коде, которого ты не трогал.
- Интеграция: `tools/worktree-finish.sh fix-cb-173-mutation-probe-refuses-dirty-tree --merge-msg
  'Merge fix/cb-173-mutation-probe-refuses-dirty-tree: <что изменилось> (CB-173)'`.
  **`--merge-msg` обязателен**, потому что ветка начинается с красных тестов (TDD): деривация
  субъекта берёт первый коммит ветки и назвала бы тест вместо предмета (канон К-4).
- **`exit 13` («main moved») лечится буквальным повтором той же команды, включая `--merge-msg`.**
  `exit 11` («main dirty») повтором **не лечится** — на main лежит чужая незакоммиченная заметка;
  жди, не трогай её (`git checkout --`, `git stash` или коммит чужого состояния сотрут чужую работу
  в момент её написания), и сообщи мне, если держится дольше нескольких минут.
- Признак того, что юнит сел, — **только `git log` на main**, никогда уведомление агента.
- Обязательный раздел отчёта **«Класс задачи и модель»**: класс по Р1, стартовый тир, сработавшие
  эскалации и финальный тир. «Не пинован» — тоже факт и он дефект.

## 8. Моя предпосылка, объявленная ГИПОТЕЗОЙ

Я **не прогоняла** оба скрипта; я читала карту CB-173 и её цитаты. Гипотеза: оба скрипта делают
`read_text()` → `write_text(mutated)` → прогон → `finally: write_text(original)` и **не** содержат
никакой проверки чистоты дерева. **Проверь это первым действием** и, если не сойдётся, —
эскалируй, а не подгоняй работу под мой текст.

Вторая гипотеза, той же природы: `tests/manual/mutate_cb31.py` — копия `mutate_cb69.py` (об этом
говорит его собственный докстринг). Если копии разошлись и охранник в них ложится по-разному —
скажи об этом, а не унифицируй скрипты: унификация есть форма (c), она вне скоупа.

## 9. Отчёт исполнителя

### Что сделано

Предпосылка брифа (пункт 8) подтвердилась при чтении обоих скриптов первым действием: и
`tests/manual/mutate_cb69.py`, и `tests/manual/mutate_cb31.py` делают
`original = path.read_text()` → запись испорченной версии → прогон pytest в подпроцессе →
`finally: path.write_text(original)`, и ни один из них нигде не проверяет чистоту дерева. Вторая
гипотеза брифа тоже подтвердилась: тело функции `run()` в обоих файлах побайтово идентично (только
списки `MUTATIONS` и целевые файлы разные), так что охранник понадобился в одной и той же точке
вставки в обоих скриптах.

Реализована форма (a), как предписано брифом — ничего сверх неё:

- Новый файл `tests/manual/mutation_guard.py` с одной публичной функцией
  `require_clean_tree(paths, *, cwd, allow_dirty=False)`. Она спрашивает
  `git status --porcelain -- <paths>` (только про файлы, которые проба собирается портить, а не
  про всё дерево), поднимает `DirtyTreeError` с текстом, называющим сами файлы, если ответ
  непуст. Fail-closed: `OSError` (git отсутствует или неисполняем) и
  `subprocess.SubprocessError` (например, таймаут) — оба ловятся отдельно (второе не подкласс
  первого, CB-79) и оба читаются как «грязно», как и ненулевой код возврата `git`. Аварийный
  выход — `allow_dirty=True`; оба скрипта прокидывают его из флага `--allow-dirty` и/или
  переменной окружения `CODEBUGS_MUTATION_ALLOW_DIRTY=1`.
- Оба существующих скрипта зовут `require_clean_tree` первой строкой работы в `__main__` — до
  первого `read_text()` в цикле `run()` — со списком уникальных путей всех своих мутационных
  целей.
- Честный тест `tests/test_mutation_guard.py` (8 тестов, обычное место — файл собирается сьютом),
  работающий на настоящем временном git-репозитории (`tmp_path` + `git init` + один коммит), а не
  на подделанном выводе `git`: чистое дерево пропускает; грязный файл-цель отказывает и называет
  его в тексте; грязный посторонний файл НЕ мешает (охранник спрашивает только про свои цели);
  `allow_dirty=True` проходит и на грязном; отсутствующий `git` (пустой `PATH`) отказывает;
  неисполняемый `git` — единственный на `PATH` (иначе CPython найдёт настоящий и проба ничего не
  докажет, как явно предупреждает `CLAUDE.md`) — тоже отказывает; `git`, отвечающий ненулевым
  кодом (запуск вне репозитория), тоже отказывает. Отдельный композиционный тест
  (`TestComposedHarnessRefusesWithoutWriting`) копирует настоящий `mutate_cb69.py` и
  `mutation_guard.py` в изолированный одно-коммитный git-репозиторий в `tmp_path`, портит один из
  целевых файлов, запускает скрипт целиком подпроцессом и проверяет, что содержимое файла на диске
  байт-в-байт совпадает с испорченной (не восстановленной, не мутированной) версией — то есть
  скрипт не успел коснуться файла вообще, а не просто «формально отказал уже после записи».

### Эскалации

Эскалаций по признакам брифа (пункт 4) нет: предпосылка сошлась с кодом, охранник не сломал
существующие пробы способом, которого бриф не предвидел (оба скрипта byte-identical в теле
`run()`, вставка в одной точке в обоих), территория не пересеклась с чужой веткой (правил только
`tests/manual/mutate_cb69.py`, `tests/manual/mutate_cb31.py`, новые файлы
`tests/manual/mutation_guard.py`, `tests/test_mutation_guard.py`, и запись в `CHANGELOG.md` —
никаких касаний `src/codebugs/findings.py`, `tests/test_dedup.py`,
`tests/golden/mcp_schema.json`).

Отдельно, по прямому запросу держателя направления, полученному в чате во время работы: это не я
удаляла чей-либо worktree и не вызывала `git worktree remove`/`prune` ни разу за всю сессию —
только `tools/worktree-setup.sh` (создание своего) в начале работы.

### Названный остаток и его оценка

Путь (B) из карты CB-173 — обрыв процесса (`SIGKILL`, таймаут, закрытие сессии) между записью
испорченной версии файла и выполнением `finally: path.write_text(original)` — этим юнитом не
закрыт, и не должен быть: бриф прямо выводит его из скоупа формы (a). Оценка вживую, а не
формально: `require_clean_tree` устраняет ровно тот класс инцидентов, что перечислен в карте
(«грязное дерево на входе» — все пять описанных случаев были именно такими: скрипт стартовал на
уже изменённом файле). Путь (B) — другой класс: дерево было ЧИСТЫМ на входе, скрипт легально его
испортил, и именно этот легальный шаг не пережил обрыв. Защититься от него формой (a) нельзя в
принципе — она проверяет состояние ДО первой записи, а не переживает обрыв ПОСЛЕ неё. Закрыть его
может форма (b) (мутация через одноразовую копию дерева — тогда портить нечего, оригинал вообще не
трогается) или (c) (общий runner с транзакционной записью/restore). Тяжесть остатка: вероятность
низкая (пять зафиксированных инцидентов в карте — все пути (A), ни одного (B)), а цена одного
случая реализации — та же, что у исходной проблемы (потеря незакоммиченной правки), так что
занижать её нельзя — если такое пять раз уже случилось от банальной причины, обрыв процесса тоже
рано или поздно случится. Дважды подряд в этом направлении исполнитель, по словам брифа, занижал
остаток; здесь остаток назван и оценён как открытый, реальный, с той же ценой инцидента, что и
закрытый путь (A) — просто менее вероятный.

### Класс задачи и модель

Класс по Р1 — **B** (оракул детерминирован автотестом: на грязном дереве проба отказывает, на
чистом идёт). Стартовый тир — `sonnet`, как предписано. Эскалаций до `opus` не потребовалось: оба
признака Р2 (два подряд неудачных подхода к одному месту, расхождение брифа с кодом) не
сработали — предпосылка брифа подтвердилась с первого чтения, реализация и тесты легли с первой
попытки без застреваний. Финальный тир — `sonnet`.

### Post-merge-контроль

В этом репозитории post-merge-гейта после интеграции нет — только два способа проверить итог: (1)
собственный alarm `worktree-finish.sh` сразу после мержа (сверка `ORIG_HEAD`/родителей мержа —
эту проверку выполняет сам скрипт при интеграции, не этот отчёт), и (2) CI-алярм
`.github/workflows/main-invariants.yml`, который проверяет, что первая линия предков `main`
состоит только из мержей и заметок плана. **Он ещё не сработал и не мог сработать**: харнес
`tools/worktree-finish.sh` мержит только локально и не делает `git push`; на момент написания
этого отчёта локальный `main` опережает `origin/main` на 19 коммитов (включая мержи других
направлений), и алярм в `main-invariants.yml` подписан на `push` в `origin/main` — то есть до тех
пор, пока кто-то (держатель направления или куратор) не запушит, GitHub Actions вообще не
запустится ни для этого мержа, ни для предыдущих 19. Это не пробел, оставленный этим юнитом, — это
штатное устройство харнеса (см. CLAUDE.md, раздел Workflow, про то, что алярм — не гейт).
