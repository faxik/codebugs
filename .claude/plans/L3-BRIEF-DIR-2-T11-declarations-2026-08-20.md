# L3-BRIEF Т-11 — декларации `source` / `reported_at_ref` / `meta` контракта BT-4

Хендофф DIR-2 (уровень 2) менеджеру задачи (уровень 3). 2026-08-20. Спека:
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md` (§1, §2,
§4, §7, §13). Контракт: `.claude/plans/BT-4-field-freshness-contract.md` v2, строки 3–5 —
**ратифицированы владельцем 2026-08-20**: все три — «заморозка, объявленная словами»,
ноль поведенческих правок.

## Тройка П-Б

**Замысел.** Решённое решением и выглядит: три поля, которые бамп сознательно не трогает,
несут это как объявленный контракт на каждом читателе, а не как молчание.

**Задание (docs-only; любая поведенческая правка = выход из скоупа = стоп):**
1. **`source` — frozen by design, first-reporter:** декларация на ТРЁХ читателях —
   докстринг `query(source=)`-фильтра, `group_by="source"`-документация, проза MCP `add`
   («Who created this finding» → уточнить «first reporter; re-observations keep the
   original, newest sources live in the occurrence ring — note an imported observation's
   ring source can be a peer tracker's»). Ссылка на CB-21: `source` объявлен intentionally
   immutable — заметка в карту CB-21 (append_note) о закрытой ячейке.
2. **`reported_at_ref` — observation-frozen, manually mutable:** докстринги `add`
   (`reported_at_ref`), `update` (существующий arm), `query(ref=)` — семантика фильтра
   объявляется: «первый наблюдённый ЛИБО вручную назначенный релизный ref» (= текущее
   поведение); ring несёт per-occurrence refs как улику; ring-читателя нет и не строится
   до потребителя latest-семантики.
3. **`meta` — top-level авторское состояние, observation-frozen:** докстринги `add`
   (`meta`), `query(meta_key=/meta_value=)`: читают авторское состояние строки;
   наблюдённая meta — per-occurrence улика в `meta.occurrences[*].meta`; продвижение
   ключей в строку — только будущий allowlist по замеренному спросу.
4. CLAUDE.md: дописать пункты контракта в дедуп-буллет CB-43 (после пункта (9) от Т-9 —
   компактно, по одному предложению на поле). CHANGELOG: короткая запись в [Unreleased]
   (документационный контракт, поведение не менялось).
5. **Wire golden ОБЯЗАТЕЛЬНО регенерировать** — MCP-описания меняются:
   `PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json` из
   worktree. Это точка сериализации: перед касанием проверь in-flight (Т-10 идёт
   параллельно в том же файле, непересекающиеся регионы; кто первым финиширует — второй
   форвард-мерджит).
6. Дешёвые пин-тесты по образцу Т-8-докстрингового: где декларация утверждает исполняемый
   факт (например «query(ref=) — точное совпадение», «meta-фильтр читает top-level»),
   пин сверяет прозу с кодом; где пин структурно нечем — сказать явно.

**Критерий приёмки:** все три поля объявлены на каждом своём читателе; golden
регенерирован и дифф состоит ТОЛЬКО из описаний; ноль поведенческих правок
(дифф src/ — только докстринги/строки описаний); CLAUDE.md/CHANGELOG на месте;
существующая сюита зелёная.

## Клеймы, границы

Карт у юнита нет; **CB-103 (population card) держит DIR-2 (CLM-18)** — НЕ переводить в
терминальный статус; §13 п.7 — явным вердиктом. Ветка `fix/bt4-declarations` (без id).
Файлы: `findings.py` (докстринги), CLAUDE.md, CHANGELOG.md, `tests/golden/mcp_schema.json`,
пин-тесты. НЕ файлы DIR-1. Worktree-харнесс, исполнитель — свежий субагент (П-В), юнит-бриф
в worktree, возврат — тройка с явным слотом эскалаций, мутационная проба руками для
пин-тестов (или явное «нечем пинить» с причиной).