Ты — преемник держателя направления **DIR-2 «Identity находок»** (уровень (2) каскада планирования),
репозиторий `/home/faxik/w/codebugs`. Смена 22.

**Правил в этом промпте нет намеренно.** Они в нормативных источниках, на которые указывает handoff.
Преемник стартует от источников, а не от пересказа.

**Первое действие: прочитай handoff ЦЕЛИКОМ** —
`.claude/plans/HANDOFF-DIR-2-2026-08-31-s21.md`.

**Второе: исполни его §0.** Открой каждый нормативный источник и сверь handoff с ним; расхождение —
чини handoff, не источник.

**Третье: перепроверяй состояние трекера ЧЕРЕЗ КОД ЧЕКАУТА, а не через MCP.** Сервер MCP поднят из
устаревшего снимка `pipx` и на части запросов отвечает по старому коду — второй handoff подряд несёт
это первым пунктом. Команда для снятия состояния — в §1 handoff'а. Заметь: голова очереди эту
причину и закрывает.

**Четвёртое, до всякой записи в `main`:** применяй проверку «идёт ли чужой финиш» — три условия
вместе, а не тождество процесса. Форма и доводы — в артефакте направления, раздел смены 20; рабочий
скрипт восстанавливай по ней, скрэтчпад прошлой сессии умер вместе с ней. Предшественник проверил
сторож двусторонне, и он окупился на живом состоянии: чужой прогон шёл при СВОБОДНОМ замке.

**Пятое: перемерь скоропортящееся**, не веря ни handoff'у, ни этому промпту: состояния карт,
захваты, `git worktree list`, хвост `.claude/plans/CASCADE-IDS.md`, `git log`, соотношение `main` и
`origin/main`, зелень набора. За прошлую смену набор менялся дважды (3358 → 3366), а `main` уезжал
при держателе четырежды.

## Голова очереди

**ВЫПУСК 0.3.0 — задача владельца, вне очереди, впереди всех карт.** Принята предшественником и НЕ
исполнена: заморозка пришла в тот же час. Полная тройка П-Б лежит в артефакте направления, раздел
«ЗАДАЧА ВЛАДЕЛЬЦА ВНЕ ОЧЕРЕДИ», и продублирована в реестре болей — бери оттуда, не из этого абзаца.
Коротко о замысле, потому что он двойной: починки честности поверхностей доезжают до пользователя, и
версия снимка `pipx` становится ОТЛИЧИМА от чекаута, чем закрывается ловушка, стоящая первым пунктом
§1 handoff'а. Номер `0.3.0`, а не `0.2.3`, потому что часть починок намеренно ломает старое
поведение. Порядок действий и ловушка с тегом — в разделе `Releasing` слоя правил `CLAUDE.md`. Окно
посадки — через куратора.

**CB-200 — бриф написан, атакован и НЕ роздан.** Лежит с шапкой «СТОП» в
`.claude/plans/L3-BRIEF-DIR-2-cb200-cli-domain-errors.md`; четыре обязательные правки перечислены в
самой шапке, номер юнита намеренно не минчен. **Не наследуй формулировку «`exit 0`» из прошлых
передаточных текстов — она опровергнута замером и атакой.**

**CB-280 — форма ВЫБРАНА предшественником: (a).** Довод и построенный замер в артефакте; довод
сильнее того, что несёт карта. Осталось написать бриф. Естественно уезжает одним юнитом с CB-281.

**CB-276 не бери как «переписать карту» — это неисполнимо через поверхность**, и почему именно —
в артефакте, раздел «ПУНКТ 4 ОЧЕРЕДИ НЕИСПОЛНИМ». Её общая форма — CB-265, чей порог уже снят
составом работы; но решать CB-265 надо ОТДЕЛЬНЫМ юнитом от CB-280, и довод против объединения взят
из самой CB-276.

## Указатели

- Артефакт направления и источник истины: `.claude/plans/DIR-2-findings-identity.md`, раздел
  «СМЕНА 21» и далее — он подробнее handoff'а и писался по ходу.
- Реестр болей уровня (1): `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- Спека ролей: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`.
- Правила репозитория: `CLAUDE.md` — слой правил, обоснования в `docs/claude-md-rationale/`.
- Аллокатор номеров: `tools/cascade-mint.sh --prefix Т --text '...'`.

## Ротация

По завершении пакета или при пороге контекста — возврат куратору сообщением с SHA для спот-чека,
затем штатная ротация: handoff без правил и **пересобранный стартовый промпт**
(`PROMPT-DIR-2-next-session.md`). Не оставляй его для прошлой смены.

## Практическое, проверенное прошлой сменой

- **Стоячий триггер перекрёстной атаки на бриф ДО раздачи окупился вторую смену подряд** и оба раза
  опроверг НЕСУЩЕЕ предписание. Ставь его безусловно.
- **Прибор, спрашивающий снизу, ловит только тот вопрос, который в него заложен.** Предшественник
  построил такой прибор, и прибор всё равно дал ложное число, потому что вопросов было задано два из
  трёх. Отсюда: прибор, чей ответ ты собираешься повторить как факт, обязан пройти через ЧУЖУЮ атаку
  — не ради проверки числа, а ради проверки ВОПРОСА.
- **Заметка карты есть такой же унаследованный текст, как её описание.** Предшественник перемерил
  описания и унаследовал заметку, а заметка несла предписание исполнителю и была устаревшей.
- **У всякого прибора, запускающего вербы пакета, рабочий каталог обязан лежать ВНЕ главного
  чекаута:** верб с именем файла по умолчанию пишет в рабочий каталог. Предшественник так испачкал
  корень, и поймал это не он, а алярм набора.
- **Codex здесь не довёл ответ четыре раза за две смены.** Фоновый `codex exec` зависает; запрет
  «не фоном» релею надо ставить условием возврата, а не строкой промпта — релей прочёл запрет и всё
  равно ушёл в фон. Отсутствие вердикта называть пробелом, а не согласием.
- **`grep` здесь — `ugrep`**, читает `.gitignore` и молча теряет попадания; счётные утверждения
  делать обходом синтаксического дерева.
