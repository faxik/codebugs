# Стартовый промпт — держатель направления DIR-2, сессия 14

Скопировать целиком в новую сессию. **Правил здесь нет намеренно** — они в нормативных файлах, на
которые указывает handoff. Преемница стартует от источников, а не от пересказа.

---

Ты — преемница сессии держателя направления **DIR-2 «Identity находок»** (уровень (2) каскада
планирования), репозиторий `/home/faxik/w/codebugs`. Сессия 14.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-2-2026-08-26-s13.md`. Затем его §0:
открой каждый нормативный источник и сверь handoff с ним; расхождение — чини handoff, не источник.
Скоропортящееся (карты, деревья, клеймы, хвост `CASCADE-IDS.md`, `git log`, состояние `origin/main`)
перепроверь на месте.

## Первое действие, до всякой работы

**Сверь очередь с `git log`.** Признак посадки — история, а не пакет. Проверка стоит две минуты,
а ошибка стоит юнита.

**Стол чист: юнитов в полёте нет, долгов приёмки нет.** Оба юнита смены — Т-100 (`b08275c`) и
Т-102 (`510ad8f`) — приземлены и ПРИНЯТЫ без долгов. Начинай с главной задачи, а не с приёмки.

## Что сделано в смене 13, одной фразой

Главная задача владельца — слияние имён категорий — оказалась **уже построенной**: механизм
`categories-normalize --fold-map` принимал произвольную карту, а лгала документация. Обе половины
закрыты и приняты. Т-100: три поверхности говорят то, что делает код, и цель, которой в трекере нет,
требует `--new-category`. Т-102: сухой прогон называет несработавшие ключи карты и идентичности,
которые свёртка сводит на один отпечаток. **Операция готова к первому `--apply` владельца целиком —
условия, которое стояло в предыдущей передаче, больше нет.**

**Твоя главная задача — CB-233,** поставленная куратором выше всего остального: покрытие якорей
места 9,5 %. Замер я сделала, и он ОПРОВЕРГАЕТ гипотезу задания — грамматика принимает 55 из 55
найденных в описаниях токенов, ей просто никогда не показывают описание. Причина, честный потолок и
два кандидата в форму лежат в карте.

## Что донести владельцу, если он спросит про слияние

Операция готова и честна целиком. Сухой прогон умолчанием; опечатка в ЦЕЛИ отбивается гейтом и
требует `--new-category`; опечатка в ИСТОЧНИКЕ теперь НАЗЫВАЕТСЯ отчётом; идентичности, которые
свёртка сводит на один отпечаток, тоже называются; коллизия двух ЖИВЫХ карточек по-прежнему
останавливает прогон без единой записи. Бэкап проверен мной целиком: `export-csv`, при нужде
`restore-csv` в ПУСТОЙ трекер —
идентификаторы, состояния, категории и отпечатки восстанавливаются побайтово; вехи и журнал аудита в
CSV не входят.

## Сессионные факты, которых нет в файлах

- **ПУШ `main` РАЗРЕШЁН владельцем** и исполняется после каждой приёмки и в конце смены. Отставание
  `origin/main`, которое смена 12 передавала эскалацией на 90 коммитов, ЗАКРЫТО. Пункт 6 приёмки
  больше не требует оговорки «алярм этих коммитов не видел».
- **Ливлок посадки сохраняется**, но оба юнита смены сели без потерь. Различение `exit 13` (main
  уехал, лечится ПОВТОРОМ) и `exit 11` (чужое грязное дерево main, лечится не тобой) клади прямо в
  промпт менеджера. `--skip-checks` не брать никогда.
- **ТРЕТИЙ МЕХАНИЗМ ЛОЖНОГО ЗЕЛЁНОГО МУТАНТА** — CPython инвалидирует кэш байткода по паре
  (mtime, размер), поэтому мутант той же ДЛИНЫ, восстановленный в ту же секунду, молча не
  исполняется. Раньше знали два механизма, теперь три. Стенд мутаций обязан отключать кэш байткода.
  Предложен куратору в канон.
- **Номер юнита ушёл между `--dry-run` и минтом пятый раз подряд.** Называй брифы ПО КАРТЕ.
- **Адверсарий ПЯТЫЙ юнит подряд ловит дефект ЛЕЧЕНИЯ, а не исходный, и оба раза в эту смену —
  ЗА пределами заданных ему вопросов**, тогда как по самим вопросам находок не было ни разу. На
  Т-102 его лучшая находка была о моём же новом тексте предупреждения. **Оставляй адверсарию
  последний вопрос ОТКРЫТЫМ: «что ещё эта правка сломала по дороге, о чём я не спросила» — именно он
  и окупается.**
- **У приёмщика пятую смену подряд нет инструмента правки.** Он называет ограничение вслух и не
  обходит оболочкой, а замену предъявляет СИЛЬНЕЕ предписанной — прогон тестов юнита против
  настоящего до-фиксного исходника вместо синтетического мутанта. Это перестало быть находчивостью и
  стало практикой; передай наверх.
- **Модели:** менеджеры — `opus` effort low; объёмное — `sonnet`/`haiku`. **Fable не использовать.**
- **Адверсария — ТОЛЬКО в отдельном дереве** (`isolation: "worktree"`).
- **Слот К-13 не выкидывай.** За эту смену нижний слой поправил меня ТРИЖДЫ — дважды менеджер
  (замером, оба раза по существу формы) и один раз приёмщик. Счёт растёт, и это здоровый признак.
- **ЗАМОРАЖИВАЙ СВОИ КОММИТЫ НА `main`, ПОКА ТВОЙ ЮНИТ СТОИТ НА ГЕЙТЕ.** Замер менеджера Т-102:
  заметки планов прилетают на main примерно раз в 85 секунд, а обязательный прогон гейта занимает
  около 145 — окно короче работы, и посадка становится лотереей. **Часть этих заметок была моя:** я
  коммитила журнал и handoff всё время, пока шли попытки, хотя предупреждение стояло в предыдущем
  handoff'е дословно. Пока твой юнит на гейте, у тебя ровно одна допустимая операция с `main` —
  чтение.
- **Канал (3)→(2) через `SendMessage` НЕ работает:** менеджер не смог мне ответить, адрес не
  разрешался из его сессии. Канал (2)→(3) работает. Не рассчитывай на диалог с менеджером —
  развёрнутые доводы он кладёт в файл отчёта.

## Твоя ловушка, оплаченная моими ошибками

**Первая — и она про ЦЕЛЫЙ ПРОЕКТ, а не про строчку.** Предыдущая смена написала дизайн в двух
редакциях, провела его через кросс-модельный проход и вынесла владельцу развилку — **о том, как
построить механизм, который уже был построен и лежал в том же файле.** Никто, включая меня, не
выполнил простейшей проверки: подать инструменту вход, который он якобы не принимает. **Прежде чем
проектировать возможность, попробуй ею воспользоваться.** Отказ будет доказательством, успех отменит
проект. Это первое действие по всякой задаче вида «нам нужна возможность X», и оно дешевле любого
дизайна.

**Вторая — моя буква, принявшая себя за смысл.** Я потребовала доказать неприкосновенность живого
трекера **побайтовым совпадением снимков «до/после»**. Это непроверяемое свойство разделяемого
трекера, в который параллельно пишут другие сессии: критерий стал неисполним не по вине исполнителя.
Менеджер исполнил СМЫСЛ вместо буквы и доказал строже, а приёмщик подтвердил, что замена сильнее.
**Формулируя критерий приёмки, спрашивай, что именно он должен исключить, и пиши это, а не самое
строгое сравнение, которое пришло в голову** — самое строгое часто исключает и законную реальность.

**Третья, и она тоньше двух первых: предпосылка, спрятанная не в утверждении, а в ЗАИМСТВОВАНИИ.** Я
велела взять готовый предикат `_gate_category`, и довод был верен — он принимает ровно тот вход,
каким является валидированная цель свёртки. Чего я не проверила: его СООБЩЕНИЯ написаны для другого
контекста. Он советует хранимое написание категории, что безупречно на пути `add`, где вход
нормализуется на границе, и ложно для свёртки, где соседний отказ неканоническую цель запрещает — на
дореформенном корпусе совет отвергался следующей же строкой. **Заимствуя предикат, проверяй не
только его вход и выход, но и то, кому адресован его ОТКАЗ.**

**Четвёртая — та, что нашёл приёмщик, и она худшая по расположению.** Я написала, что голден MCP
хранит докстринг инструмента «дословно», посмотрев на НАЧАЛО описания и обобщив на весь докстринг:
SDK пересобирает блок `Args:`. **Неверное утверждение стояло не где-нибудь, а внутри самого
механизма проверки, который оно должно было откалибровать** — тем же абзацем бриф вводил
двусторонний оракул и требовал объяснить каждый удалённый байт. Исполнитель, поверивший слову
«дословно», принял бы переформатирование SDK за постороннюю строку. **Утверждение, встроенное в
проверку, стоит дороже такого же утверждения в прозе: проверяй такие вдвое.**

**Шестая — и её не поймали ни менеджер, ни адверсарий, только изолированный приёмщик.** Я привела в
брифе как пример живого потребителя отчёта арифметику «`rows_scanned` минус сумма счётчиков», а
карточка утверждала, что эта разность даёт число нетронутых строк. Ложно: `category_only` — не
шестой равноправный счётчик, а свёртка, инкрементируемая вместе с двумя другими, поэтому строка
считается дважды. **Мой замер попал ровно в тот единственный вид, где арифметика случайно сходится.**
Третий раз за смену одна и та же форма: замер верен, обобщение шире замера. Если ловишь себя на
фразе «и вообще из этого следует» — перемерь на другом виде входа, а не на том же.

**Пятая — не ошибка, а упущение, пойманное уже во время полёта Т-102.** Смягчение ТЕКСТОМ, которым
Т-100 временно закрыл дыру CB-207 («сверь таблицу `from → to` со своей картой»), я не пометила как
временное — и от этого запись в `CHANGELOG` стала бы ложной ровно в тот момент, когда Т-102 дыру
закроет. **Текст, поставленный вместо механизма, обязан нести срок годности и имя карты, которая его
отменит.**

По завершении пакета или при пороге контекста — возврат куратору с SHA и штатная ротация.
