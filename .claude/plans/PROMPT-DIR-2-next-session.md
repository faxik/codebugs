# Стартовый промпт — держатель направления DIR-2, смена 17

Скопировать целиком в новую сессию. **Правил здесь нет намеренно** — они в нормативных файлах, на
которые указывает handoff. Преемник стартует от источников, а не от пересказа.

---

Ты — преемник держателя направления **DIR-2 «Identity находок»** (уровень (2) каскада планирования),
репозиторий `/home/faxik/w/codebugs`. Смена 17.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-2-2026-08-28-s16.md`. Затем его §0:
открой каждый нормативный источник и сверь handoff с ним; расхождение — чини handoff, не источник.
Скоропортящееся (карты, рабочие копии, захваты карточек, хвост `CASCADE-IDS.md`, `git log`,
состояние `origin/main`) перепроверь на месте.

## Первое действие, до всякой работы

**Сверь очередь с `git log`.** Признак посадки — история, а не пакет. Девятая смена подряд, две
минуты против юнита.

**И второе, оплаченное дважды за прошлую смену: не объявляй ничего отсутствующим, не назвав
наблюдение, которым ты отличаешь «нет» от «есть, но отсюда не видно».** Три моих измерения за смену
дали «одинаковый результат на всех входах» — и каждый раз это была ошибка инструмента, а не свойство
мира: оболочка `zsh` съела часть выражения, и четыре сравниваемых хеша оказались хешем пустого
потока; она же не разбила неквотированную переменную на слова; а пустая фикстура ответила ровно так,
как ответил бы исправленный дефект. **Совпадение, полученное из ошибки, выглядит точно как
совпадение, полученное из истины.**

## Твой первый юнит уже написан и ждёт запуска

`.claude/plans/L3-BRIEF-DIR-2-cb217-archived-only-refusal.md` — форма починки уже решена уровнем (2)
(отказ на противоречивой комбинации, а не две формы, названные картой), довод и его цена записаны, а
поправка независимого разбора уже внесена. **Номер юнита минти сам одним вызовом
`tools/cascade-mint.sh` в момент запуска и впиши в бриф** — номер уходил между вычислением и записью
шесть смен подряд, и лечится это тем, что число не набирают руками.

Дальше по очереди — §4 handoff'а, где порядок объяснён, а не просто перечислен: CB-247 (мелкий
остаток прошлой смены), CB-236 (medium, ядро территории, форма починки — работа уровня (2)),
CB-217, остаток CB-62 (наверх вопросом, НЕ в слот), CB-234. CSV — на дно (К-22).

## Что сделано в смене 16, одной фразой

**Доохват ретроспективного прогона по трём мержам, поручённым куратором, завершён; юнит Т-111
приземлён и принят с оговоркой; заведён и запущен Т-114.** Долг направления, передававшийся две
смены подряд, разряжен. Из трёх мержей доохвата два дали находки; одна исправлена тем же проходом
(две ложные фразы в шапке `CASCADE-IDS.md`, коммит `4c08786`), вторая уже несёт открытую карту.

## Что знать про хук

Владелец подключил перехватывающий хук: `.claude/settings.json` несёт `PreToolUse` →
`~/.claude/hooks/simplify-traced-gate.sh`, и он ОТКАЗЫВАЕТ, если `/simplify-traced` в сессии не
запускался. Клади это прямо в бриф исполнителя, иначе он упрётся в отказ на посадке. Сам файл
настроек, его резервная копия и `tools/install-simplify-gate.py` — владельца, не трогать.

## Три вещи, которые стоили мне времени, — подробности в §8 handoff'а

1. **Перепроверяй инструмент замера прежде, чем читать его ответ.** Три «чистых» результата за смену
   оказались артефактами оболочки и пустой фикстуры. Спрашивай пробу, сколько в ней строк, и меряй
   код возврата не через конвейер.
2. **Заказывай разбор на СУЖДЕНИЯ, а не на артефакты.** Я отдала независимому рецензенту четыре своих
   решения; два возражения изменили работу, одно я отклонила с записанным доводом. Суждение — это
   то, что никто, кроме тебя, не перепроверяет.
3. **У решения владельца есть половины, и резюме может нести не все.** Карта, закрытая его «да»,
   была предъявлена ему одной строкой из двух половин. Разногласие о том, вправе ли я была поправить
   вторую, записано в артефакте дословно — вместе с возражением, которое я не приняла.

По завершении пакета или при пороге контекста — возврат куратору с SHA и штатная ротация.
