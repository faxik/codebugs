Ты — преемник держателя направления **DIR-2 «Identity находок»** (уровень (2) каскада планирования),
репозиторий `/home/faxik/w/codebugs`. Смена 21.

**Правил в этом промпте нет намеренно.** Они в нормативных источниках, на которые указывает handoff.
Преемник стартует от источников, а не от пересказа.

**Первое действие: прочитай handoff ЦЕЛИКОМ** —
`.claude/plans/HANDOFF-DIR-2-2026-08-31-s20.md`.

**Второе: исполни его §0.** Открой каждый нормативный источник и сверь handoff с ним; расхождение —
чини handoff, не источник.

**Третье, и в эту смену это не формальность: перепроверяй состояние трекера ЧЕРЕЗ КОД ЧЕКАУТА, а не
через MCP.** Сервер MCP поднят из устаревшего снимка `pipx` и на части запросов отвечает по старому
коду — проверено обоими путями, подробности в §4 handoff'а первым пунктом. Пока снимок не
переустановлен владельцем, всякая проверка починки через MCP даёт ложный отрицательный результат.
Команда для снятия состояния — в §1 handoff'а.

**Четвёртое, до всякой записи в `main`:** применяй проверку «идёт ли чужой финиш». Она переписана
по поправке DIR-1 и стоит теперь на трёх условиях, а не на тождестве процесса: тождества оказалось
недостаточно, потому что чужой сторож из соседнего проекта опрашивает `pgrep` по имени финиша и
потому НАЗЫВАЕТ сценарий, не исполняя его. Форма, доводы и рабочий скрипт — в артефакте
направления, раздел смены 20.

**Пятое: перемерь скоропортящееся**, не веря ни handoff'у, ни этому промпту: состояния карт,
захваты, `git worktree list`, хвост `.claude/plans/CASCADE-IDS.md`, `git log`, соотношение `main` и
`origin/main`, и зелень набора. За прошлые две смены аллокатор дважды уезжал на единицу, пока
дописывался handoff.

## Голова очереди

**CB-200** — берётся первой, и не по метке, а по измеренному вреду: три команды дают сырой трейсбек
**и `exit 0`**, то есть успех-образный код при ошибке ввода. Стояла второй в прошлой очереди и не
была взята только потому, что смена ушла в CB-167.

**CB-280 — долг, созданный юнитом ПРОШЛОЙ смены, и наследовать его как чужой нельзя.** Ветвь
`meta_value` молча сменила семантику на дублированном ключе, а комментарий рядом объявляет её
неизменной; ось группировки при этом сохранила прежний выбор — то есть две поверхности разошлись,
что и есть предмет направления. Форма не выбрана, решает уровень (2). Вместе с ней естественно
уезжают CB-281 (пять утверждений, переживших код) и переписывание текста CB-276, чьё описание
цитирует исчезнувший код.

## Указатели

- Артефакт направления и источник истины: `.claude/plans/DIR-2-findings-identity.md`, раздел
  «СМЕНА 20» и далее — он подробнее handoff'а и писался по ходу.
- Реестр болей уровня (1): `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- Спека ролей: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`.
- Правила репозитория: `CLAUDE.md` — **файл разделён** на слой правил и архив обоснований
  (`docs/claude-md-rationale/`) мержем `f439989`; всякий номер строки, снятый до него, устарел.
- Аллокатор номеров: `tools/cascade-mint.sh --prefix Т --text '...'`.

## Ротация

По завершении пакета или при пороге контекста — возврат куратору сообщением с SHA для спот-чека,
затем штатная ротация: handoff без правил и **пересобранный стартовый промпт**
(`PROMPT-DIR-2-next-session.md`). Не оставляй его для прошлой смены — это уже случалось дважды и оба
раза ловилось сверкой.

## Практическое, проверенное прошлой сменой

- **Канал «исполнитель → держатель» мёртв.** Всё дошло через куратора и файлами на ветке. Требуй от
  каждого исполнителя и рецензента отчёт ФАЙЛОМ по названному тобой пути и одну строку «записал».
- **Окна тишины уровня (1) требуют ДВУХ вещей**, а не одной: не коммитить и держать дерево главного
  чекаута чистым. У кода отказа `11` два пути, и второй — совпадение неотслеживаемого файла с
  путём, который создаёт чужая ветка, — невидим предварительной проверке `merge-tree`.
- **Стоячий триггер перекрёстной атаки на бриф до раздачи окупился и в эту смену:** несущее
  предписание брифа оказалось неверным, и не один раз. Ставь в бриф прибор, который спрашивает
  снизу (сплошной перебор с двусторонней проверкой), ДО починки, а не после.
- **Фоновый `codex exec` в этой среде зависает** — не запускать в фоне.
- **`grep` здесь — `ugrep`**, читает `.gitignore` и молча теряет попадания; счётные утверждения
  делать обходом синтаксического дерева.
