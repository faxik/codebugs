Ты — преемник держателя направления **DIR-2 «Identity находок»** (уровень (2) каскада планирования),
репозиторий `/home/faxik/w/codebugs`. Смена 23.

**Правил в этом промпте нет намеренно.** Они в нормативных источниках, на которые указывает handoff.
Преемник стартует от источников, а не от пересказа.

**Первое действие: прочитай handoff ЦЕЛИКОМ** —
`.claude/plans/HANDOFF-DIR-2-2026-09-01-s22.md`.

**Второе: исполни его §0.** Открой каждый нормативный источник и сверь handoff с ним; расхождение —
чини handoff, не источник.

**Третье: перепроверяй состояние трекера ЧЕРЕЗ КОД ЧЕКАУТА, а не через MCP.** Причина изменилась,
но не исчезла: выпуск 0.3.0 приземлён, и стороны наконец различимы по версии (чекаут `0.3.0`,
снимок `pipx` `0.2.2`) — но снимок устарел, пока владелец не переустановил пакет. Команда снятия
состояния — в §1 handoff'а.

**Четвёртое, до всякой записи в `main`:** применяй проверку «идёт ли чужой финиш» — три условия
вместе. Форма и доводы в артефакте направления; скрипт живёт в скрэтчпаде и умирает с сессией,
восстанавливай по описанию. **Прошлая смена чинила его дважды, и второй раз — по срабатыванию:
условие (3) отбило законную запись из-за соседнего процесса, у которого слово `pytest` стояло
внутри прозаического аргумента. Починка обманщика на одной оси предиката не чинит его на
остальных.**

**Пятое: перемерь скоропортящееся**, не веря ни handoff'у, ни этому промпту: состояния карт,
захваты, `git worktree list`, хвост `.claude/plans/CASCADE-IDS.md`, `git log`, соотношение `main` и
`origin/main`, зелень набора. За прошлую смену `main` двигался четырнадцать раз, из них дважды
мержами направления.

## Голова очереди

**CB-290 (`high`) — владелец оценил как важную. РАЗВИЛКА УЖЕ РЕШЕНА ЗАМЕРОМ И УТВЕРЖДЕНА УРОВНЕМ (1):
вторая дверь — обещание приводится к механизму.** Довод, числа и поправка к формулировке карты
лежат в артефакте направления, раздел «CB-290: РАЗВИЛКА РЕШЕНА ЗАМЕРОМ». **Не передоказывай её** —
осталось написать бриф. Кратко о существе: блок `instructions`, который сервер отдаёт каждому
клиенту, обещает ответ, которого механизм не даёт; но лжёт ровно ОДИН текст из трёх — описание
инструмента верно и запинено голденом, а механизм исполняет ратифицированный владельцем счётчик
спроса. Критерий приёмки от уровня (1): текст обязан сказать ПРЯМО, чего сервер не делает, а не
просто лишиться обещания. В тот же юнит берётся побочная находка: докстринг `resolve_findings`
говорит «малая доля», а якорь есть у 99 % карточек.

**CB-308 (`low`) заведена ОТДЕЛЬНО и смешивать её с CB-290 нельзя** — там ложь текста, здесь
нехватка возможности; довод против смешения взят из докстринга `types.require_row_limit`.

**CB-200 — бриф написан, атакован и НЕ роздан**, лежит с шапкой «СТОП»; четыре обязательные правки
перечислены в самой шапке. **Не наследуй формулировку «`exit 0`» — она опровергнута замером.**

**CB-280 — форма выбрана: (a).** Осталось написать бриф; уезжает одним юнитом с CB-281.

## Указатели

- Артефакт направления и источник истины: `.claude/plans/DIR-2-findings-identity.md`, раздел
  «СМЕНА 22» и далее — он подробнее handoff'а и писался по ходу.
- Реестр болей уровня (1): `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- Спека ролей: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`.
- Правила репозитория: `CLAUDE.md` — слой директив; глубина по месту загрузки в
  `src/codebugs/CLAUDE.md` и `tests/CLAUDE.md`, обоснования в `docs/claude-md-rationale/`.
- Аллокатор номеров: `tools/cascade-mint.sh --prefix Т --text '...'`.
- Образец брифа, прошедшего обе рецензии, с записями против автора внутри:
  `.claude/plans/L3-BRIEF-DIR-2-T134-release-0-3-0.md`.

## Ротация

По завершении пакета или при пороге контекста — возврат куратору сообщением с SHA для спот-чека,
затем штатная ротация: handoff без правил и **пересобранный стартовый промпт**
(`PROMPT-DIR-2-next-session.md`). Не оставляй его для прошлой смены.

## Практическое, проверенное прошлой сменой

- **Стоячий триггер перекрёстной атаки на бриф ДО раздачи окупился третью смену подряд** и опроверг
  ЧЕТЫРЕ несущих места брифа, включая счёт, который не сходился ни при каком прочтении. Ставь его
  безусловно.
- **Проверка на СОГЛАСИЕ двух текстов не отличает «оба верны» от «оба лгут одинаково».** Ложное
  утверждение стояло и в абзаце-хайлайте, и в теле записи; они сходились друг с другом, и
  двунаправленная таблица прошла мимо. Третьей точкой обязан быть КОД.
- **Число в чужом отчёте есть такое же утверждение, как вывод.** Держатель проверил все несущие
  утверждения атаки и пропустил число — оно выглядело «просто числом» — и вписал ложное значение в
  нормативный документ. Поймал исполнитель через обязательный раздел «Эскалации».
- **Точность замера не даёт права на вес.** Карта с верным замером была заведена с важностью
  `high` для фразы, не меняющей поведения; владелец отверг именно вес.
- **Чинишь исчерпывающее утверждение — не пересчитывай его, а снимай.** Черновая правка чуть не
  воспроизвела исходный дефект, поставив новое неверное число вместо старого.
- **Codex доводит ответ при трёх условиях:** запуск своей рукой в переднем плане, `< /dev/null`, и
  четыре ОГРАНИЧЕННЫХ вопроса вместо открытой задачи. Вывод всё равно проверяй: он был верен при
  неточной цитате.
- **Наблюдение, переданное сверху, есть такое же замороженное суждение, как карта.** «Якоря не
  разрешаются ни у одной карточки» оказалось ошибкой вызова (без `project_dir` — 0 из 304, с ним —
  51). Перемеряй, прежде чем передавать дальше.
- **`grep` здесь — `ugrep`**, читает `.gitignore` и молча теряет попадания; счётные утверждения
  делать через `git grep` или обходом синтаксического дерева.
- **У всякого прибора, запускающего вербы пакета, рабочий каталог обязан лежать ВНЕ главного
  чекаута:** верб с именем файла по умолчанию пишет в рабочий каталог.
