# L3-BRIEF DIR-1 / Т-112 — CB-237: журнал попыток посадки записывает остановленный сигналом финиш как удачную посадку

Держатель направления DIR-1, уровень (2), 2026-08-28, смена 17.
Карта: **CB-237** (`medium`). Откуда взялась: находка ретроспективного прохода `/simplify-traced` по
мержу `dac248e`, пачка Б-доп, смена 16. **Дефект внесён юнитом**, который завёл журнал (CB-176), и
стал ЖИВЫМ только 27 августа, когда владелец подключил предупреждающую вставку `/simplify-traced`:
предписанная процедура — запустить финиш и остановить его самому — порождает ровно то состояние,
которое журнал записывает как удачную посадку.

**Класс по Р1: B (оракул детерминированный: тест обязан краснеть на непочиненном скрипте и зеленеть
на починенном; состояния строятся посылкой сигнала настоящему процессу). Стартовый тир: `opus`.
`effort`: medium.**

> Тир выбран не по размеру правки — она в две строки, — а по тому, где здесь можно ошибиться:
> семантика ловушек `bash` при асинхронном сигнале не выводится чтением, её приходится мерить, и
> одна из трёх названных картой форм УЖЕ опровергнута замером (см. §2). Плюс правится скрипт,
> которым приземляется вся работа проекта. **Объём отдавай дешёвым субагентам** (`sonnet`/`haiku`):
> прогоны проб с убийством процессов, снятие счётчиков из журнала, чтение файлов. Себе оставь
> решение о форме теста и разбор того, что покажут прогоны.

Ветка: `fix/cb-237-landing-journal-signal-status`.

Условий очерёдности нет. Параллельно идёт направление DIR-2 в дереве
`.worktrees/fix-cb-241-cb-242-text-truth` — файлов оно с тобой не делит, но **`main` может уехать
вперёд, пока ты работаешь**, и это нормальный ход: `exit 13` на финише лечится буквальным повтором
с тем же `--merge-msg`.

---

## §1. Замысел

`.worktrees/landing-attempts.log` — единственный прибор, которым проект знает, во сколько попыток
обходится одна посадка на `main`. Число из него уже напечатано в комментарии самого скрипта как
готовая команда для читателя, то есть предназначено человеку, а не только машине.

Прибор врёт, и врёт в льстящую сторону: попытка, остановленная сигналом завершения, записывается
кодом ноль — как удачная посадка. Значит числитель («посадок») завышен, знаменатель («попыток на
посадку») занижен, и мы считаем себя аккуратнее, чем есть. **Хуже всего то, что врать он начал от
нашей же дисциплины**: чем добросовестнее исполнитель останавливает финиш ради прохода
`/simplify-traced`, тем сильнее ошибается прибор.

**Чего юнит НЕ делает, и это правильно.** Он не переписывает `_journal_record`, не трогает
`_alarm_speak` и не меняет ни одного кода возврата самого финиша. Он не пытается поймать `SIGKILL`
— его не ловит никто, и это уже названо границей в комментарии файла. И он не задним числом чинит
уже записанные строки журнала: прошлое остаётся как есть, а неверная строка на `docs-release-0-2-2`
названа в карте поимённо, так что читатель может её вычесть.

---

## §2. Что я измерил лично 2026-08-28

Все замеры мои, на этой машине, `GNU bash 5.3.9(1)-release`. Скрипты проб лежали в скрападе;
воспроизводятся текстом ниже за минуту.

**(1) Дефект воспроизведён.** Минимальный скрипт с единственной ловушкой `trap rec EXIT`, где
`rec` читает `local rc=$?`, при `kill -TERM`: **статус процесса 143, в журнал записано `rc=0`.**
Повторено дважды — сигналом одному процессу и сигналом группе процессов; результат одинаков.

**(2) Форма (а) из карты — «добавить ловушки на INT и TERM, записывающие 130 и 143 явным
аргументом» — ОПРОВЕРГНУТА замером.** С `trap "rec 143; exit 143" TERM` при сохранённой
`trap rec EXIT` в журнал уходит **ДВЕ строки**, обе `rc=143`: сначала пишет обработчик сигнала,
потом ещё раз ловушка выхода. То есть наивная форма (а) чинит завышение посадок и заводит завышение
попыток. Карта называла эту опасность словом «надо проверить, что двойного срабатывания не будет» —
проверено, оно есть.

**(3) Минимальная форма работает, и она короче обеих.** С `trap "exit 143" TERM` при **нетронутой**
`trap rec EXIT` и **нетронутой** функции записи: **одна строка, `rc=143`**, статус процесса 143.
Механизм: `exit N` внутри обработчика сигнала выставляет статус, и ловушка выхода читает своим
`$?` уже его. Ничего передавать аргументом не нужно.

**(4) Обычный путь не сломан.** Тот же скрипт с ловушками на сигналы, завершившийся `exit 7` сам:
**одна строка, `rc=7`**. То есть добавление ловушек на сигналы не меняет ничего на нормальном пути.

**(5) ПУТЬ `INT` У МЕНЯ НЕ УСТАНОВЛЕН, И Я НЕ ВЫДАЮ ЭТО ЗА ЗНАНИЕ.** Ни в одной из проб мне не
удалось заставить `bash` подействовать на `SIGINT`: со `trap "exit 130" INT` и работающей дочерней
командой на переднем плане скрипт **доходил до конца и уходил статусом 0**, а в журнал попадал
`rc=0`. Причина, скорее всего, в моей оснастке — фоновый запуск без управляющего терминала, — а не
в самом механизме. **Это ДАННЫЕ о моей пробе, а не факт о `bash`.** Мерить путь `INT` — твоя
работа, и §4 говорит, что делать, если он поведёт себя иначе.

**(6) Живые данные, снятые сегодня.** В `.worktrees/landing-attempts.log` **19 строк и 7 нулей**
(карта смены 16 видела 15 и 6 — с тех пор село ещё три строки юнита Т-110 и одна чужая). Настоящих
посадок по этим слагам **шесть**. Лишний ноль — по-прежнему один и тот же:
`docs-release-0-2-2` в 18:55:45, финиш, который смена 15 сама записала как остановленный и
перезапущенный. **Замер повторяемый и расхождение не рассосалось — оно растёт вместе с журналом.**

---

## §3. Форма выбрана уровнем (2)

**Форма: ловушки на `INT` и `TERM`, тело которых состоит ТОЛЬКО из `exit <код>`; ловушка `EXIT` и
функция `_journal_record` не меняются ни на символ.** Коды — `130` для `INT` и `143` для `TERM`,
то есть `128 + номер сигнала`, как их печатает и оболочка.

Обязательные свойства формы, каждое отдельным пунктом:

1. **В теле обработчика сигнала не должно быть вызова записи.** Это прямое следствие замера (2):
   вызов там даёт вторую строку. Обработчик только выставляет статус, пишет — ловушка выхода.
2. **Ловушки на сигналы ставятся ровно там же, где стоит `trap _journal_record EXIT`** (строка 138),
   и ни строкой выше: выше по файлу ловушки ещё нет, и комментарий на строках 90–99 перечисляет
   пути выхода, которые линию теряют. Этот перечень надо **дополнить**, а не переписать.
3. **Это АЛЯРМ, а не гейт.** Ни один код возврата финиша не меняется, ничего не отказывает; юнит
   лишь заставляет прибор записывать то, что произошло. Скажи это словом в комментарии.
4. **Комментарий рядом обязан назвать механизм, а не намерение**: почему `exit` в обработчике
   достаточно и почему запись в обработчике — ошибка. Формулировку «двойная запись измерена»
   подкрепи собственным прогоном, а не ссылкой на этот бриф.
5. **Взаимодействие с алярмом посадки назвать явно.** После мержа скрипт ЗАМЕНЯЕТ ловушку выхода
   своей — `trap _alarm_speak EXIT` (строка 682), потому что у `bash` ловушка `EXIT` одна.
   Значит сигнал, пришедший ПОСЛЕ этой замены, пойдёт через алярм, который зовёт
   `_journal_record 15`. **Измерь, что там записывается, и назови результат словами** — либо в
   комментарии, либо в перечне путей выхода. Молча предполагать нельзя: это ровно тот шов, на
   котором дефект и живёт.

**Что решено НЕ делать, и соблазны, которые ты встретишь.**

- **Не форма (а).** Опровергнута замером (2), см. выше.
- **Не форма (в)** («объявить границу словами, прибор оставить как есть»): число уже напечатано и
  уже неверно, а объявление границы не чинит напечатанное.
- **Не переписывать `_journal_record`.** Она уже умеет принимать код аргументом, и соблазн
  «раз умеет — воспользуемся» ведёт прямо в двойную запись.
- **Не чинить прошлые строки журнала.** Журнал — запись событий, а не отчёт; правка задним числом
  уничтожит единственное свидетельство того, что дефект был.
- **Не расширять юнит на `SIGHUP`, `SIGQUIT` и прочее без замера.** Если найдёшь основание —
  это эскалация с числом, а не попутное расширение.

**Форма теста выбирается тобой, но критерий жёсткий и невыполнимый обходом: тест обязан быть
КРАСНЫМ на непочиненном скрипте.** Два пути, оба законны, назови выбранный и почему:
**(i)** прогон настоящего `tools/worktree-finish.sh` в одноразовом репозитории с посылкой сигнала —
образец такого прогона в наборе уже есть (`TestMergeSubjectDerivation` гоняет скрипт от начала до
конца в одноразовом репозитории под `--skip-checks`); **(ii)** оснастка, которая ИЗВЛЕКАЕТ строки `trap` из самого
`tools/worktree-finish.sh` и исполняет их в заглушке. Путь (ii) допустим **только** если строки
именно извлекаются из файла: рукописный двойник запрещён правилом репозитория — «дублированная
реализация на один дрейф отстоит от расхождения с оригиналом», и этот же довод стоит в докстринге
`_drift_message` соседнего юнита. Структурный тест («в файле есть строка `trap … TERM`») сам по себе
критерия НЕ закрывает: он зелен и на форме (а), которая пишет две строки.

---

## §4. Канон К-18 — предпосылка самой правки

Правка, написанная как защита от ошибки, проверяется хуже обычной: она приходит с готовым
оправданием. Предпосылки этого юнита поимённо, и каждую **построй, а не выведи из кода**:

- **П1.** Ловушка `EXIT`, сработавшая по асинхронному сигналу, читает `$?` последней завершившейся
  команды, а не статус, с которым уйдёт оболочка. (Мой замер (1).)
- **П2.** `exit N` внутри обработчика сигнала делает `$?` в ловушке `EXIT` равным `N`. (Мой замер
  (3).) **Если это не воспроизведётся на твоей версии `bash` — вся выбранная форма неверна, и это
  эскалация, а не повод сочинить обход.**
- **П3.** Запись внутри обработчика сигнала при сохранённой ловушке `EXIT` даёт ДВЕ строки. (Мой
  замер (2).)
- **П4.** У `bash` ровно одна ловушка `EXIT`, и вторая стирает первую. Это утверждение живёт в
  комментарии самого скрипта (строки ~572–573) — **проверь его, а не процитируй**: на нём стоит
  пункт 5 §3.
- **П5.** Путь `INT`. У меня он **не установлен** (замер (5)). Установи его сам. Возможных исходов
  три, и все три законны: ловушка срабатывает и пишет 130 — хорошо; ловушка не срабатывает по
  причине, которую ты назовёшь, — тогда **напиши это границей в комментарии, а не обещай починку**;
  поведение зависит от наличия управляющего терминала — назови обе ветки. **Обещать в комментарии
  то, чего ты не измерил, — самостоятельный дефект на этом направлении.**

---

## §5. Оракул

**Обязано КРАСНЕТЬ после юнита и быть ЗЕЛЁНЫМ до — покажи оба состояния:**

1. **Мутант «вернуть как было»:** убрать добавленные ловушки на сигналы. Тест обязан покраснеть,
   и сообщение обязано называть записанный код (`0` вместо `143`), а не просто «не равно».
2. **Мутант формы (а):** заменить `trap "exit 143" TERM` на `trap "_journal_record 143; exit 143" TERM`.
   Тест обязан покраснеть **по числу строк**. Этот мутант — главный: он отличает выбранную форму от
   отвергнутой, и без него юнит доказывает только «что-то пишется».

**Обязано ПРОХОДИТЬ:**

- весь набор `tests/test_worktree_harness.py` — в нём лежат структурные проверки того, что скрипт
  зовёт каждого стража; ни один из них юнит трогать не должен;
- полный набор целиком, `ruff` чист на закреплённой версии `0.15.7`;
- **нормальный путь**: финиш, завершившийся сам, по-прежнему пишет свой настоящий код (мой замер
  (4) — воспроизведи его на настоящем скрипте, а не на минимальном).

**Строка, которая на этом направлении дважды приносила больше всего: то же состояние, полученное
ДРУГИМ способом.** Найди, сколькими способами останавливается финиш на самом деле — сигнал
инструменту оболочки, истечение отведённого времени, закрытие сессии, — и скажи, сколько из них
покрыто твоим тестом, а сколько нет. Непокрытые назови словами; непокрытый и названный способ стоит
дешевле непокрытого и неназванного.

**Мутации — только в отдельном дереве** (К-5, К-15), `PYTHONDONTWRITEBYTECODE=1`, чистый
`__pycache__`, `grep -c` в подтверждение применения. **Ловушка:** проба, запущенная указанием пути
к тестам без смены рабочего каталога внутрь копии, молча измеряет главный чекаут.

---

## §6. Регламент

- Только worktree: `tools/worktree-setup.sh fix/cb-237-landing-journal-signal-status`. **Флага
  `--items` у этого скрипта НЕТ, и `tools/worktree-probe.sh` в этом репозитории не существует** —
  оба из соседнего проекта. Проверяй существование **флага**, а не только команды.
- **Уровень (3) на `main` не коммитит — никогда (К-1).** Исключение одно, К-17: один прямой коммит
  **только в этот файл брифа**, поимённо.
- **Коммить рано и часто.** Признак приземления — `git log`, никогда не уведомление.
- `uv run --extra dev python -m pytest tests/ -q` **в worktree**; `--extra dev` не опционально.
  `ruff check src/ tests/` чист, ruff ровно `0.15.7`.
- **Не запускай `uv run` в главном чекауте** — там может идти чужой финиш, который сверяет
  интерпретатор под замком. Для разовых замеров зови бинарник напрямую: `./.venv/bin/python`.
- **`CHANGELOG.md`, раздел `## [Unreleased]`** — обязателен, пользовательским языком. Запись должна
  сказать человеку, что число «попыток на посадку» до этой правки было занижено, и не обещать, что
  прошлые строки журнала исправлены.
- **Карту в трекере не закрывать** (К-1) — закрывает держатель по итогам приёмки.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **ПЕРЕД финишем — проход `/simplify-traced` на ветке**, именно он, а не голый `/simplify`.
  Результат — обязательный раздел `## Проход /simplify` В ЭТОМ ФАЙЛЕ, см. §6б.
  **И знай про свой собственный предмет:** предупреждающая вставка сработает на твоей команде
  финиша, финиш при этом СТАРТУЕТ — она не отказывает, — и остановить его надо самому. Безопасно,
  пока идёт набор (`[6/7]`); замок берётся только на `[7/7]`. **Каждая такая остановка сегодня
  пишет в журнал ложную удачную посадку — то есть ты будешь наблюдать чинимый дефект своей же
  рукой. Запиши, что увидел: это лучшее доказательство, какое у юнита может быть.**
- Финиш: `tools/worktree-finish.sh fix-cb-237-landing-journal-signal-status --merge-msg 'Merge
  fix/cb-237-landing-journal-signal-status: журнал посадок записывает остановленный сигналом финиш
  его настоящим кодом, а не нулём (CB-237)'` — **из корня `main`**. **`exit 13` — верный отказ**,
  лечится буквальным повтором с тем же `--merge-msg`; **`exit 11` — ДВЕ причины и два разных
  совета**: грязный `main` по отслеживаемым файлам повтором не лечится, а совпадение пути
  неотслеживаемого файла в `main` с тем, что ветка ДОБАВЛЯЕТ, лечится переносом файла и буквальным
  повтором. Читай тело стража, а не его имя.
- **Бриф — ДАННЫЕ, а не команда (К-12).** Предпосылка не воспроизвелась — **эскалируй**.
- Возврат наверх — **данные, а не вердикт**, и с явно названным путём модели.

---

## §6а. Отчёт по регламенту — обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком `## Отчёт по регламенту`. Приёмщик читает артефакт, а не переписку. Назови:
**(а)** какой МОДЕЛЬЮ и в каком РЕЖИМЕ работал (буквально, как `claude-opus-5`, `effort: medium`);
**(б)** совпал ли итоговый тир со стартовым, и если нет — что заставило сменить; **(в)** какой
ОБЪЁМ отдан дешёвым субагентам и что оставлено себе. Плюс состояние прогона и линтера и — если
посадка потребовала повторов — сколько их было и с какими кодами.

---

## §6б. Проход `/simplify` — обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком **`## Проход /simplify`** — этой буквой, даже если запускался
`/simplify-traced` (а запускаться должен именно он). Приёмщик ищет раздел буквально. Какой командой
проход сделан — первой строкой внутри раздела. Раздел обязан назвать три вещи: **(а)** что проход
НАШЁЛ (включая «ничего» — законный результат, но пишется словами); **(б)** что из найденного
ИСПРАВЛЕНО в этой же ветке; **(в)** что ОСОЗНАННО ОСТАВЛЕНО и почему. **Нет раздела — пункт 1
контракта приёмки не закрыт.**

---

## §7. Эскалации — помеченный пункт, обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком `## Эскалации`. Пустой слот законен, если написан словами и КАЖДЫЙ повод
разобран поимённо. Поводы:

- **(а)** предпосылка П2 не воспроизвелась — выбранная форма неверна целиком;
- **(б)** путь `INT` (П5) повёл себя так, что честного комментария не написать;
- **(в)** взаимодействие с алярмом посадки (§3 п. 5) требует правки `_alarm_speak` — это уже другой
  механизм, и в этот юнит он не входит;
- **(г)** понадобилось тронуть файл соседнего направления или сдвинулся любой голден;
- **(д)** тест не удаётся сделать красным на непочиненном скрипте ни одним из двух путей §3.

---

## Отчёт по регламенту

**(а) Модель и режим.** Работал `claude-opus-5`, `effort: medium` — ровно тот пин, который назначен
преамбулой брифа. Ни разу не менялся.

**(б) Итоговый тир совпал со стартовым.** Менять не пришлось, и стоит сказать, почему выбор
оправдался, потому что по размеру правки он выглядит избыточным: исполняемого кода в ней ровно две
строки. Оправдался он тем, что поведение ловушек оболочки `bash` при асинхронном сигнале — то есть
при сигнале, пришедшем в произвольный момент извне, — действительно не выводится чтением, и это
подтвердилось дважды, в обе стороны. Путь `INT` (реакция на прерывание с клавиатуры, `Ctrl-C`),
который держатель направления не смог установить у себя, у меня сначала тоже «не работал» — и
причиной оказалась оснастка пробы, а не сам механизм. А сигнал `SIGQUIT` (аварийное завершение по
`Ctrl-\`), который я уже собирался одной фразой объединить с `SIGHUP` (обрыв сессии), повёл себя
противоположно и заставил переписать уже написанный комментарий. Оба места я описал бы неверно,
если бы рассуждал, а не мерил.

**(в) Что отдано дешёвому тиру и что оставлено себе.** Вниз, на `sonnet`, ушли четыре отдельные
работы, и все четыре — объём, а не суждение:

1. матрица проб с убийством процессов: восемь сценариев, каждый по два прогона;
2. разведка по набору тестов — перечень классов файла `tests/test_worktree_harness.py`, устройство
   общей оснастки `armed` (она собирает одноразовый репозиторий, в котором скрипт финиша можно
   прогнать целиком), поиск всех существующих упоминаний журнала;
3. прогон двух мутантов в отдельной копии дерева, с подтверждением того, что правка применилась, и с
   дословным снятием сообщений об ошибках;
4. четыре угла обязательного прохода упрощения (повторное использование, упрощение, эффективность,
   высота решения) — четырьмя параллельными агентами.

Себе я оставил: выбор формы теста; все пять предпосылок §4 — их я перемерил лично, не полагаясь на
агента, потому что на них стоит вся выбранная форма; измерение шва с алярмом посадки; измерение цены
выбранной формы для вызывающей оболочки; решающий опыт, разделивший две объяснительные версии пути
`INT`; и решение о правке существующего теста.

**Отдельной строкой, потому что для регламента это важнее остального в этом пункте: дешёвый тир
вернул верные числа и неверную причину.** Агент матрицы измерил, что ловушка на `INT` срабатывает
только тогда, когда у процесса есть настоящий управляющий терминал, и заключил, что дело в терминале.
Я поставил разделяющий опыт — запуск без терминала, но с реакцией на `SIGINT`, унаследованной от
родителя как «игнорировать», — и получил два ответа: при обычной унаследованной реакции ловушка
срабатывает и без всякого терминала (процесс уходит кодом 130, в журнал садится 130), а при
унаследованном игнорировании не срабатывает и при терминале. Причина — унаследованная реакция;
терминал в пробе агента был совпадением, а не механизмом. Вывод, который стоит унести из юнита:
объяснение, возвращённое субагентом, проверяется отдельно от возвращённых им чисел, и стоит это один
разделяющий опыт.

**Состояние прогона и линтера.**

- `ruff check src/ tests/` — чисто; версия ровно `0.15.7`, как требует бриф.
- `tests/test_worktree_harness.py` — 320 тестов пройдено (до юнита в этом файле было 318).
- Полный набор в рабочем дереве — **3116 пройдено**, около 3 минут 15 секунд.
- Стоит записать и вот что, потому что прибор сработал на мне самом. Первый полный прогон я запустил
  в фоне и правил файл, пока он шёл. Сигнализация о подвижном дереве — она сверяет отпечаток файлов
  до и после прогона, печатает расхождение и никогда не трогает код возврата — это заметила и
  сказала. Прогон был перезапущен на неподвижном дереве, и итоговые 3116 сняты именно с него.

**Посадка.** Заполняется после интеграции — см. дописку ниже.

---

## Проход /simplify

Команда: **`/simplify-traced`** — именно он, а не голый `/simplify`: он запускает `/simplify` первой
фазой и затем отдельной фазой сверяет получившиеся изменения с исходной задачей. Проход выполнен по
ветке, до финиша.

**(а) Что проход нашёл.**

Фаза упрощения шла четырьмя независимыми углами, каждый — отдельным агентом:

- *Повторное использование* — ничего. Проверено предметно: механизм, который берёт замок интеграции
  и шлёт сигнал группе процессов, в наборе тестов не встречается больше нигде, а четыре
  существующих помощника, запускающих скрипт финиша, устроены синхронно и по устройству не могут
  отдать управление, пока скрипт не завершился, — то есть для этой задачи непригодны.
- *Упрощение* — одна находка. В новом помощнике перед сообщением об ошибке стояло ожидание
  завершения процесса, которое ничего не давало: уборка в блоке `finally` и так забирает процесс, а
  в единственном случае, ради которого это сообщение написано, ожидание подменяло внятную
  диагностику непрозрачным истечением времени без единой строки собранного вывода.
- *Эффективность* — одна находка, и содержательная. Два чтения потока вывода скрипта ничем не
  ограничены по времени, а в наборе нет общего предела времени на тест, так что зависший до
  отметки `[7/7]` скрипт подвесил бы весь прогон вместо того, чтобы уронить один тест. Стоимость
  самих тестов при этом измерена и оказалась незначимой: два новых теста добавляют около 0,6 секунды
  к классу и остаются на дешёвом краю распределения по этому файлу.
- *Высота решения* — по коду ничего. Главное возражение, которого я ждал, разобрано и снято:
  ловушки на два названных сигнала — не то «перечисление», против которого предостерегает
  `CLAUDE.md` проекта, потому что список сигналов конечен и обозрим, а границы по нему в комментарии
  проведены поимённо и с замерами. Проверено и второе: у ловушки выхода в `bash` нет способа узнать,
  что её позвал сигнал, — значит «починить функцию записи» не более глубокий слой, а несуществующий.
  Одно замечание было по тексту, и справедливое (см. пункт «б»).
- *Фаза сверки с исходной задачей* — одно расхождение, и оно объявлено, а не найдено проходом: я
  изменил ожидаемый список в существующем тесте. Разбор — в разделе «Эскалации».

**(б) Что из найденного исправлено в этой же ветке.**

1. Лишнее ожидание перед сообщением об ошибке снято; на его месте стоит объяснение, почему его там
   быть не должно.
2. Два неограниченных чтения ограничены снаружи сторожевым таймером: по истечении 90 секунд он
   убивает группу процессов, поток закрывается, оба чтения возвращаются, и тест падает с внятным
   сообщением вместо того, чтобы подвесить прогон. Мера взята с большим запасом — измеренное время
   до отметки около 0,2 секунды.
3. По замечанию о высоте текста переписан абзац про `SIGHUP`: теперь он называет довод ПРОТИВ
   отказа от ловушки, а не только довод за. Заодно туда добавлен `SIGPIPE`, который я измерил сам
   после того, как матрица показала на него: он тоже садится в журнал нулём.
4. **Сквозной перечит нового комментария как одного текста — уже после всех правок по углам — нашёл
   ещё два дефекта, и оба на стыках, то есть ровно там, где их и предсказывает правило.** Первый
   содержательный: `SIGPIPE`, добавленный правкой (3) в нижний абзац, не попал в верхний перечень
   путей, теряющих строку, — а этот перечень читатель и открывает; одно правило в двух местах, на
   один шаг от расхождения. Второй косметический, но в файле, чей предмет — точность, он тоже
   считается: от последней правки осталась осиротевшая строка с единственным словом. Оба закрыты.
   Ни один из четырёх углов их не видел: каждый смотрел свой участок, а дефект жил в композиции.

**(в) Что осознанно оставлено, и почему.**

- **Третья фаза прохода (`/reflect`) не запускалась.** Формально она уже расконсервирована: срок
  приостановки истёк, а инструмент, из-за затора в котором её приостанавливали, на проверке сообщает
  «складывать нечего». Не запускал я её по другой причине: она самостоятельно заводит карточки в
  трекере, а трекер в этом юните мне не принадлежит — бриф прямо оставляет распоряжение карточками
  держателю. Решение объявлено здесь, а не умолчано.
- **Ловушки на `SIGHUP` и `SIGPIPE` не добавлены.** Замер говорит, что та же однострочная форма их
  чинит. Держит границу не отсутствие доказательств, а объём поручения — §3 брифа прямо запрещает
  попутное расширение и требует эскалации с числом. Число ниже.
- **Параметризация двух новых тестов в один отвергнута.** Они выглядят почти одинаковыми, но каждый
  несёт свой довод, почему он существует отдельно, и объединение вытеснило бы эти доводы из места,
  где их читают.

---

## Эскалации

Слот пустой в том смысле, что ни один из пяти названных §7 поводов не сработал. Разбираю каждый
поимённо, как требует бриф, и добавляю два пункта, которых в списке не было.

- **(а) Предпосылка П2 не воспроизвелась — не наступило.** Воспроизвелась на этой машине, на
  `GNU bash 5.3.9(1)-release`, с первого раза: при ловушке `trap 'exit 143' TERM` и нетронутой
  ловушке выхода в журнал садится одна строка с кодом 143, процесс уходит кодом 143. Заодно
  подтвердились П1 (без ловушки — процесс уходит 143, а строка говорит 0) и П3 (запись внутри
  обработчика даёт две строки). П4 — что у оболочки ровно одна ловушка выхода и вторая стирает
  первую — я тоже проверил своим прогоном, а не процитировал: сработала только вторая.
- **(б) Путь `INT` повёл себя так, что честного комментария не написать — не наступило,
  наоборот.** Путь установлен: ловушка срабатывает, процесс уходит кодом 130, в журнал садится 130.
  Несрабатывание у держателя объясняется полностью и проверяемо: он запускал пробу фоновой задачей
  неинтерактивной оболочки, а такая задача наследует реакцию на `SIGINT` как «игнорировать», и
  ловушку на уже игнорируемый сигнал `bash` не ставит вовсе. Мой тест запускает процесс не так, а
  через порождение из Python, где реакция остаётся обычной — той же, что даёт процессу терминал
  оператора. Обещаний, которых я не мерил, в комментарии нет.
- **(в) Взаимодействие с алярмом посадки требует правки `_alarm_speak` — не наступило.** Правка не
  понадобилась, шов измерен целиком, тремя состояниями. До слияния (и это единственная фаза, в
  которой финиш реально останавливают) строка теперь несёт 130 или 143 вместо нуля. Сразу после
  слияния, пока вердикт алярма ещё «не смог прочитать», алярм пишет 15 явным аргументом — мои
  ловушки этого не меняют, и 15 уже описан в скрипте как посадка с неподтверждённой предпосылкой.
  После слияния, когда вердикт уже чист, алярм отдаёт журналу свой `$?`, и строка несёт 143 при
  фактически состоявшемся слиянии. Последнее — настоящая цена, и она названа словами прямо в
  комментарии: это недосчёт посадок, то есть ошибка в сторону, обратную чинимой и безопасную.
- **(г) Понадобилось тронуть файл соседнего направления или сдвинулся голден — не наступило.**
  Тронуты ровно три файла: `tools/worktree-finish.sh`, `tests/test_worktree_harness.py`,
  `CHANGELOG.md`, плюс этот файл брифа. Ни один голден не сдвигался.
- **(д) Тест не удаётся сделать красным на непочиненном скрипте — не наступило.** Оба обязательных
  мутанта покраснели, в отдельной копии дерева. Мутант «вернуть как было»: оба новых теста красные,
  и сообщение называет записанный код дословно — журнал держит строку с кодом `'0'` там, где ждали
  `'143'` и `'130'`. Мутант отвергнутой формы (запись внутри обработчика): красный по числу строк —
  две строки вместо одной, обе с кодом 143. Второй мутант различающий: он ломает только тест на
  `TERM` и оставляет зелёным тест на `INT`, потому что я мутировал только одну из двух ловушек.

**Первое, чего в списке не было, и это отступление от буквы §5.** §5 требует, чтобы весь
`tests/test_worktree_harness.py` проходил и чтобы юнит не трогал его проверки. Один существующий
тест пришлось изменить механически, и молчать об этом нельзя. Речь о тесте
`test_the_alarm_is_delivered_by_an_exit_trap`, который проверяет, что алярм посадки доставляется
именно ловушкой выхода. Он собирает все строки скрипта, начинающиеся со слова `trap`, и сверяет
список с эталоном из двух элементов. Две новые ловушки делают список четырёхэлементным, поэтому тест
краснеет от самого факта правки. Возможностей было две. Сузить сбор до ловушек выхода — дёшево и
зелено, но это ослабление: тест перестал бы видеть враждебную ловушку на любом другом сигнале.
Расширить эталон до четырёх строк — и тогда он вдобавок закрепляет, что обе новые ловушки
существуют и стоят между ловушкой журнала и ловушкой алярма, то есть ровно там, где §3 п. 2 велит им
стоять. Я выбрал второе, и рядом с изменением стоит абзац, объясняющий выбор, — по образцу того,
как в этом же тесте объяснено предыдущее его расширение. Смысл требования §5 (не ослаблять проверки
скрипта) соблюдён; буква — нет, и я сообщаю об этом сам.

**Второе — предложение с числом, ровно в той форме, которую требует §3.** Замерено, по два прогона
на случай: `SIGHUP` (обрыв сессии) и `SIGPIPE` (закрытие приёмника вывода) сегодня садятся в журнал
кодом `0`, то есть тоже читаются как посадка. Та же однострочная форма чинит оба: `trap 'exit 129'
HUP` даёт 129. Итого путей, дающих ложную посадку, четыре: `INT`, `TERM`, `HUP`, `PIPE`; этот юнит
закрывает два. Ещё два измерены и в починку не входят: `SIGQUIT` пишет правду (131) без всякой
ловушки — оболочка его игнорирует, гибнет команда переднего плана, и её код доносит до строки
`set -e`; `SIGKILL` не ловится вовсе и не оставляет строки совсем, то есть недосчитывает попытки, а
не завышает посадки. Решение о расширении — держателя.
