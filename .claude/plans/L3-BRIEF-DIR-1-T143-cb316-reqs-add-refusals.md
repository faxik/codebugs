# Бриф юнита Т-143 — CB-316: отказы поверхности создания требования

```
class:   исполнение по готовой тройке (уровень (3) каскада планирования)
model:   opus
effort:  high
branch:  fix/cb-316-reqs-add-refusals
worktree: /home/faxik/w/codebugs/.worktrees/fix-cb-316-reqs-add-refusals
card:    CB-316 (high, error_handling) — клеймо держит эта ветка
package: П-1 документа ревизии, остаток после посадки Т-140
```

## 0. Что это за работа и почему она существует

Ты исполнитель одного юнита. Юнит — остаток пакета **П-1** документа ревизии кода и архитектуры
(`.claude/plans/REVISION-2026-09-06-code-and-architecture.md`, §5, третья сборка `6b1957a`,
переписанная по вердикту кросс-модельной атаки). Пакет называется «Один источник классификации
отказов: принять приземлившееся и достроить непокрытое».

**Бо́льшая часть пакета уже приземлилась и её строить не надо.** Юнит Т-140 (карта CB-311) влит в
`main` мержем `6202da4`: появился модуль `src/codebugs/refusals.py` — единственный источник правды о
том, какое исключение считается *ожидаемым отказом* (текст написан для человека и его показывают),
а какое *аварией* (текст придерживают, в журнал уходит трассировка). Обе поверхности — командная
строка и протокол MCP — выводят своё поведение из этой таблицы. Сторож полноты построен, тесты есть.

Ветка Т-140 **не трогала файл `src/codebugs/reqs.py` вовсе**, и потому в нём остались жить две
половины, ради которых существует твой юнит.

## 1. Что именно сломано — замерено держателем, а не пересказано

Все замеры сделаны 2026-09-07 на вершине `main` `acb010f`. Воспроизведи их сам первым делом: если
хоть один не воспроизводится, останавливайся и пиши держателю, потому что тогда описание протухло.

### Половина (а) — повторный идентификатор требования падает аварией

```
uv run codebugs --tracker-root <пустой каталог> init
uv run codebugs --tracker-root <тот же каталог> reqs-add FR-1 -d "first"    # Added: FR-1
uv run codebugs --tracker-root <тот же каталог> reqs-add FR-1 -d "second"   # ← дефект
```

Второй вызов печатает **полную аварийную трассировку**, заканчивающуюся строкой
`sqlite3.IntegrityError: UNIQUE constraint failed: requirements.id`.

Механизм, целиком: функция `add_requirement` (`src/codebugs/reqs.py:107-161`) — единственная точка
записи требования, общая для обеих поверхностей, — не проверяет существование идентификатора и
ничем не защищает вставку. Единственная защита от дубликата — ограничение `PRIMARY KEY` на колонке
`id` в схеме таблицы (`reqs.py:26`). Нарушение этого ограничения библиотека доступа к базе SQLite
поднимает как исключение класса `sqlite3.IntegrityError`, и оно уходит вызывающему как есть.

**Почему это хуже, чем «некрасивый текст», и почему карта заведена как `high`.** Класс
`sqlite3.IntegrityError` — чужой для пакета: его объявляет стандартная библиотека, а не этот проект.
Единый источник `refusals.CLASSIFICATION` называет его? Нет. Значит по правилу таблицы («класс, не
названный в таблице, — авария, что бы он ни наследовал») он классифицируется как авария. Проверено
вызовом:

```
uv run python -c "import sqlite3; from codebugs import refusals; print(refusals.kind_of(sqlite3.IntegrityError))"
→ crash
```

Адаптер регистрации инструментов `server._RefusalsReachTheClient` переводит в читаемое клиентом
исключение `ToolError` **только** классы, названные отказами. Авария уходит клиенту обезличенной.
А по собственному замеру проекта, записанному в шапке `refusals.py` (строки 11–15), под библиотекой
`mcp` версии 2.1.1 авария доходит до клиента **вовсе без текста**, тогда как под 2.0.0 SDK
приписывает текст исключения к своему сообщению. В окружении разработки установлена 2.0.0, у
владельца — 2.1.1.

**Следствие, которое ты обязан перепроверить экспериментом, а не принять на веру:** то, что корпус
транскриптов за полгода записал как «сырой текст SQLite», на реальной установке владельца, скорее
всего, уже превратилось в полную потерю текста — то есть в тот самый вред, ради которого заводилась
карта CB-310, но на пути, которого её починка принципиально не достаёт: починка несёт текст только
для классов, названных в таблице.

**Улика важности — замер, а не оценка.** Реестр болей `.claude/plans/PAIN-REGISTRY-codebugs.md`,
§1б: в корпусе реальных вызовов за полгода с 2026-03-06 текст
`reqs_add: UNIQUE constraint failed: requirements.id` встретился **38 раз** — второй по частоте
текст отказа после потери текста как таковой.

### Половина (б) — обработчик создания выпал из общего помощника обработки ошибок

```
uv run codebugs --tracker-root <каталог> reqs-add FR-2 -d "x" --priority bogus
```

Печатает полную трассировку, заканчивающуюся `ValueError: Invalid priority: 'bogus'`.

Механизм: обработчик командной строки `_cmd_reqs_add` (`reqs.py:1001-1011`) не обёрнут в общий
помощник `cli.domain_errors`, тогда как соседи по файлу — `_cmd_reqs_update`, `_cmd_reqs_query`,
`_cmd_reqs_get`, `_cmd_reqs_verify` — обёрнуты. Помощник делает ровно две вещи в строго заданном
порядке: сперва пропускает без изменений аварии, которые наследуют от отказов (сегодня это ровно
`json.JSONDecodeError` — падение уже ПОСЛЕ коммита записи, при сериализации ответа), и только затем
ловит отказы по вводу, печатает одну строку в поток ошибок и завершает процесс кодом 1. Оба кортежа
с версии CB-311 выводятся из таблицы `refusals.CLASSIFICATION`, а не пишутся руками.

### Поправка к диагнозу документа ревизии — держатель проверил и опроверг

Документ ревизии и реестр болей называют вторым симптомом текст
`reqs_add: CHECK constraint failed: priority IN (…)` (31 раз за полгода). **Сегодня этот текст
данным путём не воспроизводится.** Резолвер `types.resolve_priority` вызывается в `add_requirement`
первой же строкой тела (`reqs.py:144`) и отбивает недопустимое значение раньше, чем что-либо
доходит до SQL, а кортеж `types.PRIORITIES` (`must`, `should`, `could`) совпадает с ограничением
`CHECK` в схеме (`reqs.py:30`) слово в слово. Эти 31 случай — исторические, из времени до того, как
резолвер встал на путь записи.

**Что из этого следует для тебя.** Требование ревизии «неверное значение из закрытого словаря даёт
одну строку, а не аварийную распечатку» остаётся живым и невыполненным — но его механизм — это
половина (б), отсутствующая обёртка, а вовсе не ограничение схемы. Не ищи ограничение `CHECK` и не
чини его. Это ровно тот класс ошибки, который направление ведёт третью смену подряд и который смена
23 назвала точнее всех: **прибор смотрит на написание и находит написание, а не предмет.**

## 2. Задание

Довести обе половины до состояния, когда отказ на поверхности создания требования — это **отказ со
словами, классифицированный через единый источник**, а не авария; на обеих поверхностях.

Форма решения не продиктована — ниже развилка, которую решаешь ты, и запреты, которые не обсуждаются.

### 2.1. Ратифицированное держателем решение по слою: конвертировать в домене

Отказ доводится до вызывающего **конверсией в доменном слое** — то есть `add_requirement` сама
превращает нарушение ограничения таблицы в исключение, которое таблица классификации уже называет
отказом (контракт модулей записан в `src/codebugs/CLAUDE.md`: «доменные функции поднимают
`ValueError` при негодном вводе и `KeyError` при отсутствии сущности»).

**Строку `sqlite3.IntegrityError` в `refusals.CLASSIFICATION` добавлять ЗАПРЕЩЕНО, и это не вкусовое
предпочтение.** Причина: таблица классифицирует по точному имени класса, но обе границы применяют её
как `except <кортеж>`, а `except` в Python сопоставляет **по наследованию**. Назвав чужой класс
отказом, ты объявишь отказом всякое нарушение всякого ограничения во всём пакете — а в этом дереве
таких мест много, и в большинстве из них сырая строка SQLite человеку — заведомо неверный ответ
(`findings.py` документирует это в восьми местах, например `findings.py:2109` и `:2660`). Это была бы
та самая дыра молчаливого наследования, ради закрытия которой таблица и построена, только этажом
выше. Конверсия в домене даёт то же наблюдаемое поведение и не расширяет ничью границу.

Побочная выгода, ради которой слой выбран именно этот: `add_requirement` — общая точка для обеих
поверхностей, поэтому одна правка закрывает и командную строку, и протокол, что прямо требуется
пунктом 3 критерия приёмки.

### 2.2. Развилка, которую решаешь и защищаешь ты

**Как отказ узнаёт, ЧТО именно нарушено, и как он говорит об этом, не соврав.**

`PRIMARY KEY` — не единственное ограничение, которое может нарушить эта вставка. Колонка
`description` объявлена `NOT NULL`; выясни замером, достижимо ли её нарушение с каждой из двух
поверхностей (и учти, что промежуточный слой `server.install_strict_arguments` проверяет имена
аргументов, но не обязательно их значения). Сообщение «требование FR-1 уже существует», выданное на
нарушение `NOT NULL`, — это ложь, выданная за отказ, и приёмка её не пропустит.

Известные держателю пути, каждый со своей ценой; выбери один или предложи четвёртый, но **выбор
запиши вслух с ценой**:

1. **Различать по коду ошибки библиотеки.** У исключения есть атрибут `sqlite_errorname`
   (`SQLITE_CONSTRAINT_PRIMARYKEY`, `SQLITE_CONSTRAINT_NOTNULL` и так далее). Точно и дёшево. Цена и
   предупреждение: в этом же файле, на строках 678–680, уже записано ратифицированное решение
   CB-99 — не перечислять коды, потому что ревью дважды измерило такой список неверным в обе
   стороны. Тот запрет вынесен про *классификацию* строки как негодной; здесь речь про *выбор
   сообщения*, и в проекте есть прецедент ровно такого разделения (`db.py`, CB-86: неоднозначный код
   разрешается проверкой «for message selection only»). Если идёшь этим путём — объясни в коде, чем
   твой случай отличается от того, что CB-99 запретила, иначе следующий читатель прочтёт это как
   нарушение.
2. **Различать перечитыванием.** Поймать нарушение, затем спросить у базы, есть ли строка с таким
   идентификатором. Транзакция к этому моменту уже откачена, так что чтение безопасно. Цена: лишний
   запрос на пути отказа и check-then-act, который в принципе может дать неверный ответ при
   параллельном писателе (правило CB-24 в `src/codebugs/CLAUDE.md`).
3. **Не различать вовсе.** Одно сообщение, называющее идентификатор и говорящее правду о том, что
   строка отвергнута ограничениями таблицы, без утверждения, каким именно. Цена: человеку с
   опечаткой в дубликате помогает хуже, чем прямое «уже существует».

Что бы ты ни выбрал — **сообщение не имеет права утверждать больше, чем известно**, и тест обязан
это различать: то есть должен существовать случай, где сообщение о дубликате было бы ложью, и тест
обязан краснеть, если код всё-таки его выдаёт.

### 2.3. Половина (б)

Провести `_cmd_reqs_add` через `cli.domain_errors` по образцу соседей в том же файле.

**Область умышленно узкая и расширять её нельзя.** Обработчиков командной строки без общего
помощника в дереве **29 в 11 файлах** (замер судьи кросс-модельной атаки, правка S-12; в самом
`reqs.py` их пять: `_cmd_reqs_add`, `_cmd_reqs_stats`, `_cmd_reqs_summary`, `_cmd_reqs_import`,
`_cmd_reqs_export`). В этом юните чинится **только `_cmd_reqs_add`**. Остальные 28 — предмет
отдельного пакета над популяцией, которого сегодня нет; его цена измерена нерешённой карточкой
CB-37, а не оценена на глаз. Тронешь соседей — работа не пройдёт приёмку по области.

### 2.4. Требование S-11: единый источник обязан сохранить различие областей

В `refusals.py` три производных кортежа отвечают на разные вопросы: `INPUT_REFUSALS` — то, что ловит
`cli.domain_errors` вокруг одного доменного вызова; `TRACKER_REFUSALS` — то, что ловит внешняя арка
`cli.main`, потому что оно поднимается при ОТКРЫТИИ трекера, вне области действия помощника;
`EXPECTED_REFUSALS` — их объединение, потому что у поверхности MCP граница одна. **Свести их в один
кортеж — регресс, а не упрощение.** Это требование, а не пожелание; оно записано здесь потому, что
проход упрощения — законная часть твоей работы и естественно тянется именно к такому «упрощению».

## 3. Критерий приёмки — дословно из §5 П-1, с проверяющей командой на каждый пункт

Пункты 1 и 2 — это **приёмка того, что уже приземлилось**: их надо подтвердить, а не строить.

1. **Полнота.** Каждый класс, названный в таблице `refusals.CLASSIFICATION`, достижим хотя бы из
   одного инструмента протокола, и намеренное изъятие любого члена краснит проверку.
   Команда: `uv run --extra dev python -m pytest tests/test_refusal_classification.py -q` — вывод
   «N passed». **Мутант обязателен:** удали одну строку таблицы, повтори команду; вывод обязан стать
   красным с именем изъятого класса. Верни строку на место. Запиши в отчёт обе выдачи.

2. **Названный предел, а не обещание.** В документации модуля записано, что классификация идёт по
   точному имени класса, а перехват на границах — по наследованию, поэтому гарантия держится над
   классами, объявленными этим пакетом, и не держится над классом стандартной библиотеки,
   унаследованным от уже классифицированного. Проверяется чтением шапки `refusals.py` — вывод:
   соответствующий абзац присутствует. (Держатель прочитал: абзац на строках 42–54. Твоя задача —
   убедиться, что твоя правка его не сделала ложным.)

3. **Повторный идентификатор требования даёт человекочитаемый отказ на обеих поверхностях.**
   Проверяется: `codebugs reqs-add FR-1 -d x` дважды подряд — вывод второго вызова обязан быть одной
   строкой текста проекта и ненулевым кодом возврата, а не трассировкой; и тот же сценарий через
   инструмент протокола — вывод обязан нести текст, а не обезличенное «ошибка при выполнении
   инструмента».

4. **Неверное значение из закрытого словаря даёт одну строку, а не аварийную распечатку, и на
   командной строке тоже.** Проверяется тем же способом: команда с недопустимым приоритетом, вывод —
   одна строка. (Помни поправку из §1: механизм здесь — половина (б).)

5. **Наблюдаемый клиентом договор об ошибках зафиксирован ДО правки и сверен ПОСЛЕ** (правка судьи
   S-17). Перед началом работы запиши в файл на ветке перечень кодов возврата и форм ответа, которые
   видит клиент на обеих поверхностях; после работы запиши второй и сверь командой сравнения файлов.
   Вывод: либо пусто, либо список расхождений, **каждое из которых объявлено намеренным с причиной
   на строку**.

   **Основание, из-за которого этот пункт обязателен, а не бюрократия:** файл правил подсистемы
   `src/codebugs/CLAUDE.md` сам признаёт, что эталонный снимок схемы протокола не является гейтом на
   форму ответа — «никакой `outputSchema` не снимается, а живая схема несёт
   `additionalProperties: True`, так что расширение было бы гейтом, который не может сработать».
   Значит внутренние тесты могут остаться зелёными при изменении того, что видит клиент.

   Минимальный набор сценариев для обоих снимков (расширь, если найдёшь ещё): удачное создание;
   повторный идентификатор; недопустимый приоритет; недопустимый статус; отсутствующий трекер;
   незаявленное имя аргумента (только протокол). Для каждого — код возврата процесса и форма ответа
   (одна строка текста / трассировка / `ToolError` с текстом / обезличенная ошибка).

6. **Эксперимент на обеих версиях библиотеки протокола.** Пункт 3 в его половине про протокол
   выполняется и записывается **дважды** — под `mcp` 2.0.0 (стоит в окружении) и под 2.1.1 (стоит у
   владельца); если поведение различается, различие записано. Это перенесено сюда из соседнего
   пакета П-2, потому что именно здесь оно решает вопрос: под 2.0.0 текст аварии клиенту приписывает
   SDK, и без прогона на 2.1.1 ты не отличишь настоящую починку от совпадения.

## 4. Обязательный порядок работы

1. **Воспроизведи оба замера из §1 прежде, чем что-либо править.** Не воспроизвелось — стой и пиши.
2. **Сними снимок «до»** по пункту 5 критерия. Он бесполезен, если снят после первой правки.
3. Тесты вперёд кода: красный тест на каждую половину, потом починка. Правило проекта.
4. Прогоняй в **своём рабочем дереве** и только в нём: `uv run --extra dev python -m pytest tests/ -q`
   и `uv run --extra dev ruff check src/ tests/`. Флаг `--extra dev` не опционален в свежем дереве.
   Прогон из главного чекаута проверит чужой исходник и пройдёт на дереве, которого ты не трогал.
5. **Мутационные пробы** по пунктам 1 и 3: каждый новый тест обязан краснеть на подсунутом дефекте.
   Записывай мутант и обе выдачи — это то, что читает приёмщик.
6. Проход `/simplify-traced` перед возвратом. Его результат запиши в этот файл на ветке: гейт слеп к
   двухсессионному процессу (проход делает исполнитель, финиш запускает держатель), и запись в брифе —
   единственное, по чему держатель судит, что проход был.
7. **Коммить рано и часто.** Лимит подписки за сутки срывал сессии пять раз; незакоммиченная работа
   в упавшей сессии — потеря.
8. Обнови `CHANGELOG.md` (раздел `## [Unreleased]`): наблюдаемое поведение меняется.
9. Если правка делает ложным какой-либо абзац в `src/codebugs/CLAUDE.md` или в корневом `CLAUDE.md` —
   поправь его в этой же ветке. Правило проекта: текст, ставший ложным, чинится вместе с механизмом.

## 5. Чего в этом юните НЕТ

- Прочие 28 обработчиков без `cli.domain_errors` (см. §2.3).
- Строка в `refusals.CLASSIFICATION` (см. §2.1) и любое изменение состава таблицы.
- Сведение трёх кортежей единого источника в один (см. §2.4).
- Расширение промежуточного слоя `server.install_strict_arguments` под отказ проверки аргументов —
  это пакет П-2, следующий юнит.
- Что-либо в файлах, которыми занято направление DIR-3: `pyproject.toml`, `uv.lock`,
  `.github/workflows/`, `README.md`, `tests/test_claude_md_*`. Пересечение — конфликт на посадке.

## 6. Что вернуть держателю

Одним сообщением, по-русски, без слов из двух алфавитов:

- Какое решение развилки §2.2 ты принял и какой ценой.
- Пункты критерия 1–6 поимённо: чем проверен, что вывела команда, какой был мутант и как он покраснел.
- Расхождения снимков «до/после» по пункту 5 и обоснование каждого.
- Результат прогона на обеих версиях библиотеки протокола (пункт 6).
- Итоговый прогон набора: сколько прошло, за сколько, на каком SHA, было ли дерево неподвижно.
- Что осталось незакрытым и почему — честная область важнее полного отчёта.

---

## Журнал исполнения

### 1. Воспроизведение замеров §1 — оба воспроизвелись

Половина (а): второй `reqs-add FR-1` печатает аварийную распечатку, последняя строка —
`sqlite3.IntegrityError: UNIQUE constraint failed: requirements.id`, код возврата 1.
Половина (б): `--priority bogus` печатает 23 строки, последняя — `ValueError: Invalid priority:
'bogus'`, код возврата 1. Проверка `refusals.kind_of(sqlite3.IntegrityError)` вернула `crash`.
Описание не протухло, работа продолжена.

### 2. Снимок «до» (пункт 5 критерия) — снят прежде первой правки

Скрипт `tests/manual/snapshot_cb316_error_contract.py`, снимки в `.claude/plans/cb316/`.
Шесть сценариев на двух поверхностях; для протокола записывается ещё и КЛАСС исключения,
выходящего из тела инструмента через адаптер CB-310, потому что под `mcp` 2.0.0 текст на проводе
не различает починку и совпадение — SDK этой версии приписывает текст любого исключения к своему
сообщению.

**Главный результат снимка, и он подтвердил предсказание §1 брифа замером, а не рассуждением:**
под `mcp` 2.1.1 повторный идентификатор доходил до клиента как `Error executing tool reqs_add`
**без текста вовсе**, тогда как под 2.0.0 — с текстом. То есть на установке владельца это была
уже полная потеря текста, а не «некрасивый текст SQLite».

### 3. Решение развилки §2.2 — путь 1 с отступлением на путь 3

Различать по коду ошибки библиотеки (`sqlite_errorname`), **но с падением на более слабое,
безусловно верное сообщение при любом нераспознанном коде.** Нарушение `PRIMARY KEY` даёт
`requirement FR-1 already exists`; всё прочее — `requirement FR-1 was refused by the requirements
table's constraints: <текст библиотеки>`, без утверждения, каким именно ограничением.

Замер, на котором это основано: у доменной функции достижимо нарушение `NOT NULL` четырёх
колонок (`description`, `section`, `source`, `test_coverage`) — `sqlite_errorname` там
`SQLITE_CONSTRAINT_NOTNULL`. С обеих поверхностей оно сегодня недостижимо (командная строка
требует `-d` и подменяет `None` пустой строкой; SDK протокола отбивает `None` собственной
проверкой типов раньше тела инструмента), но `add_requirement` публична, и сообщение обязано быть
верным для функции, а не для сегодняшнего состава её вызывающих.

**Цена решения, названная вслух.** Читатель, знающий про запрет CB-99 тридцатью строками ниже в
том же файле, прочитает это как нарушение, если не объяснить разницу, — поэтому она объяснена
прямо в коде `_constraint_refusal`. CB-99 запретила перечислять коды для КЛАССИФИКАЦИИ отказа,
и это решение здесь не тронуто: классифицирует по-прежнему дерево исключений
(`sqlite3.IntegrityError`, чья вся принадлежность — «эта СТРОКА нарушает ограничения таблицы»).
Код читается только для выбора одного из двух ИСТИННЫХ предложений — то же разделение, что уже
проведено в `db.py` для `SQLITE_CANTOPEN` («for message selection only», CB-86). Направление
отказа делает это безопасным: нераспознанный код падает на слабое сообщение, верное для каждого
члена класса, поэтому ошибка в коде стоит точности и никогда — истинности.

### 4. Порядок работы

Тесты написаны прежде кода: `tests/test_cb316_reqs_add_refusals.py`, 10 из 13 покраснели на
непочиненном дереве (коммит `810ab54`), три зелёные закрепляют поведение, которое починка обязана
сохранить. Затем починка (`f40f51b`), затем снимки «после» и сверка (`166b5e3`), затем
`CHANGELOG.md` (`c2f518a`), затем сквозное чтение разреза (`cd1f8d7`) и проход упрощения
(`04d252e`).

### 5. Мутационные пробы — четыре, все подтверждены

| Мутант | Что подсунуто | Как покраснело |
|---|---|---|
| 1 (пункт 1 критерия) | Изъята строка `db.TrackerExistsError` из `refusals.CLASSIFICATION` | `tests/test_refusal_classification.py`: 1 упал, 36 прошли; в тексте отказа названо изъятое — `codebugs.db.TrackerExistsError` |
| 2 (развилка §2.2) | `_constraint_refusal` возвращает сплошное «already exists» | `test_a_not_null_violation_is_not_reported_as_a_duplicate` упал: «требования FR-НОВЫЙ в таблице нет — сообщение о дубликате здесь было бы ложью» |
| 3 (половина (а)) | Снята конверсия в домене (`except … : raise`) | 8 упало из 13, включая обе поверхности |
| 4 (половина (б)) | Снят `cli.domain_errors` с обработчика | 3 упало — ровно проверки командной строки |

### 6. Проход `/simplify-traced`

Фаза 1 (`/simplify`): четыре рецензента по углам «повторное использование», «упрощение»,
«эффективность», «высота починки». Применено девять находок (коммит `04d252e`), из них две
несущие: скопированная оснастка протокола вместе с двумя обращениями к внутренности чужой
библиотеки, и мой собственный дефект — потерянное утверждение в скопированной проверке.
Отклонено с называнием причины: перевод четырёх проверок командной строки во внутрипроцессную
форму (экономия 0,8 с из 185 с, то есть 0,4 %, при потере настоящего входа процесса, а именно
код возврата процесса и есть предмет пункта 5 критерия) и вынос печати за `try/finally`
(в файле есть оба образца, выбран образец непосредственного соседа `_cmd_reqs_update`).

Рецензент по высоте вернул вердикт **«починка на верной высоте»** с замером: из восемнадцати
команд вставки в пакете четырнадцать содержательны, тринадцать из них уже дают отказ со словами,
и `add_requirement` был выпадающим. Он же измерил, что предварительная проверка существования —
механизм МЕЛЬЧЕ перехвата, а не глубже: вне транзакции она вырождается ровно в чинимый дефект, и
он это воспроизвёл на `sweep.create_sweep`.

Фаза 2 (происхождение): расхождений нет, разрез отвечает брифу и не выходит за его область.

Фаза 3 (`/reflect`): **пропущена**, и причина названа в самом скилле — владелец приостановил её
до срока не ранее 2026-09-07, а снятие приостановки требует зелёного прогона инструмента
`fold_process_rules.py` в соседнем репозитории, которого здесь нет и который я проверить не могу.
Подсистемные файлы правил проверены вручную: ложным моя правка ни один абзац не сделала.

### 7. Три находки, оставленные держателю, — вне области этого юнита

1. **`src/codebugs/merge.py:159`, `merge.start_session` — тот же дефект, живой, и на худшей
   поверхности.** Вставка идентификатора сессии, целиком приходящего от вызывающего, в таблицу с
   ограничением уникальности, ничем не защищена; предварительная проверка есть только на ветке
   `allow_restart=True` и не срабатывает, если найденная сессия ещё жива. У функции **нет команды
   в командной строке вообще** — единственная поверхность это средство протокола
   `codemerge_start`, а именно там под `mcp` 2.1.1 авария приходит клиенту без текста. То есть
   повторный `codemerge_start` с занятым идентификатором даёт агенту пустую ошибку без слова о
   причине, и спасительного отката на аварийную распечатку, как в командной строке, здесь нет.

2. **`src/codebugs/bench.py:992` — обработчик переписал правило и переписал неверно.** Плечо
   `except (ValueError, json.JSONDecodeError)` печатает опрятную строку и завершает кодом 1 для
   ОБОИХ классов, тогда как единый источник относит `json.JSONDecodeError` к авариям именно
   потому, что он означает совершившуюся запись. Область честная: внутри той же конструкции `try`
   стоит разбор `--meta` через `json.loads`, где опрятная строка уместна; дефект в том, что `try`
   охватывает заодно `import_json`/`import_csv`. Обработчик без помощника, который при этом
   ВЫГЛЯДИТ обработанным, опаснее необработанного — аварийная распечатка хотя бы громкая.

3. **Три проверки `tests/test_hand_built_surfaces.py` не могут пройти под `mcp` 2.1.1.** Они
   утверждают наличие ТЕКСТА у аварии (`assert "no connection is ever needed" in str(...)`), а под
   2.1.1 у аварии текста нет по устройству SDK — это тот самый остаток, который записан в шапке
   самого `refusals.py`. Задача `newest-sdk` в `.github/workflows/ci.yml` гоняет весь набор на
   новейшей допущенной версии, значит она сегодня красна на `main`. **Доказано, что это не моё:**
   все участвующие файлы (`tests/test_hand_built_surfaces.py`, `findings.py`, `server.py`,
   `refusals.py`, `cli.py`) на этой ветке побайтно совпадают с `main`.

### 8. Не тронуто умышленно — и это не забывчивость

Абзац в `src/codebugs/CLAUDE.md` (раздел «Error handling») утверждает: «every CLI handler that
touches a domain call routes it through `with domain_errors():`». **Это утверждение ложно, и оно
было ложно ДО моей правки** — обработчиков без помощника 29 в 11 файлах по замеру судьи
кросс-модельной атаки, моя правка уменьшила их число до 28. Правило брифа обязывает чинить абзац,
который правка СДЕЛАЛА ложным; этот таковым не стал. Чинить его здесь я не стал по второй, более
весомой причине: соседнее направление DIR-3 ведёт юнит Т-142 пакета П-11, который называется
«правда нормативных текстов», и ложный универсальный тезис в подсистемном файле правил — ровно
его предмет. Правка отсюда дала бы конфликт на посадке в работе, чья единственная задача — эту
ложность и устранить. Кроме того, у файла есть потолок размера с запасом 775 байт, а поднять его
нельзя: он объявлен в `tests/test_claude_md_size_ceiling.py`, а файлы `tests/test_claude_md_*`
занято DIR-3.
