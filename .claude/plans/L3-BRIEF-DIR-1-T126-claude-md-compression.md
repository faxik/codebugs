# L3-BRIEF DIR-1 / Т-126 — задача владельца: `CLAUDE.md` перерос предел загрузки, и нормативный текст рискует доезжать до сессий усечённым

Держатель направления DIR-1, уровень (2), 2026-08-31, смена 20.
Карта: **её нет, и это правильно** — прямая задача владельца через куратора, а не находка.
Прецедент — Т-104 («Задача владельца: README…»): носителем служит этот бриф и строка в
`CASCADE-IDS.md`. **Дефект был ДО юнита** — документ рос полтора месяца работой всех направлений.

**Класс по Р1: A** (оракул недетерминирован в главной своей половине: «не потеряно ли несущее
правило» машинно не решается, и §5 говорит это вслух вместо того, чтобы завести строковый гейт).
**Стартовый тир: `opus`. `effort`: `high`.**

> Строка не удаляется и не оставляется с заглушками — пункт 8 контракта приёмки проверяет именно
> её. Сменишь тир — назови смену в `## Отчёт по регламенту`; молчаливое наследование есть дефект.

Ветка: `docs/t126-claude-md-compression`.

**ЭТА РЕДАКЦИЯ ПЕРЕПИСАНА ЦЕЛИКОМ ПОСЛЕ КРОСС-МОДЕЛЬНОГО РЕВЮ, НАШЕДШЕГО ЧЕТЫРНАДЦАТЬ ДЕФЕКТОВ В
ПЕРВОЙ, ИЗ НИХ ТРИ ФАТАЛЬНЫХ.** Переписана как ОДИН текст, а не залатана по разделам: латание
по разделам и есть механизм, которым артефакт приобретает новые фатальные дефекты, закрывая
старые, — и два из трёх фатальных были ровно им. Где моё прежнее утверждение опровергнуто, я
оставляю опровержение видимым, а не заменяю молча.

---

## §0. Условие очерёдности и заморозка файла

**`CLAUDE.md` ЗАМОРОЖЕН НА УРОВНЕ ФАЙЛА решением куратора: пока Т-126 в работе, его не трогает ни
одно направление.** Причина измерена, а не предположена: юнит Т-124 соседнего направления
переписал `CLAUDE.md` мержем `c1e12f2` посреди подготовки этого брифа, и Т-126 переписывает около
половины файла — конкурентная правка дала бы конфликт максимального размера при форвард-мерже.

**Предшествующий юнит Т-125 УЖЕ ПРИЗЕМЛИЛСЯ** (мерж `327590d`), он правил три места в `CLAUDE.md`,
и заморозка вступила в силу после него. Проверяется **предком, а не текстом и не порядком
коммитов** — текстовая проверка ловит упоминания в чужих сообщениях, я на этом попался лично:

```
git merge-base --is-ancestor 327590d main && echo landed
```

Прежняя редакция предлагала `git log --oneline -20 | grep -i cb-266`. Так делать нельзя дважды:
окно в 20 коммитов вырабатывается за часы при нынешнем темпе каскада, и `grep` по тексту нашёл мои
собственные коммиты про CB-266 и отрапортовал посадку, которой не было.

---

## §1. Замысел

`CLAUDE.md` — документ, который **каждая сессия проекта читает целиком** прежде чем что-либо
сделать. В нём вся доктрина: устройство харнеса веток, правила базы, что каждый гейт обещает и —
главное — чего он **не** обещает.

Документ перерос предел загрузки харнеса. Опасность не в предупреждении, а в том, что за ним:
**если текст доезжает усечённым, молча не доезжают именно правила**, и не любые, а стоящие ближе к
концу файла. Сессия при этом выглядит работающей — она просто не знает части доктрины и не может
об этом сообщить, потому что не знает, чего не знает. Это тот же класс, что ведёт направление:
**защита, которая тише ломается, чем работает.** Только сломанной оказывается не одна проверка, а
способ, которым проект передаёт правила следующему исполнителю.

**Замысел владельца дословно:** каждая сессия снова читает документ целиком; **ни одно несущее
правило, ни один идентификатор карты, ни одна пинуемая тестами формулировка не теряются.**

**Чего юнит НЕ делает.**
- **Не меняет ни одного правила.** Работа по объёму текста, не по содержанию доктрины. Найдёшь
  правило, кажущееся неверным или противоречащим соседнему, — **это находка в `## Эскалации` и в
  карту трекера, но НЕ в правку.** Смена правила внутри работы по сокращению неотличима от потери
  правила, а именно от потери правил юнит и защищает.
- **Не заводит отдельный архивный файл** со старым текстом (довод в §3).
- **Не трогает `README.md`, `CHANGELOG.md`, `docs/`.**

---

## §2. Что я измерил лично, и ПРАВИЛО ПРО ЧИСЛА

**ПРАВИЛО, ЗАРАБОТАННОЕ ОТКАЗОМ: любое число этого раздела несёт SHA, на котором снято.** Первая
редакция его не имела, и это стоило фатальной находки: юнит Т-124 влил `c1e12f2`, файл вырос на
4 691 байт, а я редактировал бриф уже ПОСЛЕ этого, перемерив ровно тот пункт, который правил, и
оставив соседние пять устаревшими. **Числа ниже сняты на `dc9a9d8`. К моменту, когда ты их
читаешь, файл заморожен — но заморозку ввели после Т-125, так что перемерь сам и сверь.**

**И ВТОРОЕ ПРАВИЛО, ЗАРАБОТАННОЕ ТРИЖДЫ ЗА ОДНУ СМЕНУ: команда, вернувшая ноль или пустоту, не
является замером, пока не показано, что она вообще выполнилась.** Три моих промаха этой смены дали
ЛОЖНЫЙ НОЛЬ, и ноль — самый опасный результат, потому что не выглядит ошибкой. Конкретно:
`./.venv/bin/ruff` в этом репозитории **не существует** (ruff только через `uv run --extra dev
ruff`), а **незакавыченный** `--include=*.py` под `zsh` **обрывает `grep` целиком**. Проверяй код
возврата, версию инструмента и непустой `stderr`, прежде чем записать ноль.

1. **Размер, и ДВА числа, которые нельзя путать.** `wc -c` → **232 326 байт**; длина в **знаках** →
   **231 026**. Разница — многобайтные символы UTF-8. Называю оба: направление уже платило за
   путаницу здесь (в соседней смене запись правили с «179 байт» на «179 знаков»). **Предел 150 000
   я трактую в БАЙТАХ**, потому что байтов не меньше, чем знаков, и попадание по байтам гарантирует
   попадание по знакам, не наоборот. Установишь, в каких единицах считает харнес, — напиши в отчёт.

2. **Масса — в двух разделах из восьми.** `## Code rules` — **117 285** знаков; `## Workflow` —
   **71 730**. Вместе **189 015 из 231 026 = 81,8 %**. Остальные шесть — меньше 19 %. Работа идёт
   в двух местах, не равномерно по файлу.

3. **Внутри `## Code rules` подраздел `### Database` — самая крупная единица работы** (при
   разбиении по пустым строкам читается как один блок примерно на треть документа, потому что
   состоит из непрерывных пунктов списка). Перемерь сам, число зависит от способа разбиения — и
   это само по себе предупреждение, см. п. 4.

4. **ВЫПОЛНИМОСТЬ СОКРАЩЕНИЯ Я НЕ УСТАНОВИЛ, И ЭТО ГЛАВНАЯ ПОПРАВКА К ПЕРВОЙ РЕДАКЦИИ.** Там
   стояло: «73,6 % документа — блоки с языком пересказа ревью», и из этого делался вывод о
   достижимости цели. Довод не несёт возложенного веса, и число не воспроизводится из описания:
   блочная статистика **не отличает «блок содержит пересказ» от «блок ЕСТЬ пересказ»**, а метод я
   описал пятью оборотами, посчитав тридцатью четырьмя. Честный замер на уровне ПРЕДЛОЖЕНИЙ,
   воспроизводимый этой командой —

   ```
   python3 -c 'import re,io;md=io.open("CLAUDE.md",encoding="utf-8").read();
   s=re.split(r"(?<=[.!?])\s+",md);
   p=r"used to (read|say|claim|be)|an earlier draft|this (bullet|section|paragraph) used to|earlier version of this";
   c=[x for x in s if re.search(p,x,re.I)];
   print(len(c),sum(map(len,c)),round(100*sum(map(len,c))/len(md),2))'
   ```

   — даёт **29 предложений, 9 770 знаков, 4,23 % файла**. Требуемое сокращение до 120 000 знаков —
   **48,1 %**. **Чистая хроника правок не покрывает и десятой части нужного.** Отсюда прямое
   следствие, которое ты обязан держать в голове с первой минуты: **«цель по размеру недостижима
   без резки запрещённого» есть ВЕРОЯТНЫЙ исход, а не аварийный**, и §7(г) — законный путь, а не
   провал. Объём придётся брать слиянием и уплотнением, а не удалением хроники.

5. **Множество идентификаторов на `dc9a9d8`: 109.** А именно **98** `CB-N`, **4** `T-N`, **5**
   `ARCH-00N`, **2** `BT-N`. Две поправки, каждая из которых иначе дала бы гейт, зелёный по
   построению:
   - **Семейство `T-N` в файле ЛАТИНСКОЕ** (`T-11`, `T-23`, `T-57`, `T-59`); кириллических `Т-N`
     в документе **ноль**. Реестр каскада пишет кириллическую `Т`, и оракул, написанный ею,
     сравнивал бы пустое множество с пустым и всегда зеленел.
   - **`CB-9001` ВЫМЫШЛЕНА** — это пример внутри правила CB-51, а не карта. Она входит в 98 и
     **объявляется исключением с причиной**, а не защищается наравне с настоящими.
   Множество просмотри глазами, а не только регуляркой: рядом живут `Э-9`, `DIR-1` и подобные,
   которые под защитой не стоят, и это осознанно.

6. **`CLAUDE.md` ЧИТАЮТ ЧЕТЫРЕ УТВЕРЖДЕНИЯ В ДВУХ ФАЙЛАХ, И ЕСТЬ ТРЕТИЙ ПОТРЕБИТЕЛЬ, КОТОРОГО
   ПЕРЕБОР ПО ИМЕНИ НЕ НАЙДЁТ ПО ПОСТРОЕНИЮ.**

   **Читающие сайты:** `tests/test_worktree_harness.py` строки **5734**, **5751**, **5766**;
   `tests/test_cli_signals.py` строка **767**. Прочее в наборе, где встречается имя `CLAUDE.md`, —
   докстроки и комментарии, которые ссылаются, но файла не открывают.

   **Третий потребитель — `tests/conftest.py::_tree_fingerprint`.** Он обходит дерево через
   `os.walk` и снимает `(размер, mtime_ns)` каждого файла, `CLAUDE.md` включительно. Содержимого
   он не читает, формулировок пиновать не может и на зелень набора не влияет — **ценность в
   другом: слова `CLAUDE` в этом файле нет ни разу, поэтому перебор по имени слеп к нему по
   построению.** Практическое следствие для тебя: правка файла во время прогона поднимет алярм
   движения дерева. **Это не поломка**, алярм намеренно не трогает код возврата; но если увидишь
   его — знай, что это ты.

   **Ловушка для твоего собственного перебора:** `tests/test_add_lines_surface.py:245–251` содержит
   строки `"CLAUDE.md:8"`, `"CLAUDE.md:16"`, `"CLAUDE.md"` — это **данные параметризованной
   фикстуры**, разбор строки-местоположения; файл там не читается.

   **Что именно пинуется — девять утверждений:**
   - `rows` = **все строки файла, начинающиеся с `| `**, где бы ни стояли (сегодня **15**), а НЕ
     «строки таблицы принуждения»;
   - `rows` непусто («таблица исчезла»);
   - **ни одна** не содержит `exit 15` и (без учёта регистра) `alarm` — сегодня нарушителей **0**;
   - **ровно одна** содержит `in-lock SHA re-check`;
   - она же содержит `re-check` и `exit 13`, и НЕ содержит `The tested state is the landed state`,
     и НЕ содержит `when the lock was taken`;
   - глобально присутствуют `CB-121` и `exit 15`;
   - в окне `[первое CB-121 − 2000; + 4000]` присутствуют `alarm` и `check-then-act`;
   - глобально присутствуют `74` и `EX_IOERR`.

   **ОПАСНОСТЬ ОБРАТНА ТОЙ, ЧТО Я ОПИСАЛ СНАЧАЛА.** Окно **относительное**:
   `at = md.index("CB-121")`, срез `md[at-2000 : at+4000]`. Перемещение блока по файлу его **не
   ломает** — точка отсчёта едет вместе с текстом. Замерено: `CB-121` встречается **ровно один
   раз**, `alarm` стоит от него на **−182** знака, `check-then-act` — на **+123**; запас огромен,
   к перестановке разделов пин устойчив. **Настоящий риск — УДАЛЕНИЕ ТОКЕНА:** сокращая, легко
   счесть жаргонное `check-then-act` лишним и сказать то же по-человечески. То же с `alarm`,
   `exit 15`, `CB-121`, `74`, `EX_IOERR`. Второй риск, специфический для сжатия: **свёртка прозы
   в таблицу где угодно в файле** — новая строка с ведущим `| `, содержащая `alarm`, ломает пин в
   месте, не имеющем к исходной таблице отношения. Третий: строка, потерявшая ведущий `| `,
   **молча выпадает** из проверки.

   **Запись против меня, которую ты обязан прочесть, потому что она про доверие к этому брифу.**
   Первая редакция утверждала, что пин ломается ОТ ПЕРЕМЕЩЕНИЯ, — в разделе «что я измерил лично»,
   при том что §4 того же брифа честно помечал это как единственную непроверенную предпосылку.
   Документ спорил сам с собой через две страницы. Опроверг кросс-модельный рецензент; цитаты я
   перепроверил своими руками, прежде чем переписывать. **Верь этому разделу ровно настолько,
   насколько сам его перемерил.**

---

## §3. Форма выбрана уровнем (2)

**Форма: правка `CLAUDE.md` на месте, посекционно, с обязательным сквозным перечитом в конце, и с
ВЕДОМОСТЬЮ СНЯТОГО — по строке на каждый снятый или свёрнутый блок, — которая и есть главный
проверяемый артефакт юнита.**

1. **Сначала ИНВЕНТАРЬ, потом правка.** До первой правки построй перечень **несущих утверждений**:
   для каждого раздела — что читатель обязан унести (правило, его граница, причина узости,
   названный остаток). Инвентарь — то, против чего проверяется результат. Без него «перечитал и
   вроде всё на месте» есть проверка по удаче, признанная на этом направлении недостаточной.
   **Эту половину отдай дешёвым субагентам** (`sonnet`/`haiku`): чтение корпуса и выписывание —
   объём, а не суждение. Дорогой тир, делающий черновой объём сам, есть дефект распределения.

2. **Ведомость снятого — `.claude/plans/T126-LEDGER-claude-md-compression.md`.** Для **каждого**
   снятого или свёрнутого блока: что снято, и **либо** где преемник (в какой формулировке живёт
   теперь), **либо** явная фраза «снято, потому что …». Это дословно критерий приёмки владельца и
   единственный способ проверить работу, не сличая 232 килобайта вручную.

3. **Резать можно — и вот операциональный признак, отличающий это от п. 4** (без него два пункта
   пересекаются и решает настроение). **Хронику правки снимаем, только если выполнены ОБА условия:
   правило понятно без неё И его граница выражена ОТДЕЛЬНЫМ предложением, которое остаётся.** Если
   граница живёт только внутри истории («раньше говорили X, и это было неверно, потому что…»), то
   история несёт правило и снятию не подлежит — её надо сначала переписать в утверждение.
   Под это подпадают: повторные пересказы ОДНОЙ истории ревью (остаётся один разбор, прочие
   ссылаются); собственная история правок документа, когда она больше не меняет того, что делать;
   дублирующие рабочие примеры; церемониальные повторы общего урока.

4. **Резать НЕЛЬЗЯ — перечень КЛАССОВ, не мест:** любую формулировку правила; любой абзац честной
   границы («what this does NOT do», «residual, named and not closed», «the honest scope is the
   point») — это не украшение, а единственное, что отличает описание защиты от переобещания, и
   предмет всего направления; причину узости правила; любой из идентификаторов §2 п. 5; любую
   пинуемую формулировку §2 п. 6; названные измерения, на которых стоит решение.

5. **ТРИ ПАССАЖА С ИЗВЕСТНОЙ ЛОЖЬЮ — ОСОБЫЙ РЕЖИМ, И ЭТО НЕ ИСКЛЮЧЕНИЕ ИЗ §1, А ЕГО ПРИМЕНЕНИЕ.**
   Карта **CB-264** (открыта, юнит Т-127 отложен до твоей посадки) фиксирует, что описание
   механизма `# noqa: S608` осталось неверным: фраза про `RUF100`, «всплывающее в момент, когда
   что-либо включено», **опровергнута построением** (прогон с настоящей секцией `select` даёт ноль
   `RUF100`), и абзац занижает оценку стоимости примерно вдвое; плюс неполнота описания «настоящей
   защиты». **Тебе запрещено ПЕРЕСКАЗЫВАТЬ эти пассажи короче, сохранив ложь.** Разрешены ровно
   два действия: снять целиком с записью в ведомость («снято, содержит ложное утверждение, карта
   CB-264»), либо оставить **байт-идентичными** для Т-127. **Перефразировать нельзя** — краткий
   пересказ лжи есть ложь, которую труднее найти.

6. **Правка идёт по разделам, каждый раздел — отдельным коммитом на ветке.** Причина двойная:
   сессии здесь рвутся сетевой ошибкой посреди работы (§6), и посекционные коммиты дают точку
   возврата; и приёмщику посекционная история читается, а один коммит на 100 килобайт — нет.
   **Честная граница, которую надо знать: промежуточный коммит НЕ является безопасной точкой
   восстановления в смысле доктрины** — правило, чьи половины лежали в разных разделах, может в
   нём отсутствовать целиком. Точкой истины является только финальное состояние после п. 7.

7. **СКВОЗНОЙ ПЕРЕЧИТ РЕЗУЛЬТАТА КАК ОДНОГО ТЕКСТА, отдельным финальным действием.** Не по
   разделам. Правило самого документа: **проверка элементов не проверяет композиции**. Что нашёл
   перечит — **словами в отчёт**; «ничего» законно, но относись с подозрением, как велит документ.

**Что решено НЕ делать.**

- **Архивного файла со старым текстом не будет.** Соблазн выглядит осторожным. Отказываю доводом
  самого документа: **две копии одного правила расходятся через одну правку**, и архив
  нормативного текста немедленно становится местом, где живут устаревшие правила без гейта,
  причём следующий читатель не отличит архив от действующей редакции. История git и есть архив —
  с датами, авторством и без притязаний на действительность. То, ради чего архив хотелось завести,
  даёт ведомость.
- **Не переписывать «с нуля своими словами».** Формулировки местами пинуются тестами, местами
  являются результатом нескольких раундов ревью. **Основной приём — удаление и слияние, не
  перефразирование.**
- **Не менять структуру заголовков верхнего уровня** — на них ссылаются брифы и handoff'ы всех
  направлений. Подзаголовки внутри разделов добавлять можно и полезно.
- **Не чинить найденные дефекты доктрины по дороге** (§1), кроме режима п. 5.

---

## §4. Канон К-18 — предпосылка самой правки

Правка, написанная как защита от ошибки, проверяется хуже обычной: приходит с готовым
оправданием. Предпосылки — поимённо, каждую **ПОСТРОИТЬ**, не вывести чтением.

- **П1. Предел действительно превышен, и в каких единицах считает харнес.** Сними размер сам
  (байты и знаки). **Окажется, что предел не 150 000 или считается иначе, — эскалация ко мне**,
  от этого зависит цель.
- **П2. Сколько файлов читают `CLAUDE.md`.** Мой замер — данные. Построй свой, и **перебирай не
  только по имени**: третий потребитель (§2 п. 6) имени не содержит, и это показывает, что
  перебор по имени как метод недостаточен. **Найдёшь ещё одного — это находка, скажи мне.**
- **П3. Что четыре пинующих утверждения ЗЕЛЕНЫ ДО правки.** Прогони оба файла отдельно, запиши
  числа. Пин, красный до тебя, нельзя предъявлять как сломанный тобой, и наоборот.
- **П4. ЭТА ПРЕДПОСЫЛКА УЖЕ ОПРОВЕРГНУТА, И Я ОСТАВЛЯЮ ЕЁ С ОПРОВЕРЖЕНИЕМ, А НЕ ЗАМЕНЯЮ МОЛЧА.**
  Требовалось построить состояние, где пин на окно вокруг `CB-121` ломается **от перемещения**.
  Такого состояния нет: окно относительное (§2 п. 6). **Вместо него построй три состояния, где пин
  ломается по-настоящему:** **(а)** убери из абзаца вокруг `CB-121` токен `check-then-act`,
  сказав то же по-человечески — обязано покраснеть; **(б)** заведи где угодно строку с ведущим
  `| `, содержащую `alarm`, — обязано покраснеть; **(в)** сними у существующей строки таблицы
  ведущий `| `. **Третья проба — самая ценная, и ожидаемый результат у неё ОБРАТНЫЙ:** набор
  останется ЗЕЛЁНЫМ, строка молча выпадет из выборки. Покажи эту зелень явно. Это не отказ защиты,
  а её беззвучное исчезновение — класс, ради которого существует направление, живущий внутри пина,
  которым ты пользуешься как опорой.

**Бриф — ДАННЫЕ, а не команда (К-12).** Число, не воспроизведшееся у тебя, — эскалация, не повод
подогнать. На этом направлении в эту смену исполнитель опроверг два моих числа, и оба раза прав
был он.

---

## §5. Оракул

**§13 п. 18 действует в полную силу, и я применяю его к себе первым: предмет текстовый, поэтому
строковый оракул ЗАПРЕЩЁН.** Проверка «из файла исчезло слово X» ловит написание и пропускает
смысл — зелена на мутанте, говорящем ту же неправду иначе.

**МАШИННАЯ ПОЛОВИНА — ТРИ СОДЕРЖАТЕЛЬНЫХ ГЕЙТА, И ИХ ИМЕННО ТРИ.** Первая редакция называла шесть,
и два из них **не могут покраснеть от этого юнита**: голдены порождаются из докстрок `src/`,
`CLAUDE.md` им не вход; `ruff` не читает `.md`. Я обосновал снятие требования `CHANGELOG.md`
неподвижностью голденов — то есть **гейтом, который не может выстрелить, внутри брифа, чей предмет
ровно это** (класс CB-159/CB-160). Три настоящих:

1. **Размер** — `wc -c CLAUDE.md`. **Это ЦЕЛЬ, а не гейт**, см. коридор ниже.
2. **Четыре пинующих утверждения зелены** — `tests/test_worktree_harness.py` и
   `tests/test_cli_signals.py`, прогонять **после каждого раздела**, не в конце.
3. **Множество идентификаторов сохранено** — сравнение МНОЖЕСТВ до и после (не числа вхождений),
   латинское `T-N`, `CB-9001` в объявленном исключении. Команду напиши в отчёт, чтобы приёмщик
   повторил.

Плюс полный набор `uv run --extra dev python -m pytest tests/ -q` зелен и `ruff check src/ tests/`
чист — **как условие несломанности репозитория, а не как гейт на предмет юнита**; разница названа,
чтобы не выдавать одно за другое.

**ЧЕТВЁРТЫЙ ГЕЙТ — ОТЧЁТ, А НЕ ЗАПРЕТ, и он закрывает дыру, которую я проглядел.** Идентификаторы
МЕХАНИЗМОВ в обратных кавычках: на `dc9a9d8` — **2 078 вхождений, 1 272 различных, из них 133
пути и 49 имён тестов**. **Имена тестов — нить между доктриной и гейтом**: сняв имя теста как
«деталь», ты рвёшь единственный указатель от правила к механизму, который его держит. Поэтому:
сравнение множеств токенов до/после, результат — **ОТЧЁТ о разнице с объяснением каждой пропажи**,
не запрет. Пропажа с объяснением законна; пропажа без объяснения — нет.

**КОРИДОР ПО РАЗМЕРУ, ОДНОЙ СТРОКОЙ, потому что три числа в разных местах противоречили друг
другу.** **Цель — ≤ 120 000 байт; приемлемо — до 135 000 байт с названной причиной; ниже 100 000 —
сигнал тревоги, а не победа** (вернись и проверь по инвентарю, не ушло ли правило). **Размер —
ЦЕЛЬ, НЕ ГЕЙТ, и §7(г) законен: недостижение коридора не красит машинную половину.** Первая
редакция объявляла ≤ 120 000 «обязанным быть зелёным» и одновременно объявляла недостижимость
законной — под давлением «обязана быть зелёной» режется ровно то, что запрещает §3 п. 4. **Я
скорее приму честные 135 000 с целыми правилами, чем 118 000 с потерянным.**

**ПРОЦЕДУРНАЯ ПОЛОВИНА — МАШИННОГО ОРАКУЛА НЕ ИМЕЕТ, и я говорю это вслух вместо того, чтобы
завести строковый гейт (§13 п. 18, следствие).** «Ни одно несущее правило не потеряно» машинно не
проверяется, и никакая строка не сделает вид, что проверяется. Носители: **ведомость** (§3 п. 2) —
её полноту судит приёмщик; **сквозной перечит** (§3 п. 7); **проверка по инвентарю** (§3 п. 1) —
для каждого несущего утверждения показать, где оно живёт в новой редакции.

**ДЕВЯТЬ СПОСОБОВ ПОТЕРЯТЬ ПРАВИЛО, НЕ УДАЛИВ АБЗАЦА.** Первая редакция называла три; шесть
добавило ревю, и два последних здесь вероятнее прочих:

1. свёрнуто в ссылку, ведущую в удалённое место;
2. переехало в раздел, где его не станут искать;
3. сохранено дословно, но потеряло рядом стоявшую границу — читается шире, чем есть;
4. **потерян квантор при сохранённом предложении** («every», «only on the add path», «at most
   one») — предложение цело, правило другое;
5. **слиты два правила с общим предметом и РАЗНЫМИ областями действия** — это главный приём
   сокращения, и потому вероятнейший способ навредить;
6. утрачен НАМЕРЕННЫЙ дубль (байт-идентичный предикат в двух хуках описан дважды нарочно);
7. правило жило только внутри примера (`--separate-git-dir`), пример снят как иллюстрация;
8. **потерян указатель на гейт** — имя теста снято как деталь, и правило больше не сообщает, чем
   держится;
9. инвертирован порядок «правило → исключение», отчего исключение читается как правило.

**§13 п. 20 — примени критерий юнита к артефактам самого юнита.** Предмет — «текст обещает больше,
чем держит, и правила теряются молча». Значит ведомость, отчёт и перечит проверь тем же способом:
каждое своё число **пересчитай предметом**, а не складывая объявленные подсчёты; каждое «преемник
в разделе N» — **открой раздел N и убедись**. «Ничего не нашёл» законно, но **пишется словами**.
**Первая редакция этого брифа сама по §2 не прошла** — числа стояли без пересчёта, множество
снималось регуляркой без просмотра глазами, и в нём сидела вымышленная карта.

---

## §6. Регламент

- **Только worktree:** `tools/worktree-setup.sh docs/t126-claude-md-compression`. Работай ТОЛЬКО
  внутри созданного дерева. Флага `--items` у скрипта НЕТ, `tools/worktree-probe.sh` здесь не
  существует — оба из соседнего проекта.
- **Уровень (3) на `main` не коммитит — никогда (К-1).** Исключение одно (К-17): один прямой
  коммит **только в этот файл брифа**, поимённо.
- **КОММИТЬ В ВЕТКУ РАНО И ЧАСТО — здесь это страховка, не пожелание.** Сессии рвутся сетевой
  ошибкой посреди работы; это наблюдённый режим, он стоил этой смене нескольких обрывов, и правило
  окупилось у соседнего направления при первом же: механизм юнита пережил смерть своего автора,
  потому что был в ветке за час до неё. Раздел закончен — коммит. Инвентарь построен — коммит.
  Замер снят — коммит. Признак приземления — `git log`, никогда не уведомление.
- **Прогон в worktree:** `uv run --extra dev python -m pytest tests/ -q`; `--extra dev` **не
  опционально**. Ruff — только `uv run --extra dev ruff`, версия `0.15.7`; `./.venv/bin/ruff`
  **не существует** (§2).
- **Про `CHANGELOG.md` требование СНИМАЮ, и обосновываю ЗАНОВО, потому что прежнее обоснование
  опиралось на гейт, который не может выстрелить (§5).** Довод не «голдены не двинулись», а
  прямой: `CHANGELOG.md` написан для **пользователя пакета codebugs**, а `CLAUDE.md` — внутренняя
  доктрина разработки репозитория, которая в дистрибутив не входит и до клиента не доходит.
  **Условие обратного хода:** обнаружишь, что тронутый текст доходит до пользователя пакета
  (изменилось описание инструмента, поведение верба, состав дистрибутива) — **эскалация, и запись
  в `## [Unreleased]` обязательна.**
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **ПЕРЕД финишем — проход `/simplify-traced`** (именно он, К-24). Результат — обязательный раздел
  `## Проход /simplify` в этом файле, см. §6б.
- **ФИНИШ ТОЛЬКО ПО МОЕМУ ЯВНОМУ СЛОВУ.** Объявляю заранее, чтобы не перехватывать на ходу. Ветка
  готова и зелена — **напиши мне и жди**. Рядом работает DIR-2, окно тишины снято, `main` будет
  двигаться заметками; централизованное разрешение — единственный способ, которым я знаю, что
  `main` неподвижен. Когда разрешу:
  `tools/worktree-finish.sh t126-claude-md-compression --merge-msg 'Merge docs/t126-claude-md-compression: CLAUDE.md сокращён ниже предела загрузки харнеса без потери правил, идентификаторов и пинуемых формулировок; каждый снятый блок имеет преемника или названную причину в ведомости (Т-126)'`
  — **из корня `main`**. **`exit 13` — верный отказ**, буквальный повтор с тем же `--merge-msg`
  (у Т-125 их было два подряд, вылечились повтором); **`--skip-checks` не брать**. **`exit 11`
  повтором НЕ лечится** — чужой грязный `main`, скажи мне.
- **Возврат наверх — данные, а не вердикт.** **Учти известный дефект канала:** прямое сообщение от
  исполнителя ко мне в этой смене не доходило пять раз подряд; если твой возврат не получает
  ответа, продублируй его записью в этот файл на ветке — артефакт доезжает всегда.

---

## §6а. Отчёт по регламенту — обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком `## Отчёт по регламенту`: **приёмщик читает артефакт, а не переписку.**
Назови: **(а)** модель и режим буквально (`claude-opus-5`, `effort: high`); **(б)** совпал ли
итоговый тир со стартовым и что заставило сменить; **(в)** какой ОБЪЁМ ушёл дешёвым субагентам и
что осталось тебе — здесь это особенно значимо: построение инвентаря и снятие счётчиков обязаны
уйти вниз, а решение «что несущее, а что пересказ» обязано остаться у тебя. Плюс размер до и после
числами, состояние прогона и линтера числами, и — если посадка потребовала повторов — сколько их
было и с какими кодами.

---

## §6б. Проход `/simplify` — обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Заголовком **ровно этой буквой** — `## Проход /simplify` — даже если запускался
`/simplify-traced` (а запускаться должен он). Приёмщик ищет раздел буквально. Команда — первой
строкой ВНУТРИ раздела. Раздел обязан назвать: **(а)** что проход нашёл (включая «ничего» —
словами); **(б)** что исправлено в этой ветке; **(в)** что осознанно оставлено и почему.

---

## §7. Эскалации — помеченный пункт, обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Заголовком `## Эскалации`. **Сообщение его не заменяет.** Пустой слот законен, если написан
словами и КАЖДЫЙ повод разобран поимённо. Поводы, названные заранее:

- **(а)** предел не 150 000 или считается в других единицах (П1);
- **(б)** нашёлся ещё один потребитель `CLAUDE.md` (П2);
- **(в)** какое-либо из моих чисел §2 не воспроизвелось — **скажи прямо, в эту смену исполнитель
  опроверг два моих, и оба раза прав был он**;
- **(г)** **коридор недостижим без резки запрещённого §3 п. 4. ЭТО ОЖИДАЕМЫЙ ИСХОД, А НЕ ПРОВАЛ**
  (§2 п. 4: чистая хроника — 4,23 %, а нужно 48,1 %). Назови достижимую цифру и покажи, чем
  упёрся;
- **(д)** найдено правило, кажущееся неверным или противоречащим соседнему — в эскалацию и в
  карту, но НЕ в правку (§1);
- **(е)** пассаж CB-264 (§3 п. 5) невозможно ни снять, ни оставить байт-идентичным;
- **(ж)** сдвинулся любой голден; понадобилось тронуть файл соседнего направления.
