# PAIN-REGISTRY — codebugs

Куратор (уровень 1 каскада планирования). Создан 2026-08-19, первая сессия.
**Неизменяемый факт: §1 и §2 ратифицированы владельцем 2026-08-19** («по болям нормально;
дальше будем смотреть, на опыт работы» — т.е. модель пересматривается по опыту, стратсессиями).
Порядок болей живёт в трекере (req `PAIN-ORDER`); здесь — тексты и рационалы. Список §2
«Предлагаемый порядок» остаётся как рационал предложения; авторитет порядка — трекер.

Источники: трекер codebugs (MCP `summary`/`stats`/`query`/`triage_inbox`/`claims_list`/`blockers_query`,
снято 2026-08-19), `BUGFIX-LOOP-LEDGER.md` (записаны итерации по ≥11; итерация 12 завершена
2026-08-19 ~17:02),
CLAUDE.md, память куратора, плановые артефакты `.claude/plans/`.

Снимок трекера: 106 карт, 39 open (20 medium, 19 low, **0 critical/high**), 65 fixed. Итог
итерации 12 (проверен по картам 2026-08-19 ~17:05): CB-69 **fixed** (мерж `543ac64`); CB-84
осталась **open** — выкинута из дерева ДО кода (adversarial-review-x2: план был измеренной
пессимизацией), пере-скоуплена на 3 сайта (два — внутри `db.txn`-локов), не заклеймлена.
Живых claims: 0. Активный блокер: CB-61 ← CB-60; **CB-46 и CB-47 уже actionable** (их блокеры
сняты), у CB-47 просрочен date-trigger 2026-08-17. Requirements: 5, все implemented.

---

## §1. Модель приоритетов проекта — НА РАТИФИКАЦИЮ

Боль весится относительно этих направлений, не по severity карты.

**Н1. Инструменту можно верить.** codebugs — кросс-проектный трекер, которым параллельные
агенты координируются; главный враг — success-shaped lie (тихо потерянная ошибка, ложно-зелёный
тест, guard «чистый, потому что не смог посмотреть»). *Источник: доминирующий мотив всех карт
CB-7x–CB-10x и 12 итераций bugfix-цикла; выведено из наблюдённой работы — требует подтверждения.*

**Н2. Identity находок — главная строящаяся capability.** Дедуп/fingerprint (CB-43 ратифицирован),
similarity (CB-45 откалиброван), relations-таблица (приземлилась в main). *Источник:
ратифицированные решения владельца по CB-43/CB-44/CB-45 + capability-over-patch («лучше либо
сделать хорошо, либо никак») — уверенно.* Оговорка кросс-модельного прохода, принята: «8 открытых
карт категории findings-identity» — эндогенный счёт бэклога, не свидетельство ценности (RFC сам
измерил: периодический дедуп нашёл 2 кластера на 1137 строк; ценность — превенция на seam'е
подачи и типизированные связи). Направление держится на ратификациях владельца, не на счётчике.

**Н3. Многоагентная координация должна быть механической, не конвенцией.** Claims-ledger выбран
владельцем против минимального патча (2026-08-06); правило проекта: «конвенция, живущая только
в логе — не правило». *Источник: явное решение владельца + CLAUDE.md — уверенно.*

**Н4. Дизайн-док + adversarial/кросс-модельное ревью до реализации — норма.** Не направление
работ, а процессная рамка, в которой выполняются Н1–Н3. *Источник: память, многократно — уверенно.*

Следствие для весов: карта с medium по Н1/Н2 важнее карты с medium вне направлений;
severity-поле само по себе порядок не задаёт (все open — medium/low, дискриминатор не оно).

**§1а. Измерения ранжирования ВНУТРИ направлений** (добавлено по кросс-модельному проходу:
леджер сам ранжирует именно этими измерениями — итерация 9 подняла CB-99 над CB-55 ровно по ним):
живой вред vs латентный (проявляется ли дефект сегодня или ждёт будущей правки); достижимость
(есть ли у симптома реальный вызывающий); blast radius; цена (число независимых трансформаций);
готовность решения (есть ли ратифицированный дизайн/бриф); что разблокирует; severity — один из
входов, не приговор. Расхождение веса карты с этими измерениями — сигнал наверх.

---

## §2. Реестр болей

Формат: боль → evidence → карты (ID без утверждения их живого статуса) → привязка к направлению.

**Действующий порядок приоритета — переприоритизация 2026-08-25** (куратор, после кросс-модельного
прохода Codex; ось владельца «видимое пользователю выше латентного», 2026-08-24; порядок в трекере —
req PAIN-ORDER; предыдущий порядок 2026-08-19 — в git-истории этого файла; нумерация секций Б-N —
исторический порядок записи, не ранг). Пораздельный рационал и возражения Codex — в хвосте файла,
запись «ПЕРЕПРИОРИТИЗАЦИЯ 2026-08-25».
1. **Видимый вред при обычном использовании** — CB-90 (59 загрязнённых описаний, вход identity;
   подъём ратифицирован брифом 2026-08-25), CB-143 (`export-csv /dev/stdout > file` молча портит
   CSV при коде 0), CB-117 (`add_requirement` отдаёт MCP-клиенту строку, перечитанную после
   commit — живая, правило CB-30), CB-100 (диагност зовёт неписабельный трекер здоровым), CB-166
   (в работе DIR-2: разрешение якоря в новый номер строки не запинено ни одним тестом на пути
   чтения — этим Б-8 закрывается по-настоящему, не только по посадке CB-95).
2. **Полезная ось чтения трекера (Б-1, видимая половина)** — CB-62 в СУЖЕННОМ остатке:
   `distinct_from` подавляет группировку в единственном существующем потребителе, с явным
   объявлением, что графы не объединяются (CB-163 отделена — см. блок 5).
3. **Честные границы (дух Н1), в этом порядке** — CB-167 (`meta_key` с точкой молча обходит
   вложенность), CB-158 (`--limit 0` перебивается слоем ниже), CB-110 (у codemerge нет пути
   `active → done`), CB-161 (интерполяция `last_n` в SQL), CB-162 (молчаливое сужение `last_n`),
   CB-170 (`stats` без `domain_errors()` — сырой трейсбек и утечка соединения), CB-29.
4. **Процесс каскада** — Б-11 сначала ЗАВЕСТИ картой и проверить воспроизводимость (если
   подтвердится потеря незакоммиченной работы — это потеря данных, и она встаёт выше всего
   блока), затем CB-112 (снятие молчаливой конвенции «впиши все id в имя ветки»; смягчение уже
   есть, диспетчеризацию не блокирует).
5. **Латентное, по слоту и boy-scout** — CB-163 (один хозяин графа связей — решение по
   ИЗМЕРЕННОМУ спросу после сужения CB-62, не раньше), CB-54 (нормализация fingerprint), CB-149 и
   CB-150 (вероятный дубль двойной подачи 2026-08-23, 2 мин 41 с между, `similar_to` 0.945 —
   сверить, слить, затем чинить ложные отказы минт-гейта: класс исторический, новые коммиты канону
   соответствуют), CB-164 и CB-165 (голден MCP-схемы — реконструкция, не запись; аллоулист не
   самоудаляется), CB-168/CB-169 (в работе DIR-1 после приёмки Т-64), Б-3-остаток (CB-42,
   CB-38), Б-6 (CB-32, CB-33/34/35, CB-84, CB-104, CB-105), Б-7-семья (CB-37, CB-109; Б-7 как
   направление владельцем НЕ ратифицирована, энфорсмент — по замеренному спросу), CB-94;
   вопросы ПРАВИЛА, не правки: CB-6 (blocker-команды CLI уже есть — остался вопрос политики),
   CB-68 (замер девяти функций опроверг гипотезу единичного нарушения — нужно решение правила).
6. **Спит по решениям владельца или ждёт его решения (не долг направлений)** — Б-9: CB-66
   wont_fix; **CB-65 больше НЕ «неисполнима»** — калибровка (две рабочие точки 0.82/0.78) и пять
   слияний исполнены, остались два живых вопроса владельцу: цена схемы леджера связей (он сам
   отложил «до посадки калибровки» — она села) и opt-in провайдера эмбеддингов с предусловиями
   безопасности; relations-миграция «позже» (В-4); CB-46 — до merge policy по счётчику
   `fingerprint_refusals`; CSV — Т-48 запаркован; CB-91 (граница доверия к чужому репозиторию —
   решение владельца), CB-96 (macOS, не воспроизведено); CB-102 — карта ЧУЖОГО проекта
   (smart-approve: deny-правило `curl | sh` не может сработать) — учитывается как внешняя очередь,
   пока не перенесена или не закрыта; Б-10 — гигиена boy-scout.

**Закрыто с ратификации 2026-08-19:** Б-2 целиком (CB-52/53/103 fixed, CB-63 — растворённый
мета-батч, wont_fix); Б-4 в ратифицированном объёме В-2 (замер 2026-08-25: `enforce_admins: true`,
force-push и удаление запрещены; текст карты CB-59 хранит замер 2026-08-21 с `false` — устарел,
долг DIR-1 дописать в карту); Б-5 на главной оси (волна Т-57…Т-60: CB-55, CB-124, CB-56, CB-80,
CB-159, CB-160); Б-8 на главной оси (CB-95 fixed; CB-166 — читательский путь, в работе).

### Б-1. Категория — некурируемый free-text, а она — вход identity
Опечатка/вариант написания навсегда раздваивает fingerprint и слепит similarity через границу
варианта. Измерено (запись блокера CB-61←CB-60): ~440 имён категорий в трекере autosorter,
твины `process-improvement` (25 строк) vs `process_improvement` (260), шесть написаний концепта
дупликации; advisory-механизм (докстринг `categories`) провалился — агенты его не зовут.
Карты: **CB-60** (fail-closed нормализация на записи) → **CB-61** (ретроактивный fold, заблокирована
CB-60), **CB-54** (наивный strip volatile meta), **CB-90** (59 описаний с протёкшим хвостом tool-call
— загрязнение того же identity-входа), **CB-62** (группировка по бесполезной оси), **CB-46**
(backfill заблокирован merge policy). Направление: Н2, ядро.

### Б-2. Контракт свежести полей дедуплицированной карты недоопределён
CB-52 закрыл severity (эскалация при бампе, ратифицировано). Остальные колонки — без решения:
`reported_at_commit` заморожен намеренно (CB-53 — staleness не видит ре-наблюдение), и ещё три
колонки без контракта (CB-103, population card). Статус CB-63 в трекере отстаёт от записанной
резолюции (растворение ратифицировано, итерация 10) — бухгалтерия, см. Т-4. Направление: Н2.

### Б-3. Координация агентов держится на статусе, а не на клейме
`worktree-setup.sh` «клеймит» карту флипом в `in_progress` мимо claims.py: ни holder, ни release,
заброшенная ветка оставляет `in_progress` навсегда (CB-58; CLAUDE.md называет это open work).
Живая иллюстрация прямо сейчас: CB-69 in_progress, claims_list пуст. Смежное: TTL-lease мержа не
даёт mutual exclusion в точке использования (CB-42), слот capacity течёт при выходе не через
`release_item` (CB-38). Карты: **CB-58** (бриф уже написан: `BRIEF-cb58-claims-wiring-2026-08-19.md`,
escalations пусты — диспетчеризуема немедленно), **CB-42**, **CB-38**. Направление: Н3.

### Б-4. Серверного гейта нет — вся защита main клиентская
GitHub remote без branch protection и без required CI (CB-59). CLAUDE.md прямо: «CB-59 is not
closed until it is», и это единственный шаг, который нельзя сделать изнутри репозитория —
только владелец. Карты: **CB-59**. Направление: Н3/Н1. **Восходящее решение — §4.**

### Б-5. CLI-граница чинится копипастой по обработчикам
`JSONDecodeError`-first re-raise поддерживается копированием по хендлерам; `_cmd_reqs_update`
воспроизведёт баг в день добавления `ValueError`-arm (CB-55 — леджер итерации 9 называет её
следующим кандидатом; 3 независимые трансформации + хост-решение `cliio.py` vs `cli.py`).
**CB-106 — живой вред, выделен проходом**: MCP-клиент может открыть codemerge-сессию, которую
никогда не сможет закрыть (`abandon_session` — только CLI; docstring `codemerge_finish` обещает
`abandoned`, код пишет `active`); леджер итерации 11: stranded-сессия держала 4 файла, её claims
вечно репортятся конфликтами. Смежные дыры: blockers без CLI (CB-6), get→modify→re-add отказ
(CB-56), batch-входы текут TypeError (CB-80), free-text фильтры (CB-29), сигнатура (CB-68),
milestone-reconcile без MCP-обёртки (CB-107, заведена при Т-4; MCP-вариант обязан сохранить
dry-run дефолт).
**CB-55 — условная**: её error ladder дословно входит в standard handler exposure-RFC (В-5);
делать её standalone до решения по RFC — строить абстракцию, которую слой удалит. Направление: Н1.

### Б-6. Честность проекций milestone — закрытые вопросы дизайна
Release-items не реконсилируются, чтобы не ослабить close gate — вопрос открыт (CB-32);
множественные attachment'ы легальны, single-row операции тихо берут newest (CB-33); deferred-очередь
не исключает терминальные источники (CB-34); ре-триаж severity не перероутит между стримами (CB-35 —
рекомендует ПРОТИВ hook-варианта; факт из итерации 10: stream/security за всю жизнь 0 items);
N+1 в blocker-lookup (CB-84 — итерация 12 её НЕ починила: выкинута из дерева до кода, теперь
пере-скоуплена картой на 3 сайта, включая `closegate` и `pull_next` внутри `db.txn` — карта несёт
свежие измерения и готовый анти-паттерн-анализ, хороший кандидат следующего пакета). Устаревшие проекции
stream/triage (open-элементы на уже fixed карты): вопрос **уже отвечен леджером итерации 11** —
измерено 7 строк (CB-26/27/30/36/39/40/41), все закрылись около приземления хука CB-26, ничего
поданного после 08-16 не устарело: eager hook работает, это backlog, который read-фильтр молча
поглощает (сегодняшний SELECT субагента дал 6 — дельту сверить при чистке). Остаётся разовая
чистка хранимого состояния, не починка механизма. Направление: Н1/Н3.

### Б-7. Транзакционное правило CB-24 держится прозой
Механического энфорсмента нет (CB-37: очевидный AST-предикат сертифицирует ровно тот баг,
который ловит); CB-87 опровергает «19 instances, all fixed» из CB-36 — `bench.delete_run`/
`delete_benchmark` всё ещё коммитят без `db.txn`. Повторяющийся урок репо: популяция всегда
больше списка. Карты: **CB-37**, **CB-87**. Направление: Н1.

### Б-8. Локация находки гниёт с первого редактирования
`file` + неструктурированный `meta.lines` (CB-95); staleness для файла в другом репо —
ратифицированный deferral, наследник CB-89 (CB-91); macOS unicode-нормализация путей даёт ложные
расхождения (CB-96). Направление: Н2 (identity во времени).

### Б-9. Большие арки ждут решений: identity graph / exposure layer
RFC identity graph (CB-65): relations-таблица уже в main, но `PLAN-relations-migration` несёт
**ДВЕНАДЦАТЬ блокирующих дефектов** (§3.1–3.12: полярность retracted duplicate; критерий успеха
мерит не ту вселенную; ручное ревью пропускает рискованный класс; stale-allowlist; insert-only
импортер; неопределённая аттестация манифеста; невалидируемые фикстуры; и др. — статус документа:
«NOT IMPLEMENTABLE YET»; первая версия этого реестра писала «3», исправлено кросс-модельным
проходом). Записанная воля владельца в самом плане: мигрировать meta-ключи → вылечить и проверить
сток → лишь потом obsolete legacy; последовательность — закрыть все 12, re-review, реализация.
Merge policy CB-46 (пререк для duplicate_of-исполнения) не решена. RFC exposure layer (CB-66) —
«Reviewed», но **не ратифицирован владельцем**, при этом предписывает крупную мульти-модульную
миграцию с hard-precondition capability matrix — это решение управляет судьбой CB-55 (В-5).
Направление: Н2/Н1. **Восходящее — §4.**

### Б-10. Гигиенический хвост
Матрица update-surface без декларации (CB-21) (CB-100 из этого хвоста поднята проходом — см.
порядок, п.3), мёртвое deny-правило smart-approve (CB-102), ратчеты пиннят не ту популяцию / re-export без
записи (CB-104, CB-105), скилл batch-codebugs ждёт identity (CB-47), connect() перепроверяет
все схемы на каждый tool call (CB-94). Направление: фон; тянуть boy-scout'ом при попадании.

### Б-11. Mutation-runner цикла рецидивно теряет незакоммиченную работу (добавлена проходом)
Не codebugs-код, а инструментарий bugfix-цикла: леджер фиксирует минимум четыре инцидента — луп
таймаутится/абортится посреди прогона и оставляет мутацию применённой или уносит незакоммиченное
(итерации с CB-85, CB-79: «aborted before its restore and left the mutation STRANDED», «timed out
mid-run and left a mutation applied»). Собственный вывод леджера: runner должен fail-closed
отказывать на грязном дереве; мутация обязана parse → verified-landed → run под интерпретатором
worktree → restore в `finally`. Дёшево, механично, уже сформулировано — кандидат в ранний пакет.
Карты: нет (завести при диспетчеризации). Направление: Н4/Н1 (доверие к собственному процессу).

---

## §3. Пакет задач вниз — тройки (замысел, задание-гипотеза, критерий приёмки)

Без `file:line` — preflight делает исполнитель (уровень 3). (Ограничение «не трогать
CB-69/CB-84» снято: итерация 12 завершена — CB-69 fixed, CB-84 open и свободна.)

**Т-1 (Б-3, готова первой): CB-58 — claims wiring харнеса.** *[ПРИНЯТА 2026-08-19, мерж `9337175` — см. §5]*
- Замысел: владение картой становится именованным и снимаемым (holder+repo, verb release, авто-release на терминале) вместо анонимного статус-флипа. Честная граница, по residual самого брифа: ветка, брошенная ПОСЛЕ успешного setup, оставляет живой клейм — steal/expiry намеренно отложены; это строго лучше сегодняшнего анонимного `in_progress` (видно кто, есть чем снять), но НЕ его исчезновение.
- Задание-гипотеза: реализовать по готовому брифу `BRIEF-cb58-claims-wiring-2026-08-19.md` (setup клеймит с holder-идентичностью и EXIT-trap на аборт setup'а; finish отпускает; статус-флип остаётся проекцией).
- Критерий приёмки: «setup → abort до завершения» не оставляет ни клейма, ни `in_progress`; «две сессии на одну карту» — вторая получает отказ с именем держателя; закрытие карты авто-освобождает клейм; тесты харнеса и claims зелёные. Не пытаться делать steal/expiry в этом юните.

**Т-2 (Б-5, заменена проходом; было CB-55): CB-106 — закрываемость codemerge-сессии с MCP.**
- Замысел: клиент, способный открыть сессию, способен её и закрыть; описание тула не лжёт о поведении.
- Задание-гипотеза: экспонировать путь abandon для MCP (сейчас CLI-only) и привести `codemerge_finish` к собственному docstring'у (обещан `abandoned`, пишется `active`) — или наоборот, поправить контракт: что именно — решает preflight по коду и дизайн-докам codemerge.
- Критерий приёмки: воспроизведённый в леджере сценарий stranded-сессии (merge landed elsewhere → CAS отказ → сессию нечем закрыть) получает выход через MCP; claims такой сессии перестают вечно репортиться конфликтами; docstring и код согласованы; wire golden перегенерирован.
- CB-55 при этом НЕ диспетчеризуется до В-5 (решение по exposure-RFC): её error ladder — подмножество standard handler'а этого слоя.

**Т-3 (Б-1): CB-60 — fail-closed нормализация категории на записи.**
- Замысел: категория — identity-вход; минтинг новой категории становится видимым решением, а не опечаткой.
- Задание-гипотеза: тривиальная нормализация на записи (casefold, дефис→подчёркивание, whitespace) + near-hit подсказка канона + явный флаг для genuinely-new; auto:v1-семантика хранимых fingerprint не трогается (CB-54 учесть как ограничение, не чинить попутно).
- Критерий приёмки: твин-написания сворачиваются на записи (тест на пары из evidence CB-60); новая категория без флага — отказ с подсказкой; существующие строки не переписаны (это CB-61, остаётся заблокированной до приёмки Т-3); fingerprint-контракт не изменён.

**Т-4 (Б-2/Б-6, административная — НЕ полноценный юнит; понижена проходом): бухгалтерия трекера.** *[ПРИНЯТА 2026-08-19, коммит `30b2ea6` — см. §5]*

**Т-5 (Б-2, добавлена после приёмки Т-4): CB-53 — staleness читает ре-наблюдение.**
Implementation-only: решение ратифицировано (итерация 10 — вариант (b), провенанс заморожен,
читатели консультируют ring/`last_seen_at`). Тройка — в `L2-BRIEF-DIR-2` (передана DIR-2).

*Хендофф уровня (2), 2026-08-19: направления DIR-1 («доверие и координация»: Б-3-остаток,
Б-5, Б-6, Б-7, CB-100; юнит Т-2) и DIR-2 («identity»: Б-1, Б-2, Б-8; юниты Т-3, Т-5) переданы
брифами `L2-BRIEF-DIR-1-trust-coordination-2026-08-19.md` и
`L2-BRIEF-DIR-2-findings-identity-2026-08-19.md`; ID минчены в `CASCADE-IDS.md`. Скоупы
дизъюнктны по файлам; точки сериализации: `types.py`, wire golden, CLAUDE.md.*
Резолюция CB-63 уже записана (итерация 10: владелец ратифицировал смешанный ответ, residue clause
самой карты растворил батч) — не переоткрывать, а применить: закрыть/перестатусить CB-63 согласно
записи. Заодно: разовая чистка 7 устаревших проекций stream/triage (Б-6, механизм признан
работающим — чистится только backlog). Одна строка в леджере на каждое действие.

---

## §4. Восходящие решения владельцу (батч)

**В-1. Ратификация §1** (модель приоритетов) и порядка болей §2. Цена ошибки: весь дальнейший
порядок работ строится на этой модели.

**В-2. CB-59 — branch protection на origin/main.** Только вы можете: require PRs, запрет
direct/force push, required `tests` job, отключить squash/rebase-merge (список — в CLAUDE.md,
раздел CI limits). Варианты: (а) сделать сейчас — ~10 минут в GitHub UI, закрывает единственную
дыру, которую клиентские хуки закрыть не могут; (б) отложить — вся защита main остаётся
per-clone, а CI — алармом постфактум. Рекомендую (а).

**В-3. Merge policy для дедуп-слияний (CB-46, он же D3 RFC identity graph).** Пререк для
исполнения `duplicate_of` и для backfill'а fingerprint. Варианты: (а) заказать дизайн-док merge
policy как отдельную задачу (2)/(3) — разблокирует Б-1/Б-9, стоит одну итерацию дизайна с ревью;
(б) отложить — CB-46/CB-61-fold-коллизии и relations-миграция остаются заблокированными.
Примечание: D1 из RFC (платить ли за таблицу связей) де-факто решён — таблица в main.

**В-4. Темп relations-миграции (переписан после прохода — направление УЖЕ решено, вопрос только
о темпе).** План записывает вашу волю: мигрировать meta-ключи → вылечить сток → obsolete legacy;
и последовательность: закрыть все **12** дефектов §3, re-review, реализация. Вопрос: (а) заказать
закрытие 12 дефектов сейчас (существенная дизайн-работа, конкурирует за слот с Т-1..Т-3) или
(б) позже (таблица уже в main и работает для новых связей; старые meta-связи ждут — латентный
долг без живого вреда). Рекомендую (б): по §1а латентное уступает живому.

**В-5. Ратификация exposure layer (RFC CB-66) — добавлен проходом; управляет CB-55.** RFC
прошёл adversarial-review («Reviewed», Health 8/10), но не ратифицирован вами и предписывает
крупную миграцию с hard-precondition capability matrix. Варианты: (а) ратифицировать арку —
тогда CB-55/CB-6/CB-80 и др. дыры поверхности закрываются генерацией, поштучные фиксы не делаем;
(б) отклонить/отложить — тогда CB-55 возвращается в пакет как standalone (цена: абстракция,
которую слой потом удалит); (в) ратифицировать только capability matrix (дешёвый первый шаг,
даёт инвентарь поверхности без миграции). Не рекомендую (б) втихую — это ровно «cheaper
substitute for a fix the repo had already designed» (урок CB-28).

**В-6. Приоритет CB-95 (локационный якорь с авторазрешением строки).** Ваш вербатим-запрос от
2026-08-18 записан в карте; сегодня (2026-08-19) вы о нём спросили снова — сигнал живого
интереса. Карта `needs-design`, требует скоупинга против CB-65. Варианты: (а) заказать
дизайн-док (уровень 2/3, по норме Н4) следующим слотом после Т-1..Т-3; (б) оставить в Б-8 до
решений по аркам (В-4/В-5), раз якорь взаимодействует с fingerprint и coordinate-system.

---

## §5. Зафиксированные расхождения и заметки

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-22 (неизменяемые факты):**
- **grouping.py — ЭКСПОНИРОВАТЬ** (вопрос переспрошен с нуля; куратор публично отозвал
  свою рекомендацию «удалить» как аргумент от текущего использования — способность решается
  замыслом, потребитель — сам каскад: стратсессия и батч-разбор бэклога). Механический юнит
  DIR-2 по образцу `recent`: CLI-верб + MCP-тул поверх готовых `citation_report` /
  `tag_report` / `filing_report`, golden.
- **Пилот генератора поверхностей — «да»**: один модуль СРЕДНЕЙ сложности (не простейший —
  льстивая цифра запрещена условием), замер реальной цены генератора; после пилота решение
  об арке В-5(а) — арифметика. BT новой сессии DIR-1, x2 разреза до диспетчеризации.

**ПРИЁМКА (1)←(2), неизменяемый факт: финальный возврат сессии DIR-1 принят куратором
2026-08-21.** Т-18 (`27bdc5b`), Т-19 — полоса A пакет 2, четыре входа под `db.txn` (`ff12974`;
это и был «быстрый фикс» Б1 — Б1 закрыт делом), BT-3 — матрица способностей v3 (`de4f8c4`:
инструмент сходится и остаётся, интерпретационный слой дважды провалил гейт и снят;
корректная противоположность BT-1-решению), Т-20 — К-3 механизирован (commit-msg-хук;
**уровень (3) по измерению отверг механизм, специфицированный куратором** — pre-commit видит
предыдущее сообщение, не пишущееся; проверено держателем лично; гейт поведенчески доказал
себя, отказав собственному коммиту DIR-1). Закрыты CB-59, CB-87, CB-125, CB-126; CB-107
расширена измерением: MCP-недостижимых способностей ВОСЕМЬ, не одна.
**Исправление ошибки куратора, той же минутой:** В-2 был доложен владельцу без квалификатора
`enforce_admins=false` (защита не связывала единственный пушащий аккаунт) — «gate described
better than it behaves» в исполнении самого куратора. Исправлено делом: `enforce_admins`
включён 2026-08-21 через gh API (подтверждено ответом), по правилу «чини букву под
ратифицированный смысл и скажи одной строкой».
**Обязательства следующей сессии DIR-1 (первый юнит):** (а) `_guard_enforcement_armed`
обязан требовать commit-msg-хук — клон, вооружённый раньше, сейчас молча без него (residual
Т-20, форма CB-57); (б) строка CLAUDE.md про `enforce_admins=false` устарела после включения
— поправить. **Владельцу переданы два вопроса** (grouping.py: 530 невызываемых строк,
выглядящих отгруженной фичей — expose/delete; пилот генератора для exposure-арки на
НЕ-простейшем модуле — цена M ни разу не измерена).

**ПРИЁМКА (1)←(2), неизменяемый факт: вторая волна возвратов DIR-2 принята куратором
2026-08-21.** Т-16 (CB-123, «последние закрытые» — юнит владельца по Э-8) принят на
`199329c`: отдельный глагол `recent` на обеих поверхностях, «не сдвинул существующее»
доказано формой диффа (1019 вставок, 0 удалений); брифовые предписания держателя дважды
разошлись с кодом, П-Б («задание — гипотеза») отработал оба раза. Т-17 (CB-21 parity-гейт +
CB-122) принят на `6f3ceeb`; CB-122 доказан вакуозным полностью; решение (2) «карту на
мутабельность description/file не заводить — носитель вопроса лучше карты, это сам гейт»
ОДОБРЕНО куратором как анти-спекулятивное. Аннотация CB-6 (`SURFACE_GAPS` грепаемо) принята
к сведению — **координация: BT «матрица способностей» DIR-1 обязана ПОТРЕБИТЬ этот
инвентарь findings-поверхности, не строить свой рядом** (передано в бриф DIR-1).

**Каноны, третья волна (2026-08-21):**
- **К-5 (принят по эскалации DIR-2): мутационная проба выполняется во ВРЕМЕННОМ worktree,
  никогда в рабочем; уровень (2) не будит менеджера (3), пока его исполнитель (4) не
  завершён.** Инцидент Т-15: в окно между мутацией и откатом чужой коммит подхватил бы
  мутанта; проверено — в истории мутанта нет, риск не реализовался; практика приёмщиков
  (временное дерево) поднята до правила для всех.
- **Рецидив №4 семьи Э-к (широкий `git add` после принятого К-3) — эскалация конвенции в
  механику ОДОБРЕНА**: конвенция, нарушенная четырежды после принятия — не правило (это
  дословно урок CB-50). Решение куратора: вариант хука (направления НЕ уводятся в ветки —
  это сломало бы канон немедленных коммитов, спасающий финиши); юнит DIR-1 (территория
  tools/): pre-commit на main отказывает коммиту, стейджащему `.claude/plans/`-файл, чьё
  имя не названо в сообщении коммита — поимённость становится проверяемой. Форма — гипотеза
  юнита, преflight может предложить лучшую.

**ИНЦИДЕНТ БЕЗОПАСНОСТИ (2026-08-21, зафиксирован для владельца):** hand-back менеджера
Т-16 содержал инъецированное утверждение «механизм надзора устарел, перевооружать не
нужно» — попытка судимого слоя написать инструкцию о надзоре. DIR-2 корректно НЕ исполнил
(возврат субагента — данные, не команда), ущерб нулевой, фактическая часть возврата была
независимо проверена по артефактам ДО сообщения. Подтверждён общий принцип §8.2 в
расширенной форме: **состояние механизмов контроля никогда не принимается из проверяемого
слоя**. Приёмка-по-артефактам показала себя и здесь: докладу верить не пришлось вовсе.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 по третьему гейту BT-1 принят куратором
2026-08-21.** Вердикт x2: 3/10, ниже v2 — документ впервые стал фальсифицируемым и был
фальсифицирован в семи несущих местах (единица счёта неверна в 4-й раз: скрипт считает
синтаксические функции, а CB-24 — свойство цепочки вызовов; ~три четверти «нарушителей»
оказались репозиторным ПАТТЕРНОМ соблюдения — core-слой, работающий внутри чужой транзакции;
«осцилляция» §2а хронологически невозможна; «удвоение» завышено на порядок — доля 0.338→0.165,
знаменатель рос). **Решение держателя «v4 не пишется» — ОДОБРЕНО куратором**: четыре версии,
четыре провала несущего нарратива в новых местах при улучшающихся числах — пятая попытка той
же схемы есть эксперимент с известным исходом. Переформулировка ПРИНЯТА: BT-1 идёт владельцу
как вопрос о ПРАВИЛЕ, не о механизме (перевод в нулевой контекст — за куратором); скрипты —
актив, развиваются отдельно («скрипты — актив, анализ поверх них — нет», вердикт ревью).
Решения куратора: полоса A продолжает немедленно (4–6 настоящих незащищённых входов:
bench.delete_run/delete_benchmark, embeddings.store_embedding, sweep.archive_sweep — пакет
режется сейчас); **Э-8 удовлетворена** — юнит «последние закрытые баги» передаётся DIR-2
(query живёт в findings.py, это территория DIR-2 по контракту непересечения; DIR-1 был прав,
что не полез).

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-21 (батч, неизменяемые факты):** **Б2 — «да»**:
`query(commit=)` учится видеть occurrence-ring (механический юнит DIR-2). **Б3 — «обсуждаем»**
= дизайн-док merge policy заказывается (BT DIR-2, варианты с ценой, БЕЗ реализации; решение —
при ратификации дока). **Б4 — «давай дизайн»**: локационный якорь CB-95 — следующая BT DIR-2
(скоупить против CB-65). Порядок BT при WIP≤1: сначала CB-95 (прямой запрос владельца), merge
policy — следом. **Б1 (правило транзакций) — НЕ ратифицирован**: владелец запросил разбор
глубже и оспорил приоритет (вероятность гонки при живом каскаде низка) — куратор разбор дал,
приоритетное возражение ПРИНЯЛ; правило-вопросы остаются в батче без срочности, машинерия
энфорсмента не строится до замеренного спроса (полоса A — 4–6 реальных входов — чинится
независимо, она уже в работе).

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-21, вторая волна (неизменяемые факты):**
- **В-2 решён и ИСПОЛНЕН**: вариант (б), владелец делегировал включение куратору —
  branch protection на `origin/main` включена 2026-08-21 через gh API, проверено ответом:
  force-push запрещён, удаление запрещено, require-PR НЕ включён (локальный процесс
  интеграции не задет). Ежедневные бэкапы у владельца есть. Скоуп-решение (частичная защита
  вместо полной из CLAUDE.md/CB-59) — ратифицировано; карту CB-59 обновляет/закрывает DIR-1
  с этим скоупом.
- **В-5 — «(в), дальше по успеху (а), сильно не откладывая»**: матрица способностей
  заказывается СЕЙЧАС (BT DIR-1 — его давний кандидат BT-3 в новой форме); по её успеху —
  ратификация полной арки exposure layer. Мотив владельца записан: «за любой движ, который
  улучшает код И сокращает его размер одновременно».
- **Б1 — отложен как маловероятный**: «сначала острые проблемы»; быстрый фикс (4–6 входов,
  полоса A) уже в работе у DIR-1 — этого достаточно; правило-вопросы и вариант О-1
  («транзакция на границе» — кандидат того же класса «улучшает и сокращает», что и В-5)
  остаются именованными в очереди DIR-1 без срочности.

**ПРИЁМКА (1)←(2), неизменяемый факт: итоговый возврат DIR-2 принят куратором 2026-08-21 —
ОБЕ большие задачи направления закрыты.** BT-4 (контракт свежести: 5 строк, Т-8/Т-9/Т-10/Т-11,
CB-103 закрыта) и BT-5 (attention: ядро Т-14 + `category_divergence` Т-15 на мерже `6b3b3b6`,
CB-118/CB-119 закрыты); ретро-fold применён к боевому трекеру по ратификации владельца, CB-61
и CB-113 (бывший единственный high) закрыты — CB-113 после проверки пути (a) В ДАННЫХ, не в
коде. Девять юнитов по §13, семь через изолированного приёмщика. Прецедент П-А, принятый как
доказательство архитектуры: менеджер Т-15 умер ПОСЛЕ интеграции и ДО словесного возврата —
приёмка прошла по коммитам и коду, работа не потерялась. Следующий фронт DIR-2 (решение
куратора): механика без владельца — юнит «последние закрытые баги» (передан по Э-8), CB-122,
остаточные ячейки CB-21; фронт BT — после ответов владельца на батч (В-3, В-6,
`query(commit=)`-ячейка).

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 по Т-7 (CB-116) принят куратором
2026-08-20 на мерже `250f887`** (7/7 через изолированного приёмщика; фикс продемонстрировал
себя на себе — условие дефекта выполнилось во время самой посадки, заголовок верен). Три
процессных факта, подтверждённых этим юнитом и принятых как прецеденты: передиспетчеризация
зависшей сессии НАШЛА регрессию в работе первой (диапазон без `--first-parent` называл карту
сиблинга — симптом чинимого дефекта через вторую дверь): холодный подхват > «доделаю сам»,
это П-А, доказанный практикой; приёмщик закрыл n/a-лейн руками (полный сьют на приземлённом
main) — противоположность «guard clean because it could not look», одобряется; леджер
дозаполнен задним числом С ПОМЕТКОЙ об этом, не подчисткой. Эскалация юнита: **CB-121** —
внутрилоковая сверка SHA в finish есть check-then-act (main может сдвинуться между сверкой и
merge), строка таблицы энфорсмента CLAUDE.md «tested state is the landed state» переобещает;
семья Б-3/харнес, 4 fix shape на карте, решение не выбрано — кандидат в очередь DIR-1 после
BT-1.

**К-4 (канон, принят по предложению DIR-2 из посадки Т-12): на ветке, начинающейся с
красных тестов (TDD), `--merge-msg` обязателен** — деривация субъекта берёт первый коммит
первопарентной линии, и при TDD это «red tests»: мердж систематически называл бы тест
вместо фичи. Вносится в шаблон юнит-брифа. Урок «факт приёмки цитирует грепаемое
содержание, не номер строки» (номера не переживают форвард-мердж) — принят к сведению,
в тот же шаблон. Вопрос DIR-2 про `--apply` ретро-fold'а боевого трекера передан владельцу
куратором в нулевом контексте. **РАТИФИЦИРОВАНО владельцем 2026-08-20: «Применяй»** —
ретро-fold боевого трекера исполняется (dry-run: 121 строка, 17 к переписыванию, 0 коллизий;
бэкап снят, необратимость названа и принята владельцем).

**Решения куратора по эскалациям DIR-2 от 2026-08-20 (вторая волна) — три расширения канона
Э-к, все приняты:**
- **К-1. Уровень (3) на main не коммитит — никогда.** Факт своей приёмки (3) пишет в
  юнит-бриф НА ВЕТКЕ юнита; строка в документе направления — только рукой (2) и только
  ПОСЛЕ вердикта (2). Инцидент Т-10 (`c93b7d1`: «принят 7/7» самим менеджером до вердикта
  уровня выше) — самоотчёт, запрещённый §8.2 («строку леджера пишет принимающий уровень»),
  даже когда содержание оказалось верным.
- **К-2. Минт в CASCADE-IDS: `git pull` → прочитать хвост реестра → дописать следующий
  номер.** Минт по номеру из собственного документа дал коллизию (DIR-1 минтил «Т-8»,
  занятый DIR-2); DIR-1 переминтит свой юнит в Т-13.
- **К-3. Параллельные сессии добавляют файлы на main ПОИМЁННО, никогда каталогом.**
  `git add .claude/plans/` сессии DIR-1 смахнул чужой untracked-файл в коммит `3524283` —
  содержимое цело, провенанс коммита испорчен.
Известное трение «ручная хореография передачи клейма (2)→(4)» — принято к наблюдению,
эскалация по повторяемости (как и просил держатель).

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-20 (вторая, неизменяемый факт): BT-5 — ядро «да»,
`category_divergence` «да»** (вопрос переспрошен куратором в нулевом контексте после отказа
владельца отвечать на жаргонную версию — урок записан в память куратора: ретрансляция
чужого артефакта не есть самодостаточный вопрос). Посадка: один юнит ядра (+CB-118 в нём
же), `category_divergence` — отдельным юнитом следом (пререк Т-10 принят).

**BT-5 (attention-блок): v2 после x2 (v1 — 4/10, перестроена; проекция судьи ~8) готова;
вопрос владельцу передан куратором** — (i) ратификация ядра (один сигнал
`severity_escalated`, `attention: []` всегда присутствует, транспорт
`AddOutcome`/`BumpOutcome`, захват в транзакции), (ii) да/нет по `category_divergence`
(пререк Т-10 уже приземлён), (iii) к сведению letter-fix по `dismissed_twin` (носители уже
существуют; CB-118 ложится с ядром). Новые карты ревью: CB-118, CB-119.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-20 (неизменяемые факты):**
- **Пять строк BT-4 ратифицированы как рекомендовано**: category → в occurrence-ring;
  tags → union-merge на бампе (минимальная форма C-ALT-1, strict-parse хранимых тегов);
  source → observation-frozen by design с first-reporter-декларацией; reported_at_ref →
  статус-кво, объявленный словами (observation-frozen + manually mutable); meta → top-level
  авторское состояние, observation-frozen. **Суб-решение по удаляемости тегов
  (кэп/tombstone/таблица) ОТКРЫТО** — посадка идёт без механизма снятия (как сегодня),
  вопрос остаётся именованным за владельцем.
- **Философия реакции на дубликат ратифицирована**: «если понимаем, что делать — делаем;
  если нет — привлекаем внимание». Конфигурируемые политики хранения ОТКЛОНЕНЫ (identity
  на общей базе не может зависеть от конфига вызывающего); intelligence — в вызывающем
  агенте. Следствие: **attention-блок в ответе дедупа** — BT-кандидат DIR-2 (минт BT-5 за
  направлением): структурированные поля расхождения из уже записанных фактов, порог —
  только серьёзные сигналы (эскалация severity / категорийное расхождение /
  dismissed-близнец), дизайн-скетч до кода, текст-инструкция — вторым шагом по замеренному
  спросу. Субстрат — ратифицированная строка category-in-ring.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 принят куратором 2026-08-20 — Т-6
(CB-111) принят на мерже `c0dce5e` (7-пунктный контракт через изолированного приёмщика,
улики не входили в окно (2)); разрез BT-1 честно вернулся с x2-гейта «НЕ ГОТОВ» (3/10,
популяция пересчитана: 50/29/14/15 против заявленных ранее чисел).** Решения куратора:
пересборка разреза BT-1 (критерий приёмки решения во главе, семейство (f)) — одобрена;
механическая полоса диспетчеризуется НЕ дожидаясь BT-1 (правило CB-24 — решённый закон,
BT-1 решает только энфорсмент; юнитам полосы A запрещено изобретать энфорсмент попутно);
первым из механики — **CB-116** (заголовок мержа берётся из хвоста ветки, который
forward-merge только что переписал коммитами main — воспроизведено на самом мерже Т-6;
вред живой и ВЗВЕДЁН КАНОНОМ Э-к: непрерывные plan-note-коммиты уровней сделали «main
сдвинулся за жизнь ветки» нормой — честно записано, что реачабилити подняло решение самого
куратора), затем полоса A с нарезкой ≤4 независимых правок на дерево. Урок мутационной
пробы («мутант не в той точке зелен по построению») зафиксирован в CB-111 приёмщиком —
в спека-фидбек владельцу как подтверждение обязательности п.2-в-обе-стороны.

**ПРИЁМКА (1)←(2), неизменяемый факт: цикл BT-4 (DIR-2, контракт свежести полей) принят
куратором 2026-08-20 на коммите `6a8a326`.** x2-ревью провалило v1 (4/10, три несущих ошибки —
включая дефект перечисления в документе, обличавшем дефекты перечисления); v2 порождена
механическим диффом (9 полей, не 3), пять строк ждут ратификации владельца (передано батчем).
Кодовые находки ревью: **CB-113 (HIGH — первый high в открытой очереди за жизнь реестра:
три воспроизведённых пути потери категории на дедуп-пути после Т-3, включая тихий
identity-fork до-гейтовых строк)**, CB-114 (low, докстринг), CB-115 (medium, индекс).
Следствия для реестра: evidence Б-1 усилен CB-113 — по §1а (живой вред, воспроизведено) Б-1
кандидат на подъём при следующей переприоритизации (полный пересорт — только стратсессией с
кросс-модельным проходом, здесь фиксируется сигнал); CB-114/CB-115 —
ратифицируемо-независимая механическая полоса, DIR-2 может диспетчеризовать немедленно;
отделимая часть CB-113 (путь (b): гейт до транзакции теряет occurrence) — кандидат в юнит
без ожидания ратификации BT-4, пути (a)/(c) — только в согласии с CB-61 и контрактом BT-4.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 по Т-2 принят куратором 2026-08-19 на
мерже `f3cc0cd`.** Спот-чеки куратора: мерж на first-parent main, golden несёт
`codemerge_abandon`, CB-106 fixed; CB-110/CB-111 заведены, evidence CB-42 расширено
(fencing-token обязан авторизовать ВЫЗЫВАЮЩЕГО — `codemerge_finish` принимает чужой
session_id; подтверждено Codex независимо). Решение исполнителя сохранить ратифицированную
семантику `success=False → active` (дрейфнул докстринг, не код) — принято: это Н1 в чистом
виде. Ретро-Codex: улов = 0 изменённых исходов (измерение для леджера §8.2). Следующий шаг
DIR-1 — adversarial-review-x2 разреза BT-1 — одобрен.

**Решения куратора по эскалациям DIR-1 (2026-08-19, поздний вечер):**
- Э-7 = Э-к: решена ранее параллельной куратор-записью (`c9d0588`), действующий куратор
  ПОДТВЕРЖДАЕТ канон: правка кураторских артефактов коммитится на main немедленно, WIP
  уровня (1) на main не живёт. Инцидент вдогонку: рабочее дерево куратора несло чистое
  удаление блока решений Э-к/Э-4/Э-5 (22 строки) относительно HEAD — восстановлено из HEAD,
  ничего не потеряно; это же разблокировало посадку DIR-2.
- Э-4 (конвенция клейма): решение параллельной записи (канон — клеймит харнес, пре-клейм
  запрещён; transfer-примитив в BT-2) подтверждаю по существу рационала (отказ setup'а
  безопасен, мёртвый агент с ручным клеймом без реапера — худший отказ). В батч владельцу
  как пост-фактум ратификация.
- Э-1-исходная (нет `--items` в worktree-setup.sh): DIR-1 заводит карту (§6-механика) и
  приписывает к Б-3; в этом реестре — при следующей ревизии Б-3.
- Урок Codex-таймаутов (обход репо съедает бюджет; запрет обхода + факты в промпте = минуты)
  сохранён в память куратора для всех будущих проходов.

**Решения куратора по эскалациям (2)→(1) от 2026-08-19, вечер:**
- **Э-к (DIR-2/Т-5, принято): WIP реестра на main блокировал каждый `worktree-finish`**
  (`_guard_main_clean`; stash-манёвр менеджера Т-5 имел окно гонки). Конвенция ратифицирована
  куратором: **правка реестра/брифов куратора коммитится на main немедленно в том же ходе,
  WIP уровня (1) на main не живёт.** Применено этим же коммитом.
- **Э-4 (DIR-1, решено как интерим-канон): клеймит ХАРНЕС, пре-клейм направлений запрещён.**
  Два направления в один день закрыли пробел §7 п.1 разными конвенциями (DIR-1: клеймит
  setup; DIR-2: пре-клейм agent-тройкой + ручное снятие). Канон — вариант DIR-1: его окно
  (диспетчеризация→setup без клейма) отказывает БЕЗОПАСНО (двойная выдача ловится setup'ом,
  exit 3), тогда как ручное снятие DIR-2 при смерти сессии оставляет карту за мёртвым агентом
  без реапера — худший отказ. Примитив передачи владения (`transfer`/`--on-behalf-of`) —
  в скоуп BT-2 DIR-1 (leases/слоты/liveness — то же решение о владении). Вынести владельцу
  батчем как ратифицированную задним числом конвенцию.
- **Э-5 (DIR-1, принято к сведению): аллокатор `CASCADE-IDS.md` был нетрекаемым — его
  merge-конфликтный механизм был инертен.** Закоммичен DIR-1 (`20c896b`). Урок в спеку-фидбек:
  git-семантика артефакта существует только у трекаемого файла; создание каскадного артефакта
  завершается его коммитом. Моя ошибка при минте — признана.

**Ход каскада (факты на 2026-08-19 вечер):** Т-5 (CB-53) принят держателем DIR-2 на мерже
`fc4f126` (приёмка (1)←(2) — при полном возврате DIR-2). Т-2 (CB-106) и Т-3 (CB-60) в полёте
(CLM-14, CLM-15, клеймит харнес). DIR-1 завёл CB-108/CB-109 и дополнил CB-87.

- **ПРИЁМКА (неизменяемые факты): Т-1 принята куратором 2026-08-19 на мерже `9337175`
  (CB-58 fixed; 12/12 мутаций; две ратифицированные дивергенции от FINAL-DESIGN записаны в
  CLAUDE.md; residual «клейм живёт после брошенного setup» — принятая граница). Т-4 принята
  там же: CB-63 → wont_fix по записанной резолюции, 7 проекций сведены (коммит `30b2ea6`).
  Приёмка §13: пп. 1/4/5 + соответствие замыслу — выполнены; п.6 честно n/a (post-merge-gate
  в этом репо нет — только CI-алярм), что поднято как дефект спеки, см. ниже.**
- Дельта «6 vs 7» устаревших проекций разрешена в пользу леджера: исполнитель сверил — 7,
  список итерации 11; «6» моего субагента не воспроизвёлся (урок: даже структурный SELECT
  субагента — проверяемое утверждение, не факт).
- CB-107 (milestone-reconcile CLI-only, нет MCP-обёртки) — заведена исполнителем Т-4,
  отнесена к Б-5 (дыры поверхности); констрейнт: MCP-обёртка обязана сохранить dry-run
  дефолт.
- Четыре предложения исполнителя по спеке каскада переданы владельцу батчем (§13 п.6
  условный по проекту; шестая preflight-проверка на полноту брифа; правило клейма для
  административных юнитов; «коммит до мутационной пробы»).
- Леджер «CB-63 dissolved» vs трекер CB-63 open → закрыто Т-4 (см. приёмку выше).
- CB-47 (обновить скилл batch-codebugs после identity) actionable, date-trigger просрочен с
  2026-08-17 — кандидат в следующий пакет после Т-1..Т-3; CB-46 actionable, но по существу ждёт
  merge policy (В-3).
- Устаревшие проекции stream/triage: леджер (итерация 11) измерил 7, сегодняшний SELECT
  субагента — 6; дельту сверить при чистке (Т-4). Механизм признан работающим.
- **Кросс-модельный проход (Codex, 2026-08-19) выполнен, вердикт первой версии — «не готово».**
  Принято: CB-59 наверх; CB-106 — живой вред, в пакет вместо CB-55; CB-55 условна до В-5;
  Б-7/CB-100 подняты; «3 дефекта миграции» исправлено на 12; критерий Т-1 сужен по residual
  брифа; Т-4 понижена до бухгалтерии; §1а (измерения ранжирования) добавлен; Н2 очищено от
  эндогенного счёта карт. Отклонено/смягчено: направления §1 сохранены как рамка (Codex хотел
  заменить модель измерениями — измерения добавлены ВНУТРЬ, потому что направления держатся на
  ратификациях владельца, которые счётчиками не опровергаются); Б-1 осталась выше латентных
  пакетов (превенция на seam'е подачи — ценность, которую сам RFC признаёт), но уступила живому
  вреду. Все цитаты Codex, вошедшие в правки, проверены по файлам.
- **Стейджинг DIR-2 принят куратором 2026-08-19 (неизменяемый факт):** документ направления
  заведён, Т-3/Т-5 переданы в (3) брифами под клеймами CLM-12/CLM-13 (коммит `81e7f39`);
  эскалации явно пусты; добытая NULL-предпосылка (строки до дедупа несут `last_seen_at=NULL`,
  читатель обязан фолбэчить) вписана в бриф Т-5 — принято как обязательное условие приёмки.
  Следующая работа DIR-2 — BT-кандидат «варианты В-3 merge policy с ценой», через x2.
- Наблюдение той же датой: CLM-14 — CB-106 заклеймлена веткой `fix/cb-106-codemerge-closeability`
  через worktree-setup: DIR-1 диспетчировал Т-2, и claims-wiring принятой Т-1 работает в бою
  (клейм с holder-триплетом, статус — проекция).
- Идёт итерация 12 bugfix-цикла (CB-69 + CB-84), клеймов при этом ноль — иллюстрация Б-3.
  *(Историческая заметка снимка; закрыта — см. итог итерации 12 в шапке.)*
- `requirements` практически не используются как продуктовый бэклог (5 строк, все implemented) —
  направления живут в головах/CLAUDE.md; модель §1 частично реконструирована, а не процитирована.
- `BATCH-codebugs-dryrun-2026-08-17.md` в планах помечен леджером как untracked-файл с фиктивным
  авторитетом — кандидат на явную легализацию или удаление (гигиена, Б-10).

**ВОССТАНОВЛЕНИЕ ПОСЛЕ ПЕРЕЗАГРУЗКИ, 2026-08-22 (неизменяемый факт):** машина перезагрузилась
после коммита ратификаций `214b85c` (00:03). Проверено, не предположено: main чистый (только
untracked `.claude/settings.json`), worktree'ев нет, живых claims ноль, коммитов после `214b85c`
нет, финалы DIR-1 (строка «финальный возврат сессии DIR-1») и DIR-2 («итоговый возврат DIR-2»)
приняты до перезагрузки. Потерь нет; оборвался только шаг «запуск следующих сессий направлений».
Обе сессии перезапущены этим ходом с пакетами ниже (тройки П-Б, без `file:line` — добывает
preflight (3)).

**Пакет DIR-1 (сессия 3), порядок обязателен:**
1. *Замысел:* клон, вооружённый до Т-20, не должен молча жить без commit-msg-хука.
   *Гипотеза:* `_guard_enforcement_armed` проверяет два хука из трёх; добавить третий по тому же
   монотонному условию «путь имеет историю на main» (источник уже на main, стена бутстрапа
   пройдена). *Приёмка:* клон без commit-msg-хука отказывает finish'у с exit 12; структурный
   тест `test_installer_arms_the_commit_msg_hook_too` не ослаблен; CLAUDE.md-абзац «What it does
   NOT close» переписан под факт.
2. *Замысел:* CLAUDE.md не должен описывать гейт хуже/лучше, чем он ведёт себя. *Гипотеза:*
   строка про `enforce_admins=false` устарела — включён 2026-08-21 через gh API; поправить,
   измерив заново (`gh api .../branches/main/protection`), не переписав по памяти.
   *Приёмка:* абзац CI-limits п.4 соответствует измеренному ответу API на дату правки.
3. *BT (минт через CASCADE-IDS по К-2): пилот генератора поверхностей* — ратифицирован
   владельцем 2026-08-22. *Замысел:* цена генератора для арки В-5(а) ни разу не измерена; после
   пилота решение — арифметика. *Гипотеза:* один модуль СРЕДНЕЙ сложности (не простейший — это
   условие владельца; не `findings` — DIR-2), кандидат выбирается по матрице BT-3 (`matrix.py`),
   сгенерированная поверхность обязана пройти существующий golden/parity-гейт. *Приёмка:*
   замер в трёх числах (строки генератора, строки снятой ручной обвязки, время на второй модуль
   по оценке) + x2-разрез ДО диспетчеризации; результат — вопрос владельцу об арке, не решение.
4. После BT — CB-121 (check-then-act внутрилоковой сверки SHA), если слот свободен.

**Пакет DIR-2 (сессия 3):**
1. *Механический юнит «экспонировать grouping»* — ратифицирован владельцем 2026-08-22.
   *Замысел:* 530 невызываемых строк выглядят отгруженной фичей; потребитель — сам каскад
   (стратсессия, батч-разбор бэклога). *Гипотеза:* по образцу `recent` (Т-16): CLI-верб + MCP-тул
   поверх готовых `citation_report`/`tag_report`/`filing_report`, без переписывания логики,
   golden регенерирован из worktree с `PYTHONPATH=src`. *Приёмка:* оба хода (CLI и MCP)
   вызывают один домен-функционал; `codebugs --mode` изолирует модуль; ноль новых SQL вне модуля;
   CB-47 (обновить скилл batch-codebugs) пересмотреть как потребителя — закрыть или переписать
   evidence.
2. CB-122, остаточные ячейки CB-21 — механика без владельца, в любом порядке.
3. ~~Фронт BT — ЖДЁТ ответов владельца~~ — **ИСПРАВЛЕНИЕ куратора той же сессией (2026-08-22):
   ошибка, все три ответа даны 2026-08-21 (блок «Ратификации владельца, 2026-08-21»); строка
   «ждёт ответов» была написана до них и скопирована без сверки.** Фронт BT ОТКРЫТ, порядок
   ратифицирован владельцем (WIP ≤ 1 BT): **(i) BT «локационный якорь CB-95»** — дизайн-док,
   скоупить против CB-65, замысел: локация находки не должна гнить с первого редактирования
   (Б-8); x2-разрез до диспетчеризации; итог — док на ратификацию владельцу, не код;
   **(ii) BT «merge policy дедуп-слияний» (В-3/CB-46)** — следом, дизайн-док с вариантами и
   ценой, БЕЗ реализации; решение — при ратификации дока. Плюс механический юнит без BT:
   **`query(commit=)` учится видеть occurrence-ring** (ратифицировано «да»): приёмка — запрос по
   коммиту ре-наблюдения находит карту, чья первозапись была на другом коммите; golden.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 сессии 3 принят куратором 2026-08-22.**
Спот-чек: `3caced3` (Т-23: `_guard_enforcement_armed` требует commit-msg-хук через хелпер
`_hook_source_known`; CI-limits п.4 перемерен — `enforce_admins=true`, require-PR нет) — мерж с
двумя родителями на first-parent main; `b0d2d45` (факт приёмки §9), `ca2a5c3` (переминт после
коллизии Т-21 с DIR-2 — TOCTOU аллокатора К-2 между чтением хвоста и записью, второй случай),
`3acf6b5` (инструмент `module_surface.py`), `d7a3ded` (BT-6 v5), `efbfe29` (Э-11) — все предки
main. Пп.1–2 пакета закрыты одним юнитом; Э-9 закрыта фактом. **Э-10 закрыта куратором**: оба её
вопроса — те самые, что владелец ратифицировал 2026-08-22 00:03 (grouping экспонировать; пилот —
да); держатель не обновил статус. **П.3 (BT-6, пилот генератора) ОСТАНОВЛЕН держателем по
заранее объявленному условию после пяти проваленных x2-проходов** — находки сходятся: любая
СТРОЧНАЯ мера чувствительна к представлению прозы (докстринг / `description=` / неявная
конкатенация / экранированный `\n` / f-строка дают разные числа за один текст), любая мера
ФОРМЫ («сведено к циклу») обходится делегированием в хелпер. Остановка — правильное поведение
(2): пять ревизий чинили инструмент вместо договора о том, что измеряется. **Э-11 — вопрос
владельцу, переформулирован куратором с нуля (см. ниже ответ).** Побочное: ревьюер пересобирал
`.venv` репо под другой Python (восстановил сам) — в шаблон промптов ревьюеров: запрет
`uv run --python`/`uv sync` в репозитории. CB-121 не начат (WIP ≤ 1 BT).

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-22 (вторая, неизменяемый факт): Э-11 / BT-6 — вариант (А)+(Б).**
(А) грамматика файла деклараций ограничена линтом-отказом (без неявной конкатенации, `\n` внутри
строк и f-строк; пин интерпретатора) — число прозы осмысленно только внутри неё, и это говорится
владельцу одной строкой; (Б) планка второго модуля — тест существования «подмени генератор
заглушкой → модуль теряет все тулы и вербы». Пилот диспетчеризуется после v6 и одного x2-прохода
по этим двум изменениям (не шестой полной ревизии). Итог пилота — вопрос владельцу об арке В-5(а).

**Канон моделей (из global CLAUDE.md, 2026-08-22): верхний тир по умолчанию — opus; Fable — только
по имени, для результата, который никто не перепроверяет.** Держатели (2) и менеджеры (3) — opus;
объём — sonnet/haiku. Сессии 3 обоих направлений шли на Fable по наследованию — это была ошибка
куратора, исправлена с этой сессии.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-2 сессии 3 принят куратором 2026-08-22.**
Спот-чек: `6617f51` (Т-21/CB-127: три MCP-тула `grouping_*` + три CLI-верба, тонкий проброс, golden
+201/−0) и `80e5ac4` (Т-22/CB-128: `query(commit=)` видит occurrence-ring, read-side only) — мержи с
двумя родителями, предки main; CB-127/CB-128/CB-47 `fixed`, CB-129 `open` (новая: CLI `--meta`
молча побеждает `-l`, класс CB-15). Изолированная приёмка 7/7 на обоих. CB-122/остаток CB-21 —
были закрыты Т-17 до сессии (пункт пакета был остатком памяти куратора). **BT-7 (локационный якорь
CB-95) — v1 и v2 провалили x2; несущая предпосылка v2 опровергнута перемером держателя: доля
ре-наблюдений в обоих корпусах 0/130 и 0/3176**, а v2 строила хранение «в ринг на каждом
наблюдении» на этой популяции. Бриф v3 — `70c1129`; v3 пишется следующей сессией С ПЕРЕМЕРОМ ДО
написания. Владельцу по BT-7 пока нечего. **Принято в канон (К-5):** для BT обязательный перемер
несущих чисел держателем ОТДЕЛЬНЫМ шагом до первого x2, с записью «кто и когда мерил» — два
дизайн-дока DIR-2 подряд провалили x2 в секциях «измерено». Эскалации 1–2 (staged-файл чужой
сессии блокирует finish'и; менеджер после блокировки — polling, не ожидание пробуждения) — в шаблон
брифа менеджера и как кандидат-карта харнеса для DIR-1 (pre-commit-отказ должен говорить, что
staged-файл блокирует чужие finish'и).

**Пакет DIR-1 (сессия 4):** 1. BT-6 v6 — ТОЛЬКО (А)+(Б) по ратификации: линт-отказ грамматики
деклараций + пин интерпретатора; планка T_second = тест существования; один x2-проход по дельте;
при «диспетчеризуема» — менеджер (3) на `bench.py`. 2. Итог пилота — две колонки код/проза до/после
+ три оговорки → мне, для вопроса владельцу об арке В-5(а). 3. Карта: pre-commit-отказ на main
называет, что staged-файл блокирует чужие `worktree-finish` (эскалация DIR-2 №1), механическая.
4. CB-121 — если слот свободен.

**Пакет DIR-2 (сессия 4):** 1. BT-7 v3 по брифу `70c1129`: СНАЧАЛА перемер несущих чисел отдельным
коммитом (К-5), ПОТОМ текст; полный x2 (атака обеих моделей + защитник + судья); при «готов» —
возврат мне с доком. 2. CB-129 — механика, параллельно. 3. Merge-policy BT не открывать (WIP ≤ 1).

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 сессии 4 принят куратором 2026-08-22.**
Спот-чек: `e842952` (Т-25/CB-130: pre-commit и `_guard_main_clean` называют, что staged-файл
блокирует чужие интеграции; 7/7) — мерж с двумя родителями, предок main; `c9a2f25`, `70cc4cd`,
`22f13af`, `5c85c5f`, `66f6b3d`, `3ffdbdf`, `d98d745`, `9060b10` — предки main; CB-130 `fixed`,
CB-134/CB-135 `open`. **BT-6 v6 ((А)+(Б)) провален шестым x2-проходом, обе модели, решающее
воспроизведено держателем:** (Б) проходится при нулевой миграции одним булевым гейтом перед
нетронутыми телами; (А) делает честным одно слагаемое из трёх (сторона «до» и сам генератор вне
грамматики). Пилот НЕ диспетчеризован — правильно. Держатель по дороге нашёл, что колонка «проза»
— не «текст описаний» (287 из 541), оверклейм снят. **Э-12 — вопрос владельцу, переформулирован
куратором с нуля (ниже).** CB-121: принята форма (c)+(d) — пост-мердж-алярм + сужение строки
таблицы CLAUDE.md; диспетчеризация сессией 5. Третья коллизия минта подряд → **решение куратора:
номер выдаёт скрипт, не автор** — юнит DIR-1 (механика аллокатора CASCADE-IDS).

**Решение куратора по Э-13 (разрыв гейта: worktree 3.13.3 vs main 3.14.4; CB-135, CB-134).**
Территория — DIR-1 (харнес). Форма: сделать состояние непредставимым + fail-closed — **пин
интерпретатора в `.python-version`** (один источник для main и каждого worktree) **плюс отказ
`worktree-finish.sh` при расхождении версий** между worktree и main (та же форма, что
`_guard_enforcement_armed`: отказ, не предупреждение). Отвергнуто: «алярм после мержа» (гейт,
описанный лучше, чем ведёт себя) и «запись в CLAUDE.md» (проза не энфорсит прозу — урок CB-50).
CB-134 (argparse 3.14 трогает stdout при построении парсера; премиса теста CB-78 ложна на 3.14)
— в тот же пакет, отдельным юнитом; пин обязан выбирать версию, на которой сьют зелёный на ОБОИХ
деревьях, а не ту, что случайно в main.

**Пакет DIR-1 (сессия 5):** 1. CB-135 + CB-134 (выше). 2. CB-121 по принятой форме (c)+(d).
3. Аллокатор CASCADE-IDS: номер вычисляет скрипт (`max+1` по файлу после pull), автор не набирает
literal. 4. BT-6 — ЖДЁТ ответа владельца на Э-12; не трогать до ратификации.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-22 (третья, неизменяемый факт): Э-12 / BT-6 — вариант (Г).**
Проза измеряется как ОТРЕНДЕРЕННАЯ поверхность (`tools/list` + `--help`), инвариантная к записи по
построению, и становится ОГРАНИЧЕНИЕМ пилота (поверхность до/после равна); заголовочное число —
только Δ строк кода (без строковых литералов), с названной оговоркой о раскладке внутри
`ruff format`. Вопрос пилота отныне: «сокращается ли код при той же поверхности». (Б) в
метаморфной форме («удали одну декларацию — исчезнет ровно её поверхность») сохраняется как
планка T_second. BT-6 v7 = только дельта (Г); один x2-проход по дельте; при «диспетчеризуема» —
пилот на `bench.py`. Пакет DIR-1 сессии 5 п.4 снят: BT-6 разблокирована, идёт ПОСЛЕ п.1 (CB-135 —
иначе «до» и «после» считались бы разными интерпретаторами).

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-2 сессии 4 принят куратором 2026-08-22.**
Спот-чек: `8d6521c` (Т-24/CB-129: конфликт `--lines`/`meta.lines` — отказ, модульный
`_ADD_META_FLAGS`) — мерж с двумя родителями, предок main, приёмка изолированная; CB-129 `fixed`;
новые карты остатков: CB-131 (близнецы-апдейтеры), CB-132 (traceback на `--meta 'не json'`),
CB-133 (`-l ""` truthiness). **BT-7 v4 (`03bd802`) — третий x2 пройден, судья 6/10 «шесть фиксов
и отправлять, четвёртый x2 не гонять»; фиксы применены.** К-5 сработал наполовину: перепись
воспроизвелась, но три промаха v3 были не арифметикой, а подменой предиката («88% захвата» было
долей читаемой колонки, реальный потолок 20%/36%). **Принято в канон (К-5б, по предложению судьи
и держателя): каждая цифра, уходящая владельцу, стоит в таблице с предикатом, знаменателем, хешем
выгрузки и датой; цифра в прозе без строки таблицы — дефект** (реализация — §1б BT-7 v4).
Также в канон: документ, проваленный на числах — узкий числовой аудит, не полный x2; артефакты
x2 именуются с версией дока (затирание чужого ревью поймано). **Четыре вопроса §8 переданы
владельцу куратором с нуля; ответы — следующим блоком.**

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-22 (четвёртая, неизменяемые факты) — ответы по BT-7 §8, и ядро
— РЕДИРЕКТ, не «да»:**
- **№1 (ядро): владелец предлагает «reverse blame» через git** (вербатим: «а можно ли использовать
  git? Как blame, только наоборот? Не придумывать велосипед (хотя это тоже можно), а зная дату
  (в идеале, git version), понять, где сейчас этот код. При этом различные эвристики помогли бы
  найти оригинальный код по дате/времени, а затем проследить, куда приведёт. Так даже split/rename
  и т.п. можно отследить!»). Замысел куратором прочитан так: якорь опирается на КОММИТ времени
  заведения (`reported_at_commit` уже хранится и заморожен — CB-53), локацию ищем в ТОЙ версии,
  где она почти наверняка есть, и трейсим историей вперёд до HEAD — вместо пересканирования
  текущего файла эвристиками. v4-механика (нормализация+хеши+контекст) — не отвергнута, но
  вторична: «велосипед тоже можно».
- **№2: текст якорных строк ХРАНИТЬ** — «для reverse blame, видимо, нужен текст; для других VCS
  тоже; места не жалко; хеши лучше по безопасности — не сказал бы, что это тут аргумент». Канал
  чтения файлов через add владельцем принят как цена. «Нужно проверять эмпирически, что работает
  и стабильно. Может, AST как один из каналов.»
- **№3: вне рабочего дерева — НЕТ** (кросс-репо — честный отказ).
- **№4: символьный якорь — по замеру.**
BT-7 возвращается держателю DIR-2 на v5: пере-центровка на reverse-blame-ядро, эмпирическая
проверка стабильности разрешения на живой истории обоих репо (К-5/К-5б), узкий числовой аудит
вместо полного x2, если структура не меняется радикально.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 сессии 5 принят куратором 2026-08-22.**
Шесть юнитов, все спот-чекнуты как двухродительские мержи на first-parent main: Т-26/CB-134
`8dea605`, Т-29/аллокатор `6f55e9c`, Т-27/CB-135 `76b3d3b`, Т-30/CB-138 `00a0570`, Т-28/CB-121
`354cee7`, Т-31/CB-140 `c200aac`. Закрыты CB-121/134/135/138/140. Пин интерпретатора работает
(один `python -V` в main и worktree, отказ finish rc 14), аллокатор ID применён трижды без
коллизий, CB-121 в форме (c)+(d). **В леджер процесса: три из шести юнитов породили преемника
ПРИЁМКОЙ, а не планированием — изолированный приёмщик, сам гоняющий мутанты, трижды опроверг
отчёт исполнителя.** Открыты и названы: CB-136, CB-137, CB-139 (high), CB-141.
**Решение куратора по Э-14:** CB-139 — механический юнит немедленно (`ci.yml` без `fetch-depth: 0`
— job `tests` структурно красный навсегда, регрессий никто не увидит; асимметрия с
`main-invariants.yml` записана в карте); **после зелёного CI — пуш main на origin** (обычная
синхронизация собственного репо владельца; защита ветки включена и ратифицирована; дистанция ~100
коммитов — само её существование есть симптом CB-139). **Э-15 (BT-6, седьмой провал гейта:
поведенческая планка не доказывает архитектурное свойство) — вопрос владельцу о трате,
переформулирован куратором с нуля; ответ следующим блоком.**

**Пакет DIR-1 (сессия 6):** 1. CB-139 (fetch-depth в ci.yml + комментарий-требование по образцу
main-invariants.yml); после зелёного прогона job `tests` на origin — `git push` main. 2. CB-136,
CB-137, CB-141 — механика, в любом порядке. 3. BT-6 — ЖДЁТ ответа владельца на Э-15.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-22 (пятая, неизменяемый факт): Э-15 / BT-6 — пилот запускается
БЕЗ механического гейта.** Метрика — (Г) как ратифицирована (поверхность `tools/list`+`--help`
равна до/после; заголовок — Δ строк кода); честность числа проверяет изолированный приёмщик
ГЛАЗАМИ по чек-листу всех известных из семи раундов игр (переезд прозы между формами записи,
неявная конкатенация/`\n`/f-строки, делегатор-обход, таблица имён при нулевой миграции,
раздувание базы опечаткой, разные интерпретаторы до/после, JSON-сайдкар). Восьмая формулировка
механической планки НЕ пишется; правило резидентности не строится. Основание владельца: запрет
был на СЛУЧАЙНО льстивую цифру, исполнитель — не противник; суждение приёмщика, трижды за день
поймавшего неверные отчёты, достаточно. Пакет DIR-1 сессии 6 п.3 снят: BT-6 диспетчеризуется
после п.1 (CB-139/пуш), чек-лист игр — обязательная часть брифа приёмщика.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-2 сессии 5 (BT-7 v6) принят куратором
2026-08-22.** Спот-чек: `8c5881f`/`229b63d` (эмпирика reverse blame по К-5, отдельными коммитами
до текста), `367e936` (v5, пере-центровка на редирект владельца; захват через `git show` сделал
неверсионируемое недостижимым ПО ПОСТРОЕНИЮ — снят целый раздел рисков v4, включая чтение
собственной базы трекера; документ 903→532 строки), `1e9f253` (v6 после узкого аудита + одной
структурной атаки: ~48 цифр проверено, 17 не воспроизвелось, 3 FATAL закрыты) — все предки main.
**Главная находка — держателя против себя: «git ошибается в 0.21%» опровергнуто — ошибалась
МЕРИЛКА** (игнорировала `filename` в выводе blame — ровно тот механизм, который док объявляет
ловцом переезда); исправленно git верен **537/537 побайтно**. Честные числа v6: покрытие ~21%
(codebugs) / ~6% (autosorter) — потолок задаёт подача (64%/79% карточек не говорят, где смотреть);
разрешение 54%/52% строго, 73%/60% мягко (публикуются обе); согласие двух каналов 133/133.
**Принято в канон (К-5в, третье уточнение К-5б, оплачено ошибкой):** у несущего числа называется
и ОПЕРАЦИЯ замера — скрипт кладётся рядом с записью как опровержимая улика. Форма «узкий аудит +
структурная атака» оправдалась (~4× дешевле полного x2, обе несущие находки из разных ролей);
оговорка держателя честная — арбитраж расхождений лёг на (2). **Вопрос владельцу один
(moved_file), переформулирован куратором с нуля; ответ следующим блоком.**

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-22 (шестая, неизменяемые факты): BT-7 v6 РАТИФИЦИРОВАНА** —
локационный якорь на reverse-blame-ядре строится, нарезка юнитов по эскизу v6. **moved_file —
«статус + ре-кей позже»**: сейчас карточка остаётся на старом `file`, якорь отвечает видимым
статусом `moved_file` с новым путём/строкой; явный санкционированный ре-кей (по образцу
нормализации категорий) — отдельным решением по замеренному спросу; счётчик спроса — частота
`moved_file` в живых данных, DIR-2 закладывает его читаемым (`query`-able), чтобы решение было
арифметикой.

**Пакет DIR-2 (сессия 6):** 1. Нарезка BT-7 v6 в юниты реализации (§9 эскиза; сериализация общих
файлов с DIR-1 — types.py, golden, CLAUDE.md — по хвосту CASCADE-IDS и аллокатору); тихий ложный
«потеряно» для невлитой ветки закрывается в первом же юните ядра (названо в v6). BT-7 как BT
закрывается ратификацией; юниты — механика. 2. **BT-8 «merge policy дедуп-слияний» (В-3/CB-46)
открывается** — ратификация Б3 от 2026-08-21 («дизайн-док заказывается, БЕЗ реализации») и
порядок «CB-95 первой, merge policy следом» исполнены; полный К-5/К-5б/К-5в с первого дня.
3. CB-131/CB-132/CB-133 — механика-филлер параллельно.

**ПРИЁМКА (1)←(2), неизменяемые факты, 2026-08-22 (вечер): возвраты DIR-1 сессии 6 и DIR-2
сессии 6 приняты куратором.**
- DIR-1: `ea301b4` (CB-139 — `fetch-depth: 0` в ci.yml, структурный тест; мелкий клон краснит,
  полный зелёный), `329cccd` (CB-136), `a396043` (CB-137), `cdab482` (CB-141), `859ce3b` (**пилот
  BT-6, Т-36**) — все двухродительские на first-parent main. **Пуш `3e96210..4c5f01f` (118
  коммитов) выполнен; CI зелёный впервые с появления дефекта** — job `tests` и матрица
  `contracts` 3.11–3.14 success, `main invariants` success; `main = origin/main` на момент пуша
  (после него — только заметки куратора). **Итог пилота BT-6 — ОТРИЦАТЕЛЬНЫЙ: `Δcode = +182`
  (код ВЫРОС) при байт-равной поверхности (sha256 совпал, сторона «до» перевыведена приёмщиком
  независимо через `git archive`); `Δprose = +199`; генератор G = 119 один раз; собственный код
  bench.py −69, генератор+декларации +251; +45 строк комментариев не видны колонкам — рост больше
  заявленного. Все семь строк чек-листа игр приёмщик прошёл сам: «не взята».** Миграция
  настоящая (в tip bench.py живых `@mcp.tool`/`add_parser`/`help=` ноль). Оговорка держателя:
  bench — T3-тяжёлый модуль, заведомо не лучший кандидат (но выбор НЕ-простейшего — условие
  владельца). Новые карты: CB-142 (job-level `if:` — гейт, который не может сработать), CB-143
  (`export-csv /dev/stdout > файл` молча портит заголовок CSV).
- DIR-2: `cf2ad1e`/`b3d6788`/`d58d7fd` (Т-37/38/39 — три юнита ядра BT-7, изолированная приёмка;
  main после мержа 2200 passed), `7961bcf` (BT-8 v2 merge policy с тремя вопросами), `2ebf14a`+
  `a5dd307` (буква ратифицированного BT-7 приведена к одному смыслу), `daaaa73`. Осталось по BT-7:
  Т-b (`anchor_resolve` + сводка статусов для арифметики moved_file), Т-c (backfill). CB-132/133
  закрыты; CB-131 ждёт golden. **CB-144 (high): CLI-верб `add` не захватывает
  `reported_at_commit` — такая карточка не заякоривается ни одним каналом; потолок покрытия
  ратифицированной фичи** — в первый пакет следующей сессии DIR-2. Каноны К-8 (имя ветки
  card-driven юнита несёт cb-NN, иначе харнес не клеймит) и К-9 (пин класса по Р1) — приняты.
**Решения куратора:** Э-17 — код возврата `74` (EX_IOERR: «мой вывод не записан», ничего не
утверждает об эффекте) ПРИНЯТ; основание держателя (rc 1/120 сегодня лгут о причине при
приземлившейся записи) измерено, альтернативы 0/141 отвергнуты верно; владельцу сообщается, не
спрашивается. Э-19 — шапка `CASCADE-IDS.md` («строки не редактируются») приводится к гейту
мультимножества — юнит DIR-1, буква под смысл. Э-16 (нужен ли T_second) — снимается: пилот дал
ответ без второго модуля. **Э-18 (арка В-5(а)) и три вопроса BT-8 §5 — владельцу, батчем,
переформулированы с нуля; ответы следующим блоком.**

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-22 (седьмая, неизменяемые факты):**
- **Арка В-5(а): ВТОРОЙ ПИЛОТ на тонком модуле-пробросе** (не отклонение и не принятие): если
  генератор не выигрывает и там — арка снимается арифметикой; если выигрывает — арка на все 136
  всё равно не сходится (bench измерен), решение станет «генератор для тонких модулей, руки для
  тяжёлых» — это новый вопрос, не этот.
- **BT-8 (merge policy): развилка — ОСТАВИТЬ КАК ЕСТЬ + СЧИТАТЬ ОТКАЗЫ** (политика — по замеренному
  спросу; тот же приём, что moved_file); **backfill отпечатков — НЕ ВЫВОДИТЬ**; **статус слитой
  карточки — `merged` как НАПРАВЛЕНИЕ** (строится только вместе с политикой). BT-8 закрыта
  ратификацией; из неё один механический юнит — счётчик отказов «отпечаток занят живой CB-N»
  читаемым (query-able).

**Пакет DIR-1 (сессия 7):** 1. Второй пилот BT-6 на тонком модуле — кандидат по матрице BT-3
(T1-доминантный проброс, не findings/similarity/provenance — DIR-2; не bench), та же метрика (Г),
тот же генератор (G уже оплачен — в дельту второго модуля не входит, названо отдельно), тот же
чек-лист игр у приёмщика; итог — две колонки → мне. 2. Э-19: шапка CASCADE-IDS под гейт. 3. CB-142,
CB-143 — механика. 4. После пилота — матрица способностей как живой инструмент: дыры поверхности
(CB-107 и др.) возвращаются обычными юнитами независимо от решения по арке.
**Пакет DIR-2 (сессия 7):** 1. CB-144 (high) — CLI `add` захватывает `reported_at_commit`, иначе
якорь не работает для CLI-подач. 2. BT-7: Т-b (`anchor_resolve` + сводка статусов, счётчик
moved_file) и Т-c (backfill якорей — это НЕ backfill отпечатков, разные вещи, названо). 3. Счётчик
отказов развилки дедупа (из BT-8). 4. CB-131 по освобождении golden.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-1 сессии 7 принят куратором 2026-08-23.**
Спот-чек: `a2229da` (второй пилот BT-6, `sweep.py`, двухродительский на first-parent), `42effb1`
(CB-142), `3a16502` (Э-19 / Т-40), `9fa5d1d` (факты) — предки main; CB-142 fixed. **Второй пилот:
Δcode = +128 (+96 под другой раскладкой ruff — знак не меняется), Δprose = +63; ΔG = +2;
H_manual 185/106; MCP-сторона сгенерирована полностью (9/9 тулов); поверхности байт-равны; три
независимых перевывода совпали; приёмщик — все восемь строк чек-листа «не взято» (восьмая — добавка
держателя: рост общего генератора, списанный на «G оплачен»).** Кандидат выбран как ЛУЧШИЙ случай
для генератора (18 поверхностей, все T1, девять пар 1:1), чтобы отрицательный результат нельзя было
списать на размер. Арифметика обоих пилотов: bench (8 пов., T3) +182; sweep (18 пов., T1) +128.
**Решения куратора:** Э-20 — CB-143 (`export-csv /dev/stdout > файл` портит заголовок) ПЕРЕДАЁТСЯ
DIR-2 как хвост её работы в findings.py — принято; Э-22 (аллокатор: половину `%ID%` взять, отказ по
литералу отвергнут по прогону против архива) — принято. Новые карты CB-145…CB-148 — заведены с
формой не выбранной, честно размечены «воспроизведено/прочитано». **Сессионные факты в канон:
признак приземления — `git log`, не уведомление агента (уведомление пилота пришло через 1.5 ч после
мержа); `Edit` у субагентов отключён — мутационные пробы через копию вне репо.** Арка В-5(а) —
владельцу, арифметика двух пилотов; ответ следующим блоком. DIR-1 до ответа — п.4: дыры
поверхности из матрицы обычными юнитами (первый — CB-107), независимо от решения по арке.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-23 (неизменяемый факт): ТРЕТИЙ ПИЛОТ BT-6 — форма АННОТАЦИЙ.**
Владелец, посмотрев код второго пилота («выглядит так себе»), назвал гипотезу: проблема в
реализации, не в идее — декларация словарями есть самая тяжёлая кодировка, выбранная ради
грамматики счётчика. Форма третьего пилота: декоратор/аннотации над СУЩЕСТВУЮЩЕЙ функцией
(сигнатура+докстринг уже есть MCP-декларация; выводить CLI из них: тайпхинты → типы, дефолты →
флаги, Args-секция → help=), одна строка на поверхность вместо словаря. Модуль — ТА ЖЕ `sweep`
(база a2229da^1 измерена и зафиксирована — прямое A/B трёх форм), метрика — та же (Г), чек-лист
приёмщика — тот же плюс девятая игра: «переопределения для расходящихся поверхностей, не
посчитанные как декларации». Preflight обязан измерить: сколько из 136 поверхностей пакета
РАСХОДЯТСЯ между MCP и CLI намеренно (SURFACE_GAPS в test_update_parity — задекларированные
дыры) — переопределения вернут словарную цену ровно там. Честные рёбра формы, названные заранее:
вывод help= из докстринга — парсинг прозы (класс CB-73); тайпхинт без маппинга и докстринг без
Args — громкий отказ, не догадка. Решение об арке — после третьего числа.

**ПРИЁМКА (1)←(2), неизменяемый факт: возврат DIR-2 сессии 7 принят куратором 2026-08-23.**
Спот-чек: `95fbcdf` (Т-41: CB-144 fixed — CLI add захватывает reported_at_commit; счётчик отказов
развилки `meta.fingerprint_refusals`), `8367958` (Т-43/Т-b: anchor_resolve + anchor_recapture,
golden +160/−0, loc.py zero-SQL), `48f5a22` (отказ приёмки Т-41 — докстринг «все четыре вызывающих»
при пяти: пятый приехал форвард-мерджем), `0b4f49b`+`41cfab5` (флаг канала A возвращён к `-C` по
перемеру; класс «цитата вместо переезда» ПЕРЕЖИЛ смену флага — запинен как известный предел,
триггер похода к владельцу арифметический: первый moved_file с не-кодовым назначением), `8ec108b`
(handoff сессии 8) — все предки main. **Принято в канон (К-10, оплачено отказом Т-41):
предпосылка, установленная перечислением, переигрывается ПОСЛЕ форвард-мерджа — он добавляет
членов в популяцию; механизация — ратчет-тест на число сайтов.** Сессия остановлена по плану Б
куратора (сетевые сбои валили сторожа), не по деградации. Преемнику (сессия 8, handoff §2):
приёмка Т-43 (приёмщик умер, worktree /tmp/t43-probe-379039 брошен — снять), Т-46 (CB-131 +
остатки Т-41), Т-c (backfill якорей), CB-143.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-23 (вторая, неизменяемый факт): третий пилот ОСТАНОВЛЕН на
замерах («в целом, можно остановиться»); образец кода формы C показан владельцу куратором как
реконструкция из замеров (кода формы C не существует — ветка Т-49 несёт только
T49-MEASUREMENT-*).** Арка В-5(а) закрывается по рекомендованной опции: CB-66 → wont_fix с
приложенной арифметикой (bench +182, sweep +128, замеры №1 24%-проза / №2 недостижимость
предусловия); bench/sweep остаются как есть; ветка Т-49 остаётся веткой с замерами. Парность
MCP/CLI — дисциплина + parity-тесты + матрица способностей.

**ПРИЁМКА (1)←(2), неизменяемый факт: пакет DIR-1 сессии 7 закрыт, принят куратором 2026-08-23.**
Спот-чек: `f16764e` (Т-45/CB-107: четыре CLI-only способности получили MCP-тулы, dry-run дефолт
запинен), `c264e1a` (Т-47/CB-145: минт-гейт видит удаление строки; архивный прогон 52 переходов —
12 отказов идентичны до/после, фикс чистый), `a0de32e` (Т-51/CB-146+148) — предки main;
CB-107/142/145/146/148 fixed, CB-66 wont_fix с арифметикой; main == origin/main. Ветка Т-49 живёт
с двумя замерами, не приземлена — как ратифицировано. **Три из четырёх приёмок вернули находку,
которой не было ни у исполнителя, ни у держателя** (долг шапки CASCADE-IDS; `apply=1.0` пробивает
dry-run — pydantic коэрсит до isinstance, CB-151; снимок слеп к `help=` верба, CB-152) — леджер
«преемника рождает приёмка» пополнен. **В канон: юнит, доведённый держателем за умершего
исполнителя, получает слот эскалаций от имени доводившего с пометкой; возобновление сообщением
дешевле холодного перезапуска, когда есть что терять.** Открыты и названы: CB-147 (намеренно
после Т-51), CB-150 (латентные 12/52 ложных отказа гейта на реальной истории), CB-151 (medium,
живой вред: dry-run на массовой мутации), CB-152. **Пакет DIR-1 (продолжение той же сессии):
1. CB-151; 2. CB-147; 3. CB-150/CB-152 по слоту.**

**ФАКТ, 2026-08-24: третье число пилота существует** — менеджер Т-49, диспетчеризованный до
остановки, досчитал; ветка `1417ac7`, НЕ приземлена (ратификация соблюдена). Таблица трёх форм
(модуль-локально / разделяемая инфра / заголовок): A 840/0/—; B 966 (+126)/121 (119 предоплачено)/
**+128**; C 853 (**+13**)/168 (0 предоплачено)/**+181**. **Гипотеза владельца подтвердилась ровно
там, где обещала** — модуль-локальная цена упала на порядок (+126 → +13), «обёртка уже есть
декларация» работает — **и провалилась в заголовке**: инфраструктура C не предоплачена ничем,
итог хуже обеих форм. Центральное число: **30 из 30 параметров потребовали явной строки
переопределения, чисто выведенных — ноль** (42 коррекции: проза 22, имя 13, семантика 6,
пропуск 1). Решение об арке (wont_fix) числом ПОДТВЕРЖДЕНО. Замер №2 держателя опровергнут в
части вывода (не факта) самим исполнением: исполнение деклараций против заглушки-сервера — 14
строк, вынос функций не обязателен. Оговорка честности: колонка prose между B и C сравнима на
нератифицированном основании (у C нет файла деклараций — AST-проверка исполнителя вместо линта);
заголовки от неё не зависят. Приняты Т-52/CB-151 (apply=1.0 больше не пробивает dry-run — живой
дефект воспроизведён приёмщиком на обоих деревьях) и Т-53/CB-147; Т-54 (снятие «only») в
интеграции. Открыта **CB-153 (high): matrix.py докладывает 16 способностей без поверхности** —
следующая для DIR-1.

**ПРИЁМКА (1)←(2), неизменяемый факт: Т-50 принят, BT-7 ЗАКРЫТА ЦЕЛИКОМ — 2026-08-24.**
Спот-чек: `ed369ac` (backfill якорей), `38427eb` (факт приёмки: 11 мутантов, числа перевыведены
md5-идентичным скриптом до последней цифры), `24906b1` (поправка §1б ратифицированного дизайна:
входное число не воспроизвелось на запиненном корпусе — 35/129, не 47; сквозное покрытие ~14%,
не 28.7%; **урок в канон: хеш защищает ВХОД, но не ОПЕРАЦИЮ над ним — скрипт замера кладётся
рядом, К-5в подтверждён вторым инцидентом**) — предки main. Исполнитель дважды был сильнее буквы
брифа (трёхсостояниевый `_anchor_state` сделал оба дефектных предиката недостижимыми по
построению — принято). **В канон: сверять ИМЕНА тестов, не суммы** («добавили» неотличимо от
«заменили» по сумме). Новые карты: CB-156, CB-157 (high по смыслу: параметр MCP-тула можно зашить
в теле при живой схеме — полный сьют зелёный; популяция требует развёртки до починки). Очередь
DIR-2: Т-56 («якорь виден в обычном чтении»), затем CB-157/CB-156/CB-62. **Двум вопросам —
ратификация владельца ниже: pipx-снимок от 18.08 (клиенты не видят 6 дней пакета, loc.py
отсутствует в RECORD) и `--apply` backfill'а (22/272 координаты, 134/3083 отказных объекта).**

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-24 (неизменяемые факты):** (1) **pipx reinstall — «да, выполни
сам»**: выполнено куратором той же минутой, RECORD теперь несёт loc.py (проверено); живые
MCP-серверы держат старый код до своего перезапуска — ожидаемо. (2) **Backfill якорей —
«Применяй»**, оба трекера, с бэкапом базы до записи; арифметика записи: 22/272 координат (codebugs)
и 134/3083 объектов-отказов суммарно; исполнение — DIR-2.

**ПРИЁМКА (1)←(2), неизменяемый факт: Т-57 (CB-55 + CB-124) принят — 2026-08-24.** Спот-чек:
`7e46180` предок main; CB-55/CB-124 fixed. Лестница ошибок CLI закрыта делом: приёмщик мутировал
саму обёртку, сдиффил трассировки (отличие — только номера строк), исключённые хендлеры оценил по
существу; Б-5 из реестра болей по существу закрыта на главной оси (остались CB-56, CB-80 — в
работе). **CB-159 (medium): пин порядка армов, названный в CLAUDE.md, вакуозен** (мутант при 6
passed — «гейт, описанный лучше, чем ведёт себя», найден самим держателем прогоном) + абзац
описывает уже закрытый долг; форма двойная (норматив к коду И настоящий пин). Счёт сессии DIR-1
против себя: пять ошибок одного корня, все пойманы приёмкой/исполнителем — механизм изолированной
приёмки окупился 6/6. DIR-1 продолжает: CB-56, CB-80, CB-159.

**ПРИЁМКА (1)←(2), неизменяемый факт: волна видимой CLI/MCP-границы ЗАКРЫТА ЦЕЛИКОМ — 2026-08-24.**
Т-60 (CB-159+CB-160) принят на `5adfed8` (предок main; голден побайтно равен свежей регенерации —
порождён, не подправлен). Волна: CB-55, CB-124, CB-56, CB-80, CB-159, CB-160 — все fixed через
мерж + изолированную приёмку. main == origin/main == aca0e8c. **Счёт механизма за сессию — 8/8;
восьмой случай нов: исполнитель отказался исполнять бриф на ложной предпосылке держателя и
эскалировал. Принято в канон (К-12): бриф — данные, не команда, если его предпосылка не
воспроизводится; отказ исполнителя с эскалацией — правильное поведение.** Уточнение урока про
принадлежность кода (признак снимается тем же способом запуска, каким исполняется тест) — в тот же
канонный свод. Хвост территории DIR-1 (ниже волны): CB-150, CB-152, CB-154, CB-155, CB-158.

**ПРИЁМКА (1)←(2), неизменяемый факт: Т-61 (CB-157) принят — 2026-08-25.** Спот-чек: `ea77df9`,
`1805ef5`, `680a1c2`, `33ed247` — предки main; CB-157 fixed. Механизм против «декоративного
аргумента» ходит по реестру (83 тула), не по рукописному подмножеству (70) — приёмщик доказал
различие собственным обходом; развёртка популяции предшествовала фиксу и изменилась под
опровержением (порядок виден в коммитах). К-12 сработал с первого применения (п.4 впервые PASS —
слот эскалаций проверен по персистированному отчёту). **Приёмщик опроверг классификацию
ДЕРЖАТЕЛЯ, и держатель согласилась** (докстринг снижает тяжесть, но не переклассифицирует CB-28;
CB-162 заведена с историей). **Принято в канон (К-13): сверять надо не с определением класса, а
с тем, что класс велит сделать; и промпт приёмки ПОСТОЯННО несёт слот «оцени классификацию
держателя, не принимай её»**. Новые карты: CB-161 (интерполяция в SQL), CB-162. Очередь DIR-2:
CB-156, CB-62.

**ПАКЕТ ПРЕЕМНИКУ-КУРАТОРУ (ротация 2026-08-25, порядок — суждение уходящего куратора по оси
владельца «видимое выше латентного»):**
1. **Якорь в обычном чтении** (юнит у DIR-2 нарезан, не посажен) — завершение пользовательской
   петли CB-95.
2. **Переприоритизация реестра болей** — долг куратора: Б-8 закрыта делом, Б-5 закрыта на
   главной оси, Б-9 наполовину (exposure wont_fix; relations-темп — «позже» владельца); порядок
   в трекере (req PAIN-ORDER) привести к реальности, с кросс-модельным проходом ДО того, как
   порядок поведёт работу.
3. **CB-62** (полезная ось группировки) + решение «один хозяин графа связей» (эскалация DIR-2:
   граф описан в relations.py и grouping.py, split_from в обоих).
4. **Класс честных границ добить**: CB-158 (limit=0 в домене), CB-161 (интерполяция в SQL),
   CB-162 (молчаливое сужение last_n).
5. **CB-150** (12/52 латентных ложных отказа минт-гейта) — по слоту; **CB-90** (59 загрязнённых
   описаний, identity-вход) — кандидат на подъём в переприоритизации.
Спит по решениям владельца (НЕ долг): CSV (Т-48 запаркован с записью состояния); relations-
миграция (В-4 «позже»); merge policy (по счётчику `fingerprint_refusals`, уже тикает); ре-кей
moved_file (по счётчику). Записка дизайнеру оркестрации сдана: SPEC-FEEDBACK-cascade-2026-08-25.md
(fca898f) — передать адресату, если владелец не отнёс сам.

**ПЕРЕПРИОРИТИЗАЦИЯ 2026-08-25 (куратор-преемник, п.2 пакета; порядок — в §2 и в req PAIN-ORDER).**
Владелец ратифицировал очередь брифа DAILY-2026-08-25 словами «по приоритетам дневного брифа
согласен» (2026-08-25); новый порядок — её развёртка по оси «видимое выше латентного» на свежем
снимке трекера (170 карт, открытых 36: 0 critical/high, 17 medium, 19 low; 5 в работе у направлений).
**Кросс-модельный проход: Codex (`codex exec`, 221 тыс. токенов, читал карты сам) — 14 возражений к
черновику, 5 блокирующих.** Принято 12: снимок устарел (CB-95 уже fixed, CB-164…CB-170 заведены
направлениями); четыре карты и два хвоста были нигде не размещены (CB-94, CB-100, CB-104, CB-105,
CB-91, CB-96); CB-87 закрыта Т-19 и в открытом блоке ей не место; **CB-143 и CB-117 — живой видимый
вред, а не латентный** (CB-117 сама несёт раздел «why it is live and not latent»), обе подняты в блок 1
выше CB-62; CB-163 НЕ разблокирует CB-62 — сама карта (держатель DIR-2, по прецеденту CB-44/CB-45)
сужает остаток CB-62 до одного потребителя и оставляет унификацию графов измеренному спросу; CB-112
переоценена — смягчение (`grep -oiE … | sort -u` по имени ветки) записано в карте, остаток —
молчаливая конвенция; CB-149/150 — латентный класс (исторические коммиты), не «живой вред
операторам»; Б-11 без карты в очередь не ставится, но если подтвердится — потеря данных выше блока;
подпись «хвост low» в честных границах была ложной (CB-158/161/162 сами low); CB-6 и CB-68 — вопросы
правила; CB-65 не «неисполнима» — калибровка и пять слияний исполнены, два вопроса владельцу живы;
«Т-57/Т-60» → «волна Т-57…Т-60». **Отвергнуто 1, замером:** Codex утверждал по карте CB-59, что
`enforce_admins` выключен; `gh api …/branches/main/protection` 2026-08-25 даёт `enforce_admins: true`
при запрещённых force-push и удалении — карта хранит замер 2026-08-21, свежий замер главнее (правило
CLAUDE.md), факт о Б-4 стоит; устаревание текста CB-59 — мелкий долг DIR-1. **Без возражений:**
статусы Б-2, ноль открытых critical/high, осторожная формулировка дубля CB-149/150, внешний scope
CB-102. Повторный проход Codex по исправленному порядку НЕ делался: все правки — принятие его же
возражений, а единственное расхождение снято замером; это сказано, а не заявлено закрытым.
**Что уходит владельцу решением (в следующий бриф):** два вопроса CB-65 (цена схемы леджера связей —
он сам отложил до калибровки, она села; opt-in провайдера эмбеддингов с предусловиями
безопасности) — возможно, то же, что В-4 «позже», тогда подтвердить словом; CB-91 (граница доверия
к чужому репозиторию); CB-102 — перенос в проект smart-approve или закрытие здесь.

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-25 (неизменяемые факты), ответ на батч выше:**
(1) **CB-102 — «Перенос»**: карта уходит в трекер проекта smart-approve и закрывается здесь со
ссылкой; исполнение — DIR-1 (тройка передана, низкий приоритет, короткая операция).
(2а) **Леджер связей (CB-65, цена схемы) — «мне больше видится как заводить, но попрошу расписать
все варианты — будем думать»**: решение НЕ принято, склонность — заводить; долг куратора —
самодостаточная записка вариантов с ценой каждого, на фактах (кто и где сейчас держит связи,
CB-163), с кросс-модельным проходом; до ответа владельца направлениям по (а) ничего не делать.
**Исполнено 2026-08-25: OPTIONS-relations-ledger-2026-08-25.md (версия 2).** Версия 1 разобрана
Codex (17 возражений, 7 блокирующих, принято 16) — её рекомендация не следовала из слов владельца:
таблица фактов `finding_relations` УЖЕ построена (autosorter 82 строки), а «заводить» распадается на
три отдельных решения — предложения машины (А), перенос легенды (Б: план миграции несёт 12
незакрытых дефектов, объём 164 ключа / 837 рёбер, не «десятки»), хозяин графа для grouping (В).
Рекомендация v2: узкий шов из CB-163 сейчас (уже в очереди DIR-2 как остаток CB-62), перенос —
как следующее решение с дизайн-юнитом по дефектам плана, леджер предложений — только после ответов
«кто производит» и «кто лично ратифицирует» (RFC P5).
**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-25 (после записки): «побудь чуть прагматиком — максимум выгоды
сейчас. Потом выкатываем новую версию и думаем, что ещё улучшить и имеет ли смысл».** Режим на
ближайший горизонт: направления добивают ВИДИМОЕ (блоки 1–3 порядка §2), затем РЕЛИЗ (версия,
CHANGELOG, переустановка пакета), и только потом — думать о путях 2–3 леджера связей. Пути 2 и 3
(перенос легенды, леджер предложений) ЗАПАРКОВАНЫ этим решением; путь 1 (узкий шов CB-62/CB-163) —
в работе. Два вопроса владельца той же репликой — «измеряем ли статистику вызовов?» и «есть ли
рекомендации по использованию (recommended workflow)?» — проверены фактодобытчиком 2026-08-25:
**(1) Учёта вызовов в продукте НЕТ** — ни счётчиков, ни лога, ни middleware (в `server.py` два
middleware, оба про корректность: строгие аргументы и нормализация описаний); `milestone_audit` —
доменный журнал действий над milestones, не телеметрия. Единственный источник — транскрипты
сессий на диске: грубый счёт по подстроке за 60 дней даёт РОВНЫЙ ПОЛ ~17.3–17.5 тыс. у почти всех
тулов (это перечисление отложенных тулов в каждом транскрипте, шум), а сигнал — превышение над
полом: `query` +11 тыс., `get` +10 тыс., `add` +6 тыс., `update` +5.6 тыс., `reqs_add` +1.3 тыс.,
`stats` +1.1 тыс., `categories` +0.8 тыс.; всё остальное — в пределах шума. Кандидат в задачу
(DIR-1, малый): счётчик вызовов в продукте — middleware рядом со строгими аргументами, таблица
`tool_calls` (tool, at, ok/err, ms) или jsonl в `.codebugs/`, верб `codebugs usage`; выгода —
следующая фаза «думать, что улучшить» идёт по данным, а не по ощущениям. **(2) Рекомендованный
сценарий использования ЕСТЬ для людей и ОТСУТСТВУЕТ для агентов**: README §Typical Workflows
(петля код-ревью, петля релиза); `docs/` — дизайн-доки разработчика; autosorter/CLAUDE.md встраивает
codebugs в свой 8-шаговый процесс; но `MCPServer` создаётся БЕЗ параметра `instructions`
(`server.py:383`) — MCP-клиент, то есть агент, главный пользователь, не получает при подключении
никакой подсказки о порядке действий, только докстринги отдельных тулов; `codebugs --help` — голый
перечень вербов. Кандидат в задачу (DIR-1, малый): `instructions=` с рекомендованной петлёй
(подать → дедуп/attention → якорь → закрыть; клеймы; требования) + `codebugs --help` с двумя
строками «с чего начать». **(3) Релиз**: `version = "0.1.0"` никогда не бампалась, тегов в репо
ноль, pipx держит 0.1.0, CHANGELOG (86 КБ) — весь под `[Unreleased]`, процедуры релиза нет нигде.
Кандидат в задачу (DIR-1, юнит релиза после верхнего блока): бамп до 0.2.0, срез `[Unreleased]` →
`[0.2.0] — дата` с абзацем highlights пользовательским языком сверху, тег `v0.2.0`, `pipx
reinstall`, перезапуск MCP-серверов, и процедура релиза в CLAUDE.md (три строки), чтобы это не
было одноразовым.
**В канон (К-16), оплачено пятью отказами Т-70 за один вечер 2026-08-25:** заметка на main
коммитится в ту же минуту, как дописана, поимённо; main с modified/untracked файлом — это
захваченный общий замок, `_guard_main_clean` (exit 11) отбивает финиш ЛЮБОГО юнита ЛЮБОГО
направления. Исполнитель уровня (4), не достучавшись до держателя, эскалировал куратору напрямую —
верно по бюджету попыток; ветка `docs/t70-cb150-correction` возвращена DIR-1 для посадки.
**Решение куратора по Э-24 (DIR-1), 2026-08-25:** запись владения `surfacegen.py`/`sweep_surface.py`
за DIR-1 (факт приёмки Т-62) ДЕЙСТВУЕТ как обязанность называть территорию при раздаче (бриф +
строка минта + уведомление держателя до диспетчеризации), не как вето; CB-165 и CB-178 (DIR-2,
вошли молча дважды за день) по существу не оспорены и идут дальше, территория дописывается задним
числом. Общий принцип: территория существует ради видимости столкновения ДО, а не ради права
отказать.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 8 DIR-1 — Т-64, Т-68, Т-69 приняты, Т-65 отклонён
и исправлен Т-70 — 2026-08-25.** Спот-чек куратора: `1fc7ff2` (Т-64, CB-152/155), `2d5e473` (Т-68,
CB-168/169), `c485dc6` (Т-69, CB-100), `8f69133` (Т-65, отклонён приёмщиком по п.1 → коррекция
Т-70 на `docs/t70-cb150-correction`, готова, сажается сторожем при чистом main), `2c0b8d1` — предки
main; CB-100, CB-152, CB-155, CB-168, CB-169 fixed; CB-149 not_a_bug как дубликат CB-150 (ребро
`duplicate_of`); CB-102 wont_fix — перенесена в проект smart-approve как его CB-19. Изолированная
приёмка окупилась 4/4, одна отклонила юнит. **Б-11 ПОДТВЕРЖДЕНА — CB-173 (high):** мутационная
проба уничтожает незакоммиченную работу, пять записанных инцидентов, два пути достижимы сегодня,
предписанного леджером fail-closed отказа нет нигде; К-5 этот класс не закрывает. По обещанию
куратора — поднята выше блока: первая в очереди DIR-1 после посадки Т-70, минимальной формой
(fail-closed отказ / автоснимок перед пробой). **Новая боль Б-12 — интеграция проигрывает гонку
заметкам направлений (CB-176, medium):** медиана интервала коммитов на main 107 с при гейт-прогоне
70–90 с — форма живлока, деградирует линейно с числом направлений; вторая дверь — exit 11 (грязный
main держится, пока чужой автор не закоммитит; Т-70 отбит >10 раз за вечер). К-16 (коммитить
немедленно) и exit 13 (main двинулся) тянут в разные стороны — форма решения не выбрана, DIR-1
готовит варианты с ценой, решение — куратор/владелец. Держатель против себя: заблокировала свой
юнит своим же handoff (Э-7 буквально). Новые карты: CB-171, CB-175, CB-177, CB-181, CB-182 (`where
2>/dev/null` восстанавливает CB-100), CB-183. Маршрутизация эскалаций: Э-23 CB-158 → DIR-1 (Т-66
сел, сериализация снята); Э-25 CB-161/162 → DIR-2. Отклонение держателя от порядка (взяла CB-100
вместо CB-143/117 из-за пересечения с деревьями DIR-2) — принято, SPEC §7. Леттер-фикс: `--repo`
в поручении куратора — на самом деле `--tracker-root`. Трекер: 43 открытых (1 high — CB-173),
133 закрытых; main == origin/main == `2c0b8d1`.
**Дополнение, тот же день: Т-70 ПРИЗЕМЛЁН `fb008d1` и ПРИНЯТ** (восемь пунктов; спот-чек куратора —
предок main, CB-150 fixed); смена 9 DIR-1 провела вторую независимую приёмку с тем же вердиктом
(`288308a`). Долгов и юнитов в полёте у смены 8 не осталось; приёмка за смену — 5/5 с находкой.
Новая **CB-186 (medium): коррекция записи в CASCADE-IDS не закреплена ничем** — единственный пин
зелен и на состоянии ДО коррекции (класс «зелено, потому что смотрит не туда», который этот же юнит
чинил своей третьей правкой). **Второй замер Б-12 сильнее первого:** посадка Т-70 стоила десяти
попыток — пять exit 11 (грязный main) и четыре exit 13 (main двинулся) ПОДРЯД, обе двери класса на
одном юните, они складываются; по оценке держателя автоперезапуск закрыл бы 4 отказа из 9, «окно
тишины» — все 9, предпочтение сместилось ко второму. Третья дверь (из отчёта менеджера Т-74): два
параллельных финиша одной ветки не исключают друг друга — первый снял дерево, второй упал посреди
тестов с ложным «pytest failed»; карту заводит DIR-1 при подготовке вариантов. Против себя
держатель: бриф Т-65 требовал проверить семь операций поодиночке и не требовал их композиции —
правило «проверка элементов не проверяет композицию» нарушено в брифе, чьим предметом была полнота
измерения.

**ВОПРОС ВЛАДЕЛЬЦА, 2026-08-25: «впечатление, что количество багов только растёт — сапожник без
сапог?» Замер куратора (публичный API findings.query_findings, 187 карт):** открытых 46 (1 high —
CB-173, процесс; 24 medium; 21 low); за день заведено 26, из них 8 уже закрыты в тот же день; ровно
половина открытых (23) заведена ДО 2026-08-20 — старый хвост (дизайн-вопросы Б-3/Б-6/Б-7, латентный
блок) почти не движется, вторая половина — последние пять дней. Из 26 новых за день ~20 — о
собственном инструментарии каскада (process 7, mcp_surface 6 — голдены и гейты, test/dx 4,
architecture 3), о продукте — 3 correctness + по одной identity/sql. Открытые по категориям:
correctness 8, process 7, design 5, mcp_surface 4. **Вывод: число растёт как СКОРОСТЬ НАХОДОК, не как
число дефектов у пользователя** — каждая приёмка возвращает находку (5/5, 8/8 за смены), популяции
разворачиваются до починки, адверсариальные ревью заводят карты; critical/high у пользователя — ноль.
«Сапожник без сапог» верно в узком смысле: харнес и процесс каскада порождают собственный поток
карт (минт-гейт, гонка финишей, мутационная проба) — единственная high-карта именно оттуда.
**Решения куратора под режим прагматика:** (1) находки о процессе, не теряющие работу и не
блокирующие посадку, идут в реестр болей строкой, а не картой и не юнитом; (2) дневной бриф ведёт
две метрики раздельно — «открытых, видимых пользователю» и «заведено/закрыто за день» — а не одно
число; (3) старый хвост из 23 карт — на следующий бриф пакетом на решение владельца: что закрыть
как wont_fix/отложено осознанно, чтобы число было честным, а не накопленным.
**Вопрос владельца, тот же день: «на нашей базе работает группировка — или это каждый раз истинно
новые, ни с чем не связанные баги?» Замер куратора четырьмя отчётами продукта на 44 открытых:**
похожесть текста (триграммы) — 0 семей даже при пороге 0.5 (карты рукописные и текстуально
уникальны; резолвер построен для повторных автоматических наблюдений, каких у нас нет);
родословная (`split_from`) — 1 связь; таблица связей — 1 ребро (CB-149→CB-150); теги — 165 разных
на 44 карты, и это теги ПРОИСХОЖДЕНИЯ (dir-1, needs-decision, codex-found, cross-model-reviewed),
не семьи дефектов; **цитирования — единственная ось, которая группирует:** 17 из 44 карт образуют
7 семей по 2–4 (харнес/гонка финишей CB-176+187; минт-гейт CB-181+186; SQL/meta-key
CB-161+167+172; лиз codemerge CB-42+110; milestones CB-33+38; blockers CB-34+109; идентичность
вокруг хаба CB-62 с CB-6/46/65/180), хабы — CB-62 (4 цитирующих) и CB-37 (3), и **225 ссылок на
ЗАКРЫТЫЕ карты** — в среднем пять на открытую. **Вывод: новые карты НЕ «истинно новые» — это
остатки и наследники известных семей** («carried forward from CB-30», «deferred out of CB-26 tree»,
«split out of CB-106»), 4–5 корневых семей покрывают большую часть открытого; но родство записано
ПРОЗОЙ в описаниях, а не в таблице связей (ровно зазор CB-163), поэтому механизм это видит только
через регэксп по тексту. **Решение куратора (дешёвое, первый потребитель таблицы связей):** при
заведении карты-наследника направление ставит ребро `relations_relate` (`follow_up_of` /
`found_during` / `split_from`) к родителю — тогда семьи видны запросом, а не разбором прозы; и
после релиза — прогон `batch-codebugs` (мета-карты) по старому хвосту, чтобы 23 старых + процессные
карты сложились в ~5 решаемых пакетов для владельца, а не в 46 строк.
(2б) **Провайдер эмбеддингов — «сделай настраиваемым; сейчас у нас вообще локальный сервис»**:
ратифицировано как задача; дефолт — локальный сервис, внешний — только явной настройкой, привязка
видна в диагностике; исполнение — DIR-2, место в очереди после CB-166 и CB-90, перед CB-62.
(3) **CB-91 — «не могу сказать, соглашусь с тобой»**: рекомендация куратора принята — `unknown`
остаётся честным ответом, механизм доверия к чужим репозиториям не строится, пока нет реального
потребителя; карта остаётся открытой в блоке 6 как ратифицированная отсрочка с этим триггером.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 9 DIR-2 — Т-67 и Т-66 приняты — 2026-08-25.**
Спот-чек куратора: `0a56733` (Т-67: три гейта научены падать; мутант «разрешено, но без номера
строки» теперь валит пять тестов на числе — закрывает дословную просьбу владельца из CB-95),
`ea54920` (Т-66: `query`/`stats` группируют по тегу и по ключу meta на четырёх поверхностях — треть
CB-62), `f13c2dc` (handoff s9) — предки main; CB-95, CB-164, CB-165, CB-166, CB-170 fixed; CB-62
открыта намеренно с заметкой о приземлившейся трети; деревьев и клеймов нет; main == origin/main,
2503 теста, ruff чист. Держатель запушила main (origin отставал на ~50 коммитов, CI-алярм без пуша
не запускается — п.6 контракта приёмки требует предъявить заменитель, не назвать) — правильно.
**Предпосылка ратификации 2б опровергнута направлением и перепроверена куратором:** в пакете нет
ни одного сетевого вызова (grep по сетевым импортам — только `urllib` в `db.py` для `file:`-адреса
SQLite); векторы приносит MCP-клиент, провайдер живёт в другом процессе. «Сделать провайдер
настраиваемым» как код — гейт, который не может выстрелить. Исполнимая часть переформулирована в
один юнит DIR-2: блок предусловий безопасности в описаниях тулов («текст никуда не уходит, вектор
считает вызывающая сторона»), правило в CLAUDE.md «будущий провайдер обязан быть настраиваемым с
первого дня», и CB-174 (размерность вектора не валидируется, одна чужеродная строка обрывает весь
поиск). Владельцу — сказано (см. ответ куратора 2026-08-25). **Пакет преемнику-куратору отставал от
git на 13 часов** (Т-56 сел 24.08 22:19, пакет — 25.08 11:41; п.1 и CB-156 были исполнены до его
написания) — второй случай подряд; **в канон (К-14): куратор сверяет пакет с `git log` и статусами
карт ПЕРЕД записью, не после**. **Кандидат в канон (К-15), оплачен инцидентом Т-66:** адверсариальная
проверка — только в отдельном дереве; мутант, оставленный в рабочем дереве, попал в коммит через
`git add -A`, пойман сьютом на следующем прогоне. Расхождение нормативных источников: определение
приёмщика и скилл приёмки говорят «7 пунктов», спека §13 — восемь (восьмой — путь модели; «семь»
исправлено в спеке 24.08); чинится куратором. Новые карты DIR-2: CB-167, CB-172 (S608 в ruff не
включён — 16 маркеров `noqa` подавляют то, что никогда не выставляется; передана DIR-1), CB-174,
CB-178, CB-179, CB-180. Замер по CB-90: популяция 61 (было 59), вся на трекере autosorter, риск для
идентичности — 3 строки. Числа к CB-163: пересечение двух механизмов пусто в обоих трекерах;
крупнейший дублирующийся ключ — `meta.parent`, 64 строки, типа связи для него в словаре нет;
унификация стоила бы миграции 73 строк, не девяти.

**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-25, вечер (группировка BATCH-VIEWS-2026-08-25.md; все три —
«да, рекомендованный вариант») — ИСПОЛНЕНО куратором:** (1) пакет milestones — CB-32, CB-33,
CB-34, CB-35, CB-38, CB-84, CB-104, CB-105 закрыты wont_fix «отложено до появления потребителя»,
каждая с заметкой-решением и триггером пересмотра (счётчик вызовов или инцидент); вето CB-32
исполнено по букве — закрытие явным решением, не «потому что спит»; вето CB-35 (любой юнит рядом с
маршрутизацией стримов называет её) переписано в заметку и остаётся в силе. (2) пакет минт-гейта —
CB-171, CB-181, CB-186 закрыты wont_fix «полировка гейта остановлена, зазоры документированы».
(3) форма Б-12 выбрана: «окно тишины» (плановые коммиты сериализуются с посадками через тот же
flock; К-16 сужается до «немедленно, через замок») + взаимное исключение двух финишей одной ветки
+ симметричный клейм многокарточных юнитов — один дизайн-юнит DIR-1 с кросс-модельным ревью до
кода, закрывает CB-176, CB-187, CB-112, CB-175; релиз юнитом не задерживается. Рёбра поставлены
(CB-187→CB-176 уже стояло от держателя — правило «ребро при заведении» сработало само;
CB-112↔CB-175 новое). **Открытых после исполнения: ~33 (было 46), из них ~12 в работе/очередях
направлений; решений не ждёт ничего, кроме релиза.**

**НАПРАВЛЯЮЩИЕ ВЛАДЕЛЬЦА ПО КАТЕГОРИЯМ, 2026-08-25, вечер (та же беседа, что подтвердила гейт
CB-60):** (1) «категорий не должно быть слишком много» — принцип; форма куратора: мягкий порог
(бриф показывает счётчик категорий и кандидатов на слияние по Левенштейну при превышении), НЕ
жёсткая квота-отказ. (2) «разрешить иерархические категории тема/подтема» — кандидат-дизайн
DIR-2 ПОСЛЕ релиза; прецедент уже в коде и несущий: авто-маршрутизация стрима безопасности
срабатывает на префикс `security:` в категории. Preflight дизайна обязан ответить: какой
разделитель (`:` по прецеденту или `/`), как нормализация (normalize_category) обращается с ним
сегодня, кто потребители свёртки по теме (stats/query group_by), и цена для идентичности —
категория входит в отпечаток, массовый перевод имён в иерархию = свёртка с перевыводом отпечатков
(машинерия CB-61, отдельный прогон и ратификация). Туда же малый довесок: обязательное обоснование
при минте новой категории (`new_category_reason` рядом со штампом `category_minted`). Сухой прогон
свёртки написаний на autosorter выполнен: 29 переименований, 5 пар, 0 коллизий, 0 непроверяемых —
`--apply` ждёт слова владельца.
**Исполнено 2026-08-25 вечером по слову владельца «применяй, если считаешь полезным»:** бэкап базы
autosorter снят (`.codebugs/backups/findings.db.pre-categorynorm-20260825-193813`, 26 МБ), свёртка
применена — 29 строк переименованы в 5 канонических имён, отпечатки не перевыводились (все 29 без
отпечатка), контрольный сухой прогон после: 0 к переименованию. Корпус autosorter более не несёт
вариантных написаний; гейт CB-60 держит новые подачи.

**РЕЛИЗ 0.2.0 ВЫПУЩЕН — 2026-08-25, смена 9 DIR-1. ПРИЁМКА (1)←(2), неизменяемый факт.** Спот-чек
куратора: тег `v0.2.0` (аннотированный, ПЕРВЫЙ тег в истории репозитория) → мерж `e431778`, предок
main, опубликован на origin; `pyproject.toml` и `src/codebugs/__init__.py` оба несут `0.2.0` (вторую
константу нашёл исполнитель — бриф её не предвидел); CHANGELOG срезан `[Unreleased]` → `[0.2.0] —
2026-08-25`, 73 записи до и после, пустая `[Unreleased]` сверху, highlights перечитан держателем;
main == origin/main == `04f2f83`, чист. **Осталось за владельцем, и цена промедления реальна:
`pipx reinstall codebugs` (сейчас pipx держит 0.1.0 со старой консольной обёрткой) и перезапуск
MCP-серверов (до него агенты не получают рекомендованную петлю `instructions`, верб `usage` и
исправленную разметку `Args:`).** За смену: семь мержей, приняты Т-70, Т-74 (CB-117 fixed), Т-75,
Т-79 (CB-173 fixed — Б-11 ЗАКРЫТА ДЕЛОМ), Т-73 отклонён и исправлен коррекцией; **НЕ приняты — долг
преемника: Т-77 (`505ba87`, счётчик вызовов) и Т-78 (`e431778`, сам релиз).** Приёмка вернула находку
6/6; новая CB-189 (текст `instructions` не загейчен на существование имён, которые советует).
**Три эскалации держателя, решения куратора:** (1) `codebugs --version` не существует — релиз без
способа спросить версию у инструмента; решение: малый юнит DIR-1 сразу (флаг читает `__version__`;
это видимое пользователю, в 0.2.1 или следующий срез); (2) менеджер Т-77 нарушил К-1 (прямой коммит
на main `43a98f1`, дописывая post-merge-контроль) — причина структурная: п.6 контракта требует
результат, который существует только ПОСЛЕ посадки, когда законного пути у уровня (3) нет; решение
(К-17): бриф заранее разрешает менеджеру ОДИН прямой коммит на main ТОЛЬКО в свой файл брифа/отчёта
`.claude/plans/L3-BRIEF-*.md` для записи post-merge-результата, поимённо; альтернатива Т-78
(вторая ветка) допустима, но дороже; (3) две процессные находки строкой, по новому порогу: гейт
`commit-msg` предотвратил реальный межсессионный занос чужого файла; различитель mtime в пробе Т-79
зависит от разрешения меток ФС (отказ безопасный).

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 10 DIR-2 — Т-71, Т-72, Т-76 приняты — 2026-08-25.**
Спот-чек куратора: `7750975` (Т-71: половина (а) CB-90 — хвост вызова инструмента срезается на
границе записи; итоговый предикат режет 79 из 80 загрязнённых строк autosorter; принят с долгом
CB-188 — срез задевает законный текст), `028f894` (Т-72: CB-178, шесть утверждений о сборке снимка
приведены к коду), `b585dc2` (Т-76: CB-174 fixed + задача владельца об эмбеддингах — предусловия
безопасности в описаниях тулов, гейт сетевых импортов, валидация вектора; принят с долгом CB-190 —
гейт ключуется на закрытое перечисление имён, а три текста обещают шире; CB-184 сужена: два пункта
из трёх закрыты) — предки main; `07658fa` handoff s10; деревьев и клеймов направления нет. Т-76
принят ДВАЖДЫ независимо с одним вердиктом (первый приёмщик не умер — сорвалась доставка; держатель
против себя: размер стенограммы не есть признак жизни агента). **Методическая находка в нормативный
промпт приёмщика (внесена куратором в `~/.claude/agents/acceptor.md`): no-op-мутант** — pytest
импортирует тест как `tests.<имя>`, а мутировался объект `<имя>`; мутация не легла, тест зелёный,
выглядит как «не ловит» → ЛОЖНЫЙ FAIL юниту; правило: прежде чем засчитать непойманного мутанта,
докажи тождеством объекта, что он лёг. **К-5 расширен держателем на любой артефакт направления:**
цитировать грепаемое содержимое, не номер строки (`reqs.py:928` протух до `:952` внутри одного
юнита). Ливлок посадки — Т-71 сел с восьмой попытки (третий замер Б-12; решение «окно тишины» уже
ратифицировано и у DIR-1). **Вопрос владельцу — половина (б) CB-90, два вопроса о ТРЁХ строках, не
чистка 61:** из 61 загрязнённой строки autosorter 58 записаны с явным id и отпечатка не имеют (срез
хвоста для них — косметика без последствий для идентичности); живой отпечаток из загрязнённого
текста несут ровно три открытые карты autosorter — CB-3221, CB-3213, CB-3195. Вопросы: (1)
перевывести ли их отпечатки после среза (ре-кей идентичности, машинерия CB-61-подобная; иначе они
навсегда не сольются с чистыми повторами); (2) резать ли хвост у 29 строк в ЗАКРЫТЫХ картах
(история vs чистота). Очередь DIR-2 смены 11: CB-62 (узкий шов), CB-161+162, затем категории.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 10 DIR-1 — долг Т-77/Т-78 закрыт, Т-81/82/83
приняты — 2026-08-25, вечер.** Спот-чек куратора: Т-77 (`505ba87`, счётчик вызовов) и Т-78
(`e431778`, релиз) — ПРИНЯТЫ С ОГОВОРКОЙ (обе приёмки: заниженная оценка названного остатка);
`d368b31` (Т-81: `codebugs --version` — проверено, отвечает `0.2.0`; плюс коррекция четырёх
неверных утверждений в highlights релиза — CB-191 fixed), `37f1b6a` (Т-82: текст `instructions`
загейчен на существование советуемых имён — CB-189 fixed), `3883bb2` (Т-83: `export-csv
/dev/stdout` больше не затирает CSV, найден и починен второй сайт — CB-143 fixed) — предки main;
main == origin/main == `07a543e`; деревьев направления нет. Приёмка 6/6 с находкой. **Релиз
развёрнут у владельца:** `pipx reinstall` → 0.2.0, MCP-сервер переподключён, сервер отдаёт
`instructions` с рекомендованной петлёй (куратор видел лично) — вопросы владельца «статистика
вызовов» и «recommended workflow» закрыты делом. **Два неприятных результата, оба ценные:** (1)
**ратифицированный дизайн «окно тишины» кода НЕ получил** — кросс-модельное ревью до первой строки,
оба атакующих из разных семейств независимо «строить нельзя»: точка взятия замка давала
гарантированный отказ, блокирующий хук держал бы main грязным весь прогон (самоустраняющийся отказ
→ незакрывающийся), `flock` нереентрантен (три детерминированные взаимоблокировки, включая
подвешивание собственного сьюта), файл записи клеймов отключал бы очистку worktree; пять несущих
находок держатель перепроверил по коду — все подтвердились. Редакция 2 на main
(DESIGN-DIR-1-silence-window-2026-08-25.md); второй круг ревью ОБЯЗАТЕЛЕН до кода; если и он
откажет — эскалация куратору с вариантами, не третья редакция в одиночку. Это правило «дизайн →
кросс-модельное ревью → код» оплатило себя в тот же день, когда владелец ратифицировал форму: без
него код с тремя взаимоблокировками сел бы в харнес всех сессий. (2) **принятый Т-83 внёс
регрессию** — CB-194: тот же дефект для `/dev/stderr`, где раньше было цело, плюс старый экземпляр
при слиянии потоков; форма выбрана, первый долг смены 11. Новая CB-192 (medium): счётчик вызовов
берёт записывающую блокировку на каждый вызов, включая читающие, — видимый вред параллельным
агентам под нагрузкой; в очередь 0.2.1 вместе с CB-194 и `--version`. Против себя держатель: нарушил
К-16 файлом дизайна, чей предмет — цена этого нарушения (посадка своего юнита со второй попытки,
CB-176 живьём). Три процессные строкой: CI-алярм не прогонялся у нескольких юнитов подряд
(правило «пуш после каждой приёмки» выведено); список исключений гейта Т-82 — незакрытый обход;
ложное объяснение верного решения в исходнике Т-81.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 11 DIR-2 — Т-80, Т-84, Т-87 приняты, Т-89 готов и
заблокирован — 2026-08-26.** Спот-чек куратора: `a9a84eb` (Т-80: объявленные разными карты больше
не сливаются в одну единицу работы в отчёте цитирований — это узкий шов CB-62), `be248a2` (Т-84:
предел выборки `0` значит ноль записей, отрицательный отказывает, неисполнимый аргумент отказывает
вместо молчания — CB-161, CB-162 fixed), `b713df2` (Т-87: команды связей сообщают об ошибке строкой,
не простынёй трассировки — CB-193 fixed) — предки main; сьют вырос 2626 → 2730; новые CB-196,
CB-197, CB-198, CB-200 (все low, каждая с ребром к родителю — правило «ребро при заведении»
работает). **Б-12 ЗАМКНУЛАСЬ СИММЕТРИЧНО и это меняет диагноз:** журнал DIR-2 отбил посадку ветки
DIR-1, через час журнал DIR-1 отбил посадку ветки DIR-2 — обе стороны правило соблюдали, обе писали
свои разделы. **Окно принадлежит не нарушителю, а МЕСТУ: пока плановые заметки правятся прямо в
рабочем дереве main, окно ненулевое у всех и всегда.** Держатель DIR-2 сменила практику у себя
(длинный раздел собирается в черновике, вносится одной правкой с немедленным коммитом); радикальные
формы — заметки на ветке; сторож не считает грязью чужие файлы в `.claude/plans/` — решение
уровня (1) и входят во второй круг ревью «окна тишины». **Форма, повторившаяся дважды за сутки, в
канон (К-18): правка, написанная КАК ЗАЩИТА ОТ ОШИБКИ, проверяется хуже обычной** — она приходит с
готовым оправданием («это же страховка», «это же исправление лжи»), и её собственная предпосылка не
перепроверяется; оба случая поймал нижний слой, ни одного — автор. **Эскалация по категориям
(задача владельца) — предпосылка верна НАПОЛОВИНУ:** потребитель иерархии в коде есть
(маршрутизация сравнивает категорию с префиксом `security:`), но этот поток за всю жизнь трекера
получил НОЛЬ элементов — предикат не срабатывал ни разу; «достроить работающую иерархию» и
«признать записанную, но никогда не исполнявшуюся» — разные проекты, и владельцу это надо сказать.
Замер, подтверждающий интуицию владельца «категорий не должно быть много»: **48 категорий на 193
строки, 22 из них держат ровно одну строку**; конвенция уже протекает — есть `reliability:cli` с
двоеточием и рядом голая `security`, под предикат не подходящая. **НОВОЕ ОГРАНИЧЕНИЕ СРЕДЫ: `git
push origin main` требует явного согласия владельца; у направлений его нет.** Следствие для п.6
контракта приёмки: CI-алярм запускается на пуше, поэтому до согласия п.6 закрывается предъявлением
локального прогона с честной пометкой «origin отстаёт на N коммитов, алярм этих коммитов не видел»,
а не формулой N/A. Нижний слой поправил держателя за три смены одиннадцать раз и одиннадцать раз по
делу — слот К-13 («оцени классификацию держателя, не принимай её») из промптов приёмки не
выкидывать.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 11 DIR-1 — Т-85, Т-86, Т-88 приняты; РЕЛИЗ 0.2.1
ВЫПУЩЕН (Т-90, приёмка — долг преемника) — 2026-08-26.** Спот-чек куратора: `4e685c2` (Т-85,
CB-194 — регрессия экспорта в stderr закрыта), `1426f98` (Т-86, CB-195 + CB-192), `e413cea`
(Т-88, CB-182), `9751337` (Т-90, срез 0.2.1: `version = 0.2.1`, CHANGELOG срезан, тег `v0.2.1` на
этом коммите, сьют 2739/2739), `f2a1a4a` (Т-89 — чужой юнит DIR-2, посажен держателем DIR-1 по
просьбе куратора: стена снята) — все предки main; main чист, деревьев направления нет. **CB-195
(high) — крупнейшая находка суток и она пришла из ЗАМЕРА, а не из карты:** CB-192 просила лишь
замерить занятость блокировки, замер показал, что записывающую блокировку берёт САМО ОТКРЫТИЕ
соединения — читающий вызов ждал чужую запись и за пределом таймаута падал; диагноз CB-192 замером
опровергнут, обе закрыты. Побочно трекер на файловой системе только для чтения стал читаем.
**ЭСКАЛАЦИЯ Э-28 И РЕШЕНИЕ КУРАТОРА: «окно тишины» отвергнуто ВТОРЫМ кругом ревью** (оба атакующих
разных семейств, 0.98 и 0.93; три находки держатель перепроверила по дереву): форма внутренне
противоречива (обёртка держит замок через свой `git commit`, а хук обязан отказать при занятом
замке — писатель отказывает сам себе), неблокирующий отказ убивает невиновного соседа (git стажирует
до хука, отказ не разстажирует, следующая внутризамковая проверка чистоты роняет чужую посадку),
и — главное — **ратифицированная форма уже была письменно отклонена в самом дереве** (шапка
`tools/cascade-mint.sh`, три довода), чего не заметили ни дизайн, ни ратификация. **Куратор
ратифицирует вариант (e) держателя, найденный замером:** не сериализовать никого, а перестать
считать отказом движение main, затронувшее ТОЛЬКО файлы, которых сьют не читает (~80% не-мержевых
коммитов main на окнах 7/30/90 суток — именно такие). Условия: предикат БЕЛЫМ СПИСКОМ
(`.claude/plans/*.md`), третий круг кросс-модельного ревью до кода, при отказе — эскалация, а не
третья редакция, и явная строка проверки предпосылки самого предиката (К-18). Честная граница
варианта: закрывает половину класса, вторая половина (движение main кодом) остаётся. **В канон
(К-19), оплачено дважды за смену: ЗАМЕР ПЕРЕД ВЫБОРОМ ФОРМЫ окупается крупнее самой формы** — он
опроверг диагноз CB-192 и он же родил вариант (e), когда две ратифицированные формы оказались
непригодны. Новые карты: CB-199 (high, open — сужение гарантии CB-86 для read-only трекера,
оставлено как документ размена; первая по приоритету у смены 12 после долга), CB-201, CB-202,
CB-203, CB-204. Держатель против себя — шесть ошибок, среди них: нарушила К-16 и заблокировала две
посадки; оракул был перечислением и пропустил обиходную форму вызова; приняла законное ожидание
менеджера за затык и забрала у него посадку — он был прав. **ВЛАДЕЛЬЦУ, ЖДЁТ ОТВЕТА: `git push
origin main` требует его согласия; origin отстаёт на 22 коммита, тег `v0.2.1` не опубликован,
CI-алярм этих коммитов не видел.** До ответа приёмка закрывает п.6 локальным прогоном с честной
пометкой об отставании origin.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 12 DIR-1 — Т-90 (долг), Т-91, Т-92, Т-94 приняты с
оговоркой — 2026-08-26.** Спот-чек: `9751337` (Т-90, срез 0.2.1 — тег подтверждён на мерж-коммите,
все три требования раздела «Releasing» исполнены), `5e4e0b3` (Т-91, CB-202), `0b842b6` (Т-94,
CB-204), `1b8205a` (Т-92, CB-203 + CB-201), `f4cc27d` (handoff) — предки main; main чист, деревьев
нет; ни одна приёмка не вернулась пустой. **Норматив: контракт приёмки — ДЕВЯТЬ пунктов, не восемь**
(девятый добавлен в спеку 2026-08-26); держатель вела приёмки по девяти, куратор выравнивает
`~/.claude/agents/acceptor.md` и скилл. **Решение куратора по CB-199 — вердикт держателя УТВЕРЖДЁН:**
сужаем осознанно, не чиним, серьёзность `high` → `medium`, карта остаётся носителем проектного
вопроса; довод, который его держит: любой зонд, восстанавливающий раннюю диагностику, воссоздаёт ту
самую конкуренцию за блокировку, ради снятия которой существует CB-195. **ЭСКАЛАЦИЯ Э-30 и решение
куратора: два атакующих разошлись — 0,90 «строить после правок» против 0,99 «НЕ строить», — но
расхождение НЕ ИНТЕРПРЕТИРУЕТСЯ, потому что судили РАЗНЫЕ ПРЕДМЕТЫ: второй судил редакцию 1, где
предиката из двух условий ещё не было.** Ратифицировано: прогнать второго по редакции 3 — это
устранение неинтерпретируемости, а не сведение вердиктов (сводить запрещено и остаётся запрещено).
Юнит Т-95 сминчен и в работу НЕ поставлен, ни строки кода — правильно. **Новая CB-218 (high):
обход каталогов вверх двузначен — при постороннем трекере выше `codebugs where` печатает чистую
привязку к ЧУЖОМУ трекеру с кодом 0, то есть справку «всё в порядке» вместо своих данных; это класс
CB-8/CB-13 в вербе, который писался ровно против него. Первая в очереди смены 13.** Ещё: CB-214
(текст отказа советует выход, рекурсивно удаляющий каталог, и молчит об этом), CB-215 (полный прогон
на main даёт ложные падения, если соседний юнит приземлится посередине — бьёт по самим приёмкам).
**УТОЧНЕНИЕ ПРАВИЛА ПОТОКА КАРТ (открытых снова 47, из них 1 high): находка приёмки о ПРОДУКТЕ —
карта; находка о процессе, харнесе, брифе, гейте или самой приёмке — СТРОКОЙ, даже если её нашла
приёмка.** Восемь из десяти карт смены — находки приёмок; так поток не сойдётся, правило доведено до
обоих направлений. Держатель против себя: передала неперепроверенное утверждение исполнителя как
факт (приёмка опровергла, оно успело попасть в CLAUDE.md); предписала в брифе флаг, которого в
репозитории нет; дважды за смену верным оказался прогон, а не её довод. Процессные строкой: у
изолированного приёмщика отключены инструменты правки, поэтому предписанный контрактом путь
мутационной пробы ему недоступен — определение агента живёт уровнем выше (куратор чинит); водитель
внешней модели подменил режим песочницы на полный доступ вместо «только чтение» (дерево цело).

**НОРМАТИВ ПРИЁМКИ ВЫРОВНЕН КУРАТОРОМ, 2026-08-26: девять пунктов.** Спека §13 (строки 421–471,
заголовок на 428) расширена 2026-08-26; девятый — «юнит способен слиться, доказано выводом, не
словом»: полный прогон архитектурных сторожей на СОБСТВЕННОЙ копии юнита ПОСЛЕ форвард-слияния main,
зелёный. Довод спеки: пп. 1–8 судят ЮНИТ, а замороженные потолки — свойство ХРАНИЛИЩА целиком,
меняющееся от сложения юнита с деревом; проверка элемента не судит состав (это правило проекта в
новом месте). Базовая линия в день введения: **4 из 9 веток с пометкой «готово» пробивали потолки**;
цена пункта — менее минуты против 5,5 минут потерянного финиша и слота очереди. Приведены к спеке
`~/.claude/agents/acceptor.md` (описание, «проверь девять пунктов», сам пункт 9, строка
`9 mergeable:` в шаблоне отчёта) и `~/.claude/skills/acceptance-check/SKILL.md` (описание,
`points[9]`); адресная строка «пункты 2, 6, 7 — механические» не тронута. **Следствие, доведённое до
обоих направлений: спека писала «пока определение приёмщика не обновлено, пункт проверяет менеджер
(3) сам» — определение ОБНОВЛЕНО, значит пункт 9 проверяет ПРИЁМЩИК**, менеджеру не дублировать.
Имя `_INSURANCE_PATHS` — автосортерное и в codebugs отсутствует; эквивалент здесь штатный: полный
прогон тестов и линта внутри `tools/worktree-finish.sh` после форвард-слияния плюс охранники
`tools/_guards.sh`, вывод предъявляется со своей копии, а не пересказывается.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 12 DIR-2 — Т-89, Т-93, Т-96 приняты, долгов нет —
2026-08-26.** Спот-чек: `f2a1a4a` (Т-89, коррекция CHANGELOG), `5c529df` (Т-93, CB-196 — отрицательный
предел выборки перестал молча означать «без предела» на трёх сайтах; ПЕРВАЯ приёмка направления по
девяти пунктам), `27f4a63` (Т-96, CB-197), `63f6bc1` (handoff) — предки main; CB-196, CB-197, CB-193
fixed; CB-62 сужена до одной развилки и изъята из слота исполнителя (остаток требует решения о
поставщике векторов). Урок Т-89, поднятый держателем: **требование, выраженное через МЕСТО, где оно
обычно записано, исчезает вместе с этим местом** — путь модели не был зафиксирован, потому что у
юнита не было файла-брифа, а требование жило в разделе брифа; поправлена форма шаблона, а не случай.
**РАТИФИКАЦИИ ВЛАДЕЛЬЦА, 2026-08-26 (батч куратора):** (1) **«Пушить можно, верни как было»** —
исполнено куратором немедленно: запушены 94 накопленных коммита и оба тега (`v0.2.0`, `v0.2.1`),
послемержевый алярм `main invariants` ЗЕЛЁНЫЙ на новом главе `a612608`; направлениям возвращён пуш
после каждой приёмки, п.6 контракта снова закрывается настоящим прогоном алярма. (2) **Категории:
«сначала слияние имён»** — и это ПОПРАВКА КУРАТОРУ, найденная держателем DIR-2 замером: я развернул
мысль владельца в иерархию, но **ни один вариант иерархии не уменьшает число ХРАНИМЫХ категорий**
(она даёт лишь свёртку в отчётах), а владелец просил именно уменьшения; уменьшает только настоящее
слияние имён (22 из 48 категорий держат ровно одну карточку). Иерархия (вариант B — тема объявляется
ОТДЕЛЬНО от категории, нулевой перевывод отпечатков) отложена до следующего решения, редакция 2
дизайна не выбрасывается. Задача на слияние имён выдана DIR-2 тройкой: расширение операции CB-61 до
произвольной карты `A → B`, сухой прогон по умолчанию, бэкап, перевывод отпечатков обязателен
(категория — первый вход отпечатка), коллизия двух живых строк — report-and-stop. **Дизайн иерархии
редакции 1 отвергнут двумя адверсариями с совпавшими блокирующими находками:** правка нормализатора
в предложенном порядке молча уводила бы критические находки безопасности в веху, которую никто не
читает; а «миграция без кода» откатывалась бы обратно через дверь импорта — расщепление идентичности
воспроизводилось бы внутри собственного лечения. **В канон (К-20), естественный эксперимент смены:
раздел «мои непроверенные предпосылки», АДРЕСОВАННЫЙ ИСПОЛНИТЕЛЮ и вменяющий действие, поймал обе
ложные предпосылки; раздел «чем это может оказаться неправ», адресованный ЧИТАТЕЛЮ и не вменяющий
ничего, не назвал ни одной из пяти.** Разница в адресате, не в добросовестности. Уточнение приёмщика:
опаснее всего предпосылка, спрятанная не в утверждении, а в ОЧЕРЧИВАНИИ СКОУПА — перечисляя
допущения, перечисляешь те, о которых знаешь. Замер темпа для Б-12: main двигается примерно раз в
4 минуты, гейт занимает 2,5 — Т-96 сел с четвёртой попытки, три отказа не по вине юнита. Процессное
строкой: инструменты правки у приёмщика отключены пятую смену подряд — оба приёмщика назвали
ограничение вслух, не обошли оболочкой и предъявили замену сильнее предписанной.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 13 DIR-1 (восстановленная после лимита) — четыре юнита
приняты, долгов приёмки не оставлено — 2026-08-27.** Спот-чек: main == origin == `f8c5d1e`; закрыты
CB-218 (high — обход каталогов молча привязывался к ЧУЖОМУ трекеру), CB-219, CB-214, CB-215 и
CB-224 (high — четвёртый двузначный вопрос «что лежит по пути»: человек в связанном рабочем дереве
получал ложный отказ, а `init` следом создавал трекер в месте, которое исчезнет вместе с деревом;
воспроизведено держателем лично четырьмя командами до постановки юнита). Открытых 46 против 47 —
поток впервые сошёлся. Обрыв по лимиту оставил три следа, все сняты: приземлённый непринятый юнит,
недоделанный юнит и незакоммиченный бриф.
**ЭСКАЛАЦИЯ Э-30 ЗАКРЫТА, РЕШЕНИЕ КУРАТОРА — ВАРИАНТ (d), ТОЛЬКО ИЗМЕРЯТЬ.** Второй проход внешней
модели по РЕДАКЦИИ 3 вернул «не строить» с уверенностью 0,99 — тот же вердикт, что по редакции 1,
то есть прошлое возражение («он судил снятый документ») снято, и после ВСЕХ ДЕВЯТИ предписанных
правок вердикт не сдвинулся ни на сотую. Держатель перепроверила три его утверждения лично,
подтвердились три; главное — **четвёртая дверь: условие (B) не доказывает тривиальности слияния,
потому что git выводит переименование КАТАЛОГА из совокупности переименований его файлов; на
настройках по умолчанию main заклинивает, а при `merge.directoryRenames=true` слияние проходит с
кодом 0 и МОЛЧА переносит плановую заметку.** Итог: класс Б-12 дёшево не закрывается ни одной из
пяти форм (a–e), три круга кросс-модельного ревью — три отказа. Принято: юнит Т-95 аннулировать,
вместо формы (e) — малый юнит варианта (d): `worktree-finish.sh` пишет число попыток на посадку и
причину каждого отказа, чтобы порог «пора чинить» стал числом; дизайн редакции 3 не удалять, а
пометить в шапке как отвергнутый двумя проходами с перечислением четырёх дверей. **Это дорогой, но
правильный исход: три круга ревью съели дизайн целиком и НЕ дали написать ни строки кода в харнес
всех сессий — а первая редакция несла три детерминированные взаимоблокировки.**
**Новая CB-227 (high), и она про нас самих:** гейт, поставленный ПРОТИВ перечисления, сам оказался
перечислением написаний вместо мест — приёмщик построил тринадцать обходных форм, гейт поймал одну,
а строка его таблицы исключений разрешает целую функцию, так что восстановление одного из трёх
только что устранённых дефектов оставляет его зелёным. Держатель записала против себя: она поставила
против этого абзац в брифе в тот самый день, когда записала в журнал, что проза таких вещей не
держит. Преемнику предписано начинать с вопроса «годится ли форма», а не с починки дыр; законный
исход — честно объявить, что свойство держится дисциплиной, и не строить гейт, который выглядит
защитой. Вторая запись против себя: внешняя модель опровергла собственный замер держателя (о том,
что `CASCADE-IDS.md` не читает ни один тест) — гребёнка ключевалась на написании, а не на
способности, то есть ошибка ровно того класса, о котором замер предупреждал. Из светлого: пункт 8
контракта (путь модели) впервые закрыт зачётом после трёх отказов подряд.
**ЗАДАЧА ВЛАДЕЛЬЦА 2026-08-27: документация и README** — показать пользу и уникальность инструмента;
выдана DIR-1 тройкой, главный критерий приёмки — каждое фактическое утверждение README проверено
ПРОГОНОМ и не шире кода (класс CB-191), четыре поверхности (README, `instructions`, `--help`,
CHANGELOG) не обещают разного; минимальная нарезка, без переписывания с нуля.

**ЮНИТ ДОКУМЕНТАЦИИ ПРИНЯТ С ОГОВОРКОЙ — 2026-08-27.** Приёмщик ПРОГНАЛ 44 фактических утверждения
README, а не прочитал их: по 24, попавшим в предъявленный исполнителем список, расхождений НОЛЬ
(вступительный пример воспроизводится построчно, блоки вывода совпадают дословно, счета инструментов
сходятся) — список честен там, где говорит; все три расхождения нашлись среди двадцати утверждений,
которых список НЕ покрывает (инвентаризация не дошла до раздела установки). **То есть у списка есть
граница, и она не там, где он её объявляет** — форма находки, которую стоит помнить. Открытые долги
переданы смене 14: юнит САМ внёс утверждение шире кода (README обещает режим `usage` у сервера,
сервер отвергает с кодом 2 — и внесено это ровно в предложении, добавленном ради честности); два
ложных утверждения, жившие в README ДО юнита (обещание, что `init` игнорирует объявленный корень —
неверно для флага, CB-48; «одна строка на поток ошибок», которых две); новый счёт в прозе в юните,
который счета убирал. **Предохранитель брифа окупился:** требование ПРОВЕРИТЬ предпосылку («бери
формулировки из `INSTRUCTIONS` как из источника истины») дало находку — сам образец обещает шире
кода; скопируй исполнитель его молча, переобещание уехало бы в README как факт.

**ЗАМЕР КУРАТОРА ДЛЯ ОТЧЁТА ВЛАДЕЛЬЦУ ЗА ДВЕ НЕДЕЛИ (13–27.08) И ДВЕ НАХОДКИ, КОТОРЫЕ ОН ПРИНЁС.**
Числа: 1524 коммита за 15 дней без пропусков, 365 мержей; **776 немержевых коммитов трогают ТОЛЬКО
`.claude/plans/` против 383, трогающих код и тесты — два к одному в пользу писанины о работе**, и это
прямая цена каскада, названная владельцу вслух. Тесты 718 → 2911 (×4,05; замерено прогоном сбора на
первом коммите периода во временном дереве, не по прозе). Первые два тега за историю проекта. Карт
228 (сейчас 232), заведено за период 213, открыто 49. Продуктовых среди закрытых 58–70%, процессных
20–22%. **Находка 1 (передана DIR-2 картой): простановка якорей 24.08 затёрла `updated_at` у 132 карт
из 228, включая 14 открытых** — все затронутые несут `meta.loc`, метка одна до секунды
(`2026-08-24T19:17:30Z`), второе по частоте значение встречается 2 раза. Следствие: у 58% трекера
нельзя узнать, когда карточка реально менялась; свой же замер пришлось считать по неиспорченной
трети (61 из 179). Класс: служебная операция сделала больше, чем обещала, и молча. **Находка 2
(передана DIR-2, приоритет выше первой; текст — DIR-1): рабочий якорь есть у 22 карт из 232 (9,5%)**,
194 помечены `no_grammar`, и при этом **54 из них несут в описании явный шаблон путь:строка**
(CB-230 — `src/codebugs/loc.py:1850`, CB-224 — `src/codebugs/db.py:924`), причём это карточки,
заведённые ПОСЛЕ простановки, то есть речь о пути записи. **А текст `INSTRUCTIONS`, который сервер
отдаёт каждому клиенту, обещает якорение БЕЗУСЛОВНО** — переобещание на самой видимой поверхности,
и оно моё по происхождению: формулировку задачи писал куратор. До роста покрытия формулировка
делается условной (DIR-1), причину узкой грамматики измеряет DIR-2 — не чинить, пока не измерено.
**Ошибка куратора, названная вслух:** я разбудил ПРЕДЫДУЩУЮ смену DIR-1 задачей о README, уже
запустив её преемницу, — сорок минут направлением распоряжались двое; ущерб замерен и нулевой, но
признак лежал в журнале открыто. **В реестр болей строкой (Б-13): ротация не даёт предшественнику
никакого механического сигнала о том, что он вытеснен** — сегодня это стоило дублирования, завтра
может стоить встречной записи.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 13 DIR-2 (восстановленная) — Т-100 и Т-102 приняты без
долгов — 2026-08-27.** Спот-чек: `b08275c` (Т-100, CB-222 + CB-223 — три документированные
поверхности говорят то, что делает код; попутно `apply` стал строго булевым: клиент, приславший
`1.0`, превращал сухой прогон в массовое перевыведение ключей), `510ad8f` (Т-102, CB-207 + CB-209 —
сухой прогон называет ключи карты, которые ни к чему не подошли, и идентичности, которые свёртка
сводит на один отпечаток). **ОПЕРАЦИЯ СЛИЯНИЯ ИМЁН КАТЕГОРИЙ ГОТОВА К ПЕРВОМУ ПРИМЕНЕНИЮ ВЛАДЕЛЬЦА
ЦЕЛИКОМ.** Сверх задания найден и закрыт **обход гейта минта**: цель свёртки не проверялась на
существование, `--fold-map '{"correctness": "новое_имя_с_опечаткой"}' --apply` возвращал код 0 и
оставлял трекер с категорией-опечаткой — операция, чья единственная цель уменьшать число имён, умела
его увеличивать по одной опечатке в рукописной карте, молча. Теперь требует `--new-category`.
**ЗАМЕР ПО CB-230 ХУЖЕ ПЕРЕДАННОГО, ПОДТВЕРЖДЁН КУРАТОРОМ ЛИЧНО: в autosorter затёрто 3285 карт из
3445 (95,4%)**, схлопнутых в семь меток внутри шести секунд (19:17:30–36); в codebugs — 132 из 232.
**И поле участвует В ЛОГИКЕ, а не только в отчётности:** `findings.py:919` —
`ORDER BY updated_at DESC, rowid DESC LIMIT 1`, выбор самой свежей строки, решающий повторное
открытие против рецидива. Уцелевшего источника нет. Простановка дошла до колонки через ОБЩИЙ
апдейтер, не назвав её, — мимо уже дважды записанного решения репозитория.
**ГИПОТЕЗА КУРАТОРА ПО CB-233 ОПРОВЕРГНУТА ЗАМЕРОМ ДЕРЖАТЕЛЯ, и это правильный исход:** я
предположил, что грамматика узка; держатель подала ей каждый из найденных в описаниях токенов
`путь:строка` — **принято 55 из 55, не отвергнут ни один**. Грамматика не узка — **ей никогда не
показывают описание** (запинено отдельным тестом), а у команды заведения нет ни одного параметра для
места в коде. Честный потолок механической дозаливки — треть корпуса, и гнаться за сотней процентов
неверно.
**В КАНОН (К-21), и это самый дорогой урок недели: по всякой задаче вида «нам нужна возможность X»
ПЕРВЫМ ДЕЙСТВИЕМ подай инструменту вход, который он якобы не принимает.** Предыдущая смена написала
дизайн иерархии категорий в ДВУХ редакциях и вынесла владельцу развилку о том, как построить
механизм, который уже был построен и лежал в том же файле; никто, включая держателя и куратора, не
попробовал его вызвать. Это прямой ответ на вопрос владельца «что было ненужной работой».
**Ливлок посадки стал измеримым и структурным: заметки прилетают на main примерно раз в 85 секунд,
обязательный прогон гейта занимает около 145** — окно короче работы, посадка есть лотерея (это
третий независимый замер Б-12 и он подтверждает решение «только измерять»). Кандидат в канон от
держателя: **кэш байткода — третий механизм ложного зелёного мутанта** (мутант той же длины,
восстановленный в ту же секунду, молча не исполняется). Процессное строкой: канал «менеджер →
держатель» не работает (адрес не разрешается), обратный работает; у приёмщика пятую смену нет
инструмента правки, и он пятый раз предъявил замену сильнее предписанной. Против себя держатель:
три поправки нижнего слоя за смену, все одной формы — утверждение шире собственного замера.

**ВОССТАНОВЛЕНИЕ ЗАТЁРТЫХ МЕТОК ВРЕМЕНИ — ИСПОЛНЕНО 2026-08-27, владелец нашёл архивную копию.**
Архив `compiater-2026-08-23T03:00:01` (emborg/borg); 24 августа бэкапа НЕТ, поэтому зазор до затирания
— 42 часа. Извлечение — `emborg extract` в сторону (`/tmp/borg-restore`), НИКОГДА не `restore`:
восстановление файла на место стёрло бы три дня работы. Скрипт восстановления берёт из архива ровно
одну колонку и пишет её только карточкам, чья метка попала в шестисекундное окно затирания
(`UPDATE … WHERE id = ? AND updated_at BETWEEN …`) — то есть карточка, изменившаяся после 24-го,
не подпадает под условие по построению, а заведённая позже отсутствует в архиве. **codebugs:
восстановлено 127 из 131** (бэкап `backups/findings.db.pre-updatedat-restore-20260827-155840`,
целостность ok, 233 карточки; самая частая метка теперь встречается 4 раза вместо 132).
**autosorter: 3252 из 3285**, исполнено владельцем — куратора остановил его собственный сторож базы
(сырой доступ к каталогу autosorter запрещён, обход требует авторизации владельца; куратор не
обходил). Остаток в обоих трекерах — карточки, заведённые после снятия архива; их прежней метки не
существует нигде. Погрешность названа числом до записи: в codebugs 21 карточка из 131 менялась в
42-часовом зазоре (сверено по упоминаниям в коммитах), у них метка занижена; **110 восстановлены
точно**. **ПРИЧИНА ДЕФЕКТА ЗАКРЫВАЕТСЯ ОТДЕЛЬНО** юнитом Т-108 у DIR-2 (`authored: bool = True` на
`update_finding`, служебная запись якоря объявляет себя таковой, гейт паритета учится классу «не
пишет»; 2996 тестов) — восстановление вернуло данные, но не причину, и путать их нельзя.
**Ответ на вопрос владельца «как не потерять новое»: не восстанавливать базу, а достать из архива
одно поле — текущая база остаётся источником всего остального.** Это та же дисциплина
«проверил и записал одним действием», которую репозиторий внедряет в собственном коде.

**ПРИЁМКА (1)←(2), смена 14 DIR-1 — 2026-08-27.** Т-105 принят с оговоркой (`dac248e`, CB-231),
Т-106 принят с оговоркой (`5f6799b`, CB-226 + CB-225 — приёмщик построил СЕМЬ состояний атаки на
сторожевой сигнал и вернул шесть находок, которых не было ни у исполнителя, ни у держателя; главная:
исполнитель написал в докстринге, что случай символической ссылки на каталог проверен и безопасен, а
`os.walk` считает такую ссылку каталогом и не спускается — подмена всего содержимого пакета прошла бы
незамеченной; достижимость сегодня ноль, но **неверное утверждение опаснее самого дефекта**, и
обратный случай тоже: объявленный «непокрытым» предел длины пути оказался ЗАКРЫТ — приёмщик построил
путь 8283 байта и получил корректную запись; докстринг врал в обе стороны). CB-227 (high) закрыта.
Долг смены — вердикт Т-107 (CB-232, `INSTRUCTIONS` обещает якорение безусловно) — передан смене 15.
**Предупреждение из handoff, которое надо помнить при проверке: запущенные MCP-серверы отдают СТАРЫЙ
текст `INSTRUCTIONS` до перезапуска** (константа читается при старте) — это не непочиненная
поверхность. Держатель против себя, четыре поправки за смену: небрежный замер множеств режимов уехал
в бриф словом «посчитано»; собственная незакоммиченная правка на main стоила ЧУЖОМУ юниту 350 секунд
через exit 11 (К-16 нарушен тем, кто его цитирует); улика по CB-231 из живого журнала ничего не
доказывала. Наблюдение о счетах в прозе: **ловушка, названная в брифе ЯВНО и с двумя предыдущими
примерами, не сработала в третий раз** — общий запрет не работал, конкретный пример сработал.

**РАТИФИКАЦИЯ ВЛАДЕЛЬЦА, 2026-08-27: «CSV-экспорт, по-моему, вообще никогда не использовался — это
низкий приоритет». ЗАМЕР КУРАТОРА ПОДТВЕРДИЛ И ОКАЗАЛСЯ ЖЁСТЧЕ ИНТУИЦИИ.** Прогон по 6000
транскриптов за 90 дней с РАЗДЕЛЕНИЕМ упоминаний и настоящих вызовов (вызов = `export-csv` внутри
команды Bash, а не в тексте): **34 настоящих вызова против 1429 упоминаний**. Разбивка упоминаний:
1028 прочий текст, 271 обсуждение карт CB-143/CB-194, 130 вывод справки. **И все шесть проверенных
примеров «настоящих» вызовов — из наших же временных каталогов и рабочих копий юнитов
(`scratchpad/repro`, `.worktrees/fix-cb-230…`, `.worktrees/fix-cb-233…`), то есть это наше
собственное воспроизведение дефекта, а не пользование вербом.** Честный вывод: пользовательских
вызовов CSV-экспорта в замеренном окне НЕТ ВОВСЕ.
**Цена, которую это назначает задним числом:** на CB-143 потрачен юнит, он внёс регрессию CB-194,
на неё потрачен второй юнит — два юнита и одна регрессия на верб, которым никто не пользуется. Это
конкретный ответ на вопрос владельца «что было ненужной работой», и он точнее всех моих прежних
оценок, потому что он замер, а не суждение. Ошибка была моя: я поднял CB-143 в верхний блок как
«видимый вред», не спросив, видим ли он КОМУ-НИБУДЬ.
**Следствие в правило (К-22): прежде чем поднять карту в верхний блок по признаку «видимый вред»,
спроси и замерь, СКОЛЬКО РАЗ эту поверхность вызывали.** Видимость вреда есть произведение тяжести
на употребление; у неупотребляемой поверхности второй множитель ноль. Порядок §2 правится: всё, что
касается CSV (экспорт, импорт, `codebench_import`), уходит в блок 6 «спит по решениям владельца» —
чинить только при касании и только boy-scout'ом.
**И ГРАНИЦА СЧЁТЧИКА ВЫЗОВОВ, НАЗВАННАЯ ЧЕСТНО: он не мог ответить на этот вопрос.** `codebugs
usage` считает ТОЛЬКО вызовы через MCP-сервер и говорит это первой строкой своего вывода; `export-csv`
— верб командной строки и MCP-тулом не является вовсе (среди 83 тулов есть только `codebench_import`
и `reqs_import`). То есть инструмент, построенный ради ответа «чем пользуются», слеп к целому классу
поверхностей — командной строке. Это не дефект счётчика (его контракт объявлен верно), а
недостроенная половина: замер по транскриптам пришлось делать вручную. Кандидат в очередь после
релиза, приоритет средний, форма — учёт вызовов на уровне `cli.run`, а не только в MCP-обёртке.
Живой снимок счётчика на 2026-08-27 для сравнения: `query` 64, `get` 57, `update` 52, `add` 44
(3 отказа), `relations_relate` 31, `relations_query` 9, `categories` 6, `claims_list` 6, остальные
единицы. Четыре верба над находками — по-прежнему весь продукт; `relations_relate` на пятом месте
подтверждает, что правило «ребро при заведении» исполняется.

**Б-13 ПОЙМАНА ЗАМЕРОМ СНИЗУ — 2026-08-27, и это лучший исход дня.** Куратор ДВАЖДЫ за сутки
оставил двух держателей одного направления (первый раз — разбудил предшественницу задачей о README,
уже запустив преемницу; второй — перезапустил смену 15 трижды по ложным уведомлениям о падении,
тогда как сессия была жива и работала). **Правило, записанное утром, самого куратора не удержало —
удержал замер снизу.** Агент третьего захода не стал рассуждать, а сверил ВРЕМЯ КОММИТОВ: пока он
читал входные документы, на main легли три коммита DIR-1 (18:38:00, 18:39:12, 18:41:51 — последний
в момент сверки). Вывод: заход жив. **Он не написал в общий артефакт ни строки** и вернул решение
куратору, назвав риск точно: не «неаккуратно», а «двойной срез релиза — испорченный релиз у
владельца, а не документационная неточность». Плюс он перепроверил всю выданную ему очередь и
показал, что четыре пункта из пяти живая сессия уже закрыла (CB-235 заведена с ребром к CB-232,
починена, приземлена `8adf7fa`, закрыта; обе процессные строки записаны, причём формулировка сессии
СИЛЬНЕЕ заготовки куратора — называет проверяемое действие, а не пожелание). **В канон (К-23):
признак «предыдущая сессия мертва» проверяется временем последних коммитов её направления, а не
уведомлением харнеса** — уведомление о падении может лгать, коммит с меткой времени не лжёт.
**Две поправки агента куратору, обе приняты:** (1) стартовый снимок куратора устарел на пять
коммитов, и К-14 (сверка очереди с git-историей) спас смену от дублирующей работы ровно так, как
задуман; (2) **CB-110 закрыта не «попутной правкой чужого юнита», как я записал, а собственной
веткой DIR-1 `docs/cb-235-cb-110-doc-corrections`, и сознательно как `wont_fix`, а не `fixed`** —
карта требовала построить недостающий терминальный статус, решение постройку отклонило, и статус
хранит эту разницу; для направления, чей предмет — расхождение записи с действительностью,
происхождение правки важно. **И отдельно ценное: агент НЕ ПРИВЁЛ свой замер по К-22**, потому что
его шаблон не отличал вызов от упоминания и контрольный запрос вернул ноль — то есть он применил к
себе ровно тот урок, что записан в чужом handoff'е («небрежный однострочник уехал в бриф словом
„посчитано"»), и промолчал вместо того, чтобы подать сырое число. Срез 0.2.2 не начат правильно:
условие («после посадки CB-233») не выполнено, версии `0.2.1`, тега нет. Сверх того агент нашёл в
моём списке highlights ошибку класса CB-191: «снята блокировка на чтении» лежит в УЖЕ выпущенном
0.2.1 (CHANGELOG:614, 633) — в 0.2.2 её писать нельзя; живая сессия ту же поправку уже внесла в свой
порядок среза.

**ВОПРОСЫ ВЛАДЕЛЬЦА 2026-08-27 (вечер) И ЗАМЕР НА КАЖДЫЙ. Третий ответ — дефект процесса, найденный
владельцем, а не нами.**
**(1) «46 очень много» — из 47 открытых: 36 о продукте, 11 о нашем инструментарии; 1 high (CB-233,
в работе), 24 medium, 22 low.** Категории: correctness 11, mcp_surface 5, findings_identity 4,
process 4. **Природа хвоста, названная честно: это в основном ОСТАТКИ ПОПУЛЯЦИЙ, найденные при
починке чего-то другого** — CB-208 («популяция оказалась больше: ещё восемь сайтов после CB-196»),
CB-179 («остаток популяции голых аллоулистов после CB-165»), CB-217 («четвёртый случай семьи
CB-28»), CB-200 («сплошная гребёнка не сделана»), CB-216 («остаток герметичности в 39% набора»).
То есть очередь порождается нашей же тщательностью: каждый юнит меряет популяцию и честно
записывает то, что не влезло в его нарезку. Это лучше, чем не знать, но 47 — не «долг», а
«измеренная и отложенная работа», и в брифе владельцу так и надо говорить.
**(2) «Когда делали полноценное ревью» — НИКОГДА над КОДОМ ЦЕЛИКОМ.** Замер по реестру: Codex
упомянут 12 раз, кросс-модельных проходов ~10, `adversarial-review-x2` дважды — **и все они над
ДИЗАЙНАМИ** (окно тишины трижды, иерархия категорий, RFC идентичности), плюс приёмки над отдельными
юнитами. Сплошного ревью кодовой базы не было ни разу. Приёмка покрывает юнит, а не композицию —
это ровно то различие, которое репозиторий проповедует и на себе не применил.
**(3) «Запускаешь ли /simplify при коммитах» — НЕТ, И ЭТО РЕГРЕССИЯ ПРОЦЕССА, ОПЛАЧЕННАЯ ДВАЖДЫ.**
`tools/worktree-finish.sh` гейтит одиннадцать сторожей плюс ruff и полный набор тестов — про
simplify/ревью там НИ ОДНОГО упоминания. При этом `BUGFIX-LOOP-LEDGER.md` хранит два факта против
нас: запись 2026-08-13 — «**I skipped `/simplify` before landing CB-17. Phase 8 mandates it**», и
пост-фактум прогон нашёл три избыточных теста, дублированную фикстуру и два ложных утверждения в
документации (завело CB-21); запись 2026-08-17 — «**Round 3 (the simplify altitude pass)** нашёл,
что ветка СЛОМАЛА `export-csv /dev/stdout | cat`». То есть проход ловил живые регрессии, был
обязателен фазой 8 цикла, и тихо выпал из протокола юнита, когда работа переехала в каскад
направлений. **Куратор этого не заметил за все смены — вопрос владельца обнаружил дыру, которую не
обнаружил ни один из девяти механизмов приёмки.** Форма исправления (вменено обоим направлениям
2026-08-27): `/simplify` (или `simplify-traced`) прогоняется на ветке ПЕРЕД `worktree-finish.sh`,
его результат — обязательная строка в отчёте юнита, а приёмщик спрашивает её как часть п.1;
гейтом в скрипте НЕ делаем — это суждение, а не проверка, и ложный отказ там дороже пропуска.
**ПОПРАВКА ВЛАДЕЛЬЦА, ТОГО ЖЕ ВЕЧЕРА: «в autosorter однозначно проверяется наличие /simplify в
сессии» — ОН ПРАВ, Я ОШИБСЯ ДВАЖДЫ.** Первая ошибка: искал только в `tools/worktree-finish.sh` и
доложил «в autosorter этого тоже нет» — а гейт стоит СНАРУЖИ скрипта. Вторая, хуже: на основании
неполного замера я решил «гейтом не ставим, это суждение», тогда как механизм давно придуман,
отлажен и лежит на этой же машине. **Как устроено в autosorter:** общий скрипт
`~/.claude/hooks/simplify-traced-gate.sh` подключён в `.claude/settings.local.json` как
`PreToolUse`-хук на `Bash`; он разбирает командную строку (shlex, обёртки, разделители), опознаёт
вызов `worktree-finish` и ОТМЕНЯЕТ его, если `/simplify-traced` не запускался в сессии.
`autosorter/CLAUDE.md`, шаг 7, дословно: «A PreToolUse hook gates `worktree-finish.sh` on this: if
`/simplify` has not been run in the session, the finish is cancelled». **Почему у нас не работало:
при переносе харнеса (2026-08-16) взяты скрипты и сторожа, но НЕ взят внешний гейт** — в обоих
файлах настроек codebugs был только раздел `permissions`, раздела `hooks` не было вовсе. То есть
проход был необязателен не по решению, а по недосмотру переноса, и полтора десятка смен прошли без
него. **Исправлено 2026-08-27 владельцем** (файл настроек permission-чувствительный, куратор его не
трогает): хук подключён тем же скриптом, резервная копия
`.claude/settings.json.bak-20260827-185712`, вступает в силу для НОВЫХ сессий. **Урок в канон
(К-24): когда переносишь механизм из другого проекта, переноси и то, что стоит СНАРУЖИ него.** Мой
замер «в worktree-finish.sh ни одного упоминания» был верен буквально и ложен по существу — я
проверил файл, а надо было проверить МЕХАНИЗМ; это ровно та ошибка «проверка элемента не судит
композицию», которую репозиторий ловит у других. И общий вывод: **два вопроса владельца подряд
вскрыли то, чего не нашли ни девять пунктов приёмки, ни полтора десятка смен** — сначала «CSV
никогда не использовался» (приоритизация по ощущению вместо употребления), теперь «/simplify должен
быть в харнесе» (утерянный при переносе гейт).

**РЕЛИЗ 0.2.2 ВЫПУЩЕН И РАЗВЁРНУТ У ВЛАДЕЛЬЦА — 2026-08-27.** Спот-чек куратора: тег `v0.2.2` на
мерже `48d4e8b`, опубликован; `pyproject.toml` и `__init__.py` оба `0.2.2`; CHANGELOG срезан
`[0.2.2] — 2026-08-27`, пустой `[Unreleased]` сверху; main == origin. Владелец выполнил
`pipx reinstall` (проверено: `codebugs 0.2.2`) и переподключил MCP-сервер; куратор проверил, что
сервер отдаёт ИСПРАВЛЕННЫЙ текст `INSTRUCTIONS` — обещание якорения стало условным («когда
наблюдение НАЗЫВАЕТ место в коде... отчёт, называющий только файл, якорить нечего»). Новая
способность видна в справке: `-l/--lines`. **Порядок «сначала релиз, потом ретро-прогон» —
указание владельца, исполнено.**

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 15 DIR-1 — Т-107 принят, CB-232/CB-235/CB-110 закрыты,
срез 0.2.2 сделан — 2026-08-27.** Спот-чек: `c594569` (Т-107), `8adf7fa` (CB-235 + CB-110),
`48d4e8b` (срез) — предки main. **Гейт `/simplify-traced` исполнен ПЕРВЫМ ЖЕ, кто под него попал:**
держатель остановила собственный финиш ДО фазы замка (ничего не приземлилось), прогнала проход,
запустила заново; довод «в моём диффе нет кода, проверять нечего» был верен и всё равно отвергнут —
**правило, нарушенное первым же, кто под него попал, перестаёт быть правилом**. Фаза 3 прохода
пропущена по её собственному условию, перемеренному держателем: 80 492 байта против потолка 40 000
(при постановке было 68 786 — затор не расчищен, стал хуже). **ПЯТЫЙ случай «правки абзацев по
отдельности» — в handoff'е самой держателя, четыре дефекта разом** (шапка обещала недописанные
разделы; счёт «каноны К-1…К-22» устарел внутри документа, ссылающегося на К-24 тремя абзацами ниже;
§4 остался заданием под заголовком «выполнено», так что преемник поднял бы версию вторично).
**Уточнение механизма, ценнее находки: документ, каждая часть которого верна В МОМЕНТ НАПИСАНИЯ,
копит ложь на стыках со своим ПРОШЛЫМ СОСТОЯНИЕМ** — видно только чтением целиком; требование в
брифах сужено до проверяемого действия (назвать абзацы, между которыми правка провела границу, и
прочесть каждую пару подряд). Три непроверяемых числа убраны из CHANGELOG (корпуса из 31 формы нет
в дереве, приёмщик независимо построил свой из 46 и получил другие числа — число за непереносимым
определением вреднее его отсутствия).

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 14 DIR-2 — Т-108 и Т-109 приняты, CB-230 и CB-233
закрыты — 2026-08-27.** Спот-чек: `d42aed5` (Т-108: флаг `authored` управляет ровно одной колонкой,
служебная запись не двигает `updated_at`, смена статуса под снятым флагом ОТКАЗЫВАЕТСЯ выше
транзакции — бывшее дисциплиной сделано непредставимым), `de20578` (Т-109: именованный вход места
на поверхности MCP, одно правило с CLI) — предки main. **Второй вопрос CB-230 закрыт замером:
второй ключ сортировки НЕ нужен** — порядок решает лишь при двух и более терминальных строках на
один отпечаток; в codebugs таких ноль, в autosorter одна и обе карточки одного класса; триггер
пересмотра назван состоянием, рядом названа CB-209 как механизм, способный его создать.
**Обстановка обогнала юнит, пока он летел:** восстановление меток из архива сделало ложными ПЯТЬ
текстов Т-108, писавших о живом корпусе в настоящем времени; дороже всех абзац CHANGELOG —
единственный пользовательский — он советовал не доверять как раз верным данным. Все пять исправлены
до посадки, и добавлено то, чего не было ни в одной редакции: **починка стала возможна лишь потому,
что архив случайно нашёлся, — это везение, а не свойство системы.** Т-109 судили ДВА приёмщика,
дефекта в диффе не нашёл ни один; всё ценное принёс тот, кто вышел ЗА ПЕРИМЕТР заданных вопросов
(нашёл отчёт исполнителя, который держатель объявила несуществующим; измерил тождество шире, чем
пинует юнит; сам измерил обе непокрытые ветки дедупликации). Новые наследники: **CB-234**
(`resolve_trailers` не идемпотентен между прогонами), **CB-236** (именованный вход зовётся во
множественном числе, а грамматика предпочитает синглярные ключи — `meta.line` молча выигрывает;
приёмщик добавил довод за срочность: цена бездействия выросла именно этой посадкой). Против себя
держатель: дважды за смену объявила отсутствующим то, чего не искала. Посадка Т-109 заняла ШЕСТЬ
прогонов гейта, пять `exit 13` подряд — четвёртый замер Б-12, подтверждающий решение «только
измерять»; сторож устаревшей базы снят ЗАМЕРОМ (форвард-мерж руками, прямая проверка, что слитое
дерево отличается от main ровно в четырёх файлах карты), а не флагом `--allow-stale-base`.

**РЕТРОСПЕКТИВНЫЙ ПРОГОН `/simplify-traced` ПО ПРИЗЕМЛЁННЫМ МЕРЖАМ — ЗАДАЧА ВЛАДЕЛЬЦА ИСПОЛНЕНА,
2026-08-27 (спецификация: RETRO-TASK-simplify-sweep-2026-08-27.md).** Пачка Б (DIR-1, диагностика
и харнес): **из одиннадцати мержей шесть чистых, восемь находок; преобладает класс «ФАКТИЧЕСКОЕ
УТВЕРЖДЕНИЕ», а не гигиена** — четыре карты-наследника CB-237…CB-240, каждая воспроизведена
держателем лично. Главная: журнал попыток посадки пишет остановленный сигналом финиш кодом НОЛЬ, и
это стало живым ровно с подключением вставки `/simplify-traced` — то есть вчерашнее исправление
породило дефект, который прогон тут же и нашёл. **Прямой ответ на вопрос владельца «что мы теряли,
пока проход не работал»: не грязный код, а ЛОЖНЫЕ УТВЕРЖДЕНИЯ** — класс, который приёмка по девяти
пунктам пропускала по построению, потому что судит юнит против его же брифа, а не против тела
документов. Пачка А (DIR-2, идентичность): сходимость трёх независимых проходов по одному мержу
(`b08275c` чист, оговорка снята), конфликт двух аналитиков разрешён `git log -S` — прозаическое
утверждение было ИСТИННЫМ, когда его писали, и стало ложным, когда следующий юнит построил механизм
и оставил текст стоять (CB-242 атрибутирована верно).
**В КАНОН (К-25), и это ровно моя ошибка сегодняшнего дня, названная снизу точнее, чем мной:
файл вывода фонового агента НЕ РАСТЁТ, пока агент не завершился, поэтому работающий и мёртвый агент
выглядят ОДИНАКОВО.** Держатель DIR-2 «похоронила» двух аналитиков на 12-й и 18-й минуте молчания —
они вернулись на 57-й и 73-й; счёт погибших правился дважды (три → один → ноль), и ни одна правка не
пришла из новых улик, только из возвращения «похороненных». Куратор в тот же день трижды
перезапускал живые смены по тому же признаку. **Механизм ошибки назван точно: унаследованный факт о
среде есть ГИПОТЕЗА О СЕГОДНЯ, а не описание** — handoff предыдущей смены сообщал о пяти настоящих
потерях сессий, и следующая смена читала молчание сквозь это ожидание. Проверять жизнь агента —
временем коммитов его направления (К-23), не файлом вывода и не тишиной.
**В КАНОН (К-26): ОТРИЦАНИЕ СТОИТ ДОРОЖЕ УТВЕРЖДЕНИЯ — оно требует доказать, что смотрел в нужном
месте нужным запросом.** Четвёртый случай той же формы за смену пришёл от аналитика: грепнул, не
нашёл, объявил отсутствующим. Различие инструментов сформулировано держателем дословно: `grep`
отвечает на «вижу ли я это тем шаблоном, который набрал», `git log -S` — на «когда эта строка
появилась и исчезла». Цена ошибки измерена: один мерж оплачен трижды. Против себя держатель:
трижды за смену объявила отсутствующим то, чего не искала (запись приёмки предшественницы и два
аналитика).

**ИТОГ ПАЧКИ Б (DIR-1, смена 16) — ОКОНЧАТЕЛЬНЫЙ ОТВЕТ НА ВОПРОС ВЛАДЕЛЬЦА «ЧТО МЫ ТЕРЯЛИ, ПОКА
ПРОХОД НЕ РАБОТАЛ». Отчёт: `RETRO-REPORT-DIR-1-batch-B-2026-08-27.md`.** 14 мержей: **6 чистых,
11 находок — из них ШЕСТЬ «фактическое утверждение», четыре гигиена, ОДНА провенанс.** Провенанс
практически чист: каждый юнит сделал ровно то, что просила карта, молча большего не сделал никто.
**Формулировка держателя, точнее моей: теряли не аккуратность кода, а ВЕРНОСТЬ ТОГО, ЧТО МЫ ПРО ЭТОТ
КОД НАПИСАЛИ.** Проход ловит две формы, недоступные ни одному из девяти пунктов приёмки —
утверждение, ставшее ложным ОТ СОСЕДНЕЙ ПОЧИНКИ, и утверждение ШИРЕ СОБСТВЕННОГО ЗАМЕРА; обе
встретились по три раза. Наследники: CB-237 (журнал посадок пишет остановленный сигналом финиш как
удачную посадку — **и начал врать ровно от нашей новой дисциплины**: 6 «посадок» против 5 настоящих
мержей, «2.50 попыток» вместо 3.00), CB-243, CB-238 (ложное число уехало к пользователю ВНУТРИ
выпущенной секции 0.2.2), CB-239 (`CLAUDE.md` противоречит себе в двух соседних пунктах), CB-240,
CB-244 (из приёмки), плюс дополнение в CB-213. Долг куратора закрыт: `tools/install-simplify-gate.py`
сел мержем `32dc15e`, инструкция стала выполнимой в свежем клоне; по дороге найдены ТРИ случая
«механизм описан лучше, чем существует» — в документах о самом этом механизме, причём **инструкция
куратора описывала природу хука ВЕРНО, расходился с ней скрипт**.
**ОШИБКА КУРАТОРА В ОХВАТЕ, названная держателем и признанная: спецификация ретро-прогона называет
38 мержей (32 с картами), а на её же коммите их 52 и 44.** Одиннадцать мержей с картами не названы
ни одной пачкой, десять из них — от 25 августа: перечисление построено по недавней памяти, а не по
полному обходу истории. Четыре держатель забрал сам (три чистых, четвёртый дал CB-237). **СЕМЬ
ОСТАЛИСЬ НЕОХВАЧЕННЫМИ** — территории DIR-2 и поверхностей; решение о доохвате принимает преемник
куратора. Это ровно тот дефект, который прогон и ищет: перечисление уже́ популяции, поданное как
полное. Четыре записи держателя против себя, все одной природы: бриф построен на стороже, чьё имя
прочитано, а тело нет; оракул ключевался на НАПИСАНИИ («слова „ОТМЕНЯЕТ" быть не должно») и зелен
на той же лжи другими словами — CB-227, воспроизведённый в юните про переобещание; итоги внесены в
журнал раньше сверки; запись о приёмке вставлена посекционно и разорвала соседнюю — механизм CB-235,
пойманный за день у двух юнитов и повторённый своей рукой. Против себя держатель записала три поправки снизу
(предпосылка о ключе с кавычкой; запрет брифа прочитан шире смысла; замер по требованиям не попал в
отчёт — стал CB-180).

**СЕССИЯ КУРАТОРА 2026-08-28 — СВЕРКА HANDOFF'А И ДВА РЕШЕНИЯ УРОВНЯ (1).** Сверка §0 с
источниками: все шестнадцать файлов на месте; расхождений в норме нет. В §1 три устаревших факта,
все исправлены в handoff'е, а не в источниках: открытых карт 54, а не 46 (ровно восемь
наследников прохода); установщик вставки `tools/install-simplify-gate.py` уже в истории
(`32dc15e`); стартовый промпт DIR-1 лежал на две смены позади своего handoff'а. Живых агентов нет
(последний коммит любого направления — 27.08 23:15, К-23), клеймов ноль, `main` == `origin/main`
на `de2e843`.
**Чего handoff не видел: пять брошенных деревьев агентов в устаревшем `.claude/worktrees/`, одно
грязное** — `agent-a20d255a5d5895ce8`, ~1260 незакоммиченных строк от 25.08 в шести файлах, из
них против нынешнего main живы 755. По составу (тесты эмбеддингов, сетевой гейт) похоже на
черновик CB-174, севший потом с ветки, но «похоже» — не замер. Куратор не читал дифф: это
preflight уровня (3). Задача DIR-1 (тройка П-Б): *замысел* — ни одна строка работы не должна
пропасть молча и ни одно дерево не должно висеть без хозяина; *задание-гипотеза* — сверить 755
строк с main и доказать, что каждая либо села, либо устарела, либо ценна, и в последнем случае
перенести на типизированную ветку; четыре чистых дерева снять `git worktree remove` без `--force`;
*критерий приёмки* — по каждому из пяти деревьев одна строка в артефакте направления: вершина,
что с содержимым, чем доказано; `git worktree list` показывает только главный чекаут и живые
рабочие копии. Удаление грязного дерева — только после доказательства, и это отдельный факт в §9.

**РЕШЕНИЕ 1 — ОХВАТ РЕТРО-ПРОГОНА: СЕМЬ НЕОХВАЧЕННЫХ МЕРЖЕЙ ДООХВАТИТЬ, РАЗДЕЛИВ ПО ТЕРРИТОРИЯМ.**
Запись выше «ЗАДАЧА ВЛАДЕЛЬЦА ИСПОЛНЕНА» была написана моим предшественником до того, как DIR-1
перемерил популяцию; честная формулировка — **исполнена на 37 мержей из 44 с картами**, и семь
оставшихся закрываются этой записью, а не молчанием. Довод за доохват, а не за осознанное
закрытие: выход прохода — 11 находок на 14 мержей у DIR-1 и 3 на 11 у DIR-2, преобладающий класс
«ложное утверждение» не ловится ни одним из девяти пунктов приёмки, а сама ошибка охвата —
«перечисление у́же популяции, поданное как полное» — есть ровно то, что проход ищет; оставить её
в собственном итоге прохода значило бы повторить дефект в документе о дефекте. Цена — примерно
полсмены на направление, новых классов расходов нет, поэтому решение принято без ратификации.
Разделение: **DIR-2** — `7750975` (CB-90, срез хвоста тул-колла), `fb008d1` и `8f69133` (Т-65/Т-70,
CB-150 и CB-181, гейт минта категорий) — территория идентичности; **DIR-1** — `2d5e473` (CB-168,
CB-169), `1fc7ff2` (CB-152, CB-155), `0d791bb` (CB-156), `603beab` (CB-154) — поверхности CLI и MCP
без живого держателя; отдаю DIR-1 как расширение участка, тем же правом, каким он взял
предыдущие четыре, и потому что метод прохода у него самый свежий. Тройка П-Б для обоих:
*замысел* — популяция задачи владельца закрыта целиком, а не по памяти; *задание-гипотеза* —
тот же проход по спецификации `RETRO-TASK-simplify-sweep-2026-08-27.md` над названными мержами,
гипотеза: класс находок будет тем же (утверждение, ставшее ложным от соседней починки, или
шире собственного замера); *критерий приёмки* — таблица «мерж → чист / находка → карта» в
артефакте направления, каждая находка воспроизведена держателем лично, а не пересказана
аналитиком; итоговое число мержей выведено из `git log --first-parent`, не из этой записи;
одна закрывающая строка сюда с числами обеих пачек. Срок — до следующих юнитов очереди, потому
что это долг перед задачей владельца, а очередь — план.

**ЗАКРЫВАЮЩАЯ СТРОКА DIR-2 ПО РЕШЕНИЮ 1, 2026-08-28: три порученных мержа пройдены, два дали
находки, одна из них исправлена этим же проходом.** `7750975` (карта CB-90, срез хвоста вызова
инструмента) — находка есть, но она уже несёт открытую карту CB-188: докстринг и `CHANGELOG`
утверждают, что законную разметку в конце описания нельзя отличить от настоящей утечки, и я
опровергла это построением, а не рассуждением — приземлённый предикат режет фрагмент ленты новостей
и теряет 22 байта, тогда как словарный конъюнкт, предложенный приёмщиком, ленту не трогает, а
утечку режет. Корпусные числа того же юнита перемерены и держатся: 3452 строки на трекере
autosorter, 80 с маркером, 61 с хвостом, режет 79 и 61, ложных срабатываний ноль. `8f69133` и
`fb008d1` (юниты Т-65 и Т-70, карты CB-150 и CB-181) — две ложные фразы в шапке `CASCADE-IDS.md`,
**исправлены коммитом `4c08786`**: «те же 12 отказов на каждом прогоне» опровергнуто моим прогоном
(125 переходов, 14 отказов, хвост ровно `75,76`), а скобка «число растёт вместе с main» опровергнута
трижды на разной длине истории (77, 83 и 125 переходов — 14 отказов на каждой). Поведение
минт-гейта не тронуто ни одной строкой ни одним из трёх мержей — проверено хешем файла со снятыми
комментариями в четырёх состояниях. **Почему это правка текста, а не новая карта, хотя обе фразы
уже несла CB-186, закрытая решением владельца:** в пакете, который владелец читал (вид 2
`BATCH-VIEWS-2026-08-25.md`), CB-186 описана одной строкой «коррекция записи не закреплена тестом»,
то есть ратифицированная формулировка «полировку собственного инструмента прекращаем» относилась к
незакреплённости записи гейтом, а не к простой фактической лжи в шапке нормативного файла; гейта я
не трогала, поправлены два предложения. Число мержей выведено из `git log --first-parent --merges
--since='2026-08-25'` — их **55** на дату работы, больше названных здесь 52, потому что история с тех
пор выросла; на предмет доохвата это не влияет. Разбор целиком, включая опровергнутого мною
кандидата аналитика и мою собственную ошибку того же класса, — в `DIR-2-findings-identity.md`,
раздел «ДООХВАТ РЕТРОСПЕКТИВНОГО ПРОГОНА». Четыре мержа DIR-1 закрывает держатель DIR-1 своей
строкой.

**НАХОДКА О ПРОЦЕССЕ, ВЫШЕДШАЯ ИЗ ЭТОГО ДООХВАТА, — строкой, а не картой, и она про форму пакетов
видов, то есть про инструмент уровня (1).** Карта CB-186 несла ДВЕ половины: незакреплённость
записи тестом и простую фактическую ложь в шапке нормативного файла. В пакете, который читал
владелец, она описана одной строкой — «коррекция записи не закреплена тестом», — то есть
предъявлена ровно одна половина, и ратифицированное «да» («полировку собственного инструмента
прекращаем») закрыло карту целиком, вместе с половиной, которой в резюме не было. Ошибка ничего не
стоила: цена — два предложения текста, и они поправлены. **Но форма опасна и повторима: резюме
карты в пакете видов пишется ради экономии внимания владельца, а закрывает оно карту, а не
резюме.** Предложение уровню (1), не требующее нового механизма: строка вида обязана называть
КАЖДУЮ половину, которую закроет «да», либо прямо говорить, что закрывается только названная. Это
та же форма, за которой охотится весь ретро-прогон, — утверждение шире собственного замера, — но на
уровне (1) она дороже, потому что за ней стоит ратификация владельца, а не текст в докстринге.

**РЕШЕНИЕ 2 — СПЛОШНОЙ ОБЗОР: НУЖЕН, ФОРМА ВЫБРАНА, БЮДЖЕТ — НА РАТИФИКАЦИЮ ВЛАДЕЛЬЦУ.** Замер
предшественника (27.08) стоит: ни один кросс-модельный проход не смотрел на композицию — только
на дизайны и отдельные юниты. Но «обзор кодовой базы» как проход по коду — не то, что показал
ретро-прогон: провенанс чист, гигиена вторична, теряли ВЕРНОСТЬ НАПИСАННОГО О КОДЕ. Поэтому форма —
не ревью `src/`, а **сквозной перечит трёх нормативных текстов как ОДНОГО текста против
механизмов, которые они описывают**: `CLAUDE.md` репозитория (его читает каждая сессия — ложь там
множится на каждый будущий юнит; CB-239 и CB-213 уже показали, что он противоречит себе на
стыках), `CHANGELOG.md` (единственный пользовательский текст; CB-238 и CB-205 — числа и обещания,
уехавшие к пользователю) и описания инструментов MCP (то, что видит клиент). Это и есть
композиционная проверка, которую собственное правило репозитория («проверка элементов не
проверяет их композиции») требует и которой ни разу не было. Цена: `CLAUDE.md` — порядка 40 тысяч
слов, сверка каждого утверждения с механизмом — это фан-аут по разделам с дешёвыми читателями и
дорогим судьёй плюс внешняя модель на композицию; оценка — полная смена направления и заметная
доля дневного лимита Fable/Codex. Именно из-за цены — на ратификацию, а не в очередь; семь мержей
Решения 1 идут раньше и стоят дёшево. Без слова владельца обзор не стартует.

**DIR-1, СМЕНА 17 (2026-08-28) — ОБА ПОРУЧЕНИЯ ИСПОЛНЕНЫ; ЗАКРЫВАЮЩАЯ СТРОКА РЕШЕНИЯ 1 ЖДЁТ ВТОРОЙ
ПОЛОВИНЫ.** *Поручение о деревьях (тройка П-Б) закрыто.* Пять брошенных деревьев агентов разобраны и
сняты, `git worktree list` показывает главный чекаут и одно живое дерево соседнего направления;
построчный разбор и доказательства — в §9 артефакта DIR-1. Число «755 строк живы против main»
перемерено и заменено: оно отвечает на вопрос «насколько дерево отличается от main» и смешивает
работу агента с тем, что main нажил после 25.08. На правильный вопрос — «есть ли в дереве строка,
которой в main нет» — ответ **35 строк**, и каждая найдена в main в поздней, в пяти случаях из шести
строго более сильной редакции. Содержимое закреплено тегом `salvage/agent-a20d255a5d5895ce8-2026-08-28`
(коммит `bf38a01`, 1260 добавленных строк, сверено побайтово), поэтому «ничего не потеряно» не
зависит от верности разбора. *Поручение о доохвате (Решение 1), половина DIR-1:* **четыре мержа, один
чист (`603beab`), три несут находки, карт-наследников две** — CB-245 (`low`, наследник CB-156) и
CB-246 (`low`, наследники CB-152 и CB-168). Карт меньше, чем мержей с находками, намеренно: счёт «67
вербов» живёт в двух мержах, потому что второй юнит переписал число соседа в свой файл. Обе находки
класса «утверждение», гигиенических нет; обе воспроизведены держателем лично на голдене того
состояния, о котором утверждение говорит. Число мержей выведено из `git log --first-parent`, не из
записи выше. **Закрывающую строку Решения 1 с числами ОБЕИХ пачек я не пишу**, потому что вторая
половина (три мержа DIR-2) на момент этой записи ещё в работе, а строка, называющая себя итогом при
половине данных, — ровно то переобещание, которое проход и ищет; её пишет тот, кто закроет вторую
половину, взяв числа DIR-1 отсюда.

**РЕШЕНИЕ 1 ЗАКРЫТО — СВОДНАЯ СТРОКА КУРАТОРА, 2026-08-28.** Обе половины доохвата исполнены, и
каждый держатель честно оставил сводную строку тому, кто закроет вторую половину, — в итоге её не
написал никто, поэтому пишу я, взяв числа из двух записей выше. **Семь мержей: два чистых
(`603beab` у DIR-1 и один из трёх у DIR-2), пять с находками; наследников-карт две — CB-245 и
CB-246 (обе `low`, обе класса «утверждение»); одна находка исправлена самим проходом (`4c08786`,
две ложные фразы в шапке `CASCADE-IDS.md`); одна уже несла открытую карту (CB-188).** Вместе с
пачками А и Б и четырьмя мержами, взятыми DIR-1 в смене 16, все **44 мержа с картами** на коммите
спецификации `98483c4` пройдены; задача владельца о ретро-прогоне исполнена целиком, а не по
памяти. Гипотеза тройки подтвердилась: ни одной гигиенической находки, все — утверждения. Побочно:
DIR-2 насчитала 55 мержей с 25.08 против 52 в спецификации — история выросла, на охват не влияет.
Дополнительно из поручения о деревьях: пять брошенных деревьев сняты, содержимое грязного
закреплено тегом `salvage/agent-a20d255a5d5895ce8-2026-08-28`, ни одна строка не потеряна; моё
число «755 живы» DIR-1 заменил на честное «35 строк, которых нет в main, и каждая есть там в
поздней редакции» — мой счёт отвечал не на тот вопрос, принимаю поправку.

**РЕШЕНИЕ 3 — ОТВЕТ НА ЭСКАЛАЦИЮ Э-33 (DIR-1, смена 17): СЕМЬЯ «ПРЕДЕЛ ВЫБОРКИ» ПЕРЕДАЁТСЯ DIR-2
ЦЕЛИКОМ, CB-158 УХОДИТ ИЗ ОЧЕРЕДИ DIR-1.** Три карты об одном свойстве — сколько строк вернуть,
когда вызывающий назвал предел, — заведены порознь и не знали друг о друге: CB-158 (`medium`, явный
`--limit 0` перебивается длиной списка идентификаторов, `reqs.py` и `findings.py`), CB-208 и
CB-210 (обе `low`, отрицательный предел и ложное «ничего не найдено»). Две последние DIR-2 уже
приземлила юнитом Т-114 (`b2283dd`), приёмка в полёте. Совет DIR-1 принят целиком, включая
половину в `reqs.py`: одна семантика, починенная в двух файлах разными руками, — способ получить
два ответа на один вопрос. Тройка П-Б для DIR-2: *замысел* — предел выборки означает одно и то же
на каждой поверхности трекера; *задание-гипотеза* — сначала проверить, не закрыл ли Т-114
CB-158 попутно (гипотеза: нет, потому что Т-114 про отрицательный предел, а CB-158 про
перебивание нулевого), затем починить обе половины одним юнитом; *критерий приёмки* — явный
`--limit 0` с `--id` возвращает ноль строк на обеих поверхностях, поведение `reqs` и `findings`
пиновано одним оракулом, `PAIN-ORDER` не меняется (CB-158 там стоит без направления, блок
«честные границы»). Место в очереди DIR-2 — сразу после приёмки Т-114, пока семантика свежа, до
CB-236.

**В КАНОН (К-27), из находки DIR-2 о процессе: СТРОКА ПАКЕТА ВИДОВ ОБЯЗАНА НАЗЫВАТЬ КАЖДУЮ
ПОЛОВИНУ, КОТОРУЮ ЗАКРОЕТ «ДА» ВЛАДЕЛЬЦА, ЛИБО ПРЯМО ГОВОРИТЬ, ЧТО ЗАКРЫВАЕТСЯ ТОЛЬКО НАЗВАННАЯ.**
Механизм: резюме карты в пакете пишется ради экономии внимания владельца, а его «да» закрывает
карту, не резюме; CB-186 несла две половины, в пакете была одна, «полировку прекращаем» закрыло обе,
и простая ложь в шапке нормативного файла простояла до доохвата. Это «утверждение шире собственного
замера» уровнем выше — с ратификацией владельца позади него. Проверка при сборке пакета: прочесть
карту целиком (описание И заметки, К-22-соседство) и сверить число утверждений в карте с числом в
строке. Механизма не требует, требует одного вопроса при каждом резюме.

**ПОПРАВКА К РЕШЕНИЮ 2 ПО ЗАМЕРУ DIR-1 (смена 17), принята куратором 2026-08-28.** Область «три
нормативных текста» у́же популяции — предложение несло ту самую ошибку, которую собиралось искать.
Счёт держателя по своей смене: шесть ложных или протухших утверждений, из них два в исходниках,
одно в тестовом модуле и **три в плановых артефактах каскада** (таблица юнитов, handoff, замер в
очереди) — тексты, которые ведут работу каждой сессии и которые не проверяет ни гейт, ни голден,
ни приёмка. Поэтому область Решения 2 расширяется до четырёх классов и задаётся не списком файлов,
а предикатом: *утверждение о популяции, проверяемое прогоном механизма*. Цена, замеренная
держателем предикатом «число рядом с существительным о популяции»: свободный предикат — около 1441
кандидата (`CLAUDE.md` 91, `CHANGELOG.md` 68, `README.md` 8, `src/` 271, `tests/` 383,
`.claude/plans/` 620); строгий (только цифры, только множественное число) — 347, из них по выборке
примерно две трети — настоящие утверждения. Это КАНДИДАТЫ, а не ложь: каждый требует прогона. Оценка
для владельца: порядка 230 проверок прогоном, фан-аут дешёвыми исполнителями по классам с дорогим
судьёй на композицию — две смены направления, не одна. Ратификация по-прежнему нужна; форма
уточнена, а не заменена.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 17 DIR-1 — Т-112 ПРИНЯТ, Т-113 ПРИНЯТ С ОГОВОРКОЙ
(закрыта в тот же час), CB-237, CB-243, CB-245, CB-246 закрыты — 2026-08-28.** Спот-чек куратора:
`f41ca3c` (Т-112, журнал посадок перестал писать остановленный сигналом финиш как удачный) и
`1489700` (Т-113, три ложных утверждения на поверхностях) — предки `origin/main`, проверено
`merge-base --is-ancestor`, а не порядком коммитов; `main == origin/main == b6aadef`; набор на
состоянии передачи 3177 пройденных. Оба поручения куратора 28.08 исполнены (деревья — тег
`salvage/agent-a20d255a5d5895ce8-2026-08-28` на `bf38a01`; доохват — четыре мержа, см. сводную
строку Решения 1). Наследники: **CB-249** (журнал посадок всё ещё врёт на двух сигналах из
четырёх — форма выбрана, одна правка) и **CB-250** (хук `commit-msg` не опознаёт заметку,
названную в конце предложения; форма намеренно не выбрана). Уточнение держателя к Решению 3
ПРИНЯТО: **CB-167 (`meta_key` без кавычек) в семью «предел выборки» не входит и остаётся за
DIR-1** — я назвал в решении только CB-158, читать его шире написанного держатель верно не стал.
Наблюдение процесса из смены, стоящее переноса в канон при повторении: два побочных утверждения
субагентов не выдержали прогона, тогда как главные сошлись до цифры, — побочное лежит вне оракула
брифа и вне девяти пунктов, поэтому проверять строже надо именно его. Ротация штатная: handoff
смены 17 и промпт смены 18 пересобраны держателем; смена 18 запущена куратором.

**РЕШЕНИЕ 4 — ОТВЕТ НА ЭСКАЛАЦИЮ Э-34 (DIR-1, смена 18) ПО CB-172: ВАРИАНТ (1) СЕЙЧАС, ВАРИАНТ (2)
— ПРИНЯТЫЙ ДОЛГ С ИЗМЕРЕННОЙ ЦЕНОЙ, 2026-08-28.** Суть для чтения с нуля: линтер `ruff` умеет
правило `S608` (помечает SQL, склеенный из строк вместо связанных параметров); по коду стоят два
десятка маркеров его подавления, и `CLAUDE.md` объявляет их дисциплиной — «маркер честен, только
если что-то проверяет форму». Правило в набор проверок не входит и никогда не входило: в
`pyproject.toml` нет секции правил вовсе, `ruff` работает набором по умолчанию. Документ объявляет
механизм, которого нет, — «гейт, который не может сработать», написанный прозой, самый повторяемый
класс этого репозитория. Опасности нет: замер смены 12, записанный в карту, прочёл все 52 места и
опроверг утверждение карты о трёх незащищённых подстановках. **Решение.** (1) Малый юнит DIR-1:
снять шесть мёртвых маркеров `S608` и привести `CLAUDE.md` к правде — не «правило выключено, и это
нормально», а точная формулировка: правило не включено, маркеры ничего не проверяют, включение —
отдельный долг с ценой ниже. Два из шести маркеров лежат в файлах, числящихся за DIR-2
(`merge.py`, `tests/test_milestones_reconcile.py`): относятся к DIR-1, потому что снятие мёртвого
комментария не меняет семантики чужой территории, — при условии, что перед посадкой держатель
проверит по `git worktree list` и диффам живых веток, что ни одна не трогает эти файлы (Т-116
соседа живёт в `findings.py`/`reqs.py`). (2) Включение правила целиком — 52 неподавленных места в
17 файлах одной правкой, впервые заведённая конфигурация правил и решение о полусотне мёртвых
маркеров других правил — **принятый долг**, не отказ: карта CB-172 остаётся открытой, перенацеленная
на вариант (2) с этой ценой, и берётся в ближайшее окно тишины (обе смены на ротации или срез
релиза), одним юнитом, с окном, а не по кускам. Полсотни мёртвых маркеров других правил — факт,
которого не было ни в карте, ни в handoff'е; DIR-1 фиксирует его картой или заметкой в CB-172,
чтобы замер не пропал. Ось владельца («видимое выше латентного») это решение соблюдает: и ложь
документа, и долг — латентны, поэтому дешёвая половина сейчас, дорогая — по окну, не по очереди.
**Побочно, из той же эскалации:** второй за вечер случай «посадка и отправка на origin — два
разных действия» (мерж соседа `751fd23` отправил DIR-1). Правило держателя принимаю: третий случай
чинится механизмом, не напоминанием; кандидат — отправка внутри `worktree-finish.sh` после замка
или строка «отправлено» в самом вердикте приёмки (п.6). Пока — наблюдение, не карта.

**НАБЛЮДЕНИЕ КУРАТОРА, ИЗМЕРЕНО НА СЕБЕ, 2026-08-28 вечер — для DIR-1 (соседствует с CB-244).**
Предупреждающая вставка прохода упрощения сработала на коммите плановой заметки в главном чекауте:
команда состояла из `git commit` по heredoc'у и `git push`, финиша в ней не было, но ТЕКСТ заметки
внутри heredoc'а упоминал имя скрипта финиша. То есть вставка ключуется на подстроке во всей
командной строке, включая содержимое передаваемых данных, — «гейт на написании», класс CB-227 и
CB-244, теперь в самом хуке. Вреда нет: вставка советует и не отказывает, коммит сел, отправка
прошла. Стоимость — ложная тревога у каждого, кто пишет о финише в заметке одной командой с
коммитом; и вторая сторона той же монеты: команда, вызывающая финиш через переменную или алиас,
подстроку не несёт и вставку не получит (не измерял — гипотеза, отличать от факта). Отдаю DIR-1
как заметку к юниту Т-118 или к CB-244, не как новую карту; карту, если нужна, заводит держатель.

**ПРИЁМКА (1)←(2), неизменяемый факт: смена 16 DIR-2 — Т-111 ПРИНЯТ С ОГОВОРКОЙ (оговорка →
CB-247), Т-114 ПРИНЯТ С ОГОВОРКОЙ (обе половины закрыты решением держателя: текст — `3eb6ec4`, код —
в Т-116), Т-115 ПРИНЯТ БЕЗ ОГОВОРОК; CB-241, CB-242, CB-208, CB-210, CB-217 закрыты — 2026-08-28.**
Спот-чек куратора: `0248624`, `b2283dd`, `751fd23` — предки `origin/main`. Наследники: CB-247,
CB-248 (29 из 68 обработчиков командной строки минуют `domain_errors()` — системный разрыв, вынесен
исполнителем Т-114 с ребром `found_during`), CB-251 (`low`, порог «третья пара флагов»), CB-254.
**Т-116 (CB-158 + кодовая половина оговорки Т-114) сделан, но НЕ ПРИЗЕМЛЁН**: ветка
`fix/cb-158-explicit-limit-wins` в копии, отчёт `L3-REPORT-DIR-2-T116.md` есть, посадка получила
живой отказ (записан в handoff смены) — приёмка и посадка переходят смене 17. Расширение объёма
Т-116 («не задан» = `None` через три слоя, чтобы узкая починка не открыла молчаливую усечку на
ветке `deferred`) санкционировано держателем (`L3-ANSWER-DIR-2-T116-scope.md`) — уровень (1) не
вмешивался и не будет: форма верна по композиционному доводу. Держатель ротировалась штатно
(handoff s16, промпт смены 17); её итоговый возврат до куратора не дошёл — восстановлен по истории.
**Ответ на спор о полномочии (DIR-2 против независимого разбора) — правка шапки `CASCADE-IDS.md`
коммитом `4c08786` НЕ была обходом ратифицированного решения по CB-186.** Ратифицированное
«полировку собственного инструмента прекращаем» относилось к закреплению записи гейтом — той
половине, что была в пакете видов; простая ложь в шапке нормативного файла — вторая половина,
которой владелец не видел (это и есть происхождение К-27). Снятие двух ложных фраз без касания
гейта — правда текста, а не полировка; держатель права. **Наверх, владельцу:** развилка по остатку
CB-62 — поставщик векторов внутри пакета — уже стоит в его батче под CB-65 (рядом с ценой схемы
леджера связей); отдельного вопроса не добавляю, чтобы не дробить одно решение на два.

**ПРИЁМКА (1)←(2), неизменяемый факт: Т-116 (DIR-2, смена 17) ПРИНЯТ С ОГОВОРКОЙ на `ba89587`,
CB-158 закрыта — 2026-08-28 23:5x.** Спот-чек куратора: `ba89587` — предок `origin/main`. Семь
зачётов, один «неприменимо» (пост-мержевого механизма в репозитории нет), один — запись факта в
артефакт направления, отложенная тишиной. Различение проверено прогоном двух мутантов приёмщицы;
набор на `main` 3202 зелёных. Оговорка — четыре помощника разбора скопированы из чужого сторожа
вместо выноса — долг направления, оформлен картой **CB-257** (`low`, порог взятия); рядом
**CB-256** (`low`: одно имя `limit`, три несовместимых смысла `None` в пакете). Расширение объёма
Т-116 на три слоя подтверждено принадлежностью коммитов санкции к предкам мержа.

**ДВА ОКНА ТИШИНЫ И ЛИВЛОК Б-12, ЗАМЕРЫ ПЯТЫЙ–СЕДЬМОЙ, 2026-08-28 23:30–23:50 — И ЗАПИСЬ ПРОТИВ
КУРАТОРА.** Оба направления встали на посадке одновременно: финишу нужно ~3,5 минуты неподвижного
`main` (полный набор под образцом), а `main` в этот час писали 22 раза за 75 минут — ни одного
кодового коммита, одни плановые заметки трёх сессий, включая куратора (три отказа Т-118 подряд
пришлись на мои коммиты в реестр). Замер DIR-1 по тринадцати интервалам: шанс одной попытки
~31 %; итог — Т-116 сел с шестой попытки, Т-118 с седьмой, около 25 минут прогонов на две карты
малой важности. **Первое окно-соглашение (DIR-2) сработало; второе (DIR-1) сорвано дважды: сначала
добросовестным исполнением канона немедленных коммитов («записывай посадку сразу» против «не
двигай `main`, пока сосед садится» — два правила, не знающих друг о друге), затем сессией, о
существовании которой не знал никто.** Это моя ошибка: держательница DIR-2 смены 16 значилась в
списке агентов «завершившейся» и молчала десять минут — я запустил смену 17, а она молча ждала
свой исполнитель Т-116 и проснулась с его возвратом, чтобы закрыть смену пятью коммитами в окно
соседа. Механизм точнее К-23: **агент с живым ребёнком не мёртв, даже когда список показывает
«завершён», — он оживёт с возвратом ребёнка**; до запуска преемника направления проверять живых
детей и наличие штатной ротации, сделанной самим держателем. Разрешение: смена 16 остановлена,
направление у смены 17, пять коммитов смены 16 — законные факты её юнита, остаются. Сработало
третье окно — **явный безусловный запрет всем ветвям до события «сел SHA»**: одна попытка, один
прогон. Вывод DIR-1 дословно: разница между «договорились не писать» и «названо, кто именно молчит
и до какого события» — это разница между пятью отказами и одним прогоном. **Наверх, владельцу,
вопросом формы (после трёх кругов кросс-модельного ревью, отвергших пять форм, решение (d) «только
измерять» своё отмерило — число есть):** кандидаты — окно тишины как механизм харнеса (замок
`main` на время посадки, который заметки уважают автоматически), либо посадка, не требующая
неподвижного `main` на всё время набора. Оба — не мои решения и не сегодняшние.
**Две находки процесса, принятые в реестр из этого часа.** (а) Подпись `docs(dir-2)` называет
направление, а не сессию, поэтому две живые сессии одного направления неразличимы в истории;
предложение владельцу — номер смены в подписи (`docs(dir-2/s17)`), до его слова не вводить.
(б) Контракт приёмки предписывает мутационную пробу правкой исходного файла, а харнесс у
приёмщика этот инструмент отключает; оба приёмщика вечера (Т-114, Т-116) вышли из положения
перепривязкой объекта в памяти процесса с доказательством тождества — верно, но следующий может
отчитаться «не смог». **Решение уровня (1): проба определяется эффектом, не инструментом** —
мутант применён в изолированной копии любым способом, тождество доказано, поведение измерено;
правка `~/.claude/agents/acceptor.md` и скилла `acceptance-check` — первое действие преемника
куратора. Единственный канал держатель → исполнитель (заметка на `main`) сам двигает `main` и
ломает посадку, о которой договаривается, — часть того же вопроса формы.

**ДОПОЛНЕНИЕ К ПРИЁМКЕ Т-116: ВТОРОЙ НЕЗАВИСИМЫЙ ВЕРДИКТ, И ЛОЖНОЕ ЧИСЛО ПРОШЛО ЧЕРЕЗ РУКИ КУРАТОРА —
2026-08-28 ночь.** Приёмщик, запущенный держательницей смены 16 до того, как она замолчала, вернул
вердикт после её остановки: ПРИНЯТ С ОГОВОРКОЙ на `ba89587`, восемь зачётов, набор 3228. Оговорка
не пересекается с первой: в перечне невидимого сторожа предела выборки стоит «пять из четырнадцати
сайтов несут `offset`», перемерено предикатом самого сторожа — четыре. **Цепочка числа: вердикт
приёмщика Т-114 → пересказ куратора держательнице (моё сообщение 28.08 ~15:40) → её бриф → код
исполнителя.** Четыре руки, ни одна не перемерила; юнит, чей предмет — «обещание шире проверки»,
посадил непроверенное число, и первым звеном без замера был я — пересказал вердикт как факт.
Правило для куратора, не новое, а нарушенное: число из чужого вердикта передаётся с пометкой
«по вердикту приёмщика, не перемерено» либо не передаётся вовсе (К-26 в применении к цифрам).
Закрытие — одна строка, смена 17. Тот же приёмщик дал первое живое подтверждение CB-257 в день её
заведения (скопированный докстринг обещает больше, чем делает копия) и наблюдал, как сигнализация о
движении дерева сработала по назначению, когда его временную копию перевели на другой коммит во
время прогона. Два вердикта на один юнит — побочный продукт двойной держательницы; оба зачтены.

**ВЛАДЕЛЬЦУ, В БАТЧ, С ПОМЕТКОЙ «ЖИВОЙ ВРЕД В ЧУЖОМ РЕПОЗИТОРИИ» — 2026-08-29, из замера DIR-2 по
CB-234.** Сам `resolve-trailers` в codebugs латентен (ноль карт из 257 здесь и из 3480 в autosorter
несут его заметку), форма починки выбрана замером (повтор не пишет ничего, поэтому и дата не едет;
воспроизведено на одноразовом репозитории — два прогона, байт-в-байт одна строка). **Но у него есть
живой близнец в autosorter: скрипт `tools/auto-resolve-codebugs.py` пишет заметку той же формы, и
там дефект сработал — пять карт несут девять дословных повторов, а у ТРЁХ карт заметки не
дописывались, а ПЕРЕЗАПИСЫВАЛИСЬ: история расследования уничтожена безвозвратно.** Это не
территория DIR-2 и не трекер codebugs; карту на трекере autosorter куратор этого проекта не заводит
(граница роли), а заводит тот, кого назначит владелец, — одна команда `add` с этими числами; до
того факт живёт только здесь. Цена промедления — каждая следующая автоматическая резолюция в
autosorter затирает ещё одну заметку.

**В КАНОН (К-28): ОДИН ВЕРДИКТ ПРИЁМКИ НЕ СЧИТАЕТСЯ ПОЛНЫМ.** Два независимых приёмщика Т-116 —
побочный продукт двойной держательницы — дали два НЕПЕРЕСЕКАЮЩИХСЯ остатка (скопированные
помощники; ложное число в перечне невидимого). Ни один проход не исчерпал работу. Это довод не за
то, чтобы всегда пускать двоих, а за то, чтобы «принят» читать как «остаток не найден этим
проходом», и на юнитах, которых никто не перепроверит (правило 9 каскада моделей), второй проход
заказывать по имени. Формулировка держательницы DIR-2, принята куратором.
**Попутно принято к сведению:** CB-257 дополнена оговоркой, которой не было при заведении —
дословное сравнение копий сегодня было бы красным, копия уже расходится с образцом по существу, так
что дешёвый вариант «закрепить совпадение» стал «сначала свести, потом закрепить»; число «четыре из
четырнадцати» перемерено держательницей лично по полной популяции и уходит в юнит CB-247/CB-254
восьмым пунктом (не в летящий Т-119 — прецедент смены 16: оговорка в следующий юнит, не в летящий);
предпосылка её же брифа «пользователю виден ровно один пункт» опровергнута ею самой (видны два,
смешивает аудитории один).

**ПОПРАВКА ЦИФРЫ И ДВА КАНОНА ИЗ ВОЗВРАТА DIR-1 (смена 18) — 2026-08-29 00:5x.** В записи «ДВА ОКНА
ТИШИНЫ…» сказано «Т-118 сел с седьмой попытки» — это число ВЫЗОВОВ держателя, не попыток. Журнал
посадок несёт по слагу Т-118 **пятнадцать строк** на один приземлившийся мерж: одна rc=11, девять
rc=13, две rc=1 (ложный красный), одна rc=0, одна rc=2 уже после посадки; строки идут парами с
разницей в 1, 3 и 8 секунд, чего один процесс дать не может физически (прогон 200+ секунд), — то
есть по одному слагу шли ДВА финиша параллельно. Кто запустил второй — сам держатель: возобновив
упавшего исполнителя, он затем начал повторять финиш сам, не сказав исполнителю остановиться.
Замок отработал (мерж ровно один), но журнал — прибор, заведённый, чтобы порог «пора чинить
харнес» стал числом, — получил число, завышенное вдвое; CB-187 дописана этой уликой как вторым
вредом того же механизма (первый — убитый прогон проигравшего). Четвёртый за смену «счёт, чья
область у́же вопроса», на этот раз против самого держателя.
**В КАНОН (К-29): ПЕРЕХВАТ РАБОТЫ У ЖИВОГО ИСПОЛНИТЕЛЯ НАЧИНАЕТСЯ С ЕГО ОСТАНОВКИ** — иначе «помощь»
становится вторым конкурирующим процессом. Тот же механизм, что двойной держатель направления у
куратора в эту же ночь, уровнем ниже; формулировка держателя DIR-1.
**В КАНОН (К-30): ПРАВКА АРТЕФАКТА В ГЛАВНОМ ЧЕКАУТЕ ЛИБО КОММИТИТСЯ В ТУ ЖЕ МИНУТУ, ЛИБО НЕ
ДЕЛАЕТСЯ.** Незакоммиченная правка блокирует чужие посадки кодом 11, отказ не самоустраняется, и
автор о нём не узнаёт (CB-130): за ночь два случая одним файлом, второй — через час после того, как
держатель записал в журнал абзац против себя о первом. Проза не обеспечивает прозу, включая
покаянную; пока механизма нет — канон, а вопрос механизма (третья дверь ливлока) — в батче
владельца рядом с двумя первыми.
Статус юнитов DIR-1 смены 18 по возврату: Т-117 ПРИНЯТ С ОГОВОРКОЙ на `4fead6c` (три карты
закрыты), Т-118 ПРИНЯТ без оговорок на `5bb8295` (две карты закрыты); наследники CB-252, CB-253,
CB-255, CB-258; бриф Т-120 (вариант 1 по CB-172, Решение 4) лежит на main — выдаёт смена 19.
Т-119 DIR-2 (CB-236) сел `4fb2563`, приёмка у DIR-2.

**СМЕНА 18 DIR-1 ЗАВЕРШЕНА ШТАТНОЙ РОТАЦИЕЙ (`174b4b0`) — 2026-08-29 утро.** Вердикты Т-117/Т-118 и
наследники записаны выше; пункт 6 контракта закрыт фактом на обоих (серверные алярмы `success`).
Обобщение держателя принято в реестр рядом с К-28…К-30: **«соглашение не может обеспечить
соглашение»** — «проза не может обеспечить прозу» уровнем выше; окно-соглашение сорвано дважды
добросовестно, окно-запрет до наблюдаемого события сработало с первой попытки. Преемнику велено
проверить, не одна ли карта CB-255 и CB-258 (обе — тесты, ломающиеся от параллельной работы).
**Второй случай К-29 в ту же ночь, у DIR-2, добровольным признанием:** держательница запустила
финиш Т-119 по моему «стартуй немедленно», забыв, что её исполнителю велено ждать сигнала, — и
сигнал разбудил его тоже; два финиша разошлись на пять секунд, приземлился ЕГО (правильно: его
сессия делала проход упрощения, и посессионный гейт честно остановил её запуск, а не его). Развела
их не осторожность, а замок и снятие копии — харнес. Уточнённая формулировка К-29 от неё:
перехватывающий сначала отменяет своё же «стой», потом стартует. **К вопросу формы ливлока в батче
владельца — оба направления независимо указали на CB-187 форму (a)** (неблокирующий замок на ветку
поверх замка на интеграцию): она закрыла бы и перехват у живого исполнителя, и завышение счёта
попыток. Т-119 (CB-236) сел `4fb2563` через шов `db.named_input_conflict` (санкция держателя с
условием: приёмщик меряет само оправдание невозможности прямой формы, не доверяет ему); приёмка в
полёте.

**ПРИЁМКА (1)←(2), неизменяемые факты: Т-119 ПРИНЯТ С ОГОВОРКОЙ на `4fb2563` (обе оговорки —
держателя, обе закрыты: запись факта `2811ec4`; путь модели закреплён таблицей задним числом,
введено правило направления «путь модели пишется в бриф при выдаче»), CB-236 закрыта; Т-121 СЕЛ на
`51d5854` с первой попытки, CB-247 и CB-254 закрыты, приёмка ждёт отчёта исполнителя — 2026-08-29.**
Спот-чек куратора: оба мержа — предки `origin/main`. Против держателя, её словами: предписанную
форму Т-119 исполнитель опроверг (цикл импортов настоящий, через второе звено) — «я проверила одно
звено графа и назвала это проверкой связности», семейство «утверждение шире замера». Исполнитель
Т-121 перемерил «четыре из четырнадцати» независимо (сошлось, три прочтения дают одно множество),
нашёл ошибку в самой CB-247 (две поверхности, не три) и вывел спорное число тестом вместо правки —
отказ от буквы указания ради смысла, санкционирован держателем.
**В БАТЧ ВЛАДЕЛЬЦУ — CB-260, про оснастку:** обязательный гейт `/simplify-traced` навешен хуком,
проверяющим ФАКТ ЗАПУСКА прохода, а не результат, и третья фаза прохода (`/reflect`) в codebugs
неисполнима — нет ни файла правил, ни каталога назначения, ни скрипта свёртки; скилл написан под
соседний проект и сам разрешает пропуск. Обязательный механизм молча отдаёт две трети обещанного.
Карта несёт три варианта без выбора за владельца; это шестой пункт батча, родня К-24 (гейт,
утерянный при переносе) и Решению 4 (механизм, описанный лучше, чем существует).

**Т-120 СЕЛ (`3e52280`, вариант 1 по CB-172) — И ЧЕТВЁРТОЕ ОКНО ТИШИНЫ ПОДТВЕРДИЛО ФОРМУ:
2026-08-29 день.** Журнал по слагу — две строки: один отказ 13 (прогон, обречённый движением main
ДО объявления окна) и посадка с первой попытки внутри окна; против пятнадцати строк и девяти
отказов у Т-118 без окна. CB-172 не закрыта — перенацелена на вариант (2) по Решению 4. Приёмка —
два прохода по К-28, последовательно (двое одновременно в главном чекауте воспроизвели бы CB-258).
**Доля куратора в обречённом прогоне признана:** моя запись приёмки легла в 14:20:41, внутри чужого
прогона, и убила бы его одна; правило куратору впредь — перед коммитом на main проверять идущие
финиши ЭТОГО репозитория: `for p in $(pgrep -f worktree-finish.sh); do readlink /proc/$p/cwd; done`
(уточнение DIR-2: сторож без проверки каталога отказывал бы из-за чужих репозиториев, а сторож,
отказывающий без причины, живёт до первого, кому помешал). **Две находки DIR-2 в копилку CB-176 и
вопроса формы:** (а) её собственная инструкция исполнителю дала сторожа, не видящего то, ради чего
поставлен, — `git worktree list` показывает копии, а не идущий финиш; (б) по построению: единственный
канал отчёта исполнителя — заметка на main, значит каждый отчёт есть угроза чужой посадке; это не
про окна, это про любой отчёт вообще. Обе — довод к механизму вместо соглашения (CB-187 форма (a)
уже названа обоими направлениями).

**ПРИЁМКА (1)←(2), неизменяемый факт: Т-121 ПРИНЯТ С ОГОВОРКОЙ на `51d5854` — 2026-08-29 день.**
Записи держательницы `78ecbbe` (посадка с санкциями) и `1181de0` (факт приёмки) — предки
`origin/main`, проверено куратором. Оговорка — держательницы (в брифе нет буквы класса по Р1),
закрыта записью в леджер; правило «путь модели и класс — в бриф при выдаче» введено ею себе и уже
исполнено в брифе Т-123. Число «четыре из четырнадцати» мерили ТРОЕ независимо (держатель,
исполнитель, приёмщик) — сошлось: класс «непроверенное число по цепочке рук» починен формой, а не
цифрой. Против держательницы, её записью: «знаки против байтов» — повторила единицу измерения за
исполнителем, не проверив (вывод не страдает, разность верна в обеих). **Третий случай за сутки:
инструмент правки файлов у приёмщика отключён харнесом** — приёмщица снова обошлась подменой
читаемого тестом; довод к решению «проба определяется эффектом» усилен, правка норматива приёмщика
остаётся первым действием преемника куратора. **Уточнение правила `pgrep` (двое поймали
самосовпадение независимо — куратор и DIR-2 в один час):** сторож, ищущий подстроку в командных
строках, находит СЕБЯ, когда его собственная команда несёт эту подстроку; настоящий финиш
отличается аргументом-слагом — проверять аргументы процесса и его каталог, не совпадение строки.
Запущен Т-123 (CB-234); очередь DIR-2 дальше: CB-190, CB-184, CB-163.

**РОТАЦИЯ КУРАТОРА 2026-08-29 ВЕЧЕР; ДНЕВНОЙ БРИФ СОБРАН; И УТОЧНЕНИЕ ПРАВИЛА РАЗЛИЧЕНИЯ СМЕРТИ
АГЕНТА — добровольным признанием DIR-2.** Бриф `briefs/DAILY-2026-08-29.html` (пять решений с
вариантами и ценами, два действия руками); handoff `HANDOFF-curator-2026-08-29.md` вытесняет
вчерашний. Поправка DIR-2 к её же процедуре, принятая дословно: второй «обрыв» исполнителя Т-123
обрывом не был — исполнитель жил и молчал, потому что обзорщики прохода работают в фоне и файлов
не трогают, так что живой проход и оборванная сессия снаружи НЕРАЗЛИЧИМЫ; три отрицательных
наблюдения не складываются в положительное «мёртв»; прямая проба требует ожидания, соразмерного
проходу (~30 минут, не 8), а если ждать нельзя — честная форма «не могу отличить и действую как
при обрыве, цена названа». Воскресший исполнитель принёс находку лучше ошибки: недостижимая
ветвь, чей мутант не красит ни одного из 119 тестов, при тексте, обещавшем пин, — приведено к
правде. Т-122 (CB-258) сел `db53391` с ПЕРВОЙ попытки, одна строка журнала — третий и лучший
замер окна тишины; пуш и приёмка за DIR-1, предложение исполнителя по CB-255 (пять проб в
одноразовые копии) — решение DIR-1.

**ДОПОЛНЕНИЕ К ЗАДАЧЕ §2 П.5 HANDOFF'А (правка норматива приёмщика) — CB-263; И ЗАКРЫТИЕ ОБЕИХ
СМЕН — 2026-08-29 ночь.** `codebugs` на `PATH` — снимок `pipx`, не исходник чекаута, и по версии
они неразличимы (обе `0.2.2`): приёмщик, проверяющий починку верба запуском по имени, увидит
дореформенное поведение и честно заключит «не приземлилось»; тесты не защищают — набор и бинарник
смотрят на разные деревья. Правка норматива приёмщика делает ОБЕ вещи одним юнитом: «проба
определяется эффектом, не инструментом» и «проверка запуском — через окружение проверяемого
чекаута, никогда через голое имя на PATH». Рядом урок держательницы: проверка «прошло ли»
спрашивает про СВОЙ объект, не про состояние вообще — `git log -1` после отказавшего коммита
отвечает про чужую работу и звучит утвердительно. **Обе смены закрыты штатно:** DIR-2 смена 17 —
четыре юнита (`ba89587`, `4fb2563`, `51d5854`, `e3688e3`), пять карт закрыто, шесть заведено с
порогами, пять записей против себя; DIR-1 смена 19 — ротация `a542829`, Т-122 (`db53391`) сел с
первой попытки под окном. Живых держателей нет; преемник куратора запускает смены 18 (DIR-2) и 20
(DIR-1) из их PROMPT-файлов. Сторож куратора «идёт ли финиш» уточнён после собственной ложной
тревоги: висящая оболочка с текстом скрипта в командной строке — не финиш; предикат — аргумент-
скрипт плюс непустой слаг плюс каталог, как и записала DIR-2.

**СЕССИЯ КУРАТОРА 2026-08-30 — ВХОД ПРЕЕМНИКА: СВЕРКА ЧИСТА, Т-122 В РЕЕСТР, НОРМАТИВ ПРИЁМЩИКА
ПОДГОТОВЛЕН, ДВЕ НОВЫЕ ЗАДАЧИ ВНИЗ.** Сверка §0 (субагент, одиннадцать пунктов): расхождений нет;
«§16.3» — внутренняя нумерация спеки, не markdown-заголовок, так спека сама себя цитирует в пяти
местах — правки handoff'а не требует. Сторож финишей при первом же применении поймал сам себя
подстрокой в собственной командной строке — уточнённый предикат (аргумент-скрипт + слаг + каталог)
отработал верно: финишей не шло.
**ПРИЁМКА (1)←(2), неизменяемый факт: Т-122 ПРИНЯТ БЕЗ ОГОВОРОК на `db53391` (вердикт DIR-1 смены
19, запись `bd71d31`), CB-258 закрыта — 2026-08-30.** Спот-чек куратора: `db53391` — предок
`origin/main` (`merge-base --is-ancestor`); `main == origin/main == 874a5ef`. Посадка с первой
попытки под окном тишины, одна строка журнала — третий и лучший замер окна.
**§2 п.5 handoff'а исполнен в подготовленном виде.** Правка `~/.claude/agents/acceptor.md` и скилла
`acceptance-check` написана целиком (обе половины: «проба определяется эффектом, не инструментом» и
CB-263 «запуск — из окружения проверяемой копии, никогда по голому имени на PATH; вопрос „прошло
ли“ — про свой объект»), но классификатор прав отклонил прямую запись в `~/.claude/` — по правилу
smart-approve файлы ждут руки владельца в scratchpad сессии куратора, команда применения — в
отчёте владельцу. До применения решение уровня (1) действует через этот реестр: приёмщик с
отключённым Edit делает мутацию перепривязкой объекта с доказательством тождества, «инструмент
недоступен» — не BLOCKED. Отказ классификатора куратор не обходил — то самое правило, которое
только что вписано приёмщику.
**ЗАДАЧА ВЛАДЕЛЬЦА (прямое указание 2026-08-30): СЖАТЬ `CLAUDE.md` — тройка П-Б отдана DIR-1 вне
очереди.** Документ превысил предел загрузки харнесса: 227 129 знаков против 150 000 — харнесс
предупреждает, нормативный текст рискует доезжать до сессий усечённым, то есть молча не доезжают
именно правила. *Замысел*: каждая сессия снова читает документ целиком; ни одно несущее правило,
ни один идентификатор карты, ни одна пинуемая тестами формулировка не теряются. *Задание-гипотеза*:
резать повторные пересказы историй ревью, не правила; популяцию тестов, читающих `CLAUDE.md` и
пинующих его прозу, исполнитель добывает preflight-ом сам; дореформенный текст остаётся в истории
git. *Критерий приёмки*: размер ниже предела с запасом; полный набор зелёный, включая тесты,
читающие `CLAUDE.md`; сквозной перечит результата как одного текста выполнен и его итог назван.
**Против куратора:** до чтения промпта роли успел лично сделать preflight (нашёл тесты, пинующие
прозу) и создать worktree `docs/claude-md-compaction` — дерево снято, пустая ветка (ноль коммитов)
удалена, находки переданы держателю с пометкой «не перемерено, добыть заново».
**ВТОРАЯ ЗАДАЧА DIR-1, структурная и малая: погасить алярм `main-invariants` правкой буквы, не
отмывкой базлайна.** Столкнулись два механизма уровня (1): дневной бриф владельца пишется в
`.claude/plans/briefs/*.html`, а допуск хука и CI — `.claude/plans/*.md` первым уровнем; красный
алярм маскирует следующее настоящее нарушение. *Замысел*: на main по-прежнему только мержи и
плановые артефакты, и алярм снова умеет гореть. *Задание-гипотеза*: расширить допуск (pre-commit
и `main-invariants.yml`) на путь брифов — смысл ратифицированного правила сохраняется, бриф есть
та же плановая заметка; базлайн не двигать. *Критерий*: алярм зелен на текущей истории (лежащий
HTML-бриф включая) и красен на мутанте — произвольном файле вторым уровнем вне briefs/; обе
стороны показаны. Владельцу — одна строка «буква правлена ради смысла» (уже в отчёте куратора).
Смены запущены: DIR-1 смена 20, DIR-2 смена 18 — фоном, пин opus, из их PROMPT-файлов.

**НАМЕРЕНИЕ ВЛАДЕЛЬЦА, ЗАПИСАНО ДО ПОДТВЕРЖДЕНИЯ (2026-08-30 вечер): НЕДЕЛЬНЫЙ КРИТИЧЕСКИЙ ОБЗОР —
«ещё раз пройтись по изменениям последней недели и критическим взглядом посмотреть, всё ли нужно,
хорошо ли сделано», возможно завтра.** Не задача — намерение; работа не стартует до его слова.
Подготовка преемнику: популяция выводится предикатом `git log --first-parent --since=<неделя>`
(на вечер 30.08 — 79 интеграционных мержей, 662 коммита первой линии; перемерить при старте, к
утру вырастет). Апертура ШИРЕ Решения 2 из батча (там — верность утверждений против механизмов;
здесь — нужность и качество сделанного), поэтому при подтверждении предъявить владельцу ОДНО
решение о форме, не два (прецедент «не дробить» — запись о CB-62/CB-65): варианты — обзор недели
поглощает Решение 2, предшествует ему или заменяет; цена каждого — с чисел выше, фан-аут дешёвыми
читателями с дорогим судьёй на композицию.
**Поправка DIR-2 к различению смерти агента, принята куратором; запись против куратора, вреда
нет.** Отчёт харнесса «агент упал: сеть» о СЕССИИ держателя пришёл, когда падал её ИСПОЛНИТЕЛЬ, —
держательница жила и работала; моё «ты оборвалась» она опровергла надёжным различителем (прямой
отчёт о падении ребёнка плюс содержательная мысль, оборванная на полуслове, — против штатной
записи смены в истории). Уточнение к К-23 и записи от 29.08: сигнал харнесса о падении так же
требует различения «чей это был процесс», как тишина — различения «жив или молчит».
**Т-124 (CB-190) СЕЛ: мерж `c1e12f2`, посадка с первой попытки, CB-190 закрыта, побочная находка —
CB-267; приёмка за DIR-2.** Возврат исполнителя дошёл до куратора, потому что адрес держателя из
сессии исполнителя оказался недостижим («No agent named 'claude' is reachable») — дефект канала
возвратов, второй случай за сутки (первый — 29.08, «итоговый возврат до куратора не дошёл»);
передан держателю указателем, юнит куратором не принимался. Правило «коммить в ветку рано»
окупилось при первом же обрыве: механизм юнита пережил смерть своего автора, потому что был в
ветке за час до неё.

**ЗАМОРОЗКА `CLAUDE.md` НА УРОВНЕ ФАЙЛА — решение куратора по находке F5 вердикта противника на
бриф Т-126, 2026-08-30.** Пока Т-126 (сжатие документа, перепишет около половины файла) не сел
мержем, `CLAUDE.md` не трогает ни одно направление и ни один исполнитель; нужная юниту правка
откладывается или передаётся текстом держателю DIR-1 для включения в Т-126. Основание: коллизия
уже реализовалась один раз без вреда (Т-124 вырастил файл на 4 691 байт под инвентарём брифа
Т-126), а конкурентная правка при посадке Т-126 даст конфликт максимального размера — финиш
мержит main в дерево перед прогоном. Оба направления уведомлены сообщениями. Снимается фактом
посадки Т-126 автоматически. Вердикт противника на бриф целиком: годен с правками, фатальны
F1–F3 (устаревшие числа инвентаря; невоспроизводимый довод выполнимости; §5 против §7(г)) —
дословный текст передан держателю DIR-1, правка брифа за ним; числа вердикта куратором не
перемерены.

**ПРИЁМКА (1)←(2), неизменяемые факты: СМЕНА 18 DIR-2 ЗАКРЫТА ШТАТНО — Т-124 ПРИНЯТ (CB-190,
мерж `c1e12f2`), Т-128 ПРИНЯТ С ОГОВОРКАМИ (CB-179 и попутная CB-268, мерж `dc9a9d8`) —
2026-08-31.** Спот-чек куратора: `c1e12f2`, `dc9a9d8` и соседский `327590d` (Т-125, CB-266) —
предки `origin/main`, проверено `merge-base --is-ancestor`; набор на `main` вырос 3261 → 3350.
Новые карты смены: CB-265, CB-269; CB-268 заведена и закрыта в ту же смену — вычислением
(правило «ведущее подчёркивание не создаёт нового имени» выводится из словаря, а не пополняет
перечень). Три карты очереди перемерены с отменой их собственных предпосылок: CB-163 (пересечение
двух графов пусто — спроса на объединение нет), CB-184 (сужена до одного следствия), CB-179
(популяция вдвое больше заявленной, арифметика самой карты не сходилась — 9 против суммы 10).
Вывод смены, принят в реестр: «утверждение шире замера» за смену — семь случаев, ВСЕ недооценки;
гипотеза держательницы — недооценка не мешает работать дальше и потому не замечается, переоценка
спотыкается о первую проверку; рецепт, отличивший единственный устоявший замер, — исполнитель
строил замер ЗАНОВО и сравнивал итоги, а не сверял список. Записи против держательницы — в её
артефакте, включая неприменённое правило сторожа финишей, отбившее посадку соседей.
**Расхождение ротации: стартовый промпт `PROMPT-DIR-2-next-session.md` НЕ пересобран (лежит для
смены 18)** — дефект «промпт позади handoff'а», уже пойманный сверкой 28.08 у DIR-1. Смена 19
запущена куратором промптом стандартной формы от `HANDOFF-DIR-2-2026-08-31-s18.md`; пересборка
файла — за новым держателем при его собственной ротации. Вершина `21407ec` (запись приёмки Т-128)
лежала только локально — отправлена вместе с этим коммитом.

**ЗАДАЧА ВЛАДЕЛЬЦА О `CLAUDE.md` ПОСТАВЛЕНА ЗАНОВО — ЦЕЛЬ ПРОВЕРЕНА И ЗАМЕНЕНА (слово владельца
2026-08-31: «давай заново, и убедись, что цель правильная сначала»).** Старая цель — «не превысить
150 тыс. знаков, иначе усечение» — СНЯТА как опровергнутая: усечения нет в двух измеренных
окружениях (сессия держателя DIR-1 и сессия куратора — файл впрыскивается целиком), число 150 000
— из предупреждения харнеса, вреда за ним не найдено. Настоящий вред — ЦЕНА ВПРЫСКА: 230 413 байт
≈ 58 тыс. токенов платит каждая сессия и каждый субагент каскада до первой строки работы (порядка
двух тысяч запусков агентов по журналам одной машины — оценка масштаба). Новая цель: **сессия
платит контекстом только за правила действия; вся память проекта остаётся в репозитории находимой
по указателям; ни одно правило не исчезает молча.** Не-цели, явно: порог 150 тыс.; свежесть
снимка (реальный отдельный дефект — карту заводит DIR-1, с наблюдением исполнителя Т-126, что
харнес умеет перевкладывать файл посреди сессии). Тройка П-Б отдана DIR-1: слой правил + архив
обоснований, КАЛИБРОВКА ПЕРВОЙ (целевой размер выводится из неё с SHA замера, не назначается —
уроки F1/F2), пины тестов перенацеливаются, для каждого правила — преемник или явная строка
«снято: причина». Потолок «5–6 %» прежнего разбора признан артефактом циркулярного критерия
приёмки, который написал куратор («ничего не потерять из файла» объявляло необходимым всё
написанное) — запись против куратора. Достигнутое Т-126 сжатие приземлено (`2eceaec`, с первой
попытки) и служит базой калибровки; заморозка `CLAUDE.md` переезжает с Т-126 на новый юнит и
действует до его посадки.

**ПРИЁМКА (1)←(2) И РЕШЕНИЕ 5 — ОТВЕТ НА ПРЕДЛОЖЕНИЕ DIR-2 ПО CB-163: смена 19 DIR-2 закрыта
штатно, форма (h) РАТИФИЦИРОВАНА, постройка — с первым потребителем — 2026-08-31.** Спот-чек
куратора: все восемь SHA смены (`ad5bfe2` … `eb21ed3`) — предки `origin/main == b47bcd6`;
расхождение «промпт позади handoff'а» закрыто держательницей (PROMPT пересобран для смены 20).
Юнитов не приземлено — выход смены решение, а не код; очередь стояла три четверти смены, и одна из
трёх причин — заморозка `CLAUDE.md` куратора, чья цена оказалась выше объявленной (блокировала две
карты; принято как данные к цене таких заморозок). **Решение 5, по `L2-PROPOSAL-DIR-2-CB163`
редакция 3 (две независимые модели, 27+11 находок, три редакции):** (1) чтение ратифицировано —
«карта Б отпочковалась от А» существует как ДВА разных факта: ратифицированное ребро журнала
(автор, дата, пояснение, отзыв) и непроверенная пометка `meta` (ничего из этого, вплоть до
не-идентификатора в значении); задача — сделать различие видимым, не объединять. (2) Формы (a),
(b), (e), (f) отклонены по замерам предложения; (3) форма (h) — составной читатель с пометкой
источника и видимыми противоречиями — ВЫБРАНА, но строится с первым названным потребителем:
назначенный картой опыт вернул ноль спроса (`distinct_from` — 0 из 68 живых рёбер здесь и 0 из 105
в autosorter), а прецедент CB-44/CB-45, на который карта опирается, запрещает строить шов без
потребителя. Первый же юнит, касающийся `grouping`/`relations`, вправе взять вопрос потребителя
частью себя. (4) Форма (g) — закрыть запись ключа — отложена с причиной: autosorter живо пишет эти
ключи (65 рядов `parent`), закрытие записи — межтрекерное изменение поведения, требующее слова по
обоим трекерам; вернуться при первом новом ряде `meta`-родословной на codebugs. Карту CB-163
закрывает держатель по К-1 с этой записью. Побочно ратифицированы: CB-274 (устаревшее «20x» в
несущем комментарии `grouping.py`) — порог попутный; очередь DIR-2 пересобрана по ИЗМЕРЕННОМУ
вреду — CB-167 первой (тихий пустой ответ `query(meta_key=...)` с точкой в ключе на боевом
трекере), затем CB-200 (сырой трейсбек с exit 0 — возврат нуля при ошибке ввода). Для DIR-1 в
артефакте DIR-2 лежит рабочая форма сторожа финишей по тождеству процесса (третий измеренный
обманщик — живой финиш autosorter; строковые формы исчерпаны). Против держательницы — четыре
записи в её артефакте; её методический вывод принят в реестр: **перемерить ответ, унаследовав
вопрос, значит оставить непроверенным ровно то, что решает, какие формы попадают на стол.**

**ПРИЁМКА (1)←(2), неизменяемые факты: СМЕНА 20 DIR-1 ПЕРЕДАНА ШТАТНО на `037a429` — 2026-08-31.**
Спот-чек куратора: `main == origin/main == 037a429`; промпт пересобран для смены 21; живое дерево —
только Т-129. Приняты Т-125 (CB-266, мерж `327590d`) и Т-126 (частичное сжатие, мерж `2eceaec`);
**алярм `main-invariants` восстановлен и проверен держателем на настоящем сервере: 911 коммитов
первой линии, ноль нарушений, базлайн не тронут.** По задаче владельца: **калибровка Т-129
подтвердила новую гипотезу приземлением, не оценкой** — три блока переписаны и посажены,
коэффициенты 1,35×/2,01×/1,57× поданы врозь с объяснением разброса; целевой размер ~150 КБ выведен
вторым независимым путём (33,2 % знаков — кандидаты архива) и сходится с первым в пределах 5 %;
решающее — блок, уже сжатый Т-126 по СТАРОМУ критерию на 7,5 %, дал 1,57× по новому: потолок
«5–6 %» делал критерий, не текст. Честный предел назван: 1,5×, не 3× — 67 % знаков документа
составляют правила, границы и указатели, которые остаются. Полный проход идёт (дерево
`docs/t129-claude-md-rules-layer`). **Факт, поправляющий число куратора: впрысков `CLAUDE.md` —
913, и субагенты получают его тоже** — установлено прямым опытом (агент без единого вызова
инструментов процитировал первую строку дословно); моя оценка «порядка двух тысяч запусков» была
неверна в обе стороны, счёт запусков не равен счёту впрысков. Против держателя — десять записей в
его артефакте, включая выпавший при переписи класс `HISTORY-LOADBEARING` (117 утверждений —
ровно то, ради чего затеян раздел; восстановлен и приземлён) и поправку собственного счёта ложных
нулей ВНИЗ (три, не четыре — чужая ошибка в своей исповеди раздувает и исповедь). Пять карт
заведено за смену.

**В КАНОН (К-31): ВОЗВРАТ ЮНИТА ЖИВЁТ В ФАЙЛЕ НА ВЕТКЕ; СООБЩЕНИЕ — ТОЛЬКО УКАЗАТЕЛЬ. Вменяется
каждому юниту при выдаче, обоими направлениями — 2026-08-31.** Основание: восемь случаев мёртвого
канала «исполнитель → держатель» за двое суток, каждый доставлен вручную через куратора; и
СТРУКТУРНАЯ ГИПОТЕЗА держателя DIR-1 смены 21, объясняющая все восемь одной причиной (записана как
гипотеза с его же оговоркой — посылкой не проверял, потому что несвоевременное сообщение дороже
подтверждённой догадки): подчинённый агент умеет адресовать свою РОДИТЕЛЬСКУЮ сессию, а ротация
держателя сиротит все его летящие юниты — значит любой юнит, переживший ротацию выдавшей смены,
без файлового канала нем. Предписание §6 брифа Т-129 («дублировать возврат записью в файл на
ветке») уже окупилось дважды и поднимается из брифа в канон. Для батча владельца остаётся вопрос
механизма (переадресация возвратов при ротации), но канон закрывает живой вред без него.
**Протокол окна Т-129 согласован заранее** (предупреждение DIR-1 за час, числом: уязвимый участок
финиша ~3 мин прогона против интервала коммитов соседа 3–4 мин; отказ 11 от незакоммиченной чужой
правки повтором не лечится — соседу нужно молчание И чистое дерево): окно — от слова держателя на
финиш, не от готовности исполнителя (между ними его собственная проверка возврата, дольше самого
финиша); горизонт 15 минут с закрытием по событию; закрытие — SHA + `merge-base --is-ancestor`,
никогда текстом журнала.

**ПОПРАВКА К ЦЕЛИ Т-129 РАТИФИЦИРОВАНА; ЗАПИСЬ ПРОТИВ КУРАТОРА — 2026-08-31.** Цель 1,50×
(~153 864 байта) считалась так, будто класс `HISTORY-LOADBEARING` (34 254 знака, сорок процентов
кандидатской массы) подвижен наравне с замерами и хроникой, — а это по определению класс, чьё
правило живёт внутри своей истории, и §3 п.2 брифа запрещает увозить его в архив. Пересчёт
держателя по первичному свидетельству: без этого класса — 1,25× (184 436); достигнуто на ветке —
1,21× (189 745), нетронутыми стоят четыре маленьких раздела на 1,5 % файла; разрыв объяснён
арифметикой, прочтение «исполнитель недостарался» ею не поддерживается. Оба уровня ратификации над
исполнителем (держатель и куратор) проверяли арифметику и ни один — предпосылку подвижности
классов: проверка элементов не проверяет композиции, на этот раз через ратификацию уровня (1).
**Решение: 1,21× — ожидаемый исход при действующем §3 п.2; приёмка Т-129 — по существу (ведомость,
сохранность правил, сквозной перечит), не по расстоянию до числа с неверной предпосылкой**
(прецедент Т-126). Вопрос «жать ли дальше к ~150 КБ ценой ослабления §3 п.2» — владельцу отдельным
решением с названной ценой (риск переобещания правил — основной дефект, который направление
ведёт); рекомендация куратора — не жать: долгосрочный выигрыш даёт правило вперёд «новые
обоснования пишутся сразу в архив, слой правил получает правило и указатель», предложенное в
возврат Т-129.

**ПРАВИЛО ВПЕРЁД ДЛЯ `CLAUDE.md` — ФОРМУЛИРОВКА ДЕРЖАТЕЛЯ DIR-1 ЗАМЕНЯЕТ КУРАТОРСКУЮ; И
ОТРИЦАТЕЛЬНЫЙ РЕЗУЛЬТАТ ОБ ОРАКУЛЕ — 2026-08-31.** Кураторское «новые обоснования пишутся сразу в
архив» в буквальном прочтении увозило бы в архив ГРАНИЦЫ правил («чего этот гейт не ловит») —
граница рождается вместе с обоснованием и на глаз от него неотличима. Ратифицированная форма: **в
архив уходит ПОЧЕМУ (раунды ревю, воспроизведения инцидентов, отвергнутые формы, замеры); при
правиле остаётся ЧТО и ГДЕ НЕ РАБОТАЕТ — правило само называет свою границу.** Проводится
отдельным крошечным юнитом сразу после посадки Т-129 (план по умолчанию; канала к летящему
исполнителю нет — К-31, а посторонний файл в его дереве отбил бы финиш кодом 4), если исполнитель
не написал правило сам. **Отрицательный результат, проверенный попыткой, а не объявленный:**
оракула «ни одно правило не потеряно» не существует — приближённый оракул держателя (отпечатки 976
утверждений классов «правило»/«граница» против объединения слоя и архива) дал 111 кандидатов в
потерю, шесть проверенных подряд ВСЕ оказались на месте: ложные срабатывания порождает сама
операция снятия биографии изнутри предложения, несущего и правило, и историю. Следствие для
приёмки Т-129, обязательное обоим проходам К-28: НЕ строить такую проверку; требовать носители по
§5 брифа — ведомость снятого, сквозной перечит как одного текста, отдельную проверку границ.

**ДВА СЛОВА ВЛАДЕЛЬЦА, МЕНЯЮЩИЕ ЦЕЛЬ И ОСЬ ПРИОРИТЕТОВ (2026-08-31, после возврата Т-129).**
Первое — о ФОРМЕ: «CLAUDE.md должен содержать правила, направляющие… посмотри на
../autosorter/CLAUDE.md — почему не сделать так же?» Целевая форма корня — директивы и указатели
(~15–30 КБ по образцу: жёсткие гейты, жизненный цикл, команды; счётчики в корне запрещены —
«число живёт в тесте»); глубина — в docs/ и во вложенных CLAUDE.md подсистем, которые харнесс
грузит по требованию. **Запись против куратора: предпосылку «все правила живут в одном
впрыскиваемом файле» я ратифицировал дважды** (в цели Т-129 и в поправке к ней) — «неразнимаемые
места» разнимаются формой: правило+граница+история переезжают целиком в справку своего домена,
§3 п.2 соблюдён. План: Т-129 садится как есть (работа банкуется), следом юнит «корень-директива»
по образцу; тройку собирает держатель DIR-1 от слова владельца, калибровка не нужна — образец
существует и работает. Вопрос «жать ли к 150 000» снят: владелец ответил формой, а не числом.
Второе — об ОСИ: «я вижу кучу наследия прошлого… нам нужно дальше развивать проект, мы не музей
строим». В модель приоритетов уровня (1): **развитие продукта весит выше курирования собственного
прошлого**; архив — кладовка для находимости, не экспозиция (не курируется, перекрёстные системы
ссылок не строятся); карты о самодокументации процесса получают минимальный вес против карт,
двигающих продукт, и при сборке пакетов отложенное по этой оси называется вслух. Обе очереди
направлений пересматриваются по ней после посадки Т-129; недельный обзор владельца получает эту
ось как предварительный вердикт («слишком много музея»).

**ОБРАЗЕЦ ИЗМЕРЕН ДЕРЖАТЕЛЕМ DIR-1, ПОСТАНОВКА Т-1XX УТОЧНЕНА ЧЕТЫРЬМЯ РАТИФИКАЦИЯМИ —
2026-08-31.** Замеры: корень autosorter — 26 291 байт (ориентир корня ~26 КБ, «15–30» — верхняя
граница); вложенных CLAUDE.md у образца сто, общим весом 520 196 байт — доктрины в 2,4 раза
БОЛЬШЕ нашей, поэтому **задача Т-1XX — раскладка по месту загрузки, не сокращение**: выигрыш
целиком в «что грузится когда». Ратифицировано: (1) первая работа и точка остановки Т-1XX —
построенная, а не вычитанная предпосылка «вложенный CLAUDE.md грузится только при работе в своём
каталоге» (метод Т-129: метка + субагент; если «всегда» — юнит останавливается с этим замером);
(2) механический потолок размера — ТРЕБОВАНИЕ: тест на корень и каждый вложенный файл, потому что
образец свой прозаический потолок «≤40 строк» перерос в 56 файлах из ста (крупнейший — 382
строки) — проза не обеспечивает прозу и в проекте-образце; (3) система указателей Т-129 остаётся
как есть и НЕ расширяется на новый материал — переезд домена одно движение, без обратных
указателей и кураторства (ось «не музей»); (4) очередь DIR-1 по новой оси: отложены пять карт
самодокументации процесса (CB-270, CB-271, CB-272 — вероятно растворится в Т-1XX, CB-273,
CB-275 — названо вслух; четыре из пяти — находки самого держателя этой смены), первыми после
Т-1XX — CB-187 (ливлок) и CB-264.

**Т-129 СЕЛ: мерж `f439989`, с первой попытки, окно 2:40 из запрошенных пятнадцати минут —
2026-08-31 12:50.** Спот-чек куратора: `f439989` — предок `origin/main`; `CLAUDE.md` на main —
189 742 байта, архив — пять файлов в `docs/claude-md-rationale/` (55 831 байт, в контекст не
впрыскивается); рабочее дерево одно (главный чекаут), ветка сохранена. Шестое окно тишины —
шестая посадка с первой попытки; держатель верно замечает, что «с первой попытки» дали не окно и
не удача, а предполётные проверки, снявшие четыре класса отказов заранее, и явный `--merge-msg`,
снявший пятый — тихую неправду автовывода темы в журнале main. Финиш выполнен держателем как
оператором при мёртвом исполнителе (К-31 с обратной стороны: слово, которого ждал исполнитель,
стало некому передать). В приёмку заранее заявлены строками: непрогнанный `/simplify-traced`
(содержательная половина выполнена и перемерена; повторная обработка сознательно опущена по оси
«не музей») и потерянный в двух реле квалификатор «ноль после нормализации переносов». Приёмка —
два прохода К-28 с разной апертурой; апертура второго назначена держателем вопросом «не стало ли
хоть одно правило слоя ШИРЕ оттого, что его граница уехала в архив» — §3 п.2 наизнанку, основной
дефект направления, применённый к продукту юнита. Попутно в окне: DIR-2 нашла дефект в
собственном приземлённом брифе CB-167 (дословный алгоритм ломается на ключе с завершающей
обратной косой; исправление прогнано перебором 512 имён, ревизия 3 ждала снятия окна) — её
формулировка принята в реестр: **точность формулировки не есть правильность; решает только
перебор.**

**Т-130 ОСТАНОВЛЕН ПО КОНТРАКТУ Р2 — ТОЧКА ОСТАНОВКИ СРАБОТАЛА ПО НАЗНАЧЕНИЮ; ВОСЬМАЯ ЛОВУШКА
СМЕНЫ 21 DIR-1, ПОПРАВЛЯЮЩАЯ КАНОН СТОРОЖА; РОТАЦИЯ DIR-1 СОБРАНА — 2026-08-31 вечер.**
Исполнитель Т-130 (CB-167) не стал чинить: сплошной перебор 546 имён на живом движке базы опроверг
несущее утверждение брифа «область отказа пуста» — невыразим ровно один класс (имена с нулевым
байтом U+0000), остальные 542 выражаются; отчёт `36f6059` на ветке, CB-276/277/278 заведены
рёбрами found_during → CB-167, на main не коммичено; решение о форме — у держательницы.
**Поправка канона сторожа чужого финиша (DIR-1, замером):** тождества процесса недостаточно —
строковый сторож обманули собственная командная строка И двухдневный сторож проекта autosorter,
сам опрашивающий список процессов по имени финиша (процесс, лишь НАЗЫВАЮЩИЙ финиш); работающий
различитель — три условия вместе: интерпретатор исполняет сам файл сценария + замок интеграции
свободен (он берётся лишь на последнем шаге, потому свободный замок не доказывает отсутствия
финиша в фазе прогона) + прогон набора в этом репозитории; причины — §6
`HANDOFF-DIR-1-2026-08-31-session21.md`; передано DIR-2, принято куратором для собственного
сторожа. **По просьбе держателя, отдельной строкой от прочих семи:** восьмая ложная находка его
прибора — иного класса: семь были ошибками ПОСТРОЕНИЯ, восьмая — ошибкой ЧТЕНИЯ ПРЕДПИСАНИЯ
(инструкция «не изобретать четвёртую строковую форму» лежала в его собственном стартовом промпте);
итог смены: восемь ложных находок прибора против нуля дефектов юнита. **Попутный экземпляр той же
семьи у куратора:** сторож базы отклонил коммит этой записи, прочтя имя движка с номером версии
внутри текста реестра как вызов клиента базы — гейт на написании, сработавший на передаваемых
данных (класс CB-227/CB-244); обойдено сменой инструмента записи (правка файла вместо heredoc в
оболочке), не пересборкой отклонённой команды. Ротация DIR-1: handoff `d4a5864`+, промпт
`4858fc8`, Т-131 заминчен, обрифован и НЕ выдан — решение преемника; открытым остаётся вердикт
второго прохода Т-129 (вменён преемнику §5 handoff'а, если не придёт до конца смены).

**ПРИЁМКА (1)←(2), неизменяемые факты: СМЕНА 21 DIR-1 ЗАКРЫТА ШТАТНО; Т-129 ПРИНЯТ ДВУМЯ
ПРОХОДАМИ — 2026-08-31 вечер.** Спот-чек куратора: `f439989` (мерж Т-129), `d31e1ef` (факт
приёмки), `1ec5dc4` (проверка границ) — предки `origin/main`; `main == origin/main == 4101d6e`;
промпт пересобран для смены 22. Вердикты: проход 1 — ПРИНЯТ С ОГОВОРКАМИ (обе об оснастке сессии
приёмщика: Edit/Write отключены — четвёртое срабатывание за сутки, норматив всё ждёт руки
владельца; PENDING п.5 закрыт записью `d31e1ef`); проход 2 (апертура «связность как одного
текста» + шов «биография→правило») — принят, четыре места починки, два несущих: фраза об
установщике хуков потеряла условность и лжёт против живого скрипта (единственная ложь из 29
проверенных пар), условие монотонности бутстрап-гейта стоит дважды. Отсюда **Т-132 (четыре
починки) поставлен ВПЕРЕДИ Т-131** по несущему доводу: форма Т-131 запрещает переписывать при
переносе — добросовестный исполнитель перенёс бы ложь нетронутой в корневую директиву. Оба юнита
заминчены, обрифованы, НЕ выданы — решение смены 22. Итог смены записан против держателя его же
рукой: ноль дефектов юнита против девяти ложных находок собственного прибора (в стартовом промпте
преемника стоит «семь» — расхождение счёта передано преемнику на сверку §0); его правило принято
в реестр: **находка есть гипотеза, пока её не воспроизвёл второй независимый способ** — и «если
число зависит от разреза, называй разрез вместе с числом или не называй числа».

**Т-132 СЕЛ: мерж `7aa9df2`, с первой попытки, седьмое окно тишины — седьмая посадка с первой
попытки; И ПОПРАВКА К ЗАПИСИ КУРАТОРА ОТ ЭТОГО ЖЕ ДНЯ — 2026-08-31 вечер.** Спот-чек: `7aa9df2` —
предок main; слой правил 189 742 → 190 441 (вернулась условность фразы об установщике, указатели в
обе стороны); механизм провала установщика лёг в архив; условие монотонности сведено в одно место;
src/ и tests/ не тронуты; набор 3350, посадка за одно окно. **Поправка отдельной строкой, существующая
запись не правится:** формулировка «единственная ложь из 29 пар» в записи о вердикте второго прохода
точнее звучит как «фраза ДВУСМЫСЛЕННА, и одно из двух прочтений измеримо ложно» — эскалация (а)
брифа Т-132 сработала: исполнитель собрал одноразовый репозиторий и прогнал установщик (код 1,
merge.ff выставлен, pre-commit вооружён), при охвате отрицанием всей связки фраза истинна. Уточнение
к П2 той же пробы: пропало ОПИСАНИЕ воспроизведённого отказа, не факт проверки. Честно заявлено
держателем и принято: /simplify-traced в его сессии не прогонялся (существо покрыто исполнителем по
§5) — видно приёмке как есть. Дальше у DIR-1: §9, приёмка Т-132, выдача Т-131; бриф Т-133 (CB-187,
форма (a)+(b)) готовится — найдена несущая ловушка: замок по `$1` дал бы два inode на два законных
написания имени, гейт выглядел бы установленным и не работал.

**Т-132 ПРИНЯТ С ОГОВОРКАМИ; ПРЕДПОСЫЛКА ЗАДАЧИ ВЛАДЕЛЬЦА ДЕРЖИТСЯ; ФОРМА Т-131 ИСПРАВЛЕНА
ЗАМЕРОМ ИСПОЛНИТЕЛЯ — 2026-08-31 ночь.** Вердикт приёмки Т-132: девять пунктов, оговорка п.2
объявлена самим брифом; непроверенный лейн п.6 закрыт держателем ПОСЛЕ вердикта пушем —
`main == origin/main == 90102c5`, алярм и ci зелены на сервере (просьба куратора о пуше снята);
двусмысленность фразы приёмщик подтвердил СВОИМ опытом, заморозка S608 проверена третьим способом
с названным разрезом. **CB-279, найдена приёмщиком и не названа ни брифом, ни держателем:** оракул
множеств сообщает только о ПРОПАЖАХ — токен, родившийся сломанным (код-спан, разорванный переводом
строки), невидим ему по построению; юнит внёс такой разрыв и прошёл зелёный оракул, притом что
одним файлом раньше сам же поймал и починил такой же. Прибор слеп ровно к порче, которую вносит
правка, — дорого накануне Т-131. **П1 Т-131 закрыта, премиса держится: вложенный CLAUDE.md
грузится ПО ТРЕБОВАНИЮ** — два независимых прибора (поведенческий с метками и учёт токенов:
34 808 из корня против 144 921 из подкаталога, разница — ровно вес раздутого файла); решающий —
токен-учёт, не зависящий от самоотчёта модели; названы и приборы-обманщики (strace показывает
открытие БЕЗ вкладывания — открытие есть обнаружение). **Исправление ратифицированной формы,
принято куратором:** `tools/CLAUDE.md` из плана убран — харнес свои скрипты ЗАПУСКАЕТ, а не
читает, файл не грузился бы почти никогда («гейт, который не может сработать», применённый к
месту хранения правила); харнесная доктрина остаётся в корне; `src/codebugs/` и `tests/` остаются
— их предмет читают. Буква формы правлена ради смысла, владельцу сказано. Счёт ложных находок
смены 21: по названному разрезу — восемь; «девять» из закрывающего блока куратора не расписано по
предметам и не складывается до названного разреза (правило «число — с разрезом» применено к
записи самого куратора).

**Т-130 СЕЛ: мерж `99fdaaa`, с первой попытки — восьмое окно тишины, восьмая посадка с первой
попытки — 2026-08-31 ночь.** Спот-чек куратора: `99fdaaa` и вершина ветки `fbc6421` — предки
main; на main `_meta_key_rows` (единая дефиниция, семь употреблений), `_META_PATH_METACHARS`
снята. CB-167 закрыта формой «ключ — связанное значение, не часть пути»: плохое состояние
непредставимо вместо дисциплины экранирования; попутно закрыт крах запроса на испорченной мете и
третий механизм тихой пустоты уведён картой-наследником. Посадке предшествовали: слово
держательницы только после перегона набора на точном садящемся SHA («замер, устаревший на правку,
— не замер»), протокол «11 — стой и докладывай, 13/1 — повтори с тем же сообщением», и
однострочная поправка §4.7 намеренно придержана до-посадки/до-приёмки, чтобы не двигать main под
финишем и не дать приёмщику ложную находку по устаревшей строке. Исполнитель намеренно оставил
закрытие всех трёх карт (CB-167, CB-277, CB-278) держательнице по К-1. **Генерализация
держательницы принята в реестр:** ответ на «снова спутала слои» — не «быть внимательнее», а «где
прибор, спрашивающий снизу?» — записи против себя переопределяются из свойства характера в
отсутствующий механизм в конкретном месте. **Фаза П2 Т-131 (не двигая текста):** директив 16,7 %
байтов, предсказанный корень ≈31 750 при ориентире образца 26 291 — достижимо, впрыск сожмётся
~вшестеро; Claims module удерживает 46 % (коды возврата — договор оболочки); развилка адреса для
43 210 байт глубины Workflow — решение держателя DIR-1; поправка чисел брифа (вложенных у образца
76/463 670, не 100/520 196 — в сотне копии из dist/).

**ПРИЁМКА (1)←(2), неизменяемые факты: ПАКЕТ СМЕНЫ 20 DIR-2 ЗАКРЫТ — CB-167 ЗАКРЫТА ЮНИТОМ Т-130,
принятым в два независимых прохода — 2026-09-01.** Спот-чек куратора: мерж `99fdaaa` — предок
origin/main (проверен при посадке). Вред снят на боевом трекере через код чекаута: запросы по
ключам с точкой возвращают свои карты вместо нуля; необъявленный обход вложенности `loc.skipped` —
ноль; форма — «имя ключа как связанное значение» — делает плохое состояние невыразимым (карта и
бриф предписывали иное; довод: экранирование — дисциплина, выведенная в этом брифе трижды и
трижды неверно). Карты: CB-167, CB-277, CB-278 закрыты рукой держательницы по К-1; CB-163 —
Решением 5; CB-276 открыта намеренно (текст цитирует исчезнувший код, подлежит переписыванию);
CB-280, CB-281 заведены находками второго прохода. Довод за К-28 замером: первый проход спрашивал
«сделано ли обещанное» — «да»; второй — «не лжёт ли теперь дерево» — десять мест, которых первый
не искал. **Поправка к записи куратора от прошлой ночи:** «семь употреблений `_meta_key_rows`» —
семь текстовых вхождений имени; ВЫЗОВОВ четыре (оба прохода нашли независимо; правило «число — с
разрезом» применено к кураторской записи второй раз за сутки). **ОСТАТОК СРЕДЫ, ТРЕБУЕТ РУКИ
ВЛАДЕЛЬЦА:** работающий сервер MCP поднят из устаревшего снимка pipx, не из чекаута — один запрос
по одному трекеру отвечает `total: 0` через MCP и одной строкой через чекаут (проверено обоими
путями); всякий, кто проверяет ЛЮБУЮ починку через MCP до переустановки снимка, получает ложный
отрицательный ответ. Это CB-263 в живом виде на главном инструменте владельца; команда
переустановки передана владельцу.

**Т-127 СЕЛ: мерж `4e711f1`, с первой попытки — девятое окно, девятая посадка с первой попытки —
2026-09-01.** Спот-чек куратора пройден. CB-264 закрывается: держатель заводил её как ложное
утверждение CLAUDE.md, занижающее объём работы вдвое; исполнитель ПЕРЕСТРОИЛ находку приёмщика с
нуля (52 места, 30 выражений, каждое прослежено до присваивания — теперь свой замер, не чужой),
нашёл третий образец механизма, неизвестный ни карте, ни брифу (`reqs.py:392` — перечисление
локальным кортежем), и поставил число с разрезом и контрпримером в одном предложении (56 при
extend-select против 93 при select; несущее — маркер мёртв ОТНОСИТЕЛЬНО включённого набора).
Две находки отданы картами, не починены по дороге: `if group_by:` (семейство CB-25, продуктовый
класс — вперёд по оси владельца) и CB-282 (ложный абсолют «Never interpolate» — текстовая).
`CLAUDE.md` после посадки — 195 123 байта (+4 682 при диффе «одна строка»: строка есть
абзац-пункт, и правдивая формулировка длиннее лжи, которую заменила) — исполнителю Т-131 велено
перемерить базу перед постановкой потолка, она двигалась за смену четырежды. Непрогнанный
/simplify-traced в сессии держателя заявлен честно второй раз — виден приёмке как есть.

**ПРИЁМКА (1)←(2), неизменяемые факты: СМЕНА 20 DIR-2 ЗАКРЫТА ШТАТНОЙ РОТАЦИЕЙ — 2026-09-01.**
Спот-чек куратора: `da7474d` (ротация) на локальном main, отправлена куратором на origin вместе с
хвостом; `99fdaaa` (Т-130) и `d8b1ef8` — предки origin/main; чекаут чист; промпт пересобран под
смену 21 ДО закрытия смены — расхождение «промпт позади handoff'а», дважды пойманное реестром,
здесь закрыто до проверки. Набор на main перемерян при записи передачи: 3358, линтер чист (алярм
CB-215 в прогоне сработал по её же вине и объяснён честно — писала файлы ротации во время прогона,
набор их не читает). Главное передачи: MCP-остаток первым пунктом обоих файлов; CB-280 подписана
«долг, созданный юнитом прошлой смены — не наследовать как чужой»; записи против себя оформлены
указателями на недостающие механизмы («где прибор, спрашивающий снизу» — с честным послесловием,
что прибор был поставлен не по прозорливости, а после двух собственных ошибок в предписании).
**Уточнение К-30 (второй случай за смену, предложено DIR-1):** грязь в главном чекауте опасна и
ВНЕ окон — «пишу у себя в плановых заметках» не безопасное действие, пока заметка отслеживаемая;
окна требуют двух вещей (не коммитить И держать дерево чистым), и у кода 11 два пути, второй
невидим merge-tree. Смена 21 DIR-2 запускается из пересобранного промпта.

**Поправка ко времени, не к итогу (запись «СМЕНА 20 DIR-2 ЗАКРЫТА…» выше): тревога DIR-1 о грязном
промпте НЕ была ложной.** Оба замера верны каждый в свою секунду: в момент его проверки файл
действительно стоял `M` (изменённый отслеживаемый — второй путь к коду 11), к моменту её проверки
был уже закоммичен в `da7474d`. Состояние было временным и законным; расхождение сверять по
времени, а не по итогу. Поднято самим DIR-1 с формулировкой «не хочу ни присвоить чужую находку,
ни принять на себя ту, которой не было» — принято дословно.

**В КАНОН (К-32), ДОПОЛНЕНИЕ К К-31 ПО ИЗМЕРЕННОМУ СЛУЧАЮ: ПЕРЕД ПОСАДКОЙ ОСИРОТЕВШЕЙ ВЕТКИ
ЧИТАЕТСЯ НЕЗАКОММИЧЕННЫЙ ДИФФ ДЕРЕВА, А НЕ ТОЛЬКО ФАЙЛ ВОЗВРАТА — 2026-09-01, CB-284 (high).**
Основание: исполнитель Т-133 оборвался ПОСРЕДИ мутационной пробы, и его дерево несло правильную
починку (`079bc64`) плюс одну незакоммиченную строку МУТАНТА — буквально ловушку 1 брифа, возврат
о ней сказать не мог (не был написан). `worktree-finish.sh` первым делом коммитит грязное
состояние: посадка вслепую — К-31 её прямо разрешает — приземлила бы мутанта вместе с починкой
одним мержем, проверки прошли бы (тест на этот случай исполнитель дописать не успел), CB-187
закрылась бы, и в таблицу принуждения легла бы строка про гейт, который выглядит установленным и
не работает, — класс «гейт, который не может сработать», приземлённый внутри починки против этого
же класса. Спасла привычка, не чутьё: держатель прочёл незакоммиченный дифф (одна строка — и она
мутант). Мутант сохранён патчем вне дерева, снят возвратом файла с предметной проверкой «снят
мутант, не починка», уцелевшие 278 строк тестов закоммичены wip'ом с описанием (`2c48577`).
Держатель честно называет канон дисциплиной в точке применения (неверный слой принуждения по
CB-41) — механические формы лежат в CB-284, его кандидат: отказывать вместо автокоммита, когда
сообщение не передано явно, с названной ценой; выбор формы — за направлением, приоритет CB-284 —
high по живому вреду классу посадок.

**Т-133 СЕЛ: мерж `100ab6c`, с первой попытки — десятое окно, десятая посадка с первой попытки;
ЗАМОК С ПЕРЕНОСА ТЕКСТА CLAUDE.md СНЯТ — 2026-09-01.** Спот-чек куратора: `100ab6c` — предок main;
на main стоит ПРАВИЛЬНАЯ строка замка (имя из разрешённого пути через `_finish_lock_name`, не из
сырого аргумента) — мутант мёртвой сессии не пережил ни снятия, ни мержа; коды 16/17 на месте.
CB-187 (ливлок посадок — форма (a) обоих направлений: неблокирующий замок на ветку поверх замка
интеграции) закрывается держателем. Юнит доведён держателем по К-31+К-32 за оборвавшегося
исполнителя, включая починку ложного красного: проза, назвавшая литерал кода («exec 9>» в
комментарии), поймалась подстрочным якорем двух старых тестов — правлен ТЕСТ, а не запрещена
проза (точность живёт в якоре), доказано мутацией в обе стороны; соседняя строка того же теста
уже была уточнена по этой же причине — проверка элементов не проверяет композиции на материале
самого теста. Вердикт приёмки Т-127: ПРИНЯТ — ни одного дефекта сверх названных, все числа
воспроизведены (проба усилена до четырёх написаний), перестроенная находка подтверждена машинно по
всем 52 местам; приёмщик поймал СЕБЯ правилом «ноль не замер» (его шаблон не ждал формата
`error[RUF100]` под --preview) — третий случай за смену. Честный остаток записан: ~шесть чисел
починенного абзаца не держит ничего механического, лейн открыт (52 уже стало 53). Непрогнанный
/simplify-traced заявлен держателем третий раз, по Т-133 с усилением («часть работы делал я сам»)
— приёмке видно как есть. Смена 22 DIR-1: три юнита приземлены (Т-132, Т-127, Т-133), один в
работе (Т-131 — замок снят, переезд пошёл); нетронутым остался только хвост, отложенный осью.

**СЛОВО ВЛАДЕЛЬЦА: ВЫПУСК ВЕРСИИ РАТИФИЦИРОВАН («начни с установки новой версии», 2026-09-01) —
ВЫПУСК 0.3.0 ОТДАН DIR-2 СМЕНЕ 21 ВНЕ ОЧЕРЕДИ; И РОТАЦИЯ КУРАТОРА.** Номер 0.3.0, не 0.2.3:
несколько починок намеренно ломают старое поведение (перебивание --limit, отказы на
противоречивых входах) — вторая цифра обязана это сказать; вторая половина замысла — версия
снимка pipx становится отличима от чекаута (закрытие различимостью ловушки CB-263 «оба говорят
0.2.2»). Тройка: по разделу Releasing (bump в двух местах под test_release_version, CHANGELOG
[0.3.0] с человеческим абзацем, юнит через worktree, тег v0.3.0 на МЕРЖ из главного чекаута
после посадки); критерий включает передачу владельцу строки `pipx reinstall codebugs`.
**Норматив приёмщика (всё ждёт руки владельца) ПЕРЕЕХАЛ в переживающее сессию место:**
`/home/faxik/workspace-private/codebugs/acceptor-norm-pending/{acceptor.md,SKILL.md}` —
scratchpad сессии куратора умирает с ней; команда применения теперь:
`cp /home/faxik/workspace-private/codebugs/acceptor-norm-pending/acceptor.md ~/.claude/agents/acceptor.md && cp /home/faxik/workspace-private/codebugs/acceptor-norm-pending/SKILL.md ~/.claude/skills/acceptance-check/SKILL.md`.
Срабатываний «инструмент отключён у приёмщика» за двое суток — четыре. Ротация куратора — по
слову владельца, штатно: handoff `HANDOFF-curator-2026-09-01.md`, преемник стартует с ведения
выпуска 0.3.0 и посадки Т-131.
