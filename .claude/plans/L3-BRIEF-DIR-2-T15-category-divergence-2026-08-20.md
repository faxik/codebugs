# L3-BRIEF Т-15 — сигнал `category_divergence` (BT-5)

Хендофф DIR-2 (уровень 2) менеджеру задачи (уровень 3). 2026-08-20. Спека ролей:
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`.
Дизайн: `.claude/plans/BT-5-dedup-attention-block.md` v2, §«Условное дополнение» —
**владелец ратифицировал «да» 2026-08-20**. **Диспетчеризуется ТОЛЬКО после приёмки Т-14**
(ядро attention) — сигнал живёт внутри уже существующего блока.
Пререк выполнен: Т-10 посажен (`0671881`), ring несёт `category`.

## Тройка П-Б

**Замысел.** Категорийное расхождение наблюдения и карты — записанный факт, зарытый на три
уровня внутри ринга, который филеры доказуемо не читают. Сигнал его ИМЕНУЕТ top-level.
Обоснование честно слабее, чем у ядра (после Т-10 факт выводим из двух полей одного
ответа) — и это записано в дизайне, не скрыто.

**Задание (ратифицированная форма):**
1. Запись `{"signal": "category_divergence", "observed": ..., "stored": ...}` в тот же
   `attention`-список; допустима на ветках, где есть сматченная строка (матрица
   деривируется из branch-таблицы, как в Т-14; recurrence — против близнеца).
2. **Сравнение — normalize-обе-стороны** (`normalize_category` на наблюдённой И хранимой):
   расхождение НАПИСАНИЙ — не сигнал, расхождение ИМЁН — сигнал.
3. **Non-string хранимое значение — skip, не raise** (BLOB переживает affinity SQLite и
   `normalize_category` на нём падает): политика та же, что у `_existing_categories` —
   одна легаси-строка не должна ломать каждый будущий add.
4. **Provenance-булево снимается на входе наблюдения**, если понадобится: после fallback-
   деривации `fingerprint` non-None в обоих случаях, так что тест `fingerprint is not None`
   МИСКЛАССИФИЦИРУЕТ. Живая экспозиция расхождения — restore-строки и explicit-id строки.
5. Популяция ограничена гейтом CB-60 (расхождение достижимо между категориями, которые
   гейт пропустил) — назвать в тестах, не в прозе.
6. Матрица и порядок сигналов расширяются (два сигнала могут со-фирить на одном бампе):
   детерминированный порядок, каждый — не более раза; totality-тест из Т-14 расширяется.

**Критерий приёмки:** наблюдение с другой КАТЕГОРИЕЙ по supplied fingerprint в живую карту
даёт ровно одну запись `category_divergence` с нормализованными обеими сторонами (тест,
красный до правки); расхождение только НАПИСАНИЙ сигнала НЕ даёт (тест); non-string
хранимое — skip без исключения (тест); со-фаер с `severity_escalated` — обе записи,
детерминированный порядок (тест); golden/CHANGELOG по факту изменения описаний;
сюита зелёная.

## Попутный груз этого юнита (из эскалаций Т-12 — CLAUDE.md нельзя коммитить на main напрямую)

7. **Две фактические правки CLAUDE.md**, которые Т-12 намеренно не делал во избежание
   конфликта в тех же абзацах: (а) правило CB-43 п.6 говорит «re-keying must renegotiate
   the index — **future card**» — ре-негоциация СОСТОЯЛАСЬ (юнит Т-12, мердж `8eff064`:
   `findings.normalize_categories` / MCP `categories_normalize` / CLI
   `categories-normalize`, коллизии report-and-stop), точку входа назвать; (б) абзац CB-60
   «Stored rows are never rewritten: … **CB-61's retro-fold gap, deliberately left open**»
   — гэп закрыт тем же юнитом, формулировку поправить (упомянув, что на боевом трекере
   fold — отдельное решение владельца, а не следствие посадки).
8. **`--merge-msg` ОБЯЗАТЕЛЕН при финише** (урок Т-12): деривация субъекта мерджа берёт
   ПЕРВЫЙ коммит первопарентной линии ветки, а при TDD это «red tests» — без явного
   сообщения мердж-коммит называет тест-коммит вместо фичи.

Карт у юнита нет (CB-113 многоюнитная у DIR-2; её путь (a) — за Т-12, не за этим юнитом) —
§13 п.7 удовлетворяется явным вердиктом. Ветка `fix/bt5-category-divergence`. Файлы:
`findings.py`, тесты, golden/CHANGELOG при изменении описаний; НЕ файлы DIR-1.
Worktree-харнесс, исполнитель — свежий субагент (П-В), TDD, `uv run --extra dev`,
ruff 0.15.7; юнит-бриф в worktree на ветке; на main напрямую не коммитить; факт своей
приёмки — в юнит-бриф, не в документ направления. Возврат — тройка с явным слотом
эскалаций; мутационная проба руками.