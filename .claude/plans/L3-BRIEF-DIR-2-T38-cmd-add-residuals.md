# L3-БРИФ DIR-2 / Т-38 — CB-132 + CB-133: два остатка Т-24 в `_cmd_add`

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2. **Модель:** opus; объём — sonnet/haiku.
**Полоса:** механика-филлер, параллельна юнитам BT-7.

## 1. Задание — две карточки, один хендлер, РАЗНЫЕ механизмы

Обе живут в `_cmd_add` (`src/codebugs/findings.py`, CLI-регистрация). Обе — остатки
приёмки Т-24 (CB-129), заведены мной по слоту эскалаций, а не найдены заново.

**CB-132 (medium, `error_handling`).** `--meta` с не-JSON или не-объектом вылетает СЫРЫМ
traceback'ом: `json.loads(args.meta)` без арма над ним. `codebugs add … --meta 'не json'`
даёт `json.JSONDecodeError`, `--meta '[1,2]'` — `TypeError: cannot convert dictionary update
sequence element #0`. CLAUDE.md называет этот класс прямо: «хендлер, который не ловит
ничего, нарушает правило ровно так же, как ловящий в неверном порядке» (урок CB-19/CB-79).
Направление фикса: арм `json.JSONDecodeError` с текстом, называющим негодное значение, плюс
явный отказ, когда распарсенный payload не объект.
**Ограничение, которое легко нарушить:** порядок армов `json.JSONDecodeError` ПЕРЕД
`ValueError` — действующий контракт, пиненный `tests/test_findings.py::TestRetriageCliContract`.
Не потревожь его.

**CB-133 (low, `silent_default`).** `-l ""` по-прежнему молча отбрасывается: засев meta
идёт через `if value:` по `_ADD_META_FLAGS`, то есть по TRUTHINESS. Явно набранная пустая
строка не приземляется, конфликтом не считается, вызов рапортует успех. **Это НЕ регрессия**
(до Т-24 было `if args.lines:` — то же самое), это инстанс уже дважды ратифицированного
класса: CB-25 («нет фильтра» = None и пустая строка, никогда truthiness) для чтения и
**CB-82** («на пути ЗАПИСИ None — единственное „не передано“») для записи. Флаг CLI — путь
записи, значит применяется CB-82.
**Суб-решение, которое ты обязан принять и НАЗВАТЬ:** `-l ""` должен отказывать или
сохранять пустую строку? CB-82-консистентный ответ — отказывать. Если код покажет иное —
доложи, не исполняй молча.

**Предпосылки (спот-чекни; расхождение = СТОП и доклад):** грепаемые якоря —
`_ADD_META_FLAGS` и охраняющий его `if value:`; `meta.update(json_meta)` внутри `_cmd_add`.
Т-24 уже перенёс разбор `--meta` ДО `db.connect()`, поэтому соединение на этом пути больше
не течёт — то есть CB-132 сегодня «не хуже», и юнит правит ФОРМУ ОТКАЗА, а не утечку.

## 2. Границы — узкие и обязательные

- **Только CLI.** MCP-обёртки НЕ трогаются, `tests/golden/mcp_schema.json` НЕ регенерируется
  и не должен измениться ни на байт. Если golden сдвинулся — ты вышел за скоуп, откатывай.
  (Причина: golden сейчас занят пилотом BT-6 направления DIR-1 на `bench.py`.)
- **НЕ ТРОГАЙ**: `tools/`, `bench.py`, `types.py`, `CLAUDE.md`, `provenance.py`
  (там параллельно идёт юнит Т-37 того же направления).
- Родственная карта **CB-131** (прецеденция `meta_update` не объявлена в описаниях MCP-тул)
  в этот юнит НЕ входит: она двигает golden и ждёт освобождения. Не прихватывай её.

## 3. Тесты

- **Поведенческие, через `cli.main` в процессе** (прецеденты: `tests/test_fsio.py`,
  `tests/test_findings.py`), а не только на хелпере: оба дефекта живут в склейке парсера и
  хендлера.
- Минимум: не-JSON `--meta`; `--meta` с массивом; `-l ""`; `-l ""` вместе с `--meta`,
  несущим `lines`; и контрольные случаи, которые ДОЛЖНЫ проходить (обычный `-l "10-20"`,
  обычный валидный `--meta`) — иначе набор односторонний.
- **Мутационная проба во ВРЕМЕННОМ worktree** (`git worktree add <tmp> <sha>` → проба →
  `remove --force`), НИКОГДА в рабочем дереве. Обязательные мутанты: снятие нового арма
  (должен вернуть traceback); возврат `if value:` вместо строгой проверки.
- Часть новых тестов обязана быть КРАСНОЙ до фикса — покажи это числом в факте приёмки.

## 4. CHANGELOG

`-l ""` начинает отказывать — это видимое пользователю изменение, строка в нерелизнутой
секции, написанная для того, кто вызывает CLI, а не для нас. CLAUDE.md правь ТОЛЬКО если
правило репозитория стало ложным.

## 5. Каноны (нарушение = провал приёмки §13)

1. **К-1. (3) на main НЕ коммитит.** Факт приёмки (3)←(4) — в ЭТОТ файл на ветке.
2. **К-2. Минт — только `tools/cascade-mint.sh`**, номер рукой не набирается.
3. **К-3. `git add -- <path>` поимённо**, никогда каталогом.
4. **К-4. `--merge-msg` обязателен.** Предлагаемый:
   `Merge fix/t38-cmd-add-residuals: a malformed --meta now refuses instead of printing a traceback, and an empty -l no longer vanishes (CB-132, CB-133)`
5. **Факт приёмки цитирует грепаемое содержимое, не номер строки.**
6. **Слот эскалаций исполнителя переносится СЮДА ДОСЛОВНО**; пусто — явно «ПУСТО».
7. **К-7. Предписание брифа — предпосылка**, добывается из кода.
8. **Э-4. Клеймит харнес.**

## 6. Харнес

- `tools/worktree-setup.sh fix/t38-cmd-add-residuals` — из КОРНЯ репо; имя несёт обе карты
  через текст, клеймы берёт харнес.
- Работа только в `.worktrees/fix-t38-cmd-add-residuals`.
- `uv run --extra dev python -m pytest tests/ -q`; `uv run --extra dev ruff check src/ tests/`;
  ruff **0.15.7**.
- Финиш: `tools/worktree-finish.sh fix-t38-cmd-add-residuals --merge-msg '…'` из корня.
- **ЗАПРЕЩЕНО**: `uv run --python`, `uv sync`, пересборка `.venv`. Эксперименты — `/tmp`.
- **Блокировка finish'а чужим staged-WIP:** НЕ стэшить чужое, НЕ ждать пробуждения —
  polling раз в ~60 с, затем повтор finish тем же `--merge-msg`.

## 7. Слот эскалаций менеджера (3) → держателю (2)
_(пусто — пишется явно «ПУСТО»)_

## 8. Слот эскалаций исполнителя (4) → менеджеру (3), ДОСЛОВНО
_(пусто — пишется явно «ПУСТО»)_

## 9. Факт приёмки (3)←(4)
_(пишет менеджер (3) здесь, на ветке)_
