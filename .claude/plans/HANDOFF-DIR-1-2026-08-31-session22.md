# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-31, смена 22

(вытесняет `HANDOFF-DIR-1-2026-08-31-session21.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

**Писался под замораживание** (куратор ушёл на ротацию, сессия держателя могла умереть вместе с
его). Поэтому он полон, но скоропортящееся в §5 могло уехать за минуты — перемеряй.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов**, называй число явно в каждом промпте. Пункт 9 ссылается на
  `_INSURANCE_PATHS`, которого здесь НЕТ (эквивалент — `ruff check src/ tests/` плюс полный набор);
  **пункт 7 умеет быть закрытым НЕВЫПОЛНЕНИЕМ** (у задач владельца карты нет вовсе); **пункт 5
  закрывается записью вызывающего ПОСЛЕ вердикта** — это не провал приёмщика.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md` — слой правил; обоснования в
  `docs/claude-md-rationale/` (пять файлов). **Числа размера здесь НЕ ПРИВОДЯТСЯ намеренно:** за одну
  смену 22 файл двигался шесть раз, и всякое записанное число протухало к следующему абзацу.
  Меряй сам и называй, на каком SHA.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §9 неизменяемые факты
  приёмки (ДВА блока «Смена 22» в конце), §10 эскалации, §11 журнал, §13 шаблон брифа.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. **Аллокатор ID:**
  `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| **Т-132** — четыре места слоя правил из вердикта второго прохода Т-129 | `7aa9df2` | **ПРИНЯТ С ОГОВОРКАМИ** |
| **Т-127** (карта CB-264) — описание механизма `# noqa: S608` приведено к правде в трёх местах | `4e711f1` | **ПРИНЯТ** (девять пунктов, дефектов сверх названных ноль) |
| **Т-133** (карта CB-187) — взаимное исключение двух финишей одной ветки | `100ab6c` | приземлён; **ПРИЁМКА НЕ ЗАПУСКАЛАСЬ — ЭТО ДОЛГ** |
| **Т-131** — задача владельца, слой директив | — | П1 и П2 закрыты, форма исправлена дважды, замок снят, **двигает текст** |

**Заведены карты:** CB-279 (оракул множеств слеп к токену, родившемуся сломанным), CB-282 (два
описания одного механизма разошлись после посекционной правки), CB-283 (`if group_by:` — молчаливый
несгруппированный ответ вместо отказа), **CB-284 (`high`** — финиш сам коммитит грязь, поэтому
мутант мёртвой сессии садится молча), **CB-285** (половина (b) починки CB-187 приземлилась без
единого теста). **Закрыты:** CB-264, CB-187.

**Ратифицирован канон К-32:** перед посадкой осиротевшей ветки читается НЕЗАКОММИЧЕННЫЙ ДИФФ, а не
только файл возврата.

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
   **Попадание в `main` проверяй `git merge-base --is-ancestor`, никогда текстом и никогда порядком
   коммитов.** При сравнении «до и после» приземлённой ветки базой бери ПЕРВОГО РОДИТЕЛЯ МЕРЖА:
   после посадки `merge-base` вернёт саму вершину ветки, и сравнение выродится в сравнение файла с
   собой.
2. **Т-131 — голова, он ЖИВОЙ и двигает текст.** Ветка `docs/t131-root-directive-layer`. Его возврат
   живёт в файле брифа на ветке (К-31). **Окно на его посадку проси у преемника куратора.**
   Перед посадкой — обязательно К-32 (прочти незакоммиченный дифф) и **проверь оракул §5 брифа**,
   а не только зелень набора: на этом я сегодня уже ошибся, см. CB-285.
3. **ДОЛГ ПРИЁМКИ: Т-133 приземлён и не принят.** Запускай девятью пунктами; у него есть
   **известная дыра — CB-285**, назови её приёмщику явно, чтобы он не искал её заново, а проверил
   остальное.

## 3. Что преемник обязан знать до того, как начнёт

1. **ЗАДАЧА ВЛАДЕЛЬЦА ЖИВА И ДОСТИЖИМА, ПРЕМИСА ПОДТВЕРЖДЕНА ЗАМЕРОМ.** Вложенный `CLAUDE.md`
   грузится ПО ТРЕБОВАНИЮ — по двум независимым поводам: цепочка предков рабочего каталога, и
   ЧТЕНИЕ файла в подкаталоге инструментом чтения. Мерено двумя приборами; второй — учёт токенов, и
   он не зависит от того, что модель о себе говорит. Предсказание: директив 16,7 % байтов, корень
   ≈36 149, впрыск жмётся примерно вшестеро.
2. **ФОРМА ЗАДАЧИ ИСПРАВЛЕНА ДВАЖДЫ, И ОБА РАЗА ИСПОЛНИТЕЛЕМ, А НЕ ДЕРЖАТЕЛЕМ.** Первое:
   `tools/CLAUDE.md` отменён — скрипты харнеса ЗАПУСКАЮТ, а не читают, поэтому харнесная доктрина
   туда уехать не может и остаётся в корне. Второе: `Claims module` держит 46 % директив, значит
   **«модульный раздел равно глубина» наивно**. Третье решение уровня (2): глубина Workflow едет в
   `docs/claude-md-rationale/workflow.md`, потому что систему указателей расширять запрещено словом
   владельца; отдельный файл под `milestones/` решено НЕ заводить — структура позволяет, содержимое
   не оправдывает (1,5 %).
3. **НОРМАТИВ С ДВУМЯ АВТОРАМИ:** «имя подсистемы внутри правила чаще указывает на МЕСТО ПЕРВОЙ
   ПОЛОМКИ, чем на область действия — лексика не есть адрес»; и **«дробить некуда» и «дробить
   незачем» — разные утверждения**: первое про структуру, второе про содержимое, и вывод о втором
   из первого есть вывод о композиции из проверки элемента.
4. **ПОРЯДОК ЮНИТОВ ПЕРЕД ЗАДАЧЕЙ ВЛАДЕЛЬЦА НЕСУЩИЙ, И ЭТО НЕ ПРО СКОРОСТЬ.** Её форма велит
   переносить текст ЦЕЛИКОМ и запрещает переписывать, поэтому всякая непочиненная ложь станет
   директивой корневого файла. По этому доводу перед ней прошли Т-132 и Т-127. **CB-282 по тому же
   доводу стоило бы закрыть до переноса — не успели, расхождение переедет.**
5. **ГЛАВНОЕ ПРО ПРИБОР.** У держателя смены 22 — ЧЕТЫРЕ ложные находки или пропуска собственного
   прибора (§7). **Три из четырёх поймала ОДНА привычка: печатать улику рядом с выводом.**
   Четвёртую не поймало ничто — её нашёл исполнитель уже после посадки. Правило смены 21 остаётся
   первым: находка есть ГИПОТЕЗА, пока её не воспроизвёл второй независимый способ.
6. **ЗЕЛЁНЫЙ НАБОР НЕ ЕСТЬ ВЫПОЛНЕННЫЙ ОРАКУЛ.** Он доказывает, что ничего не сломалось, и НЕ
   доказывает, что новое поведение чем-то проверяется: код без единого теста не может покраснеть,
   поэтому его отсутствие в наборе неотличимо от его исправности. Это CB-285, и это дыра в самой
   процедуре предполёта — в ней пять пунктов и ни одного про оракул брифа.

## 4. Очередь преемнику

**1. Т-131 — ГОЛОВА, живой, двигает текст.** Довести до посадки и приёмки. Задача владельца, третий
заход на один предмет; недостижение объявленной цели, доказанное числом, здесь оплачивается наравне
с достижением.

**2. ДОЛГ: приёмка Т-133** (девять пунктов), с явно названной дырой CB-285.

**3. CB-285** (`medium`) — дописать три недостающих теста половины (b), и рассмотреть вариант (3)
карты: механический сторож «ни один объявленный код возврата не смеет остаться неутверждаемым».
Он бы поймал этот случай сам и закрывает класс, а не экземпляр.

**4. CB-284** (`high`) — форма не выбрана; четыре варианта названы в карте, предпочтительный (a)
«отказывать вместо автокоммита, когда сообщение коммита не передано явно», с названной ценой.

**5. CB-283** (`medium`, продуктовый) и **CB-282** (`low`, текстовый).

**ОТЛОЖЕНО ОСЬЮ ПРИОРИТЕТА** (продукт старше самодокументации): CB-272, CB-273, CB-275, CB-270,
CB-271, CB-279. **Хвост (`low`):** CB-253, CB-252, CB-250, CB-240.

## 5. Скоропортящееся — перепроверить на месте

На момент записи: `main == origin/main == 9f8af49` плюс возможные коммиты после. Все три мержа смены
(`7aa9df2`, `4e711f1`, `100ab6c`) — предки `origin/main`, проверено. Алярм `main-invariants` зелён на
`9f8af49`. **Рабочих деревьев два: главный чекаут и `docs-t131-root-directive-layer`.**

**Главный чекаут делит индекс с DIR-2** (у них выпуск 0.3.0; пути с Т-131 не пересекаются). В твоём
индексе могут оказаться чужие файлы. Лечится перечислением своих путей после `--` в команде коммита,
а не `--no-verify`. Файл настроек владельца `.claude/settings.json` и его копия — **не трогать**.
**Ни одна ветка никогда не удаляется.**

**ГРЯЗЬ В ГЛАВНОМ ЧЕКАУТЕ ОПАСНА И ВНЕ ОКОН.** Неотслеживаемый файл финишу — «note», а изменённый
ОТСЛЕЖИВАЕМЫЙ — `exit 11`, и повтором он не лечится. «Пишу у себя в плановых заметках» не есть
безопасное действие, пока заметка отслеживаемая (уточнение К-30, принято куратором).

## 6. Инструментальное — ЛОВУШКИ ЭТОЙ СМЕНЫ

- **«Внешний безопасный каталог» может оказаться ТЕМ ЖЕ репозиторием.**
  `/home/faxik/workspace-private/codebugs` — симлинк на `/home/faxik/w/codebugs`, хотя среда
  объявляет его отдельным рабочим каталогом. **И сторож мусора его бы не поймал:**
  `_guard_untracked_scratch_at_root` ловит только `*.py`, `tmpXXXXXX` и `.codebugs-export-*`.
- **Функция, извлечённая из скрипта, тащит зависимости из ДРУГИХ файлов.** `_resolve_worktree_path`
  живёт в `worktree-finish.sh`, а `_BRANCH_TYPES` — в `tools/_guards.sh`; проба без него молча
  вырождается и даёт правдоподобный ложный ответ. **Печатай значения, на которых стоит проба.**
- **`strace` показывает ОТКРЫТИЕ файла, а открытие есть обнаружение, а не вкладывание в контекст.**
  Оракул «файл открыт — значит загружен» объявил бы остановку задачи владельца там, где её нет.
- **Прибор, читающий дерево, сам стоит на копии дерева, которая может отстать** (CB-215 на самом
  читающем).
- **Оракул множеств сообщает только о ПРОПАЖАХ** — токен, родившийся сломанным, невидим ему по
  построению (CB-279).
- **ПРОЗА, НАЗЫВАЮЩАЯ ЛИТЕРАЛ КОДА, ЛОМАЕТ ЯКОРЯ, КОТОРЫЕ НА ЭТОТ ЛИТЕРАЛ СМОТРЯТ.** Комментарий
  Т-133 назвал дескриптор замка, и два структурных теста стали находить комментарий вместо
  оператора. **Правится ТЕСТ, а не проза:** точность живёт в якоре, а запрет прозе объяснять есть
  дисциплина в точке применения.
- **`git commit -- <path>` не видит НЕОТСЛЕЖИВАЕМЫЙ файл** — сперва `git add -- <путь>` поимённо.
- **`git rev-parse --short A B` отказывает** («Needed a single revision»); без `--short` работает.
- Из прошлых смен, всё ещё живое: **`./.venv/bin/ruff` НЕ СУЩЕСТВУЕТ** (только
  `uv run --extra dev ruff`, `0.15.7`); **незакавыченный `--include=*.py` под `zsh` обрывает `grep`
  целиком**; **прогон по именам ФАЙЛОВ вместо КЛАССОВ** даёт `no tests ran`; **`nohup` ставит
  `SIGHUP` в игнор и даёт ложный отказ двух тестов сигналов**; **приёмщиков гоняй в их СОБСТВЕННЫХ
  копиях**; **отключённый `Edit` у приёмщика — НЕ BLOCKED**; **прямой вызов `sqlite3` запрещён**.

## 7. Записи против меня

**Их ЧЕТЫРЕ. Три — ложные находки прибора, и все три поймала одна привычка печатать улику рядом с
выводом. Четвёртая — ПРОПУСК, и её не поймало ничто.**

1. **Проба ловушки Т-133 без `_BRANCH_TYPES`** — молча обошла пустой массив и дала правдоподобное
   «ловушка не воспроизводится». Закрыла бы несущий пункт брифа.
2. **«Внешний безопасный каталог»**, оказавшийся симлинком на корень репозитория.
3. **Прокси милстоунной доли, завысивший в 18 раз** (29 483 против 1 646), и — хуже числа — вывод
   по инерции: опроверг структурное «дробить некуда» и счёл сказанным что-то про содержательное
   «дробить незачем». Вывод о композиции из проверки элемента, на нашем же материале.
4. **САМАЯ СЕРЬЁЗНАЯ (CB-285): я приземлил половину починки, у которой нет ни одного теста.**
   Прогнал полный набор, линтер, предполёт из пяти проверок — и не сверил, выполнен ли ОРАКУЛ §5
   моего же брифа, который требовал этих проб поимённо. **Проверил зелень, а не выполнение
   оракула; проверил элемент, а не композицию.** Нашёл не я, а исполнитель, уже после посадки,
   против собственной работы.

**Что из этого следует.** Смена 21 вывела: главный источник ложных находок у держателя — его
собственный прибор. Моя добавляет два уточнения. Первое: **лечится это не осторожностью, а одним
механическим приёмом** — печатью улики рядом с выводом, — и приём переносится на любого участника,
включая приёмщика (сегодня он поймал им себя). Второе, и оно неприятнее: **приём ловит ложные
находки, но НЕ ловит пропуски.** Запись 4 — не то, что я увидел неверно, а то, чего я не посмотрел
вовсе, и против этого нужен не приём, а пункт в процедуре или механический сторож.

**И то, что стоит держать в голове при чтении моих решений: ДВАЖДЫ за смену исполнитель опроверг мою
ратифицированную форму, и оба раза был прав** (отмена `tools/CLAUDE.md`; «модульный раздел равно
глубина» наивно). Бриф как ДАННЫЕ (К-12) — не вежливость, а работающий механизм: оба опровержения
пришли из пунктов, которые я сам же и написал.
