# HANDOFF DIR-3, смена 2 (2026-09-07)

Передача от держателя направления DIR-3 «Сквозной обзор и сопровождаемость» преемнику того же
уровня (2). **Пишется по ходу смены, а не в конце**: за сутки 6–7 сентября сессии в этом проекте
обрывались лимитом подписки пять раз, и передача, отложенная на конец, не пишется никогда.

**Правил в этом файле нет.** Норма живёт в нормативных источниках, перечисленных ниже; здесь —
только факты этой смены и суждения, которые я принял и которые преемник не выведет заново.

## Нормативные источники — читать их, а не пересказ

1. `.claude/plans/L2-BRIEF-DIR-3-review-and-maintainability-2026-09-07.md` — хендофф куратора.
2. `.claude/plans/DIR-3-review-and-maintainability.md` — **артефакт направления**, мой основной
   рабочий документ: скоуп, ратифицированные расхождения, факты приёмки, эскалации, журнал.
3. `.claude/plans/REVISION-2026-09-06-code-and-architecture.md` §5 — источник заданий (пакеты).
4. `.claude/plans/REVIEW-X2-REVISION-2026-09-06-judge.md` — правки судьи; мои S-13 и S-14.
5. `CLAUDE.md` репозитория — регламент веток, финиша, минта номеров.
6. `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md` §16.3 —
   таксономия классов задачи и правила пина модели.

## Что сделано в этой смене

### Заведено и закоммичено

- **Артефакт направления** `.claude/plans/DIR-3-review-and-maintainability.md`, коммит `7006b1e`
  на `main`. Одиннадцать разделов по образцу артефакта DIR-1.
- **Карта CB-314** (`high`, `process`, файл `pyproject.toml`) — карта пакета П-3
  «воспроизводимость среды». Заведена мной, потому что существующей не было: соседние CB-172
  (набор правил линтера) и CB-267 (границы версий транзитивных зависимостей) живут на тех же
  файлах, но предмет у них другой, и слияние их с CB-314 было бы ошибкой.
- **Два номера юнитов через `tools/cascade-mint.sh --prefix Т`:**
  - **Т-141** — пакет П-3, карта CB-314.
  - **Т-142** — пакет П-11, ведущая карта CB-302 плюс CB-290…CB-301 и CB-306 как сырьё.

### Суждения, которые я принял и которые не надо переоткрывать

**1. Опасение куратора о пересечении с чужим направлением снято фактом.** Хендофф просил сверить
`git diff --stat main...refactor/cb-311-one-source-for-the-classification` перед первым разведочным
проходом. Вывод команды **пуст**: ветка юнита Т-140 направления DIR-1 уже влита в `main` мержем
`6202da4`. Оставшееся рабочее дерево `.worktrees/accept-t140` — приёмочное, только на чтение.
Пересекаться не с чем, и оба моих пакета идут параллельно без всякой сериализации: П-3 живёт в
файлах сборки и непрерывной интеграции, П-11 — в двух файлах правил, общих файлов ноль.

**2. Расхождение имён «DIR-4» и «DIR-3» ратифицировано как поправка буквы.** Документ ревизии
писался до внесения направления в реестр идентификаторов и предлагал завести новое направление
«сопровождаемость» под номером 4; куратор вместо этого расширил существующее DIR-3. Во всех текстах
читать «DIR-4» документа ревизии как «DIR-3». Записано в артефакте как Р-1.

**3. Восьмое решение владельца поднято куратору как эскалация Э-1 и ОТВЕЧЕНО в ту же смену.**
Пакет П-11 обязан свести к правде противоречие между корневым запретом подстановки значений в текст
SQL и тремя задокументированными исключениями из него; документ ревизии прямо пишет, что выбор
формулировки за владельцем, но среди семи вопросов §4 этого решения нет. Куратор принял решение на
уровне (1) и сообщает владельцу одной строкой как расхождение в букве. Форма — **вариант Б с одной
фразой-указателем**, лучше обоих, что я предлагал, и полный текст с доводом лежит в артефакте, §10.
**Оговорка приёмки не нужна: решение принято, а не отложено** — прежний план «применить безопасный
вариант и записать оговорку» отменён.

Одно место, где я поправил **букву** требования куратора в пользу его **смысла**, и это записано
вслух: требование «число мест с подстановкой без причины на строке = 0», прочитанное буквально,
охватывает **69** мест в тринадцати файлах (мой замер линтером на `7006b1e`) — неделимое изменение
через весь пакет, вчетверо шире правила нарезки, уже измеренное картой CB-172 как отдельная крупная
работа. Замысел требования держится на **узкой** популяции: местах, подставляющих **значение**, а не
идентификатор из закрытого перечисления, — а их ровно три. Бриф ключует сторожа на предикат
«подставлено значение».

**4. Классы юнитов назначены по оракулу Р1 спеки, а не по этажу.**
- **Т-141 — класс B с одним включением класса C, поэтому тир `opus`, `effort: medium`.** Три из
  четырёх критериев приёмки несут детерминированный оракул (команда и ожидаемый вывод названы), но
  третий требует **составить** явный короткий список библиотек, на подложенной несовместимой версии
  которых проверка обязана краснеть, — а это суждение без оракула. Смешанный случай по правилу
  спеки округляется вверх.
- **Т-142 — класс C, тир `opus`, `effort: high`.** Обязательный первый шаг пакета — отнести каждое
  числовое утверждение двух файлов правил к одному из трёх классов — есть суждение по каждому
  утверждению, и оракула у него нет вовсе. Сторож, который строится следом, — уже класс B, но
  деэскалация внутри одного юнита не делается.

## Что НЕ начато и почему — граница смены

**Не стартуют по прямому указанию куратора:** П-4 (ждёт П-1 в чужом направлении DIR-1);
П-6, П-8, П-9, П-10 (ждут ответов владельца на семь вопросов §4 документа ревизии — придут через
куратора); П-7 (после П-4 и П-11). Разведку по ним делать можно и полезно; работу — нет.

## Открытые вопросы наверх

- **Э-1** — **ЗАКРЫТА в эту же смену** ответом куратора (артефакт §10). Других открытых вопросов
  наверх на момент этой записи нет.

## Диспетчеризовано

Оба юнита запущены фоном на `opus`, брифы закоммичены на `main`.

| Юнит | Ветка | Пакет | Класс / усилие |
|---|---|---|---|
| Т-141 | `fix/cb-314-environment-reproducibility` | П-3, воспроизводимость среды | B с включением C / `medium` |
| Т-142 | `fix/cb-302-truth-of-normative-texts` | П-11, правда нормативных текстов | C / `high` |

Обоим предписано: финиш **не** запускать самим, вернуться держателю и ждать окна; перед возвратом —
проход `/simplify-traced`, затем кросс-модельный разбор диффа внешней моделью. Карты не закрывать.

**Разграничение карт `dir-3` между Т-142 и будущим пакетом П-7 сделано и записано в артефакте, §5.**
Признак — «число и указатель против прозы», взятый из самого документа ревизии. Без него один юнит
закрыл бы чужие карты, а другой остался бы без предмета.

## Состояние на момент последней записи

См. также журнал направления (артефакт §11) и факты приёмки (артефакт §9).

- `main` = `4dd649d` на момент последнего коммита брифа.
- Живых клеймов на начало смены: 0; после создания рабочих деревьев их будет по одному на юнит —
  скрипт `worktree-setup.sh` клеймит карты, названные в имени ветки.
- Порядок коммитов смены: `7006b1e` артефакт → `7d5f923` бриф Т-141 и передача → `acb010f` стартовый
  промпт преемника → `4dd649d` бриф Т-142. Плюс два коммита реестра идентификаторов от
  `tools/cascade-mint.sh` между ними.
