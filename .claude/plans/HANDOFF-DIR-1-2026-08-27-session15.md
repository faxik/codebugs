# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-27, смена 15

(вытесняет `HANDOFF-DIR-1-2026-08-27-session14.md`; следующий вытесняет этот)

**Дописан полностью; черновиком коммитился намеренно.** Сеть роняет сессии пачками весь день — за
смену 14 их упало четыре, приёмщик Т-107 обрывался дважды, — и handoff, написанный целиком в
конце, есть ставка на то, что конец наступит. Поэтому он рос по частям, и каждая часть
коммитилась в момент написания. Пустых разделов не осталось.

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов.** Файлы приёмщика местами говорят «семь»: называй число
  явно в каждом промпте приёмки.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий; **Releasing** — для
  срезов; **Testing** — правила тестов на конкуренцию и жильцы `tests/conftest.py`.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §5 таблица юнитов, §6
  ратифицированные расхождения, §9 неизменяемые факты приёмки (смена 15 первой), §10 эскалации,
  §11 журнал (смена 15 в конце), §13 шаблон брифа и §13 п. 17.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md` — начинай бриф копированием.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. **Пронумерованные каноны живут
  там** — числа не привожу, потому что они растут внутри смены: за одну эту добавлено три. К-22:
  прежде чем поднимать карту по признаку «видимый вред», замерь, сколько раз эту поверхность
  вызывали. К-23: жива ли сессия, решает время коммитов её направления, а не уведомление харнеса.
  К-24: перенося механизм из другого проекта, переноси и то, что стоит СНАРУЖИ него. **Аллокатор
  ID:** `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| приёмка Т-107 (CB-232, `high`) сведена, вердикт пришёл транзитом | `c594569` | **ПРИНЯТ С ОГОВОРКОЙ**, CB-232 закрыта, остаток → CB-235 |
| CB-235 + CB-110, две документационные поправки уровня (2) | `8adf7fa` | приземлено с первой попытки; CB-235 `fixed`, CB-110 `wont_fix` |
| срез **0.2.2** | `48d4e8b` | **выполнен**; тег `v0.2.2` на мерже, `main` и тег запушены |

**Долгов приёмки не оставляю: единственный унаследованный (Т-107) закрыт первым действием.**
Юнитов вниз в эту смену не диспетчеризовано — очередь упиралась в `findings.py`, который держали
две ветки DIR-2.

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь очередь с `git log --first-parent` и `git worktree list`.** Уведомлению не верь никогда:
   в эту смену `main` уходил вперёд между двумя моими соседними командами трижды, причём один раз
   чужой коммит оказался у меня локально раньше, чем на `origin`, и мой `git push` унёс чужую
   работу вместе со своей (безвредно, но знать стоит).
2. **Прочитай в §11 артефакта блок «Смена 15» целиком, обе его части** — там записи против меня и
   решения уровня (2), которые преемник обязан знать до того, как возьмётся за CB-172. Счёт не
   привожу намеренно: он и есть та форма, которую это направление снимает.
3. **Срез 0.2.2 УЖЕ сел** (`48d4e8b`, тег `v0.2.2`) — §4 оставлен как описание выполненного
   порядка, а не как задание. Следующий срез режется по нему же.
4. **Знай, что смену 15 вели ДВЕ сессии DIR-1.** Куратор запустил замену, поверив уведомлению
   харнеса о падении; замена сделала работу по К-24 (гейт `/simplify-traced`), эта сессия —
   приёмку, две карты и срез. Отсюда канон **К-23**: жива ли сессия, решает время коммитов её
   направления, а не уведомление. В §11 артефакта обе линии записаны раздельно и не переписаны.

## 3. Очередь преемнику

1. **CB-172 (`S608` не включён в ruff).** Замер снят мной на сегодняшнем `main` и записан в §11:
   52 срабатывания при учёте маркеров, 68 без учёта, реально подавляют 16 из 22 маркеров текста.
   **Распределение по файлам — это и есть ответ на вопрос «почему не режется по территориям»:**
   `findings.py` 21, пакет `milestones/` 13 (закрыт решением владельца), `reqs.py` 6, `sweep.py` 5,
   `bench.py` 4, по одному в `blockers.py`, `embeddings.py`, `merge.py`. Форма НЕ выбрана; выбирать
   замером (К-19), а не соображением. **Ловушка, которую бриф обязан назвать:** `select` в ruff
   ЗАМЕНЯЕТ набор по умолчанию, добавляет `extend-select`.
2. **CB-158** (явный `--limit` перебивается длиной списка `ids`; два сайта — `reqs.py:321` и
   `findings.py:2691`). Предпосылка перепроверена мной на живом трекере в эту смену и
   воспроизводится: `query --id CB-150 --limit 0` даёт 1 строку, `--id A,B --limit 1` даёт 2,
   контроль без `--id` даёт 0.
3. **CB-167** (`meta_key` строит путь JSON без кавычек). Территория `findings.py`.
   **Инструментальная мелочь, найденная в эту смену:** у CLI-верба `query` флага `--meta-key`
   НЕТ вовсе — фильтр существует только на поверхности MCP, так что воспроизводить карту через
   командную строку нельзя, нужен вызов инструмента.
4. **Блок «честные границы»** — как в очереди смены 14.

## 4. Срез 0.2.2 — ВЫПОЛНЕН; ниже порядок, по которому режется следующий

**Сделано:** мерж `48d4e8b`, тег `v0.2.2` на нём, `main` и тег запушены. Условие куратора
(резать после посадки CB-230 и CB-233) выполнено: `d42aed5` и `de20578` соответственно.
**За владельцем остаётся `pipx reinstall` и ПЕРЕПОДКЛЮЧЕНИЕ MCP-серверов** — `INSTRUCTIONS`
читается один раз при старте сервера, и до перезапуска клиент получает прежний текст.

**Порядок для СЛЕДУЮЩЕГО среза (раздел Releasing в `CLAUDE.md`), с тем, что проверено на этом.
Числа версии ниже — подставляемые, а не `0.2.2`:**

1. `version` в `pyproject.toml` **и** `__version__` в `src/codebugs/__init__.py` — оба места, одно
   без другого не проходит. Гейт `tests/test_release_version.py` сверяет ЧЕТЫРЕ канала, включая
   метаданные УСТАНОВЛЕННОГО дистрибутива, — поэтому набор гоняется в рабочей копии, где
   `uv run` пересоберёт editable-установку, и только там сверка честна.
2. `## [Unreleased]` → `## [<версия>] — <дата>`; ПУСТОЙ `## [Unreleased]` остаётся сверху (гейт
   требует, чтобы он существовал НАД новейшей выпущенной секцией, но не требует, чтобы он был
   пуст). Разделитель даты — тире; в файле живы обе формы, гейт принимает `-`, `–` и `—`.
3. Первым абзацем секции — **highlights пользовательским языком**. **Проверь по `CHANGELOG.md`, в
   КАКОЙ секции лежит каждый пункт, который тебе назвали для highlights.** На этом срезе два
   пункта из шести оказались достижениями предыдущей версии, и написать их в новую значило бы
   повторить CB-191 — четыре фактических расхождения в абзаце highlights 0.2.0.
4. **Не оставляй в записях числа, чей источник не лежит в дереве.** На этом срезе убраны три
   («31 ways», «8 before», «27 after»): корпуса из 31 формы нет нигде, а приёмщик независимо
   построил свой из 46 и получил другие числа. Довод — в §11 артефакта.
5. Посадка веткой через `tools/worktree-finish.sh` (`main` напрямую не правится). **После**
   посадки — `git tag -a v<версия> <sha мержа>` из главного чекаута; тег, сделанный на ветке,
   указывает на коммит, который не приземлился.
6. Пуш `main` и пуш тега — ОТДЕЛЬНЫМИ командами.
7. **Предупреждение владельцу отдельной строкой:** `INSTRUCTIONS` MCP-сервера читается при
   СТАРТЕ (константа модуля уходит в `MCPServer(instructions=…)`, `server.py:625`), поэтому после
   обновления пакета сервер нужно переподключить — иначе клиент продолжает получать прежний текст.
   `pipx reinstall` за владельцем.

**Проверено мной прогоном до среза, выборочно, четыре пользовательских утверждения из
`[Unreleased]`:** отрицательный `--limit` отказывает кодом 1 с объяснением, почему ноль и минус
единица — разные вещи; `sweep-mark` объявляет `--undo` и `--state` взаимоисключающими прямо в
подсказке; `categories-normalize` с несуществующей целью отказывает кодом 1 и называет ближайшие
существующие категории; `where` над каталогом без права входа говорит «could not confirm … could
not look at it (Permission denied)» вместо «ничего нет». Все четыре записи `CHANGELOG.md`
описывают поведение верно. **Делай так же: релизная заметка проверяется прогоном, а не чтением.**

## 5. Скоропортящееся — перепроверить на месте

Статусы карт; состав рабочих деревьев (`git worktree list`, **никогда** уведомление); отставание
`origin/main`; хвост аллокатора. Ветка `feature/t49-bt6-third-pilot-annotations` — запаркованный
пилот, решение владельца, не трогать. Чужие рабочие деревья под `.claude/worktrees/agent-*` — не
мои; **удалять их нельзя даже когда они выглядят брошенными**, см. §6.

## 6. Инструментальное

- **Пуш разрешён и делается после каждой приёмки; теги релиза тоже.** `git push` отдельной
  командой (составная бывает отклонена целиком вместе с коммитом).
- **Перед `tools/worktree-finish.sh` теперь обязателен гейт `/simplify-traced` (К-24).** Он
  напоминает о себе хуком, но НЕ блокирует команду — финиш стартует, и остановить его надо самому.
  Останавливать безопасно, пока идёт набор (`[6/7]`): замок берётся только на `[7/7]`, и до него
  ничего не приземляется. Убивать по образцу (`pkill`) политика окружения запрещает — пользуйся
  остановкой фоновой задачи по её идентификатору. **Фаза 3 гейта (`/reflect`) остаётся
  пропущенной**, и её собственное условие я перемерил: `tools/fold_process_rules.py --dry-run` в
  autosorter даёт 80 492 байта против потолка 40 000, то есть затор не расчищен.
- **Установщик гейта в git НЕ ОТСЛЕЖИВАЕТСЯ.** `.claude/plans/INSTRUCTION-simplify-gate-2026-08-27.md`
  лежит на `main` и велит запускать `python3 <codebugs>/tools/install-simplify-gate.py`, а этого
  файла в истории нет — он есть только в этой рабочей копии. В свежем клоне инструкция
  невыполнима. Починка за автором инструкции (куратором) и требует ветки: на `main` напрямую
  разрешены только `.claude/plans/*.md`.
- **Уборка чужих рабочих копий запрещена вслепую.** `git worktree prune` и `remove --force` по
  всему дереву запрещены: в эту смену чужая уборка снесла ЖИВУЮ копию приёмщика. Чистота дерева
  НЕ является признаком брошенности — приёмщик, гоняющий набор, оставляет дерево чистым по
  построению.
- **Не запускай `uv run` в главном чекауте, пока может идти чужой финиш**: он пересобирает
  окружение, которое `_guard_interpreter_matches_main` сверяет под замком. Для разовых замеров
  вызывай бинарник напрямую — `./.venv/bin/ruff`, `./.venv/bin/codebugs`.
- **`exit 13` — верный отказ**, лечится буквальным повтором с тем же `--merge-msg`; **`exit 11`
  повтором не лечится**. Журнал попыток на посадку — `.worktrees/landing-attempts.log`.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **`tools/worktree-probe.sh` в этом репозитории НЕ существует**, флага `--items` у
  `tools/worktree-setup.sh` тоже нет — оба из соседнего проекта.
