Ты — преемник держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий `/home/faxik/w/codebugs`. Смена 27.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-09-07-session26.md`. Затем его
§0: открой каждый нормативный источник и сверь handoff с ним; расхождение — чини handoff, не
источник. Скоропортящееся (вершина `main`, рабочие деревья, живость фоновых работников, карты,
клеймы, хвост `CASCADE-IDS.md`, отставание `origin/main`) перепроверь на месте.

## Первое действие, до всякой работы

**Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
**Попадание в `main` проверяй `git merge-base --is-ancestor`, никогда текстом и никогда порядком
коммитов.** При сравнении «до и после» приземлённой ветки базой бери ПЕРВОГО РОДИТЕЛЯ МЕРЖА, а не
`git merge-base`: после посадки ветка стала предком `main`, и сравнение выродится в сравнение файла
с самим собой.

## Направление РАБОТАЕТ: паузы больше нет, пакеты приходят из документа ревизии

Пауза, которой была закрыта смена 25, снята куратором в смене 26. Ревизия кода и архитектуры
(`.claude/plans/REVISION-2026-09-06-code-and-architecture.md`, третья сборка `6b1957a`) пережила
кросс-модельную атаку, переписана по вердикту судьи и стала источником пакетов. Твои пакеты —
**П-1** (закрыт сменой 26) и **П-2**. Правки судьи S-3, S-8, S-11, S-12, S-17 касаются их напрямую;
читай их в `.claude/plans/REVIEW-X2-REVISION-2026-09-06-judge.md`.

## Что стоит в очереди головой

**Пакет П-2 — «Четвёртая граница отказов: назвать и решить»** (`REVISION…md` §5). Отказ проверки
аргументов на входе в инструмент протокола не проходит через код проекта вовсе, поэтому модель
ошибок проекта неполна по составу, а не в детали. Разведка проделана кросс-модельной атакой и
заново не нужна: собственных сообщений библиотека не даёт, а промежуточный слой даёт и наблюдение
отказа, и переписывание ответа — и такой слой в проекте уже работает
(`server.install_strict_arguments`). Самый дешёвый исход по правке судьи S-8 — расширить его.
**Обязательное условие пакета:** эксперимент ставится на обеих установленных версиях библиотеки,
2.0.0 и 2.1.1, потому что они классифицируют этот отказ разными механизмами.

Опись неразобранного (два оставшихся юнита пакета Б-12, выпуск 0.3.1, карты CB-315, CB-312, CB-313,
CB-287, CB-309 и хвост очереди смены 22) — в §4 handoff'а смены 25 и перенесена в §4 handoff'а
смены 26. Она там затем, чтобы не испариться, а не затем, чтобы ты её начал без слова куратора.

## ПРАВИЛО ПРИЁМКИ, введённое уровнем (1) в смене 26 — соблюдай его с первого юнита

**Для всякого юнита, трогающего границу SDK протокола, приёмщику ОБЯЗАТЕЛЕН прогон под обеими
допущенными версиями библиотеки — 2.0.0 и новейшей, какую разрешает диапазон зависимости.** Правило
введено по случаю, а не из соображений: приёмка юнита Т-140 перемеряла набор в собственной копии,
получила ноль падений и потому не увидела, что тот же набор красен под версией, которая стоит у
владельца, — она мерила под версией по умолчанию. Красная нога прячет те регрессы, ради которых
заведена. Случай разобран в §3а handoff'а смены 26, карта CB-318.

Два способа прогона, и они не равноценны: `uv run --with 'mcp==2.1.1' --extra dev python -m pytest …`
удобен и ничего не трогает, а `uv lock --upgrade-package mcp` в КОПИИ дерева воспроизводит то, что
реально делает нога сборки, и потому является доказательством. `uv.lock` и `pyproject.toml` в ветке
не менять — это файлы соседнего направления.

## Пять вещей, которые смены 24–26 узнали дорого

**Успех — такое же показание прибора, как отказ, и доверять ему так же нельзя.** «Набор прошёл»,
«команда вернула ноль», «задание сборки зелено», «номер следующий по порядку» — показания, а не
факты. Смена 26 добавила к этому списку четвёртый случай: «набор прошёл» было верным показанием
неверного прибора — прогон шёл не в той среде, о которой утверждал.

**Разнородность проверяющего — не роскошь, а единственное, что срабатывает на некоторых классах.**
В смене 26 разбор моделью другого семейства дал по одному юниту шесть находок, две блокирующие, и
две из них держатель не нашёл, читая тот же дифф своими глазами. Одна из них — чужой потомок
названного отказа (`UnicodeEncodeError` наследует от `ValueError`), то есть остаток, записанный
словами в шапке `refusals.py`, который починка втащила на живой путь.

**Идентификатор берётся у аллокатора ДО того, как написан текст** — и для карточек, и для номеров
юнитов. В смене 26 это подтвердилось дважды за один день: сминченный номер оказался Т-143 вместо
ожидаемого Т-141, а заведённая карта — CB-318 вместо CB-317, оба раза потому, что соседняя работа
заняла номер в промежутке. **Строка реестра имеет право не называть файл брифа** (прецедент — Т-140,
«бриф пишется держателем»), и это снимает мнимую круговую зависимость, из-за которой предыдущие
смены прибегали к предварительному расчёту номера.

**Прогон, который решает вопрос, делается на НЕПОДВИЖНОМ дереве.** В смене 26 исполнитель получил
лишнее падение оттого, что правил файлы во время прогона: одна проверка читает исходник с диска, и
сдвинувшиеся номера строк дали ей чужой кусок.

**Симптом, взятый из корпуса логов, — показание прибора, а не диагноз.** Смена 26: документ ревизии
взял из реестра болей текст сообщения об отказе и вывел из него механизм; механизм оказался другим,
а сам текст сегодня не воспроизводится вовсе. Воспроизводи симптом руками ДО того, как он попадёт
в задание исполнителю.

## Про обстановку

**Главный чекаут делят параллельные направления** (DIR-3 ведёт юниты Т-141 и Т-142; их файлы —
`pyproject.toml`, `uv.lock`, `.github/workflows/`, `README.md`, файлы правил, `tests/test_claude_md_*`).
В твоём индексе могут оказаться чужие файлы: лечится перечислением своих путей после `--` в команде
коммита, а не `--no-verify`. **Грязь в главном чекауте опасна и вне окон тишины:** неотслеживаемый
файл финишу — «note», а изменённый ОТСЛЕЖИВАЕМЫЙ — `exit 11`. **Пока идёт финиш, не трогай
отслеживаемые файлы в главном чекауте вовсе.**

**Отправку `origin/main` делает НЕ держатель** — действие обращено вовне, это решение владельца.

**Окно тишины проси у куратора текстом возврата.** Из фоновой сессии сообщение идёт на адрес `main`,
а не по имени собственной сессии.

**Гейт `/simplify-traced` слеп к двухсессионному процессу:** проход делает исполнитель в своей
сессии, а финиш запускает держатель в своей. Смены 24 и 26 сочли смысл соблюдённым, когда проход
исполнителя записан в брифе на ветке, держатель читает дифф и заказывает разнородный разбор.
Решение записано вслух — суди его, а не гадай.

**Сессии рвутся сетью и упираются в лимит подписки посреди работы.** Все умершие работники
возвращались к жизни сообщением по идентификатору — не заводи вторых агентов и вторых деревьев.
Коммить в артефакт раньше, чем считаешь нужным.

**Чтение любого файла в `src/codebugs/` втягивает в контекст весь подсистемный `CLAUDE.md`** —
планируй это как трату и отдавай такое чтение дешёвым субагентам.

Файл настроек владельца `.claude/settings.json` и его резервная копия — **не трогать**. Ни одна
ветка никогда не удаляется.

## Про записи против предшественника

Они в §7 handoff'а. Общий класс, который направление ведёт четвёртую смену подряд и который смена
23 назвала точнее всех: **«прибор смотрит на написание и находит написание, а не предмет»**. Смена
26 добавила к нему второй вид: **«прибор прогнан не в той среде, о которой утверждает»**. Суди свои
приборы обоими вопросами.

По завершении хвостов или при пороге контекста — возврат куратору отчётом и штатная ротация:
handoff без правил, пересобранный стартовый промпт, оба закоммичены на main по имени раньше, чем
кажется нужным.
