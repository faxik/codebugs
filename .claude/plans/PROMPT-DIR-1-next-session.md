Преемник сессии держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий /home/faxik/w/codebugs. Сессия 14.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-08-27-session13.md`. Затем — его
§0: открой КАЖДЫЙ нормативный источник (спека каскада
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`, `CLAUDE.md` репо,
`DIR-1-trust-coordination.md`, реестр болей) и сверь handoff с ним; расхождение — чини handoff, не
источник. **Правил в этом промпте нет намеренно.**

Порядок действий — handoff §2. Скоропортящееся (статусы карт, состав рабочих деревьев, отставание
`origin/main`, хвост `CASCADE-IDS.md`) перепроверь на месте, не бери на веру.

**Долгов приёмки я не оставляю: все четыре приземлённых юнита смены приняты, все с оговоркой.**

## Твой первый долг — прочесть эскалацию, а не писать код

**Э-31 в §10 артефакта: «окно тишины» в ратифицированной форме (e) СТРОИТЬ НЕЛЬЗЯ.** Второй проход
внешней модели по редакции 3 вернул тот же вердикт, что и по редакции 1, с уверенностью 0,99, и
провенансное возражение прошлой смены («он судил снятый документ») этим снято.

**Три его утверждения я перепроверил лично; подтвердились три** — в том числе главное: условие (B)
не доказывает тривиальности мержа, потому что git выводит переименование каталога из совокупности
переименований его файлов. На настройках ПО УМОЛЧАНИЮ это заклинивает `main`, при
`merge.directoryRenames=true` — молча переносит плановую заметку. Разбор с таблицей замеров — в Э-31.

**Редакцию 4 я не писал и тебе не советую**: ратификация запрещает редактировать в ответ на отказ.
Честный вывод, названный самим промптом ратификации, — класс не закрывается дёшево, остаётся вариант
(d), только измерять. **Решение уровня (1).** Юнит Т-95 сминчен под форму (e) и не запущен; его
судьба (аннулировать или перенацелить) — часть того же решения, сам не трогай.

## Что стоит знать до того, как начнёшь

**Дважды за смену решал ЗАМЕР, а не довод, и один раз он был против меня.** Я подтвердил находку
приёмщика по CB-224 четырьмя собственными командами — и был опровергнут внешней моделью по замеру,
который сам просил преемника беречь как факт. **Механизм моей ошибки: гребёнка ключевалась на
НАПИСАНИИ**, поэтому не увидела двух чтений настоящего реестра, спрятанных за константой и за сборкой
пути из сегментов. То есть я совершил ровно тот дефект, о котором этим замером и предупреждал.

**Пункт 8 контракта приёмки (путь модели) провалился ТРИ раза подряд**, и это дефект моей
дисциплины, а не исполнителей: правило стояло прозой с сессии 5, а я требовал строку в отдельных
поручениях — то есть чинил перечислением. Противодействие поставлено:
`.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`. **Начинай бриф копированием этого файла.** Он не гейт, а
заготовка, и §13 п. 17 говорит это вслух.

**Форму юнита Т-99 выбирал я, оракул к ней предписывал тоже я, и ни то ни другое не поймало
собственного нарушения** — предел в двадцать строк вместе с алфавитной сортировкой прячет именно
`src/` и `tests/`, вопреки несущему свойству, которое я сам и записал. Отсюда CB-226. Проверяй
СОСТАВ предписанных свойств, а не каждое по отдельности.

**И то же самое, но крупнее, на юните Т-101.** Я выбрал форму «свойство обязано держаться гейтом,
потому что перечисление не сходится третий раз подряд» — довод верен и остаётся верным. А
поставленный гейт **сам оказался перечислением**, только написаний вместо мест: он ключуется на
тексте вызова и поймал одну обходную форму из тринадцати, а строка его таблицы исключений разрешает
целую функцию, так что восстановление одного из трёх только что устранённых дефектов оставляет его
зелёным. Против этого я поставил абзац в брифе — **в тот самый день, когда записал в §13 п. 17, что
проза таких вещей не держит.** Отсюда CB-227, единственная `high` в трекере, и начинать по ней надо
с вопроса, годится ли форма, а не с починки дыр.

## Что сработало и стоит держать

**Требование «попробуй обойти НОВЫЙ страж так же, как предшественник обошёл старый», поставленное
ГЛАВНЫМ требованием промпта приёмки.** Третья смена подряд приносит больше всего: у Т-99 из трёх
атак приёмщика одна удалась и дала карту.

**Параллельная работа приёмки и следующего юнита.** За смену — четыре юнита и три приёмки.
Ограничение одно: два юнита в один файл дадут конфликт на посадке, поэтому `CLAUDE.md` и
`tests/conftest.py` сериализуй.

## Ограничение среды, изменившееся в лучшую сторону

**Пуш РАЗРЕШЁН владельцем.** Отставание `origin/main` держалось нулевым всю смену, и **пункт 6
контракта приёмки закрывается по-настоящему** — алярмы `ci` и `main invariants` эти коммиты видели.
Оговорка «origin отстаёт на N, алярм этих коммитов не видел» больше не нужна: **не переноси её в
промпты приёмки.** Но `git push` запускай ОТДЕЛЬНОЙ командой — составная отклоняется целиком вместе
с коммитом.

## Очередь — handoff §6

Коротко: ответ по Э-31 → **CB-227** (единственная `high`) → **CB-225 вместе с CB-226** (обе в
`tests/conftest.py`, врозь дадут конфликт) → **CB-213** (правка нормативного документа) → CB-220 →
CB-212 → CB-211 вместе с CB-199 → CB-205 и срез 0.2.2 → блок «честные границы».

**Пункт 8 контракта приёмки впервые закрыт зачётом** — на Т-101, первом брифе, написанном с
заготовки. Три предыдущих юнита закрыли его отказом. Заготовка работает, пользуйся ею.

**CB-158 не брать, пока в полёте ветка соседнего направления по CB-196.** Пакеты milestones и
минт-гейта закрыты владельцем. Категории и идентичность — у DIR-2.

По завершении пакета или при пороге контекста — итоговый возврат куратору с SHA для спот-чека.
