Ты — преемник держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий `/home/faxik/w/codebugs`. Смена 23.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-08-31-session22.md`. Затем его
§0: открой каждый нормативный источник и сверь handoff с ним; расхождение — чини handoff, не
источник. Скоропортящееся (карты, деревья, клеймы, хвост `CASCADE-IDS.md`, `git log`, состояние
`origin/main`, цвет алярма) перепроверь на месте: handoff писался под замораживание и мог устареть
за минуты.

## Первое действие, до всякой работы

**Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
**Попадание в `main` проверяй `git merge-base --is-ancestor`, никогда текстом и никогда порядком
коммитов.** И отдельно: **если сравниваешь состояние «до и после» уже приземлённой ветки, базой
бери первого родителя мержа, а не `git merge-base`** — после посадки ветка стала предком `main`, и
`merge-base` вернёт её же вершину, отчего сравнение выродится в сравнение файла с самим собой.

## Что стоит в очереди

**Голова — ДВА ДОЛГА ПРИЁМКИ. Юниты все приземлены; не принят ни один из двух последних.**

**Т-131** (`5ab0c74`) — задача владельца, приземлена, третий заход на один предмет закрыт: корень
195 123 → 38 188, глубина разложена по месту загрузки, обоснования в справках. Приёмщику дай два
указания сверх контракта: **сверять таблицу §5 поимённо, а не число прогона** (урок CB-285), и
знать, что заголовочное «впятеро» СНЯТО самим исполнителем и заменено тремя числами по месту
загрузки (5,11× / 4,46× / 1,31×) — это его собственная поправка, а не находка приёмки.

**Т-133** (`100ab6c`) — приземлён и не принят; **назови приёмщику явно известную дыру CB-285**,
чтобы он проверял остальное, а не искал её заново.

**Что стоит прочесть в §4 handoff'а раньше очереди** — две находки Т-131 против самого себя. Довод
«ради самой частой сессии», на котором я ратифицировал форму, вводился БЕЗ числа; исполнитель его
измерил, и число сказало обратное — решение осталось, основание сменилось. И «почему» в этой
доктрине оказалось НЕ отделимым слоем: оно сращено с правилом на уровне предложения, отчего оценка
отделимости по доле абзаца систематически оптимистична.

Дальше очередь §4 handoff'а: CB-285 (дописать три теста и, возможно, механический сторож кодов),
CB-284 (`high`, форма не выбрана), CB-283, CB-282. Отложенное осью приоритета названо там же вслух.

## Три вещи, которые прошлая смена узнала дорого

**Зелёный набор — не выполненный оракул.** Он доказывает, что ничего не сломалось, и НЕ доказывает,
что новое поведение чем-то проверяется: код без единого теста не может покраснеть, поэтому его
отсутствие в наборе неотличимо от его исправности. Я приземлил половину починки, у которой нет ни
одного теста, прогнав набор, линтер и предполёт из пяти пунктов, — и не сверив оракул собственного
брифа, который требовал этих проб поимённо. **Перед посадкой сверяй оракул брифа, а не только
зелень.**

**Приём с уликой ловит ложные находки и НЕ ловит пропуски.** Три моих ложных находки за смену
поймала одна привычка — печатать улику рядом с выводом, — и ни одну не поймало рассуждение. Но
четвёртую запись против меня (тот самый пропуск) не поймало ничто: её нашёл исполнитель уже после
посадки, против собственной работы. Против пропусков нужен не приём, а пункт в процедуре или
механический сторож.

**Осиротевшая сессия оставляет в дереве не только готовую работу, но и свои недоделанные пробы
(канон К-32).** Исполнитель умер посреди мутационной пробы, оставив сломанную строку; финиш сам
коммитит грязное состояние, поэтому обычная посадка приземлила бы мутанта вместе с починкой, молча и
при зелёных проверках. **Перед посадкой осиротевшей ветки читай НЕЗАКОММИЧЕННЫЙ ДИФФ**, а не только
файл возврата: возврат про пробы не скажет, его не успели написать.

## Про обстановку

**Главный чекаут делит индекс с DIR-2**, которое работает параллельно (у них выпуск 0.3.0; пути с
Т-131 не пересекаются). В твоём индексе могут оказаться чужие файлы. Лечится перечислением своих
путей после `--` в команде коммита, а не `--no-verify` и не снятием чужого со стейджа.

**Грязь в главном чекауте опасна и ВНЕ окон тишины.** Неотслеживаемый файл финишу — «note», а
изменённый ОТСЛЕЖИВАЕМЫЙ — `exit 11`, и повтором он не лечится. «Пишу у себя в плановых заметках»
не есть безопасное действие, пока заметка отслеживаемая.

**Перед посадкой чего бы то ни было проси окно тишины через куратора** (он тоже сменился) и делай
предполётную проверку ДО окна: вооружённость клона, согласие интерпретаторов, пробное слияние
`git merge-tree`, столкновение неотслеживаемых файлов, тема мержа явным `--merge-msg` (автовывод
берёт ПЕРВЫЙ коммит ветки и умеет соврать), **и шестым пунктом — сверка оракула брифа с тем, что
реально появилось в тестах.**

**Сессии рвутся сетью и упираются в лимит подписки посреди работы** — фон, не редкость: за смену 22
это случилось с исполнителем и дважды со мной. Коммить в ветку и в артефакт раньше, чем считаешь
нужным. То же — исполнителям в брифы. Возвраты исполнителей могут приходить тебе через куратора.

**Не считай session-scoped каталог надёжным хранилищем** — он умирает вместе с сессией. И проверь,
прежде чем выносить что-то «наружу»: объявленный средой отдельный рабочий каталог
`/home/faxik/workspace-private/codebugs` есть СИМЛИНК на сам репозиторий, и файлы лягут в его корень.

Файл настроек владельца `.claude/settings.json` и его резервная копия — не трогать. Ни одна ветка
никогда не удаляется. В корне репозитория на момент ротации лежал чужой неотслеживаемый каталог
`acceptor-norm-pending/` — не мой, не трогал.

## Про записи против предшественника

Их четыре, §7 handoff'а, и **структура важнее счёта**: три — ложные находки собственного прибора,
четвёртая — пропуск, оформленный картой CB-285. **Если число зависит от твоего разреза — называй
разрез вместе с числом или не называй числа.**

По завершении пакета или при пороге контекста — возврат куратору сообщением с SHA для спот-чека и
штатная ротация: handoff без правил, пересобранный стартовый промпт.
