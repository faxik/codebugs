# Стартовый промпт — держатель направления DIR-1, смена 18

Скопировать целиком в новую сессию. **Правил здесь нет намеренно** — они в нормативных файлах, на
которые указывает handoff. Преемник стартует от источников, а не от пересказа. (Пересобран
держателем смены 17 при ротации 28.08.)

---

Ты — преемник держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий `/home/faxik/w/codebugs`. Смена 18.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-08-28-session17.md`. Затем его
§0: открой каждый нормативный источник и сверь handoff с ним; расхождение — чини handoff, не
источник. Скоропортящееся (карты, деревья, клеймы, хвост `CASCADE-IDS.md`, `git log`, состояние
`origin/main`) перепроверь на месте.

## Первое действие, до всякой работы

**Сверь очередь с `git log --first-parent` и `git worktree list`.** Уведомлению не верь. По
handoff'у: долгов приёмки нет, оба юнита смены 17 приняты и все их карты закрыты. Начинай с работы.

## Что стоит в очереди и чем она отличается от прежней

Очередь — §4 handoff'а, и она перепроверена прогоном, а не унаследована: у большинства карт там
стоит замер, снятый 28 августа, а у двух — поправки к числам, которые в прошлом handoff'е были
неверны. Самые дешёвые пункты стоят первыми и помечены нулями: у обоих форма уже выбрана или
названа замером.

Две карты очереди живут в `src/codebugs/findings.py` — на территории соседнего направления. Прежде
чем брать их, прочитай рамку над эскалацией Э-33 в §10 артефакта направления: куратор решил вопрос
границы 28 августа, но решение покрыло не всё, о чём эскалация спрашивала, и это в рамке сказано.

## Два вопроса, ждущие не тебя

**Решение 2 куратора — сплошной перечит нормативных текстов — ждёт слова владельца**, без него не
стартует. Моя поправка к области этого решения принята куратором и записана в реестре болей: замер
показал, что названные три текста у́же популяции. Числа и границы замера — в журнале направления.

**Закрывающая строка Решения 1** (охват ретроспективного прохода) написана куратором; моя половина
чисел лежит в реестре болей записью смены 17.

## Что знать про хук

Владелец подключил перехватывающую вставку: `.claude/settings.json` несёт `PreToolUse` →
`~/.claude/hooks/simplify-traced-gate.sh`; она ПРЕДУПРЕЖДАЕТ, не блокирует — финиш стартует,
остановить его надо самому, безопасно пока идёт набор `[6/7]`. Клади это в бриф исполнителя. Файл
настроек и его резервная копия — владельца, не трогать. Помни: до мержа `f41ca3c` каждая остановка
финиша писала в журнал посадок ложную удачную посадку; теперь на двух сигналах из четырёх пишется
правда, а про два оставшихся заведена карта, и она первая в очереди.

По завершении пакета или при пороге контекста — возврат куратору с SHA для спот-чека и штатная
ротация: handoff без правил, пересобранный стартовый промпт.
