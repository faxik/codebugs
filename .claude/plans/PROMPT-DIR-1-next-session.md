Преемник сессии держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий /home/faxik/w/codebugs. Сессия 11.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-08-25-session10.md`. Затем — его
§0: открой КАЖДЫЙ нормативный источник (спека каскада
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`, `CLAUDE.md` репо,
`DIR-1-trust-coordination.md`, реестр болей) и сверь handoff с ним; расхождение — чини handoff, не
источник. **Правил в этом промпте нет намеренно.**

Порядок действий — handoff §2. Скоропортящееся (исход приёмки Т-83, статусы карт, состав worktree,
состояние CI, отставание `origin/main`, хвост `CASCADE-IDS.md`) перепроверь на месте, не бери на веру.

## Единственный возможный долг, и его надо выяснить ПЕРВЫМ действием

**Юнит Т-83 (мерж `3883bb2`, карта CB-143 — экспорт в собственный stdout затирал CSV диагностикой)
приземлён, карта закрыта, приёмка была ЗАПУЩЕНА И НЕ ЗАВЕРШЕНА на момент моей ротации.** Проверь её
исход. Если вердикт вернулся отказом — коррекция идёт раньше всей очереди; если принят — запиши
неизменяемый факт приёмки в §9 артефакта направления, этого я сделать не успел.

Все остальные долги закрыты: Т-77, Т-78, Т-81, Т-82 приняты, их факты в §9 записаны.

## Что произошло с ратифицированным дизайн-юнитом, и почему это НЕ провал

Владелец ратифицировал форму «окно тишины» (CB-176 + CB-187 + CB-112 + CB-175) и потребовал
кросс-модельного ревью до кода. Ревью прогнано, и **оно отклонило форму**: два атакующих из разных
семейств моделей независимо показали три детерминированные взаимоблокировки, систематическую замену
самоустраняющегося отказа на незакрывающийся и механизм, отключавший бы очистку worktree на каждом
финише. **Гейт сработал ровно так, как задуман, — до кода, а не после.**

Редакция 2 дизайна лежит в `.claude/plans/DESIGN-DIR-1-silence-window-2026-08-25.md` и меняет ФОРМУ,
а не детали. Её §6 перечисляет, что обязан атаковать **второй круг**. Первый пункт там — измерить, а
не рассудить. **В код без второго круга не отдавать.**

## Очередь — handoff §6

Коротко: долг по Т-83 (если есть) → второй круг ревью дизайна и затем юнит → CB-158 (самая дешёвая
видимая польза, сериализовать после Т-66 у DIR-2) → CB-182 (форму я выбрал и обосновал в §6 handoff)
→ CB-192 → блок, требующий дизайн-решений (CB-172, CB-167, CB-110).

Пакеты milestones и минт-гейта закрыты владельцем — не трогать. CB-161/CB-162 у DIR-2.

## Три вещи, которых нет в нормативах и которые стоили дорого именно мне

1. **Длинный документ, который ты пишешь в отслеживаемый файл на `main`, блокирует ВСЕ worktree
   клона с первого сохранения.** Я держал редакцию дизайна незакоммиченной, пока писал её, и она
   отказала посадку моего же юнита кодом `exit 11`. Пиши такое частями с коммитом каждой части, либо
   в scratchpad с переносом готового.
2. **В брифе надо явно назвать, КТО закрывает карту юнита.** Я не назвал — приёмка отказала пункт 7
   контракта, потому что карта осталась `open`, а в соседнем брифе той же смены исполнитель закрыл её
   сам. Одно обязательство, два разных умолчания.
3. **Требуй оценку названного остатка С ОБЕИХ СТОРОН отдельным пунктом брифа.** Пять юнитов подряд
   занижали остаток, называя его правдиво в одну сторону — выгодную. Пункт, вписанный в бриф явно,
   сработал с первого раза: исполнитель сам назвал, что его правка шире буквального дефекта.

По завершении пакета или при пороге контекста — итоговый возврат куратору с SHA для спот-чека.
