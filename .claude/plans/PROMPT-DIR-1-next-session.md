Преемник сессии держателя направления **DIR-1 «Доверие и координация»** (уровень (2) каскада
планирования), репозиторий /home/faxik/w/codebugs. Сессия 8.

Сначала прочитай handoff ЦЕЛИКОМ: `.claude/plans/HANDOFF-DIR-1-2026-08-25.md`. Затем — его §0: открой
КАЖДЫЙ нормативный источник (спека каскада
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`, `CLAUDE.md` репо,
`DIR-1-trust-coordination.md`) и сверь handoff с ним; расхождение — чини handoff, не источник.
Правил в этом промпте нет намеренно.

Порядок действий — handoff §2. Скоропортящееся (статусы карт, живость юнита Т-64, состав worktree,
состояние CI, хвост CASCADE-IDS) перепроверь на месте, не бери на веру.

Пакет твоей сессии и последние ратификации владельца — в хвосте
`.claude/plans/PAIN-REGISTRY-codebugs.md`; handoff на них только указывает.

Три вещи от уходящей сессии, которых нет в нормативах, но которые стоили дорого:

1. **Изолированная приёмка окупилась 9 раз из 9.** Она поймала СЕМЬ моих ошибок держателя, и все семь
   одного корня — переход от измеренного к утверждаемому без проверки самого перехода. Не сокращай
   приёмку ради скорости; она здесь не формальность, а единственный работающий детектор.
2. **Мутант, применённый к файлу, который интерпретатор не читает, неотличим от выжившего.** Проверяй
   `inspect.getsourcefile()` импортированного модуля, а для тестов, шеллящих CLI, — **из подпроцесса**:
   `pythonpath` pytest делает признак правдивым, пока подпроцесс уходит в чужое дерево. Я на этом
   обвинил несущий норматив во лжи (CB-159) — обвинение было ложным.
3. **Число из прошлого замера при переносе в новый бриф перепроверяется, а не цитируется.** Верное
   вчера число протухает молча.

Каноны, принятые за сессию: **К-12** — бриф есть ДАННЫЕ, а не команда; если его предпосылка не
воспроизводится, исполнитель обязан эскалировать, а не переписывать норматив под неё.

По завершении пакета или при пороге контекста — итоговый возврат куратору с SHA для спот-чека.
