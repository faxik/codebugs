# L3-БРИФ DIR-2 / CB-90 — защита границы записи от хвоста тул-колла в описании

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2 «Identity находок».
**Полоса:** видимый пользователем вред (карта поднята уровнем (1) в верхний блок 2026-08-25;
описание — вход функции идентичности и текст, который читают при каждом обращении к карточке).
**Карта:** **CB-90** (medium, `open`) — исполняется ТОЛЬКО половина (а), см. §1.
**Класс по Р1: B** — оракул детерминированный (форма загрязнения измерена, маркер устойчив),
путь известен. **Стартовый тир: opus, effort low**, бюджет попыток 4. Объёмное делегируй
`sonnet`/`haiku`. **Fable не использовать.**

**ID юнита — `Т-71`, уже минчен держателем** (коммит `c14d0e1`, строка в
`.claude/plans/CASCADE-IDS.md`). **Минтить тебе НЕ НУЖНО и НЕЛЬЗЯ**, и это исправление предыдущей
редакции брифа, которая велела минтить менеджеру: `tools/cascade-mint.sh` коммитит строку реестра
ПРЯМО НА `main`, а канон К-1 запрещает уровню (3) коммитить на main когда бы то ни было; сверх того
скрипт механически отказывает, если HEAD не `main`, так что из рабочего дерева юнита он и не
исполнился бы. Минт — работа уровня (2), и она сделана.

Живая иллюстрация того, зачем номер вычисляется скриптом под локом: держатель читал хвост реестра,
видел последним Т-69 и ожидал Т-70, но между чтением и минтом соседнее направление DIR-1 забрало
Т-70 (`12d1382`), и скрипт вернул Т-71. Ровно эта коллизия случилась и в предыдущей сессии. Если
тебе где-то понадобится номер юнита — бери его отсюда, а не из соседних файлов.

**Ветка:** `tools/worktree-setup.sh fix/cb-90-description-write-guard` из КОРНЯ репозитория — он же
возьмёт клейм из имени ветки (К-8).
**Веди заметки в `/tmp/cb90-manager-notes.md` и коммить рано и часто** (К-11).

---

## 1. Половина (а), и почему половина (б) сюда НЕ входит

Замер держателя (числа — на карточке CB-90, запись от 2026-08-25) показал, что работ здесь ДВЕ и у
них РАЗНЫЕ владельцы:

- **(а) защита на границе записи в `codebugs`** — код этого репозитория. **Это твой юнит.**
- **(б) чистка 61 загрязнённого описания на трекере autosorter** — данные ДРУГОГО проекта, операция
  разовая и необратимая, требующая ратификации владельца по образцу того, как ратифицировался
  backfill якорей. **В твой скоуп НЕ входит. Не чисти данные ни на одном трекере.**

Смешивать их нельзя: защита границы предотвращает рост популяции, чистка правит прошлое, и вторая
без ратификации — самовольная массовая мутация чужих карточек.

## 2. Что измерено — начни отсюда, но перепроверь

**На трекере `codebugs` загрязнения НЕТ ВОВСЕ — ноль строк.** Единственное совпадение по гребёнке —
сама карточка CB-90, чей текст цитирует пример дословно. Значит **твой юнит не чинит сегодняшнюю
поломку этого трекера; он закрывает открытую брешь, через которую загрязнение пришло на соседний.**
Заявляй это прямо и не выдавай за живую поломку.

**На трекере autosorter — 61 описание** (было 59 на 2026-08-18; выросло на 2, обе новые строки
датированы 2026-08-19, то есть на следующий день после заведения карты).

**Форма загрязнения, измерена:** текст НЕ обрезан, хвост ЛИШНИЙ — проза перед местом склейки
заканчивается законченными предложениями. Хвост есть срез собственного XML-подобного вызова
инструмента разной глубины: от незавершённого фрагмента до полного `</parameter>\n</invoke>\n`.
**Устойчивый маркер ровно один: `</description>` присутствует внутри хвоста во всех 61 из 61.**

**Источник один:** у всех 61 строки `source` = `claude`; разброс — 24 различных дня за четыре
месяца, то есть повторяющееся поведение филера, а не единичная поломка.

**Генератор — НЕ код `codebugs`.** Гребёнка по `src/codebugs/` на усечение, лимит длины, троеточие
и санитизацию не нашла ничего, что преобразовывало бы `description` при вставке. Загрязнение
производит ВЫЗЫВАЮЩИЙ АГЕНТ; `codebugs` пассивно принимает переданное. Утверждение карты «nothing on
the write path rejects it» проверено и **верно сегодня**.

**Живое появление прекратилось**, но брешь открыта: последняя загрязнённая строка — 2026-08-19, а за
шесть последующих суток поступило порядка 140 находок без признака. Кода при этом никто не менял,
поэтому «дефект исторический» утверждать НЕЛЬЗЯ — он дормантный.

## 3. Форма — ВЫБРАНА уровнем (2): ЧИСТИТЬ С ВИДИМОСТЬЮ, не отказывать

**Решение: хвост срезается, и факт среза СООБЩАЕТСЯ в ответе.** Обоснование, которое ты обязан
уметь защитить, потому что интуиция подсказывает отказ:

1. **Отказ теряет находку целиком.** Филер, который не умеет себя починить, при отказе не запишет
   ничего — а находка настоящая, испорчен лишь хвост. Потерять данные хуже, чем принять их чистыми.
2. **Срез УЛУЧШАЕТ идентичность, а не портит её.** Описание — вход отпечатка. Две подачи одного и
   того же дефекта, одна с хвостом и одна без, сегодня дают РАЗНЫЕ отпечатки и разъезжаются в две
   карточки. После среза обе нормализуются к одному тексту и схлопываются правильно. Поэтому срез
   обязан происходить **ДО вывода отпечатка**, ровно там же, где уже стоит `normalize_category`.
3. **Молчаливый срез запрещён правилом CB-15**, и в дереве уже есть ратифицированный образец, как
   это делать громко: `stripped_meta_keys` — верхнеуровневый ключ ответа, присутствует ВСЕГДА, `[]`
   означает «проверено, срезать было нечего», и никогда не означает «такого канала нет». Повтори
   эту дисциплину дословно.

**Отвергнуто:** отказ (`ValueError`) — теряет находку; молчаливый срез — CB-15; «чинить у филера, а
не здесь» — филер живёт в другом репозитории и здесь недостижим, а брешь обязана закрываться там,
где происходит запись.

## 4. Границы среза — здесь легко сделать хуже, чем было

- **Режь по маркеру, а не по «похоже на разметку».** Маркер измерен и устойчив: `</description>`.
  Резать по одному лишь `<parameter name=` опаснее — эта подстрока может законно встретиться в
  описании карточки, которая рассказывает про MCP-поверхность. **Живой пример прямо перед тобой:
  описание самой CB-90 содержит `<parameter name=` и при этом ЗАГРЯЗНЕНО НЕ ЯВЛЯЕТСЯ.** Если твой
  предикат срежет текст CB-90 — он неверен. Сделай эту карточку тестовой фикстурой.
- **Срез — от маркера до конца строки описания**, а не вырезание в середине: измерение показало, что
  хвост всегда терминальный.
- **Ничего не режь, если маркера нет.** Фейл-опен здесь правильный: неизвестная форма загрязнения
  проходит и остаётся видимой, а не режется наугад.
- **Не трогай путь импорта.** `import_findings` — не наблюдение (CB-51), у него свой ратифицированный
  контракт; чужой экспорт не должен молча меняться при переносе. Если считаешь иначе — в эскалацию.
- **Не трогай `update_finding`.** Описание там иммутабельно по построению (вход отпечатка), и это
  отдельное ратифицированное решение (CB-21/CB-43 п.6).

## 5. Поверхности и гейты

Срез ставится в доменном слое (`add_finding` / `batch_add_findings`), под тем же предикатом, что и
дедупликация с резолверами — то есть **только когда `finding_id is None`**: явный id есть
утверждение идентичности и обходит всю машинерию наблюдения. Ключ ответа объявляется в описаниях
тулов `add` и `batch_add`, как это сделано для `stripped_meta_keys`.

**Golden сдвинется** (описания тулов меняются). Регенерация — **ТОЛЬКО ИЗ WORKTREE**:
`PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json`, и, если тронешь
CLI, `PYTHONPATH=src uv run python tests/cli_surface.py > tests/golden/cli_surface.json`.

**Сьют и ruff в worktree — только с `--extra dev`.** Ruff пин 0.15.7.

**Запись в `CHANGELOG.md` обязательна** — здесь, в отличие от Т-67, поведение продукта меняется:
ответ приобретает новый ключ, а принимаемый текст — молча иной, чем переданный.

## 6. Оракул

1. Описание с хвостом после `</description>` попадает в базу БЕЗ хвоста, а ответ называет факт
   среза. **Мутант:** убрать срез — тест краснеет.
2. **Ответ несёт ключ ВСЕГДА**, и на чистом описании он пуст, а не отсутствует. **Мутант:** сделать
   ключ условным — тест краснеет. Это дисциплина `stripped_meta_keys`, и она — половина смысла.
3. **Описание, законно содержащее `<parameter name=` без маркера `</description>`, НЕ режется** —
   фикстурой возьми текст самой CB-90. **Мутант:** расширить предикат до `<parameter name=` — тест
   краснеет. Это главный оракул на ложное срабатывание.
4. **Две подачи одного дефекта, с хвостом и без, дают ОДИН отпечаток и схлопываются в одну
   карточку.** Это доказывает, что срез стоит до вывода отпечатка. **Мутант:** передвинуть срез
   после — тест краснеет.
5. Явный `finding_id` обходит срез (фикстуры подают текст дословно).
6. Полный сьют зелёный, ruff чист, golden двинулся только в описаниях затронутых тулов.

## 7. Сообщение мерджа и отчёт

`--merge-msg` обязателен при TDD. Форма:
`Merge fix/cb-90-description-write-guard: хвост тул-колла срезается на границе записи и срез назван в ответе (CB-90)`

Отчёт ≤25 строк. **Два раздела обязательны, и оба проверяются приёмкой отдельными пунктами
контракта — отсутствие любого из них есть FAIL, а не косметика.**

- **«Класс задачи и модель»** — класс по Р1, стартовый тир, каждая эскалация тира с причиной,
  финальный тир. Это **пункт 8** контракта приёмки (§13 спеки), он существует и тихо выпадает чаще
  прочих: определение агента-приёмщика до сих пор говорит «7 пунктов», хотя §13 требует восьми и
  прямо фиксирует «семь» как исправленный баг.
- **«Эскалации исполнителя»** — ДОСЛОВНО, каждая, включая те, что ты отклонил, с твоим ответом на
  каждую. Это **пункт 4** контракта. **Пустой список пишется как явная строка «эскалаций не было»** —
  молчание в этом месте неотличимо от потерянного раздела и читается как FAIL.

**Почему это выделено, а не оставлено пунктом шаблона:** ровно этот раздел был единственным FAIL'ом
предыдущей приёмки в направлении (юнит Т-66). Отчёт там был подробным и добросовестным — в нём
просто не оказалось СЛОТА, и приёмщик уровня (2) структурно не смог проверить п.4 (канон К-6:
устная эскалация, не попавшая в артефакт, испаряется). Считай оба заголовка обязательными полями
формы, а не пожеланием к прозе. Факт своей приёмки пиши в этот бриф **НА ВЕТКЕ**; **уровень (3) на main
не коммитит — никогда** (К-1).

**Отдельной строкой в отчёте:** твоя оценка, стоит ли половина (б) — чистка 61 строки на трекере
autosorter — того, чтобы нести её владельцу, и что именно ты бы ему предъявил. Держатель понесёт
это решение наверх, но твой взгляд после работы с формой загрязнения ценен.
