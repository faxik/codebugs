# Бриф юнита Т-74 (DIR-1) — CB-117: `add_requirement` отдаёт клиенту строку, перечитанную после коммита

**Класс по Р1: B** (оракул детерминирован — тест: возвращённая строка есть та, которую записал этот
вызов, и второго чтения после коммита в функции нет). **Стартовый тир: `sonnet`, effort: medium.**
Эскалация до `opus` разрешена по признаку Р2 — назвать вслух в отчёте, пункт 4.

**Ветка:** `fix/cb-117-add-requirement-returning`. Создаётся
`tools/worktree-setup.sh fix/cb-117-add-requirement-returning`.
**Карта юнита: CB-117** (одна). **Территория: `src/codebugs/reqs.py`, `tests/test_reqs.py`.**
**Чужая живая территория, не заходить:** `src/codebugs/findings.py`, `tests/test_dedup.py`,
`tests/golden/mcp_schema.json` — там работает ветка DIR-2 `fix/cb-90-description-write-guard`.
`CHANGELOG.md` — общий файл, см. пункт 6.

**Замечание о территории, чтобы не было спора задним числом:** `reqs.py` не был записан ни за одним
направлением. Куратор поставил CB-117 в очередь DIR-1 при переприоритизации 2026-08-25 — этим он и
назначен. Названо здесь заранее и намеренно.

---

## 1. Что болит

`add_requirement` вставляет строку, **делает `conn.commit()`**, и **только потом** отдельным
`SELECT` перечитывает её из базы и возвращает вызывающему. Между коммитом и этим чтением строку
может изменить или удалить другой писатель — `update_requirement` существует и тоже открыт наружу
через MCP. Клиент получает описание, статус или приоритет, которых его собственный вызов **не
писал**, и ничто в ответе не помечает, что строка чужая.

Это дословно правило репозитория, CB-30 следствие (5): *«capture the mutated row with
`UPDATE … RETURNING *`, never re-read after the commit»*. И это тот же самый дефект, что был
приземлён как CB-111 в `merge.abandon_session`, — во втором модуле.

**Почему это не латентно.** У CB-111 был безобидный период: единственный потребитель печатал одно
неизменяемое поле, и увидеть расхождение было негде. Здесь весь словарь **всегда** уходил
MCP-клиенту, поэтому безобидного периода не было никогда.

**Второе свойство той же строки, и оно отдельное:** голый `conn.commit()` коммитит незакоммиченную
работу вызывающего, если тот держал открытую транзакцию (правило CB-24, следствие (1)). Сегодня
недостижимо — `server.py` и обработчики CLI открывают свежее соединение, — но это свойство мест
вызова, а не функции. Удаление `conn.commit()` закрывает и это.

## 2. Что делать — форма выбрана, она же названа в карте

**Ровно так:**

1. Обернуть тело функции в `with db.txn(conn):`.
2. `INSERT … RETURNING *` вместо голого `INSERT`; возвращаемый словарь построить **из курсора этого
   же INSERT**.
3. **Удалить собственный `conn.commit()` функции.** Не оставлять «на всякий случай»: `db.txn`
   отдаёт `False` под чужой открытой транзакцией, и тогда `commit()` коммитит чужую работу — ровно
   то, что чинится.

**Две ловушки, обе измерены на приземлении CB-111, переносятся дословно:**

- **После `RETURNING` `rowcount` читать нельзя никогда.** Он равен `0`, пока курсор не исчерпан, то
  есть сообщает «ничего не произошло» о записи, которая уже легла, — хуже, чем отсутствие проверки.
- **`fetchone()` обязан стоять ВНУТРИ блока `db.txn`.** Вынесенный ниже блока, он умирает с
  «cannot commit transaction - SQL statements in progress».

**Третья вещь, из правила CB-24 следствие (2):** преобразование строки в словарь (`db.row_to_dict`)
может упасть на испорченном сохранённом `meta`/`tags`. Разбирайся, где ему стоять, сознательно:
внутри блока падение откатит запись, о которой контракт уже обещал, что она легла. Если сдвигаешь
преобразование за пределы блока — скажи об этом в отчёте явно, это осознанное решение, а не деталь.

## 3. СКОУП — ровно одна функция

Карта называет соседей: `reqs.batch_add_requirements`, `reqs.import_markdown`,
`embeddings.batch_store_embeddings`, `reqs._migrate_to_lowercase`. **Не трогать ни одного.**
Карта сама говорит, что они не проверены и что `_migrate_to_lowercase` — настоящее препятствие
(коммитит неявно через `executescript`, и попытка обернуть его в `db.txn` падает). Популяция —
предмет большой задачи BT-1, а не этого юнита. Режим владельца на эту смену: минимальная нарезка.

Если по дороге увидишь у соседа тот же дефект — **не чини, заведи наблюдение в отчёт**, я решу.

## 4. Эскалации (ПОМЕЧЕННЫЙ ПУНКТ — отдельным заголовком в отчёте НА ВЕТКЕ)

Раздел с заголовком **«Эскалации»** обязателен. Пустой — тоже явный ответ: так и напиши, «эскалаций
нет». Сообщение в чат его не заменяет — приёмщик судит артефакт.

Эскалируй, не решая сам: если предпосылка брифа не сошлась с кодом (пункт 8); если `db.txn` ведёт
себя иначе, чем тут написано; если правка тянет за собой соседнюю функцию.

## 5. Честный тест

Тест обязан **краснеть на сегодняшнем коде** и зеленеть на исправленном. Оракул — не «функция
вернула словарь», а **«вернулась именно та строка, которую записал этот вызов»**.

Как это поймать, не изобретая гонку: в `tests/test_reqs.py` уже есть образец —
`RecordingConnection`, подкласс соединения, перехватывающий `execute` (шаблон SQL, до подстановки
значений). Правило репозитория говорит прямо: **утверждай про шаблон SQL, а не про исполненный
оператор**, потому что трассировка отдаёт параметры уже подставленными и отличить настоящее
присваивание от того же текста внутри значения невозможно.

Минимум:

- **Второго `SELECT` после коммита в функции нет** — проверяется по перехваченным операторам:
  после INSERT никакого отдельного чтения `requirements` не следует.
- **Возвращённый словарь есть строка этого вызова** — по значениям всех полей, которые вызов задал.
- **Собственного `conn.commit()` у функции нет** (перехват `commit`).

**Композиция, отдельным пунктом, потому что проверка элементов не проверяет их сочетание.** Три
утверждения выше — про элементы. Собери и **сочетание**: вызов `add_requirement` изнутри уже
открытой транзакции вызывающего (`db.txn` тогда отдаёт `False`) — и покажи, что чужая транзакция
**не закоммичена** этим вызовом. Это ровно то свойство, ради которого удаляется `commit()`, и без
такого теста оно остаётся утверждением. Этот пункт — предмет приёмки наравне с остальными.

**Мутационная проба (пункт 2 приёмки).** **Во временном worktree, никогда в рабочем дереве** (канон
К-5; `git archive` в scratchpad — скрипта `tools/worktree-probe.sh` в этом репозитории **нет**).
Мутант ставь **в точку дефекта**: верни `conn.commit()` и повторное чтение — тест обязан
покраснеть. `PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, и **подтверди `grep -c`, что мутант
лёг в файл**: иначе «мутант выжил» и «мутант не применён» неразличимы.

## 6. `CHANGELOG.md` — часть критерия приёмки

Ратификация владельца 2026-08-25: **каждый принятый юнит оставляет запись пользовательским языком.**

- Секция `## [Unreleased]`, подсекция `### Fixed`.
- **Коротко: 4–8 строк.** Длинные записи в файле — прежний режим, больше не образец.
- Языком того, кто пользуется: `reqs_add` возвращал строку, перечитанную из базы после записи, и в
  редком случае это могла быть уже чужая версия; теперь возвращается ровно то, что записал ваш
  вызов. Без слов «RETURNING», «транзакция», «следствие (5)».
- **Стилем и языком файла** (английский).
- **Отдельным, ПОСЛЕДНИМ коммитом ветки** — файл трогают три направления сразу.

## 7. Регламент

- **Только свой worktree.** Правка в основном чекауте — редактирование main.
- **Уровень (3) на `main` не коммитит никогда** (К-1). Свою работу пиши **в этот бриф на ветке**;
  строку в документе направления пишу я, после вердикта приёмщика.
- Тесты и линт — в worktree, **обязательно `--extra dev`**:
  `uv run --extra dev python -m pytest tests/ -q`, `uv run --extra dev ruff check src/ tests/`.
- Интеграция: `tools/worktree-finish.sh fix-cb-117-add-requirement-returning --merge-msg
  'Merge fix/cb-117-add-requirement-returning: <что изменилось> (CB-117)'`.
  **`--merge-msg` обязателен** — ветка начинается с красных тестов (TDD), и деривация субъекта
  назвала бы тест вместо предмета (К-4).
- **`exit 13` («main moved») лечится буквальным повтором той же команды**, включая `--merge-msg`;
  **`exit 11` («main dirty») повтором не лечится** — на main чужая незакоммиченная заметка. Жди и не
  трогай её: `git checkout --`, `git stash` или коммит чужого состояния сотрут чужую работу в момент
  её написания. Держится дольше нескольких минут — скажи мне.
- Признак посадки — **только `git log` на main**.
- Обязательный раздел отчёта **«Класс задачи и модель»**: класс по Р1, стартовый тир, сработавшие
  эскалации, финальный тир.
- **Голдены (`tests/golden/*`) двигаться НЕ должны** — сигнатура и поверхность инструмента не
  меняются. Если сдвинулись — это находка, эскалируй.

## 8. Моя предпосылка, объявленная ГИПОТЕЗОЙ

Я прочитала `reqs.py` глазами и вижу `conn.commit()` с последующим `SELECT … WHERE id = ?` в конце
`add_requirement`. Чего я **не** прогоняла: что `db.txn` в этом файле уже импортирован и что
`RETURNING *` даёт здесь те же колонки, что нынешний `SELECT *`. **Проверь первым действием**;
не сойдётся — эскалируй, а не подгоняй под мой текст.

## 9. Отчёт исполнителя

*(Заполняется на ветке. Обязательные разделы: «Что сделано», «Эскалации», «Названный остаток и его
оценка», «Класс задачи и модель», «Post-merge-контроль» — post-merge-гейта в репозитории нет, есть
CI-алярм `main-invariants.yml`: назови его и его результат, «n/a» без названного заменителя не
принимается.)*
