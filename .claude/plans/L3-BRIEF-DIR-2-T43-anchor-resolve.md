# L3-БРИФ DIR-2 / Т-43 — BT-7 юнит Т-b: `anchor_resolve` (чтение якоря) + `anchor_recapture`

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2 «Identity находок».
**Полоса:** ядро ратифицированной BT-7 (не механика-филлер).
**Карт у юнита НЕТ** — значит имя ветки по номеру ЮНИТА законно, и строки про клеймы этот
бриф не несёт (К-8: ветка без карт строку про клеймы не несёт вовсе).

**Класс по Р1: B** — оракул детерминированный (ратифицированный дизайн `BT-7-location-anchor.md`
v6 + сьют + грепаемые строки), путь известен НЕ полностью: спецификация `anchor_recapture`
в четырёх точках и раскладка ответа по статусам решаются на месте внутри названных границ.
**Стартовый тир: opus, effort medium**, бюджет попыток 6. Over-pin относительно таблицы Р1
записан против меня явно; основание — сессионный факт куратора «менеджеры (3) — opus» и
объём юнита. Объёмное (греп-развёртки, чтение корпуса, перечисление вызовов) делегируй
sonnet/haiku. **Fable не использовать.**

---

## 1. Что строится

**Читающая половина локационного якоря.** Пишущая половина уже стоит на main: юнит Т-39
посадил `src/codebugs/loc.py` — захват якоря в резолверном шве CB-45, грамматика §4,
инварианты Р2, закрытая таблица исключение→токен Р8. Этот юнит строит то, ради чего захват
существует: **разрешение якоря в текущую координату на HEAD**.

Нормативный источник — `.claude/plans/BT-7-location-anchor.md` **v6, РАТИФИЦИРОВАН
владельцем** (реестр болей, шестая ратификация). Твои разделы: **Р5** (каскад разрешения и
форма ответа), **Р6** (нормализатор канала B), **Р2** (инварианты объекта, проверяются НА
ЧТЕНИИ), **Р4** (санкционированный путь починки), **§9** (нарезка — что именно вменено
этому юниту), **§10** (что дизайн НЕ делает — это границы, а не долги).

**Читай v6 сам и целиком.** Ниже — не пересказ решений, а то, чего в документе нет:
границы юнита, ловушки, вменённые тесты и каноны.

---

## 2. Предпосылки — спот-чекни ВСЕ; расхождение = СТОП и доклад

Я проверил каждую лично на `main` перед написанием брифа (К-7).

- **`loc.py` не выпускает НИ ОДНОГО SQL и не владеет схемой.** В файле ровно одна
  регистрация — `db.register_pre_add_resolver("loc.capture", …, meta_keys=("loc",),
  updatable_keys=("loc",))`; `register_tool_provider`, `register_cli_provider`,
  `register_schema` в нём отсутствуют. **Это ратифицированное свойство модуля, а не
  случайность** (CLAUDE.md: «the package's FIRST self-registering non-domain module —
  legal because it issues ZERO SQL»). Юнит обязан его СОХРАНИТЬ.
- **Читающей поверхности не существует:** `grep -rn 'anchor_resolve\|resolve_anchor\|loc_resolve' src/`
  даёт ноль. `read_anchor` определена и не вызывается ниоткуда — она ждёт именно этот юнит.
- **Публичный path-API из Т-37 стоит и уже потребляется:** `provenance.worktree_root(*, project_dir=None)`
  и `provenance.resolve_in_worktree(*, root, file_path)` (плюс `WorktreePath` с полями
  `root`/`rel`/`abspath`). `loc.capture` зовёт их обеих. `_resolve_candidate` остаётся
  приватной. **Структурный тест `tests/test_loc.py` уже пинит, что `loc` зовёт
  `worktree_root(project_dir=project_dir)`, а НЕ форму с ambient-cwd** — не сломай этот пин.
- **Константы `loc.py`:** `ANCHOR_VERSION = 2`, `NORM_VERSION = "v1"`, `MAX_ANCHOR_LINES = 5`,
  `MAX_TEXT_BYTES = 2048`, `MIN_ANCHOR_CHARS = 16`, `CAPTURE_BUDGET_S = 2.0`.
  **`CONTEXT_LINES` НЕ СУЩЕСТВУЕТ** — v6 Р6 называет `CONTEXT_LINES = 8` как параметр канала B,
  и его ещё предстоит завести ЗДЕСЬ (контекст по построению не хранится в объекте, он
  вычисляется на чтении). **`MIN_ANCHOR_CHARS` = 16, а не 24** — Т-a исполнил вменённый ему
  пересчёт; v6 §Р6 несёт старое число, и это буква, приведённая к смыслу посадкой. Не
  «чини» её обратно.
- **Словари токенов уже закрыты:** `REASONS` (21 токен, включая `commit_not_ancestor`,
  `repo_mismatch`, `verify_mismatch`, `unsupported_anchor_version`, `invalid_anchor`,
  `retracted`) и `CAPTURE_REASONS` (14 — подмножество, достижимое на захвате). **Читающие
  токены, которых нет в `CAPTURE_REASONS`, существуют ровно для этого юнита.** Новый токен
  заводится ТОЛЬКО дописыванием в `REASONS`; свободной строкой причина не отдаётся никогда.
- Тесты якоря живут в `tests/test_loc.py` — туда же идут твои.

---

## 3. Обязательные свойства (каждое названо в v6; отсутствие = провал приёмки)

### 3.1 Каскад Р5 — порядок несущий, и первый шаг не косметический

`0. гейт предка → 1. канал A → 2. ОБЯЗАТЕЛЬНАЯ сверка → 3. канал B → 4. отказ`.

- **Гейт предка идёт ПЕРВЫМ, до всякого трейса.** `git merge-base --is-ancestor <commit> HEAD`.
  Без него штатный процесс ЭТОГО репозитория (карточка заводится на невлитой ветке — здесь
  так заводится каждая) даёт **ТИХИЙ ЛОЖНЫЙ «потеряно»**: `blame --reverse` возвращает код 0
  и пустой stderr и атрибутирует строку коммиту ветки. Ответ — `unknown(commit_not_ancestor)`,
  состояние временное, снимается мержем ветки. **Это находка структурной атаки по v5, и она
  единственная в списке, которая ломает не редкий, а ОБЫЧНЫЙ случай.** Тест обязателен.
- **Канал A:** `git blame --reverse -p -C -C -L line,end <commit>..HEAD -- <path>`, с
  `-c core.quotePath=false`. Живые строки — те, чей sha равен HEAD.
  **`-C -C` — не украшение:** на живой истории `-C` ловит 1 переезд, `-C -C` — 2. Цена
  измерена и не взрывается.
  **`core.quotePath=false` обязателен:** по умолчанию git C-квотирует не-ASCII пути в
  porcelain, и этот же дефолт репозиторий уже лечил трижды.
  **Путь берётся из записи `filename` вывода blame, а НЕ из записанного пути.** Это ровно
  та деталь, чья подмена дала держателю фантомные «git ошибается в 0.21%»: сверка по
  записанному пути объявляет ПЕРЕЕЗД ошибкой. Исправленно — 537/537 побайтно.
- **Сверка не пропускаемая и читается по РАЗРЕШЁННОМУ пути** (`HEAD:<путь из filename>`).
  Частично выживший пролёт определён явно: сверяется каждая дожившая строка; ответ выдаётся,
  если сошлись ВСЕ дожившие, и несёт `survived: <n>/<m>`. Ни одной дожившей — `lost`.
  Часть сошлась, часть нет — `verify_mismatch`.
  **Честная граница, которую обязан знать и ты, и докстринг:** сверка отсекает грубую ошибку,
  а НЕ доказывает правильность — 50% (codebugs) и 35% (autosorter) верно разрешённых строк
  имеют текст, не уникальный внутри своего файла на HEAD.
- **Канал B** (только если A не ответил): нормализовать `text` по `norm` и искать в
  `HEAD:<записанный путь>`; несколько кандидатов различать контекстом (`CONTEXT_LINES = 8`).
- **Инварианты Р2 проверяются НА ЧТЕНИИ, и нарушение даёт `unknown(...)`, НИКОГДА исключение.**
  Причина структурная: restore хранит meta дословно, а `updatable_keys` не валидирует ничего
  (§10 п.5), поэтому вручную собранный `loc` — достижимое состояние. `read_anchor` уже
  реализует эту половину — используй её, не пиши вторую.

### 3.2 Форма ответа

Ровно как в v6 Р5: ключи `status` (`current | moved | moved_file | lost | ambiguous | unknown`),
`path`, `line`, `end`, `channel` (`"git" | "content" | null`), `reason`, `resolved_against`.

- **`channel` присутствует ВСЕГДА** — читателю важно, каким механизмом получен ответ.
- **`moved_file` — отдельный статус, а не «moved с другим путём».** Якорь может разрешиться
  в ДРУГОЙ файл, и потребитель обязан это ВИДЕТЬ, а не получить номер строки в файле, о
  котором не спрашивал.
- Ключи ответа безусловны (`null` — нормальный ответ, «оценено, нечего сказать»); прецедент —
  `claims._response`, у которого все пятнадцать общих ключей всегда на месте, и
  `attention`-блок BT-5, где `[]` значит «оценено, ничего не сработало», а не «канала нет».

### 3.3 Сводка статусов — это ратифицированный СЧЁТЧИК СПРОСА, а не удобство

Шестая ратификация владельца по `moved_file`: «сейчас карточка остаётся на старом `file`,
якорь отвечает видимым статусом `moved_file` с новым путём/строкой; явный санкционированный
ре-кей — отдельным решением по замеренному спросу; **счётчик спроса — частота `moved_file`
в живых данных, DIR-2 закладывает его читаемым**, чтобы решение было арифметикой».

Отсюда: батч-контракт `anchor_resolve` (по образцу `check_findings`) обязан возвращать
**сводку по статусам** — сколько строк популяции пришло в каждый статус, с той же закрытой
вокабулярностью. Владелец прочитает из неё частоту `moved_file`.
**Число уйдёт владельцу, значит несёт свой предикат (К-5б):** докстринг обязан сказать, ЧТО
именно знаменатель — «строки, у которых ЕСТЬ якорь», а не «все карточки», иначе доля
`moved_file` будет посчитана от популяции, к которой она не относится.

### 3.4 `anchor_recapture` — специфицирован в ЧЕТЫРЁХ точках, все четыре в код и в тесты

v6 §9 перечисляет их поимённо, и «специфицирован» значит: поведение выбрано, записано в
докстринге и закреплено тестом. Точки:

1. **Читает ли внутри транзакции.**
2. **Заменяет ли НЕУДАЧА валидный прежний якорь — НЕТ.** Это единственная из четырёх, у
   которой ответ уже дан дизайном; остальные три выбираешь ты внутри границ.
3. **Перебивает ли тумбстоун (`loc: null` = «не перезахватывать») — только по ЯВНОМУ флагу.**
4. **Сверяется ли с версией строки** перед перезаписью.

**Зачем этот верб вообще:** Р4 называет его САНКЦИОНИРОВАННЫМ ПУТЁМ ПОЧИНКИ. `updatable_keys`
открывает `loc` на update и не валидирует НИЧЕГО, поэтому руками собранный мусор будет
принят на записи и прочитан как `unknown(invalid_anchor)`. Починка — это верб, который
строит объект САМ, а не человек, набирающий JSON.

### 3.5 Чего НЕ вводить — решение v4, сохранённое v6

`get(resolve_anchor=)` и `staleness_check(anchor=)` **не вводятся**: первое заставило бы ядро
знать имена ключей расширения, второе дало бы два разных корня в одной записи ответа.

---

## 4. Границы — узкие, и одна из них про чужое измерение

### 4.1 `loc.py` НЕ приобретает SQL

Разрешение читает строки находок. **Аксессор строк живёт в `findings.py`** — по образцу
уже существующего `findings.similarity_candidates` (сырые строки, `meta_json` как хранимая
СТРОКА по следствию 4 CB-24, детерминированный `ORDER BY`). Никакой другой модуль не
SELECT-ит из `findings`. Если по дороге захочется положить один невинный `SELECT` в `loc.py`
— это отмена ратифицированного свойства модуля, СТОП и доклад.

### 4.2 Golden ЗАНЯТ соседним направлением — и цена не только твоя

`anchor_resolve` и `anchor_recapture` — новые MCP-тулы, значит `tests/golden/mcp_schema.json`
сдвинется. **Одновременно DIR-1 ведёт второй пилот генератора поверхностей на `sweep.py`
(юнит Т-42), и метрика этого пилота — БАЙТ-РАВЕНСТВО поверхности до/после.** Твой новый тул,
приземлившийся в середине их измерения, добавляет разницу в поверхность, которую их
приёмщик обязан объяснить.

Поэтому порядок работ жёсткий:

1. Строй **движок + CLI-верб + тесты** — golden при этом НЕ двигается ни на байт. Это
   основная масса юнита.
2. **Регистрация MCP-тулов и регенерация golden — ПОСЛЕДНИМ коммитом ветки.**
3. **Перед этим коммитом** проверь `git worktree list` и хвост `.claude/plans/CASCADE-IDS.md`:
   если дерево юнита Т-42 (DIR-1, `sweep.py`) ещё живо — **polling раз в ~2–3 минуты**, не
   стэшить, не торопить, не обходить. Приземляйся, когда их дерево снято.
4. Регенерация — ровно командой из CLAUDE.md:
   `PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json`
   **из worktree**. Голый `python` из worktree резолвит `codebugs` через editable-install,
   указывающий на main, и снимет НЕ ТО ДЕРЕВО.
5. Если форвард-мердж всё же дал конфликт по golden — **разрешай РЕГЕНЕРАЦИЕЙ, никогда
   текстовым слиянием**: это порождённый артефакт, и любая ручная склейка его лжёт. После
   регенерации покажи в факте приёмки, что дифф golden содержит ТОЛЬКО ожидаемые добавления.

### 4.3 Прочие границы

- **НЕЛЬЗЯ:** `tools/`, `.github/`, `sweep.py`, `bench.py` (территория пилота DIR-1),
  `types.py`, `CLAUDE.md` (кроме случая, когда правило репозитория стало ЛОЖНЫМ),
  `.python-version`.
- **`findings.py` трогает параллельно мой же юнит Т-41** (CB-144 + счётчик отказов) — в
  `_cmd_add` и `update_finding`. Твоя правка там — только новый аксессор. Конфликт
  форвард-мерджа вероятен у конца `tests/test_findings.py`; разрешается в worktree,
  НИКОГДА коммитом разрешения на main.
- **Backfill (юнит Т-c) в этот юнит НЕ входит.** Он идёт следующим и обязателен, dry-run по
  умолчанию. Не прихватывай.

---

## 5. Тесты — что обязано быть покрыто

- **Гейт предка** — карточка на невлитой ветке даёт `unknown(commit_not_ancestor)`, а НЕ
  `lost`. Фикстура обязана строить настоящую невлитую ветку, а не имитировать её.
- **Переезд файла** — разрешение в другой файл даёт `moved_file` с новым путём. Помни
  измеренное: контролируемый split НЕ воспроизводится ни на одном пролёте ≤3 строк, поэтому
  фикстура с пролётом в 2 строки будет зелёной при сломанном `-C -C`. Пролёт 4–5.
- **Сверка** — три исхода порознь: все дожившие сошлись (ответ + `survived: n/m`), ни одной
  дожившей (`lost`), часть разошлась (`verify_mismatch`).
- **Канал B** — отвечает там, где A не ответил; `channel` это показывает.
- **Инварианты на чтении** — мусорный `loc`, записанный через `update_finding(meta_update=)`,
  даёт `unknown(invalid_anchor)`, а не исключение; незнакомая версия — `unsupported_anchor_version`;
  тумбстоун — `retracted`.
- **`repo_mismatch`** — карточка, чей `repo` не совпадает с деревом разрешения.
- **Все четыре точки `anchor_recapture`** — по тесту на точку; п.2 (неудача не затирает
  валидный якорь) обязателен.
- **Сводка статусов** — знаменатель именно тот, что обещан докстрингом.
- **Контрольные случаи, которые ДОЛЖНЫ проходить** — иначе набор односторонний и его пройдёт
  реализация, отвечающая `unknown` на всё: обычная живая строка даёт `current` через канал
  `git`; строка, сдвинутая правкой выше по файлу, даёт `moved` с новым номером.
- **Часть новых тестов КРАСНАЯ до реализации — покажи ЧИСЛОМ.**
- **Мутационная проба во ВРЕМЕННОМ worktree** (`git worktree add --detach` → проба →
  `remove --force`), НИКОГДА в рабочем дереве; изоляцию источника напечатай ДО пробы
  (вакуозная проба уже стоила репозиторию раунда). Обязательные мутанты:
  - снят гейт предка (должен упасть тест невлитой ветки);
  - `-C -C` → `-C` (должен упасть тест переезда);
  - сверка читает ЗАПИСАННЫЙ путь вместо разрешённого (должен упасть тест переезда/сверки);
  - снят `core.quotePath=false` (нужна фикстура с не-ASCII путём — если её нет, скажи это
    вслух, а не тихо пропусти мутанта).

---

## 6. Каноны (нарушение = провал приёмки §13)

1. **К-1. Уровень (3) на main НЕ коммитит — никогда.** Факт приёмки (3)←(4) — в ЭТОТ файл
   НА ВЕТКЕ. Строку в документе направления пишу я и только после своего вердикта.
2. **К-2. Минт — только `tools/cascade-mint.sh`.** ID этого юнита уже сминчен (Т-43).
   **Знай ловушку, которую я сам допустил час назад:** скрипт защищает НОМЕР, но не
   защищает поля, ПРОИЗВОДНЫЕ от номера, если они набраны в `--text` литералом — строка
   Т-43 несёт неверное имя брифа, и поправка дописана отдельной строкой реестра.
3. **К-3. Файлы на main добавляются ПОИМЁННО** (`git add -- <path>`), никогда каталогом.
4. **К-4. `--merge-msg` ОБЯЗАТЕЛЕН.** Предлагаемый:
   `Merge feature/t43-anchor-resolve: a stored location anchor now resolves to a current line on HEAD, with an ancestor gate, a mandatory verify and a moved_file status (BT-7 Т-b)`
   Напоминание, почему это не формальность: деривация субъекта берёт ПЕРВЫЙ коммит
   первопарентной линии ветки, и при TDD это «red tests».
5. **Факт приёмки цитирует ГРЕПАЕМОЕ СОДЕРЖАНИЕ, а не номер строки.**
6. **Слот эскалаций исполнителя (4) переносится СЮДА ДОСЛОВНО**; пусто — явно «ПУСТО» с
   причиной. Устная эскалация, не попавшая в артефакт, испаряется.
7. **К-7. Предписание брифа — тоже предпосылка**, добывается из кода. В §2 я уже назвал два
   места, где ратифицированный документ разошёлся с приземлённым кодом (`MIN_ANCHOR_CHARS`,
   `CONTEXT_LINES`). Найдёшь третье — докладывай, это ожидаемо.
8. **К-9. Класс по Р1 и стартовый тир** — §0. В факте приёмки зафиксируй ФАКТИЧЕСКИЙ путь
   модели: класс, стартовый тир, число эскалаций с внешним признаком Р2, финальный тир.
9. **Э-4. Клеймит ХАРНЕС**, пре-клейм направлением запрещён. У этого юнита карт нет, поэтому
   `worktree-setup.sh` честно скажет `branch names no card — nothing to claim` — **это
   ожидаемый вывод, а не отказ.**
10. **§13 п.6: post-merge-контроль.** В этом репозитории post-merge-gate'а нет — назови
   заменяющий механизм и его результат (CI-алярм / прогон сьюта на main после мерджа).
   «n/a» без названного заменителя не принимается.

---

## 7. Харнес

- `tools/worktree-setup.sh feature/t43-anchor-resolve` — из КОРНЯ репо.
- Работа только в `.worktrees/feature-t43-anchor-resolve`.
- `uv run --extra dev python -m pytest tests/ -q` — **`--extra dev` не опционален в worktree**.
- `uv run --extra dev ruff check src/ tests/`; ruff **0.15.7**.
- Интерпретатор пинован `.python-version`; `worktree-finish.sh` ОТКАЗЫВАЕТ при расхождении
  версий worktree/main. **ЗАПРЕЩЕНО**: `uv run --python`, `uv sync`, пересборка `.venv`.
  Эксперименты — в `/tmp`.
- Финиш: `tools/worktree-finish.sh feature-t43-anchor-resolve --merge-msg '…'` из корня.
- **Блокировка finish'а чужим staged-WIP на main:** НЕ стэшить чужое, НЕ ждать пробуждения —
  polling раз в ~60 с, затем повтор finish тем же `--merge-msg`.

---

## 8. Слот эскалаций менеджера (3) → держателю (2)

### СТРУКТУРНОЕ — меняет ратифицированное решение или шов между решениями

**ПУСТО.** Ни одно ратифицированное решение и ни один шов между решениями этим юнитом не
задет. Единственный кандидат — вопрос про `-C -C` — снят с этого слота держателем (2) как его
собственный (см. Н-1); флаг оставлен как ратифицировано.

### ДЕТАЛЬ — решено на месте, названо вслух

**Д-1. Ключ `survived` — v6 Р5 требует его и НЕ несёт в схеме ответа.** Шаг 2 предписывает
«ответ … несёт `survived: <n>/<m>`», а литерал записи ответа четырьмя абзацами ниже
перечисляет только `status/path/line/end/channel/reason/resolved_against`. Внутреннее
расхождение документа. Заведён БЕЗУСЛОВНЫМ ключом по канону §3.2 (`null` — нормальный ответ).

**Д-2. `resolved_against` несёт `mtime_ns`/`size` — факты РАБОЧЕГО ДЕРЕВА, тогда как v5→v6
перенесла всё чтение в хранилище объектов.** Набор ключей ратифицирован, поэтому не тронут;
заполняется `os.stat` по РАЗРЕШЁННОМУ пути в чекауте, `null` когда файла там нет. Смысл,
который это сохраняет: разрешение читает git, а человек, получивший номер строки, открывает
рабочее дерево — и единственное, чего он не видит из одного `head`, это разошёлся ли файл
перед ним с коммитом.

**Д-3. У статуса `ambiguous` нет токена в закрытом словаре §4.3.** Отдаётся `reason: null`;
токен не минчен, потому что §2 брифа разрешает заводить токен только дописыванием в `REASONS`,
а санкции на это у юнита нет. Статус сам себя объясняет.

**Д-4. Якорь БЕЗ поля `repo` достижим — `read_anchor` его не валидирует.** Не расширял
контракт Т-a; разрешение отказывает `repo_mismatch` (fail-closed): единственная работа поля —
не допустить УВЕРЕННО НЕВЕРНОГО ответа, значит «не могу проверить» есть отказ.

**Д-5. `anchor_recapture` читает вне транзакции, а сверяет и пишет — внутри одной.** Точка 1
из четырёх решена этим РАЗДЕЛЕНИЕМ, а не «да/нет»: захват тратит до `CAPTURE_BUDGET_S` на
строку в подпроцессах, и удержание лока через батч перевалило бы за `busy_timeout=5000`
конкурирующего писателя после третьей строки — а `SQLITE_BUSY` не классифицируется
`db._is_environmental`, то есть тот писатель получит сырой traceback (§7.3 называет это
ценой). Побочный выигрыш: точка 4 стала настоящим CAS, а не check-then-act с окном в две
секунды.

**Д-6. Дефолт `anchor_recapture` — dry-run**, по образцу `findings.normalize_categories`.
Бриф этого не вменял; форма выбрана как уже принятая в репозитории для массовых скрабов.

**Д-7. Имена MCP-тулов `anchor_*` при модуле `loc`** — префикс называет ОБЪЕКТ, как их
называет §9 дизайна и все раунды ревью. `loc_` совпал бы с именем файла и не совпал бы ни с
чем, что читатель дизайна видел. Записано в докстринге `register_tools` как исключение.

### НАЙДЕНО, НЕ ЧИНЮ — вне скоупа, докладываю

**Н-1. Различающей фикстуры `-C` против `-C -C` на `blame --reverse` НЕ СУЩЕСТВУЕТ —
измерено, причина структурная.** Оба флага прогнаны на контролируемом split и четырёх его
вариантах (двух-коммитный split; split + отслеживаемый rename; split + `cp`+`git rm`;
приёмник создан коммитом, НЕ трогающим источник) — вывод **побайтно идентичен во всех**.
Причина специфична для НАПРАВЛЕНИЯ обхода: дополнительный скоуп `-C -C` — копии из файлов,
которые коммит СОЗДАНИЯ приёмника не изменял; при обратной трассировке ИСТОЧНИКА источник по
построению изменён в коммите, удалившем строки, значит обычного `-C` уже достаточно.
Различитель существует, но только ВПЕРЁД и на файле-НАЗНАЧЕНИИ (`-C` атрибутирует поднятый
блок коммиту split'а, `-C -C` — исходному), и развернуть его нельзя: reverse blame отказывает
на пути, которого нет в начале диапазона (`fatal: no such path split_out.py in <C1>`, rc 128),
и rename/copy-детекции через эту границу не делает.

**Мутант `-C -C` → `-C` поэтому НЕ заявлен убитым** — по прямому решению держателя (2).
Вакуозный «kill» здесь был бы хуже отсутствия мутанта: это ровно тот дефект («считалось не то,
что утверждалось»), за который направление уже платило раунд. Структурный пин
`TestBlameInvocation` заведён и в его докстринге сказано прямо, что он НЕ является
поведенческим убийцей и на этом основании мутанта убитым объявлять нельзя.
**Флаг в реализации НЕ ТРОНУТ**: `-C -C` остаётся как ратифицировано; оправдан ли он в
реверс-режиме — вопрос держателя против записи замера v6, не мой.

**Н-1б. Отказ канала доставки.** Отчёт измерительного субагента до меня не дошёл напрямую
(`SendMessage` не разрешил имя) и пришёл через куратора к держателю. Урок принят и исполнен:
измерение положено в артефакт на ветке (докстринг `TestBlameInvocation` + эта строка) тем же
ходом, а не оставлено в переписке.

**Н-1в. Канал B МАСКИРУЕТ сломанный канал A, и это свойство каскада, а не тестов.** Найдено
мутационной пробой: со снятым `core.quotePath=false` канал A получает C-квотированный путь,
корректно падает на сверке, и канал B отвечает тем же `current`. Ратифицированное свойство
дизайна — «два независимых механизма согласны 133 раза из 133» — ровно то, что делает
запасной канал маской для этого класса дефектов. В коде не чиню (каскад ратифицирован);
починил в НАБОРЕ: каждый тест канала A утверждает свой `channel`. Стоит знать при чтении
любых будущих цифр `lost`-rate.

**Н-2. Т-c (backfill) обязателен и в этот юнит не входит** — вменено брифом §4.3, не трогал.

---

## 9. Слот эскалаций исполнителя (4) → менеджеру (3), ДОСЛОВНО

**ПУСТО.** Причина: юнит исполнен менеджером (3) лично, без диспетчеризации исполнителя (4).
Уровень (4) не создавался — на sonnet ушла только объёмная ИЗМЕРИТЕЛЬНАЯ работа (построение
git-фикстур и снятие формата вывода `git blame --reverse`), которая по §16.3 является
делегированием тира, а не юнитом уровня (4), и потому своего слота эскалаций не имеет. Её
результат вошёл в артефакт напрямую (Н-1 в §8 и докстринг `TestBlameInvocation`) и
перепроверен мной исполнением.

---

## 10. Факт приёмки (3)←(4)

### Предпосылки §2 — перепроверены ЛИЧНО на приземлённом коде

| предпосылка | как проверена | результат |
|---|---|---|
| `loc.py` не выпускает SQL, одна регистрация | грепаемо: в файле ровно `db.register_pre_add_resolver("loc.capture", …)` | ✓ и СОХРАНЕНО — аксессор ушёл в `findings.anchor_candidates`; `TestZeroSql` (AST по узлам вызова) зелёный на новом коде |
| читающей поверхности нет | грепаемо: `anchor_resolve\|resolve_anchor\|loc_resolve` в `src/` | ✓ ноль до юнита |
| `read_anchor` без вызывающих | грепаемо | ✓ теперь вызывается из `resolve_findings`, вторая половина не написана |
| path-API Т-37 стоит | грепаемо: `def worktree_root(*, project_dir`, `def resolve_in_worktree(*, root, file_path` | ✓ |
| пин «`loc` зовёт `worktree_root(project_dir=project_dir)`» | прогон `tests/test_loc.py` | ✓ не сломан |
| константы (`ANCHOR_VERSION=2`, `NORM_VERSION="v1"`, `MAX_ANCHOR_LINES=5`, `MAX_TEXT_BYTES=2048`, `MIN_ANCHOR_CHARS=16`, `CAPTURE_BUDGET_S=2.0`) | грепаемо | ✓ все шесть совпали, включая `MIN_ANCHOR_CHARS=16` (не 24) |
| `CONTEXT_LINES` не существует | грепаемо | ✓ заведена здесь = 8 |
| `REASONS` 21 токен, `CAPTURE_REASONS` 14 | грепаемо | ✓ ни один токен не добавлен |

**Третье расхождение дизайна с кодом (К-7) найдено — их два, Д-1 и Д-2 выше.** Оба —
внутренние расхождения самого v6, а не последствия посадки: Р5 требует ключ `survived`,
которого нет в его же литерале записи ответа; `resolved_against` несёт два поля рабочего
дерева после того, как v5→v6 убрала чтение с диска. Оба приведены к смыслу, ратифицированный
набор ключей не сужен.

### Каскад в том виде, в каком реализован

`repo`-гейт → `rev-parse --verify <commit>^{commit}` (+ проба `--is-shallow-repository` на
неразрешимом коммите) → **гейт предка** `merge-base --is-ancestor` → **канал A**
`git -c core.quotePath=false blame --reverse -p -C -C -L line,end <commit>..HEAD -- <path>`
→ **обязательная сверка** по РАЗРЕШЁННОМУ пути (`HEAD:<filename из porcelain>`), побайтно,
пролёт группируется по путям (split кладёт пролёт в два файла) → **канал B** (нормализация
`norm`, поиск в `HEAD:<записанный путь>`, разведение кандидатов контекстом `CONTEXT_LINES=8`
с ОБЕИХ сторон из хранилища объектов) → классификация неудавшегося blame через `read_blob`.

`repo`-гейт стоит ПЕРЕД гейтом предка намеренно: он единственный защищает от УВЕРЕННО
НЕВЕРНОГО ответа, остальные — от отсутствующего.

**Формат porcelain перепроверен исполнением** (git 2.53.0): заголовок
`<sha> <строка-в-HEAD> <строка-в-исходной> [<count>]` — поле 2 это HEAD. Запись `filename`
эмитится ОДИН РАЗ на коммит (штатная де-дупликация porcelain), поэтому парсер её мемоизирует
по sha, а `filename`, встреченный внутри записи, всегда перебивает мемо — иначе split
схлопнулся бы на путь, пришедший первым. Премиса П4б подтверждена дословно: `blame --reverse`
с невлитой ветки даёт **rc=0, пустой stderr** и атрибуцию коммиту ветки.

### Четыре точки `anchor_recapture` — как решены и почему

| точка | решение | основание |
|---|---|---|
| 1. читает ли внутри транзакции | **РАЗДЕЛЕНО**: скан и захват — вне транзакции, сверка версии и запись — внутри ОДНОЙ `db.txn` | §7.3: батч захватов под локом перевалит `busy_timeout=5000` после третьей строки, а `SQLITE_BUSY` не классифицируется и выйдет сырым traceback'ом. Побочно делает точку 4 настоящим CAS |
| 2. заменяет ли НЕУДАЧА валидный якорь | **НЕТ** — исход `kept`. Расширено на тумбстоун (см. ниже) | дано дизайном; отказ обычно про СРЕДУ, а якорь, который он затрёт, исправен в клоне с историей |
| 3. перебивает ли тумбстоун | только по `force_tombstone=True`; иначе исход `tombstoned` | `loc: null` пишется РУКОЙ именно для этого; массовый скраб, сметающий его молча, сделал бы тумбстоун ненаписуемым на практике |
| 4. сверяется ли с версией строки | **ДА**, CAS по ЗНАЧЕНИЮ якоря внутри транзакции точки 1; исход `stale` | сверка по значению, а не по timestamp строки: посторонняя запись статуса в том же окне не должна стоить починки |

### Числа

| измерение | значение |
|---|---|
| новых тестов КРАСНЫХ до реализации | **48** (прогон `tests/test_loc.py` в отдельном detached-worktree на базе ветки `f52651c`; изоляция напечатана до прогона: `src/` идентичен базе, `def resolve_findings\|def recapture_findings` в `src/` = 0) |
| тех же тестов ЗЕЛЁНЫХ после | **122** (файл целиком; из них 72 теста Т-a на базе уже были зелёными). 120 на момент замера «красных», +2 пина регистрации MCP в последнем коммите |
| полный сьют в worktree | **2281 passed**, 0 failed, 103.0 s (2248 до форвард-мерджа, принёсшего 33 теста Т-42) |
| сьют на базе ветки `f52651c` | 2200 (2199 passed + 1 failed — пин Т-a «loc не регистрирует провайдеров», ИНВЕРТИРОВАННЫЙ этим юнитом, а не удалённый) |
| `ruff check src/ tests/` | All checks passed (ruff 0.15.7) |
| дифф `tests/golden/mcp_schema.json` | **+160 строк, −0**, один непрерывный хунк; тулов 77 → 79; добавлены ровно `anchor_recapture`, `anchor_resolve`; удалённых нет |

### Мутационная проба

Во ВРЕМЕННОМ detached-worktree (`git worktree add --detach` → проба → `remove --force`),
никогда в рабочем дереве. Изоляция напечатана ДО первого мутанта: путь дерева, совпадение
SHA с веткой, md5 `loc.py` в обоих, различие inode. Каждый мутант проверяется на
НЕВАКУОЗНОСТЬ (`git diff --quiet` после правки) и откатывается перед следующим.

| # | мутант | ожидаемый убийца | итог |
|---|---|---|---|
| M1 | снят гейт предка | `TestAncestorGate::test_a_card_filed_on_an_unmerged_branch_is_not_reported_lost` | **убит** (1 failed) |
| M2 | `-C -C` → `-C` | — | **ПЕРЕЖИЛ, и это законный результат, а не пропуск.** Различающей фикстуры на `blame --reverse` не существует — измерено на пяти формах, структурная причина названа (Н-1). Убитым НЕ заявляю по решению держателя (2). Структурный пин `TestBlameInvocation` мутанта ловит, но поведенческим убийцей не является и в его докстринге это сказано |
| M3 | сверка читает ЗАПИСАННЫЙ путь | `TestMovedFile` + `TestVerify::test_the_verify_reads_the_resolved_path_not_the_recorded_one` | **убит** (3 failed) |
| M4 | снят `core.quotePath=false` | `TestNonAsciiPaths` (фикстура с не-ASCII путём ЕСТЬ, мутант не пропущен) | **пережил первый прогон**, см. Н-1 — канал B маскировал; после усиления теста (`channel == "git"`) **убит** |
| M5 | номера строк porcelain транспонированы | `TestChannelAControls` | **убит** (2 failed) |
| M6 | снята точка 2 recapture | `TestRecaptureFourPoints::test_point2_…` | **убит** (1 failed) |
| M7 | снята точка 4 recapture | `TestRecaptureFourPoints::test_point4_…` | **убит** (1 failed) |
| M8 | знаменатель сводки = `total` | `TestBatchSurface::test_the_summary_denominator_…` | **убит** (4 failed) |

**M4 — самый ценный результат пробы, и он против меня:** тест, читавший только `status`,
пропускал мутанта, потому что запасной канал давал тот же ответ. Ровно то, ради чего проба
вменена.

### Границы юнита — что НЕ тронуто

`tools/`, `.github/`, `sweep.py`, `bench.py`, `types.py`, `CLAUDE.md`, `.python-version` —
ноль правок (проверяемо диффом ветки). В `findings.py` — ровно ОДИН новый аксессор
`anchor_candidates`, ничего больше; `_cmd_add` и `update_finding` (территория Т-41) не
тронуты. Backfill (Т-c) не прихвачен. `get(resolve_anchor=)` и `staleness_check(anchor=)`
не введены (§3.5).

**Сериализация golden исполнена по §4.2:** движок, CLI-верб и тесты приземлились тремя
коммитами при НЕПОДВИЖНОМ golden (проверено `git diff --quiet HEAD -- tests/golden/` после
каждого); регистрация MCP и регенерация — последним коммитом, после того как дерева Т-42 не
стало в `git worktree list` (polling ~2.5 мин). Регенерация — ровно командой из CLAUDE.md
`PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json` ИЗ
worktree.

### Путь модели (К-9)

- **Класс по Р1:** B (оракул детерминированный, путь известен не полностью).
- **Стартовый тир:** opus, effort medium; бюджет попыток 6.
- **Эскалаций с внешним признаком Р2:** **0.** Финальный тир — opus, стартовый.
- **Делегировано вниз (§16.3):** одна измерительная задача на sonnet — построение git-фикстур
  и снятие формата вывода `git blame --reverse -p` (порядок полей, де-дупликация `filename`,
  коды возврата `merge-base --is-ancestor`, подтверждение П4б, и семь форм для `-C` против
  `-C -C`). Результат перепроверен мной исполнением: порядок полей независимо подтверждён
  тем, что `TestChannelAControls` зелёный, а мутант M5 (транспонирование) убит.
- **Fable не использовался.**

### §13 п.6 — пост-мердж-контроль

Post-merge-gate'а в этом репозитории нет. Заменяющие механизмы и их результат:
1. **Прогон полного сьюта на `main` после мерджа** — заполняется ниже фактическим числом.
2. **`.github/workflows/main-invariants.yml`** — АЛЯРМ (не гейт: workflow не может отказать
   в пуше), утверждает, что первопарентная линия `main` несёт только мерджи и плановые
   заметки. Мердж этого юнита — `--no-ff` через `worktree-finish.sh`, то есть по построению
   мерж-коммит на первопарентной линии.
3. **`ci.yml` job `tests`** — прогон сьюта и ruff на пуше.

**SHA мерджа:** заполняется после финиша.
**Результат прогона сьюта на main после мерджа:** заполняется после финиша.
