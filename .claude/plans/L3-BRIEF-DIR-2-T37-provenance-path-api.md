# L3-БРИФ DIR-2 / Т-37 — BT-7 юнит Т-0: публичный path-API в `provenance`

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2. **Модель менеджера:** opus;
объёмные подзадачи (грепы, свипы) — sonnet/haiku. **Полоса:** механическая, первый юнит
ратифицированного BT-7 v6.

**Ратификация, под которой этот юнит существует:** владелец ратифицировал BT-7 v6
2026-08-22 (реестр `56e60f5`). Дизайн-док — `.claude/plans/BT-7-location-anchor.md` (v6).
Читай его §Р3 и §Р5 — там сказано, ЗАЧЕМ нужен этот API; здесь сказано, ЧТО построить.

## 1. Задание

`loc.py` (следующий юнит) обязан привязывать захват якоря к **worktree-корню** и
разрешать путь внутри него. Сегодня оба умения приватны:

- `provenance._repo_root(cwd)` — worktree-корень, `realpath`, `None` если git не может.
- `provenance._resolve_candidate(root, file_path)` — разрешение пути в корне.

Правило модулей репозитория (CLAUDE.md): домен не тянет приватности другого домена.
Значит нужен ПУБЛИЧНЫЙ вход. Построй его.

**Предпосылки, добытые мной из кода (спот-чекни первым делом; расхождение = СТОП и доклад):**
- `provenance.py:59` `_repo_root(cwd)`, `:75` `_resolve_candidate(root, file_path)` — оба
  приватны, оба уже существуют и работают; изобретать заново НЕ надо, надо ЭКСПОНИРОВАТЬ.
- `_repo_root` идёт через `db.git_rev_parse("--show-toplevel", silent=True, cwd=cwd)`,
  который уже несёт кортеж исключений CB-79. Не заводи шестой рукописный `subprocess` —
  докстринг `_repo_root` прямо объясняет, почему их ровно пять.
- `provenance.head_sha(*, project_dir=None)` (`:252`) — образец сигнатуры публичной функции
  этого модуля с `project_dir`. Держись его формы.
- `_resolve_candidate` НАМЕРЕННО сохраняет финальный симлинк (см. его докстринг). Для
  классификации git-блоба это верно. **Не меняй его поведение** — если новому API нужна
  другая семантика, он её выражает СВОИМ параметром, а не правкой чужого контракта.

**Форма — гипотеза, преflight вправе предложить лучшую.** Мой набросок:
`provenance.worktree_root(*, project_dir: str | None = None) -> str | None` и
`provenance.resolve_in_worktree(root: str, file_path: str) -> str`.
Смысл, который держим: **захват якоря должен уметь спросить «какой у меня корень» и
«куда указывает этот путь» публично, без обращения к приватностям и без ambient cwd.**
Буква (имена, ровно два входа или один) — твоя.

**Явный НЕ-скоуп, и он важен.** v5 требовала от этого юнита ещё и структурный `new_path`
в вердикте `renamed`. **v6 это требование СНЯЛА** — переезд теперь отслеживает
`git blame --reverse` (запись `filename`), и парсер прозы не нужен. Не строй его.

## 2. Тесты

- Публичные функции покрываются напрямую: обычный чекаут; **linked worktree** (там
  `describe_root()` даёт корень main, а нужен корень ВЕТКИ — это ровно то, ради чего юнит);
  каталог без git → `None`, не исключение; несуществующий `project_dir`.
- **Мутационная проба обязательна и делается во ВРЕМЕННОМ worktree**
  (`git worktree add <tmp> <sha>` → проба → `git worktree remove --force`), НИКОГДА в
  рабочем дереве (инцидент Т-15).
- Ничего в существующем поведении `file_status`/`check_findings` не должно сдвинуться:
  докажи это формой диффа (экспонирование = обёртки), а не только зелёным прогоном.

## 3. Каноны (нарушение любого = провал приёмки §13)

1. **К-1. Уровень (3) на main НЕ коммитит — никогда.** Факт своей приёмки (3)←(4) пишешь
   в ЭТОТ файл НА ВЕТКЕ. Строку в документе направления пишет только (2).
2. **К-2. Минт — только `tools/cascade-mint.sh --prefix Т --text '…'`**, номер рукой не
   набирается (четыре коллизии подряд стоили этого правила).
3. **К-3. Файлы на main добавляются ПОИМЁННО** (`git add -- <path>`), никогда каталогом.
4. **К-4. На TDD-ветке `--merge-msg` ОБЯЗАТЕЛЕН.** Предлагаемый:
   `Merge fix/t37-provenance-path-api: worktree root and in-worktree path resolution become public, so anchor capture needs no private reach and no ambient cwd (BT-7 Т-0)`
5. **Факт приёмки цитирует ГРЕПАЕМОЕ СОДЕРЖАНИЕ, не номер строки** — номера не переживают
   форвард-мердж.
6. **Слот эскалаций исполнителя (4) переносится СЮДА ДОСЛОВНО.** Пусто — пишется явно
   «ПУСТО». Устная эскалация, не попавшая в артефакт, испаряется.
7. **К-7. Предписание в брифе — тоже предпосылка**, добывается из кода. Моё — тоже.
8. **Э-4. Клеймит ХАРНЕС**, пре-клейм направлением запрещён.

## 4. Харнес

- `tools/worktree-setup.sh fix/t37-provenance-path-api` — из КОРНЯ репо.
- Работа ТОЛЬКО в `.worktrees/fix-t37-provenance-path-api`. Проверяй чекаут перед каждой правкой.
- Тесты/линт В worktree и С `--extra dev`:
  `uv run --extra dev python -m pytest tests/ -q`, `uv run --extra dev ruff check src/ tests/`.
  Ruff пин **0.15.7**. `ruff format` НЕ гейт.
- Интеграция — `tools/worktree-finish.sh fix-t37-provenance-path-api --merge-msg '…'`, из корня.
- **ЗАПРЕЩЕНО** тебе и субагентам: `uv run --python`, `uv sync`, пересборка `.venv`.
  Эксперименты — под `/tmp`.
- **НЕ ТРОГАЙ**: `tools/`, `bench.py` (там сейчас пилот BT-6 направления DIR-1),
  `tests/golden/mcp_schema.json` (занят тем же пилотом), `types.py`, `CLAUDE.md`.
  Этот юнит их не требует: он целиком внутри `provenance.py` и его тестов.
- **Блокировка finish'а чужим staged-WIP на main:** НЕ стэшить чужое, НЕ ждать пробуждения —
  **polling** `git status --porcelain` раз в ~60 с до снятия, затем повтор finish с тем же
  `--merge-msg`. Доложи фактом, работу не бросай.

## 5. Слот эскалаций менеджера (3) → держателю (2)

**Э-1. Форма разошлась с наброском §1 — не по букве (она мне делегирована), а по ТОМУ,
ЧТО экспонируется, и это меняет бриф Т-a.** Набросок отдавал ЭЛЕМЕНТ:
`resolve_in_worktree(root, file_path) -> str` — абсолютный кандидат. Потребитель по Р3
читает `git show <commit>:<path>`, а `git show` берёт путь ОТ КОРНЯ РЕПОЗИТОРИЯ, не
абсолютный. Значит `loc.py`, получив только элемент, обязан был бы сам восстановить
композицию, которую `file_status` уже делает встроенно: containment
(`commonpath` + арм `ValueError`) и `os.path.relpath`. Это вторая реализация РЕШЕНИЯ О
СКОУПЕ в другом модуле — ровно тот дефект, о котором репозиторий пишет своими словами
(«a check that validates elements cannot validate their composition»; «duplicated rather
than shared is one drift away from disagreeing with itself»). Поэтому наружу вынесена
КОМПОЗИЦИЯ (`WorktreePath{root, rel, abspath}`), а `file_status` посажен на неё, чтобы
реализация осталась ровно одна. Стоимость названа честно: диффа «только обёртки» больше
нет, доказательство неизменности поведения — ниже, три независимых.

**Э-2. Т-a обязан ВСЕГДА передавать `project_dir` явно; API этого не навяжет.**
`worktree_root(project_dir=None)` падает на ambient cwd — так же, как `file_status`, и
иначе он был бы несогласован с собственным модулем. Р3 требует «НИКОГДА через ambient
cwd»; выполнить это требование может только вызывающий. В докстринге это сказано, но
предписанием это должно стать в брифе Т-a, а не остаться нотой.

**Э-3. `rel` для директории — это `"."`, и `git show <commit>:.` — не то, что нужно Т-a.**
Значение `file` в трекере бывает директорией и со слэшем на конце (CB-88 это прямо
называет). `resolve_in_worktree` их разрешает — она отвечает «куда указывает путь», а не
«блоб ли это». Отбраковку не-блоба держит git на стороне Т-a (§7.1: границу держит git),
но токен отказа (`skipped: …`) обязан быть предусмотрен в грамматике Т-a.

## 6. Слот эскалаций исполнителя (4) → менеджеру (3), переносится ДОСЛОВНО

**ПУСТО** — уровень (4) не запускался. Юнит механический и целиком внутри одного файла и
его тестов; объём не оправдывал делегирования, дробление работы стоило бы дороже, чем
принесло. Слот пуст по факту отсутствия исполнителя, а не по факту отсутствия эскалаций.

## 7. Факт приёмки (3)←(4)

**Принято мной, менеджером (3), 2026-08-22, на ветке `fix/t37-provenance-path-api`,
коммит `3f8c0e7`.** Уровень (4) не запускался (см. §6), поэтому приёмка — самопроверка
менеджера по коду и по прогонам, и она названа именно так, а не выдана за независимую.

**Что построено** (греп по содержанию, не по номерам строк):
- `src/codebugs/provenance.py`: `def worktree_root(*, project_dir: str | None = None)`,
  `def resolve_in_worktree(*, root: str, file_path: str)`, `class WorktreePath(NamedTuple)`.
- `file_status` посажен на публичный вход: `placed = resolve_in_worktree(root=root, file_path=file_path)`.
- `tests/test_provenance.py`: `class TestWorktreeRootIsPublic`,
  `class TestResolveInWorktreeIsPublic`, `class TestFileStatusGoesThroughThePublicResolver`.

**Предпосылки §1 спот-чекнуты по коду до исполнения, все подтвердились:** `_repo_root`
на `provenance.py:59` и `_resolve_candidate` на `:75` существуют и приватны; `_repo_root`
идёт через `db.git_rev_parse("--show-toplevel", silent=True, cwd=cwd)` (шестого рукописного
`subprocess` не заведено); `head_sha(*, project_dir=None)` на `:252` — форма выдержана;
финальный симлинк `_resolve_candidate` сохраняет, и его контракт НЕ тронут (пин —
`test_a_final_symlink_is_preserved_not_followed`). Структурный `new_path` в вердикте
`renamed` НЕ строился (не-скоуп v6).

**Тесты §2 покрыты:** обычный чекаут; подкаталог; **linked worktree** — и там же прямой
контраст с `db.describe_root()`, который стоя в ТОМ ЖЕ каталоге отвечает корнем main
(`test_linked_worktree_gives_the_branch_root_not_mains`); каталог без git → `None`;
несуществующий `project_dir` → `None`; удалённый cwd → `None` (форма CB-79). Фикстура
`git_project` для linked-теста НЕ годится и это записано в самом тесте: она коммитит свой
`.codebugs/`, линкованное дерево получает его копию, обход трекера останавливается там —
контраст стал бы вакуумно ложным по причине, не относящейся к проверяемому свойству.

**Прогоны В worktree, `--extra dev`:** `tests/test_provenance.py` 109 passed (было 92);
полный `tests/` — **2040 passed**; `ruff check src/ tests/` — All checks passed, ruff 0.15.7.

**Мутационная проба — во ВРЕМЕННОМ worktree** (`git worktree add --detach /tmp/t37-mut
3f8c0e7` → проба → `git worktree remove --force`; рабочее дерево не трогалось, инцидент
Т-15 учтён). База 109 passed. Шесть мутаций, каждая поймана:

| # | мутация | результат |
|---|---|---|
| M1 | `worktree_root` возвращает сам каталог, а не git-корень | 3 failed |
| M2 | снят гейт `cwd is None` | 1 failed |
| M3 | снят отказ по containment | **7 failed, из них 4 — ПРЕЖНИЕ тесты `file_status`** |
| M4 | `rel` не релятивизируется | **14 failed, из них 9 — ПРЕЖНИЕ тесты `file_status`** |
| M5 | финальный симлинк разрешается физически | 1 failed |
| M6 | `file_status` снова встраивает композицию (поведение ТО ЖЕ) | 1 failed — только пин проводки |

**Чем доказано, что поведение `file_status`/`check_findings` не сдвинулось — три
независимых довода, а не один зелёный прогон:**
1. **Форма диффа.** Вынос дословный: строки `_resolve_candidate` → `commonpath` →
   `relpath` перенесены без правки логики, вызов заменён механически, ветка `root is None`
   и текст отказа `out_of_repo (worktree {root})` остались в `file_status`.
   `check_findings` не тронут ни одной строкой (он и дальше зовёт приватный `_repo_root`).
2. **M3 и M4 валят ПРЕЖНИЕ тесты** — 4 и 9 соответственно, включая
   `TestPathIsResolvedBeforeItIsJudged::test_an_in_repo_symlink_escaping_the_worktree_is_out_of_scope`
   и весь `TestRenameProbeParsing`. То есть вынесенный код — ровно тот, от которого
   зависит поведение `file_status`, и оно приколочено УЖЕ СУЩЕСТВОВАВШЕЙ сюитой, а не
   новыми тестами.
3. **M6** показывает обратное направление: поведенчески ТОЖДЕСТВЕННЫЙ откат к встроенной
   композиции краснит ровно один тест — пин проводки. Значит «одна реализация» —
   проверяемое свойство, а не намерение в докстринге.
