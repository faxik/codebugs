# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), 2026-08-22
(первый handoff этой роли — вытеснять нечего; следующий вытесняет этот)

## 0. Нормативные входы — УКАЗАТЕЛИ, сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — разделы держателя: §1, §2 (П-А/П-Б/П-В), §7, §8.3б, §13 (контракт приёмки), §16.3, §17.
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ по решениям направления:**
  `.claude/plans/DIR-2-findings-identity.md` — журнал фактов, шаблон юнит-брифа (каноны
  К-1…К-9), эскалации куратору. Всё, что похоже на правило, живёт там, а не здесь.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md` — ратификации
  владельца и пакеты сессий. Пакет сессии 7 — коммит `28c7f8c`; ратификации по BT-8 — `c0e45e7`.
- **Ратифицированный дизайн, который сейчас строится:** `.claude/plans/BT-7-location-anchor.md`
  (v6) — §9 несёт нарезку оставшихся юнитов Т-b и Т-c.
- **Закрытая BT-8:** `.claude/plans/BT-8-merge-policy.md` — шапка несёт ратификацию.
- **Правила репозитория:** `CLAUDE.md` (Workflow — харнес worktree; Code rules; Error handling).
- **Память:** `MEMORY.md` → `feedback_no_direct_sql_use_mcp`, `feedback_track_in_codebugs`,
  `feedback_relay_needs_translation`, `project_worktree_isolation_flow`.
- **Аллокатор ID:** `tools/cascade-mint.sh` (минт делает скрипт, не рука; шапка
  `CASCADE-IDS.md` объясняет почему).

## 1. Сессионное состояние (то, чего нет больше нигде)

- **main @ `c0e45e7`** на момент ротации; мои коммиты после — `0f6a4b2` и коммиты handoff.
- **Живых агентов, воркфлоу и моих worktree — НЕТ.** Ничего не ждёт возврата.
- **`DIR-1-trust-coordination.md` был MODIFIED в дереве** — это WIP чужого направления,
  не трогать. Мои файлы чисты.
- **Golden (`tests/golden/mcp_schema.json`) ЗАНЯТ DIR-1** — идёт второй пилот генератора
  поверхностей на тонком модуле. Из-за этого **CB-131 отложена** (она двигает golden).
  Сериализация — через хвост `CASCADE-IDS.md`; перепроверь занятость свежим
  `git worktree list`, не верь этой строке.
- **Скоропортящееся, перепроверить на месте:** статусы карт, занятость файлов, хвост
  аллокатора, состав worktree DIR-1.
- **Артефакты замеров BT-7/BT-8** (скрипты + выгрузки с записанными sha256):
  `/home/faxik/.cache/bt7-corpus-2026-08-22/`, `/home/faxik/.cache/bt7-revblame/`.
  Кэш, не гарантирован — если исчез, замер перевыполняется скриптами из записей замеров.

## 2. Первые действия (по порядку)

1. **Сверить §0 с источниками** — открыть каждый и проверить, что этот handoff их не
   переврал. Расхождение чинится в handoff.
2. Прочитать пакет сессии 7 в реестре (`28c7f8c`) и ратификации BT-8 (`c0e45e7`)
   **вербатим**, не по пересказу здесь.
3. Прочитать журнал `DIR-2-findings-identity.md` с конца — последние ~15 строк несут итог
   сессии 6, каноны К-8/К-9 и три моих ошибки замера, названные против себя.
4. Начать с **CB-144** (high, готова к брифу: дефект локализован, грепаемые якоря в карте)
   и **Т-b** по `BT-7-location-anchor.md` §9 — они независимы по файлам и идут парой.
5. Т-c брать после Т-b: в брифе обязательно назвать, что это **backfill ЯКОРЕЙ, а НЕ
   backfill отпечатков** — второе владельцем прямо отклонено (`c0e45e7`).

## 3. Открытое для владельца

Открытых BT — **ноль**; вопросов в очереди к владельцу — **нет**. Ближайший кандидат в
вопрос появится, когда счётчики (`moved_file`, отказы развилки дедупа) наберут данные:
владелец дважды подряд отложил решение до момента, когда спрос станет арифметикой.
Батч живёт в реестре болей, не здесь.

## 4. Наблюдения процесса (кандидаты уже карты либо каноны)

- Три моих провала подряд были не в арифметике, а в том, ЧТО посчитано: доля читаемых
  файлов вместо заякориваемых; калибровка на 93% чужого кода; сверка по записанному пути.
  Лекарство — К-5б/К-5в, они в реестре; журнал §«Уроки процесса» несёт разбор.
- Т-39 показал форму сильнее моей: «первопартийность» как «файл, отслеживаемый git» —
  конструкция вместо списка каталогов. Стоит перенимать.
- Два дефекта шаблона брифа (К-8, К-9) нашли приёмщик и менеджер, не я.
- CB-144 — потолок покрытия ратифицированной фичи, найден юнитом вне скоупа.
