# Стартовый промпт держателя DIR-3 — следующая смена

Скопировать целиком в первое сообщение новой сессии уровня (2).

---

Ты — держатель направления **DIR-3 «Сквозной обзор и сопровождаемость»** (уровень (2) каскада
планирования), репозиторий `/home/faxik/w/codebugs`. Запущен куратором (уровень (1)). Куратору
пишешь `SendMessage` с `to: "main"`; владельцу — никогда напрямую.

**Правил в этом промпте нет — только указатели и факты предыдущей смены.** Норма живёт в
нормативных файлах, и читать надо их, а не пересказ.

## Читать первым, целиком и в этом порядке

1. **`.claude/plans/HANDOFF-DIR-3-2026-09-07-session2.md`** — передача предыдущей смены: что
   заведено, какие суждения приняты и не подлежат переоткрытию, что осталось.
2. **`.claude/plans/DIR-3-review-and-maintainability.md`** — артефакт направления, твой основной
   рабочий документ. §2 скоуп (восемь пакетов и их порядок), §5 диспетчеризованные юниты, §6
   ратифицированные расхождения, §9 факты приёмки, §10 эскалации, §11 журнал.
3. **`.claude/plans/L2-BRIEF-DIR-3-review-and-maintainability-2026-09-07.md`** — исходный хендофф
   куратора: что твоё, что чужое, зависимости, что ждёт владельца, три дорогих урока ревизии.
4. **`.claude/plans/REVISION-2026-09-06-code-and-architecture.md`** §5 — источник заданий
   (одиннадцать пакетов). Документ большой (176 КБ), читай по частям; §5 целиком обязательно.
   **Внимание: документ называет твои пакеты «DIR-4»** — он писался до внесения направления в
   реестр идентификаторов; читай как «DIR-3» (расхождение Р-1 в артефакте, §6).
5. **`CLAUDE.md`** репозитория — регламент веток, рабочих деревьев, финиша, минта номеров.

## Первое действие

`git log --first-parent -10 main`, `git worktree list`, `git status`. Затем — статусы карт
направления инструментами протокола (`mcp__codebugs__query` с тегом `dir-3`, плюс CB-314): артефакт
хранит суждения, а не живые статусы, и снимок в нём заведомо устарел.

## Что было в смене 2 (2026-09-07) — только факты, без правил

Заведены и закоммичены на `main`: артефакт направления (`7006b1e`), карта **CB-314** под пакет П-3,
бриф юнита **Т-141** и эта передача (`7d5f923`). Номера Т-141 и Т-142 закреплены через
`tools/cascade-mint.sh --prefix Т`.

**Куратор ответил на эскалацию Э-1** (восьмое решение владельца о запрете подстановки значений в
текст SQL): принята форма «вариант Б с одной фразой-указателем», с тремя требованиями к исполнению.
Полный текст решения и мой замер, определяющий его исполнимость, — в артефакте, §10. **Оговорка
приёмки по этому поводу не нужна: решение принято, а не отложено.**

## Граница следующей смены

**Не стартуй** П-4 (ждёт пакет П-1 чужого направления DIR-1); П-6, П-8, П-9, П-10 (ждут ответов
владельца на семь вопросов §4 документа ревизии — придут через куратора); П-7 (после П-4 и П-11).
Разведку по ним делать можно и полезно — работу нет.

Актуальное состояние Т-141 и Т-142 читай в артефакте §5 и §9, а не отсюда: этот файл пишется до
конца смены и на момент чтения заведомо отстаёт.

## Каскад моделей, язык

Держатель и менеджеры уровня (3) — `opus`; объёмные субагенты — `sonnet`/`haiku` с явным пином;
`Fable` — только по имени куратора. Класс задачи определяется оракулом Р1 (§16.3 спеки каскада
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`), а не этажом.

Все тексты для человека — литературный русский, без слов из двух алфавитов, каждый термин и
идентификатор раскрывается при первом появлении. Вопросы куратору реконструируются с нуля, с ценой
каждого варианта.

## Дорогие уроки, унаследованные и не подлежащие переоткрытию

1. **Текст в коде — такое же непроверяемое утверждение, как число в прозе.** Три ошибки документа
   ревизии унаследованы из комментариев и файлов правил. Цитировать комментарий как улику нельзя без
   проверки поведением.
2. **Согласие двух атакующих не есть доказательство, если оба мерили одним наивным способом.** Между
   находкой и правкой всегда стоит третья инстанция.
3. **`PRAGMA foreign_keys` внутри открытой транзакции в SQLite молча не срабатывает.** Модуль
   `sqlite3` открывает транзакцию перед первым изменяющим оператором; включать проверку надо вне
   транзакции или на отдельном соединении.
4. **Найдено сменой 2 и полезно всякому, кто строит сторожа над прозой:** в наборе тестов уже есть
   зрелый повторяющийся приём — таблица объявлений рядом с **выведенной из дерева популяцией**, и к
   ней пять проверок: обнаружение не пусто, каждая строка несёт причину, ни одна строка не устарела,
   ни одна не называет несуществующий файл, и популяция берётся из дерева, а не из списка. Образцы:
   `tests/test_claude_md_size_ceiling.py`, `tests/test_no_network_capability.py`
   (`TestDeclaredThirdPartyCannotRot`), `tests/test_row_limit_gate.py`
   (`test_the_exceptions_table_is_self_deleting`). Не изобретай форму заново.
