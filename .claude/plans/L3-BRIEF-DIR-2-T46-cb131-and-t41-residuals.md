# L3-БРИФ DIR-2 / Т-46 — CB-131 + два остатка отклонённого Т-41

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2. **Полоса:** механика.
**Класс по Р1: B** — оракул детерминированный (сьют, греп, golden), путь известен.
**Стартовый тир: opus, effort low**, бюджет попыток 4. Over-pin относительно таблицы Р1
записан против меня: основание — сессионный факт куратора «менеджеры (3) — opus».
Объёмное делегируй sonnet/haiku. **Fable не использовать.**

## 0. Почему три вещи в одном дереве

Все три — правки ОПИСАНИЙ и одной строки CHANGELOG в `findings.py`/`reqs.py`, и две из них
двигают golden. Вести порознь значило бы два конфликта по golden на ровном месте.
Независимых правок три — лимит ≤4 не нарушен.

## 1. Задача A — CB-131 (основная, из пакета куратора)

Прецеденция «`meta_update` мержится ПОСЛЕДНИМ и побеждает» объявлена в ДОМЕННЫХ докстрингах
`update_finding` и `reqs.update_requirement`, но НЕ на описаниях MCP-тулов `update` и
`reqs_update` — то есть ровно на той поверхности, где оба конфликтующих аргумента достижимы
одновременно. MCP-клиент, передавший `notes=` и `meta_update={"notes": …}`, получает
спроектированное задокументированное поведение, о котором его собственное описание тула
молчит.

**ЧЕГО ДЕЛАТЬ НЕЛЬЗЯ, и это половина задания:** НЕ «чинить» это отказом на коллизии. Это НЕ
дефект CB-129. `meta_update` — универсальный аварийный ход, называющий ключ хранения напрямую,
и «последняя запись побеждает» — ровно то, что делает неисправимый штамп невозможным (форма
CB-26). Отказ убрал бы путь починки `similar_to`/`category_minted`/`fingerprint_refusals`.
**Скоуп — два описания тулов, ничего больше.**

CLI не затронут: у верба `update` нет `--meta-update` вовсе (заявленная дыра `SURFACE_GAPS`,
ось CB-6). Не заводи его здесь.

**Golden ДВИНЕТСЯ** (описания тулов в снимке). Это ожидаемо и разрешено: дерево пилота DIR-1
(Т-42) снято, golden свободен — перепроверь `git worktree list` сам. Регенерация:
`PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json` ИЗ WORKTREE
(голый `python` резолвит пакет через editable-install на main и снимет не то дерево).
Дифф golden обязан содержать ТОЛЬКО изменения этих двух описаний — покажи это в факте приёмки.

## 2. Задача B — ложная премиса в докстринге, из-за которой Т-41 ОТКЛОНЁН

`src/codebugs/findings.py`, грепаемо: `All four callers of `update_finding` were read`.
**Перечисление неверно.** Мой греп по приземлённому коду даёт ПЯТЬ реальных сайтов вызова:
`loc.py:1421`, `milestones/triage.py:165`, `provenance.py:865`, `findings.py:3407`,
`findings.py:3819`. Пятый — `loc._apply_recapture` — приехал ФОРВАРД-МЕРДЖЕМ юнита Т-43 в
ветку Т-41 уже после того, как перечисление было сделано, и переигран не был.

**ВЫВОД УЦЕЛЕЛ, ОБОСНОВАНИЕ — НЕТ.** `_apply_recapture` передаёт только
`meta_update={"loc": fresh}` и НЕ передаёт `status`, поэтому предикат отказа
(`status ∈ LIVE_STATUSES`) на нём сработать не может; решение «под чужой транзакцией счётчик
не пишется» остаётся верным. Ложна записанная ПРИЧИНА: докстринг утверждает, что оба
амбиентных вызывающих передают ТЕРМИНАЛЬНЫЙ статус, тогда как один из них статуса не передаёт
вовсе.

**Что сделать:**
1. Переиграй перечисление САМ, по приземлённому коду, и запиши в докстринг ВЕРНОЕ: сколько
   сайтов, какие из них амбиентные, и по какому признаку у каждого предикат отказа не
   срабатывает (у `triage_dismiss` — терминальный статус; у `_apply_recapture` — статуса нет
   вовсе). Формулируй так, чтобы новый вызывающий делал утверждение ЛОЖНЫМ ЗАМЕТНО.
2. **Запинь перечисление тестом, а не прозой.** Тест считает сайты вызова `update_finding`
   в `src/` (AST либо греп с явным предикатом) и падает при появлении нового. Прецеденты в
   репозитории: `TestOpenCallSitesRatchet`, `TestWriteCallSitesRatchet`,
   `TestEscalateOptOutRatchet`. Это и есть механизация урока: форвард-мердж обязан КРАСИТЬ
   сьют, а не тихо обесценивать причину.

## 3. Задача C — CHANGELOG обещает несуществующий флаг

`CHANGELOG.md`, грепаемо: `update --meta-update`. У CLI-верба `update` такого флага нет
(заявленная дыра `SURFACE_GAPS`). Строка написана для вызывающего CLI и для него ЛОЖНА.
Почини на верное: путь починки — MCP `update(meta_update=…)`; если хочешь назвать отсутствие
CLI-написания — назови его как известную дыру оси CB-6, а не как «скоро будет».

## 4. Тесты

- CB-131: описания тулов проверяются снимком golden — этого достаточно, отдельный тест
  описания не нужен. **Но убедись, что дифф golden не несёт ничего лишнего.**
- Задача B: ратчет-тест обязателен и обязан быть КРАСНЫМ до его написания — покажи числом.
- **Мутационная проба во ВРЕМЕННОМ worktree** (`git worktree add --detach` → проба →
  `remove --force`), НИКОГДА в рабочем дереве; путь SUT напечатай ДО пробы. Мутанты:
  (i) добавить фиктивный шестой вызов `update_finding` — ратчет обязан упасть;
  (ii) откатить одно из двух описаний тулов — golden-тест обязан упасть.

## 5. Границы

- **МОЖНО:** `src/codebugs/findings.py`, `src/codebugs/reqs.py`, `tests/`, `CHANGELOG.md`,
  `tests/golden/mcp_schema.json`.
- **НЕЛЬЗЯ:** `tools/`, `.github/`, `sweep.py`, `bench.py`, `types.py`, `loc.py`,
  `provenance.py`, `.python-version`. `CLAUDE.md` — только если правило репозитория стало
  ЛОЖНЫМ (не ожидаю).
- **Поведение НЕ меняется ни на байт.** Это юнит про то, что поверхность ГОВОРИТ, и про одну
  ложную премису. Если поймал себя на правке логики — ты вышел за скоуп.

## 6. Каноны (нарушение = провал приёмки §13)

1. **К-1.** (3) на main НЕ коммитит; факт приёмки (3)←(4) — в ЭТОТ файл на ветке.
2. **К-2.** Минт — только скриптом; ID этого юнита уже сминчен (Т-46).
3. **К-3.** `git add -- <path>` поимённо, никогда каталогом.
4. **К-4. `--merge-msg` обязателен.** Предлагаемый:
   `Merge fix/cb-131-meta-update-precedence: the MCP tool descriptions now state the meta_update-wins precedence their domain docstrings already declared, and the caller enumeration behind the refusal counter is corrected and ratcheted (CB-131)`
5. **Факт приёмки цитирует ГРЕПАЕМОЕ СОДЕРЖАНИЕ, не номер строки.**
6. **Слот эскалаций исполнителя переносится ДОСЛОВНО**; пусто — явно «ПУСТО» с причиной.
7. **К-7. Предписание брифа — тоже предпосылка.** В §2 я уже назвал пять сайтов; **перепроверь
   это число сам, не переписывай моё**. Если их окажется иное количество — это находка, а не
   опечатка, и она подтверждает ровно тот урок, ради которого пишется ратчет.
8. **К-8.** Ветка card-driven юнита несёт `cb-NN`: `fix/cb-131-meta-update-precedence`.
9. **К-9.** Класс и тир — §0; фактический путь модели зафиксируй в факте приёмки.
10. **§13.6.** Post-merge-gate'а в репозитории нет — НАЗОВИ заменяющий механизм и его
   РЕЗУЛЬТАТ (полный сьют на main после мерджа, числом). «n/a» без заменителя не принимается.
   **Юнит Т-41 был отклонён в том числе за пропуск этого пункта — не повтори.**
11. **§13.7.** Карта юнита — CB-131 → терминальный статус. CB-144 НЕ трогай: она уже `fixed`,
   и её приёмку закрываю я.
12. **К-5б.** Каждое число, уходящее в доклад, несёт свою ОПЕРАЦИЮ. Менеджер Т-41 доложил
   `2247 passed`, приёмщик на том же мердже намерил `2300` — не повтори: печатай команду и её
   вывод, а не память о ней.

## 7. Харнес

- `tools/worktree-setup.sh fix/cb-131-meta-update-precedence` — из КОРНЯ репо.
- Работа только в `.worktrees/fix-cb-131-meta-update-precedence`.
- `uv run --extra dev python -m pytest tests/ -q`; `uv run --extra dev ruff check src/ tests/`;
  ruff **0.15.7**. Интерпретатор пинован `.python-version`.
- **ЗАПРЕЩЕНО:** `uv run --python`, `uv sync`, пересборка `.venv`. Эксперименты — `/tmp`.
- Финиш: `tools/worktree-finish.sh fix-cb-131-meta-update-precedence --merge-msg '…'`.
- Чужой staged-WIP на main блокирует finish: НЕ стэшить чужое — polling ~60 с и повтор тем же
  `--merge-msg`.

## 8. Слот эскалаций менеджера (3) → держателю (2)
### СТРУКТУРНОЕ
### ДЕТАЛЬ
### НАЙДЕНО, НЕ ЧИНЮ

## 9. Слот эскалаций исполнителя (4) → менеджеру (3), ДОСЛОВНО

## 10. Факт приёмки (3)←(4)
