# L3-BRIEF DIR-1 / Т-138 — CB-284: финиш коммитит грязное дерево, показав только имена файлов

Держатель направления DIR-1, уровень (2). Бриф написан 2026-09-01 сменой 23; выдан сменой 24
(2026-09-06) после исправления номера.

> **НОМЕР ЮНИТА ИСПРАВЛЕН, И ЭТО НЕ КОСМЕТИКА.** Смена 23 назвала этот юнит «Т-136», не обратившись
> к аллокатору `tools/cascade-mint.sh`. К тому часу номер Т-136 аллокатор уже выдал направлению
> DIR-2 (коммит минта `358bc4b`, 2026-09-01 12:57; юнит DIR-2 — карточки CB-289 и CB-307), а для
> CB-284 не было выдано никакого номера вообще: строки с ним в реестре `.claude/plans/CASCADE-IDS.md`
> не существовало. Два юнита под одним номером сделали бы ведомость приёмки неоднозначной, поэтому
> смена 24 минтила номер заново — **Т-138** (коммит `eacc6a8`) — и переименовала этот файл.
> **Везде ниже и во всех своих ответах называй юнит Т-138.** Старое имя файла
> (`…-T136-cb284-…`) не существует ни в одном коммите: файл был неотслеживаемым.
Карта: **CB-284** (`high`, `process`). **Задача владельца:** он рассмотрел карту лично, в пятёрке
отобранных из восьмидесяти четырёх, и сказал: «Это нужно нормально поправить. Правильное решение
скорее всего очень простое.» **Второе предложение — часть условия задания, а не пожелание.**
Тяжесть подтверждена на уровне владельца; переоценивать её не надо.

**Класс по Р1: B (оракул механический). Стартовый тир: `opus`. `effort`: `low`.**

> Строка выше — ПЛАН; пункт 8 контракта приёмки требует ещё и ФАКТА, в разделе
> `## Отчёт по регламенту` этого файла: модель и режим буквально, совпал ли итоговый тир со
> стартовым, какой объём отдан дешёвым субагентам.

Ветка: `fix/cb-284-show-what-is-committed`.

---

## §0. ЧИТАЙ ПЕРВЫМ: центральное утверждение карты ОПРОВЕРГНУТО, предмет сужен

**Карта утверждает, что финиш молча коммитит любое грязное дерево. Это неверно, проверено
держателем тремя способами до выдачи брифа.** `tools/worktree-finish.sh:469-486`: фаза `[1/7]`
печатает `git status --short`, и если сообщение коммита НЕ передано — печатает «Uncommitted changes
and no commit message given», пример вызова, и выходит кодом 1. Путь коммита в сценарии ровно один
(строка 483) и он под этим условием; `COMMIT_MSG` — позиционный `${2:-}` без умолчания.

**Отказ стоит с ПЕРВОГО коммита харнеса** (`adab3e7`): `git log -S 'Uncommitted changes and no
commit message given' -- tools/worktree-finish.sh` даёт ровно один коммит. Он был на месте и когда
карту заводили, и когда случился описанный ею случай. **Вариант (a) карты — предпочтительная в ней
починка — уже реализован. Не делай его заново.**

**Откуда ошибка карты, и это твой главный ориентир:** она опирается не на код, а на прозу — сама
цитирует `CLAUDE.md`, «It commits any dirty state, runs the guards, forward-merges main…». Фраза
стоит и сейчас (`CLAUDE.md:299`), утверждает поведение БЕЗУСЛОВНО и умалчивает про условие.

**Что в карте остаётся правдой:** исполнитель Т-133 оборвался, оставив в дереве мутанта рядом с
278 строками настоящих тестов; держатель поймал его чтением незакоммиченного диффа; из этого
ратифицирован канон К-32.

---

## §1. Замысел — что чинить на самом деле

**Остаточный дефект лежит в ГРАНУЛЯРНОСТИ УЛИКИ.** Опасен только путь «сообщение передано»: там
дерево коммитится после того, как напечатан `git status --short`, то есть **только имена файлов**.
В случае 31 августа эта строка выглядела бы как ` M tools/worktree-finish.sh` — неотличимо от
законной правки того самого файла, который юнит и обязан был править.

**Улика печатается на уровне, который по построению не может различить мутанта и работу.** Канон
К-32 велит читать незакоммиченный ДИФФ именно потому, что список файлов этого не показывает, — то
есть канон компенсирует дисциплиной ту нехватку, которую дёшево закрыть в инструменте.

**Чего юнит НЕ делает:** не добавляет ни одного нового отказа, не меняет поток управления, не
трогает путь «без сообщения» (он уже правильный), не требует подтверждения. Карта справедливо
отвергла подтверждение как негодное для неинтерактивного запуска — но отвергла она печать ВМЕСТЕ с
подтверждением, а печать сама по себе этим доводом не задета.

---

## §2. Форма выбрана уровнем (2) — две правки, обе маленькие

**(1) На пути автокоммита печатать СОДЕРЖИМОЕ, а не имена.** Место точное: между `git add -A`
(строка 482) и `git commit` (строка 483) — там индексная форма диффа уже включает и
неотслеживаемое, что важно: оставленная проба может быть новым файлом. Форма на твоё усмотрение,
но она обязана удовлетворять §3; ориентир — сводка изменений плюс сам дифф.

**(2) Привести `CLAUDE.md:299` к правде — назвать условие.** Одна фраза. Иначе следующий читатель
заведёт эту карту заново: она уже стоила карты `high` и личного рассмотрения владельца.

**ДЕРЖИСЬ ПРОСТОТЫ — ЭТО УСЛОВИЕ ВЛАДЕЛЬЦА, А НЕ СТИЛЬ.** Не вводи флагов, режимов, настроек
объёма вывода, обрезки по числу строк и порогов «слишком большой дифф». Каждое выглядит разумным и
каждое возвращает ту же болезнь: решение о том, что показать, снова принимает не тот, кто смотрит.
Считаешь, что без порога нельзя — **эскалация (б), а не порог.**

---

## §3. Оракул — сверяется ПОИМЁННО, а не числом прогона

1. **Тест: автокоммит печатает содержимое изменения.** Оставь в дереве правку ОДНОЙ строки в
   файле, который ветка и так законно правит, — воспроизведи форму случая 31 августа, а не удобный
   случай с новым файлом. В выводе обязан быть виден сам изменённый текст.
2. **Тест: неотслеживаемый оставленный файл тоже показан содержимым.** Отдельная половина:
   `git status --short` показывает такой файл как `??`, а дифф до индексации его не видит.
3. **Тест: путь «без сообщения» не изменился** — по-прежнему отказ кодом 1 и прежний текст. Правка
   не смеет ослабить существующий отказ.
4. **Тест: чистое дерево не стало печатать лишнего** — ветка автокоммита не исполняется вовсе.
5. **Каждый тест доказан мутацией:** сломай печать — тест обязан покраснеть; верни — позеленеть.
   Мутация только в рабочем дереве ветки, никогда в `main`.
6. **Полный набор зелен**, `ruff check src/ tests/` чист, ruff ровно `0.15.7` (версия закреплена в
   `uv.lock`, а НЕ в `pyproject.toml` — искать пин в очевидном месте бесполезно). Число прошедших
   назови **С РАЗРЕЗОМ**: на каком SHA, прогон или сбор, включая ли твои новые тесты.
7. **Правка `CLAUDE.md` перечитана как ОДНА фраза в своём абзаце**, а не вставлена рядом: соседние
   предложения не должны начать противоречить ей.

---

## §4. Регламент

Полностью по `L3-BRIEF-SKELETON-DIR-1.md` — прочти его §6, §6а, §6б, §6в и §7. Специфичное:

- **Три обязательных раздела уже заведены заголовками в конце этого файла** — наполняй по ходу,
  тем же коммитом, что и работу. Не откладывай до конца: обрывы сессий здесь штатный фон, и юнит
  Т-133 оборвался, не написав ни одного из них.
- Только worktree: `tools/worktree-setup.sh fix/cb-284-show-what-is-committed`. **Флага `--items` у
  скрипта НЕТ, `tools/worktree-probe.sh` в этом репозитории не существует.**
- `uv run --extra dev python -m pytest tests/ -q` **в worktree**; `--extra dev` не опционально.
  Никогда не проверяй свои правки прогоном из `main`.
- **`CHANGELOG.md` не трогай.** **Карту CB-284 не закрывать** (К-1).
- **Прямой вызов клиента базы из командной строки запрещён политикой окружения**; статусы карт
  читай только MCP-тулами.
- **ПЕРЕД финишем — проход `/simplify-traced`**, именно он.
- **Финиш НЕ запускай сам.** Окно посадки идёт через держателя и куратора. Готова ветка — вернись
  держателю сообщением и жди. Твой предмет — сам сценарий финиша; встречный запуск его же есть
  ровно то состояние, которое чинил юнит Т-133.
- **Ловушка порядка опций:** `git commit -- <путь> -m '<текст>'` ОТКАЗЫВАЕТ, потому что `--`
  завершает разбор опций; сообщение обязано стоять ДО разделителя.

---

## §5. Поводы эскалации — перечень к разделу «Эскалации» в конце файла

- **(а)** Печать диффа ломает существующий тест, читающий вывод фазы `[1/7]`. Скажи, какой именно;
  ослаблять свой тест ради этого не надо.
- **(б)** Считаешь, что без порога на объём вывода нельзя. **В эскалацию, не в порог.**
- **(в)** Обнаружил, что путь «без сообщения» на самом деле обходится (флагом, переменной среды,
  ещё чем-то) — это опровергает §0 и меняет предмет. **Сообщи немедленно.**
- **(г)** Правка `CLAUDE.md:299` тянет за собой соседние неверные утверждения — в эскалацию и в
  карту, НЕ в правку: файл делят другие направления.
- **(д)** Сдвинулся любой голден; понадобилось тронуть файл соседнего направления.

---

# РАЗДЕЛЫ ИСПОЛНИТЕЛЯ — заведены держателем, наполняются ПО ХОДУ (§6в шаблона)

## Отчёт по регламенту

*(наполняется по ходу работы, с первого коммита ветки)*

**(а) Модель и режим.** Работаю моделью `claude-opus-5[1m]` (семейство Opus 5, окно в миллион
токенов), режим `effort: low`. Это ровно то, что предписано шапкой брифа: «Стартовый тир: `opus`,
`effort`: `low`».

**(б) Совпадение итогового тира со стартовым.** Совпал: вся работа сделана на стартовом тире
`opus`, повышать до `fable` не потребовалось и понижать тоже. Предмет оказался ровно того размера,
каким его описал держатель, — одна строка печати в сценарии и одна фраза в `CLAUDE.md`.

**(в) Объём, отданный дешёвым субагентам.** На модель `sonnet` отдан сплошной перебор набора
тестов: какие тестовые функции вообще касаются фазы `[1/7]` сценария `tools/worktree-finish.sh`,
какие сверяют её текст и как устроен типовой стенд, прогоняющий скрипт целиком. Это чёрная работа
перечисления по трём с половиной тысячам тестов, и она принесла главный факт предполёта: **ни один
тест набора никогда не запускал сценарий финиша с грязным рабочим деревом**, то есть вся половина
фазы `[1/7]`, которая коммитит за оператора, была не покрыта вовсе. Результат я перепроверил
чтением сам — открыл фикстуру `armed` (`tests/test_worktree_harness.py:3851`), структурную проверку
`TestGuardsAreActuallyInvoked` (там же, `:2105`) и оба места, где сверяется маркер `"[1/7]"`.

Себе я оставил: чтение самого сценария и разбор его аргументов, выбор формы печати, написание
четырёх тестов, все пять мутационных проб и их истолкование, правку `CLAUDE.md` и сверку её с
соседними абзацами. Прогоны набора тестов запускал сам, а не через субагента, потому что прогон
обязан идти в рабочем дереве ветки: субагент со своим рабочим каталогом легко измерил бы чужое
дерево, а это ровно та ошибка, от которой предостерегает регламент.

**Ветка и база.** Ветка `fix/cb-284-show-what-is-committed` создана штатным
`tools/worktree-setup.sh` в рабочем дереве `.worktrees/fix-cb-284-show-what-is-committed`. База —
состояние `main` на момент создания, коммит `e456d01` («журнал смены 24»); коммит `c53a7df`,
которым держатель положил на `main` этот самый бриф, в базу входит (проверено
`git merge-base --is-ancestor`).

**Что именно сделано, двумя правками.**

1. **Сценарий.** В фазе `[1/7]`, между сторожем `_guard_leaked_repr` и самим `git commit`, добавлены
   две строки: заголовок `What is being committed:` и печать
   `git diff --cached --stat -p | sed 's/^/    /'`. Читается ИНДЕКС, а не рабочее дерево, и это
   не деталь: оставленная проба часто оказывается неотслеживаемым файлом, которого `git diff` до
   индексации не видит вовсе, — то есть печать, поставленная до `git add -A`, чинила бы ровно
   половину беды. Поток управления не тронут, ни одного нового отказа не добавлено, путь «без
   сообщения» не изменён.
2. **`CLAUDE.md`.** Фраза «It commits any dirty state» заменена на называющую условие: дерево
   коммитится только когда передано сообщение коммита, патч печатается перед коммитом, а грязное
   дерево без сообщения отклоняется кодом 1.

**Оракул §3 — как выполнен каждый пункт.**

- **п. 1 (содержимое изменения видно).** Тест
  `test_a_leftover_edit_is_printed_as_the_changed_text`. Ветка честно коммитит свой файл
  `tests/test_thing.py`, после чего в нём остаётся незакоммиченной правка ОДНОЙ строки
  (`assert 1 == 2  # LEFTOVER_MUTATION_PROBE`) — форма случая 31 августа, а не удобный случай с
  новым файлом. Проверяется наличие самой строки в выводе; имя файла в выводе было и до правки и
  ничего не значило, потому что ветка этот файл править вправе.
- **п. 2 (неотслеживаемый файл показан содержимым).** Тест
  `test_a_leftover_untracked_file_is_printed_as_content_too`. Проба кладётся в `tests/`, а не в
  корень, и это не произвол: сторож `_guard_untracked_scratch_at_root` отказывает на постороннем
  верхнеуровневом `.py` кодом 4 и до печати дело не доходит вовсе. Одним каталогом ниже — там, где
  и лежала настоящая проба 31 августа, — дерево коммитится, и это тот самый случай, где содержимое
  необходимо.
- **п. 3 (путь «без сообщения» не изменился).** Тест
  `test_a_dirty_tree_with_no_message_is_still_refused_untouched`: код возврата 1, прежний текст
  «Uncommitted changes and no commit message given», имена файлов по-прежнему печатаются, а
  содержимое И ЗАГОЛОВОК — нет.
- **п. 4 (чистое дерево не печатает лишнего).** Тест `test_a_clean_worktree_prints_no_diff_at_all`:
  вывод содержит `✓ Clean` и не содержит заголовка печати.
- **п. 5 (каждый тест доказан мутацией).** Пять проб, все — только в рабочем дереве ветки, `main`
  не трогался; после каждой сценарий восстанавливался `git checkout -- tools/worktree-finish.sh`.
  **M1** — печать удалена целиком: красны тесты 1 и 2, зелены 3 и 4. **M2** — печать поднята выше
  `git add -A` и читает рабочее дерево (`git diff` вместо `git diff --cached`): красен ТОЛЬКО тест
  2, тест 1 зелен — то есть половина про неотслеживаемый файл поймана отдельно. **M3** — вся печать
  поднята выше проверки на пустое сообщение: красны 1, 2 и 3, причём тест 3 краснеет **именно на
  проверке заголовка** (проверено чтением сообщения об ошибке: `assert 'What is being committed:'
  not in …`), а не на содержимом — до `git add -A` индекс пуст, содержимое не печатается, и
  проверка на одно лишь содержимое осталась бы зелёной. Эта мутация и заставила усилить тест 3.
  **M4** — печать продублирована в ветку чистого дерева: красен ТОЛЬКО тест 4. **M5** — печать
  сужена до добавленных файлов (`--diff-filter=A`): красен ТОЛЬКО тест 1, то есть он ловит то, чего
  не ловит тест 2. Итог: у каждого из четырёх тестов есть мутация, от которой он краснеет в
  одиночку.
- **п. 6 (набор зелен, линтер чист).** См. следующий абзац.
- **п. 7 (правка `CLAUDE.md` перечитана как одна фраза в своём абзаце).** Перечитано целиком.
  Противоречия с соседями нет, и один сосед новую фразу прямо подтверждает: пункт про подсказку
  повтора двумя абзацами ниже говорит, что «positional commit message applies only to a still-dirty
  worktree», — это то же условие, сказанное с другой стороны. Абзац остался одним пунктом списка;
  ни одно соседнее предложение не переписано, кроме переносов строк, съехавших от удлинения.

**Прогон набора тестов и линтера.** `ruff 0.15.7`, `ruff check src/ tests/` — `All checks passed!`.
Полный набор в рабочем дереве ветки: **3382 пройдено за 224,27 с** (`uv run --extra dev python -m
pytest tests/ -q`), ноль отказов и ноль пропусков. Это ПРОГОН, а не сбор, и он включает мои четыре
новых теста: предыдущий юнит направления зафиксировал на своём итоге 3378 тестов, разница ровно
четыре. Итоговый прогон на неподвижном дереве — ниже, после прохода `/simplify-traced`.

## Проход /simplify

*(пока пусто, наполняется по ходу — не откладывать до конца работы)*

## Эскалации

*(наполняется по ходу; каждый повод из §5 разбирается поимённо)*

**Ни один повод из §5 не сработал.** Разбор поимённо:

- **(а) «Печать диффа ломает существующий тест, читающий вывод фазы `[1/7]`».** **Не сработал.**
  Такого теста в наборе не было и нет: сплошной поиск по `tests/` не даёт ни одного совпадения на
  строки `Working tree status`, `Uncommitted changes`, `no commit message given`, `Committing:`,
  `✓ Committed`, `✓ Clean` и на `git status --short`; единственное, что вообще сверяется, — сам
  маркер фазы `"[1/7]"`, и только как признак того, что до этой фазы дошли
  (`tests/test_worktree_harness.py:6729` и `:6775`). Подтверждено делом, а не только поиском:
  полный набор зелен, 3382 из 3382. **Попутно выяснилась вещь крупнее самого повода** — ни один
  тест никогда не запускал сценарий финиша с ГРЯЗНЫМ деревом, то есть половина фазы, которая
  коммитит за оператора, не была покрыта ничем. Мои четыре теста — первые, кто её вообще исполняет.
- **(б) «Без порога на объём вывода нельзя».** **Не сработал: порог не нужен, и это проверяемое
  утверждение, а не вкус.** Печатается ровно то, что через мгновение станет коммитом, — если вывод
  велик, значит велик и коммит, и человек, который вот-вот им обзаведётся, обязан это видеть.
  Любой порог возвращает исходную болезнь в новом виде: решение о том, что достойно показа, снова
  принимает сценарий, который судить об этом не может, — и худший случай для порога это ровно
  худший случай для человека, потому что большой автокоммит и есть тот, в котором прячется лишнее.
- **(в) «Путь „без сообщения“ обходится».** **Не сработал.** Проверено чтением разбора аргументов:
  сообщение коммита берётся единственным присваиванием `COMMIT_MSG="${2:-}"`
  (`tools/worktree-finish.sh:278`) из позиционных аргументов, оставшихся после снятия флагов; ни
  переменной среды, ни флага, ни умолчания у него нет. Опровержения §0 не нашёл — наоборот,
  подтвердил его третьим способом: строка отказа заведена первым же коммитом харнеса (`adab3e7`,
  проверено `git log -S`), а тест
  `test_a_dirty_tree_with_no_message_is_still_refused_untouched` теперь держит этот отказ
  поведенчески.
- **(г) «Правка `CLAUDE.md` тянет за собой соседние неверные утверждения».** **Не сработала.**
  Абзац перечитан целиком вместе с соседями; ни одного утверждения, которое новая фраза сделала бы
  ложным, не обнаружено, а один сосед её прямо подтверждает (см. п. 7 оракула выше). Файл делят
  другие направления, поэтому тронут ровно один пункт списка.
- **(д) «Сдвинулся любой голден; понадобилось тронуть файл соседнего направления».** **Голден не
  сдвинут, чужих файлов не тронуто, но одна цифра заслуживает внимания держателя.** `CLAUDE.md`
  накрыт ДВУСТОРОННИМ порогом в `tests/test_claude_md_size_ceiling.py`: не больше 42 188 байт
  (иначе отказ) и не меньше 38 188 (иначе отказ по «полому потолку» — потолок, до которого далеко,
  это гейт, который не может сработать). Файл был 40 546 байт, стал 40 777; запас до верхней
  границы — 1 411 байт. Правку это пропускает без единого касания констант, но запас мал, и
  следующая правка этого файла может в него не поместиться. Это наблюдение, а не просьба.

**Наблюдение, доставшееся от оборвавшейся смены, — записано, чтобы не потерялось.** Держатель
передал находку куратора, добытую из стенограммы умершей сессии, и прямо поручил сохранить её в
предмете карточек CB-284/CB-285; отдельной карточки под неё никто не заводил. Суть: сторож
куратора, отвечающий на вопрос «идёт ли сейчас чужая посадка», дважды за час сработал ложно, и оба
раза по одному механизму — он искал процессы по подстроке `worktree-finish`, а эта подстрока
содержится в командной строке самого поиска, так что оболочка, выполняющая запрос, попадала в его
же результат: рабочий каталог наш, возраст ноль секунд, вид совершенно убедительный. Второй способ
соврать — поймать одноимённый сценарий из соседнего репозитория `/home/faxik/w/autosorter`.
Исправленный предикат требует трёх условий разом: это запуск САМОГО сценария
(`tools/worktree-finish.sh <слаг>`), а не любая строка с таким текстом; рабочий каталог — наш
репозиторий; собственный процесс и процессы поиска исключены. Класс ошибки куратор назвал так:
**«прибор смотрит на написание и находит написание, а не предмет»**.

**Почему это относится к моему юниту, и почему я всё же не потащил это в правку.** Родство прямое:
список имён файлов (`git status --short`) — это написание, а содержимое изменения — предмет, и
CB-284 ровно про расстояние между ними. Но предмет юнита не расширяется: владелец поставил условие
«правильное решение скорее всего очень простое», а сторож параллельных посадок живёт вне этого
репозитория и не имеет отношения ни к одной строке моего диффа. Записано как наблюдение — и, если
держателю оно покажется достойным карточки, заводить её должен он: закрывать и заводить карточки
уровню (3) не положено.
