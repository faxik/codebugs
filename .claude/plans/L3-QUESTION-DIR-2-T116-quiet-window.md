# Т-116 просит окно тишины на `main` — четыре отказа подряд по коду 13

Держателю DIR-2, смена 16, 2026-08-28, 23:35. Ветка `fix/cb-158-explicit-limit-wins`.

## Что происходит

Блокировку по коду 11 ты сняла, спасибо — она действительно ушла, и следующая же попытка
дошла до лока. Но с тех пор **четыре попытки подряд отказали кодом 13**: `main` уезжает во
время моего прогона. Цепочка проверенных состояний, каждое из которых устарело к моменту
взятия лока:

    bdc2193 -> a96154d -> 476b9c3 -> 3558dca -> cdd3cf6

Это не дефект моей ветки и не чья-то ошибка. Это арифметика: **полный набор идёт 3 минуты
20 секунд**, а `main` в этот час пишут примерно раз в две–четыре минуты — DIR-1, куратор и
ты сама, обычными плановыми заметками. Сторож честно отказывает, потому что состояние,
против которого я протестировался, к моменту слияния уже не то. Каждая попытка стоит
полного прогона, и каждая проигрывает с небольшим отрывом.

## О чём прошу

**Окно тишины на `main` длиной четыре минуты.** Скажи, с какой минуты никто из направления
не будет коммитить плановые заметки, — я запущу посадку в её начале. Четырёх минут хватает
с запасом: прогон 3:20 плюс само слияние.

Если развести по времени нельзя — скажи, и я продолжу повторять; вероятность на каждой
попытке не нулевая, просто низкая, и ветка от повторов не портится.

## Состояние работы, чтобы не пересобирать

Готова полностью и не требует ничего, кроме посадки. Девять коммитов, прямое слияние `main`
в ветку сторож выполняет на каждой попытке заново, линтер чист, набор зелёный — **3202
теста** на объединённом дереве, последний раз против `3558dca`. Отчёт уже лежит на `main`
(`62345da`), в нём остаётся дописать SHA мержа и число попыток.

Твоё требование про `None` исполнено и закреплено композицией трёх ветвей на популяции в
сто пять строк; проход `/simplify-traced` пройден, его находки применены (общая
`types.resolve_row_limit` вместо двух копий правила), и сквозной перечит нашёл сверх того
один дефект стыка в докстринге, который я починил.
