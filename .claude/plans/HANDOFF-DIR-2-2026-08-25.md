# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), 2026-08-25
(вытесняет `HANDOFF-DIR-2-2026-08-23.md`; следующий вытесняет этот)

Написан уходящей сессией 8 по порогу контекста, на чистой границе. **Правил здесь нет
намеренно** — только указатели, сессионные факты и первые действия.

## 0. Нормативные входы — УКАЗАТЕЛИ, сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13 (контракт приёмки — **ВОСЕМЬ** пунктов), §16.3, §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md` — журнал
  фактов, шаблон юнит-брифа (каноны К-1…К-13), эскалации.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- **Ратифицированный дизайн BT-7:** `.claude/plans/BT-7-location-anchor.md` — **ИСПОЛНЕН ЦЕЛИКОМ**,
  нарезка §9 закрыта. Несёт мою поправку §1б (число дизайна не воспроизвелось).
- **Закрытая BT-8:** `.claude/plans/BT-8-merge-policy.md`.
- **Улики замеров:** `BT-7-MEASUREMENTS-FLAGS-2026-08-23.md`,
  `BT-7-T50-BACKFILL-MEASUREMENTS-2026-08-24.md`, `T61-CB-157-survey.md`.
- **Правила репозитория:** `CLAUDE.md`. **Аллокатор ID:** `tools/cascade-mint.sh`.

## 1. Сессионное состояние (то, чего нет больше нигде)

**Перепроверь ВСЁ на месте — `git log`, `git worktree list`, `claims_list`, статусы карт.**

- **main @ `3de882c`** на момент написания; `origin/main` отставал на 1.
- **Приземлено и ПРИНЯТО сессией 8:** Т-43 (`01d405d`/`8367958`), Т-46 (`7366228`), Т-50
  (`ed369ac`), Т-56 (`c83352a`), Т-61 (`ea77df9`). **Т-63 (`0d791bb`) приземлён, ПРИЁМКА В
  ПОЛЁТЕ** — её вердикт первым делом записать фактом.
- **BT-7 ЗАКРЫТА** по всей нарезке §9 (Т-0, Т-a, Т-b, Т-c).
- **Backfill якорей ПРИМЕНЁН на обоих трекерах** по ратификации владельца (реестр `f5f5fd5`):
  codebugs 22 координаты / 136 отказных объектов, покрытие 13.92%; autosorter 272 / 3083, 8.11%.
  Идемпотентность проверена. **Бэкапы:** `/home/faxik/tracker-backups/*-20260824-191701Z.db`.
- **Т-48 / CB-143 ЗАПАРКОВАН** по переприоритезации владельца (CSV — низкий приоритет). Клейм
  отпущен, карта `open`, **ветка `fix/cb-143-stdout-alias-diagnostic` и бриф СОХРАНЕНЫ**.
  Возобновление стоит одного `worktree-setup`. **Не потеряй:** популяция этого дефекта — ДВА
  сайта (`findings.py` и `reqs.py`), карта называет только один.
- **Карты, заведённые сессией 8:** CB-156 (`fixed`, Т-63), CB-157 (`fixed`, Т-61), **CB-161**
  (интерполяция `last_n` в SQL, low, open), **CB-162** (экземпляр CB-28, low, open), **CB-163**
  (граф описан дважды, medium, open, ЧУЖАЯ территория).
- **Нарезка CB-62 зафиксирована** (журнал, `734eb1a`): (1) `group_by="tag"` и произвольный ключ
  `meta` — механика; (2) семантическая ось — **кандидат в BT**, два пункта нарезаются ВМЕСТЕ;
  (3) `distinct_from` подавляет — **суженно**, в единственном потребителе, со ссылкой на CB-163.
- **Каноны:** К-10 и К-13 ратифицированы куратором; К-11 и К-12 предложены, К-12 применялся и
  окупился (п.4 контракта впервые вернулся PASS вместо BLOCKED).
- **Живых клеймов моего направления на момент написания — ноль.**
- **DIR-1 работает параллельно**, у них в полёте Т-64. Golden сериализуй хвостом `CASCADE-IDS.md`.

## 2. Первые действия (по порядку)

1. **Сверить §0 с источниками.** Расхождение чинится в handoff.
2. **Записать вердикт приёмки Т-63** фактом в журнал. Приёмщику особо вменено проверить моё
   ЛОЖНОЕ утверждение о гейте (см. §4) и оценить мою классификацию по К-13.
3. **CB-62 — минтить и брифовать** по зафиксированной нарезке. Начинать с (1), она механическая.
4. **CB-161 / CB-162** — механическая полоса, обе с неизмеренной популяцией: развёртка
   предшествует починке.
5. **CB-163** — НЕ твоя территория единолично; форму несёт куратору/владельцу, популяция
   (`meta.split_from` против рёбер в таблице) не измерена и обязана предшествовать выбору.

## 3. Открытое для владельца

Вопрос про `--apply` **закрыт** — владелец ответил «Применяй», исполнено. Открытых BT — ноль.

Назревают два, оба ждут арифметики, а не мнения:
- **Форма заведения карточек.** Т-50 измерил: потолок покрытия якоря ставит НЕ механизм, а форма
  подачи — `no_grammar` даёт 77.6% и 79.5% отказов. Ещё код в `loc.py` покрытия не поднимет.
  Ирония, записанная в журнал: обе карты, заведённые мной в тот же день, попали в `no_grammar`.
- **Прежний кандидат жив:** класс «сверка не отличает КОПИЮ от ПЕРЕЕЗДА», триггер арифметический
  — первый `moved_file` в живых данных, чьё назначение не исходный код.

## 4. Наблюдения процесса

- **К-11 (предложен): диспетчеризация обязана быть РЕСТАРТУЕМОЙ.** Промпт длинного агента несёт
  «веди заметки в `/tmp/<юнит>-notes.md`» и «коммить рано и часто». Оплачено шестью обрывами
  сторожа за сессию; Т-46 на четвёртом старте поднялся с готового дерева только по заметкам,
  Т-50 уцелел ядром, закоммиченным перед смертью.
- **К-12 (предложен, применялся): отчёт исполнителя ПЕРСИСТИРУЕТ уровень (2)**, дословно, ДО
  запуска приёмщика. Иначе п.4 контракта структурно неисполним — приёмщик вернул по нему BLOCKED,
  потому что отчёт существовал только как переписка.
- **Я ошиблась ЧЕТЫРЕЖДЫ за сессию, и все четыре — ОДНА форма: утверждение о покрытии/популяции
  без проверки пути.** «Пять реестров» над списком из шести; закрытый список секций; классификация
  CB-28, опровергнутая приёмщиком (К-13 родился отсюда); и «golden годен как гейт» — менеджер
  показал мутантом, что снимок собирается НЕ через адаптер. **Гейт годен не потому, что данные
  попадают в снимок, а потому, что снимок проходит через починенный код.**
- **Субагенты безупречны в ИЗМЕРЕНИЯХ и ошибаются в КЛАССИФИКАЦИЯХ** — числа Т-50 сошлись до
  последней цифры, развёртка Т-61 переиграна после форвард-мерджа; а «это блок кода» и «это форма
  CB-28» оказались неверны. Измерение несёт свою операцию, классификация — суждение.
- **Дважды за сессию живой MCP-сервер отдавал устаревший пакет** (снимок pipx старше `loc.py`;
  работающий процесс держит старые описания до перезапуска). Кодом не чинится — операционное.
