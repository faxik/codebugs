# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), 2026-08-25
(вытесняет `HANDOFF-DIR-2-2026-08-23.md`; следующий вытесняет этот)

Написан уходящей сессией 8 при **ротации ВСЕГО каскада** — куратор ротируется, моя сессия жила
внутри его и не переживает. **Правил здесь нет намеренно** — только указатели, факты и первые
действия. Преемник стартует от НОРМАТИВНЫХ файлов, а не от пересказа.

## 0. Нормативные входы — УКАЗАТЕЛИ; сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13 (контракт приёмки — **ВОСЕМЬ** пунктов), §16.3 (класс по Р1, тиры),
  §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md` — журнал
  фактов, шаблон юнит-брифа (каноны К-1…К-13), эскалации, персистированные отчёты исполнителей.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- **Ратифицированный дизайн BT-7:** `.claude/plans/BT-7-location-anchor.md` — **ИСПОЛНЕН ЦЕЛИКОМ**,
  нарезка §9 закрыта. Несёт мою поправку §1б (число дизайна не воспроизвелось на его же корпусе).
- **Закрытая BT-8:** `.claude/plans/BT-8-merge-policy.md`.
- **Улики замеров:** `BT-7-MEASUREMENTS-FLAGS-2026-08-23.md`,
  `BT-7-T50-BACKFILL-MEASUREMENTS-2026-08-24.md`, `T61-CB-157-survey.md`.
- **Правила репозитория:** `CLAUDE.md`. **Аллокатор ID:** `tools/cascade-mint.sh` (номер ВЫЧИСЛЯЕТ
  скрипт; литерал не набирать — он защищает НОМЕР, но не производные от номера поля).

## 1. Фактическое состояние по git (перепроверь на месте)

**main @ `fca898f`** на момент написания. `origin/main` отставал на 1. Живых деревьев DIR-2 нет;
живых клеймов DIR-2 нет. Дерево DIR-1 `fix/t64-cb-152-instrument-truth` — чужое, не трогать.

**Приземлено и ПРИНЯТО сессией 8** (пять юнитов, SHA сверены лично):

| юнит | мердж | что |
|---|---|---|
| Т-43 | `01d405d` + `8367958` | BT-7 Т-b: `anchor_resolve`/`anchor_recapture` |
| Т-46 | `7366228` | CB-131 + оба остатка отклонённого Т-41 |
| Т-50 | `ed369ac` | BT-7 Т-c: backfill ЯКОРЕЙ |
| Т-56 | `c83352a` | якорь виден в обычных путях чтения (`get`/`query`/`staleness_check`) |
| Т-61 | `ea77df9` | CB-157: механизм против «декоративного аргумента» |

**Т-63 (`0d791bb`) ПРИЗЕМЛЁН, приёмка была В ПОЛЁТЕ и вердикт НЕ ЗАПИСАН.** Это единственный
незакрытый долг направления. Признак работы приёмщика — брошенный worktree `/tmp/t63-probe-sut`
(detached на `0d791bb`); если приёмщик умер, снять `git worktree remove` (без `--force`, сперва
посмотрев `git status` внутри).

**BT-7 ЗАКРЫТА** по всей нарезке §9 (Т-0, Т-a, Т-b, Т-c).

**Backfill якорей ПРИМЕНЁН на обоих трекерах** по ратификации владельца (реестр `f5f5fd5`),
исполнен мной по протоколу бэкап → apply → контрольный dry-run: codebugs 22 координаты / 136
отказных объектов, покрытие 0 → 13.92%; autosorter 272 / 3083, 0 → 8.11%. Идемпотентность
подтверждена (повторный dry-run даёт ноль кандидатов). **Бэкапы:**
`/home/faxik/tracker-backups/{codebugs,autosorter}-findings-20260824-191701Z.db`.

**Т-48 / CB-143 ЗАПАРКОВАН** по переприоритезации владельца (CSV — низкий приоритет). Клейм
отпущен, карта `open`, **ветка `fix/cb-143-stdout-alias-diagnostic` и бриф СОХРАНЕНЫ** —
возобновление стоит одного `worktree-setup`. **Не потеряй:** популяция этого дефекта — ДВА сайта
(`findings.py` и `reqs.py`), карта называет только один.

**Карты сессии 8:** CB-156 (`fixed`, Т-63), CB-157 (`fixed`, Т-61), **CB-161** (интерполяция
`last_n` в SQL, low, open), **CB-162** (экземпляр CB-28, low, open), **CB-163** (граф описан
дважды, medium, open — ЧУЖАЯ территория, форму не выбирать единолично).

**Очередь, зафиксированная куратором:** CB-156 → CB-62. **CB-156 УЖЕ ЗАКРЫТА** юнитом Т-63, то
есть указание куратора на момент его написания отставало от git; действующая очередь — **CB-62**,
затем CB-161/CB-162 (обе с неизмеренной популяцией).

**Нарезка CB-62 зафиксирована** (журнал, `734eb1a`), три единицы, не одна:
1. **Механика:** `group_by="tag"` и произвольный ключ `meta` в `query_findings`/`get_stats`. Ось
   есть (`grouping_filing` по фиксированному `("sprint","plan")`), параметризации нет.
2. **Семантическая ось — кандидат в BT, не в механическую полосу:** вектора не пробрасывает ни
   одна поставленная поверхность, блок по категории не отключаем, нужен порог со своей
   калибровкой. Два пункта нарезаются **ВМЕСТЕ** — по замеру самой карты: поодиночке они не
   прятали ничего видимого, вместе дважды за два дня дали уверенный ложный ответ «дубликатов нет».
3. **`distinct_from` подавляет группировку — СУЖЕННО**, в единственном существующем потребителе
   (компоненты графа цитат в `grouping`), с явным объявлением, что графы это не объединяет.

**Эскалация про граф, описанный дважды, — вынесена ОТДЕЛЬНОЙ картой CB-163**, решение уровня (2)
по прецеденту CB-44/CB-45. Суть: `split_from` живёт И как тип связи в `relations.py`, И как ключ
`meta`, который траверсит `grouping.py`; ребро, поставленное одним путём, невидимо другому, и два
поставленных тула отвечают на один вопрос по-разному. Вопрос принадлежности БЛОКИРУЕТ пункт (3)
нарезки, но не РЕШАЕТСЯ им — `relations.py` появился вне DIR-2. Популяция (`meta.split_from`
против рёбер в таблице) не измерена и обязана предшествовать выбору формы.

**Три части CB-62 УЖЕ закрыты и заказывать их заново нельзя:** бэклинки «кто цитирует CB-N»
(`grouping.citation_report` — это юнит Т-21 моего же направления), траверс
`split_from`/`split_children` с containment, и провенанс-колонки в `export-csv` (закрыто CB-97 —
утверждение карты об этом сегодня ЛОЖНО).

**Каноны:** К-10 и К-13 ратифицированы куратором; К-11 и К-12 предложены. К-12 применялся и
окупился за один юнит — п.4 контракта впервые вернулся PASS вместо BLOCKED.

## 2. Первые действия преемника (по порядку)

1. **Сверить §0 с источниками.** Расхождение чинится в handoff, не в источнике.
2. **Закрыть долг Т-63:** получить/перезапустить приёмку и записать вердикт ФАКТОМ в журнал.
   Приёмщику было особо вменено проверить моё **ложное** утверждение о гейте (§4 ниже) и оценить
   мою классификацию по К-13. Брошенный worktree `/tmp/t63-probe-sut` снять.
3. **CB-62** — минтить и брифовать по зафиксированной нарезке, начиная с пункта (1), он
   механический. Пункт (2) — кандидат в BT, то есть дизайн-док на ратификацию, а не юнит.
4. **CB-161 / CB-162** — механическая полоса; у обеих популяция не измерена, развёртка
   предшествует починке.
5. **CB-163** — форму несёт куратору/владельцу; единолично не решать.

## 3. Открытое для владельца

Вопрос про `--apply` **закрыт**: владелец ответил «Применяй», исполнено, числа в журнале.
Открытых BT — ноль. Назревают два, оба ждут арифметики, а не мнения:

- **Форма заведения карточек.** Т-50 измерил: потолок покрытия якоря ставит НЕ механизм, а форма
  подачи — `no_grammar` даёт 77.6% (codebugs) и 79.5% (autosorter) всех отказов, `no_commit` лишь
  6.4%/11.1%. **Ещё код в `loc.py` покрытия не поднимет.** Ирония, записанная в журнал против
  себя: обе карты, заведённые мной в тот же день, попали в `no_grammar`.
- **Прежний кандидат жив:** класс «сверка не отличает КОПИЮ от ПЕРЕЕЗДА» (§10 п.4г дизайна),
  запинен как известный предел; триггер арифметический — первый `moved_file` в живых данных, чьё
  назначение не является исходным кодом.

## 4. Наблюдения процесса

- **К-11 (предложен): диспетчеризация обязана быть РЕСТАРТУЕМОЙ.** Промпт длинного агента несёт
  «веди заметки в `/tmp/<юнит>-notes.md`» и «коммить рано и часто». Оплачено шестью обрывами
  сторожа за сессию: Т-46 на четвёртом старте поднялся с готового дерева ТОЛЬКО по заметкам,
  Т-50 уцелел ядром, закоммиченным агентом прямо перед смертью.
- **К-12 (предложен, применялся): отчёт исполнителя ПЕРСИСТИРУЕТ уровень (2)**, дословно, ДО
  запуска приёмщика. Иначе п.4 контракта структурно неисполним — приёмщик вернул по нему BLOCKED,
  потому что отчёт существовал только как переписка. После введения — сразу PASS.
- **Я ошиблась ЧЕТЫРЕЖДЫ за сессию, и все четыре — ОДНА форма: утверждение о покрытии или
  популяции без проверки ПУТИ.** «Пять реестров» над списком из шести; закрытый список секций при
  замере; классификация CB-28, опровергнутая приёмщиком (К-13 родился отсюда); и «golden годен как
  гейт» — менеджер показал мутантом, что снимок собирается НЕ через адаптер. Формула для
  преемника: **гейт годен не потому, что данные попадают в снимок, а потому, что снимок проходит
  через починенный код.**
- **Субагенты безупречны в ИЗМЕРЕНИЯХ и ошибаются в КЛАССИФИКАЦИЯХ.** Числа Т-50 сошлись до
  последней цифры, развёртка Т-61 переиграна после форвард-мерджа — а «это блок кода» и «это форма
  CB-28» оказались неверны. Измерение несёт свою операцию, классификация — суждение. Отсюда К-13
  в обеих половинах, включая постоянный слот в промпте приёмки «оцени классификацию держателя, не
  принимай её».
- **Дважды за сессию живой MCP-сервер отдавал устаревший пакет:** снимок pipx оказался старше
  `loc.py` (лечится `pipx reinstall`, выполнено куратором), и работающий процесс держит старые
  описания до перезапуска. Кодом не чинится — операционное, знать при чтении «якорь = 0».
