# DIR-1 — «Доверие и координация» (Н1/Н3)

Документ направления, уровень (2) каскада планирования. Заведён 2026-08-19.
Держатель: сессия уровня (2), роль принята 2026-08-19.

**Что это за файл.** Курируемый артефакт уровня (2) по §3 спеки каскада: авторское суждение,
не состояние. **Живых статусов карт здесь нет и быть не должно** — авторитет статуса карты
это трекер (`mcp__codebugs__query`), авторитет порядка болей — `reqs_get PAIN-ORDER`.
Зеркалирование одного факта в оба хранилища запрещено. Здесь: разрезы на большие задачи,
ратифицированные расхождения, неизменяемые факты приёмки, эскалации.

Вход: `L2-BRIEF-DIR-1-trust-coordination-2026-08-19.md` (хендофф куратора).
Спека ролей: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`.
Реестр болей (тексты, рационалы): `.claude/plans/PAIN-REGISTRY-codebugs.md` §1–§2,
ратифицированы владельцем 2026-08-19.

---

## §1. Замысел направления (из брифа, не переписан)

codebugs — кросс-проектный трекер, которым координируются параллельные агенты. Направление
отвечает за две ратифицированные ценности владельца:

- **Н1 «инструменту можно верить».** Главный враг — *success-shaped lie*: тихо потерянная
  ошибка, ложно-зелёный тест, guard «чистый, потому что не смог посмотреть».
- **Н3 «координация механическая, не конвенция».** Клейм и индекс вместо статуса и дисциплины.

**Критерий успеха направления:** у каждого success-shaped-пути в скоупе есть либо фикс, либо
записанное решение «почему нет»; координационные примитивы не полагаются на поведение сессий.

Формулировка «либо фикс, либо записанное решение» — рабочая: закрытие карты и запись
«не чиним, потому что …» имеют здесь равный вес. Что запрещено — оставить путь без ответа.

## §2. Скоуп

**Боли:** Б-3-остаток, Б-5, Б-6, Б-7, плюс отдельная карта CB-100.
**Файлы направления:** `merge.py`, `milestones/`, `blockers.py`, `bench.py`, `cli.py`,
`server.py`, `tools/`.
**Не мои (DIR-2, identity):** `findings.py`, `similarity.py`, `provenance.py`.
**Точки сериализации (общие с DIR-2):** `types.py`, wire golden
(`tests/golden/mcp_schema.json`), `CLAUDE.md`. При касании — проверить claims/in-flight и
сериализоваться, а не мержить параллельно.

**Явно вне скоупа по решению куратора:** CB-55 не диспетчеризуется до ратификации В-5
(exposure-RFC) — её error ladder дословно входит в standard handler того слоя, и standalone-фикс
построил бы абстракцию, которую слой удалит.

## §3. Состояние на момент заведения (снято 2026-08-19, П-А: передобыто, не унаследовано)

Снимок, а **не** живой статус — перепроверять трекером перед любым действием.

- Все 20 карт скоупа (CB-6/29/32/33/34/35/37/38/42/55/56/59/66/68/80/84/87/100/106/107) —
  `open`. Проверено `query(ids=[...])`, 2026-08-19.
- Живых claims: **0** (`claims_list` → `{"count":0}`).
- Worktrees: только primary; `main` = `d576683`, дерево чистое кроме плановых `.md`.
- Т-1 (CB-58) принята куратором на мерже `9337175` — вход направления, не его работа.

## §4. Большие задачи

Определение (§13): **BT = одно нерешённое проектное решение + его карты-симптомы.**
WIP-лимит: **≤1 открытая BT**. BT выносится владельцу на ратификацию **до** диспетчеризации;
кросс-модельный проход (`adversarial-review-x2`) каждой BT — обязателен (§8.3б).

Разрезы — в §7 (кандидаты) и §8 (принятые). Пока не ратифицировано ни одной.

## §5. Диспетчеризованные юниты

| Юнит | Карты | Состояние процесса | Ссылка |
|---|---|---|---|
| Т-2 | CB-106 | **принят (2)←(3) 2026-08-19** | `L3-HANDOFF-T2-cb106-codemerge-closeability.md` |
| Т-6 | CB-111 | **принят (2)←(3) 2026-08-20** | `L3-BRIEF-DIR-1-T6-cb111-abandon-returning.md` |

*Колонка «состояние процесса» — про каскад (передан / принят / возвращён), не про статус карты.*

## §6. Ратифицированные расхождения (letter fixed, meaning preserved)

**Р-1. §7 п.1 «клейм на каждую карту юнита при диспетчеризации» исполняется ХАРНЕСОМ, а не
держателем направления — для юнитов, все карты которых названы в имени ветки.**

- *Добытый факт:* `tools/worktree-setup.sh:70-74` выводит `CB_IDS` **исключительно** из имени
  ветки (`grep -oiE 'cb-?[0-9]{1,}'`), и клеймит каждую тройкой
  `--holder <branch> --holder-kind branch --repo <root>` (`:182`, `:198`). Флага `--items`
  в этом порту харнеса **нет** (проверено grep'ом по всему файлу; `--items` — из скрипта
  autosorter, описанного в CLAUDE.md, не из этого репозитория).
- *Почему буква ломает замысел:* владение — это **тройка**. Пре-клейм от моего имени
  (`holder=DIR-1`, `kind=agent`) — другой претендент, поэтому `worktree-setup.sh` получил бы
  `held_by_other` → **exit 3, FATAL**, и оборвал бы setup сессии (3). Т.е. буквальное
  исполнение п.1 сделало бы диспетчеризацию невозможной.
- *Замысел п.1* — измеренная утечка «попутная карта юнита осталась `open`», т.е. «клеймится
  КАЖДАЯ карта юнита, а не только первичная из имени ветки». Для **однокарточного** юнита,
  чья карта названа в ветке, харнес удовлетворяет замысел полностью.
- *Решение:* для однокарточных юнитов клеймит харнес; я не пре-клеймлю. Для **много**карточных
  замысел сегодня **структурно не удовлетворён** — см. Э-1.

**Р-2. Канон клейма ратифицирован куратором 2026-08-19: КЛЕЙМИТ ХАРНЕС, пре-клейм
направлений ЗАПРЕЩЁН.** Р-1 выше был моим решением; теперь это не моё расхождение, а норма
каскада. Обоснование куратора — сравнение режимов отказа, а не предпочтение: окно
«диспетчеризация → setup без клейма» отказывает БЕЗОПАСНО (двойная выдача ловится setup'ом,
exit 3), тогда как ручное снятие пре-клейма при смерти сессии оставляет карту за мёртвым
агентом без реапера. Следствие для меня: Р-1 остаётся как запись рассуждения, но действующее
правило — Р-2, и оно шире (относится и к однокарточным, и к многокарточным юнитам).
Примитив передачи владения (`transfer` / `--on-behalf-of`) отнесён **в скоуп BT-2**.

## §7. Кандидаты в большие задачи

Разрезы черновые до ратификации владельцем. Каждый обязан пройти `adversarial-review-x2`.

**BT-1 (кандидат). Механический энфорсмент транзакционного правила CB-24.**
Нерешённое решение: **чем** энфорсить (CB-37; записанная ловушка — наивный AST-предикат
сертифицирует ровно тот баг, который ловит). Карты-симптомы: CB-87.
Предусловие разреза: **передобыть популяцию** — урок репо «популяция всегда больше списка»
(CB-24 назвала 4 сайта, сweep нашёл 19). Передобыча запущена.
*Первое подтверждение получено раньше сweep'а и не из него:* preflight Т-2 наткнулся на
**`merge.abandon_session` (`merge.py:141-156`)** — читает через `_get_session`, пишет двумя
`execute`, закрывает голым `conn.commit()` **вне `db.txn`**, затем **перечитывает строку
после коммита** вместо `UPDATE … RETURNING *` (consequence-5 из CB-30). Спот-чекнуто лично.
Ни CB-87, ни разбор CB-36 этот сайт не называют. Т.е. популяция снова больше списка — и
нашлась она попутно, что само по себе аргумент за механический энфорсмент, а не за сweep.

### BT-1: передобытая популяция (сweep 2026-08-19, метод и цитаты — ниже)

Метод: `grep -rn "conn.commit()" src/` → **47 совпадений, 28 исполняемых** (19 — проза в
докстрингах, в основном предупреждения «не возвращай `conn.commit()`», оставленные CB-24/40);
затем чтение тела каждой функции и классификация. Пересечение с уже обёрнутыми в `db.txn`
функциями — **пустое**, множества дизъюнктны по построению.

- **15 из 28 — `ensure_schema`/миграции** (DDL-бутстрап). Классифицированы безопасными.
- **13 живых нарушений**, все — **check-then-act (класс B)**.
- **Read-modify-write (класс A) — НОЛЬ.** Все CB-24-образные merge'и `meta` уже под `db.txn`.

**Это и есть решающий факт для разреза BT-1**, и он сильнее, чем «популяция больше списка».
CB-37 записывает ловушку так: наивный AST-предикат «сертифицирует ровно тот баг, который
ловит, и слеп к чтениям за хелперами и к cross-table check-then-act». Измерено:
**100 % живой популяции — ровно тот класс, к которому предикат слеп.** Пять чтений спрятаны
за хелперами (`sweep._resolve_sweep:130`, `merge._get_session:65`,
`_spine._milestone_exists:32`, `_spine._get_milestone:38`, `_spine._validate_item_ref:47`).
Т.е. решение CB-37 нельзя принимать как «написать ратчет по `conn.commit()`» — такой ратчет
дал бы зелёный на всей популяции. Формулировка нерешённого решения уточняется:
**чем ловится check-then-act через границу хелпера, если синтаксического признака нет.**

Живые сайты (спот-чекнуты лично помеченные **[СЧ]**):

| Функция | commit | Что пересекает разрыв |
|---|---|---|
| `sweep.create_sweep` | `sweep.py:232` | существование `name` решает, кидать ли, до INSERT |
| `sweep.archive_sweep` | `sweep.py:592` | lookup-then-write через `_resolve_sweep` |
| **[СЧ]** `merge.abandon_session` | `merge.py:154` | существование сессии гейтит записи; + перечитывание после коммита |
| `blockers.add_blocker` | `blockers.py:174` | дубль-проверка **ничем не подпёрта** — нет UNIQUE-индекса, два конкурентных вызова вставят дубль блокера |
| `bench.delete_run` | `bench.py:771` | `result_count` прочитан до DELETE и возвращается уже неверным |
| **[СЧ]** `bench.delete_benchmark` | `bench.py:804` | **асимметрия удаления**: результаты — по снятому списку `run_ids` (`:800-802`), сами прогоны — безусловно `WHERE benchmark = ?` (`:803`); прогон, вставленный в разрыв, теряет строку и **оставляет осиротевшие результаты** |
| `embeddings.store_embedding` | `embeddings.py:62` | при удалении требования в разрыве UPDATE матчит 0 строк, функция возвращает `"stored": True` |
| `milestones.create_milestone` | `foundation.py:57` | подпёрто PK → гонка даёт необработанный `IntegrityError` |
| **[СЧ]** `milestones.update_milestone` | `foundation.py:92` | `from_state = current["state"]` (`:74`) — **устаревшее прочитанное значение уносится в audit-INSERT** (`:90-91`); аудит записывает переход, которого не было. Плюс `return _get_milestone(conn, id)` (`:93`) — перечитывание после коммита, consequence-5 CB-30; сweep этого не назвал, нашёл я |
| `milestones.add_milestone_item` | `foundation.py:282` | подпёрто UNIQUE → гонка даёт необработанный `IntegrityError` |

Ратчета, покрывающего эту популяцию, **нет**: существующие (`test_claims.py::test_24…` —
считает сырые `BEGIN IMMEDIATE`; `TestOpenCallSitesRatchet`; `TestWriteCallSitesRatchet`;
`TestEscalateOptOutRatchet`) считают другие популяции.

**Следствие для разреза.** Симптомы BT-1 шире CB-87 (`bench.delete_run`/`delete_benchmark`):
это 10 сайтов в 6 файлах. Два из них — не TOCTOU-на-существование, а **порча данных**:
осиротевшие результаты в `delete_benchmark` и ложная запись аудита в `update_milestone`.
Оба — Н1 буквально (success-shaped lie: операция отчитывается успехом поверх испорченной
записи). `add_blocker` — третий, отдельного рода: единственная защита от дубля снята
конкурентностью, потому что за проверкой нет индекса.
**Открытый вопрос разреза, который я обязан вынести владельцу:** BT-1 — это «решение CB-37»
(один энфорсмент) ИЛИ «решение CB-37 + починка 10 сайтов»? Второе — не одна большая задача,
а программа. Рекомендую резать BT-1 как **решение + 3 сайта с порчей данных**, остальные
семь — механической полосой после того, как энфорсмент назван.

**BT-2 (кандидат). Судьба лизинга и слотов: одно решение о liveness/владении.**
Нерешённое решение: чем заменяется TTL как признак живости держателя. Карты-симптомы:
CB-42 (TTL-lease не даёт mutual exclusion в точке использования), CB-38 (слот capacity течёт
при выходе не через `release_item`). Смежная запись CLAUDE.md: renewal делает expiry
неразрешающим на self-owned пути, но не ограничивает живого-но-заклинившего держателя —
«это нужен liveness detection, не timestamp». Т.е. решение уже названо в коде как отсутствующее.

*Разрез уточнён приёмкой Т-2, и уточнение существенное.* Исполнитель воспроизвёл end-to-end,
а Codex независимо подтвердил из другого направления: **дело не в TTL и не в
`abandon_session` — НИЧТО в модуле не связывает сессию с её вызывающим.** `codemerge_finish`
— MCP-тул с самого начала — принимает чужой `session_id`, проходит гейт `merging`, помечает
чужую сессию `done` и отпускает её лок, после чего третья сессия получает `proceed: True`,
пока чужой git-мерж ещё идёт. Значит формулировка BT-2 расширяется: **fencing-token обязан
авторизовать ВЫЗЫВАЮЩЕГО, а не только упорядочивать записи.** Это же объясняет, почему Т-2
намеренно не гейтил `codemerge_abandon` — гейт на одном туле переместил бы дыру, а не закрыл.
CB-42 несёт оба доказательства.

*И третье расширение, уже от куратора (решение по Э-4, 2026-08-19):* примитив **передачи
владения клеймом** (`transfer` / `claim --on-behalf-of`) отнесён в скоуп этой же BT — на том
основании, что лизинг, слоты и передача клейма суть **одно решение о владении**, а не три.
Т.е. BT-2 теперь охватывает владение и в codemerge (fencing-token для вызывающего), и в
claims-леджере (передача), и в capacity (утечка слота). Это заметно крупнее исходного
разреза CB-42+CB-38, и при подготовке к ратификации я обязан проверить, не распалась ли она
на две.

**BT-3 (кандидат). Подготовка В-5 (exposure-RFC) к ратификации — НЕ реализация.**
Дешёвый первый шаг из брифа: capability matrix. Продукт — варианты владельцу **с ценой**,
не миграция. Управляет судьбой CB-55 / CB-6 / CB-80 / CB-107.

## §8. Принятые большие задачи

*(пусто)*

## §9. Неизменяемые факты приёмки

Пишутся строкой, никогда не редактируются (П-В: приёмка не завершена, пока артефакт уровня
не отражает принятое как неизменяемый факт).

**Т-2 (CB-106) ПРИНЯТ держателем DIR-1 2026-08-19 на мерже `f3cc0cd`.** Приёмка (2)←(3) по
§13 — пп. 1, 4, 5 плюс соответствие замыслу направления:

- **п.1 (дифф прочитан против ЗАМЫСЛА, не задания) — выполнено лично.** Замысел «клиент,
  способный открыть сессию, способен её и закрыть; описание тула не лжёт» удовлетворён
  обеими половинами: `codemerge_abandon` объявлен тулом (поверхность 5 → 6), докстринг
  `codemerge_finish` приведён к коду (`status→active`) и указывает на новый тул. Скоуп диффа
  чистый — `merge.py`, его тесты, golden, дизайн-док; в чужие файлы не залез.
  Проверено сверх отчёта: гейт `WHERE session_id=? AND status != 'done'` NULL-безопасен,
  потому что `status TEXT NOT NULL DEFAULT 'active'` (`merge.py:35-37`) — обычная для этого
  репо ловушка «`NOT NULL` есть NULL, а `WHERE NULL` исключает» здесь не срабатывает.
- **п.4 (слот эскалаций заполнен явно) — выполнено**, три пункта, разобраны в §10.
- **п.5 (артефакт уровня несёт неизменяемый факт) — эта строка.**
- **Соответствие замыслу НАПРАВЛЕНИЯ (Н1):** юнит закрыл success-shaped-путь и не оставил
  ни одного без ответа — то, что не чинилось, вынесено картами (CB-110, CB-111) или
  доказательством в существующую карту (CB-42), а не умолчанием.

**Доказательства перепроверены мной, а не приняты на слово:** мердж `f3cc0cd` — настоящий
merge-коммит на first-parent main; `1664 passed`, `ruff … All checks passed!` — прогнаны
заново на main; `codemerge_abandon` присутствует в `tests/golden/mcp_schema.json`; CB-106 в
трекере `fixed`. Десять мутационных проб (девять красных, одна зелёная-негативная) приняты
по отчёту исполнителя — сам их не повторял, и это граница приёмки, а не утверждение.

**Что юнит сделал сверх задания, и почему это принято, а не сочтено scope creep:** отказ
абандонить `done`-сессию (иначе фикс уничтожал бы запись об успешном мерже — третье
следствие самой карты, приходящее через её же фикс), снятие оверклейма в двух описаниях и
починка лжи в `allow_restart`. Всё это — тот же дефект «описание лжёт клиенту», что и
задание; расширение по замыслу, а не по объёму.

**Т-6 (CB-111) ПРИНЯТ держателем DIR-1 2026-08-20 на мерже `c0dce5e`** (рабочий коммит
`c00df82`, ветка `fix/cb-111-abandon-returning` сохранена). Приёмка прогнана **изолированным
приёмщиком** через `/acceptance-check`: в моё окно вернулся вердикт, улики остались у него —
это и есть смысл скилла, и я его соблюдал (сам дифф не читал).

Семь пунктов §13: **п.1 PASS** (замысел, включая соблюдение раздела «чего не делать» брифа —
чтение не обёрнуто ради CB-24, гейт владения не добавлен, соседние сайты популяции не тронуты);
**п.2 PASS в обе стороны** — откат `src/codebugs/merge.py` на `c0dce5e^1` роняет 3 из 4 новых
тестов, а мутант ловушки 3 (чтение `rowcount` сразу после `execute`) даёт 10 падений;
**п.2b PASS** — недискриминирующий четвёртый тест ЯВНО подписан как пин сохранённого поведения
(`tests/test_merge.py:333` и его докстринг), по правилу репозитория; **п.3 PASS** — оба
`conn.commit()` ушли, `rowcount` исполняемо нигде не читается, ложный комментарий отсутствует
в `src/`, `session_id` действительно `TEXT PRIMARY KEY`; **п.4 PASS**; **п.5** — эта строка;
**п.6 n/a с обоснованием** — механизма post-merge-gate в репозитории нет, и
`tools/worktree-finish.sh:10` говорит это прямым текстом; **п.7 PASS** — карта терминальна,
клейм снят. Wire golden не сдвинулся (md5 `41885826207c1dd3e6375e070aa3339a` по обе стороны),
содержимое мерджа ограничено `merge.py` и `tests/test_merge.py`.

**Замечание приёмщика, принятое и исполненное:** признание исполнителя о том, что его ПЕРВЫЙ
мутант ловушки 3 был эквивалентным (чтение `rowcount` стояло после `fetchone()`, где значение
уже 1, — то есть проба не могла упасть по построению), нигде в приземлённых артефактах не
фиксировалось. Дописано в CB-111. **Мутационная проба, поставленная не в ту точку, зелена по
построению и неотличима от пройденной** — тот же класс, что вакуумный тест, и ровно то, ради
чего п.2 требует прогона в обе стороны.

**Известный дефект, на вердикт не влияющий:** субжект мерджа `c0dce5e` описывает чужую заметку
плана (BT-4 от DIR-2) — заведено как CB-116, содержимое мерджа при этом корректно.

## §10. Эскалации наверх (куратору / владельцу)

Слот обязателен и может быть явно пуст. Сейчас **не пуст**.

**Разобранные куратором 2026-08-19 (коммит `2e3c4f7`) — оставлены как запись, не как
открытые:**
- **Э-7 РЕШЕНА.** Канон: правка кураторских артефактов коммитится на main **немедленно тем
  же ходом**; WIP уровня (1) на main не живёт. Применяю то же к своим — с этого момента
  каждый артефакт DIR-1 уходит на main в том же ходе, что и создаётся.
- **Э-4 РЕШЕНА** как интерим-канон в пользу варианта DIR-1 → перенесена в §6 как **Р-2**;
  примитив передачи владения ушёл в скоуп BT-2 (§7).
- **Э-1 ЗАКРЫТА ЗАВЕДЕНИЕМ КАРТЫ: CB-112**, отнесена к боли Б-3. Карта несёт fix shape
  (портировать `--items`, объединить с branch-derived `CB_IDS`, переиспользовать
  существующий цикл клейма, обработку кодов возврата и EXIT-trap) и **два ограничения,
  которые фикс обязан не сломать**: pure-git branch-collision guard остаётся branch-keyed,
  а `CODEBUGS_SETUP_NO_CLAIM` и `--allow-duplicate` сохраняют текущую семантику.
- **Э-5 принята к сведению**, аллокатор трекается с `20c896b`.
- **Э-6 закрыта** ретро-проходом (см. ниже, там же практический урок про Codex).

**Открытых эскалаций от DIR-1 на этот момент нет.** Слот пуст **явно**: всё, что было
поднято, разобрано куратором или закрыто заведением карты. Следующая порция придёт с
вердиктом кросс-модельного ревью BT-1.

**Э-1. Замысел §7 п.1 структурно не удовлетворён для многокарточных юнитов — в этом репо
нечем клеймить попутную карту.**
`worktree-setup.sh` берёт карты только из имени ветки (§6, Р-1). Юнит из двух карт
(`CB-42`+`CB-38`, `CB-32`+`CB-33`+`CB-34`) оставит вторую и третью незаклеймлёнными —
ровно измеренная утечка, ради которой п.1 написан. Это **Н3 буквально**: координация
опирается на то, что человек вспомнит доклеймить руками.
Цена: без ответа BT-2 и любой пакет по Б-6 диспетчеризуются с известной дырой.
Варианты: (а) портировать `--items` из autosorter-скрипта в `tools/worktree-setup.sh`
(инженерная работа в моём скоупе — `tools/` мой); (б) конвенция «одна карта на ветку» —
но это конвенция, т.е. ровно то, что Н3 запрещает; (в) принять как границу и записать.
**Рекомендую (а)** и прошу разрешения завести карту.
*Не блокирует Т-2* (однокарточный юнит).

**Э-6. Т-2 приземлился БЕЗ гетерогенного кросс-модельного прохода, и я принял его всё
равно — с оговоркой.** Codex/Sol дважды упал по таймауту 600 s, агент Antigravity вернул
пустой отчёт. Покрытие — два независимых раунда Opus, второй прицельно по фиксам первого,
и это окупилось: обе структурные добавки первого коммита оказались дефектны (отказ гейта
оставлял открытую write-транзакцию; проверка описания сравнивала МНОЖЕСТВО статусов и
поэтому проходила при переставленных ветках — ложь CB-106, пересказанная её же фиксом).
Стоячее правило требует Codex для merge-gating изменений; здесь его не было. Принял,
потому что дифф хирургический, десять мутационных проб и два раунда нашли структурное, а
не косметику — но **отклонение записано, а не замолчано**, и это ровно тот случай, для
которого §8 держит независимый детектор escape.

**Ретро-проход ВЫПОЛНЕН тем же вечером и закрыл отклонение.** Codex/gpt-5.6-sol, read-only,
по приземлённому диффу. Первый заход снова упал по таймауту — он тратил бюджет на обход
репозитория; со вторым, где обход был запрещён явно, а факты схемы поданы в промпте, ответ
пришёл за минуты. **Урок практический: Codex здесь падает не от сложности задачи, а от
разведки; давай ему самодостаточный дифф.**
Вердикт: **ни одного нового блокирующего дефекта.** Две находки уровня HIGH — обе
подтверждают уже записанные остатки, а не открывают новые: (1) `conn.commit()` на пути
отказа коммитит незакоммиченную работу вызывающего — это CB-111, и Codex добавил к нему
резкость, которой у карты не было (компенсирующий `ROLLBACK` вызывающего молча ничего не
делает, плюс комментарий «empty transaction» ложен при внешней транзакции); (2) любой
клиент может закрыть чужую сессию — это CB-42, и Codex пришёл к нему **сам, из другого
направления**, сценарием через `merging` и лок, что независимо подтверждает решение НЕ
гейтить один `codemerge_abandon`. Гейт `status != 'done'` Codex признал бездефектным,
совпав с моей собственной проверкой на NOT NULL.
**Значение для леджера §8.2: улов ретро-прохода = 0 изменённых исходов.** Приёмка устояла,
и это измерение, а не самоотчёт.

**Э-7. Некоммиченный артефакт уровня (1)/(2) в main БЛОКИРУЕТ интеграцию любого юнита —
системно, не разово.** `_guard_main_clean` отказал финишу Т-2 на ~5 минут из-за
незакоммиченной правки `PAIN-REGISTRY-codebugs.md`; DIR-2 эскалировал то же самое коммитом
`d196407`. Исполнитель правильно не стал коммитить и стэшить чужой живой файл. Это цена
партиции §3 (суждение → .md в том же репо, что и код): пока куратор редактирует реестр,
конвейер стоит. Прошу решения — держать кураторские артефакты в отдельном дереве/ветке,
либо принять как норму с явным протоколом «редактируй реестр только между интеграциями».

**Э-4. Два направления разрешили ОДИН протокольный пробел ПО-РАЗНОМУ, и это Н3 буквально:
в леджере нет примитива ПЕРЕДАЧИ клейма.** Наблюдено вживую, не выведено.

- Я (Р-1): **не пре-клеймлю**, клеймит харнес. Подтверждено практикой — `CLM-14` на CB-106,
  `holder=fix/cb-106-codemerge-closeability`, `kind=branch`, взят `worktree-setup`.
- DIR-2: **пре-клеймит** от `holder=DIR-2, kind=agent` (`CLM-12` на CB-60, `CLM-13` на CB-53)
  и в примечании самого клейма пишет инструкцию: *«Release this claim (holder=DIR-2,
  kind=agent) immediately before executor worktree-setup — setup takes the branch claim»*.

Оба варианта работают, у обоих разная цена. Мой оставляет **окно** между диспетчеризацией и
setup'ом, в котором карта не заклеймлена. У DIR-2 окна нет, но снятие клейма — **ручной шаг,
который сессия обязана не забыть**, а `steal`/expiry отложены по дизайну (CLAUDE.md, Claims
module) и реапера нет: смерть сессии (3) между диспетчеризацией и setup'ом оставляет карту
за мёртвым агентом, снимается только руками.

**Общая причина одна: владение — тройка, а глагола «передать владение» не существует.**
Оба направления нуждались в «заклеймить за того, кто придёт следом, и дать ветке
унаследовать», и оба изобрели обходной путь — разный. Это ровно тот дефект, который Н3
называет: координация решается конвенцией, и две параллельные сессии пришли к разным
конвенциям в один день. Прошу вынести владельцу как кандидата в примитив леджера
(`transfer`/`claim --on-behalf-of`), либо ратифицировать один из двух обходных путей как
канон каскада, чтобы направления перестали расходиться.

**Э-5. `CASCADE-IDS.md` до этой сессии не был в git, а весь его механизм — git'овый.**
Файл объявляет себя аллокатором ID, где «конкурентный минт даёт merge-конфликт — это
желаемый отказ». Нетрекаемый файл конфликтов не даёт: два направления, минтящие BT
одновременно, просто перезаписали бы друг друга без единого сигнала. Закоммичен мной в
`20c896b`. Наблюдение более общее: артефакты каскада — точки восстановления по §7, и часть
из них (оба L2-брифа, правка `PAIN-REGISTRY`) на момент письма всё ещё не в git; правку
реестра я не трогаю, она кураторская и, возможно, в работе.

**Э-3. Семь из десяти найденных нарушений транзакционного правила не имеют карт, и три из
десяти — класс «порча данных», а не TOCTOU.** Карты есть только у `bench.delete_run` /
`delete_benchmark` (CB-87). Без карт: `sweep.create_sweep`, `sweep.archive_sweep`,
`merge.abandon_session`, `blockers.add_blocker`, `embeddings.store_embedding`,
`milestones.create_milestone`, `update_milestone`, `add_milestone_item`.
Прошу решения: заводить ли карты сейчас (я — уровень (2), карт не завожу без спроса) или
они существуют только как популяция внутри BT-1. Цена «не заводить»: популяция снова живёт
в прозе — ровно тот дефект, из-за которого CB-24 назвала 4 сайта вместо 19.

**Э-2. §13 п.6 приёмки («лог post-merge-gate разобран, `.FAILED`-маркера нет») в этом репо
n/a.** Уже поднято куратором при приёмке Т-1; повторяю как живое, потому что оно относится
и ко всем моим будущим приёмкам: post-merge-gate здесь нет, есть CI-аларм
(`main-invariants.yml`), который по собственному признанию CLAUDE.md **не гейт**.

## §11. Журнал направления

- **2026-08-19.** Роль принята. Бриф прочитан целиком, спека §1/§2/§7/§8.3б/§13 прочитана.
  Предпосылки брифа передобыты (§3). Заведён этот документ. Расхождение Р-1 разрешено
  по смыслу и записано. Т-2 передан в (3). Кандидаты BT-1..BT-3 нарезаны, не ратифицированы.
- **2026-08-19, Т-2.** Preflight выполнен на уровне (2) (дешёвый субагент + личный спот-чек
  цитат) и **изменил форму юнита**. Решающий факт: `docs/superpowers/plans/2026-03-25-codemerge.md:1263-1275`
  предписывает `finish(success=False)` → **revert-to-active**, и код (`merge.py:187`) ему
  следует; дрейфнул **докстринг** (`merge.py:657-662`, зафиксирован и в wire golden
  `tests/golden/mcp_schema.json:801`). CB-106 предлагала обратное — привести код к
  докстрингу. Это НЕ эскалировано, и вот почему: куратор в тройке Т-2 дословно делегировал
  выбор «preflight'у (3) по коду и **дизайн-докам** codemerge», т.е. предвидел ровно этот
  разрешающий источник; расхождение здесь — в букве задания-гипотезы, замысел («описание
  тула не лжёт») удовлетворяют оба варианта. Хендофф несёт факт с provenance и оставляет
  решение (3), запретив молчаливое переписывание семантики.
  Побочные поправки к карте, переданные вниз: функция называется `check_overlaps`
  (`merge.py:408`), а не `check_conflicts`; реапера для застрявших `active`-сессий не
  существует и это отложено дизайном намеренно (`docs/2026-03-25-codemerge-design.md:433`),
  поэтому liveness остаётся предметом BT-2, а не Т-2.
- **2026-08-19, BT-1.** Популяция передобыта с нуля (не унаследована ни из CLAUDE.md, ни из
  трекера) — §7. Результат изменил формулировку нерешённого решения: живая популяция целиком
  состоит из класса, к которому наивный AST-предикат слеп, так что «написать ратчет по
  `conn.commit()`» — не решение, а зелёный на всём. Кросс-модельный проход по разрезу
  (`adversarial-review-x2`, §8.3б) **ещё не выполнен** — BT-1 не может идти на ратификацию
  владельцу до него.
- **2026-08-19, карты.** Владелец на вопрос о заведении карт ответил «делай, как считаешь
  правильным, только потом защити эту позицию перед куратором». Заведены **две** —
  **CB-108** (`update_milestone`: устаревшее `from_state` в audit-INSERT + перечитывание
  после коммита) и **CB-109** (`add_blocker`: единственный из 13 сайтов, чья дубль-проверка
  не подпёрта ни PK, ни UNIQUE — проверено лично по `BLOCKERS_SCHEMA`, там три обычных
  индекса и `id INTEGER PRIMARY KEY AUTOINCREMENT`). Измерение про осиротевшие результаты
  дописано в **CB-87**, новой карты для него не заводил — это её собственный скоуп.
  Остальные пять сайтов оставлены без карт намеренно и перечислены в CB-87, чтобы выбор был
  виден, а не выглядел упущением.
  **Защита позиции перед куратором:** порог — не severity, а класс отказа. Три сайта пишут
  или сохраняют неверные данные и отчитываются успехом (ложная строка аудита; осиротевшие
  результаты; раздутый счётчик блокеров, из-за которого actionable-карта вечно читается как
  deferred) — это буквально Н1, success-shaped lie, и по §1а «живой вред vs латентный» они
  живые. Остальные семь — TOCTOU-на-существование, где проигравший либо получает
  `IntegrityError` и дубль не приземляется, либо ничего не портит; они латентные, и по тому
  же §1а уступают. Заведение всех восьми раздуло бы бэклог на 20 % и потребовало бы
  перечитывать ратифицированный сегодня `PAIN-ORDER` ради латентного долга; заведение нуля
  оставило бы три случая живой порчи данных жить только в прозе — тот самый механизм, из-за
  которого CB-24 назвала 4 сайта вместо 19. Выбранная середина закрывает живой вред картами,
  а латентный хвост держит популяцией внутри BT-1, где у него и есть смысл.
- **2026-08-19, приёмка Т-2.** Юнит принят на мерже `f3cc0cd` (§9 — неизменяемый факт).
  Дифф прочитан лично против замысла; тесты (`1664 passed`), линт и присутствие
  `codemerge_abandon` в golden перепроверены мной на main, а не приняты по отчёту.
  Решения по эскалациям (3): **Э-1 (шов с BT-1) — вердикт «да, остро достижимо», и я
  переклассифицировал собственную оценку**: `merge.abandon_session` был в моей же выметенной
  популяции помечен «низкий вред, без карты», что было верно до `f3cc0cd` и стало неверно
  после — заведена **CB-111**, список «без карт» в CB-87 исправлен. Порог применён тот же,
  что к CB-108/CB-109; применить его только к тем сайтам, которые я перечислил первыми, было
  бы ровно той ошибкой перечисления, против которой этот документ и написан.
  **Э-2 (остаток `active → done`) — принято как отдельная карта CB-110**: это вопрос о том,
  что ЗНАЧИТ `done`, с двумя несовместимыми ответами и миграцией схемы в каждом, а не дыра
  поверхности. **Э-3 — принято**, все четыре пункта разнесены: пропущенный кросс-модельный
  гейт → Э-6 (закрыт ретро-проходом), блокировка интеграции незакоммиченным реестром → Э-7,
  фантомный символ `merge_stats` в тексте CB-106 → исправлен в CB-110, находка про
  `2026-03-25-codemerge-design.md:157` → учтена в §7 BT-2.
- **2026-08-20.** Решения куратора применены (Р-2 в §6, Э-1 → CB-112, скоуп BT-2 расширен
  примитивом передачи). Разрез **BT-1** оформлен самодостаточным документом
  `BT-1-CUT-transaction-rule-enforcement.md` и отправлен на `adversarial-review-x2` — гейт
  §8.3б, без него BT не идёт владельцу. **Собственный вариант (e) измерен ДО отправки и
  наполовину опровергнут:** переопределение `commit()` в подклассе соединения обходится
  тремя путями (`execute("COMMIT")`, присваивание `isolation_level`, `with conn:`) —
  измерено пробой, не рассуждением. Своё предложение обязан судить тем же порогом, что и
  чужое, поэтому (e) переформулирован в (e′) с ратчетом на три обхода; проверено, что живые
  вхождения всех трёх есть только внутри самого `db.txn`.
  **Т-6 (CB-111)** диспетчеризован механической полосой — не ждёт BT-1, потому что «чем
  энфорсить правило вообще» и «починить один сайт с живым вредом» независимы.
  WIP: открытых BT — ноль (BT-1 на ревью, не ратифицирована); диспетчеризованных юнитов — один.

---

## §12. BT-1: вердикт кросс-модельного ревью (2026-08-20) и разбор моего провала

**Вердикт `adversarial-review-x2`: НЕ ГОТОВ. Здоровье дизайна 3/10. Ратифицировать нечего.**
Гейт §8.3б отработал обеими половинами; Codex ответил за ~4 минуты — самодостаточный промпт с
текстом документа внутри и запретом обхода репозитория сработал, как и предсказывалось.

### Что оказалось ложным

**§5 не ослаблен, а ПЕРЕВЁРНУТ.** Я утверждал, что 100 % живой популяции невидимо наивному
AST-предикату, и на этом строил весь выбор механизма. **Семь из сайтов ВИДНЫ** — у них
настоящий предкоммитный `conn.execute("SELECT …")` прямо в теле функции. Проверил лично два:
`sweep.create_sweep` (`sweep.py:204-206` → commit `:232`) и `embeddings.store_embedding`
(`embeddings.py:53` → `:62`). Среди видимых — обе карты CB-87 и две из трёх, которые разрез
клал внутрь.

**§4 неверен во всех четырёх числах.** `grep -rn "conn.commit()" src/ | wc -l` даёт **50**, не
47 — перепроверил лично. Ревью намерило 50 / 29 / 14 / **15 живых сайтов в 14 функциях**.
Пропущены целые функции, и одна из них — `reqs.add_requirement` (`reqs.py:132` commit → `:133`
перечитывание, отдаваемое MCP-клиенту) — **буквально класс вреда CB-111, который разрез
объявлял высшим приоритетом**. Проверил лично; завёл CB-117.

**Метод слеп там, где я объявил его пустым.** `reqs._migrate_to_lowercase` коммитит неявно
через `conn.executescript` (`reqs.py:75`, проверил лично) — вообще без `conn.commit()`. Моя же
секция «чего я не знаю» подняла эту возможность и объявила дыру «эмпирически пустой» за одно
предложение до собственной оговорки про `executescript`.

**(e′) не спасается ратчетом.** Ревью нашло ещё шесть обходов перехвата `commit()`, включая
`conn.autocommit = True` — четвёртое написание, которого в моём ратчете из трёх нет, — и
показало, что все три «несокрытых» обхода прекрасно прячутся (`conn.execute(MODULE_CONSTANT)`,
`with get_conn():`, `setattr`). Обоснование «перечисление тут работает» снято.

### Два моих провала, и они разной природы

**П-1. Я не передобыл УНАСЛЕДОВАННУЮ посылку.** Тезис о слепоте предиката пришёл из CB-37; я
передобыл популяцию, но не утверждение, которое ВЫБИРАЛО механизм. При этом собственное свежее
предложение (e) я измерил и наполовину убил до отправки. Т.е. порог у меня применялся к тому,
что я придумал, и не применялся к тому, что мне выдали. **Унаследованная посылка — ровно то
место, где перепроверка нужнее всего**, и это формулировка судьи, а не моя.

**П-2. Опровергающий факт был у меня в руках, и я вывел из него обратное.** Проверяя §8.3, я
сам измерил, что `db.txn` коммитит через `conn.execute("COMMIT")` (`db.py:567`), и записал это
как довод «альтернативные написания живут только внутри `db.txn`». Из того же факта следует,
что `db.txn` **никогда не зовёт `conn.commit()`** — значит перехват `commit()` не трогает
санкционированный путь, разрешённое множество пусто, и после (e′) единственным способом
закоммитить становится ратчетируемый обход. Элемент верен, композиция перевёрнута: это
буквально «проверка элементов не проверяет их композицию», и я совершил её внутри документа,
который цитирует этот урок.

**Ещё одна корневая находка, которой не назвал ни один атакующий — её дал судья:** у документа
**нет критерия приёмки решения**. §2 называет операционное свойство (write-lock ДО чтения),
§6 ранжирует варианты по покрытию популяции, и в этом зазоре §5 получил вес, которого ему не
положено. С записанным критерием (e′) и (b) отпадают в две строки независимо от того, сколько
сайтов видит предикат. Отдельно замечено, что половину «write-lock ДО чтения» я дописал к
цитате из CLAUDE.md сам — т.е. свойство я понимал и всё равно не сделал его критерием.

### Что из этого следует для нарезки

Судья рекомендует резать **по границе «исправление / решение»**, потому что склейка держалась
на ложной зависимости: все исправления популяции механизмонезависимы.

- **Полоса A — механическая, ратификации НЕ требует, запускать сразу:** 15 сайтов в 14
  функциях, пакетами по ≤4 независимых правок на дерево. CB-111 уже приземлён именно так, и
  это подтверждает, что зависимости от механизма нет.
- **BT-1 настоящая — ТОЛЬКО CB-37**, с записанным критерием приёмки решения во главе,
  пересобранной популяцией и добавленным семейством **(f) — гейт на ЧТЕНИЕ**, которое обе
  модели назвали независимо и которое единственное совпадает по форме с формой правила.
- **Микро-BT C — «что такое один и тот же блокер»** (это вторая половина CB-109; карта несёт
  собственное нерешённое решение и потому не годится в симптомы).

### Исправления, уже сделанные

Неверные числа я разнёс по четырём картам — исправлено во всех: **CB-87** (ретракция целиком),
**CB-108**, **CB-109** (ревью прямо указало, что числа туда протекли), **CB-111**. Завёл
**CB-117** на `reqs.add_requirement`. Корректные числа записаны как ИЗМЕРЕНИЕ РЕВЬЮ, а не моё:
лично я перепроверил только 50 и два сайта, и подменять неверные числа непроверенными значило
бы повторить дефект, а не починить его.

**Пересборку документа делаю я, не ревьюер** — он отказался править §5 и §6 сознательно,
чтобы не стать соавтором того, что судил. Правильное решение.
- **2026-08-20, зависание Т-7 — и это дефект МОЕГО брифа, а не только сессии.** Сессия (3)
  доделала работу (коммит `667c75f`, 17:31), затем встала на `codex exec` и провисела **три
  часа** без единой записи на диск; её последние слова — «Запускаю независимое кросс-модельное
  ревью диффа — Codex». Остановлена мной по сигналу владельца, работа цела, юнит передан
  свежей сессии на доведение до посадки.
  **Причина в брифе:** я предписывал `< /dev/null` и запрет обхода репозитория, но **не
  предписал `timeout`** — при том что мои собственные удачные вызовы в этот же день шли с
  `timeout 540` и `timeout 420`, и именно они и отработали. Т.е. я знал рабочую форму и передал
  вниз неполную. Это ровно та же ошибка, что и усечённая цитата CB-37(b): **передано половину
  определения, работавшего целиком.**
  **Правило в шаблон брифа, действует немедленно:** вызов `codex exec` вниз передаётся ТОЛЬКО в
  форме `timeout 420 codex exec … < /dev/null`, с запретом обхода репозитория, с диффом
  ТЕКСТОМ в промпте и с лимитом объёма ответа; и с явным разрешением **отказаться от
  кросс-модельной половины после двух таймаутов, назвав это вслух в возврате**. Молчаливый
  пропуск гейта не принимается, названный — принимается.
  **Кандидат в канон каскада (касается и DIR-2, и куратора):** ни один вызов внешней модели не
  уходит вниз без таймаута. Стоимость известна и измерена: три часа на одном юните.

## §13. Шаблон брифа — накопленные поправки (все из наблюдённых отказов, не из соображений)

Каждая строка стоила времени, и стоила его один раз. Правила действуют для всех моих будущих
брифов; кандидаты в канон каскада помечены.

1. **Вызов внешней модели уходит вниз ТОЛЬКО с таймаутом.** `timeout 420 codex exec … <
   /dev/null`. Цена пропуска измерена: три часа зависшей сессии на Т-7. **Кандидат в канон.**
2. **Плюс `--sandbox read-only` и явный запрет исследовать репозиторий.** Из возврата Т-7:
   с обоими Codex отвечает за минуты и не пытается ходить по дереву. Дифф подаётся ТЕКСТОМ в
   промпт, не путём, и задаётся лимит объёма ответа.
3. **Не полагаться на имя модели, вычитанное из каскадного документа.** Т-7: имя
   `gpt-5.6-codex-sol` отвергается на ChatGPT-аккаунте («not supported»), и без фолбэка на
   модель по умолчанию кросс-модельной половины бы не было вовсе. Бриф обязан разрешать
   фолбэк явно. **Кандидат в канон** — имя модели это конфигурация окружения, а каскадный
   документ её не знает.
4. **Явное разрешение отказаться от кросс-модельной половины после двух таймаутов, назвав это
   вслух в возврате.** Молчаливый пропуск гейта не принимается, названный — принимается.
   Без этого разрешения исполнитель зажат между «нельзя пропустить» и «оно не идёт» и висит.
5. **«Закоммить перед мутационной пробой».** Т-7: `git checkout -- <file>` в пробе снёс
   несохранённые правки исполнителя. Дешёвая строка, предотвращает потерю работы.
6. **«Ставь мутант В ТОЧКУ дефекта».** Т-6: первый мутант оказался эквивалентным и был зелёным
   по построению. Проба не в той точке неотличима от пройденной.

## §14. Наблюдение уровня (1): канон немедленных коммитов имеет три ИЗМЕРЕННЫЕ цены

Не возражение против канона — он решает реальную проблему (WIP уровня (1) на main блокировал
интеграцию, Э-7). Но за один день он трижды проявился как источник, и это уже не совпадение:

1. **CB-116** — заголовок мержа выводился из хвоста, загрязнённого форвард-мерджем main; чем
   чаще движется main, тем чаще срабатывает. Приземлено, но частота была прямым следствием.
2. **Аллокатор `CASCADE-IDS.md`** — обещанный шапкой merge-конфликт при конкурентном минте
   **не может произойти**, потому что оба направления коммитят прямо на main последовательно,
   а прямые последовательные коммиты не сливаются. Я на этом и получил коллизию Т-8.
3. **CB-121** (заведена исполнителем Т-7) — «протестированное состояние есть приземляемое»
   оказалось окном, а не инвариантом: флок сериализует финиши, но main-половину окна
   открывает обычная санкционированная работа, то есть непрерывный поток плановых коммитов.
   Таблица энфорсмента в CLAUDE.md утверждает это без оговорок — документ переобещает.

**Общая форма:** канон увеличил частоту движения main, а три механизма были спроектированы в
предположении, что main движется редко. Ни один из трёх не сломан каноном — все три были
дефектны и до него; канон превратил редкое в частое. Выношу куратору не как «откатить», а как
вход в решение: если main будет двигаться ещё чаще (а с ростом числа направлений будет), эти
три — первые, кто это почувствует, и CB-121 из них единственный, где цена — приземление
непротестированного дерева.
