# L3-BRIEF DIR-1 / Т-68 — CB-168 + CB-169: границы механизма CLI-снимка

**Класс по Р1: B** — оракул детерминирован (гейт на псевдониме либо отказывает, либо нет; детейл-луп
либо называет разошедшийся ключ, либо печатает пустое тело), но путь неизвестен: сопоставление
надо перестроить, а не дописать строку. **Стартовый тир: sonnet, effort medium, бюджет попыток 6.**
Р2 (три правки подряд без смены состояния гейта; повтор вызова с теми же аргументами; половина
бюджета без проверяемого промежуточного факта) — стоп и возврат «не смог + что понял».
Автор брифа: держатель DIR-1 (уровень (2)), 2026-08-25.
Карты: **CB-168** и **CB-169**. Нормативы: `CLAUDE.md` — разделы **CLI**, **Testing**;
`tests/cli_surface.py` (генератор снимка, юниты Т-51 и Т-64); `tests/test_cli_surface.py` (гейт).

---

## §1. Почему они вместе

Обе карты — остатки принятого юнита Т-64 (мерж `1fc7ff2`), обе найдены его изолированным
приёмщиком сверх заказанного, обе живут в паре файлов `tests/cli_surface.py` /
`tests/test_cli_surface.py`, и предмет у них один: **механизм снимка честно ведёт себя на своих
границах**. Шов общий не только тематически, но и почти механически — это одна пара файлов и один
вопрос «что делает снимок, когда действительность не такая, как предполагали». Если по ходу
окажется, что они мешают друг другу, скажи, и разделим.

## §2. CB-168 — fail-closed гейт ложно отказывает на псевдонимах верба

**Механизм.** Т-64 научил снимок читать справку САМОГО верба: она живёт на псевдо-действии
родительского парсера (`sub._choices_actions`), а не среди действий подпарсера. Цена опоры на
приватный атрибут закрыта fail-closed: `_verb_actions_by_dest(sub)` строит словарь по `.dest` и
поднимает `MissingChoicesActionsError`, если атрибут пуст/отсутствует или верб не разрешается.

Предпосылка «`dest` псевдо-действия равен имени верба» верна для всех 67 сегодняшних вербов и
измерена дважды. Но она ломается не будущим Python'ом, ради которого страховка писалась, а штатной
возможностью сегодняшнего argparse: `sub.add_parser("real", aliases=["alias1"])` кладёт в
`sub.choices` **оба** имени и ровно **одно** псевдо-действие.

**Измерено приёмщиком Т-64 на живом парсере:** добавлен верб с псевдонимом — отказ с текстом «no
pseudo-action … matches verb(s) ['zz-alias'] … the dest-equals-verb-name assumption … no longer
holds». Сообщение объявляет сломанной предпосылку о версии argparse, тогда как сработала обычная
возможность той же версии.

**Почему это дороже, чем красный гейт.** `collect_cli_surface` — это **ГЕНЕРАТОР** голдена, а не
только гейт. Значит первый же, кто законно добавит псевдоним верба, получит не красный гейт,
который чинится регенерацией, а **невозможность регенерировать**: документированная команда падает
исключением. Плюс сообщение уводит от причины.

**ФОРМА ВЫБРАНА УРОВНЕМ (2): вариант (a) карты — ключевание на ПРИМИТИВ.** Сопоставлять
псевдо-действия с вербами не по равенству имён, а по тому отношению, которым их связывает сам
argparse: **тождеству объекта парсера**. Ожидаемый механизм (это ГИПОТЕЗА, см. §4 — проверь её
первым делом): `sub.choices` — это `_name_parser_map`, и для верба с псевдонимом
`sub.choices["real"] is sub.choices["alias1"]`, то есть имена естественно группируются по объекту
парсера, а `dest` псевдо-действия — это первичное имя группы.

Вариант (b) (разбирать `metavar` вида `'real (alias1)'`) **отвергнут**: он парсит человекочитаемую
строку, то есть заводит вторую основу сравнения — ровно то, за что сама CB-152 отвергла свой
вариант (b). Вариант (c) (оставить отказ, но с честным текстом) **отвергнут как основной**, но
остаётся названным запасным: см. §7 эскалация (б).

**ПОДРЕШЕНИЕ, КОТОРОЕ ОСТАЁТСЯ ЗА ТОБОЙ И КОТОРОЕ НАДО НАЗВАТЬ ВСЛУХ:** что попадает в снимок для
верба-псевдонима. Разумных ответов минимум два — псевдоним несёт ту же запись `verb_action`, что и
первичный верб; либо псевдоним отмечается как псевдоним (`"alias_of": "real"`) и справку не
дублирует. Выбери, **объясни выбор одной фразой в записи на ветке**, и покажи поведение тестом на
синтетическом парсере. Плохого ответа тут нет, есть неназванный.

## §3. CB-169 — детейл-луп печатает пустое тело на новом ключе

`tests/test_cli_surface.py` несёт написанный вручную детейл-луп, существующий ровно потому, что
голое `assert current == expected` над 67 вербами читателю ничего не даёт — это сказано его
собственным комментарием. Луп сравнивает только ключ `actions`. Т-64 добавил ключ `verb_action`, и
луп его не знает.

**Измерено приёмщиком Т-64:** при дрейфе `verb_action` печатается

```
CLI surface drifted from golden.
added verbs: []
removed verbs: []
drifted:
```

и обрывается. Под КРАСНЫМ гейтом человек читает вывод, утверждающий «вербов не добавилось, не
убавилось, разошедшихся нет». Сырое сравнение словарей от pytest остаётся, поэтому дефект не
пропускается — ломается только диагностика, и потому severity низкая.

**ФОРМА ВЫБРАНА УРОВНЕМ (2): вариант (a) карты — сравнивать ВСЕ ключи записи верба**, обходя
объединение ключей обеих сторон, а не поимённый `actions`. Тогда следующий добавленный ключ
покрывается без правки этого файла. Вариант (b) (дописать `verb_action` рядом с `actions`)
**отвергнут прямо**: это добавление формы к перечислению, то есть та самая починка, которую
репозиторий уже признал неверной — правило чинится ключеванием на примитив, а не четвёртой формой.
Вариант (c) (fail-closed на непокрытые ключи) вариантом (a) **поглощается**: при обходе объединения
непокрытых ключей не остаётся. Если по ходу окажется, что (a) даёт нечитаемый вывод, — это §7
эскалация (в), а не молчаливая подмена на (c).

## §4. ПРЕДПОСЫЛКИ ДЕРЖАТЕЛЯ — ПРОВЕРЬ ИХ ПЕРВЫМ ДЕЙСТВИЕМ, А НЕ ПРИМИ

Я НЕ прогоняла эти механизмы сама: описание CB-168 целиком принадлежит приёмщику Т-64, а мой §2
опирается на чтение исходников argparse по памяти. Оба утверждения ниже — **гипотезы**:

1. **`sub.choices` даёт тождество объекта:** `sub.choices["real"] is sub.choices["alias1"]` истинно,
   и `sub.choices` — это `_name_parser_map`, куда `add_parser` кладёт и первичное имя, и каждый
   псевдоним.
2. **Псевдо-действие НЕ несёт ссылки на парсер** — у него есть только `dest` (первичное имя),
   `metavar` (`'real (alias1)'`) и `help`. Поэтому «пойти от псевдо-действия к парсеру» напрямую
   нельзя, и группировать надо со стороны `sub.choices`.

**Проверь обе на живом парсере, до единой правки.** Если хотя бы одна не воспроизвелась — форма (a)
может оказаться неисполнимой в том виде, в каком я её описала, и это **эскалация (б) §7**, а не
повод молча взять (b) или (c). Канон К-12: бриф — данные, не команда; предпосылка не
воспроизвелась — эскалируй. Правило оплачено ошибкой держателя на Т-60.

Столь же обязательно **воспроизвести сам дефект** обеих карт своими руками ДО правки: отказ на
псевдониме (CB-168) и три строки пустого тела (CB-169). Держатель их не воспроизводил, и в картах
это сказано прямо.

## §5. Инварианты и границы

- **ГОЛДЕН `tests/golden/cli_surface.json` ДВИГАТЬСЯ НЕ ДОЛЖЕН, и это проверяемый инвариант, а не
  надежда.** В `src/` сегодня нет ни одного `aliases=` (проверено грепом приёмщиком Т-64 — перепроверь).
  Значит перестройка сопоставления обязана дать **байт-идентичный** снимок. Покажи это: пустой дифф
  голдена, плюс регенерация документированной командой даёт байт-в-байт закоммиченный файл. **Если
  голден сдвинулся — что-то тронуто сверх замысла**, это признак, а не мелочь.
- **Существующие ассерции не ослабляются**; порядок вербов и аргументов сохраняется; fail-closed на
  настоящее исчезновение `_choices_actions` (пустой список, отсутствующий атрибут, `None`) обязан
  остаться ГРОМКИМ — приёмщик Т-64 проверил все три формы, ни одна не должна стать тихой.
- **Бюджет ≤4 файла:** `tests/cli_surface.py`, `tests/test_cli_surface.py`, и до двух файлов тестов.
  Пятый — названный.
- **Тест на псевдоним строится на СИНТЕТИЧЕСКОМ парсере**, не добавлением псевдонима в боевой CLI:
  добавление настоящего псевдонима — это изменение поверхности, чего юнит не заказывал, и оно
  сдвинуло бы голден.
- **Чужие файлы, названные поимённо, чтобы конфликт был виден до, а не после:** у соседнего
  направления в полёте юниты, трогающие `src/codebugs/findings.py`, `src/codebugs/reqs.py`,
  `src/codebugs/server.py`, `src/codebugs/surfacegen.py`, `tests/dump_schema.py` и
  `tests/golden/mcp_schema.json`. **Ни одного из них не касайся.** У моего направления параллельно
  идёт Т-65 по `tools/pre-commit-hook.sh` и шапке `.claude/plans/CASCADE-IDS.md` — тоже не твоё.
  Пересечений с твоим бюджетом нет ни одного, и так должно остаться.

## §6. Мутанты — оракул

В КОПИИ вне репозитория или во временном worktree (канон К-5), никогда в рабочем дереве.
`PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, и **подтверди `grep -c`, что мутант применён** —
без этого «мутант выжил» и «мутант не применён» неразличимы.

**Убедись, КАКОЙ КОД исполняется:** `inspect.getsourcefile()` импортированного модуля, а для тестов,
шеллящих CLI подпроцессом, — признак снимается **из подпроцесса**, тем же способом запуска, каким
исполняется тест. Правило оплачено ошибкой держателя на Т-60: `pythonpath` pytest делает признак
правдивым, пока подпроцесс уходит в чужое дерево через устаревший editable-install.

Обязаны КРАСНЕТЬ после юнита (и оба сегодня ЗЕЛЁНЫЕ — покажи оба состояния):
1. **CB-168:** синтетический парсер с `aliases=[...]` — на сегодняшнем `tests/cli_surface.py` даёт
   `MissingChoicesActionsError`; после правки обязан давать снимок, а новый тест — краснеть, если
   правку откатить.
2. **CB-169:** дрейф ключа `verb_action` — на сегодняшнем `tests/test_cli_surface.py` даёт
   заголовок `drifted:` с пустым телом; после правки диагностика обязана НАЗВАТЬ ключ, а новый тест
   — краснеть при откате.
Обязано ПРОХОДИТЬ: нетронутое дерево целиком; полный сьют; голден без диффа.

## §7. ЭСКАЛАЦИИ — ПОМЕЧЕННЫЙ ПУНКТ, ОБЯЗАН СТОЯТЬ В ЭТОМ ФАЙЛЕ НА ВЕТКЕ

Отдельным заголовком `## Эскалации`. **Сообщение его не заменяет** — приёмщик судит артефакт.
Пустой слот законен, если написан словами; разбери все поводы поимённо.

**(а)** дефект какой-либо из карт не воспроизвёлся — карта неверна, и это данные, а не препятствие;
**(б)** предпосылки §4 не воспроизвелись, и форма (a) для CB-168 неисполнима в описанном виде;
**(в)** сравнение всех ключей (CB-169) даёт нечитаемый вывод, то есть лечение хуже болезни;
**(г)** голден сдвинулся, и ты считаешь сдвиг законным — **это стоп**, а не решение исполнителя:
инвариант §5 держит весь юнит;
**(д)** подрешение §2 (что попадает в снимок для псевдонима) оказалось не косметическим, а меняющим
контракт поверхности.

## Эскалации

Ни один из пяти поводов §7 не сработал — разобраны поимённо ниже, не молчанием.

**(а) дефект какой-либо из карт не воспроизвёлся.** Не сработал: оба дефекта воспроизведены моей
рукой ДО единой правки, каждый на настоящем коде из `tests/cli_surface.py` /
`tests/test_cli_surface.py` (не на пересказе). Для CB-168 — синтетический парсер с
`sub.add_parser("real", aliases=["alias1"])`, вызов `_verb_actions_by_dest` (дореформенное имя)
дал текст отказа «no pseudo-action in sub._choices_actions matches verb(s) ['alias1'] by `.dest`
— the dest-equals-verb-name assumption … no longer holds» — слово в слово то, что называет карта.
Для CB-169 — синтетическая пара `current`/`expected`, различающаяся только ключом `verb_action`,
прогнанная через дореформенный детейл-луп ЦЕЛИКОМ (скопирован из файла построчно, не пересказан):
результат — `drifted:\n` без единой строки под заголовком, ровно то, что называет карта.

**(б) предпосылки §4 не воспроизвелись.** Не сработал: обе гипотезы держателя подтверждены на
живом парсере под закреплённым интерпретатором (3.14.4). Гипотеза 1 —
`sub._name_parser_map is sub.choices` истинно, `sub.choices["real"] is sub.choices["alias1"]`
истинно, `sub.choices["real"] is <объект парсера real>` истинно. Гипотеза 2 — у псевдо-действия
`real` нет ссылки на парсер (проверены кандидаты `_parser`/`parser`/`_choices`, есть только
`choices` — унаследованный атрибут `Action`, не указывающий на подпарсер), есть только `dest='real'`
(первичное имя), `metavar='real (alias1)'`, `help`. Ровно на этой связи и построена правка: не по
имени, а по тождеству объекта `sub.choices[verb]`.

**(в) сравнение всех ключей (CB-169) даёт нечитаемый вывод.** Не сработал: вывод читаем и называет
разошедшийся ключ по имени со старым/новым значением (`foo.verb_action: {'help': 'old help'} ->
{'help': 'NEW help'}`), а для ключа `actions` сохранена прежняя, более мелкая построчная разбивка
по действию и атрибуту — вариант (a) поглотил (c), как и предполагала карта, без деградации
читаемости.

**(г) голден сдвинулся.** Не сработал: `PYTHONPATH=src uv run python tests/cli_surface.py` после
правки даёт файл, побайтово идентичный закоммиченному `tests/golden/cli_surface.json` (`diff`
пуст). Ожидаемо — сегодня в `src/` нет ни одного `aliases=` (перепроверено грепом заново на
ветке), и для верба без псевдонима перестроенная функция резолвит ТОТ ЖЕ объект действия, что и
прежняя (`dest == имя верба` продолжает выполняться, просто перестаёт быть единственным путём).

**(д) подрешение §2 оказалось меняющим контракт поверхности.** Не сработал в смысле «нежданная
находка», но подрешение принято и стоит здесь явно, как требует брифа: **вербу-псевдониму снимок
кладёт байт-в-байт ту же запись `verb_action` (и, как следствие устройства argparse, ту же запись
`actions`), что и первичному вербу** — не потому что это специально закодировано, а потому что
`sub.choices["real"] is sub.choices["alias1"]` буквально один и тот же объект подпарсера, и
раздельная пометка `"alias_of": "real"` кодировала бы информацию, которой сам argparse не
располагает. Это ничего не меняет в СЕГОДНЯШНЕМ контракте (нет вербов с alias), а для БУДУЩЕГО
верба с псевдонимом означает: в снимке появится дополнительный ключ верхнего уровня (имя
псевдонима), содержимое которого зеркалит первичный верб. Это расширение словаря `verbs`, а не
новый вид записи внутри него, поэтому не читаю это как смену контракта.

## Запись о работе (Т-68, исполнитель уровня (3))

**CB-168.** Функция переименована `_verb_actions_by_dest` → `_verb_actions_by_identity`
(`tests/cli_surface.py`), сопоставление вербов и алиасов с псевдо-действием построено на тождестве
объекта парсера: каждое псевдо-действие резолвится через свой `.dest` (первичное имя) к объекту
подпарсера РОВНО ОДИН РАЗ, затем каждое имя в `sub.choices` (и первичное, и псевдоним) находит своё
псевдо-действие по совпадению этого объекта. Три формы fail-closed сохранены: пустой/отсутствующий
`_choices_actions` при непустых `sub.choices`; `dest` псевдо-действия, не называющий существующий
парсер; имя в `sub.choices`, чей объект парсера не нашёлся ни у одного псевдо-действия.

**CB-169.** Диагностика дрейфа в `tests/test_cli_surface.py` вынесена из тела теста в отдельную
функцию `_drift_message(current, expected)` — чтобы синтетический тест гонял РОВНО ТОТ ЖЕ код, что
и боевой гейт, а не копию, рискующую разойтись (собственное правило репозитория). Луп теперь
обходит объединение ключей записи верба (`set(cur_verb) | set(exp_verb)`), а не литеральный
`["actions"]`; для ключа `actions` сохранена прежняя точная разбивка по индексу действия и
атрибуту, для любого другого ключа — строка «было → стало».

**Мутанты-оракулы.** Оба воспроизведены на дореформенном коде (см. «Эскалации» (а) выше). Новые
тесты `TestVerbActionsByIdentityAliasBoundary` и `TestDriftMessageNamesEveryKey`
(`tests/test_cli_surface.py`) пином держат правку; для обоих отдельно проверено «мутант красит»:
дореформенная логика `_verb_actions_by_dest`, прогнанная напрямую на синтетическом парсере из
копии `main`-версии файла вне рабочего дерева (`/tmp/.../scratchpad/prefix_check/`), даёт тот же
отказ, что цитирует карта CB-168; дореформенный детейл-луп, прогнанный на той же синтетической паре
`current`/`expected`, даёт тот же пустой `drifted:` из карты CB-169. Код исполнения проверен:
`inspect.getsourcefile` подтвердил, что тесты в worktree читают файлы из
`.worktrees/fix-t68-cb168-snapshot-boundaries/tests/`, не из main; `grep -c` подтвердил присутствие
новых имён (`_verb_actions_by_identity`, `_drift_message`) в файлах на диске.

**Голден.** `PYTHONPATH=src uv run python tests/cli_surface.py` после правки даёт файл, побайтово
идентичный `tests/golden/cli_surface.json` (`diff` пуст) — инвариант §5 выдержан.

**Полный сьют и линт (в worktree, закреплённый интерпретатор 3.14.4).**
`uv run --extra dev python -m pytest tests/ -q` → `2442 passed in 166.86s`.
`uv run --extra dev ruff check src/ tests/` → `All checks passed!` (ruff 0.15.7 — версия
проверена командой `uv run --extra dev ruff --version` перед прогоном).

**Бюджет файлов.** Тронуто 2 из ≤4: `tests/cli_surface.py`, `tests/test_cli_surface.py`.
Проверено `git diff --stat $(git merge-base HEAD main) HEAD -- .` (не голое `git diff --stat main`
— main тем временем ушёл вперёд на параллельных сессиях, и наивное сравнение против движущегося
`main` ложно показывало бы удаление чужих файлов направления, которых эта ветка не касалась). Ни
`src/codebugs/findings.py`, `src/codebugs/reqs.py`, `src/codebugs/server.py`,
`src/codebugs/surfacegen.py`, `tests/dump_schema.py`, `tests/golden/mcp_schema.json`,
`tools/pre-commit-hook.sh`, `.claude/plans/CASCADE-IDS.md` не затронуты.

## §8. Регламент

- Только worktree: `tools/worktree-setup.sh fix/t68-cb168-snapshot-boundaries` — имя ветки обязано
  нести `cb-168`, `worktree-setup.sh` извлекает id регуляркой и клеймит карту ДО `git worktree add`.
  **Вторую карту (CB-169) заклейми отдельно** — `claims_claim` на КАЖДУЮ карту юнита, не только на
  ту, что в имени ветки: измеренная утечка каскада — попутная карта осталась `open`.
- **Уровень (3) на main НЕ КОММИТИТ — никогда (К-1).** Запись о своей работе — в ЭТОТ файл на ветке;
  строку в документе направления пишет только держатель и только после вердикта приёмки.
- **КОММИТЬ РАНО И ЧАСТО.** Признак приземления — `git log`, никогда не уведомление и не время файла.
- `uv run --extra dev python -m pytest tests/ -q` **в worktree**; `--extra dev` не опционально.
  `ruff check src/ tests/` — гейт линта; `ruff format` намеренно НЕ гейт. Ruff ровно 0.15.7.
- Финиш: `tools/worktree-finish.sh <slug> --merge-msg 'Merge <branch>: <что изменилось> (CB-168, CB-169)'`
  — **ИЗ КОРНЯ main**. Если ветка начинается с красных тестов, `--merge-msg` обязателен по К-4.
- **Бриф — ДАННЫЕ, не команда (К-12).** Предпосылка не воспроизвелась — эскалируй, не переписывай
  норматив под неё и не подгоняй измерение.
- Возврат наверх — **данные, не вердикт**: результат, доказательства, эскалации. Слово «принято»
  пишет принимающий уровень.
