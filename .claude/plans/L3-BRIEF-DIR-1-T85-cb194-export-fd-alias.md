# Бриф юнита Т-85 (DIR-1) — CB-194: экспорт в собственный stderr портит файл, а слияние потоков не починено

**Направление:** DIR-1 «Доверие и координация», уровень (2), смена 11, 2026-08-25/26.
**Карта:** CB-194 (medium, `correctness`, файл `src/codebugs/fsio.py`). Ребро `follow_up_of` к CB-143
уже стоит в трекере.
**Класс по Р1: B** — оракул детерминирован (девять строк таблицы решений ниже проверяются прогоном
команды и чтением получившегося файла, никакого суждения). **Стартовый тир: `sonnet`. `effort:
medium`.** Повышать тир только с названной вслух причиной в отчёте.
**Карту CB-194 закрывает ИСПОЛНИТЕЛЬ**, в конце работы, вызовом `mcp__codebugs__update(finding_id=
"CB-194", status="fixed")`. Это сказано явно, потому что в прошлую смену два моих брифа разошлись в
умолчании и приёмка отказала пункт контракта.

---

## 1. Что болит, и почему это долг направления, а не рядовая карта

Предыдущий юнит этого направления (Т-83, мерж `3883bb2`) закрывал CB-143: `codebugs export-csv
/dev/stdout > out.csv` молча портил CSV — строка «Exported N findings…» ложилась поверх заголовка
файла, код возврата 0, stderr пуст. Юнит починил главный сценарий и **тем же касанием создал тот же
дефект там, где его не было, а один старый экземпляр оставил нетронутым.** Изолированный приёмщик
нашёл оба; юнит принят с оговоркой намеренно (откат отнял бы работающее исправление главного
сценария ради узкого), и починка идёт вперёд.

**Я воспроизвёл оба лично на приземлённом `main`** (не на словах карты), в одноразовом трекере в
scratchpad, интерпретатором из `.venv` главного дерева:

* **(А) Регрессия.** `codebugs export-csv /dev/stderr 2> out.csv` → `rc=0`, файл начинается строкой
  `Exported 0 findings to /dev/stderr`, следом обрезанный заголовок `scription,source,tags,…`.
  На состоянии ДО правки Т-83 тот же вызов оставлял заголовок целым, потому что диагностика уходила
  в stdout, то есть на терминал. **Правка сделала хуже там, где было хорошо.**
* **(Б) Уцелевший экземпляр.** `codebugs export-csv /dev/stdout > out.csv 2>&1` → `rc=0`, файл
  испорчен ровно как до правки. Это не регрессия, а тот же симптом в **более распространённой форме
  вызова**, чем починенная: слияние обоих потоков в один файл — обычная идиома скриптов.
* **(В) Контроль.** `codebugs export-csv /dev/stdout > out.csv 2>/dev/null` → заголовок цел. Главный
  сценарий CB-143 действительно починен, и его починку нельзя потерять.

Симметрично на `reqs-export` — там второй экземпляр той же логики.

**Корень один, поэтому и карта одна.** Обработчик задаёт вопрос «совпадает ли назначение с тем
каналом, куда я собираюсь напечатать диагностику», а получает ответ на другой вопрос — «писал ли я
на месте» (`in_place`). Эти два ответа расходятся ровно в двух из девяти случаев, и оба расхождения
— молчаливая порча пользовательского файла.

---

## 2. Что делать — форма выбрана уровнем (2), дизайна не требуется

**`fsio.atomic_write` обязан вернуть не «писал ли я на месте», а КАКИЕ дескрипторы этого процесса
оказались тем же открытым файлом, что и назначение.** Обработчик из этого сам выбирает канал.

**Правило выбора канала — одно предложение, и оно намеренно сформулировано как поправка к сегодняшнему
поведению, а не как новая таблица:**

> Возьми канал, который выбирается сегодня (`stderr`, если запись шла на месте, иначе `stdout`).
> Если этот канал — тот же файл, что и назначение, возьми другой. Если и другой — тот же файл,
> не печатай ничего.

Из такой формулировки **по построению следует, что ни один сегодня правильный случай не меняется**:
переключение происходит только там, где сегодняшний выбор и есть порча. Это несущее требование
приёмки, а не пожелание — см. §5.

### Единственный классификатор сохраняется, второго не заводится

Запрет из брифа Т-83 остаётся в силе: **второго места, где решается вопрос «мой ли это дескриптор»,
быть не должно.** `fsio` уже перечисляет все дескрипторы процесса вместе с парой устройство/inode
(`_held_open_inodes`), то есть **уже знает**, какой именно дескриптор совпал, и схлопывает это в
один бит. Починка — перестать схлопывать. Обработчику запрещено вычислять совпадение самому: второй
экземпляр этой логики может разойтись с первым под гонкой (дескриптор переоткрыт между двумя
вызовами) или при будущей правке одного из двух.

**Приёмщик Т-83 отдельно разобрал, почему довод исполнителя «сузить значило бы завести второй
предикат» был неверен: бриф запрещал второе МЕСТО, где вопрос решается, а не возврат именно булева
значения.** Форма возврата свободна; мест по-прежнему одно.

### Форма возврата — на твоё усмотрение, но с двумя обязательствами

Сегодня контекстный менеджер отдаёт кортеж `(handle, in_place: bool)`. Разумная форма — отдавать
вместо булева небольшой объект (или именованный кортеж), у которого есть и сегодняшний ответ, и
множество совпавших дескрипторов. Выбирай сам, но:

1. **`in_place` обязан остаться выводимым и не может разойтись с множеством совпадений.** Непустое
   множество совпавших дескрипторов означает запись на месте — это инвариант, и его надо утвердить
   (`assert`/проверка в тестах), иначе одно значение снова становится двумя мнениями.
2. **Существующие утверждения в `tests/test_fsio.py` про `in_place is True/False` не ослабляются.**
   Их можно переписать под новую форму возврата — нельзя удалить или заменить на более слабые.
   Ослабление действующего утверждения — отдельный повод для отказа приёмки.

---

## 3. Механизм, который я проверил сам, — объявляю ГИПОТЕЗОЙ, проверь перед тем, как на него опереться

Я измерил (Python 3, этот хост), и это не рассуждение:

* `os.stat('/dev/stdout')` **по исходному пути**, когда stdout — труба, отрабатывает и даёт
  устройство/inode самой трубы, совпадающие с `os.fstat(1)`. Ломался в CB-143 не `stat` по исходному
  пути, а `os.stat(resolved)`, где `resolved` — сфабрикованное имя `/proc/<pid>/fd/pipe:[N]`,
  которого на диске нет.
* `os.stat('/dev/stderr')`, когда stderr перенаправлен в обычный файл, даёт устройство/inode этого
  файла, совпадающие с `os.fstat(2)`; при этом ветка `_FD_DIR` не срабатывает вовсе — `realpath`
  разрешается прямо в файл, и работает ветка «обычный файл, чей inode мы держим открытым».

**Отсюда моя гипотеза: определить совпадение по каждому дескриптору отдельно можно во всех четырёх
ветках `in_place`, той же машинерией, что уже есть.** Проверь это прогоном, а не чтением: если в
какой-то ветке ответ недоступен, скажи об этом вслух и примени правило неопределённости из §4.

---

## 4. Неопределённость решается в пользу молчания, и это названо явно

Если для какого-то дескриптора совпадение **установить не удалось** (например, `os.stat` по пути
отказал в ветке, где иначе не понадобился бы), считай этот дескриптор **возможно совпавшим**.
Следствие: в худшем случае теряется строка подтверждения, но файл не портится.

Направление в сторону молчания выбрано осознанно: потеря подтверждающей строки — неудобство,
которое видно сразу; порча экспортированного файла молчалива и обнаруживается через недели. Обрати
внимание, что это **не** трогает обычный случай: при записи через временный файл с заменой
(`in_place` ложно) совпадение невозможно по построению, поэтому неопределённость там не возникает.

---

## 5. Оракул — девять строк, и он проверяется командой, а не рассуждением

Каждая строка проверяется прогоном настоящего верба и чтением первых байт получившегося файла.
Столбец «сегодня» — измеренное состояние приземлённого `main`; столбец «обязано быть» — критерий
приёмки.

| # | вызов | назначение = fd 1 | назначение = fd 2 | сегодня | обязано быть |
|---|---|---|---|---|---|
| 1 | `export-csv out.csv` (файл существует) | нет | нет | stdout | **stdout, без изменений** |
| 2 | `export-csv new.csv` (файла ещё нет) | нет | нет | stdout | **stdout, без изменений** |
| 3 | `export-csv /dev/null` / именованный канал | нет | нет | stderr | **stderr, без изменений** |
| 4 | `export-csv /dev/stdout > out.csv` | да | нет | stderr | **stderr, без изменений** (починка CB-143 — потерять её нельзя) |
| 5 | `export-csv /dev/stdout \| head -1` (труба) | да | нет | stderr | **stderr, без изменений** |
| 6 | `export-csv out.csv > out.csv` (тот же inode держим открытым) | да | нет | stderr | **stderr, без изменений** |
| 7 | `export-csv /dev/stderr 2> out.csv` | нет | да | **stderr — ПОРТИТ** | **stdout** |
| 8 | `export-csv /dev/stdout > out.csv 2>&1` | да | да | **stderr — ПОРТИТ** | **не печатать вовсе, rc=0** |
| 9 | `export-csv /dev/stderr > out.csv 2>&1` | да | да | **stderr — ПОРТИТ** | **не печатать вовсе, rc=0** |

**Строки 1–6 — это половина оракула, а не фон.** Шесть седьмых таблицы существуют затем, чтобы
починка строк 7–9 не оплачивалась изменением поведения там, где оно верно. Тест, покрывающий только
строки 7–9, приёмку не проходит.

**Строки 8 и 9 честно означают тишину при `rc=0`.** Третьего канала нет: любой напечатанный байт
попадёт в тот самый файл, который пользователь просил экспортировать. Молчание — единственный
честный ответ, и оно обязано быть записано в `CHANGELOG` пользовательскими словами (§7), а не
оставлено как сюрприз.

---

## 6. Честный тест

1. **Начни с красных.** Напиши тесты на строки 7, 8, 9 ДО правки кода и покажи в отчёте, что они
   красные на неправленом дереве. Если ветка начинается с красных тестов, **`--merge-msg` при финише
   ОБЯЗАТЕЛЕН** — деривация субъекта мержа берёт первый коммит первопарентной линии, а при таком
   старте это «red tests», и мерж систематически называл бы тест вместо починки.
2. **Обе поверхности.** `export-csv` (findings) и `reqs-export` (reqs) — два экземпляра одной
   логики. Тест на одной поверхности покрытием второй не является.
3. **Строки 1–6 закрепляются тоже**, как охранники от регресса; там, где такое утверждение уже есть в
   `tests/test_fsio.py`, переиспользуй его, а не заводи второе.
4. **Мутационная проба обязательна, и мутант ставится В ТОЧКУ дефекта** — в выбор канала, а не
   рядом. Перед пробой **закоммить** свою работу (в прошлом юните направления `git checkout --` в
   пробе снёс несохранённые правки). Прогон под `PYTHONDONTWRITEBYTECODE=1` с чистым `__pycache__`, и
   **подтверди `grep`-ом, что мутант действительно применён** — иначе «мутант выжил» и «мутант не
   применён» неразличимы, а это ровно класс «зелёная по построению проба».
5. **Полный сьют и оба голдена.** Юнит не трогает ни инструментов MCP, ни вербов CLI, поэтому
   **`tests/golden/mcp_schema.json` и снимок поверхности CLI двигаться не должны**; сдвиг любого из
   них — сигнал, что скоуп разъехался, и повод остановиться и доложить, а не регенерировать голден.

---

## 7. `CHANGELOG.md` — часть критерия приёмки, и здесь есть отдельный долг

**Долг.** Запись про CB-143 в разделе `### Fixed` **переобещает**: «The confirmation now goes to your
terminal (stderr) instead of into the file whenever the destination is your own standard output» —
неверно при слиянии потоков, где stderr и есть этот файл. Исправь формулировку в том же касании.

**Новая запись** — в существующий `## [Unreleased]`, пользовательским языком, без внутренних
идентификаторов в роли объяснения (номер карты в скобках — можно, вместо объяснения — нельзя). Она
обязана сказать три вещи: экспорт в собственный поток ошибок больше не портит файл; слияние обоих
потоков в один файл больше не портит его; и что в единственном случае, когда оба потока указывают на
экспортируемый файл, подтверждающая строка **не печатается вовсе** — потому что печатать её некуда,
кроме как в сам файл.

---

## 8. Эскалации (ПОМЕЧЕННЫЙ ПУНКТ — отдельным заголовком в отчёте НА ВЕТКЕ)

В отчёте на ветке обязан стоять раздел с заголовком **«Эскалации»**. Пустой раздел со словами
«эскалаций нет» — это ответ; отсутствие раздела ответом не является, и приёмка отказывает по нему
пункт контракта. Сообщение мне в чат раздела не заменяет: приёмщик судит артефакт на ветке.

Эскалируй немедленно, не продолжая работу, если: правило §2 («ни один сегодня правильный случай не
меняется») оказывается невыполнимым без изменения поведения одной из строк 1–6; или обнаружится
третий потребитель `fsio.atomic_write` помимо двух известных; или для починки понадобится тронуть
`cli.run`/обработку сигналов (CB-78/CB-134 — соседняя, но чужая территория).

---

## 9. Регламент

* **Работа только в worktree.** `tools/worktree-setup.sh fix/cb-194-export-fd-alias`, работа внутри
  `.worktrees/fix-cb-194-export-fd-alias`, интеграция `tools/worktree-finish.sh` с `--merge-msg`.
  Проверяй, в каком чекауте ты находишься, перед каждой правкой файла.
* **`uv run --extra dev python -m pytest tests/ -q` — из worktree, `--extra dev` не опционален.**
  Никогда не проверяй правки worktree прогоном сьюта из `main`: `pythonpath = ["src"]` разрешается
  относительно того чекаута, где ты запускаешься.
* **`tools/worktree-probe.sh` в этом репозитории НЕ существует** — это инструмент соседнего проекта.
  За ним ходили пять агентов подряд. Пробы делаются `git archive` в scratchpad.
* **`exit 13` при финише — ВЕРНЫЙ отказ** (`main` сдвинулся, пока шёл гейт), лечится буквальным
  повтором той же команды **с тем же `--merge-msg`**. **`exit 11` повтором не лечится** — это грязный
  `main`, чужая незакоммиченная работа; остановись и доложи мне, не пытайся чинить чужое дерево.
* **В дереве из `git archive` один тест падает всегда** — тот, что читает настоящую историю git. Это
  артефакт метода, а не твоя поломка.
* **К-1: уровень (3) на `main` не коммитит — никогда.** Ни заметок, ни отчётов. Свой отчёт пишешь в
  ЭТОТ файл, но **на ветке юнита**. Единственное исключение — раздел «Post-merge-контроль» после
  приземления мержа (К-17), и его пишет менеджер, один прямой коммит.

---

## 10. Класс задачи и модель (ОБЯЗАТЕЛЬНЫЙ раздел отчёта)

В отчёте на ветке обязан стоять раздел, называющий: класс по Р1 (здесь — **B**, оракул детерминирован),
фактически использованный стартовый тир и `effort`, и все повышения тира с причиной. «Не пинован» —
тоже факт, и он тоже записывается.

---

## 11. Названный остаток и его оценка — В ОБЕ СТОРОНЫ, отдельным разделом

Это требование появилось потому, что пять юнитов подряд занижали остаток, называя его правдиво в
одну — выгодную — сторону. В прошлом юните исполнитель впервые назвал обе стороны, и **обе оказались
неверны по существу**; поймала это приёмка, а не бриф.

Поэтому: назови остаток и **обоснуй его прогоном или чтением кода в обе стороны** —
(1) чем твоя правка **шире** буквального дефекта карты, и (2) чем она **уже** его. Довод вида «это
безопасно по построению, потому что X консервативен» принимается только с показанным случаем, где X
перестаёт быть консервативным, и объяснением, почему он сюда не достаёт. Приёмщику вменено
перепроверить каждую сторону прогоном, а не убедиться, что она названа.

Полезная граница, замеренная приёмщиком прошлого юнита и передаваемая тебе даром: структурный ратчет
`TestWriteCallSitesRatchet` смотрит только на вызовы `open`, поэтому `Path.write_text` или
`os.fdopen` он бы не увидел; сегодня таких вызовов в пакете нет, но твой довод «третьего сайта нет»
обязан опираться на прогон, а не на этот ратчет.

---

## 12. Отчёт исполнителя

### Что сделано

Я лично воспроизвёл все три дефектные строки оракула (7, 8, 9) на приземлённом `main` до
начала работы, в одноразовом трекере в scratchpad, тем же интерпретатором, что использует
worktree: `export-csv /dev/stderr 2> out.csv` даёт файл, начинающийся строкой `Exported 0
findings to /dev/stderr` поверх обрезанного заголовка; та же порча — при слиянии обоих
потоков через `2>&1`, что для `/dev/stdout`, что для `/dev/stderr` в качестве аргумента
назначения. Это совпало с тем, что описывал бриф, и подтвердило гипотезу §3 (стат по
исходному пути, не по разрешённому, добирается до трубы там, где `realpath` не может
построить текстовое имя — измерено, а не просто принято на веру: ниже есть отдельный
случай, где это оказалось шире, чем требовал оракул).

**Корень дефекта.** Функция `fsio.atomic_write` знала о совпадении дескрипторов процесса с
назначением файла (через `_held_open_inodes()`), но схлопывала это знание в один булев
флаг `in_place` — «писал ли я в файл на месте». Оба потребителя (`export-csv` в
`findings.py` и `reqs-export` в `reqs.py`) на основании этого флага решали, куда печатать
строку подтверждения после экспорта («в stderr, если писали на месте, иначе в stdout»), не
имея возможности узнать, не является ли САМ stderr (или оба потока сразу) тем же файлом,
куда только что записались данные. Отсюда две дыры: если ТОЛЬКО stderr — псевдоним
назначения (строка 7), подтверждение всё равно уходило туда и портило файл; если ОБА
потока — псевдоним назначения (строки 8, 9), портился он в любом случае, потому что третьего
канала нет.

**Правка.** `atomic_write` теперь возвращает не булево значение, а объект `WriteResult` с
двумя полями: `in_place` (та же классификация, что и раньше, смысл не поменялся) и
`matched_fds` — множество дескрипторов ЭТОГО процесса (кандидаты — только 1 и 2, то есть
stdout и stderr, больше ничего опрашивать не нужно), которые оказались тем же открытым
файлом, что и место записи. Вычисляется это внутри `fsio.py`, единственным местом — новой
функцией `_open_fd_identities()` (та же логика перечисления `/proc/self/fd`, что раньше жила
внутри `_held_open_inodes`, только с сохранением номера дескриптора, а не только множества
инодов) и `_matched_fds_for()`, которая сравнивает идентичность назначения с идентичностью
каждого открытого дескриптора. Идентичность назначения на обычном пути берётся из уже
вычисленного `os.stat`, а в единственном особом случае — когда путь резолвится в
каталог `/proc/.../fd` (труба вместо файла) — используется стат по ИСХОДНОМУ пути (а не по
сфабрикованному `resolved`), что, согласно гипотезе брифа, добирается до идентичности трубы
через магический символьный линк ядра там, где `realpath` не может построить существующее
текстовое имя. Это подтверждено прогоном, а не просто взято на веру (см. ниже).

Выбор канала для диагностики теперь живёт РОВНО в одном месте — функции
`fsio.diagnostic_stream(result)` — а не дублируется в `findings.py` и `reqs.py` по
отдельности, что и было причиной, по которой Т-83 починил один экземпляр логики и оставил
второй сломанным. Правило (буквально то, что предписано в §2 брифа): взять канал, который
выбирается сегодня (stderr, если писали на месте, иначе stdout); если этот канал —
назначение, взять другой; если и другой — назначение, не печатать ничего.

**Инвариант из §2, пункт 1, утверждается, а не только документируется**: непустое множество
`matched_fds` при `in_place is False` вызывает `AssertionError` в `__post_init__` датакласса
`WriteResult` (не `assert`, а `raise` — по конвенции этого репозитория для несущих
инвариантов, которую перечисляет `CLAUDE.md`, поскольку `assert` вырезается под `-O`).
Утверждающие проверки в `tests/test_fsio.py` про `in_place is True/False` не ослаблены —
переписаны под новую форму возврата (`dest.in_place is True`, где раньше было
`in_place is True`), как и разрешал §2, пункт 2 брифа.

**Тесты.** Красные тесты на строки 7, 8, 9 (`tests/test_fsio.py::
TestCB194BothStreamsCanAliasTheDestination`, четыре штуки — по одной на каждую поверхность
для строки 7, и по одной на каждую поверхность для строк 8/9) показаны красными на
неправленом дереве через `git stash` правки в `src/` при сохранённых тестах (не через откат
коммита — коммит с правкой уже был сделан к тому моменту ради безопасной мутационной пробы,
поэтому стэш был единственным способом развести тесты и код без потери истории). Все четыре
упали именно с тем содержимым файла, что предсказывал бриф (обрезанный заголовок или строка
подтверждения внутри CSV/markdown). После возврата правки — зелёные. Отдельно закреплена
строка 6 (`export-csv out.csv > out.csv` буквально тем же путём, не через `/dev/stdout`) —
она и раньше работала правильно, но явно не была протестирована на уровне CLI, а бриф
специально подчёркивает, что первые шесть строк — «половина оракула, а не фон».

**Найдено сверх оракула, тоже проверено прогоном.** Проверяя гипотезу §3 на трубах, я
обнаружил четвёртый непокрытый брифом случай: `export-csv /dev/stdout 2>&1 | cat` — труба
(не файл), разделяемая между stdout и stderr через дублирование дескриптора. У труб нет
общего смещения записи, поэтому дефект там не «затирание заголовка», а «лишняя строка в
конце потока» — измерено на дереве ДО этой карты (коммит `4dfa381`, только с починкой
CB-143): комбинированный вывод оканчивается строкой `Exported 1 findings to /dev/stdout`
после CSV-строк, то есть поток, который клиент мог бы передать дальше в `sort` или в
CSV-парсер, получает нестроку данных как будто это данные. Починка того же механизма
(`diagnostic_stream` плюс расчёт через исходный путь) устраняет и этот случай без
дополнительного кода — я закрепил это отдельным тестом
(`test_export_csv_to_dev_stdout_piped_with_stderr_merged_prints_nothing`).

**Мутационная проба.** Перед пробой правка была закоммичена (коммит `ea0c5e9`) — по
требованию §6, пункт 4 брифа, чтобы `git checkout --` мог откатить именно мутанта, а не
живую работу. Мутант поставлен ТОЧНО в точку дефекта — тело `diagnostic_stream` заменено на
безусловное `return sys.stderr if result.in_place else sys.stdout` (буквально то поведение,
что было до правки, то есть мутант — это не гипотетическая порча, а воспроизведение
реального бага). Каталоги `__pycache__` вне `.venv` вычищены, прогон — под
`PYTHONDONTWRITEBYTECODE=1`. Применение мутанта подтверждено `grep`-ом по исходнику ДО
прогона (иначе «мутант выжил» и «мутант не применился» неразличимы). Все четыре теста на
строки 7-9 упали на мутанте — проба поймала дефект. Мутант отменён через
`git checkout -- src/codebugs/fsio.py`, дерево вернулось к состоянию коммита без остатка
(`git status`/`git diff --stat` после отмены — пусто), полный `tests/test_fsio.py` —
снова зелёный (34 теста).

**Полный сьют и голдены.** `uv run --extra dev python -m pytest tests/ -q` — **2718 тестов
пройдено**, 0 упавших (прогон с уже добавленным тестом на трубу-псевдоним). `uv run
--extra dev ruff check src/ tests/` — чисто.
`git diff --stat -- tests/golden/` — пусто: ни `mcp_schema.json`, ни `cli_surface.json` не
сдвинулись, что и требовалось (юнит не трогает ни один MCP-инструмент и ни один CLI-верб —
только внутреннюю функцию `fsio.atomic_write` и её двух потребителей).

**CHANGELOG.** Запись про CB-143 в `### Fixed` исправлена — убрано переобещание
«confirmation now goes to your terminal (stderr) instead of into the file whenever the
destination is your own standard output», которое было неверно ровно в тех двух случаях,
которые чинит эта карта. Добавлена новая запись про CB-194 пользовательским языком, без
внутренних идентификаторов вместо объяснения: сказано, что экспорт в собственный поток
ошибок больше не портит файл, что слияние обоих потоков в один файл больше не портит его, и
что в единственном случае, когда оба потока указывают на экспортируемый файл, строка
подтверждения не печатается вовсе — потому что печатать её больше некуда.

### Эскалации

Эскалаций нет. Ни одно из трёх условий §8 не сработало: правило §2 («ни один сегодня
правильный случай не меняется») оказалось выполнимым без изменения поведения строк 1-6 —
это проверено прогоном всего `tests/test_fsio.py` (включая `TestCompatibility` и
`TestCB143DiagnosticDoesNotCorruptTheFile`, которые как раз и являются оракулом для этих
шести строк) и отдельно закреплено новым тестом на строку 6. Третьего потребителя
`fsio.atomic_write` не обнаружено — `grep -rn "atomic_write" src/` перед началом работы
показал ровно два вызывающих места (`findings.py`, `reqs.py`) и определение в самом
`fsio.py`; других мест, где создаётся или читается файл через эту функцию, в пакете нет.
Трогать `cli.run`/обработку сигналов (CB-78/CB-134) не понадобилось — весь механизм
починки лежит внутри `fsio.py` и двух точек его использования, ни одна из них не
пересекается с установкой диспозиции сигналов или с проверкой закрытого stdout при входе в
процесс.

### Класс задачи и модель

Класс по Р1: **B** — оракул детерминирован (девять строк таблицы решений проверяются
прогоном команды и чтением получившегося файла, как и было заявлено в §0 брифа; расширение
оракула десятым случаем — труба, разделяемая между потоками — тоже проверено прогоном, а не
суждением). Стартовый тир: `sonnet`, `effort: medium`, как предписано брифом. Тир НЕ
повышался — вся работа (чтение кода, формулировка механизма, тесты, мутационная проба,
интеграция) выполнена на стартовом тире; повода назвать вслух причину для повышения не было.

### Названный остаток и его оценка — в обе стороны

**Чем правка шире буквального дефекта карты, обосновано прогоном.**

1. Возврат `atomic_write` изменён с булева значения на датакласс `WriteResult` — это
   расширение публичного контракта функции за пределы строго необходимого для починки трёх
   строк оракула: можно было бы обойтись, например, вторым отдельным параметром или
   атрибутом на файловом хендле, сохранив `in_place` булевым. Я выбрал датакласс, потому
   что бриф явно предлагал такую форму («небольшой объект... у которого есть и сегодняшний
   ответ, и множество совпавших дескрипторов») и потому что альтернатива (два отдельных
   булевых значения без единой точки утверждения инварианта) хуже держит инвариант из §2
   пункта 1. Cтоимость этого расширения реальна и видна по `diff`: пришлось переписать пять
   мест в `tests/test_fsio.py`, где `in_place` разыменовывалось напрямую как булево
   значение (`test_an_inode_this_process_holds_open_is_written_in_place` и четыре теста в
   `TestCompatibility`) — все они правились под новую форму, ни одно утверждение не
   ослаблено (проверено построчным сравнением: было `assert in_place is True/False`, стало
   `assert dest.in_place is True/False`, критерий тот же).
2. Механизм генерализуется на случай, которого нет в буквальном оракуле брифа: труба,
   разделяемая между stdout и stderr через `2>&1 | cat` (`export-csv /dev/stdout` в таком
   контексте). Я специально проверил это прогоном на дереве ДО этой карты (коммит
   `4dfa381`, только с починкой CB-143) — там дефект воспроизводится в виде лишней строки
   `Exported 1 findings to /dev/stdout`, приклеенной к концу потока после CSV-строк, что
   для потребителя вниз по конвейеру (например, `sort` или CSV-ридер) читается как
   некорректная запись данных. На дереве с этой правкой тот же вызов даёт чистый поток без
   единого байта подтверждения — работает тот же путь `diagnostic_stream` плюс расчёт
   идентичности через исходный (не разрешённый до конца) путь, ни строки нового кода под
   этот случай не понадобилось. Закреплено отдельным тестом
   (`test_export_csv_to_dev_stdout_piped_with_stderr_merged_prints_nothing`).
3. Функция `_matched_fds_for` при неопределённости (не смогли установить идентичность
   назначения) консервативно считает совпавшими СРАЗУ ОБА дескриптора-кандидата (1 и 2),
   хотя неопределённость могла бы касаться только одного из них. Это осознанно шире
   необходимого (в духе §4 брифа — «в худшем случае теряется строка подтверждения») ценой
   более частого молчания там, где часть информации на самом деле была доступна. На
   практике этот путь ни разу не сработал в моих прогонах (гипотеза §3 подтвердилась во
   всех проверенных сценариях, включая труду), так что цена сегодня равна нулю, но
   встроена она заведомо шире, чем нужно для наблюдаемых сценариев.

**Чем правка уже буквального дефекта карты, обосновано прогоном или чтением кода, а не
одним лишь «это безопасно по построению».**

1. Путь неопределённости (`dest_id is None` после провала стата по исходному пути в ветке
   fd-алиаса/трубы) НЕ проверен прогоном — я не смог сконструировать сценарий, в котором
   `os.stat(path)` на `/dev/stdout`/`/dev/stderr` действительно бросает исключение на этой
   машине: гипотеза §3 подтверждалась каждый раз, включая случай с трубой, разделённой
   между двумя потоками. Это защитный код, оправданный рассуждением (§4 брифа прямо
   допускает такую ветку), но НЕ покрытый красно-зелёным циклом — честно называю это
   непроверенной веткой, а не скрываю за общим «безопасно по построению». Довод в пользу
   того, что она всё же не опасна: она может только СУЗИТЬ множество каналов для печати
   (в худшем случае — до пустого, то есть до молчания), никогда не расширить его так, чтобы
   напечатать в сам файл назначения — это следует из кода `_matched_fds_for`, а не из
   догадки: `dest_id is None` ведёт к `frozenset({1, 2})`, то есть к максимально
   консервативному множеству, а не к пустому.
2. Множество кандидатов в `matched_fds`/`diagnostic_stream` жёстко ограничено {1, 2}
   (stdout, stderr) — никакой другой дескриптор (например, 0 — stdin) никогда не
   проверяется. Сегодня это ровно то, что нужно: оба существующих потребителя выбирают
   только между stdout и stderr. Если появится третий потребитель `atomic_write` с иной
   парой каналов, он не сможет переиспользовать `diagnostic_stream` без изменения кода —
   но это ровно то условие эскалации, что названо в §8 брифа, и здесь не сработало, потому
   что третьего потребителя нет (проверено `grep`-ом, см. «Эскалации» выше).
3. Утверждение инварианта в `WriteResult.__post_init__` ловит только ОДНУ форму нарушения
   (непустое `matched_fds` при `in_place is False`) — оно не может и не пытается доказать,
   что `matched_fds` вычислено ПРАВИЛЬНО в остальных случаях (например, что оно не пропустило
   реально совпавший дескриптор). Корректность вычисления проверяется отдельно, тестами по
   оракулу и ручными прогонами выше, а не самим инвариантом — это структурная страховка от
   будущей правки, ломающей форму данных, а не доказательство правильности значения.
4. Ратчет `TestWriteCallSitesRatchet` смотрит только на вызовы `open`, поэтому
   `Path.write_text`/`os.fdopen` вне `fsio.py` он не увидит — это ограничение унаследовано
   от предыдущего юнита направления (передано мне даром в §11 брифа) и не переоценивалось
   заново; довод «третьего сайта нет» у меня, как и предписано, опирается на `grep`
   (см. «Эскалации»), а не на этот ратчет.
