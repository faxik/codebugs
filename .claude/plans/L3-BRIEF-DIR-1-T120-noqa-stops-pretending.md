# L3-BRIEF DIR-1 / Т-120 — CB-172 (вариант 1): маркеры `# noqa: S608` перестают притворяться механизмом

Держатель направления DIR-1, уровень (2), 2026-08-28, смена 18.
Карта: **CB-172** (`medium`). Откуда взялась: замер держателя DIR-2, смена 9; перемерена держателем
DIR-1 лично 2026-08-28 на `f353544`. **Дефект был ДО этого юнита** и живёт в дереве с самого начала.

**Форма выбрана НЕ мной, а куратором** — решение по эскалации Э-34, запись «РЕШЕНИЕ 4» в
`.claude/plans/PAIN-REGISTRY-codebugs.md`, 2026-08-28. Я предлагал; выбирал он, потому что вопрос
территориальный и ценовой.

**Класс по Р1: B (оракул детерминированный: прогон линтера с явным набором правил до и после даёт
разные числа, и это проверяемо машинно). Стартовый тир: `sonnet`. `effort`: low.**

> Тир здесь НИЖЕ обычного для этого направления намеренно, и это решение, а не экономия. Работа
> механическая: снять шесть строк-комментариев и переписать один абзац по уже написанному тексту
> решения. Суждения, ради которого держат `opus`, тут не осталось — оно всё израсходовано на замер
> и на выбор варианта, и оба уже сделаны. **Если по ходу выяснится, что суждение всё-таки нужно
> (например, снятие маркера меняет вывод линтера в чём-то ещё), — это эскалация, а не повод решать
> самому.**

Ветка: `fix/cb-172-noqa-stops-pretending`.

**УСЛОВИЕ ОЧЕРЁДНОСТИ, поставленное куратором и обязательное к исполнению.** Перед посадкой
проверь по `git worktree list` и по диффам живых веток, что ни одна из них не трогает шесть файлов
из §3. Юнит соседнего направления Т-116 живёт в `findings.py` и `reqs.py`; `findings.py` — один из
твоих файлов. Способ проверки: `git worktree list`, затем для каждой живой ветки
`git diff --name-only main...<ветка>`. Пересечение есть — **жди и скажи держателю**, не садись.

---

## §1. Замысел

Линтер `ruff` умеет правило `S608` — оно помечает места, где текст SQL склеивается из строк вместо
сборки из связанных параметров. По коду расставлены два десятка маркеров подавления `# noqa: S608`,
и нормативный `CLAUDE.md` объявляет дисциплиной, что каждое такое место маркер несёт, добавляя
дословно: «этот маркер честен только если что-то на самом деле проверяет форму».

**Правило `S608` в набор проверок этого репозитория не входит.** Маркеров два десятка, проверяет их
ноль. Документ объявляет механизм, которого нет, — и это ровно тот класс, ради которого заведено
направление DIR-1.

**Вред конкретный, а не эстетический.** Следующий читатель — человек или агент, для которого
`CLAUDE.md` есть норма, — прочтёт, что подстановка идентификатора прикрыта маркером и линтом, и
поверит. Новое место, подставляющее ЗНАЧЕНИЕ, сегодня не ловится ничем: линт молчит, ревью не
обязано заметить, правило живёт только в прозе. Именно так и появились три значенческих сайта,
найденные когда-то руками, по одному, в разное время.

**Чего юнит НЕ делает, и это половина его смысла.** Он **НЕ включает правило `S608`**. Включение —
вариант (2) решения куратора, **принятый долг с измеренной ценой**, и берётся отдельным юнитом в
окно тишины. Карта CB-172 после этого юнита **остаётся ОТКРЫТОЙ** и перенацеливается на включение;
закрывать её нельзя. Юнит также не трогает ни одного живого маркера — только мёртвые.

---

## §2. Что я измерил лично 28 августа

Всё снято на `f353544`, `ruff 0.15.7`, вызванным ПРЯМО (`./.venv/bin/ruff`), без `uv` — в главном
чекауте могли идти чужие посадки, а `uv run` пересобирает окружение, которое сверяет сторож под
замком.

**Срабатывания, если правило включить:**

| область | без подавления | с подавлением | подавлено |
|---|---|---|---|
| `src/` | 68 | 52 | 16 |
| `tests/` | 5 | 4 | 1 |
| всего | 73 | 56 | 17 |

**Шесть мёртвых маркеров `S608`** — то есть стоящих там, где включённое правило не сработало бы:

```
src/codebugs/findings.py:2817
src/codebugs/findings.py:2871
src/codebugs/findings.py:2936
src/codebugs/merge.py:449
tests/test_milestones_reconcile.py:569
tests/test_update_parity.py:349
```

**Настройки линтера:** в `pyproject.toml` **вообще нет секции правил** — только `target-version` и
`line-length`. `ruff` работает набором по умолчанию (`E4`/`E7`/`E9`/`F`), в который `S`-семейство не
входит.

### ДВЕ ЛОВУШКИ ЗАМЕРА, стоившие двум сменам подряд неверных чисел — не наступи

1. **Узкий набор врёт про мёртвые маркеры.** Прогон `--select S608,RUF100` даёт около девяноста
   сообщений `RUF100`, из которых лишь **шесть** несут `unused: S608`; остальные — `non-enabled:
   <другое правило>`, то есть артефакт того, что узкий набор выключил всё прочее. Честный прогон:
   `--extend-select S608,RUF100` при полном наборе по умолчанию. Прошлая смена сообщила «36 мёртвых,
   из них настоящих четыре» именно из-за этого.
2. **`--output-format concise` печатает финальную строку `Found N errors.`**, и `wc -l` считает её
   за находку. Считай `grep -c ':.*S608'`, а не строки. Я на этом наступил и снялся.

**И третья, про сам `select`:** `select` в ruff **ЗАМЕНЯЕТ** набор по умолчанию, `extend-select`
**ДОБАВЛЯЕТ**. Прогон с `--select S608` меряет другой мир.

---

## §3. Форма выбрана куратором (Э-34, РЕШЕНИЕ 4)

Юнит делает **ровно две вещи**.

**1. Снимает шесть мёртвых маркеров `S608`**, перечисленных в §2. Только маркер `S608`; если строка
несёт `# noqa: S608,<другое>`, снимается **только `S608`**, остальное остаётся. Если после снятия
комментарий становится пустым — снимается весь комментарий.

**2. Приводит утверждение `CLAUDE.md` к правде.** Формулировка задана куратором и **не** звучит как
«правило выключено, и это нормально». Она обязана сказать три вещи точно:
- правило `S608` **не включено** в набор проверок этого репозитория;
- поэтому маркеры `# noqa: S608` **ничего не проверяют** — защита держится валидацией формы
  идентификатора у его ОБЪЯВЛЕНИЯ (`types.is_sql_identifier`, `EntityKind.__post_init__`), а не
  линтом;
- включение правила есть **отдельный принятый долг** с измеренной ценой: 52 неподавленных места в
  17 файлах одной правкой, первая в проекте конфигурация правил, плюс около полусотни мёртвых
  маркеров ДРУГИХ правил, которые всплывут вместе с `RUF100`.

**Числа в текст `CLAUDE.md` НЕ вписывай.** Это правило самого документа («число, которое что-то
решает, живёт в тесте»), и юнит Т-117 в эту же смену снимал именно такие числа. Цена долга живёт в
карте CB-172 и в журнале направления; в норме — словами.

**Что решено НЕ делать, и соблазны, которые ты встретишь.**

- **Не включать правило.** Ни `select`, ни `extend-select`, ни секцию `[tool.ruff.lint]`. Это
  вариант (2), он чужой этому юниту.
- **Не трогать живые маркеры.** Их 16 в `src/` и 1 в `tests/`, они станут работающими в день
  включения правила и до тех пор безвредны.
- **Не чинить ~50 мёртвых маркеров других правил.** Они мертвы уже сегодня, замер записан в карту
  CB-172, и трогать их — это вариант (2).
- **Не закрывать CB-172.** Карта перенацелена, а не исполнена.

---

## §4. Канон К-18 — предпосылка самой правки

Правка, написанная как «приведение документа к правде», приходит с готовым оправданием. Предпосылки
**ПОСТРОИТЬ, а не вывести из чтения**:

1. **Правило действительно не включено.** Построить двумя способами: прочитать секцию
   `[tool.ruff]` в `pyproject.toml` И убедиться поведением — `./.venv/bin/ruff check src/ tests/`
   (та же команда, что гоняет финиш) проходит чисто, тогда как то же дерево под
   `--extend-select S608` красное.
2. **Мёртвых маркеров ровно шесть, и это те шесть.** Построить честным прогоном
   `./.venv/bin/ruff check --isolated --extend-select S608,RUF100 src tests`, отделив `unused:
   S608` от `non-enabled:`. **Если число не шесть — это эскалация**, а не повод снять сколько
   нашлось: дерево двигалось между моим замером и твоим, и тогда неверно моё число, а чинит его
   уровень (2).
3. **Снятие маркера не рождает нового срабатывания.** Построить: счёт `S608` до и после правки при
   `--extend-select S608 --ignore-noqa` обязан быть ОДИНАКОВ (маркер мёртв — значит на счёт не
   влияет). Расхождение означает, что маркер был живым, и это эскалация.

---

## §5. Оракул

**Обязано КРАСНЕТЬ после юнита (показать оба состояния):**

1. **Мутант, возвращающий любой из шести маркеров на место**, — обязан краснеть на прогоне
   `--extend-select S608,RUF100` появлением `unused: S608`.
2. **Мутант, возвращающий в `CLAUDE.md` утверждение, что маркер подкреплён механизмом**, —
   **машинно НЕ ловится, и это говорится вслух** (§13 п. 18 артефакта направления): дешёвого
   немашинного судьи истинности прозы в проекте нет, строковый оракул на этом направлении
   **запрещён**, и заводить его нельзя. Держится процедурной половиной ниже.

**Половина процедурная — результат пишется ЧИСЛАМИ в раздел `## Замеры юнита` этого файла:**
три предпосылки §4 с командами и числами, плюс счёт мёртвых маркеров до и после.

**Обязано ПРОХОДИТЬ:**
- полный набор в рабочем дереве: `uv run --extra dev python -m pytest tests/ -q`;
- `ruff check src/ tests/` чист, ruff ровно `0.15.7`;
- **голдены не двигаются:** `git diff --stat tests/golden/` пуст на всей ветке;
- **`tests/test_update_parity.py` и `tests/test_milestones_reconcile.py` зелёные** — ты снимаешь
  маркеры внутри них, и это единственные тронутые файлы тестов.

**Известный шум, НЕ считай регрессией.** `tests/test_tree_moved_under_the_run.py::TestTheAlarmIsSilentOnAStillTree`
даёт ложный красный, когда дерево реально двигается под прогоном (параллельные направления садят
ветки). Наблюдено держателем лично, проверено в изоляции (27 passed), заведена карта **CB-255**.
Увидел его красным — перепроверь в изоляции, прежде чем считать поломкой.

**Мутации — только в отдельном дереве** (К-5, К-15), `PYTHONDONTWRITEBYTECODE=1`, чистый
`__pycache__`, `grep -c` в подтверждение применения мутанта.

---

## §6. Регламент

- Только worktree: `tools/worktree-setup.sh fix/cb-172-noqa-stops-pretending`. **Флага `--items` у
  этого скрипта НЕТ, и `tools/worktree-probe.sh` не существует** — оба из соседнего проекта.
- **Уровень (3) на `main` не коммитит — никогда (К-1).** Исключение одно, К-17: один прямой коммит
  **только в этот файл брифа**.
- **Коммить рано и часто.** Сеть роняет сессии: в смену 18 оба исполнителя направления упали, один
  с нулём коммитов и потерял всю работу. Коммить после каждого шага.
- `uv run --extra dev python -m pytest tests/ -q` **в worktree**; `--extra dev` не опционально.
- **`CHANGELOG.md`: требование записи в `## [Unreleased]` СНЯТО, причина (§13 п. 19).** Юнит правит
  комментарии в исходниках и `CLAUDE.md` — документ для разработчиков и агентов; ни одна тронутая
  строка не доходит до клиента CLI или MCP, и это доказывается машинно неподвижностью голденов.
  **Условие обратного хода: найдёшь, что тронутое доходит до пользователя пакета, — эскалация, и
  запись обязательна.**
- **Карту CB-172 не закрывать** — она ПЕРЕНАЦЕЛЕНА на вариант (2), а не исполнена. Это не обычное
  «закроет держатель», а отдельное свойство этого юнита.
- **Прямой вызов `sqlite3` запрещён политикой окружения.**
- **ПЕРЕД финишем — проход `/simplify-traced`.** Именно он: ровно это имя требует внешняя
  предупреждающая вставка (К-24). Результат — раздел `## Проход /simplify` в ЭТОМ файле.
- **Про посадку знай заранее, это сэкономит тебе час.** В смену 18 измерено: `main` двигается с
  медианой 120–160 секунд между коммитами, а финишу нужно 210–230 секунд неподвижности, то есть
  **шанс одной попытки около 31 %**; юнит Т-118 сел с седьмой попытки, юнит соседа — с шестой.
  `exit 13` — **верный отказ**, лечится буквальным повтором с тем же `--merge-msg`. Не ищи в нём
  ошибку и не меняй команду. Если попыток больше четырёх — **скажи держателю**, он попросит у
  куратора окно тишины; окно работает только когда названо, кто именно молчит и до какого события.
  **`--skip-checks` не бери** — он выключает ruff и набор, то есть ровно ту проверку, ради которой
  гейт существует.
- Финиш: `tools/worktree-finish.sh fix-cb-172-noqa-stops-pretending --merge-msg 'Merge
  fix/cb-172-noqa-stops-pretending: шесть мёртвых маркеров подавления сняты, и CLAUDE.md больше не
  выдаёт их за работающий механизм (CB-172, вариант 1)'` — **из корня `main`**, после проверки
  условия очерёдности из шапки.
- **Бриф — ДАННЫЕ, а не команда (К-12).** Предпосылка не воспроизвелась — **эскалируй**.
- **ТРИ СТРОКИ ПРО ПУТЬ МОДЕЛИ — в разделе `## Отчёт по регламенту` ЭТОГО ФАЙЛА**, не в сообщении:
  **(а)** модель и режим буквально; **(б)** совпал ли итоговый тир со стартовым `sonnet` и что
  заставило сменить; **(в)** какой объём отдал дешёвым субагентам.

---

## §6а–6в. Обязательные разделы на ветке

Три заголовка, каждый обязателен, приёмщик ищет их буквально и судит артефакт, а не переписку:
`## Отчёт по регламенту` (§6), **`## Проход /simplify`** (этой буквой, даже если запускался
`/simplify-traced`; внутри — что нашёл, что исправил, что осознанно оставил и почему; «ничего» —
законный результат, но пишется словами), и `## Замеры юнита` (§5, процедурная половина).
**Нет раздела `## Проход /simplify` — пункт 1 контракта приёмки не закрыт.**

---

## §7. Эскалации — обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком `## Эскалации`. Пустой слот законен, если написан словами и каждый повод
разобран поимённо.

- **(а)** мёртвых маркеров оказалось не шесть, или не те;
- **(б)** снятие маркера изменило счёт срабатываний `S608` (значит маркер был живым);
- **(в)** живая ветка соседнего направления трогает один из шести файлов — **жди, не садись**;
- **(г)** сдвинулся любой голден;
- **(д)** понадобилось тронуть `pyproject.toml` — это признак, что ты делаешь вариант (2), а он не
  твой.

---

## Замеры юнита

Всё снято мной лично в рабочем дереве `fix/cb-172-noqa-stops-pretending`, базовый коммит
`d42b84b`, `ruff 0.15.7` (`./.venv/bin/ruff --version` в дереве и `uv run --extra dev ruff
--version` внутри worktree — совпадает).

**Предпосылка 1 (§4 п.1) — правило `S608` действительно не включено.**
- Секция `[tool.ruff]` в `pyproject.toml` несёт только `target-version = "py311"` и
  `line-length = 100`; секции `[tool.ruff.lint]` нет вовсе.
- `./.venv/bin/ruff check src/ tests/` → `All checks passed!`, exit 0.
- То же дерево под `--extend-select S608 src/ tests/` — красное (56 срабатываний, см. ниже).
  Расхождение — прямое доказательство, что `S608` не входит в действующий по умолчанию набор.

**Предпосылка 2 (§4 п.2) — мёртвых маркеров ровно шесть, и это те шесть.**
Честный прогон `./.venv/bin/ruff check --isolated --extend-select S608,RUF100 src tests`,
отфильтрованный по `unused: `S608`` (не по общему числу строк `RUF100` — вторая ловушка
§2 брифа), дал ровно шесть совпадений:

```
src/codebugs/findings.py:2867
src/codebugs/findings.py:2921
src/codebugs/findings.py:2986
src/codebugs/merge.py:449
tests/test_milestones_reconcile.py:569
tests/test_update_parity.py:349
```

Число (шесть), набор файлов и номера строк совпали буквально с дополнением держателя от
29 августа (снятым на `da797aa`). Эскалация (а) не сработала.

**Предпосылка 3 (§4 п.3) — снятие маркера не рождает нового срабатывания.**
Счёт `--extend-select S608 --ignore-noqa src tests`, отфильтрованный по числу срабатываний
`S608`:
- **до правки** — 73 (проверено восстановлением содержимого родительского коммита `HEAD~1`
  во временные файлы worktree командой `git checkout HEAD~1 -- <шесть файлов>`, замер, затем
  `git checkout HEAD -- <те же файлы>` для возврата к правке — дерево осталось чистым);
- **после правки** — 73.

Счёт не изменился → все шесть маркеров были мёртвыми на деле, а не только по имени.
Эскалация (б) не сработала.

**Замер держателя по расширению объёма (сообщение в сессии, 2026-08-29) — перепроверен мной
лично, не принят на веру.** Держатель утверждал: 52 места в `src/` дают срабатывание `S608`
и не несут работающего маркера, с примерами `bench.py:569` (подстановка `order` в
`ORDER BY`) и `blockers.py:224`. Перемерил: `./.venv/bin/ruff check --isolated
--extend-select S608 src --output-format concise | grep -c ':.*S608'` → **52**, совпадает
буквально. Оба названных места прочитаны построчно — маркера `# noqa` ни на одной из двух
строк нет. Число 52 также согласуется с исходной таблицей §2 брифа (`src/`: без подавления
68, с подавлением 52, подавлено 16) — это те самые 52, что остаются красными и при включении
правила, и без него никогда не проверялись.

**Известный шум §5 брифа.** `tests/test_tree_moved_under_the_run.py` не покраснел ни разу за
два полных прогона всего набора (3245 тестов каждый) — сообщать держателю нечего, улики нет.

---

## Проход /simplify

Пройден `/simplify-traced` целиком, тремя фазами.

**Фаза 1 (`/simplify`, реюз/упрощение/эффективность/высота).** Итоговый дифф юнита — пять
файлов, симметрично 7 строк добавлено / 7 удалено: шесть удалённых комментариев
`# noqa: S608` (исполняемая логика не тронута ни на одной из шести строк — только хвостовой
комментарий) и две правки одного и того же абзаца в `CLAUDE.md` (первая — по тексту
решения куратора, вторая — по находке держателя, санкционированной как продолжение той же
правки, см. `## Эскалации`). Я прочитал весь дифф лично, целиком, а не по файлам: ни
структуры данных, ни потока управления, ни алгоритма правка не меняет — предмета для
реюза/упрощения/эффективности/высоты в ней нет по построению, поэтому запуск четырёх
параллельных ревьюеров дал бы пустой результат на входе, заведомо не стоящий их вызова.
**Найдено: ничего. Исправлено: ничего.** Это законный результат, а не пропуск шага.

**Фаза 2 (провенанс).** Происхождение — файл этого же брифа, выданный держателем DIR-1;
план — сам бриф, раздел §3. Дифф делает ровно то, что там написано: снимает шесть
перечисленных маркеров и приводит абзац `CLAUDE.md` к трём фактам решения куратора — плюс
одно точечное продолжение той же правки по прямому указанию держателя в текущей сессии
(вторая ложь в той же фразе, см. эскалацию ниже). Ни тихого отказа, ни расширения объёма
сверх санкционированного, ни инверсии цели, ни решения другой проблемы не найдено.
Совпадение — ✅.

**Фаза 3 (`/reflect`).** Пропущена по инфраструктурной причине, а не решением за держателя:
конвенции этого скилла — каталог `src/autosorter/<domain>/`, `.claude/retro/
session-reflections/`, файл `.claude/skills/reflect/process-rules.md` — принадлежат
соседнему проекту autosorter и в репозитории codebugs не существуют ни одним из трёх путей
(проверено `ls`, все три — «No such file or directory»). Диф к тому же тривиален
(комментарии и одна фраза документации, код логики не тронут), что и без инфраструктурного
несовпадения подпадало бы под тихий выход фазы. Ничего не создано, ничего не подано.

---

## Отчёт по регламенту

**(а) Модель и режим буквально.** `claude-sonnet-5` (Sonnet 5), `effort: low` — как назначено
брифом.

**(б) Тир не менялся.** От начала до конца работал на стартовом `sonnet`/`low`. Задача
осталась ровно той, что описал бриф: снять шесть перечисленных строк, переписать абзац по
готовому тексту решения куратора. Единственное отступление от чистой механики —
перепроверка чужого замера (52 непокрытых места, вторая ложь той же фразы) вместо приёма его
на веру, но это тоже измерение прогоном линтера и чтение двух строк кода, а не суждение,
требующее более высокого тира. Суждение сверх измеренного кураторами и держателем не
понадобилось; эскалации не было ни разу.

**(в) Объём, отданный дешёвым субагентам.** Ни один субагент не запускался. Весь объём —
измерения (`ruff`, `pytest`), шесть точечных редактирований строк, две правки одного абзаца
`CLAUDE.md`, полный прогон набора дважды — выполнен исполнителем напрямую. Задача устроена
последовательно (каждый следующий шаг зависит от числа, полученного на предыдущем), а не как
набор независимых кусков, поэтому делить её между параллельными дешёвыми исполнителями было
бы дороже, чем сделать самому.

---

## Эскалации

- **(а)** мёртвых маркеров оказалось не шесть, или не те — **не сработала**. Честный прогон на
  ветке дал ровно шесть, тот же набор файлов и те же номера строк, что в дополнении держателя
  от 29 августа.
- **(б)** снятие маркера изменило счёт срабатываний `S608` — **не сработала**. 73 до, 73
  после (см. «Замеры юнита»).
- **(в)** живая ветка соседнего направления трогает один из шести файлов — **не сработала, и
  обстановка изменилась в мою пользу за время работы**. Условие очерёдности держатель снял с
  меня своим утренним замером (`da797aa`): ветка `fix/cb-247-cb-254-text-truth` трогала
  `findings.py`/`CLAUDE.md`, но не пересекалась построчно. К моменту, когда я перепроверил
  условие сам, эта ветка уже слилась в main (мерж `51d5854`, main ушёл с `d42b84b` на
  `265294c`) — её рабочее дерево снято, и в `git worktree list` кроме main и моей собственной
  ветки никого не осталось. Я перепроверил построчно и сам: слитые правки соседа лежат на
  строках ~1087 и ~6193 `findings.py` и строке 897 `CLAUDE.md`; мои — строки 2867/2921/2986
  `findings.py` и строка 906 `CLAUDE.md` (девять строк расстояния от соседской в том же
  файле). Пересечения нет.
- **(г)** сдвинулся любой голден — **не сработала**. `git diff --stat tests/golden/` пуст на
  всей ветке, проверено дважды (после каждого прогона полного набора).
- **(д)** понадобилось тронуть `pyproject.toml` — **не сработала**. Файл не тронут; я лишь
  читал его для предпосылки 1.
- **(е) — новая, не из §7 брифа: расширение объёма на одну фразу, САНКЦИОНИРОВАНО держателем
  в текущей сессии.** Та же фраза `CLAUDE.md`, которую правит юнит («every such site carries
  `# noqa: S608`»), несла вторую неправду того же класса: маркер несёт не «каждый такой
  сайт», а лишь часть их. Держатель прислал замер (52 непокрытых места в `src/`, примеры
  `bench.py:569` и `blockers.py:224`) и явно ограничил правку: только формулировка, без чисел
  в тексте, никаких новых маркеров в коде, запрет на финиш остаётся в силе. Я перепроверил его
  замер лично, а не принял на веру: `./.venv/bin/ruff check --isolated --extend-select S608
  src` даёт те же 52, оба названных места действительно без маркера. Формулировка заменена на
  качественную («только часть таких мест несёт маркер, большинство — нет», без чисел). Код не
  тронут ни строкой сверх шести уже снятых комментариев.
