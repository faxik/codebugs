# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-31, смена 20

(вытесняет `HANDOFF-DIR-1-2026-08-29-session19.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

Писался по ходу смены и коммитился в момент написания (К-30). **Смена прошла в режиме постоянных
обрывов сессии сетевой ошибкой API** — это не редкость, а фон; всё несущее коммитилось раньше
обычного именно поэтому, и тебе стоит делать так же.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов**, называй число явно в каждом промпте. Пункт 9 ссылается на
  `_INSURANCE_PATHS`, которого здесь НЕТ (эквивалент — `ruff check src/ tests/` плюс полный набор);
  пункт 6 закрывают два серверных алярма; **пункт 7 умеет быть закрытым НЕВЫПОЛНЕНИЕМ** — и в эту
  смену это случилось дважды (CB-266 обязана была остаться `open` до моего вердикта; у Т-126 карты
  НЕТ вовсе, это задача владельца, прецедент Т-104).
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §9 неизменяемые факты
  приёмки (**блок сессии 20 в начале**), §10 эскалации (**Э-35 в начале, ОТКРЫТА**), §11 журнал
  (смена 20 в конце), §13 шаблон брифа.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`, читать вместе с §13 пп. 18–20.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. **Аллокатор ID:**
  `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| **Т-125 — CB-266**: алярм `main-invariants` горел на честной истории | `327590d` | **ПРИНЯТ С ОГОВОРКАМИ** двумя проходами, **CB-266 закрыта** |
| **Т-126 — задача владельца**: сжать `CLAUDE.md` | `2eceaec` | приземлён; **цель НЕ достигнута и это измеренный законный исход** — см. §2 п. 3 |
| CB-270, CB-271 | — | заведены как остатки Т-125 (нашёл второй проход приёмки) |
| CB-272 | — | заведена исполнителем Т-126: четыре места, где `CLAUDE.md` лжёт о своей структуре |
| Э-35 | — | эскалация наверх: **предпосылка задачи владельца не подтвердилась** |

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
   **Попадание проверяй `git merge-base --is-ancestor`, НИКОГДА текстом и никогда порядком
   коммитов** — я на этом попался лично: сторож, гревший `grep -i cb-266` по журналу, нашёл мои
   собственные коммиты и отрапортовал посадку, которой не было.
2. **Приёмка Т-126 БЫЛА В ПОЛЁТЕ на момент написания этой строки — перепроверь, вынесен ли
   вердикт**, и если нет, дождись возврата приёмщика и вынеси сам. Запись идёт в §9 артефакта.
   Долгов приёмки, кроме этого, нет.
3. **Заморозка `CLAUDE.md` СНЯТА** — она держалась, пока Т-126 был в работе, и он приземлился.
4. **Голова очереди — решение владельца по Э-35, а не работа.** Пока формы не выбрали, не начинай
   ни сокращения, ни разнесения по вложенным `CLAUDE.md`.

## 3. Что преемник обязан знать до того, как начнёт

1. **ПРЕДПОСЫЛКА ЗАДАЧИ ВЛАДЕЛЬЦА НЕ ПОДТВЕРДИЛАСЬ, И ЭТО ГЛАВНЫЙ РЕЗУЛЬТАТ СМЕНЫ.** Задача
   ставилась от вреда «нормативный текст доезжает усечённым». **Усечения нет**: при 232 326 байтах
   файл вкладывается в сессию целиком, проверено двумя независимыми способами. **Числа 150 000 нет
   ни в одном файле репозитория.** Зато найден другой дефект того же семейства: сессия получает
   **устаревший СНИМОК** — инжектируемая копия берётся раз при старте и не обновляется. У меня
   самого в контексте всю смену лежала редакция без CB-266 при свежем файле на диске.
   **Сокращение файла этого не лечит вовсе.** Подробности и три формы — Э-35.
2. **КОРИДОР НЕДОСТИЖИМ, И ЭТО ЗАМЕР, А НЕ ОЦЕНКА.** 1 456 несущих утверждений по 159 знаков,
   **96,3 % в классах, запрещённых к резке**; пять ПРИЗЕМЛЁННЫХ калибровочных правок дают 4,2 %;
   межразделового дубля нет (275 байт). Достижимо ~220 000 против требуемых 120 000–135 000.
   **Исполнитель предъявил замер вместо результата — это ровно то, что бриф объявлял законным
   исходом, и это правильное поведение, а не провал.** Инвентарь, ведомость и оракул множеств
   приземлены и переживут любое решение о форме.
3. **К-28 ОКУПИЛСЯ ВТОРОЙ РАЗ ПОДРЯД, И СНОВА ИМЕННО РАЗНОЙ АПЕРТУРОЙ.** На Т-125 первый проход
   закрыл восемь пунктов из девяти и остатка не нашёл; второй, которому был задан ОДИН вопрос —
   «не шире ли обещание механизма, по маршруту, которого никто не перечислял», — принёс CB-270 и
   CB-271. **Заказывая второй проход, меняй апертуру, а не исполнителя**, и гоняй проходы
   ПОСЛЕДОВАТЕЛЬНО, каждый в своей копии.
4. **КРОСС-МОДЕЛЬНОЕ РЕВЮ БРИФА ДО ИСПОЛНЕНИЯ ОКУПИЛОСЬ ДОРОГО.** По стоячему правилу владельца
   (план есть, исполнение не начато) я заказал его на бриф Т-126: **14 находок, 3 фатальные**, и
   оно стоило нуля времени, потому что юнит всё равно ждал посадки соседа. Оно убило мой довод о
   выполнимости (73,6 % оказались 4,23 %) и нашло в моей же машинной половине **два гейта, которые
   не могут выстрелить**, одним из которых я обосновывал снятие требования. Заказывай его.
5. **ФИНИШ ТОЛЬКО ПО СЛОВУ ДЕРЖАТЕЛЯ, ОБЪЯВЛЕННОМУ ЗАРАНЕЕ** — дисциплина смены 19, продлена, и
   снова окупилась. Побочная выгода та же: ты точно знаешь, когда `main` неподвижен.
   **И следствие, которое я узнал дорого: пока чей-то финиш идёт, твой грязный файл в главном
   чекауте отказывает ему кодом 11, который повтором НЕ лечится**, тогда как код 13 лечится
   буквальным повтором. Поэтому коммить свои заметки ДО того, как дал слово на финиш.

## 4. Очередь преемнику

**0. РЕШЕНИЕ ВЛАДЕЛЬЦА ПО Э-35** — не работа, но голова очереди: пока форма не выбрана, задача
владельца не движется.

**1. CB-272** (`medium`, завела смена 20) — четыре места, где `CLAUDE.md` лжёт о собственной
структуре: «Four rules» при пяти, ссылка «above» указывающая вниз, «the rule immediately above»
лежащее на 167 строк ниже, и утверждение про `74` с «same CLI ownership» при нуле вхождений в
разделе `### CLI`. Последнее коварно: соседнее предложение той же формы про `141` истинно, и
параллель делает ложь невидимой.

**2. CB-271** (`medium`) — четыре описания допуска остались при старой букве после CB-266, и
худшие два **печатает оператору сам `install-hooks.sh` при каждом вооружении клона**. Дословно
форма CB-264. Дёшево и структурно.

**3. CB-270** (`medium`) — буква допуска судит ПУТЬ и никогда РОД ОБЪЕКТА: символическая ссылка,
подмодуль и исполняемый бит ложатся под именем, которое допуск читает как дневной бриф. **Класс
УНАСЛЕДОВАН** — те же три проходят и по старой `.md`-ветви, — но приземлённая фраза «widens by
exactly that one shape and no wider» верна только про форму пути. Скорее всего чинится ПРОЗОЙ.

**4. CB-264** (`medium`, юнит **Т-127 заминчен и НЕ выдан**) — описание механизма `# noqa: S608`
неверно в трёх местах. **Пассаж оставлен Т-126 БАЙТ-ИДЕНТИЧНЫМ намеренно**, чтобы Т-127 правил его
целиком. Часть 1 имеет детерминированный оракул; части 2 и 3 машинного оракула не имеют.
**Мой замер сверх карты: число мёртвых маркеров НЕ ЕДИНСТВЕННОЕ** — 32 при включённом `S608` и 48
при одном `RUF100` по `src/`, 91 по `src/ tests/`, тогда как карта называет 54. Расхождение обязан
развести исполнитель, **а не объявить карту неправой**.

**5. CB-255** (`medium`) — форма выбрана держателем смены 19 и НЕ та, что предлагал исполнитель;
три предпосылки юнит обязан ПОСТРОИТЬ. **Я намеренно НЕ пускал её параллельно с Т-126**: её предмет
— алярм движения дерева в `tests/conftest.py`, ровно тот механизм, срабатывания которого бриф
Т-126 велел исполнителю ожидать. Теперь она свободна.

**6–10.** CB-252 (`low`), CB-250 (`low`), CB-253 (`low`), CB-240 (`low`), CB-167 (`low`, живёт в
`findings.py` — территория DIR-2, перед выдачей проверь, не работает ли сосед в этом файле),
CB-187 (`medium`, ливлок; форма выбрана в карте).

## 5. Скоропортящееся — перепроверить на месте

`main == origin/main == 696e45d` на момент записи; рабочих деревьев моего направления нет.
**Алярм `main-invariants` ЗЕЛЁН** — падал шесть пушей подряд и прошёл на первом после посадки
Т-125. `CLAUDE.md` на `main` — 230 413 байт.

**DIR-2 работает параллельно и коммитит на `main` постоянно.** Её деревья в `.worktrees/` не твои.
**Файл настроек владельца `.claude/settings.json` и его резервная копия — не трогать**, они
untracked и посадке не мешают.

**Ни одна ветка никогда не удаляется.** `feature/t49-bt6-third-pilot-annotations` — запаркованный
пилот, решение владельца.

## 6. Инструментальное — ТРИ ЛОВУШКИ, ДАЮЩИЕ ЛОЖНЫЙ НОЛЬ

**Общее правило, вменённое мной себе по ходу смены и заслуживающее стать каноном: команда,
вернувшая ноль или пустоту, НЕ ЯВЛЯЕТСЯ ЗАМЕРОМ, пока не показано, что она вообще выполнилась** —
код возврата, версия инструмента, непустой `stderr`. Три моих промаха этой смены не пережили бы
этой проверки, и ни один не выглядел ошибкой.

- **`./.venv/bin/ruff` НЕ СУЩЕСТВУЕТ.** Прошлая передача советовала его для разовых замеров; в
  `.venv/bin` лежат `python`, `codebugs` и ещё несколько, ruff среди них нет. Рабочая форма —
  `uv run --extra dev ruff`, версия `0.15.7`.
- **Незакавыченный `--include=*.py` под `zsh` ОБРЫВАЕТ `grep` ЦЕЛИКОМ.** Оболочка не находит
  шаблон и прекращает команду. Пиши `--include='*.py'`.
- **Сторож посадки, гревший текст, поймал мои же коммиты.** Проверяй `git merge-base
  --is-ancestor <ветка> main`, никогда `grep` по журналу.
- **Приёмщикам называй ИМЕНА тестовых классов**, а не только файлы: исполнитель Т-126 получил
  `no tests ran` — ложный ноль ровно того же рода.
- **Приёмщиков гоняй в их СОБСТВЕННЫХ копиях** (CB-258). И знай обратную сторону: у приёмщика
  Т-125 обе копии удалил посторонний процесс в этом же клоне прямо во время работы.
- **Минт заводит НОВУЮ категорию только с `new_category=True`.** Брал существующую
  `contract_mismatch`.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**

## 7. Записи против меня

Их шесть, и **пять — одной формы**, которой у прошлой смены не было: не «утверждение шире замера»,
а **«замера не было вовсе, а результат я записал»**. Три из пяти дали ложный ноль.

1. **Пустой вывод несуществующего `./.venv/bin/ruff` принят за «ноль срабатываний»** и едва не
   уехал в бриф как замер. Поймал случайно — заметил, что `--version` ничего не напечатал.
2. **Незакавыченный `--include=*.py` оборвал `grep`**, и я «установил», что ни один тест не пинует
   выражение допуска. Опроверг исполнитель: `tests/test_worktree_harness.py:1598` пинует копию из
   workflow. **Следствие оказалось в пользу юнита**: защита была не нулевой, а частичной — одна
   копия из трёх, — то есть вменённый мной гейт обоснован, но по более узкой причине.
3. **Предположение о поведении теста, записанное в раздел «что я измерил лично»** — при том что в
   §4 того же брифа я честно пометил его непроверенным. Бриф спорил сам с собой через две
   страницы. Опровергло кросс-модельное ревю.
4. **Правка брифа без перемера чисел, устаревших под ним** после чужого мержа: перемерил ровно тот
   пункт, который правил, оставив соседние пять. Нашёл противник.
5. **Тот же дефект повторился через час** — номера строк пинов (5734/5751/5766) я перенёс из
   замера, сделанного до посадки Т-125, в редакцию, которую перемерял после. Верные — 5995/6012/6027,
   назвал исполнитель. **Один и тот же промах, дважды за смену, после того как я его записал.**
6. **Сторож посадки, отрапортовавший «LANDED» по совпадению с моими собственными коммитами.**
   Поймал только потому, что прочёл вывод, а не поверил слову.

**Что из этого следует для тебя.** Прошлая смена записала: охотник за утверждениями шире замера
приобретает склонность утверждать, что чужие утверждения шире замера. Моя добавляет соседнюю форму:
**охотник за «гейтом, который не может сработать», сам пользуется командами, которые не могут
сработать, и читает их молчание как ответ.** Дважды за смену исполнители опровергали мои числа, и
**оба раза правы были они** — это не случайность, а следствие того, что бриф есть данные, а не
команда, и К-12 работает ровно тогда, когда исполнителю сказано, что опровергать разрешено.
