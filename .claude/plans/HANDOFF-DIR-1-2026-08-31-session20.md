# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-31, смена 20

(вытесняет `HANDOFF-DIR-1-2026-08-29-session19.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

Писался по ходу смены и коммитился в момент написания (К-30). **Смена прошла в режиме постоянных
обрывов сессии сетевой ошибкой API** — это фон, а не редкость; всё несущее коммитилось раньше
обычного именно поэтому, и тебе стоит делать так же.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов**, называй число явно в каждом промпте. Пункт 9 ссылается на
  `_INSURANCE_PATHS`, которого здесь НЕТ (эквивалент — `ruff check src/ tests/` плюс полный набор);
  пункт 6 закрывают два серверных алярма; **пункт 7 умеет быть закрытым НЕВЫПОЛНЕНИЕМ** — в эту
  смену это случилось дважды: CB-266 обязана была остаться `open` до моего вердикта, а у Т-126
  карты НЕТ вовсе (задача владельца, прецедент Т-104).
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §9 неизменяемые факты
  приёмки (**блок сессии 20 в начале: Т-126, затем Т-125**), §10 эскалации (**Э-35 в начале**),
  §11 журнал (смена 20 в конце), §13 шаблон брифа (пп. 18–20 несущие).
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. **Аллокатор ID:**
  `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| **Т-125 — CB-266**: алярм `main-invariants` горел на честной истории | `327590d` | **ПРИНЯТ С ОГОВОРКАМИ** двумя проходами, **CB-266 закрыта**, алярм зелен |
| **Т-126 — задача владельца**: сжать `CLAUDE.md` | `2eceaec` | **ПРИНЯТ С ОГОВОРКАМИ**; цель НЕ достигнута, и это измеренный законный исход |
| **Э-35** | — | эскалация: **предпосылка задачи владельца не подтвердилась**; владелец задачу ПЕРЕФОРМУЛИРОВАЛ |
| **Т-129** — новая формулировка задачи владельца | — | заминчен, бриф написан, **ИСПОЛНИТЕЛЬ ЗАПУЩЕН И РАБОТАЕТ** |
| CB-270, CB-271 | — | остатки Т-125, нашёл второй проход приёмки |
| CB-272 | — | завёл исполнитель Т-126: `CLAUDE.md` лжёт о своей структуре в четырёх местах |
| CB-273 | — | свежесть вложенной копии `CLAUDE.md` не гарантирована и **не наблюдаема** |

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь состояние с `git log --first-parent` и `git worktree list`.** Уведомлению не верь.
   **Попадание проверяй `git merge-base --is-ancestor`, НИКОГДА текстом и никогда порядком
   коммитов** — я на этом попался: сторож, гревший `grep -i cb-266` по журналу, нашёл мои
   собственные коммиты и отрапортовал посадку, которой не было.
2. **Т-129 БЫЛ В ПОЛЁТЕ на момент записи.** Первая его работа — **КАЛИБРОВКА**, и он обязан
   вернуться с числами ДО того, как возьмётся за остальной документ. **Финиш только по твоему
   явному слову** — я объявил ему это заранее.
3. **Долгов приёмки нет.** Оба юнита смены приняты, оба факта записаны в §9.
4. **Заморозка `CLAUDE.md` ДЕЙСТВУЕТ и переехала на Т-129** — до его посадки файла не трогает
   никто.

## 3. Что преемник обязан знать до того, как начнёт

1. **ЗАДАЧА ВЛАДЕЛЬЦА ПЕРЕФОРМУЛИРОВАНА, И ЭТО ГЛАВНОЕ СОБЫТИЕ СМЕНЫ.** Прежняя цель («сжать ниже
   150 000 знаков, потому что текст доезжает усечённым») **снята как опровергнутая**: усечения
   нет, файл вкладывается целиком, числа 150 000 нет ни в одном файле репозитория. Новая цель —
   **слой правил отдельно от архива обоснований**, обоснование **расходом**: впрыск ≈230 КБ
   (≈58 тыс. токенов) входит в каждую сессию и каждого субагента до первой строки работы.
   **Первая работа Т-129 — калибровка, и «гипотеза неверна» объявлена законным исходом.**
2. **КОРИДОР НЕДОСТИЖИМ, И ЭТОТ ВЫВОД УСТОЯЛ ПОД НЕЗАВИСИМЫМ ПЕРЕСЧЁТОМ.** 1 456 несущих
   утверждений по 159 знаков, **96,3 % в классах, запрещённых к резке**; приёмщик классифицировал
   20 утверждений сам и получил **95 %** — щедрее, а не строже. Даже по его прочтению потолок
   ≈12,5 % (≈203 000 байт) против требуемых 120 000–135 000.
   **НО: первичное свидетельство под этим замером в дерево НЕ ПРИЗЕМЛИЛОСЬ.**
   `T126-baseline.json` — снимок ИДЕНТИФИКАТОРОВ (`bytes`, `chars`, `cards`, `cyrillic_T`,
   `backticks`, `tests`, `paths`), а не инвентарь доктрины; инвентарь остался в `inv-01…inv-07`
   вне ветки. **Я описал этот файл неверно и в Э-35, и в брифе Т-129** — обе записи исправлены,
   Т-129 вменено построить инвентарь заново И ПРИЗЕМЛИТЬ.
3. **К-28 ОКУПИЛСЯ ВТОРОЙ РАЗ ПОДРЯД, И СНОВА ИМЕННО РАЗНОЙ АПЕРТУРОЙ.** На Т-125 первый проход
   закрыл восемь пунктов и остатка не нашёл; второй, которому был задан ОДИН вопрос — «не шире ли
   обещание механизма, по маршруту, которого никто не перечислял», — принёс CB-270 и CB-271.
   **Меняй апертуру, а не исполнителя**; проходы последовательно, каждый в своей копии.
4. **КРОСС-МОДЕЛЬНОЕ РЕВЮ БРИФА ДО ИСПОЛНЕНИЯ ОКУПИЛОСЬ ДОРОГО.** По стоячему правилу владельца
   (план есть, исполнение не начато) я заказал его на бриф Т-126: **14 находок, 3 фатальные**, и
   оно стоило нуля времени, потому что юнит всё равно ждал соседа. Оно убило мой довод о
   выполнимости (73,6 % оказались 4,23 %) и нашло в моей же машинной половине **два гейта, которые
   не могут выстрелить**, одним из которых я обосновывал снятие требования. Заказывай.
5. **ФИНИШ ТОЛЬКО ПО СЛОВУ ДЕРЖАТЕЛЯ, ОБЪЯВЛЕННОМУ ЗАРАНЕЕ.** Продлено с прошлой смены, окупилось
   снова. **Следствие, узнанное дорого: пока чей-то финиш идёт, твой грязный файл в главном
   чекауте отказывает ему кодом 11, который повтором НЕ лечится**, тогда как 13 лечится буквальным
   повтором. Коммить свои заметки ДО того, как дал слово на финиш.
6. **ГЛАВНЫЙ ЧЕКАУТ ДЕЛИТ ИНДЕКС С СОСЕДНИМ НАПРАВЛЕНИЕМ, И ХУК ЭТО ЛОВИТ.** В конце смены мой
   коммит был отвергнут `commit-msg`-хуком: в индексе лежал **чужой** файл
   (`HANDOFF-DIR-2-…s19.md`), которого я не добавлял, — DIR-2 работает в том же дереве.
   **Лечится не `--no-verify`, а коммитом по путям:** `git commit -m '…' -- <мои файлы>`. Это
   строго лучше, чем `git restore --staged` чужого файла: не трогает чужую работу вовсе.
   **Заметь иронию, она поучительна: это тот самый хук, который расширил Т-125, и он поймал своего
   же автора на том самом дефекте провенанса, ради которого написан.**

## 4. Очередь преемнику

**1. Т-129 (в полёте)** — надзор, приём калибровочных чисел, слово на финиш. Голова очереди.

**2. CB-272** (`medium`) — четыре места, где `CLAUDE.md` лжёт о собственной структуре: «Four
rules» при пяти; ссылка «above», указывающая вниз; «the rule immediately above» на 167 строк ниже;
и утверждение про `74` с «same CLI ownership» при нуле вхождений в разделе `### CLI`. Последнее
коварно: соседнее предложение той же формы про `141` истинно, и параллель делает ложь невидимой.

**3. CB-271** (`medium`) — четыре описания допуска остались при старой букве после CB-266, и
худшие два **печатает оператору сам `install-hooks.sh` при каждом вооружении клона**. Дословно
форма CB-264, и возникла так же: шесть описаний приведены к факту, седьмое пропущено.

**4. CB-273** (`medium`) — свежесть вложенной копии `CLAUDE.md` не гарантирована И НЕ НАБЛЮДАЕМА.
**Формулировка намеренно у́же первой:** измерено И что снимок может быть устаревшим часами (мой
собственный контекст всю смену), И что он может обновиться посреди сессии (наблюдение исполнителя
Т-126); **что решает — НЕ диагностировано**. Это наблюдение уровня харнеса, и починка может лежать
вне этого репозитория.

**5. CB-270** (`medium`) — буква допуска судит ПУТЬ и никогда РОД ОБЪЕКТА: символическая ссылка,
подмодуль и исполняемый бит ложатся под именем, которое допуск читает как дневной бриф. **Класс
УНАСЛЕДОВАН** (те же три проходят и по старой `.md`-ветви), но приземлённая фраза «widens by
exactly that one shape and no wider» верна только про форму пути. Скорее всего чинится ПРОЗОЙ.

**6. CB-264** (`medium`, юнит **Т-127 заминчен и НЕ выдан**) — описание механизма `# noqa: S608`
неверно в трёх местах. **Пассаж оставлен Т-126 БАЙТ-ИДЕНТИЧНЫМ намеренно** (проверено приёмщиком
по MD5), чтобы Т-127 правил его целиком. **Мой замер сверх карты: число мёртвых маркеров НЕ
ЕДИНСТВЕННОЕ** — 32 при включённом `S608` и 48 при одном `RUF100` по `src/`, 91 по `src/ tests/`,
тогда как карта называет 54. Расхождение обязан развести исполнитель, **а не объявить карту
неправой**.

**7. CB-255** (`medium`) — форма выбрана держателем смены 19 и НЕ та, что предлагал исполнитель.
**Я намеренно НЕ пускал её параллельно с Т-126**: её предмет — алярм движения дерева в
`tests/conftest.py`, ровно тот механизм, срабатывания которого бриф Т-126 велел ожидать. **Тот же
довод действует и против запуска рядом с Т-129.**

**8–12.** CB-252 (`low`), CB-250 (`low`), CB-253 (`low`), CB-240 (`low`), CB-167 (`low`, живёт в
`findings.py` — территория DIR-2, перед выдачей проверь, не работает ли сосед в этом файле),
CB-187 (`medium`, ливлок; форма выбрана в карте).

## 5. Скоропортящееся — перепроверить на месте

`main == origin/main == e78c41f` на момент записи. **Алярм `main-invariants` ЗЕЛЁН** (падал шесть
пушей подряд, прошёл на первом после посадки Т-125). `CLAUDE.md` на `main` — **230 413 байт**.
Живо **одно** рабочее дерево моего направления — Т-129.

**DIR-2 работает параллельно, коммитит на `main` постоянно и делит с тобой индекс главного
чекаута** (§3 п. 6). Её деревья в `.worktrees/` не твои. **Файл настроек владельца
`.claude/settings.json` и его копия — не трогать.**

**Ни одна ветка никогда не удаляется.** `feature/t49-bt6-third-pilot-annotations` — запаркованный
пилот, решение владельца.

## 6. Инструментальное — ЧЕТЫРЕ ЛОВУШКИ, ДАЮЩИЕ ЛОЖНЫЙ НОЛЬ

**Правило, вменённое мной себе по ходу смены и заслуживающее стать каноном: команда, вернувшая
ноль или пустоту, НЕ ЯВЛЯЕТСЯ ЗАМЕРОМ, пока не показано, что она вообще выполнилась** — код
возврата, версия инструмента, непустой вывод. Ни один из моих промахов не пережил бы этой проверки,
и ни один не выглядел ошибкой.

- **`./.venv/bin/ruff` НЕ СУЩЕСТВУЕТ** (прошлая передача советовала его). Рабочая форма —
  `uv run --extra dev ruff`, версия `0.15.7`.
- **Незакавыченный `--include=*.py` под `zsh` ОБРЫВАЕТ `grep` ЦЕЛИКОМ.** Пиши `--include='*.py'`.
- **Прогон по именам ФАЙЛОВ вместо имён КЛАССОВ** дал исполнителю `no tests ran`, прочитанное как
  зелень. **Приёмщикам и исполнителям называй имена классов**: здесь это
  `TestPostMergeAlarmIsNotAGate` и `…::test_the_code_is_declared_in_claude_md`.
- **Сторож посадки, гревший текст, поймал мои же коммиты** и отрапортовал «LANDED».
- **Приёмщиков гоняй в их СОБСТВЕННЫХ копиях** (CB-258). Обратная сторона: у приёмщика Т-125 обе
  копии удалил посторонний процесс в этом же клоне прямо во время работы.
- **`Edit` у приёмщика бывает отключён.** Ратифицированный обход — мутация перепривязкой объекта в
  памяти с доказательством тождества; оба приёмщика смены так и сделали. «Инструмент недоступен»
  не есть BLOCKED.
- **Минт заводит НОВУЮ категорию только с `new_category=True`.**
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**

## 7. Записи против меня

Их восемь, и **шесть — одной формы**, которой у прошлой смены не было: не «утверждение шире
замера», а **«замера не было вовсе, а результат я записал»**. Четыре дали ложный ноль.

1. **Пустой вывод несуществующего `./.venv/bin/ruff` принят за «ноль срабатываний»** и едва не
   уехал в бриф как замер. Поймал случайно.
2. **Незакавыченный `--include=*.py` оборвал `grep`**, и я «установил», что ни один тест не пинует
   выражение допуска. Опроверг исполнитель: `tests/test_worktree_harness.py:1598`. Следствие
   оказалось в пользу юнита: защита была не нулевой, а частичной — одна копия из трёх.
3. **Предположение о поведении теста, записанное в раздел «что я измерил лично»** — при том что в
   §4 того же брифа я честно пометил его непроверенным. Бриф спорил сам с собой через две страницы.
4. **Правка брифа без перемера чисел, устаревших под ним** после чужого мержа.
5. **Тот же дефект повторился через час:** номера строк пинов перенёс из замера до посадки Т-125.
   **Один и тот же промах дважды за смену, ПОСЛЕ того как я его записал.**
6. **Сторож посадки, отрапортовавший «LANDED» по совпадению с моими же коммитами.**
7. **В Э-35 — эскалации, которую читает владелец, — я вывел МЕХАНИЗМ из одного исхода:** «снимок
   берётся раз при старте и не обновляется». Опровергнуто наблюдением исполнителя Т-126: харнес
   перевложил файл посреди живой сессии. Опровержение оставлено в тексте видимым.
8. **Я назвал `T126-baseline.json` инвентарём доктрины в двух артефактах сразу** — в Э-35 и в
   брифе Т-129, — повторив утверждение чужого отчёта как собственное измерение. Бриф при этом уже
   был выдан исполнителю и указывал ему на несуществующий файл; поправку послал отдельным
   сообщением по К-29, оба артефакта исправлены.

**Что из этого следует для тебя.** Прошлая смена записала: охотник за утверждениями шире замера
приобретает склонность утверждать, что чужие утверждения шире замера. Моя добавляет соседнюю
форму: **охотник за «гейтом, который не может сработать», сам пользуется командами, которые не
могут сработать, и читает их молчание как ответ.** И третью, из записи 8: **чужой отчёт, повторённый
без проверки, становится твоим утверждением и наследует твой авторитет.** За смену исполнители и
приёмщики опровергли **шесть** моих чисел, и **каждый раз правы были они** — это не случайность, а
следствие того, что бриф есть данные, и К-12 работает ровно тогда, когда исполнителю сказано, что
опровергать разрешено.
