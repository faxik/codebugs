# HANDOFF — куратор codebugs, 2026-08-28 (вытесняет: HANDOFF-curator-2026-08-27.md)

## 0. Нормативные входы (указатели, НЕ пересказ)
- Роль: `~/.claude/skills/pain-curator/SKILL.md`; ротация: `~/.claude/skills/role-rotate/SKILL.md`
- Спека: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  §4–§5, §8, §13 (девять пунктов), §16.3, §17
- Приёмщик: `~/.claude/agents/acceptor.md`, `~/.claude/skills/acceptance-check/SKILL.md`
- Память: MEMORY.md → curator-autodispatch, relay-needs-translation, no-direct-sql-use-mcp,
  smart-approve-permission-stack, resume-dead-agents-by-message (добавлена 28.08)
- Артефакт уровня: `.claude/plans/PAIN-REGISTRY-codebugs.md` — источник истины по ратификациям,
  приёмкам, канонам К-1…К-27 и Решениям 1–4 от 28.08; порядок работ — req `PAIN-ORDER` в трекере

## 1. Сессионное состояние (скоропортящееся — перепроверить, не верить)
- main @`3558dca` == origin на момент записи (двигался всю ночь; сверь `git log --first-parent`).
- Релиз 0.2.2 стоит; после него на main — только плановые заметки и мержи направлений.
- **Живые агенты этой сессии умрут вместе с ней**: держатель DIR-1 (смена 18, стартовала ~21:00)
  и держатель DIR-2 (смена 17, стартовала ~23:40), у каждого возможны исполнители. Их журналы
  коммитятся после каждого шага, поэтому преемник держателя стартует из handoff + артефакта
  направления, как после любого обрыва. Если эту сессию закрывают до их возвратов — преемник
  куратора запускает обе смены заново из их PROMPT-файлов, предварительно сверив по `git log`
  и `git worktree list`, что сели и что стоит в копиях.
- Копии на момент записи: `.worktrees/fix-cb-158-explicit-limit-wins` (Т-116 DIR-2, сделан, не
  приземлён, отказ посадки записан в handoff DIR-2 s16) и
  `.worktrees/fix-cb-249-cb-244-harness-signal-and-gate-state` (Т-118 DIR-1, готов на `b72d609`,
  ждал коммита журнала держателя, `exit 11`). Т-117 DIR-1 сел `4fead6c`, приёмка у DIR-1.
- Т-116 сел `ba89587` (принят с оговоркой → CB-257), Т-118 сел `5bb8295` (приёмка у DIR-1). Обе копии
  сняты финишем. **DIR-2 ведёт смена 17; держательница смены 16 остановлена куратором** после того,
  как оказалась живой (реестр, запись «ДВА ОКНА ТИШИНЫ…», против куратора) — не возобновлять её.
- Открытых карт ~52, critical/high — ноль. Клеймов — только у живых веток.
- API за вечер 28.08 обрывал агентов восемь раз (держателей и исполнителей, всегда посреди шага).
  Работа не терялась ни разу. Механика возобновления — память `resume-dead-agents-by-message`.
- `.claude/settings.json` и его копия — владельца, не трогать. Устаревший `.claude/worktrees/` пуст.

## 2. Первые действия преемника (по порядку)
1. Сверить §0 с источниками; расхождение — чинить handoff, не источник.
2. Прочитать в реестре записи от «СЕССИЯ КУРАТОРА 2026-08-28» до конца — там Решения 1–4, К-27,
   приёмки смен 17 DIR-1 и 16 DIR-2 и кому что передано.
3. Сверить живость держателей по времени коммитов их направлений (К-23), не по тишине; если их
   нет — запустить смены из `PROMPT-DIR-1-next-session.md` (смена 18) и
   `PROMPT-DIR-2-next-session.md` (смена 17) фоном, пин opus, с сессионными фактами из §1.
4. Принимать возвраты держателей: спот-чек SHA (`merge-base --is-ancestor … origin/main`), факт
   приёмки в реестр, следующая смена из пересобранного промпта.
5. Привести норматив приёмщика к решению «проба определяется эффектом, не инструментом»
   (реестр, находка (б) в записи «ДВА ОКНА ТИШИНЫ…»): `~/.claude/agents/acceptor.md`,
   `~/.claude/skills/acceptance-check/SKILL.md`.
6. Если владелец ответил по батчу (§3) — записать ратификацию в реестр и развернуть в задачу
   направлению тройкой П-Б; иначе не поднимать повторно.

## 3. Открытое для владельца (батч, указатель)
Реестр, записи «РЕШЕНИЕ 2» и «ПОПРАВКА К РЕШЕНИЮ 2»: бюджет на сквозной перечит нормативных текстов
(две смены, 347 кандидатов). Из handoff 27.08 §3 без изменений: карта слияния категорий
(`CATEGORY-MERGE-CANDIDATES-2026-08-26.md`), подключение вставки к шести проектам
(`INSTRUCTION-simplify-gate-2026-08-27.md` §1–2). Развилка CB-62/CB-65 — под CB-65 в `PAIN-ORDER` п.6. **Новое 28.08 ночью:** вопрос формы по ливлоку
посадки (реестр, «ДВА ОКНА ТИШИНЫ…») и подпись коммитов с номером смены.

## 4. Наблюдения процесса
- Возвраты исполнителей уровня (4) приходят куратору, когда их держатель мёртв или только что
  возобновлён; куратор передаёт держателю УКАЗАТЕЛЬ (SHA, что требует решения), не пересказ, и
  не принимает юнит сам.
- Оба держателя оставили сводную строку Решения 1 друг другу — её написал куратор; сводные строки
  по решениям уровня (1) пишет уровень (1), не ждёт снизу.
- «Посадка и отправка на origin — два разных действия» — второй случай за вечер (реестр,
  Решение 4); третий — механизм, не напоминание.
- Предупреждающая вставка ключуется на подстроке всей командной строки, включая heredoc, —
  измерено на коммите куратора (реестр, запись после Решения 4), передано DIR-1 к CB-244.
