# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-29, смена 19

(вытесняет `HANDOFF-DIR-1-2026-08-29-session18.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

Писался по ходу смены и коммитился в момент написания (К-30). **Законченный документ перечитан
ЦЕЛИКОМ как один текст** — что нашёл сквозной перечит, сказано в конце §7.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов.** Файлы приёмщика местами говорят «семь»: называй число
  явно в каждом промпте. **Два пункта здесь означают не то, что написано:** пункт 9 ссылается на
  набор `_INSURANCE_PATHS`, которого здесь НЕТ (он из соседнего проекта) — эквивалент `ruff check
  src/ tests/` плюс полный набор; пункт 6 закрывают два серверных алярма. **Пункт 7 умеет быть
  закрытым НЕВЫПОЛНЕНИЕМ** — см. §3 п. 2.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий; **Releasing** —
  для срезов; **Testing** — правила тестов на конкуренцию и жильцы `tests/conftest.py`.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §5 таблица юнитов
  (**её граница названа словами: велась по 22 августа, индексом текущего состояния не является**),
  §6 ратифицированные расхождения, §9 неизменяемые факты приёмки (**блок сессии 19 в начале**),
  §10 эскалации, §11 журнал (смена 19 в конце), §13 шаблон брифа — **пункт 20 заработан этой
  сменой**, §14 наблюдение про частоту движения `main`.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md`. Читай вместе с §13, особенно
  пп. 18–20.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. Пронумерованные каноны и РЕШЕНИЯ
  куратора живут там. **Аллокатор ID:** `tools/cascade-mint.sh --help`; реестр
  `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| Т-120 — CB-172 вариант (1): шесть мёртвых маркеров `S608` и правда `CLAUDE.md` | `3e52280` | **ПРИНЯТ С ОГОВОРКОЙ** двумя проходами; обе оговорки первого прохода закрыты мной в тот же час |
| Т-122 — CB-258: пробный каталог ушёл из живого дерева | `db53391` | **ПРИНЯТ БЕЗ ОГОВОРОК**, села с первой попытки, **CB-258 закрыта** |
| CB-172 | — | перенацелена на вариант (2), запись в карте; цена долга перемерена и исправлена |
| CB-255 | — | **форма выбрана мной**, и не та, что предлагал исполнитель — §4 п. 1 |
| CB-264 | — | заведена: три места, где описание механизма `S608` осталось неверным ПОСЛЕ Т-120 |
| CB-253 | — | пересчитана; **и туда же записано моё ложное обвинение в её адрес** (§7 п. 2) |
| §13 п. 20 | — | новый пункт шаблона: юнит обязан применить свой критерий к своему же артефакту |

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь очередь с `git log --first-parent` и `git worktree list`.** Уведомлению не верь никогда.
2. **Долгов приёмки нет.** Оба юнита приняты, оба факта записаны в §9, CB-258 закрыта, CB-172
   намеренно оставлена открытой (это не долг — см. §3 п. 2).
3. **Прочитай §3 целиком до первого действия.** Там четыре вещи, каждая из которых стоила
   времени в эту смену или предотвратила его трату.
4. **Голова очереди — CB-264, и это структура, а не деталь.** Ложное утверждение в `CLAUDE.md`,
   которое занижает объём будущей работы примерно вдвое, и приземлил его МОЙ юнит.

## 3. Что преемник обязан знать до того, как начнёт

1. **К-28 ОКУПИЛСЯ В ПЕРВЫЙ ЖЕ РАЗ, КОГДА БЫЛ ПРИМЕНЁН, И ОКУПИЛСЯ ИМЕННО РАЗНОЙ АПЕРТУРОЙ.**
   Я заказал на Т-120 два прохода приёмки. Первый — девять пунктов. Второй — один вопрос: «правда
   ли переписанный абзац `CLAUDE.md` целиком, как ОДИН текст, против дерева». Копия первого прохода
   дала бы те же механические проверки и не нашла бы ничего. Второй нашёл **ложь, которую мой же
   юнит и добавил**, и ещё две находки. **Заказывая второй проход, меняй апертуру, а не исполнителя.**
2. **ПУНКТ 7 КОНТРАКТА УМЕЕТ БЫТЬ ЗАКРЫТЫМ НЕВЫПОЛНЕНИЕМ, И ЭТО НАДО ПИСАТЬ В ПРОМПТ ПРИЁМЩИКА.**
   CB-172 обязана была остаться `open`: решение куратора разделило её на исполненный вариант (1) и
   вариант (2) — принятый долг, а карта есть единственный носитель этого решения. Приёмщик, не
   предупреждённый об этом, засчитал бы незакрытую карту как долг. Проверять надо не терминальность,
   а что статус именно `open`, а не `in_progress`.
3. **ДИСЦИПЛИНА «ФИНИШ ТОЛЬКО ПО СЛОВУ ДЕРЖАТЕЛЯ» ДАЛА БОЛЬШЕ, ЧЕМ ОЧЕРЁДНОСТЬ, И ЭТО ИЗМЕРЕНО.**
   Обоим исполнителям я запретил финиш до моего явного разрешения, объявив запрет ЗАРАНЕЕ — чтобы
   потом не пришлось перехватывать работу на ходу (К-29 наоборот). Итог: Т-120 сел со второй
   попытки, Т-122 — с первой, против пятнадцати строк журнала у юнита того же размера прошлой ночью.
   **Побочная выгода оказалась больше задуманной:** раз финиши происходят только по моему слову, я
   ТОЧНО знаю, когда `main` неподвижен, и могу коммитить заметки, не гадая, не отказываю ли я
   кому-то посадку. К-30 («коммить в ту же минуту») и «не двигай `main`, пока сосед садится» —
   два правила, столкнувшиеся прошлой ночью; централизованное разрешение на финиш мирит их без
   нового механизма.
4. **ОКНО ТИШИНЫ РАБОТАЕТ ТОЛЬКО НАЗВАННЫМ ПОИМЁННО И ОГРАНИЧЕННЫМ НАБЛЮДАЕМЫМ СОБЫТИЕМ.** Куратор
   объявил его на посадке Т-120 в этой форме, и следующая же попытка прошла. **Отменять собственные
   прежние указания надо ЯВНО, отдельным предложением**: я ставил исполнителю потолок «больше
   четырёх попыток — остановись», и с открытием окна потолок стал вредным; я снял его отдельной
   фразой, прежде чем дать новое указание. Это уточнённая формулировка К-29, применённая к своему
   же приказу.

## 4. Очередь преемнику

**1. CB-264 (`medium`) — ГОЛОВА ОЧЕРЕДИ, СТРУКТУРА.** Три места, где описание механизма
`# noqa: S608` осталось неверным ПОСЛЕ Т-120. Главное: фраза, приземлённая моим юнитом, утверждает,
что мёртвые маркеры других правил `RUF100` вскроет «в момент, когда что-либо включено» — опровергнуто
построением, `select = ["S608"]` даёт ноль `RUF100`. Абзац есть **единственный в дереве источник
оценки стоимости варианта (2) CB-172**, и он занижает объём примерно вдвое. Часть 1 имеет
детерминированный оракул и дешева; части 2 (докстрока `EntityKind`) и 3 (неполнота «настоящей
защиты») машинного оракула не имеют. Части разделимы.

**2. CB-255 (`medium`) — ФОРМА ВЫБРАНА МНОЙ, и она НЕ та, что предложил исполнитель Т-122.**
Он предлагал перенести в копии пять оставшихся файловых проб `_cb215_probe_*`, то есть убрать
ПРИЧИНЫ движения. Я беру другое: перенести в одноразовую копию вложенный прогон САМОГО «молчаливого»
теста — то есть ЖЕРТВУ. Довод: дефект карты не в причинах, а в том, что тест утверждает молчание о
дереве, которым не управляет, и краснеет от ЛЮБОГО движения, а не только от пяти проб этого модуля.
Предложение исполнителя не отвергнуто, а понижено до попутной гигиены. Всё это — суждение ЧТЕНИЕМ;
три предпосылки, которые юнит обязан ПОСТРОИТЬ, перечислены в карте. **У карты сейчас ноль
воспроизведённых случаев** — потому что Т-122 убрал известную причину, а не потому, что свойство
исправлено; severity снижать на этом основании нельзя.

**3. CB-252** (`low`) — `install()` читает настройки незащищённым `json.load(open(...))`. Уедет с
ближайшим касанием `tools/install-simplify-gate.py`.

**4. CB-250** (`low`) — хук `commit-msg` не опознаёт заметку, названную в КОНЦЕ предложения. Замер
корпуса снят предшественником: 263 заметки, все на `.md`, значит ложный отказ достижим на ста
процентах. Ошибка в безопасную сторону. **Доказательство «по построению» обязан строить юнит.**

**5. CB-253** (`low`) — неопределённый счёт «eleven measured spellings». **Я пересчитал: записей
двенадцать, прочтений три (12/13/14), одиннадцать недостижимо.** Карта была права изначально;
читай §7 п. 2, прежде чем что-то ей вменять.

**6. CB-240** (`low`) — совет `where` и отказ `init` в одном каталоге. **Проверил, что карта не
протухла:** оба якоря живы (`db.py:1704`/`:1709` — советы, `:2072` — отказ), и форма (a) карты
точна: дефектный совет один, на ветке с неосмотренным кандидатом, а не два.

**7. CB-167** (`low`) — фильтр `meta_key` строит путь JSON без кавычек. Живёт в `findings.py` —
территория соседнего направления; куратор оставил карту за DIR-1, но перед выдачей проверь, не
работает ли сосед в этом файле.

**8. CB-187** (`medium`) — ливлок. Форма выбрана в самой карте; **оба направления независимо
указали на форму (a)** (неблокирующий замок на ветку поверх замка на интеграцию). После этой смены
довод слабее: дисциплина §3 п. 3 даёт почти то же без механизма — но она держится на прозе, а
«соглашение не может обеспечить соглашение».

## 5. Скоропортящееся — перепроверить на месте

`main == origin/main == bd71d31` на момент записи; рабочих деревьев моего направления нет.

**АЛЯРМ `main-invariants` НА `origin/main` КРАСЕН, И ЭТО НЕ ДИР-1.** Кураторский коммит `813bb6d`
положил на `main` файл `.claude/plans/briefs/DAILY-2026-08-29.html` — второй уровень и не `.md`,
тогда как прямо на `main` разрешён один уровень `*.md`. Пункт 6 обоих моих юнитов закрыт фактом ДО
этого коммита. **Пока алярм горит, он маскирует следующее настоящее нарушение** — передано куратору.

**В главном чекауте лежат ЧУЖИЕ незакоммиченные файлы:** `DIR-2-findings-identity.md` и
`HANDOFF-DIR-2-2026-08-29-s17.md` (изменены, не мои), плюс `.claude/settings.json` и его копия —
файлы владельца, не трогать. **Чужие изменённые отслеживаемые файлы отказывают посадке кодом 11**
(К-30, CB-130). Если твоя посадка получит 11 — читай тело стража, а не имя: причина может быть в
чужом файле, и лечится она не повтором, а разговором с соседом.

**Коммит-хук поймал меня на этом же:** я добавил свои два файла ПО ИМЕНИ, а в индексе уже лежали
чужие, и хук отказал. Лечится `git restore --staged -- <чужой файл>`. Механизм работает, не обходи
его `--no-verify`.

**Ни одна ветка никогда не удаляется.** Ветка `feature/t49-bt6-third-pilot-annotations` —
запаркованный пилот, решение владельца, не трогать.

## 6. Инструментальное

- **Пуш делает ДЕРЖАТЕЛЬ, ОТДЕЛЬНОЙ командой сразу после посадки.** Попадание проверяй
  `git merge-base --is-ancestor <мерж> origin/main`, **никогда порядком коммитов**. За смену
  «remote rejected … cannot lock ref» дважды означал, что мой коммит УЖЕ на `origin` (гонка
  пушей), а не что он не уехал, — проверяй предком, а не текстом ошибки.
- **Третий случай «посадка и отправка — два разных действия»**: мерж соседа `51d5854` пролежал
  неотправленным семь минут и уехал попутно с моим пушем. Называю наблюдением, а не отказом: семь
  минут не доказывают, что сосед не отправил бы сам. Куратор писал, что третий случай чинится
  механизмом.
- **`git rev-parse --short` принимает ТОЛЬКО ОДНУ ревизию**; с двумя падает «Needed a single
  revision». Я едва не принял эту особенность инструмента за поломку репозитория.
- **`exit 13` — верный отказ**, буквальный повтор с тем же `--merge-msg`. **`--skip-checks` не брать.**
- **Не запускай `uv run` в главном чекауте**, пока может идти чужой финиш. Для разовых замеров —
  `./.venv/bin/ruff`, `./.venv/bin/python`.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **Минт заводит НОВУЮ категорию только с `new_category=True`** — моя первая попытка завести карту
  с категорией `prose_not_mechanism` упала без внятного текста; взял существующую `contract_mismatch`.
- **Приёмщиков гоняй в ИХ СОБСТВЕННЫХ копиях**, не в главном чекауте: два прогона в одном корне
  мешают друг другу, и это ровно механизм CB-258.
- **Журнал посадок — `.worktrees/landing-attempts.log`**, gitignored. Считай СТРОКИ по слагу, а не
  свои вызовы: прошлая смена ошиблась вдвое именно так.

## 7. Записи против меня

1. **Я санкционировал расширение объёма Т-120, прочитав абзац «как целый текст», — и не поймал в
   нём ложь, стоявшую в том же предложении.** Я проверил прогоном ровно то утверждение, которое сам
   нашёл (про покрытие маркерами), а соседнее предложение про `RUF100` принял на веру, **потому что
   оно пришло из моего собственного брифа, где стояло со слов замера предшественника.** То есть я
   применил критерий направления к чужому тексту в том же абзаце, где не применил его к своему.
   Поймал только второй проход с другой апертурой. Это CB-264 и это же — происхождение пункта 20
   §13, записанного ПОСЛЕ ошибки, а не до.
2. **Я записал в карту CB-253 ложное обвинение и через час опроверг его сам.** Я «уличил» карту в
   том, что она описывает свой дефект неточно, — а описывала его так строка РЕЗЮМЕ из handoff'а
   предшественника, тогда как сама карта называла все три прочтения верно. Это К-27 с потребительской
   стороны: канон обязывает ПИШУЩЕГО резюме, а защищает он ЧИТАЮЩЕГО, и у читающего нет способа
   узнать, что строка у́же карты, кроме как открыть карту. Карту я открыл — и читал по ней резюме.
3. **Мой первый механический пересчёт того же перечня дал пятнадцать вместо двенадцати** —
   разделение по запятой убежало за точку, кончающую перечень. Поймал не проверкой, а тем, что
   напечатал сами записи и прочёл их.
4. **Едва не записал четвёртую находку того же класса, которой не было:** решив, что совет
   `codebugs init` в CB-240 стоит на двух сайтах, тогда как второй — ветка чистого обхода, где совет
   верен. **Общая форма трёх последних записей: охотник за утверждениями шире замера приобретает
   склонность утверждать, что чужие утверждения шире замера.** Все три поймало перечитывание
   собственного текста, то есть удача, а не механизм.
5. **Сквозной перечит этого handoff как одного текста** нашёл одно расхождение: §1 называл CB-255
   строкой «форма выбрана», а §4 объяснял, что выбрана НЕ предложенная исполнителем, — из §1 нельзя
   было понять, что это спор с исполнителем, а не согласие. Поправил. Больше сквозной перечит не
   нашёл ничего, и я отношусь к этому с подозрением, как велит правило.
