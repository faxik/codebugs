# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-26, смена 12

(вытесняет `HANDOFF-DIR-1-2026-08-26-session11.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов, а не восемь.** Девятый («юнит способен слиться, доказано
  выводом») добавлен решением куратора 2026-08-26, уже после того, как был написан мой стартовый
  промпт, где стояло «восемь». Я вёл приёмки по спеке. Файлы приёмщика местами говорят «семь» —
  называй число явно в каждом промпте приёмки. **Девятый пункт в codebugs требует перевода:** спека
  называет механизм соседнего проекта (`_INSURANCE_PATHS`), которого здесь НЕТ; заменитель —
  то, что гоняет `worktree-finish.sh` в дереве юнита после прямого мержа.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий; **Releasing** — для
  срезов; **Testing** — правила тестов на конкуренцию.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §5 таблица юнитов, §6
  ратифицированные расхождения, **§9 неизменяемые факты приёмки** (смена 12 первой), **§10
  эскалации — там Э-30, главное, что я оставляю**, §11 журнал, §13 шаблон брифа.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md` — каноны куратора, включая К-18.
- **Аллокатор ID:** `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| юнит | мерж | карты | итог |
|---|---|---|---|
| Т-90 | `9751337` | нет | **ПРИНЯТ С ОГОВОРКОЙ** — долг приёмки прошлой смены закрыт; заведены CB-205, CB-206 |
| Т-91 | `5e4e0b3` | CB-202 | **ПРИНЯТ С ОГОВОРКОЙ**, CB-202 закрыта; заведены CB-212, CB-213 |
| Т-94 | `0b842b6` | CB-204 | приземлён — герметичность набора тестов |
| Т-92 | `1b8205a` | CB-203, CB-201 | приземлён — у достижимости трекера появился третий исход |
| Т-95 | — | CB-176 | **сминчен и в работу НЕ поставлен намеренно, см. §3** |

**Заведено за смену:** CB-205 и CB-206 (находки приёмки Т-90), CB-212 и CB-213 (находки приёмки
Т-91), CB-211 (остаток, вынесенный менеджером Т-92). **CB-206 закрыта мною как дубль** — то же
измерение уже лежало в примечаниях CB-200 у соседнего направления, точнее моего.
**CB-199 понижена с `high` до `medium`** и сужена осознанно, с записью.

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Прочитай Э-30 в §10 артефакта целиком.** Это блокирующее решение уровня (1), и пока его нет,
   «окно тишины» стоит.
2. **Проверь, не пришёл ли ответ.** Если пришёл — форма выбрана, дизайн (редакция 3) готов, бриф
   пишется по нему.
3. **Сверь очередь с `git log`** — три направления работают параллельно.
4. **Проверь отставание `origin/main`.** Пуш требует явного согласия владельца; за смену отставание
   выросло с 22 до примерно 70.

## 3. Э-30 — коротко, чтобы понять вес, не открывая артефакт

Куратор ратифицировал **вариант (e)** карты CB-176: не сериализовать никого, а перестать считать
отказом движение `main`, затронувшее только файлы, которых сьют не читает. Условия ратификации:
белый список (не чёрный), обязательный третий круг кросс-модельного ревью ДО кода, эскалация вместо
редактирования при отказе, и явная строка К-18 про предпосылку самого предиката.

Круг проведён. **Два атакующих разных семейств моделей РАЗОШЛИСЬ:**

| атакующий | вердикт | уверенность | что судил |
|---|---|---|---|
| первый (Claude) | СТРОИТЬ ПОСЛЕ ПРАВОК | 0,88 → 0,90 | редакции 1 и 2 |
| второй (Codex/Sol) | **НЕ СТРОИТЬ** | **0,99** | **редакцию 1** |

Второй вернул «строить нельзя», а это ровно условие (iii) моего промпта: не редактировать, а
эскалировать. Куратор отдельно предписал при расхождении не сводить вердикты самому. **Поэтому
Т-95 сминчен и не запущен, ни строки кода не написано.**

**Обстоятельство, которое надо знать, но которое НЕ является сведением вердиктов:** второй атакующий
судил редакцию 1, где предикат состоял из ОДНОГО условия. Редакции 2 и 3 добавили второе — префиксное
непересечение путей окна и ветки, — которого он не видел. Часть его находок адресована предикату,
которого в нынешнем документе уже нет; одна (слепота пиновки к чтению через `git` и не-Python
подпроцессы) не адресована и составляет настоящее разногласие о требуемой строгости.

**Моя рекомендация, не решение:** прогнать второго атакующего по РЕДАКЦИИ 3, прежде чем решать. Без
этого расхождение неинтерпретируемо. Если и тогда «не строить» — честный вывод тот, что назван
промптом: класс не закрывается дёшево, остаётся вариант (d), только измерять.

**Дизайн готов и приземлён:** `.claude/plans/DESIGN-DIR-1-silence-window-e-2026-08-26.md`, редакция 3,
все девять предписанных ревью правок внесены.

## 4. МОИ ОШИБКИ, записываю против себя

1. **Я передал куратору неперепроверенное утверждение исполнителя КАК ФАКТ, и оно оказалось ложным.**
   Отчёт Т-91 утверждал, что старый страж был «структурно неприменим, ни одна функция инициализации
   схемы не передаёт литерал в вызов». Опровергла приёмка, не я. Перепроверил после: в пакете 185
   литеральных вызовов исполнения, 58 из них DML, и две — ровно те точки, которые чинил CB-195.
   **Отчёт противоречил сам себе через две страницы** — первая строка его собственной таблицы гласит
   обратное. Дисциплина направления требует перепроверять лично всё, что передаётся дальше как факт;
   я нарушил её на самом громком утверждении смены. Утверждение успело попасть в `CLAUDE.md`
   (карта CB-213).
2. **Я предписал в брифе Т-92 флаг `--items`, которого в этом репозитории НЕТ** — это инструмент
   соседнего проекта. То есть я совершил ровно ту ошибку, о которой сам предупреждаю в каждом брифе
   отдельной строкой («`tools/worktree-probe.sh` здесь не существует, за ним ходили шесть агентов»).
   Менеджер сохранил ЗАМЫСЕЛ, а не букву: назвал ветку так, что обе карты попали в имя. **Проверяй
   существование флага, а не только команды.**
3. **Я завёл карту, не спросив трекер о соседях находки** (CB-206). Дубль обнаружился только когда я
   пошёл читать соседнюю карту по другому поводу. Отпечаток тут не помогает по построению: чужая
   запись жила в ПРИМЕЧАНИЯХ другой карты, куда дедупликация не смотрит.
4. **Мой собственный сторож фонового процесса был дефектен того самого класса, что я ловлю**: шаблон
   `pgrep -f` совпадал с командной строкой самого сторожа, поэтому он никогда не увидел бы
   завершения. Поймал сам, переписал на ожидание по идентификатору процесса.
5. **Текст реестра я написал заранее под номер Т-93, а аллокатор выдал Т-94** — соседнее направление
   сминтило в промежутке. Это дословно ошибка предшественницы, повторённая мной. Правильный порядок
   прежний: сминтить, ПОТОМ писать текст с выданным номером.

## 5. Что работает и стоит продолжать

- **Изолированный приёмщик окупается каждым запуском.** Три приёмки заказаны — все принесли находки,
  которых не было ни у исполнителя, ни у меня. Пять карт смены из шести заведены находками приёмок.
- **Требование «попробуй обойти НОВЫЙ страж так же, как предшественник обошёл старый».** Поставленное
  главным требованием приёмки Т-91, оно дало восемнадцать мутаций и три необъявленных слепых пятна.
  Для любого юнита, чинящего страж, это должно быть первой строкой промпта приёмки.
- **Строка «то же состояние, полученное ДРУГИМ способом» в оракуле.** В Т-92 она дала пять новых
  способов сделать трекер недостижимым сверх трёх известных.
- **Замер перед выбором формы, второй раз подряд.** Замер по CB-172 опроверг её диагноз об опасности
  и УДЕШЕВИЛ форму; замер по CB-167 опроверг её утверждение «живого вреда нет» и показал вред прямо
  в этом трекере. Обе карты стали решаемыми.
- **«Спроси исполнителя, а не диагностируй по молчанию».** Долг прошлой смены; применил к водителю
  внешней модели вместо того, чтобы забирать работу.

## 6. Очередь преемнику

1. **Э-30 — ответ уровня (1).** Пока его нет, «окно тишины» стоит. Дизайн готов.
2. **Приёмки Т-92 и Т-94**, если не закрыты мною. Обе заказаны в конце смены.
3. **CB-213** — неверное объяснение в `CLAUDE.md` плюс счёт в прозе плюс шестая позиция устаревшей
   прозы. Три правки одного файла, режутся одним юнитом, и это правка НОРМАТИВНОГО документа.
4. **CB-212** — новый ратчет переобещает и на трёх состояниях слабее заменённого. Требует решения
   уровня (2) о форме: закрыть три формы и добавить пин обратного направления, либо переписать
   обещание как свойство.
5. **CB-211** — повреждённый файл базы выглядит здоровым у `where` и роняет любой другой верб сырым
   traceback. **Читать вместе с CB-199** — возможно, один остаток, возможно, два.
6. **CB-205** — сводный абзац выпуска 0.2.1 обещает больше, чем держит код. Коррекция в срезе 0.2.2.
7. **Срез 0.2.2** — накопилось: четыре юнита с записями плюс CB-205. Порядок — раздел **Releasing**;
   `pipx reinstall` и перезапуск MCP-серверов за владельцем.
8. **Блок «честные границы», обе карты ПЕРЕМЕРЕНЫ мною в эту смену, бери числа как факт:**
   **CB-172** — механизм подтверждён (правило `S608` не в наборе, 20 маркеров, из них 16 работают,
   3 не прикрепляются из-за склейки f-строк, 1 избыточен), но диагноз об опасности ОПРОВЕРГНУТ: из
   52 срабатываний незащищённых НЕТ ни одного, три «значенческих» сайта закрыты соседним юнитом уже
   после наблюдения карты. Дешёвый вариант (a) теперь достаточен.
   **CB-167** — дефект воспроизведён трижды, включая боевой инструмент на настоящем трекере; карта
   утверждала, что живого вреда нет, а он есть: два верхнеуровневых ключа с точкой на CB-3, и запрос
   по ним отвечает «ничего нет». Потребителей обхода вложенности НЕТ (гребёнка по 12 трекерам и по
   дереву). Опасение, ради которого карта требовала осторожности, снято.
   **CB-110** — семантика `done`, трогает CHECK-ограничение, то есть миграцию схемы. Не мерилась.
9. **CB-158** — форма выбрана, числа перемерены прошлой сменой. **НЕ БРАТЬ, пока в полёте ветка
   соседнего направления по CB-196**: обе трогают обработку предела выборки, это столкновение.

**Пакеты milestones и минт-гейта закрыты владельцем — не трогать. CB-161/CB-162 и категории — у DIR-2.**

## 7. Скоропортящееся — перепроверить на месте

Статусы карт; исход приёмок Т-92 и Т-94; **запушены ли `main` и тег `v0.2.1`** и переставлен ли pipx
у владельца; живость юнитов (`git log` ветки и `git worktree list`, **никогда** уведомление);
отставание `origin/main`; хвост аллокатора (последний мой — **Т-95**, но DIR-2 минтит параллельно).

## 8. Инструментальное

- **`git push origin main` требует явного согласия владельца, у направлений его нет.** Следствие для
  пункта 6 приёмки: закрывать локальным прогоном с честной пометкой «origin отстаёт на N, алярм этих
  коммитов не видел». Это честная формулировка, а не «n/a». Называй N в каждом промпте приёмки.
- **`tools/worktree-probe.sh` в этом репозитории НЕ существует, и флага `--items` у
  `tools/worktree-setup.sh` ТОЖЕ НЕТ.** Оба — из соседнего проекта. Второе я узнал, наступив сам.
- **Повторяя утверждение CI-алярма локально, обязательно `-c core.quotePath=false`.** Без него
  не-ASCII пути приходят экранированными, и приёмка Т-91 получила на этом шестнадцать призраков.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **`exit 13` — ВЕРНЫЙ отказ**, лечится буквальным повтором с тем же `--merge-msg`. **`exit 11`
  повтором НЕ лечится.** За смену наблюдался и третий вид отказа посадки: конфликт ПРЯМОГО мержа в
  дереве ветки, когда два юнита дописали в один раздел `## [Unreleased]`. Это харнес работает как
  задуман — конфликт всплыл в безопасном месте; разрешается внутри дерева, обе записи сохраняются.
- **Ловушка мутационной пробы:** проба, запущенная указанием пути к тестам БЕЗ смены рабочего
  каталога внутрь копии, молча измеряет главный чекаут. Пиши это в каждый бриф и промпт приёмки.
- **В дереве из `git archive` один тест падает всегда** — тот, что читает настоящую историю git.
- **Негерметичность набора тестов (CB-204) ПОЧИНЕНА юнитом Т-94**, но пока приёмка не вернулась,
  предупреждай агентов о посторонних каталогах `.codebugs` выше временного дерева по-прежнему.
- **Перед заведением карты посмотри `categories` И спроси трекер о СОСЕДЯХ находки**, а не только о
  её отпечатке: чужая запись может лежать в примечаниях другой карты.
- **Уведомления субагентов опаздывают и повторяются.** Признак приземления — только `git log`.
- **Водитель внешней модели может подменить режим песочницы**: мой запустил Codex с полным доступом
  вместо предписанного «только чтение». Главное дерево уцелело, но проверяй.
- **`.claude/settings.json` лежит на `main` неотслеживаемым** и не мой. Трогать не надо.
