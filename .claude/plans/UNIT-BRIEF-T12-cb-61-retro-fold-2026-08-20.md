# UNIT-BRIEF Т-12 — CB-61 ретро-fold категорий + CB-113(a)

Хендофф уровня (3) исполнителю (4). 2026-08-20. Родитель:
`.claude/plans/L3-BRIEF-DIR-2-T12-retro-fold-2026-08-20.md`. Карты: **CB-61** (её закрывает
этот юнит; клейм держит ветка `fix/cb-61-category-retro-fold`, брать ничего не надо) и
**CB-113 путь (a)** — карту CB-113 НЕ трогать, не клеймить, статус не менять: терминал
назначает DIR-2 на приёмке.

## Замысел

CB-60 защищает только НОВЫЕ строки. Существующий корпус хранит вариантные написания
категорий, а `fingerprint` **хранится**, не пересчитывается, и категория — вход дериватного
хеша. Отсюда CB-113(a): до-гейтовая строка, ре-наблюдённая СВОИМ ЖЕ написанием, форкает
identity (наблюдение нормализуется до хеширования, хранимый хеш держит старое написание;
гейт не отказывает, потому что `_existing_categories` нормализует хранимые написания для
теста членства). Ретро-fold сворачивает корпус к канону, ре-деривирует хранимые `auto:v1`
хеши и тем закрывает форк.

## Preflight-вердикты менеджера (проверены на main `3cab8de`, 2026-08-20)

1. `types.normalize_category` — types.py:190; casefold + strip + `[-\s]+ → _`; `""` проходит.
2. `_derive_fingerprint(category, file, description, meta)` — findings.py:459; канонический
   JSON-массив `[category, file, _normalize_for_fingerprint(description, meta)]`, префикс
   `auto:v1:`, sha256[:32]. Категория — вход, подтверждено.
3. `_normalize_for_fingerprint` (findings.py:416) вырезает из описания **строковые** значения
   meta под волатильно-выглядящими КЛЮЧАМИ. Отсюда следствие, на котором стоит весь шаг 3
   этого юнита: хранимая `meta` — НЕ та `meta`, что участвовала в деривации (машинные ключи
   `occurrences`, `category_minted`, `similar_to`… добавлены после). Обычно они хеш не
   двигают (не строки / не волатильные ключи), но **это надо проверять по строке, а не
   предполагать** — см. round-trip ниже.
4. `ux_findings_fingerprint_live` — findings.py:76, partial UNIQUE по `fingerprint`
   WHERE `fingerprint IS NOT NULL AND status IN ('open','in_progress','stale')`
   (`LIVE_STATUSES`, findings.py:59).
5. `_existing_categories` (findings.py:332) читает `DISTINCT category` по **всем** строкам,
   терминальные включительно → fold обязан покрывать терминальные строки тоже, иначе старое
   написание остаётся легитимным каноном.
6. `register_tools` — findings.py:2156, `register_cli` — findings.py:2486. Регистрация только
   здесь; `cli.py`/`server.py` — файлы DIR-1, НЕ трогать.
7. Существующий MCP-инструмент `categories` (findings.py:2479) + домен `get_categories`
   (:2145) — образец стиля и место рядом для нового кода.

## Задание

### 1. Доменная функция `normalize_categories`

В `findings.py`, рядом с `get_categories`:

```python
def normalize_categories(
    conn, *, fold_map: dict[str, str] | None = None, apply: bool = False
) -> dict[str, Any]:
```

**Дефолт `fold_map=None`** — механический fold: для каждого хранимого написания цель =
`normalize_category(написание)`. Это ровно те три механических пары, к которым кросс-модельный
ревью CB-61 велел сузить первую миграцию; семантические семейства из референсного census — НЕ
реализовывать и не подмешивать.

**Валидация переданной `fold_map`** (до всего): ключи и значения — строки; значение обязано
быть каноническим (`normalize_category(v) == v`), иначе `ValueError` с указанием пары — иначе
fold породил бы написание, которое следующее же наблюдение свернёт снова. `{}` — легальный
явный no-op (отчёт с нулями), не ошибка; `None` — дефолт. Это разные вещи (доктрина
«no filter — `None` и `""`, не truthiness»).

**Популяция**: все строки `findings` (терминальные включительно, вердикт 5), у которых
`category` — строка и цель fold отличается от хранимого написания. Не-строковая `category`
(легаси explicit-id строка) — пропуск + счётчик, не исключение (та же логика, что в
`_existing_categories`).

**Политика по fingerprint для каждой переименовываемой строки:**

| Хранимый fingerprint | Действие |
|---|---|
| `NULL` | только переписать `category`; хеш не трогать |
| не `auto:v1:` (supplied) | только переписать `category`; хеш **не трогать** |
| `auto:v1:…` | **round-trip**, затем ре-деривация — см. ниже |

**Round-trip — обязателен, это ядро честности шага 3.** Для `auto:v1`-строки сначала
проверь, что хранимые входы вообще воспроизводят хранимый хеш:
`_derive_fingerprint(старая_category, file, description, json.loads(meta_json)) == fingerprint`.

- совпало → ре-деривация с новой категорией, те же `file`/`description`/`meta` (та же версия
  нормализатора, меняется ТОЛЬКО категорийный вход — дисциплина CB-54);
- не совпало (или `meta_json` не парсится) → строка **пропускается целиком**: ни категория,
  ни хеш не пишутся, строка попадает в `unverifiable` отчёта. Обоснование, зафиксируй его в
  докстринге: переписать категорию без ре-деривации — это и есть дефект, который карта
  закрывает (перманентный десинк identity), а сфабриковать хеш из входов, которые
  доказуемо не воспроизводят хранимый, — хуже. Не останавливает прогон (стоп ратифицирован
  только для коллизий), но `unverifiable > 0` печатается заметно, как решение для оператора.

**Коллизии (ратифицировано владельцем: report-and-stop, НЕ авто-мерж).** Посчитай
пост-fold мультимножество `(fingerprint, status)` по всем строкам с не-NULL хешем
(переименованные — с новым значением, остальные — с хранимым). Любой fingerprint, который
пост-fold держат **≥2 ЖИВЫЕ** строки (`LIVE_STATUSES`) — коллизия. Пре-существующих дублей
среди живых быть не может (частичный индекс), значит любая найденная коллизия порождена
fold-ом. При хотя бы одной коллизии: **не пишется НИЧЕГО** (весь прогон, не «эта пара»),
отчёт несёт `stopped: true` и **именованный** список пар (`fingerprint` + список id + их
категории «из/в»). Коллизионные пары — материал для В-3/CB-46, их надо назвать.

**Транзакция.** Весь `apply`-прогон — ОДИН `db.txn(conn)`: чтение популяции под write-lock
(CB-24), решение о коллизиях, затем UPDATE'ы. Никакого `conn.commit()` внутри. Стоп =
ничего не легло. Dry-run (`apply=False`) **не открывает write-транзакцию вообще** и не пишет
ни байта.

**Ring НЕ переписывается** — `meta` не участвует в UPDATE ни одним байтом. Occurrence-записи —
вербатим-улики истории.

**Ре-кей — санкционированная точка входа, и это надо объявить.** `update_finding` документирует
`fingerprint` как immutable и обязан таким остаться; эта миграция пишет хеш своим собственным
UPDATE и в докстринге прямо говорит: это единственный санкционированный ре-кей, релаксация
объявлена здесь, `update_finding` её не получает. Не звать `update_finding`.

UPDATE — без `RETURNING` (правило RETURNING), `rowcount != 1` на строку → исключение (весь
прогон откатывается).

**Форма отчёта** (`dict[str, Any]`, одинаковая в dry-run и apply):
`applied`, `stopped`, `fold_map` (только реально работающие пары), `rows_scanned`,
`renames` (список `{id, from, to, fingerprint_action, old_fingerprint, new_fingerprint}`),
`counts` (`category_only`, `refingerprinted`, `supplied_untouched`, `null_untouched`,
`unverifiable`, `skipped_non_string`), `collisions`, `unverifiable`.

### 2. MCP-инструмент `categories_normalize`

В `register_tools` (findings.py:2156), рядом с `categories`. Домен findings — без префикса
(задокументированное исключение). Аргумент `fold_map: dict | str | None = None` (str —
JSON, конвенция репо для JSON-аргументов MCP), `apply: bool = False`. Докстринг — dry-run
по умолчанию, стоп на коллизии, ring не трогается.

### 3. CLI-глагол `categories-normalize`

В `register_cli` (findings.py:2486) + обработчик `cmd_categories_normalize` (домен findings
— без префикса, исключение). Флаги: `--apply` (без него — dry-run), `--fold-map JSON`,
`--json` (сырой отчёт вместо человеческой сводки). Вывод — через `fmt.py`, файлов не пишем.
**Код выхода**: dry-run — всегда `0`; `--apply` с коллизией — печать именованных пар и
**exit 1** (команда отказала). Ошибки домена ловятся по правилу порядка арм
(`json.JSONDecodeError` раньше `ValueError`).

### 4. Wire golden + CHANGELOG

- `PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json` — **ИЗ
  worktree** (голый `python` из worktree резолвит `codebugs` через editable-install главного
  чекаута и снял бы снимок чужого дерева).
- CHANGELOG: новая команда, dry-run по умолчанию, стоп на коллизии, ring не трогается,
  supplied/NULL хеши не трогаются. Прецедент формы — запись CB-52.

## НЕ в скоупе

- CB-46 (backfill/merge-policy коллизий — за В-3), CB-90, CB-62, счётчик минта.
- Семантические fold-семейства из census-референса.
- `update_finding` и его контракт immutability — ни байта диффа.
- Файлы DIR-1: `cli.py`, `server.py`.
- `CLAUDE.md` — НЕ трогать (правка о ре-кее уходит эскалацией на уровень (2)).
- **Регионы параллельного Т-14 (BT-5 attention) в том же `findings.py`**: возврат `_add_one`,
  возврат `_bump_row`, `_finalize_add`, докстринги MCP `add`/`batch_add`. Не занимать.

## Критерии приёмки (TDD — красный тест раньше фикса)

1. **CB-113(a) закрыт.** Фикстура симулирует до-гейтовую строку прямым INSERT'ом:
   `category="Process Improvement"`, `fingerprint=_derive_fingerprint("Process Improvement",
   file, desc, meta)`. **До fold**: `add_finding` с тем же написанием/файлом/описанием
   создаёт НОВУЮ строку (форк) — assert. **После** `normalize_categories(apply=True)`:
   категория `process_improvement`, хеш = `_derive_fingerprint("process_improvement", …)`;
   то же наблюдение **бампает** исходную строку (`was_new` False, `occurrence_count` 2).
2. **Dry-run не пишет ни байта**: снимок `SELECT id, category, fingerprint, meta_json FROM
   findings` (плюс хеш файла БД для file-based теста) до и после — идентичны.
3. **Коллизия → стоп и нулевая запись**: искусственная пара — две ЖИВЫЕ строки, чьи
   `auto:v1` хеши расходятся только категорией-вариантом и которые после fold сходятся в один
   хеш. `apply=True` → `stopped: true`, пара **названа** в отчёте, БД не изменилась ни в одной
   строке (включая те, что свернулись бы успешно). CLI `--apply` в этом случае — exit 1.
4. **supplied/NULL нетронуты**: строка с supplied fingerprint и строка с `fingerprint IS NULL`
   получают новую категорию, хеш — байт в байт прежний; счётчики отчёта это отражают.
5. **Round-trip/unverifiable**: строка с `auto:v1`-хешем, который не воспроизводится из
   хранимых входов (подставить `auto:v1:` + произвольные 32 hex) — пропускается ЦЕЛИКОМ
   (категория тоже не меняется), попадает в `unverifiable`, прогон продолжается.
6. **Ring не тронут**: после `apply=True` `meta.occurrences` переименованных строк идентичны
   до-fold значению.
7. **Терминальные строки сворачиваются** (вердикт 5): `fixed`-строка с вариантным написанием
   после fold несёт канон — иначе `_existing_categories` продолжит легитимизировать вариант.
8. **Валидация fold_map**: неканоническая цель → `ValueError` с указанием пары; `{}` — no-op
   с нулями; не-строковые ключ/значение → `ValueError`.
9. Вся сюита зелёная **ИЗ worktree**: `uv run --extra dev python -m pytest tests/ -q`;
   `uv run --extra dev ruff check src/ tests/` (ruff 0.15.7) чист; wire golden совпадает.

## Протокол

Работа ТОЛЬКО в `/home/faxik/w/codebugs/.worktrees/fix-cb-61-category-retro-fold`. На main не
писать; `git checkout <branch> -- <files>` на main = редактирование main. Коммиты по ходу
(typed branch уже есть). Финиш НЕ запускать — это делает менеджер. Возврат: что сделано,
имена добавленных тестов, факт «красный до / зелёный после» по п.1 и п.3, результат сюиты и
ruff, **именованный список коллизионных пар этого трекера** (если dry-run на реальном
`.codebugs/findings.db` их нашёл — прогони dry-run ИЗ worktree против реального трекера,
он ничего не пишет), расхождения с этим брифом (пустой список — явно).

## §12 — вердикты приёмки (3)←(4), 2026-08-20

Приёмка проведена менеджером Т-12 по контракту SPEC-planner-cascade §13, семь пунктов.

**1. Дифф прочитан против ЗАМЫСЛА, не только задания — PASS.** Замысел: «корпус свёрнут к
канону, identity-форк до-гейтовых строк закрыт». Прочитаны `_validate_fold_map` (:2183),
`_fold_row_decision` (:2213), `_plan_category_fold` (:2261), `normalize_categories` (:2347) и
все семь ханков диффа (`@@ -220`, `-471`, `-2153`, `-2482`, `-2707`, `-2907`, `-2928` — все
аддитивные, кроме однострочной подстановки `_AUTO_V1_PREFIX` в `_derive_fingerprint`).
Замысел исполнен: коллизия решается по ПОСТ-fold занятости частичного индекса
(`effective` на :2314, `LIVE_STATUSES` на :2315), а не по паре, и стоп — на весь прогон.
Границы соблюдены механически: `git diff --name-only main...HEAD` — ровно пять файлов,
`cli.py`/`server.py` не тронуты (0 строк), регионы параллельного Т-14 (`_add_one`,
`_bump_row`, `_finalize_add`) в диффе отсутствуют.

**2. Мутационная проба — PASS, руками, 4/4 мутанта пойманы РАЗНЫМИ тестами.**
М-1 `if report["stopped"]:` → `if False:` (стоп на коллизии выключен) → 2 failed
(`test_apply_stops_names_the_pair_and_writes_nothing`, `test_cli_apply_exits_1_and_names_the_pair`),
причём падение приходит как `sqlite3.IntegrityError: UNIQUE constraint failed:
findings.fingerprint` — то есть без стопа миграция реально упирается в частичный индекс.
М-2 round-trip выключен → 1 failed (`test_a_row_whose_inputs_do_not_reproduce_its_hash_is_skipped_whole`).
М-3 `if not apply:` → `if False:` (dry-run пишет) → 1 failed (`test_dry_run_leaves_rows_and_bytes_identical`).
М-4 в UPDATE пишется `old_fingerprint` вместо `new_fingerprint` — то есть САМ дефект CB-61
(категория переписана, хеш не ре-деривирован) → 1 failed (`TestCb113aFork::test_after_the_fold_the_same_observation_bumps_instead`).
Дерево восстановлено байт в байт после каждой пробы (`git status` чист).

**3. Предпосылки preflight перепроверены против ПРИЗЕМЛЁННОГО кода — PASS.** Все семь
вердиктов держатся; вердикт 3 (машинные ключи meta не двигают хеш) исполнителем не принят
на веру, а ИЗМЕРЕН: round-trip на реальном трекере даёт `unverifiable: 0` на 12
`auto:v1`-строках. main за время юнита двигался (`3cab8de → c5f8586`), но `findings.py`,
golden и CHANGELOG на main не менялись — `git diff --stat 3cab8de..HEAD` по этим путям пуст,
Т-14 ещё не приземлился, форвард-мердж тривиален.

**4. Слот эскалаций — заполнен явно, четыре пункта, см. ниже.**

**5. Артефакт уровня — этот файл, §12 (неизменяемый факт приёмки).** Факт приёмки пишется
СЮДА, не в документ направления: строка в `DIR-2-findings-identity.md` — за уровнем (2).

**6. Post-merge-контроль (адаптирован) — PASS.** В codebugs post-merge-gate нет, есть только
CI-алярм `.github/workflows/main-invariants.yml`, который не может отказать в пуше. Названный
ЗАМЕНЯЮЩИЙ механизм — гейт `worktree-finish.sh`: он делает форвард-мердж main В worktree и
гоняет `ruff check` + полную сюиту против КОМБИНИРОВАННОГО дерева до мерджа на main, под
`flock`, с in-lock перепроверкой SHA (exit 13, если main сдвинулся). Результат — в возврате
менеджера вместе с merge-SHA.

**7. Карты — CB-61 → `fixed` после мерджа (branch-клейм снимается автоматически).**
CB-113 НЕ трогается: путь (a) закрыт этим юнитом, но терминал карты назначает DIR-2 на
приёмке (2)←(3); на неё пишется только `append_note` о закрытии пути (a).

### Эскалации на уровень (2) — четыре, явно

1. **`CLAUDE.md` требует двух правок, которые этот юнит намеренно не делал** (риск конфликта с
   параллельными юнитами в тех же абзацах). Обе — фактические, не стилистические:
   (а) правило CB-43 п.6 говорит «`fingerprint` … documented immutable at update (re-keying must
   renegotiate the index — **future card**)» — эта карта и есть та ре-негоциация, и точка входа
   теперь существует и объявлена в докстринге `normalize_categories`;
   (б) абзац CB-60 заканчивается «Stored rows are never rewritten: … **CB-61's retro-fold gap,
   deliberately left open**» — гэп закрыт.
2. **Fold на РЕАЛЬНОМ трекере не применён — только dry-run.** 17 строк ждут (12 ре-деривация,
   5 только категория), коллизий нет, `unverifiable: 0`. Запускать ли `--apply` и когда —
   решение уровня (2)/владельца, а не менеджера юнита: это разовая миграция боевых данных.
3. **`tests/test_category_gate.py::TestLegacyWeirdRows` не проверяет то, что декларирует** —
   вставляет `5` в колонку `category` с TEXT-аффинностью, SQLite возвращает `'5'`, то есть
   «не-строковая категория» так не воспроизводится (нужен `CAST(… AS BLOB)`). Замечено
   исполнителем при написании собственной фикстуры, где ошибка была измерена и исправлена.
   Вне скоупа Т-12 — кандидат в отдельную карту.
4. **Известный предел, объявленный в докстринге, а не закрытый**: сконструированная `fold_map`
   может нарушить частичный индекс в ПРОМЕЖУТОЧНОМ состоянии (в середине цикла) при валидном
   финальном — это `sqlite3.IntegrityError` и откат всей транзакции, то есть fail-closed и
   ничего не легло, но сообщение будет сырым исключением, а не отчётом о коллизии.

### Ратифицированные расхождения с этим брифом (пять, все приняты)

1. Колонка называется `meta`, не `meta_json` (бриф ошибся в букве — смысл сохранён).
2. `updated_at` при fold-е не бампается: он фиксирует АВТОРСКУЮ правку, миграция написания
   ничего не авторствует. Принято.
3. Тесты — в новом файле `tests/test_category_fold.py`, чтобы не пересечься с Т-14. Принято.
4. Введена константа `_AUTO_V1_PREFIX`, `_derive_fingerprint` переведён на неё — одно
   определение версии на двух читателей, анти-дрейф. Принято (2 строки, вне регионов Т-14).
5. Форма записи в `unverifiable` выбрана исполнителем — брифом не задавалась. Принято.

**Вердикт: ПРИНЯТ (7/7).**
