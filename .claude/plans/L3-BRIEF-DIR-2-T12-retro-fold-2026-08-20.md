# L3-BRIEF Т-12 — CB-61: ретро-fold категорий + CB-113(a)

Хендофф DIR-2 (уровень 2) менеджеру задачи (уровень 3). 2026-08-20. Спека:
`~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md` (§1, §2,
§4, §7, §13). Финальный юнит посадки контракта BT-4 (строки 1–5 уже посажены: Т-8/Т-9/
Т-10/Т-11). Карта CB-61 разблокирована посадкой Т-3 (`entity_resolved`);
**ратифицировано владельцем 2026-08-20: коллизии при ре-деривации репортятся и
останавливают, НЕ авто-мержатся** — merge policy (В-3) эту карту не держит.

## Тройка П-Б

**Замысел.** CB-60 защищает только НОВЫЕ строки; существующий корпус хранит вариантные
написания, и его identity-инструменты слепы через границу варианта — включая
воспроизведённый CB-113(a): до-гейтовая строка, ре-наблюдённая СВОИМ ЖЕ написанием,
форкает identity вместо бампа (нормализованное наблюдение деривирует другой хеш, чем
хранимый). Ретро-fold сворачивает корпус к канону и закрывает форк.

**Задание (по тексту CB-61, прочитай карту целиком через MCP):**
1. Одноразовая команда `categories-normalize` (CLI-глагол + MCP-инструмент; регистрация —
   через `register_cli`/`register_tools` в `findings.py`, ничего в `cli.py`/`server.py`
   не править — это файлы DIR-1): fold map (variant → canonical), дефолт — Т-3-нормализация
   (`normalize_category`); **dry-run отчёт ДО любой записи** (что свернётся, какие
   fingerprint-ре-деривации, какие коллизии) — дефолтный режим, запись только явным флагом.
2. Bulk-переписывание `findings.category` по fold map.
3. Ре-деривация ХРАНИМЫХ fingerprint у переименованных строк — ТОЛЬКО `auto:v1:*`
   (category — вход дериватного хеша; supplied-fingerprints не трогать; NULL — не трогать);
   та же версия нормализатора, меняется только категорийный вход (дисциплина CB-54).
4. **Коллизия ре-деривации** (новый хеш совпал с хешем другой ЖИВОЙ строки, частичный
   уникальный индекс `ux_findings_fingerprint_live`) → отчёт + СТОП до записи этой пары,
   не авто-мерж (ратифицировано; коллизионные пары — материал для В-3/CB-46, назвать их
   в возврате). Вся запись — в одной транзакции (`db.txn`): стоп = ничего не легло.
5. **Ring НЕ переписывается** — occurrence-записи это вербатим-улики истории, fold их не
   трогает.
6. НЕ в скоупе: CB-46 (backfill, за В-3), CB-90, CB-62, счётчик минта.

**Критерий приёмки:** CB-113(a)-сценарий закрыт — до-гейтовая строка со старым написанием
после fold бампается своим же написанием, не форкает (тест, красный до fold-а прогона);
dry-run не пишет ни байта (тест); коллизия → стоп с отчётом и нулевой записью (тест с
искусственной коллизионной парой); supplied/NULL fingerprints нетронуты (тест); wire golden
регенерирован (новый инструмент); CHANGELOG; сюита зелёная.

## Предпосылки

Текст CB-61 (fold map, шаги 1–4, дешевизна на legacy: NULL-fingerprint строки — только
category-rewrite); CB-113(a) воспроизведён пробами защитника BT-4
(`review-x2-bt4-defender.md`); контракт BT-4 посажен целиком (Т-8..Т-11) — сверяй
предпосылки по символам на preflight, main двигался весь день.

## Клеймы, границы, протокол

CB-61 держит DIR-2 (CLM-25): сними непосредственно перед setup
(`claims_release`, holder=DIR-2, kind=agent, repo=/home/faxik/w/codebugs), затем
`bash tools/worktree-setup.sh fix/cb-61-category-retro-fold` — setup возьмёт branch-клейм
(имя ветки несёт id). Exit 3 → СТОП и доклад. **CB-113 (CLM-24) НЕ клеймить и НЕ
закрывать** — путь (a) закрывает этот юнит, но терминал CB-113 назначает DIR-2 на приёмке;
append_note о закрытии пути (a) — да. Файлы: `findings.py`, тесты, golden, CHANGELOG;
НЕ файлы DIR-1. Worktree-харнесс, исполнитель — свежий субагент (П-В), TDD,
`uv run --extra dev`, ruff 0.15.7; юнит-бриф в worktree; на main напрямую не коммитить.
После посадки: CB-61 → fixed (§13 п.7; автоснимет branch-клейм). Возврат — тройка с явным
слотом эскалаций; мутационная проба руками; коллизионные пары этого трекера (если есть) —
именованным списком в возврате.