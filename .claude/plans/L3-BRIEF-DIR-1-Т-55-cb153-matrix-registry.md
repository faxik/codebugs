# L3-BRIEF DIR-1 / Т-55 — CB-153: матрица способностей перестаёт быть слепой к сгенерированной поверхности

**Класс по Р1: B** — оракул детерминирован: инструмент либо видит 13 тулов и 13 вербов `bench`+`sweep`,
либо нет. **Стартовый тир: sonnet, effort medium**, бюджет попыток 6.
Автор брифа: держатель DIR-1 (уровень (2)), 2026-08-24.
Карта: **CB-153 (high)** — `mcp__codebugs__get`, читать целиком.
Нормативы: `CLAUDE.md` — **MCP tool registration**, **CLI**, **Testing**.

---

## §1. Дефект, измеренный числами

`.claude/plans/exposure-scripts/matrix.py` находит MCP-тулы **АСТ-обходом по СИНТАКСИСУ декоратора**
(`decorator_is_mcp_tool`, матчит `@mcp.tool()`/`@mcp.tool`). После пилотов BT-6 поверхность `bench` и
`sweep` не пишется декоратором — она **эмитируется** `surfacegen.emit_tools()` из деклараций. Итог,
прогнан мной на сегодняшнем main:

    BOTH surfaces:  44      MCP only: 29      CLI only: 4      NEITHER: 32
                                                               ^^ из них 16 ФАНТОМНЫХ

Шестнадцать: семь способностей `bench` и девять `sweep`. **Слепота на ОБЕИХ сторонах** — CLI-половина
ключуется на `add_parser` внутри `register_cli`, а он тоже сгенерирован; в выводе эти строки идут как
`-  -  (none)`.

**Инструмент не падает и не молчит — он уверенно докладывает ПРОТИВОПОЛОЖНОЕ.** Ошибка направлена в
сторону лишней работы: идущий по матрице искать дыры экспозиции найдёт 16 несуществующих и может
продублировать уже экспонированное. Цена уже уплачена однажды — замер популяции CB-151 держателем был
занижен на 4 из 18 по этой же причине.

## §2. ПРЕФЛАЙТ ДЕРЖАТЕЛЯ: популяция слепых инструментов — ОДИН, и это измерено

Карта требовала начать с измерения популяции инструментов; я его сделал, чтобы ты не тратил на это
проход:

- **`matrix.py` — слеп** (обе стороны). Это твой предмет.
- **`module_surface.py` — НЕ слеп, но его метрика сменила СМЫСЛ.** Он ключуется на спаны функций
  `register_tools`/`register_cli`, а не на декоратор. На мигрированном `sweep` он честно отдаёт
  `wiring = 6 кода / 2 прозы` (было 158/186 до миграции), потому что функция обвязки действительно
  теперь трёхстрочная, а содержание уехало в декларации, которые считает `--sloc`. **Число не лжёт —
  оно отвечает на другой вопрос.** В скоуп юнита он НЕ входит; назови это в возврате как факт.
- **`tests/test_milestones_surface.py`** упоминает `@mcp.tool` только в прозе (это пер-модульный
  замороженный список имён, не свип). Не слеп.
- **`tests/test_strict_bool_gates.py`** упоминает декоратор только в комментариях, документирующих
  ровно эту слепоту; сам ходит через реестр. Не слеп.
- Shell- и py-свипов по литералу `@mcp.tool` больше нет.

**Если найдёшь пятый инструмент — это находка, назови её; но не ищи специально, популяция измерена.**

## §3. Форма выбрана уровнем (2): реестр вместо АСТ, с переиспользованием готового

Вариант (a) карты. **И половина работы уже сделана другими юнитами — не пиши её заново:**

- **MCP-сторона:** `tests/_mcp_schema.collect_tool_schemas()` строит настоящий сервер через реестр и
  отдаёт **83 тула**, включая девять `codesweep_*`. Проверено мной.
- **CLI-сторона:** `codebugs.cli.build_parser(mode="all")` — примитив, извлечённый юнитом Т-51
  специально ради того, чтобы поверхность можно было перечислить, не собирая подпарсеры руками, —
  отдаёт **67 вербов**, включая девять `sweep-*`. Проверено мной.

**Оба видят то, чего не видит АСТ.** Это ключевание на примитив: «что РЕАЛЬНО зарегистрировано»,
а не «как это записано в исходнике», и оно закрывает класс для любой будущей формы регистрации, а не
для двух известных.

**Вариант (b) карты — научить АСТ распознавать `emit_tools()` — ОТВЕРГНУТ**: это перечисление форм
регистрации, третья форма вернёт слепоту. Не бери его, даже если он покажется дешевле.

**Цена, которую надо принять и назвать вслух:** инструмент перестаёт быть чисто статическим — ему
нужен импорт пакета и рабочее окружение. Часть его метрик (LOC спанов, размеры тел) **по-прежнему
требует АСТ**, поэтому он станет ГИБРИДОМ. **Скажи в докстринге прямо, какая половина откуда**:
состав поверхности — из реестра, размеры — из АСТ. Гибрид, чья граница не названа, — это следующая
слепота.

## §4. Плюс страховка (вариант (c)), и почему она НЕ заменяет §3

У `matrix.py` есть секция **SELF-CHECK**, чьё собственное описание обещает ловить устаревание
(«sourced from tracker cards and CLAUDE.md, NOT from this script»). **Она не сработала**, потому что
ни одна её строка не утверждает ничего про поверхность `bench`/`sweep`: строки самопроверки — это
ПЕРЕЧИСЛЕНИЕ, а класс перечислением не закрыть.

Добавь строку: `sweep` и `bench` имеют непустую MCP- и CLI-поверхность. Это ловит РЕГРЕСС дёшево и
делает слепоту громкой. **Но это страховка, а не лекарство** — так и напиши рядом, иначе следующий
читатель решит, что класс закрыт строкой самопроверки.

## §5. Инварианты и границы

- **Числа матрицы обязаны ИЗМЕНИТЬСЯ, и это цель, а не регресс.** Ожидание: 16 фантомов уходят из
  `NEITHER`, ячейки `BOTH`/`MCP only`/`CLI only` растут соответственно. **Приведи ячейки до и после**
  и объясни каждую сдвинувшуюся цифру. Цифра, которую ты не можешь объяснить, — находка.
- **Не меняй смысл ячеек.** «CLI only» по-прежнему значит «MCP-клиент не дотягивается».
- **`module_surface.py` не трогать** (§2).
- **Файловое множество:** `.claude/plans/exposure-scripts/matrix.py` и, если понадобится, ОДИН
  тест-файл. Прода не касаться вовсе. Пятая правка — названная.
- Если импорт `tests/_mcp_schema` из скрипта в `.claude/plans/` окажется неудобным — **решай явно и
  назови решение**: либо импортировать, либо построить сервер тем же способом внутри matrix.py. Не
  копируй логику молча в третье место.

## §6. Мутанты — оракул

`PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, `grep -c` подтверждает применение; мутанты — в
КОПИИ вне репозитория (`Edit` у субагентов отключён).

Обязаны КРАСНИТЬ строку самопроверки: удалить регистрацию провайдера `sweep` из
`_ensure_modules_loaded`; подменить `emit_tools` заглушкой, отдающей ноль тулов.
Обязано ПРОХОДИТЬ: нетронутое дерево.
**И главная проба, доказывающая, что дефект был:** на состоянии ДО фикса матрица кладёт 16 названных
способностей в `NEITHER`; после — не кладёт ни одной. Покажи оба прогона.

## §7. ЭСКАЛАЦИИ — ПОМЕЧЕННЫЙ ПУНКТ, ОБЯЗАН СТОЯТЬ В ЭТОМ ФАЙЛЕ НА ВЕТКЕ

Отдельным заголовком, в твоей секции записи; сообщение его не заменяет. Пустой слот законен, если написан.
Эскалируй: **(а)** реестровый обход даст числа, которые ты не можешь объяснить; **(б)** гибрид
окажется невозможен без правки прода; **(в)** найдёшь пятый слепой инструмент; **(г)** окажется, что
мой префлайт §2 неверен.

## §8. Регламент

- Только worktree: `tools/worktree-setup.sh fix/t55-cb-153-matrix-registry` (имя несёт `cb-153`, К-8).
- **(3) на main не коммитит НИКОГДА (К-1).** **КОММИТЬ РАНО И ЧАСТО** — среда валит агентов; признак
  приземления — `git log`, не уведомление.
- `uv run --extra dev python -m pytest tests/ -q` в worktree; `--extra dev` не опционально.
- `tools/worktree-finish.sh` — **ИЗ КОРНЯ main**, `--merge-msg` (К-4), форма
  `Merge <branch>: <что изменилось> (CB-153)`.
- Возврат — **данные, не команда**.

---

# ЗАПИСЬ ИСПОЛНИТЕЛЯ (уровень (3)) — данные, не вердикт

## Эскалации

(пусто — ни один из четырёх триггеров §7 не сработал: реестровый обход дал числа,
которые я объяснил все до одного (см. ниже); гибрид не потребовал правки прода —
файловое множество осталось ровно `matrix.py`; пятого слепого инструмента не нашёл;
префлайт §2 (популяция слепых инструментов = один, `matrix.py`) подтвердился --
`module_surface.py` действительно не слеп, `tests/test_milestones_surface.py` и
`tests/test_strict_bool_gates.py` упоминают декоратор только в прозе/комментариях.)

## Форма (реализовано вариант (a) + страховка (c), (b) не бран)

`matrix.py` стал ГИБРИДОМ, граница названа в докстринге отдельным разделом
«THE HYBRID BOUNDARY»:

- **Состав поверхности (существование тула/верба) -- из РЕЕСТРА.** MCP -- через
  `tests/_mcp_schema.collect_tool_schemas()` (переиспользован без изменений, как
  просил бриф). CLI -- через `codebugs.cli.build_parser(mode="all")` (примитив
  Т-51/CB-146), `sub.choices` даёт имена вербов, `commands` -- реальные
  колбэки-обработчики.
- **Увязка «тул/верб -> способность» и все LOC/тир-метрики -- по-прежнему из АСТ**,
  ДЛЯ УЖЕ ДЕКОРИРОВАННЫХ/`add_parser`-детектируемых тулов; SIZE-секция ниже
  трогает ровно тот же набор `mcp_tools`/`cli_verbs`, что и раньше (байт-в-байт
  идентична до/после -- см. ниже). Не расширял её на bench/sweep: у сгенерированного
  тула нет исходного «тела обёртки», которое можно было бы партиционировать на
  docstring/code/signature -- это не «недоделал», а сознательная граница гибрида.
- **Новые (реестровые) тулы/вербы линкуются к способностям НЕ АСТ-паттерном на
  `emit_tools()` (вариант (b), отвергнут картой и брифом), а РАЗВОРАЧИВАНИЕМ РАНТАЙМ-
  ЗАМЫКАНИЯ**: обёртка `surfacegen.build_tool` всегда несёт свободные переменные
  `calls`/`manual`; какая из них не None -- то и есть настоящий вызываемый объект
  (либо сама способность, либо диспетчер вроде `_tool_bench_list`). Для диспетчера
  его АСТ-тело резолвится тем же `resolve_calls`, что уже использовался для CLI-
  хендлеров. Это не третья форма распознавания синтаксиса -- это интроспекция
  РЕАЛЬНОГО зарегистрированного объекта, поэтому закрывает класс для будущих форм
  регистрации так же, как и реестровое перечисление имён.
- **Страховка (c)**: в SELF-CHECK две новые строки -- «`bench` has non-empty MCP AND
  CLI surface» и то же для `sweep` -- с явной пометкой в тексте самой строки:
  «insurance (not the fix)» плюс комментарий в коде, что это перечисление двух
  имён модулей и класс не закрывает; закрывает класс реестровая половина выше.
- Добавлен флаг `--ast-only`, воспроизводящий СТАРЫЙ (слепой) отчёт байт-в-байт --
  нужен ТОЛЬКО для мутационной пробы №4 ниже (два заголовочных лейбла отличаются
  косметически, поведение -- нет; см. diff в проверке).

## Числа матрицы ДО и ПОСЛЕ, и объяснение каждого сдвига

Прогон на сегодняшнем main (до правки) -- ровно то, что измерил держатель в CB-153:

    BOTH surfaces:  44      MCP only: 29      CLI only:  4      NEITHER: 32
    MCP tools (AST) : 70    CLI verbs (AST): 54

После правки (реестровая половина включена по умолчанию):

    BOTH surfaces:  60      MCP only: 29      CLI only:  4      NEITHER: 16
    MCP tools (registered): 83    CLI verbs (registered): 67
    MCP tools (AST-visible): 70   CLI verbs (AST-visible): 54   (SIZE-секция -- не изменилась)

Объяснение каждой сдвинувшейся цифры:

- **BOTH: 44 -> 60 (+16).** Ровно 16 именованных карточкой способностей
  (7 bench + 9 sweep) переехали из NEITHER в BOTH -- потому что КАЖДАЯ запись в
  `bench_surface.SURFACE`/`sweep_surface.SURFACE` объявляет `mcp=` и `cli=` парой
  (проверил чтением обоих файлов), так что генерация всегда даёт способности ОБЕ
  поверхности разом, никогда одну. Ни одна не попала в MCP-only/CLI-only.
- **NEITHER: 32 -> 16 (-16).** Ровно те же 16, и только они -- сверено программно
  (множество из 16 имён карточки минус НЕТ-множество «после» = пустое; то же
  множество минус НЕТ-множество «до» = пустое, то есть все 16 были там).
  Оставшиеся 16 в NEITHER -- это уже существовавшие `blockers.*`/`db.*`/
  `findings.*_candidates`/`milestones.reconcile.*`/`reqs.batch_add_requirements`
  строки, ни одна из них не касается bench/sweep и все они не изменились.
- **MCP only: 29 -> 29, CLI only: 4 -> 4 (без изменений).** Ожидаемо: у bench/sweep
  нет способностей с ОДНОЙ поверхностью (см. предыдущий пункт), так что эти два
  множества не могли ни вырасти, ни сжаться от этой правки.
- **MCP tools (registered): 83, CLI verbs (registered): 67.** Совпадает буквально
  с числами, названными держателем в §3 брифа (83 и 67) -- я их не выдумал, а
  прогнал `collect_tool_schemas()`/`build_parser(mode="all")` лично.
- **MCP tools (AST-visible): 70, CLI verbs (AST-visible): 54 -- БЕЗ ИЗМЕНЕНИЙ.**
  83-70=13 и 67-54=13: ровно столько реестровых имён невидимы АСТ-обходу (4 bench-
  тула + 9 sweep-тулов на MCP-стороне, симметрично на CLI). SIZE-секция отчёта
  (LOC/докстринг/тело/сигнатура, T1/T2/T3-тиры, CLI-boilerplate) байт-в-байт
  идентична до/после -- проверено `diff` по срезу отчёта между `SIZE` и
  `UNDER-COUNT` заголовками, пусто.
- Секция REGISTRY-ONLY SURFACES (новая) перечисляет все 26 реестровых
  тулов/вербов, невидимых АСТ (13 MCP + 13 CLI), и для КАЖДОГО показывает, в
  какую способность он резолвится -- UNRESOLVED ни разу не встретилось.
- Необъяснённых цифр нет.

## §6 Мутанты -- обе пробы

Мутанты собраны в копии репозитория ВНЕ дерева (`Edit` у меня не отключён, но
следовал протоколу брифа буквально): `/tmp/.../scratchpad/mutant_repo/` -- копия
`src/`, `tests/`, `.claude/plans/exposure-scripts/matrix.py`, `pyproject.toml`;
запуск через `uv run` из этого worktree (тот же venv), `PYTHONDONTWRITEBYTECODE=1`,
`__pycache__` вычищен перед каждым прогоном.

1. **Убрана регистрация `sweep` из `_ensure_modules_loaded`** (закомментирован
   импорт в копии `db.py`, `grep -c sweep db.py` = 1, только сам комментарий) ->
   строка самопроверки `` `sweep` has non-empty MCP AND CLI surface `` -- MISMATCH.
   `bench` осталась OK. Красная строка ровно одна, ровно та, что ожидалась.
2. **`surfacegen.emit_tools` подменена заглушкой, отдающей `[]`** (`grep -c
   emit_tools surfacegen.py` = 1, только определение) -> ОБЕ строки самопроверки
   (`bench` И `sweep`) -- MISMATCH, потому что обе способности идут через
   `emit_tools`. Строка «MCP capture agrees with collect_tool_schemas()» осталась
   OK -- ожидаемо: обе стороны видят один и тот же (уменьшенный) реестр, расхождения
   между ними мутант не создаёт, поэтому его и не ловит; ловит именно страховка (c).
3. **Нетронутое дерево (сам worktree, без мутаций) -- оба ряда OK.** Прогнано выше
   при первой сборке отчёта.
4. **Главная проба.** На состоянии ДО фикса (`--ast-only`, что байт-в-байт
   воспроизводит старый отчёт кроме двух косметических лейблов в шапке) -- все 16
   названных карточкой способностей лежат в NEITHER. На состоянии ПОСЛЕ (по
   умолчанию, реестровая половина включена) -- ни одна из 16 там не лежит.
   Проверено программно множественным сравнением, не визуальным чтением:
   `{16 имён} ⊆ NEITHER(до)` = True, `{16 имён} ∩ NEITHER(после)` = пустое множество.

## Тесты и файловое множество

`uv run --extra dev python -m pytest tests/ -q` в worktree: 2337 passed (без
изменений от базовой линии -- matrix.py не часть тестового дерева и его импорт не
успел ничего заимпортировать в саму сессию тестов). `ruff check` над правленным
файлом -- чисто. Тест-файл НЕ добавлял: у `.claude/plans/exposure-scripts/matrix.py`
и раньше не было pytest-покрытия (это не `src/`/`tests/`), а роль регрессионного
гейта здесь играет мутационная проба §6, требуемая брифом напрямую. Файловое
множество -- ровно один файл: `.claude/plans/exposure-scripts/matrix.py`. Прод
(`src/codebugs/**`) не тронут.

## Побочная находка (не эскалация, не изменение) -- предсуществующий MISMATCH

Строка самопроверки «CB-107: reconcile_all is CLI-only, no MCP wrapper» -- MISMATCH
и ДО, и ПОСЛЕ правки, байт-в-байт одинаково. Не моя территория (другая карта,
не CB-153) и вне файлового множества брифа -- оставил как есть, называю вслух per
СС5 (структура/деталь): деталь, не мешающая структуре этого юнита.

