# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-29, смена 18

(вытесняет `HANDOFF-DIR-1-2026-08-28-session17.md`; следующий вытесняет этот)

**Правил здесь нет намеренно** — только указатели, фактическое состояние и первые действия.
В вопросах НОРМЫ handoff'у не верь: сверяй с §0 и чини handoff, а не источник.

Писался по ходу смены и коммитился в момент написания. **Законченный документ перечитан ЦЕЛИКОМ как
один текст** — что нашёл сквозной перечит, сказано в конце §7.

## 0. Нормативные входы (указатели, НЕ пересказ)

- **Роль и регламент:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, §13; §16.3 (класс по Р1, тиры); §17 про ротацию.
  **Контракт приёмки — ДЕВЯТЬ пунктов.** Файлы приёмщика местами говорят «семь»: называй число
  явно в каждом промпте приёмки. **Два его пункта здесь означают не то, что написано:** пункт 9
  ссылается на набор `_INSURANCE_PATHS`, которого здесь НЕТ (он из соседнего проекта), эквивалент —
  `ruff` плюс полный набор; пункт 6 требует механизм, который в проекте есть, и здесь это два
  серверных алярма. **За смену 18 пункт 6 закрыт ФАКТОМ дважды** — см. §3 п. 1.
- **Харнес, гейты, доктрина репо:** `CLAUDE.md`, раздел **Workflow** — несущий; **Releasing** — для
  срезов; **Testing** — правила тестов на конкуренцию и жильцы `tests/conftest.py`.
- **Артефакт направления:** `.claude/plans/DIR-1-trust-coordination.md` — §5 таблица юнитов (**её
  граница названа словами: велась по 22 августа, индексом текущего состояния не является**),
  §6 ратифицированные расхождения, §9 неизменяемые факты приёмки (**блок сессии 18 в начале**),
  §10 эскалации (**Э-34 заведена и закрыта в эту же смену**), §11 журнал (смена 18 в конце),
  §13 шаблон брифа — **пп. 18 и 19 заработаны сменой 17 и оба применены в эту смену**,
  §14 наблюдение про частоту движения `main` — **оно перестало быть теорией, см. §3 п. 3**.
- **Заготовка брифа:** `.claude/plans/L3-BRIEF-SKELETON-DIR-1.md` — начинай бриф копированием.
  Читай вместе с §13 пп. 18–19 артефакта.
- **Реестр болей:** `.claude/plans/PAIN-REGISTRY-codebugs.md`. **Пронумерованные каноны и РЕШЕНИЯ
  куратора живут там** — чисел не привожу, они растут внутри смены. **Аллокатор ID:**
  `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md`.

## 1. Что сделано за смену

| что | мерж | итог |
|---|---|---|
| Т-117 — CB-238, CB-239, CB-213: правда нормативного текста | `4fead6c` | **ПРИНЯТ С ОГОВОРКОЙ**, все три карты закрыты |
| Т-118 — CB-249, CB-244: журнал посадок и опознание вставки | `5bb8295` | **ПРИНЯТ** без оговорок, обе карты закрыты |
| CB-172: замер и эскалация Э-34 | — | решена куратором (РЕШЕНИЕ 4), вариант (1); **бриф Т-120 написан и ждёт выдачи** |
| CB-250: замер корпуса имён | — | форма из «невыбираемой» стала выбираемой |
| наследники | — | заведены CB-255 (мной), CB-252 и CB-253 и CB-258 (исполнителями) |
| CB-187 | — | дописана живым подтверждением с уликой из журнала посадок |

## 2. ПЕРВЫЕ ДЕЙСТВИЯ

1. **Сверь очередь с `git log --first-parent` и `git worktree list`.** Уведомлению не верь никогда.
2. **Прочитай §3 п. 1 и §7 этого файла до первого действия** — там два правила, которые я нарушил
   в эту смену ПОСЛЕ того, как записал их против себя.
3. **Долгов приёмки нет: оба юнита приняты, все пять их карт закрыты.**
4. **Первое дело очереди готово к выдаче: бриф Т-120 уже написан** (`.claude/plans/L3-BRIEF-DIR-1-T120-noqa-stops-pretending.md`).
   Форма решена куратором, замер снят, условие очерёдности в шапке брифа. Проверь только, что оно
   выполнено, и выдавай.

## 3. Что преемник обязан знать до того, как начнёт

1. **ПРАВКА АРТЕФАКТА В ГЛАВНОМ ЧЕКАУТЕ ЛИБО КОММИТИТСЯ В ТУ ЖЕ МИНУТУ, ЛИБО НЕ ДЕЛАЕТСЯ.** Канон
   куратора, принятый по МОЕМУ нарушению, дважды за одну ночь. Незакоммиченная правка отслеживаемого
   файла в главном чекауте отказывает ЧУЖОЙ посадке кодом 11, и автор об этом не узнаёт: отказ видит
   сосед, а не ты. Мой журнал заблокировал сначала Т-116, потом Т-119 соседнего направления; второй
   раз — **через час после того, как я записал первый случай в журнал как ошибку.** Куратор
   закоммитил файл своей рукой, чтобы разблокировать чужую посадку. **Пиши — коммить — потом думай
   дальше.** Не запускай приёмщика или долгую операцию, оставив дерево грязным: пока ты ждёшь, ты
   не читаешь сообщений и не узнаешь, что кого-то блокируешь.
2. **ПЕРЕХВАТ РАБОТЫ У ЖИВОГО ИСПОЛНИТЕЛЯ НАЧИНАЕТСЯ С ЕГО ОСТАНОВКИ.** Тоже моя ошибка. Я
   возобновил упавшего исполнителя сообщением, а потом начал доводить его посадку сам, не сказав ему
   прекратить, — и получил два процесса финиша по одному слагу. Замок это выдержал (мерж один), но
   журнал попыток получил ПАРЫ строк с разницей 1–8 секунд, а после посадки — ещё одну строку по
   слагу, чьё дерево уже снято. Улика и разбор дописаны в CB-187.
3. **ПОСАДКА СТОИТ ДОРОГО, И ЭТО ИЗМЕРЕНО. Не ищи ошибку в отказе кодом 13 — её там нет.**
   Финишу нужно 210–230 секунд неподвижности `main`; за час вокруг моей посадки `main` принял 34
   коммита, то есть движение раз в 1,8 минуты. **Шанс одной попытки около 31 %.** Мой Т-118 сел
   после пятнадцати строк журнала (девять из них rc=13), юниты соседа — с шестой и со второй.
   `exit 13` лечится буквальным повтором с тем же `--merge-msg`. **`--skip-checks` не бери** — он
   выключает ruff и набор, то есть ровно ту проверку против текущего `main`, ради которой гейт
   существует; ослабление гейта, чтобы пролезть, есть разбираемый класс внутри его же лечения.
4. **ОКНО ТИШИНЫ РАБОТАЕТ, ТОЛЬКО ЕСЛИ НАЗВАНО, КТО МОЛЧИТ И ДО КАКОГО СОБЫТИЯ.** Главная находка
   смены, §7 п. 1. Окно-соглашение («договорились не писать») было нарушено дважды подряд и оба раза
   добросовестно. Окно с безусловным запретом всем ветвям до наблюдаемого события («до слов „DIR-1
   сел SHA“») сработало с первой попытки. **Просить окно — законно и дёшево; просить его словами
   «давайте не будем писать» — бесполезно.**
5. **Проверяй утверждения ПРОГОНОМ, и строже всего — СВОИ СОБСТВЕННЫЕ ЧИСЛА.** За смену четыре
   случая счёта, чья область у́же вопроса, и **два из них мои**: я опроверг чужое ложное число своим
   ложным числом (§7 п. 2), и я доложил куратору «семь попыток», посчитав свои вызовы вместо строк
   журнала. Форма одна: **доверие к объявлению вместо пересчёта предмета**, и внимательностью она не
   лечится — оба раза мы были внимательны.
6. **Полный набор на состоянии передачи — 3228 пройденных, ноль упавших**, снято приёмщиком Т-118 в
   независимом клоне на `5bb8295`; `ruff check src/ tests/` чист, ruff ровно `0.15.7`.

## 4. Очередь преемнику

**0. Т-120 — CB-172, вариант (1). БРИФ УЖЕ НАПИСАН, форма решена куратором, замер снят.**
`.claude/plans/L3-BRIEF-DIR-1-T120-noqa-stops-pretending.md`. Снять шесть мёртвых маркеров
`# noqa: S608` и привести утверждение `CLAUDE.md` к правде. Тир понижен до `sonnet` намеренно:
суждение израсходовано на замер и выбор варианта. **Карту CB-172 НЕ закрывать** — она остаётся
открытой и перенацелена на вариант (2), включение правила, принятый долг с измеренной ценой
(52 места, 17 файлов, первая в проекте конфигурация правил, плюс ~50 мёртвых маркеров других
правил). Условие очерёдности — в шапке брифа.

**1. CB-255** (`medium`, моя) — собственный тест алярма «дерево двинулось под прогоном» даёт ложный
красный ровно от того условия, о котором алярм сообщает. Наблюдено при посадке Т-118, воспроизведено
в изоляции (27 passed при красном в полном наборе). Вред: ложный красный неотличим от настоящего без
отдельного расследования, появляется под нагрузкой и стоит на пути посадки. **Форма НЕ выбрана**,
три кандидата названы в карте.

**2. CB-258** (`low`, от исполнителя Т-118) — тест оракула CB-226 создаёт каталог с `chmod 0300` в
живом дереве на время вложенного прогона, второй читатель дерева спотыкается; красный один раз из
семи. **Форма намеренно не выбрана: дешёвый путь может обессмыслить тест.** Родня CB-255 — обе про
тесты, ломающиеся от параллельной работы; **посмотри, не одна ли это карта, прежде чем брать обе.**

**3. CB-253** (`low`) — внутри перечня, у которого Т-117 снял совокупный счёт, остался счёт одного
маркера («eleven measured spellings»), и он **не определён**: список читается и как одиннадцать, и
как двенадцать, смотря одно ли написание `eval`/`exec` или два. Уедет попутной правкой.

**4. CB-252** (`low`, от исполнителя Т-118) — `install()` читает настройки незащищённым
`json.load(open(...))`. Уедет с ближайшим касанием `tools/install-simplify-gate.py`.

**5. CB-250** (`low`) — хук `commit-msg` не опознаёт заметку, названную в КОНЦЕ предложения.
**Замер корпуса снят мной: 263 заметки, ВСЕ оканчиваются на `.md`, ни одна не несёт байта вне класса
имени.** Значит ложный отказ достижим на ста процентах заметок, а не в углу. Форма стала выбираемой;
доказательство «по построению» я прошёл ЧТЕНИЕМ и потому НЕ засчитываю — строить состояния обязан
юнит. Ошибка в безопасную сторону, поэтому карта остаётся `low`.

**6. CB-240** (`low`) — не перепроверял ни я, ни предшественник; карта несёт построенное состояние
от смены 16.

**7. CB-167** (`low`) — фильтр `meta_key` строит путь JSON без кавычек. Воспроизведено предшественником
на живом трекере: `query(meta_key="loc.v")` вернул 243 строки при населении 244, честный ответ ноль.
**Живёт в `src/codebugs/findings.py` — территория соседнего направления**; куратор решением от
28 августа оставил карту за DIR-1, но перед диспетчеризацией посмотри, не работает ли сосед в этом
файле прямо сейчас.

**8. CB-187** (`medium`) — не моя очередь, но я дописал в неё живое подтверждение. Форма выбрана в
самой карте: (a) неблокирующий замок на ветку поверх замка на интеграцию, (b) честный текст для
случая «мой каталог исчез». Обе дёшевы, и после этой ночи обе выглядят своевременно.

## 5. Скоропортящееся — перепроверить на месте

Статусы карт; состав рабочих деревьев (`git worktree list`, **никогда** уведомление); отставание
`origin/main`; хвост аллокатора. Ветка `feature/t49-bt6-third-pilot-annotations` — запаркованный
пилот, решение владельца, не трогать. **Ни одна ветка никогда не удаляется**, включая мои две.

**Тег `salvage/agent-a20d255a5d5895ce8-2026-08-28` отправлен на `origin` мной в начале смены** —
до этого он жил только на этой машине, то есть факт приёмки «ни одна строка не потеряна» держался
на одном диске. Проверять больше не нужно, но знай, что он там.

**В главном чекауте неотслеживаемыми остаются `.claude/settings.json` и его резервная копия — файлы
владельца, не трогать.** Они не блокируют посадку (финиш называет их «not a blocker»).

**Рабочие деревья соседнего направления в `.worktrees/` — не твои.** Мои два сняты финишем.

## 6. Инструментальное

- **Пуш делает ДЕРЖАТЕЛЬ, ОТДЕЛЬНОЙ командой сразу после посадки** — составная бывает отклонена
  целиком. Попадание проверяй не порядком коммитов, а
  `git merge-base --is-ancestor <мерж> origin/main`. **За смену я отправил на `origin` чужой мерж
  `751fd23`, пролежавший неотправленным**: пункт 6 контракта закрывают серверные алярмы, а они не
  видят коммита, пока он не отправлен.
- **`exit 11` — ДВЕ РАЗНЫЕ ПРИЧИНЫ.** Грязный `main` по ОТСЛЕЖИВАЕМЫМ файлам повтором не лечится
  (это §3 п. 1). Но тот же код возвращается, когда НЕОТСЛЕЖИВАЕМЫЙ файл в `main` совпадает по пути
  с тем, что ветка ДОБАВЛЯЕТ, — этот случай лечится переносом файла и повтором. Читай тело стража, а
  не его имя.
- **`exit 13` — верный отказ**, буквальный повтор с тем же `--merge-msg`. Не меняй команду.
- **Не запускай `uv run` в главном чекауте, пока может идти чужой финиш.** Для разовых замеров —
  `./.venv/bin/ruff`, `./.venv/bin/codebugs`, `./.venv/bin/python`.
- **Прямой вызов `sqlite3` из командной строки запрещён политикой окружения.**
- **`tools/worktree-probe.sh` не существует**, флага `--items` у `tools/worktree-setup.sh` нет —
  оба из соседнего проекта. **`worktree-setup.sh` клеймит ВСЕ карты, названные в имени ветки**, а не
  одну; мой бриф Т-117 утверждал обратное и был неправ.
- **Журнал посадок — `.worktrees/landing-attempts.log`**, gitignored, локален для клона. Строка:
  метка времени, слаг, код возврата. Идентификатора процесса в ней НЕТ, поэтому «чей это вызов» из
  журнала не выводится — только сколько их было.
- **Установщик вставки — `tools/install-simplify-gate.py`**, режим аудита без аргументов.
- **Минт: `--dry-run` не резервирует номер.** Между моим `--dry-run` и минтом сосед занял номер, и я
  получил Т-120 вместо Т-119, написав имя файла заранее. **Сминтить, ПОТОМ писать.** Второй такой
  случай в реестре — значит правило не соблюдается не по забывчивости одного человека.

## 7. Записи против меня

1. **Главная находка смены получена ценой двух моих нарушений подряд, и это стоит читать вместе.**
   Окно тишины, охраняемое соглашением, было нарушено дважды: сперва соседом, добросовестно
   исполнявшим канон немедленных коммитов, потом сессией, о существовании которой не знал никто.
   Вывод — **«соглашение не может обеспечить соглашение»**, тот же запрет, что «проза не может
   обеспечить прозу», на уровень выше. Но моя собственная роль в нём непочётная: **я сам дважды
   заблокировал чужие посадки незакоммиченной правкой**, причём второй раз — через час после того,
   как записал первый случай в журнал как свою ошибку. Запись против себя была сделана и не
   сработала как защита. Это лучшее доказательство тезиса, какое я мог бы предъявить, и оно
   получено самым дорогим способом.
2. **Я опроверг ложный счёт другим ложным счётом.** Брифу Т-117 я дал как ЗАМЕР сумму «11 + 6 + 6 =
   23», сложив ОБЪЯВЛЕННЫЕ подсчёты вместо пересчёта предметов. Один из слагаемых оказался неверен
   (карта CB-253). Спасла форма, а не аккуратность: я предписал «снять число, а не пересчитать», и
   только поэтому в нормативный документ не приземлилось свежее ложное число — внутри юнита, чей
   предмет есть ложные счета.
3. **Я доложил куратору «семь попыток», посчитав свои вызовы, а не строки журнала.** Их пятнадцать.
   Четвёртый за смену случай счёта, чья область у́же вопроса; три предыдущих я записал против других.
4. **Я перехватил посадку у живого исполнителя, не остановив его**, и породил два параллельных
   финиша по одному слагу (§3 п. 2).
5. **Сквозной перечит этого handoff как одного текста** нашёл одно расхождение: §1 сперва называл
   Т-120 «выданным», тогда как он только написан и ждёт выдачи, а §4 двумя экранами ниже говорил
   правду. Механизм CB-235 в моём собственном тексте — ровно тот, которым занята вся смена; поправил,
   ошибку оставляю записью. Больше сквозной перечит не нашёл ничего, и я отношусь к этому с
   подозрением, как велит правило.
