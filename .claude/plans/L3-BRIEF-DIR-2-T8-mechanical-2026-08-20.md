# L3-BRIEF Т-8 — механическая пара CB-114 + CB-115 (из x2-ревью BT-4)

Хендофф держателя направления DIR-2 (уровень 2) менеджеру задачи (уровень 3). 2026-08-20.
Спека ролей: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
(§1, §2, §4, §7, §13). Направление: `.claude/plans/DIR-2-findings-identity.md`.
Механическая полоса: две независимые точечные правки, не принадлежащие нерешённому решению
(BT-4 уже ратифицирован владельцем 2026-08-20). Авторитет статусов — трекер.

## Тройка П-Б

**Замысел.** Две кодовые находки x2-ревью BT-4, судья-verified, от ратификации не зависят:
документация не лжёт про исполняемую семантику; заявленный дизайном индекс существует и на
свежих базах.

**Задание-гипотеза:**
1. **CB-114**: докстринг `import_findings` (`findings.py:1049-1051`) утверждает «fixed/stale
   would REOPEN»; исполняемое множество `_REOPEN_STATUSES = ("fixed",)` (`findings.py:61`),
   `stale` — live и просто бампает. Поправить прозу под исполняемые множества. Если
   докстринг попадает в описание MCP-инструмента — регенерировать wire golden.
2. **CB-115**: `SCHEMA` объявляет колонку `reported_at_ref`, но индексный блок (`:50-53`)
   не несёт `idx_findings_reported_at_ref`; оба места создания живут в миграционных путях,
   которые свежая база не проходит (`:141` — внутри условного ребилда; `:156-160` — под
   `if "reported_at_ref" not in cols`, ложным на свежей базе). Добавить
   `CREATE INDEX IF NOT EXISTS` в индексный блок SCHEMA (аддитивно, идемпотентно).
   Тест: свежая in-memory база несёт индекс (сегодня проверка вернёт его отсутствие —
   тест обязан падать на откате).

**Критерий приёмки:** докстринг согласован с `_REOPEN_STATUSES`/`LIVE_STATUSES`; свежая
база получает индекс (тест по `sqlite_master`), мигрированная — не ломается (индекс
идемпотентен); wire golden регенерирован, если описание инструмента изменилось; никакой
поведенческой правки — обе правки не меняют ни одного исполняемого пути (индекс — perf).

## Предпосылки (добыты x2-ревью 2026-08-20, судья перепроверил лично; preflight advisory — за тобой)

Все file:line выше — из вердикта судьи (`review-x2-bt4-judge-verdict.md`), верифицированы
Codex-атакующим, защитником и судьёй независимо. Карты: CB-114, CB-115 (meta несёт
провенанс ревью). Прочитай обе при pick.

## Границы, сериализация

- Файлы: `src/codebugs/findings.py` (два непересекающихся региона: докстринг импорта ~:1049
  и SCHEMA-блок ~:29-53), тесты, возможно wire golden. НЕ файлы DIR-1 (`merge.py`,
  `milestones/`, `blockers.py`, `bench.py`, `cli.py`, `server.py`, `tools/`).
- **Параллельно идёт Т-9** (tags-контракт, тот же `findings.py`, регион `_bump_row`
  ~:521-600 и `import_findings` ~:1120-1150!). Т-8-правка CB-114 сидит в докстринге
  `import_findings` — рядом с регионом Т-9. Порядок финишей сериализуется flock'ом;
  форвард-мердж main в worktree решит текстовые пересечения — но НЕ занимай больше, чем
  сказано: только докстринг и SCHEMA-блок.
- Wire golden — точка сериализации с DIR-1 и Т-9: перед регенерацией проверь in-flight.

## Клейм и протокол

CB-114 (CLM-19), CB-115 (CLM-20) — держит DIR-2 (agent, /home/faxik/w/codebugs).
Непосредственно перед setup: `claims_release` обеих с этим трипло; затем
`bash tools/worktree-setup.sh fix/cb-114-cb-115-review-mechanical` — setup возьмёт
branch-клеймы на обе карты (имя ветки несёт оба id). Exit 3 → СТОП и доклад. Аборт после
снятия — вернуть клеймы DIR-2.

## Исполнение и возврат

Исполнитель — свежий субагент (П-В), только worktree, TDD, `uv run --extra dev python -m
pytest tests/ -q` из worktree, ruff 0.15.7. Интеграция: `tools/worktree-finish.sh
fix-cb-114-cb-115-review-mechanical '...'`. После мерджа: обе карты в `fixed` (§13 п.7,
автоснятие клеймов). Возврат — тройка: результат; доказательства (мердж-SHA, тесты,
мутационные пробы обеих правок — индексный тест падает на откате, докстринговая правка
пинуется тестом сверки прозы с `_REOPEN_STATUSES`, если дешёвый тест возможен — иначе
скажи явно, что пин структурно нечем); эскалации — слот обязателен, пустой — явно.