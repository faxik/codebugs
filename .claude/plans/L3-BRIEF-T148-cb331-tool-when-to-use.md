# Бриф юнита Т-148 — CB-331: «когда пользоваться» в описаниях инструментов

```
class:    ЛЯГУШКА с одним усилением (правка только текстов, поведения нет, по образцу)
model:    sonnet (исполнитель); держатель — opus, уровень (3)
branch:   docs/cb-331-tool-when-to-use
worktree: /home/faxik/w/codebugs/.worktrees/docs-cb-331-tool-when-to-use
card:     CB-331 (medium, mcp_surface) — клеймо держит эта ветка
```

## 0. Замысел (от куратора)

Агент, впервые видящий список инструментов codebugs, по ОПИСАНИЯМ сам понимает, в какой ситуации
нужен каждый инструмент малоиспользуемых семейств, — тогда будущий замер самостоятельного
использования мерит нужность, а не незнание. Замер куратора 23.09: самостоятельных вызовов этих
семейств — единицы, почти все вызовы шли по подсказке; описания говорят ЧТО, ни одно — КОГДА.

## 1. Перечень (42 инструмента на `main` @ `53b79ec`)

- семейства по префиксу: `codesweep_*` (9), `codemerge_*` (9), `codebench_*` (4), `milestone_*` (12);
- `pull_next`, `wip_status`;
- векторы: `reqs_embed`, `reqs_batch_embed`, `reqs_search_similar`, `reqs_embedding_stats`;
- одиночные: `blockers_check`, `claims_held_by`, `relations_unrelate`.

## 2. Что сделать

1. В докстринг MCP-обёртки (то, что уходит клиенту как description) каждого инструмента перечня —
   короткая фраза о СИТУАЦИИ применения, начинающаяся с `Use when` (английский, как все описания).
   Место — сразу после первой строки-резюме (вторым абзацем), чтобы её видел и клиент, обрезающий
   длинные описания. Одна-две фразы; механику не повторять; ситуацию называть языком задачи
   агента («a review of many files that will outlive this session»), а не языком реализации.
   Для парных/обратных операций — назвать пару (`relations_unrelate` ↔ `relations_relate`,
   `pull_next` ↔ `release_item`, `codemerge_start` → … → `codemerge_finish`, `codesweep_next` ↔
   `codesweep_mark`, `reqs_embed` ↔ `reqs_search_similar`; ищи остальные сам). Для векторов —
   честно: вектор вычисляет ВЫЗЫВАЮЩАЯ сторона, у пакета нет поставщика эмбеддингов.
   Где инструмент — одноразовый ремонт (`milestone_reconcile`), фраза говорит, в какой ситуации
   он нужен, и что в обычной работе — нет.
2. `INSTRUCTIONS` в `src/codebugs/server.py`: дописать ситуации для векторов (вектор вычисляет
   вызывающая сторона) и для снятия ошибочной связи (`relations_unrelate`), назвать
   `claims_held_by` («что держит агент X»), и одну строку: для просмотра списков `query`/`recent`
   брать `view="summary"`, полную карточку — `get`. Стиль и длина — как у соседних пунктов;
   тест `tests/test_server.py::TestInstructionsNamedToolsExist` требует, чтобы каждое имя в
   обратных кавычках было живым инструментом (`view` как токен в кавычках его уронит — пиши
   `` `query(view="summary")` ``, голова сведётся к `query`).
3. Тест `tests/test_cb331_tool_when_to_use.py`: перечень берётся ИЗ РЕЕСТРА сервера
   (`tests._mcp_schema.collect_tool_schemas()` — живой каталог через производственный
   регистратор): семейства — отбором по префиксу из живых имён, одиночные — объявленным набором,
   каждое имя которого обязано присутствовать в каталоге (иначе набор сгниёт молча). Проверяет:
   (а) у каждого — `Use when` в описании; (б) предпосылки: семейства непусты, число не меньше
   ожидаемого нижнего порога; (в) объявленные пары: описание одной стороны называет другую.
   Проба порчей обязательна: удалить фразу у одного инструмента → тест краснеет; удалить имя пары
   → краснеет тест пар; переименовать одиночный в наборе → краснеет проверка присутствия.
   Выводы прогонов — в отчёт.
4. Снимок `tests/golden/mcp_schema.json` пересобрать осознанно:
   `PYTHONPATH=src uv run --extra dev python tests/dump_schema.py > tests/golden/mcp_schema.json`
   из рабочего дерева; в диффе снимка меняются ТОЛЬКО описания инструментов перечня (проверь:
   ни одной `inputSchema` не тронуто). Прогони `tests/test_exposure_matrix.py`.
5. `CHANGELOG.md`, `[Unreleased]` — одна запись для пользователя (раздел `Changed`).

## 3. Чего не делать

- Не менять ни одной строки кода, кроме докстрингов и строки `INSTRUCTIONS`. Нормализацию
  описаний (`server._NormalizedDescriptions`, `dedent_docstring`), сторожа и промежуточные слои НЕ
  трогать. Если окажется, что без этого нельзя, — ОСТАНОВИСЬ и доложи (это слон).
- Не передавать `description=` в регистрацию — только докстринг (иначе обход нормализации, CB-164).
- Не трогать `CLAUDE.md` (оба файла правил у потолка).
- Не сливать, не удалять ветку: посадку делает держатель.

## 4. Отчёт держателю

Коммит(ы) на ветке; таблица проб порчей с выводом; подтверждение, что в снимке изменились только
описания; результат `test_exposure_matrix.py`; полный набор и `ruff check` в рабочем дереве
(`uv run --extra dev ...`) — последние строки вывода.

## 5. Итог (дописан держателем 2026-09-23 после посадки)

Приземлено мержем `925fdb4`; CB-331 закрыта (`fixed`). Коммиты ветки: `8afc5e4` (исполнитель
`sonnet`: 42 докстринга, `INSTRUCTIONS`, снимок, `CHANGELOG`, тест) и `6b5aa4d` (правки держателя
после `/simplify`). В снимке схем менялись только описания, ни одной `inputSchema`; сравнение
синтаксических деревьев девяти затронутых модулей с `main` при замене строк заглушкой совпало —
изменились только строки. Перечень в брифе ошибался на единицу: `milestone_*` — 11 инструментов, не
12; всего 42 верно.

**Тест и проба порчей.** `tests/test_cb331_tool_when_to_use.py` берёт семейства отбором по префиксу
из живого каталога, одиночные — объявленным набором с проверкой присутствия. Три порчи исполнителя
(фраза у `blockers_check`, имя пары в `codesweep_next`, опечатка в имени одиночного) — каждая
роняет ровно свой тест; порчу фразы `blockers_check` держатель повторил сам — красный, 1 упал.

**`/simplify`** (4 проверяющих `sonnet`): принято четыре правки — `blockers_check` больше не
называет себя «альтернативой `pull_next`» (он только сообщает, а `pull_next` закрепляет работу и
списывает загрузку — буквальное прочтение ломало учёт); фраза `codesweep_status` пересказывала первую
строку; у `milestone_close` двойное отрицание; каталог в тесте собирался пять раз, теперь один
(`functools.cache`). В `INSTRUCTIONS` исправлен порядок векторных шагов: сначала поиск
`reqs_search_similar`, `reqs_embed` — сохранение. Отклонено: общий помощник «каталог по имени» для
трёх тестовых файлов — вне диффа. Фаза 3 (`/reflect`) не выполнялась: в этом репозитории нет её
очереди, класс — лягушка.

**Холодный читатель.** Ключи и сценарии записаны до прогонов, не менялись. Один прогон на клетку.

| Условие | Модель | До | После |
|---|---|---|---|
| A: прямой вопрос, полные описания | haiku | 12/12 | 12/12 |
| A: прямой вопрос, полные описания | sonnet | 12/12 | 12/12 |
| A: только имена + `INSTRUCTIONS` | haiku | 11/12 | 11/12 |
| A: только имена + `INSTRUCTIONS` | sonnet | 11/12 | 12/12 |
| B: задача без подсказки, полные описания | haiku | 10/12 | 9/12 |
| B: задача без подсказки, полные описания | sonnet | 12/12 | 10/12 |
| B: задача без подсказки, только имена + `INSTRUCTIONS` | haiku | 7/12 | 10/12 |
| B: задача без подсказки, только имена + `INSTRUCTIONS` | sonnet | 10/12 | 11/12 |

Условие «только имена + `INSTRUCTIONS`» — то, в котором агент реально работает (инструменты
codebugs отложены, описание приходит только после сознательной загрузки). Выше шума одного прогона
лишь одна клетка: haiku, набор B, 7 → 10 — выиграли «новое требование → поиск похожих», «релиз
не закрыть раньше времени → веха», «что держит агент X» — ровно ситуации, дописанные в `INSTRUCTIONS`.
Остальные сдвиги ±1–2 в пределах шума; просадки «B, полные описания» — это выбор соседнего
инструмента (`blockers_query` вместо `blockers_check`, `categories`+`batch_add` вместо обхода),
а не непонимание. Устойчивые промахи во всех условиях: B1 «аудит 40 файлов» — haiku во всех
четырёх прогонах не берёт никакой инструмент (задача не упоминает трекер, и агент о нём не
вспоминает); B5 «приземлил то, чего ждали другие» — `blockers_check` почти не выбирается. Фраза
`blockers_check` и строка `INSTRUCTIONS` переписаны ПОСЛЕ замера по находке `/simplify`; повторно
не мерились, чтобы не подгонять текст под сценарии, — B5 теперь не независимая улика.

**Вывод:** описания узким местом не были (полные описания и до правки давали потолок); в реальном
условии работает только приветственный текст, и его дополнение дало заметный прирост у слабой модели;
оставшееся узкое место — агент не вспоминает о трекере посреди задачи, которая о трекере не говорит.
Мерить это может только замер по транскриптам через 2–3 недели — и лишь после переустановки
сервера владельцем (`pipx install --force /home/faxik/w/codebugs`), иначе агенты видят старые тексты.
