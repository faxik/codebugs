# L3-BRIEF DIR-1 / Т-51 — CB-146: у CLI-поверхности появляется ловец

**Класс по Р1: B** — оракул ДЕТЕРМИНИРОВАН: каждый мутант поверхности обязан краснить снимок, и это
проверяется прогоном, а не рассуждением. **Стартовый тир: sonnet, effort medium**, бюджет попыток 6.
Упрёшься в решение, а не в работу — эскалируй мне.
Автор брифа: держатель DIR-1 (уровень (2)), 2026-08-23.
Карты: **CB-146** (главная) и **CB-148** (её живой симптом закрывается тем же ловцом — см. §2).
Читай обе через `mcp__codebugs__get`. Нормативы: `CLAUDE.md` — **Workflow**, **CLI**,
**MCP tool registration** (там образец того, как в этом репозитории живёт голден и как он
регенерируется), **Testing**.

---

## §1. Что не так, измеренным числом

    модуль     тестов   тестов, вызывающих ВЕРБЫ   тест на `--mode <модуль>`
    sweep        83              0                        нет
    merge        70              0                        нет
    blockers     64              0                        нет
    relations    20              0                        нет
    claims       34              6                        нет

Потеря верба, съеденная `help`-строка, изменившийся `dest`, `type` или `choices` **не краснят сьют
вообще**. `tests/test_relations.py::test_relations_is_registered_in_all_three_hardcoded_lists`
выглядит покрытием и им не является: он утверждает `"relations" in inspect.getsource(cli.main)` —
статическую строку в исходнике, а не исполнение верба.

Это перестало быть теоретическим: два модуля (`bench`, `sweep`) теперь строят свою CLI-поверхность
ГЕНЕРАТОРОМ (`surfacegen.py`) — арка В-5(а) закрыта, но эти две миграции остаются в дереве. У
порождённой поверхности стоящего ловца нет вовсе; во время пилотов её держал авторский снимок
внутри юнита, и он ушёл вместе с юнитом.

## §2. Форма выбрана уровнем (2): вариант (a) карты, но снимок ГЕНЕРИЧЕСКИЙ

**Структурный снимок CLI-поверхности ВСЕХ доменов против отдельного голден-файла.**

Ключевое отличие от того, что просится само: **пинуются ВСЕ атрибуты каждого argparse-действия, а
не список из шести.** Я измерил это на пине 3.14.4 перед тем, как писать бриф: `vars(action)` даёт
одиннадцать атрибутов — `dest`, `option_strings`, `nargs`, `default`, `type`, `choices`,
`required`, `help`, `metavar`, `const`, **`deprecated`**.

Отсюда следствие, ради которого форма и выбрана: **тот же ловец закрывает живой симптом CB-148.**
Та карта говорит, что `emit_cli` пробрасывает ключевые слова argparse насквозь без allowlist, и
`deprecated=True` меняет видимую пользователю поверхность, оставаясь невидимым для утверждения,
судившего четыре атрибута поимённо. Генерический снимок видит его, потому что видит всё.

**Это ключевание на ПРИМИТИВ вместо перечисления** — правило, которое этот репозиторий переучивал
уже трижды (CB-142, мутант R1 второго пилота, шапка CASCADE-IDS). Не заменяй его списком атрибутов:
список — это то же перечисление, только в новом месте.

**Отвергнуто и почему:** (c) «минимальный ратчет: состав вербов + непустой `help=`» закрывает
названный подкласс и оставляет открытым класс — а карта заводилась именно на отсутствие ловца;
(b) поведенческие тесты на каждый верб дороже и проверяют другое (исполнение, а не декларацию) —
они законны, но это отдельная работа, и она не про эту карту; (d) «ничего не делать» отвергнуто
владельцем де-факто, раз CB-146 в пакете.

**Цена названа прямо, а не обнаружена потом:** правка любого `help`-текста теперь требует
регенерации CLI-голдена. Это ровно та цена, которую репозиторий уже платит за wire-golden, и
регенерация обязана быть ОДНОЙ командой, названной в докстринге теста, по образцу
`tests/dump_schema.py`.

## §3. Инварианты

- **Домены берутся из РЕЕСТРА CLI-провайдеров, а не из захардкоженного списка.** Иначе новый домен
  не попадёт в снимок и escape'ится молча — та же дыра, что закрывает `matrix.py` своим
  self-check'ом. Список доменов в тесте — это перечисление; реестр — примитив.
- **Парсер строится через РЕАЛЬНЫЙ путь** (`cli.py`, режим `--mode <домен>`), а не сборкой
  подпарсеров руками. Снимок должен отражать то, что видит пользователь.
- **`type` сериализуется по имени** (`int`, `str`, …). Безымянный callable (lambda, partial) —
  **громкий отказ генератора снимка**, а не `repr`: `repr` лямбды несёт адрес, голден начнёт
  хлопать, и ловец превратится в шум, который отключат.
- **Порядок действий сохраняется** — перестановка аргументов меняет `--help` и обязана краснить.
- **Существующие тесты не ослабляются**, `tests/test_relations.py` не удаляется (он проверяет
  регистрацию, пусть и статически, — это другое утверждение).
- **Голден кладётся отдельным файлом** рядом с `tests/golden/mcp_schema.json`; wire-golden не
  трогается вовсе.

## §4. Ловушки

1. **Снимок, собранный не через `--mode`, а прямым вызовом `register_cli`** — пропустит дефект
   проводки в `cli.py` и будет зелёным на сломанном верхнем уровне.
2. **Захардкоженный список доменов** — см. §3, главная ловушка этого юнита.
3. **Список атрибутов вместо `vars(action)`** — воспроизводит ровно тот дефект, который юнит
   закрывает (см. §2).
4. **`-h`/`--help` присутствует в каждом парсере** — включай, он стабилен; исключение его руками
   есть ещё одно перечисление.
5. **Нестабильная сериализация** (адреса, множества без сортировки, `dict` с недетерминированным
   порядком) — голден начнёт хлопать. Сортируй там, где порядок не несёт смысла, и НЕ сортируй
   порядок действий, где он смысл несёт.
6. **Бюджет правок ≤4 на дерево:** генератор снимка (может жить в `tests/`), голден-файл, тест,
   и — если понадобится — четвёртая **названная**.
7. **Мутанты — в КОПИИ вне репозитория** (`git archive` / клон в scratchpad); `Edit` у субагентов
   отключён, обходить это записью в рабочее дерево не нужно.

## §5. Мутанты — оракул. Каждый прогоняется, ни один не рассуждается

`PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, **`grep -c` подтверждает применение мутанта**.
Обязаны КРАСНИТЬ:
1. удалён один `add_parser` (верб исчез);
2. стёрта `help=` у одного аргумента;
3. переименован `dest`;
4. изменён `type` (`int` → `str`);
5. добавлен `deprecated=True` к одному аргументу — **это мутант CB-148, он обязан краснить**;
6. переставлены местами два `add_argument` в одном вербе;
7. добавлен новый домен с вербом, отсутствующим в голдене.
Обязано ПРОХОДИТЬ: нетронутое дерево; регенерация голдена документированной командой возвращает
зелёный.

## §6. ЭСКАЛАЦИИ — ПОМЕЧЕННЫЙ ПУНКТ, ОБЯЗАН СТОЯТЬ В ЭТОМ ФАЙЛЕ НА ВЕТКЕ

**Отдельным заголовком, в твоей секции записи. Сообщение его НЕ ЗАМЕНЯЕТ** — приёмщик судит
артефакт. Пустой слот законен, если написан.

Эскалируй: **(а)** окажется, что снимок нестабилен между прогонами по причине, которую ты не
можешь устранить сортировкой; **(б)** реестр CLI-провайдеров не даёт списка доменов и его придётся
обходить; **(в)** генерический снимок ловит что-то, что законно меняется само (тогда это находка о
поверхности, а не о тесте); **(г)** поймёшь, что закрытие CB-148 этим ловцом неполно — тогда
скажи, что именно осталось, картой это оформлю я.

## §7. Регламент

- Только worktree: `tools/worktree-setup.sh fix/t51-cb-146-cli-surface-catcher`. **Имя несёт
  `cb-146` (канон К-8)** — сетап клеймит карту по регулярке из имени ветки ДО создания дерева;
  вернёт **3** — фатально, скажи мне.
- **(3) на main не коммитит НИКОГДА (К-1).** Факт своей приёмки — в этот бриф, на ветке.
- **КОММИТЬ РАНО И ЧАСТО.** Среда сегодня валит агентов пачками; незакоммиченное — единственное,
  что обрыв стоит. Признак приземления — `git log`, не уведомление.
- `uv run --extra dev python -m pytest tests/ -q` в worktree — **`--extra dev` не опционально**.
- `tools/worktree-finish.sh` — **ИЗ КОРНЯ main**. TDD-ветка → **`--merge-msg` обязателен (К-4)**,
  форма `Merge <branch>: <что изменилось> (CB-146)`.
- Параллельно идут юниты DIR-2. Пересечений быть не должно; появятся — **скажи, не разруливай**.
- Возврат — **данные, не команда**.

---

# ЗАПИСЬ ИСПОЛНИТЕЛЯ (уровень (3)) — данные, не вердикт

## Что сделано

Три файла (генератор/голден/тест) + одна НАЗВАННАЯ правка production-файла (`src/codebugs/cli.py`),
двумя коммитами на ветке `fix/t51-cb-146-cli-surface-catcher`:

- `34b89ad` — исходная поставка: `tests/cli_surface.py`, `tests/golden/cli_surface.json`,
  `tests/test_cli_surface.py`, и извлечение `build_parser()` из `cli.main()`.
- `2f036f8` — самопроверка нашла реальную дыру в собственном же ловце (не в CB-148 буквально, а в
  ТОЙ ЖЕ форме дефекта) и закрыла её тем же приёмом: `type(action).__name__` под ключом
  `action_class`.

### Правка production-файла (названа явно)

`src/codebugs/cli.py:103` — новая функция `build_parser(mode="all", pre_parser=None)`, извлечённая
из тела `main()`. Она строит дерево подпарсеров ТЕМИ ЖЕ ДВУМЯ ПРИМИТИВАМИ, что и `main()` — вызов
`_register_builtins(sub, commands)` и цикл `for provider in db.get_cli_providers(mode=mode):
provider.register_fn(sub, commands)` (`cli.py:139`) — так что дефект проводки в любом из них виден
снимку. `main()` теперь просто вызывает `build_parser(mode=pre_args.mode, pre_parser=pre_parser)`
вместо инлайновой сборки; поведение не изменилось (полный сьют зелёный до и после, включая оба
`inspect.getsource(cli.main)`-теста — см. ниже, почему они не пострадали).

**Почему список `--mode` НЕ переехал в `build_parser`, хотя мог бы.** `tests/test_relations.py::
test_relations_is_registered_in_all_three_hardcoded_lists` и `tests/test_grouping_surface.py`
утверждают `"relations"/"grouping" in inspect.getsource(cli.main)` — то есть судят ИМЕННО исходник
`main()`, буквально. Если бы список `choices=[...]` со всеми именами доменов переехал в
`build_parser`, эта строка пропала бы из тела `main()` и оба теста бы упали. Поэтому список
`--mode`/`--tracker-root` остался инлайново внутри `main()` без изменений (буква тестов сохранена),
а `build_parser` принимает уже построенный `pre_parser` как родителя — без второй копии списка
доменов и без риска рассинхронизации. Прогнан полный сьют — оба теста зелёные (см. числа ниже).

## Ответы на два вопроса

**(а) Домены берутся из РЕЕСТРА, а не из списка — где именно.**
`tests/cli_surface.py:146` вызывает `cli.build_parser(mode=mode)` (по умолчанию `mode="all"`), а
внутри неё `cli.py:139` делает `for provider in db.get_cli_providers(mode=mode): provider.
register_fn(sub, commands)`. `db.get_cli_providers()` — это чтение реестра `_cli_providers`
(`db.py:159`), заполняемого каждым доменным модулем через `register_cli_provider()` при импорте.
`collect_cli_surface()` нигде не перечисляет имена доменов — она вызывает
`db._ensure_modules_loaded()` (импортирует известные модули, что заставляет их
зарегистрироваться) и дальше идёт целиком через реестр. Проверено прогоном: с `mode="all"` снимок
содержит 67 вербов из ВСЕХ 13 зарегистрированных провайдеров (`bench, blockers, claims, findings,
grouping, loc, merge, milestones, provenance, relations, reqs, similarity, sweep`), и мутант №7
(добавлен новый самозарегистрированный домен `_mutant_fake_domain` + строка в
`_ensure_modules_loaded()`) подтвердил это экспериментально: новый верб `fake-verb` появился в
снимке автоматически (без правки `tests/cli_surface.py`) и тест покраснел, потому что его нет в
голдене.

**(б) Безымянный callable в `type=` — что именно и что нашлось.**
`tests/cli_surface.py::_serialize_type` (строки ~64–80): `None` → `None`; иначе читает
`getattr(type_obj, "__name__", None)`. Если имени нет ИЛИ оно буквально `"<lambda>"` (лямбда ИМЕЕТ
`__name__`, просто равное этой строке — проверил прогоном, поэтому проверка на голое наличие
`__name__` не сработала бы) — генератор **бросает `UnserializableArgumentType`**, громко, а не
падает в `repr`. Иначе сериализует как `module.qualname` (builtins сокращается до голого имени:
`int`, `str`).

Реальный обход всего `src/codebugs/*.py` (`grep -n "type="`) нашёл ОДИН нестандартный `type=` —
`grouping.py:788: type=_hub_degree_arg` — это именованная модульная функция (не лямбда, не
`functools.partial`), сериализуется как `codebugs.grouping._hub_degree_arg`. Лямбд и `partial` в
`type=` в текущем дереве НЕТ. `UnserializableArgumentType` в проде сегодня НЕ срабатывает — это
инвариант на будущее, проверенный отдельно: сконструировал `add_argument(type=lambda x: x)` в
одноразовом REPL и убедился, что генератор бросает исключение с текстом, называющим объект,
а не тихо кладёт `repr` с адресом в голден.

## Мутанты — что прогнал и что каждый показал

Все восемь — включая находку (б) ниже — применены К КОПИЯМ вне репозитория (`git archive HEAD` в
`/tmp/.../scratchpad/cb146-mutants/`, копия `.venv` в каждую), никогда в самом дереве. Каждое
применение подтверждено `grep -c`/`grep -n` на мутировавшем файле ПЕРЕД прогоном.

| № | Мутация | Файл:строка | Результат |
|---|---|---|---|
| 1 | удалён `add_parser("where", ...)` | `cli.py` (builtins) | КРАСНЫЙ — verb `where` пропал |
| 2 | стёрта `help=` у `--limit` в вербе `get` | `findings.py:4389` | КРАСНЫЙ — `help: "..." -> None` |
| 3 | `dest="trigger_kind"` на `--trigger-type` в `blockers-add` | `blockers.py:733` | КРАСНЫЙ — `dest` изменился |
| 4 | `type=int` → `type=str` на `blocker_id` в `blockers-resolve` | `blockers.py:748` | КРАСНЫЙ — `type: "int" -> "str"` |
| 5 (=CB-148) | `deprecated=True` на `--threshold` в `similarity-check` | `similarity.py:477` | КРАСНЫЙ — `similarity-check[4].deprecated: False -> True`, единственная строка diff |
| 6 | переставлены `--repo`/`--note` в вербе `claim` | `claims.py:791-792` | КРАСНЫЙ — позиции в списке `actions` разъехались |
| 7 | новый домен `_mutant_fake_domain` + строка в `_ensure_modules_loaded()` | `db.py` + новый файл | КРАСНЫЙ — верб `fake-verb` появился в снимке, отсутствует в голдене |
| 8 (моя находка, см. (б)-раздел выше и коммит `2f036f8`) | `action="append"` добавлен к `--tags` (иначе не тронут) в вербе `add` | `findings.py:4330` | КРАСНЫЙ — `add[7].action_class: '_StoreAction' -> '_AppendAction'` |

**Обязано ПРОХОДИТЬ, тоже прогнано:**
- нетронутое дерево (копия без мутации) — зелёный, `1 passed`;
- регенерация голдена документированной командой (`PYTHONPATH=src uv run python
  tests/cli_surface.py > tests/golden/cli_surface.json`) на мутанте №5 и на мутанте №8 по отдельности
  — оба возвращают `1 passed` немедленно после регенерации.

## Числа

- Полный сьют в рабочем дереве (после ОБОИХ коммитов): `uv run --extra dev python -m pytest tests/
  -q` → **2326 passed** (~110s), без единого падения или skip.
- `uv run --extra dev ruff check src/ tests/` → **All checks passed!**
- Снимок: **67 вербов** из **13** зарегистрированных доменов, `tests/golden/cli_surface.json` —
  4760+ строк (после добавления `action_class` — не пересчитывал построчно, файл регенерирован
  документированной командой, размер тот же порядок).
- Бюджет правок (брифа §4 п.6, ≤4 в дереве): 3 файла (генератор, голден, тест) + 1 названная
  (`src/codebugs/cli.py`, извлечение `build_parser`) = 4. Второй коммит НЕ добавил новый файл —
  только доправил уже посчитанные генератор+голден.

## Остатки / не сделано

- Верхнеуровневые флаги `--mode`/`--tracker-root` (сам `pre_parser` из `main()`) НЕ входят в снимок
  — `build_parser()` для снимка строит их с `pre_parser=None` (голый `add_help=False`), потому что
  эти два флага не влияют на СОСТАВ подпарсеров для данного `mode` и не относятся к измеренному в
  §1 классу дефектов (потеря верба/`help`/`dest`/`type`/`choices`). Если это когда-нибудь понадобится
  — отдельное расширение снимка, не в бюджете этого юнита.
- `has_handler` в снимке (верб зарегистрирован в `sub`, но отсутствует в `commands`) добавлен как
  дешёвая доп. проверка сверх требования брифа — ни один мутант её не бьёт целенаправленно (все
  register_cli в дереве держат этот инвариант сами), но она ничего не стоит и ловит соседний класс
  дефекта (верб виден в `--help`, падает `KeyError` при вызове).

## Эскалации

Пусто. Ни одно из четырёх условий §6 не сработало:
- (а) — снимок стабилен между прогонами (несколько независимых регенераций дали побайтово
  идентичный голден; `container` — единственный источник нестабильности — структурно исключён).
- (б) — реестр CLI-провайдеров дал полный список доменов без обхода (см. ответ (а) выше).
- (в) — не встретил ничего, что генерический снимок ловит, но что законно меняется само по себе.
- (г) — самостоятельно нашёл и закрыл ОДНУ дополнительную дыру той же формы, что CB-148 (мутант
  №8, `action=` меняет класс Action без изменения `vars(action)`), не дожидаясь картирования —
  это не остаток CB-148, а новая находка внутри собственного решения; исправлена в этом же юните
  (коммит `2f036f8`), так что эскалировать нечего. Если держатель DIR-1 сочтёт, что она заслуживает
  отдельной карты для протокола (а не молчаливого исправления внутри Т-51) — это его решение, не
  моё; я решил закрыть на месте, потому что дыра была в СОБСТВЕННОМ артефакте этого юнита, а не в
  постороннем коде.
