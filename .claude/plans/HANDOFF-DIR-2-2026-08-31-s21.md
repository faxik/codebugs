# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), смена 21 (2026-08-31)

(вытесняет `HANDOFF-DIR-2-2026-08-31-s20.md`; следующий вытеснит этот)

**Правил здесь нет намеренно** — только указатели, факты и первые действия. Стартуй от НОРМАТИВНЫХ
файлов, а не от пересказа.

## 0. Нормативные входы — УКАЗАТЕЛИ; сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §12 (два ОБЯЗАТЕЛЬНЫХ раздела брифа), §13 (приёмка, девять пунктов), §16.3 (Р1–Р7),
  §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md`, раздел
  «СМЕНА 21» и далее — он подробнее этого handoff'а и писался по ходу.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- **Каноны направления** — в артефакте, раздел «Шаблон юнит-брифа DIR-2 — обязательные пункты»
  (К-1 … К-11); каноны куратора К-12 … К-32 — в реестре болей.
- **Правила репозитория:** `CLAUDE.md` — слой правил, плюс архив обоснований в
  `docs/claude-md-rationale/`. Номера строк, снятые до мержа `f439989`, мертвы.
- **Аллокатор номеров:** `tools/cascade-mint.sh --prefix Т --dry-run`.
- **Готовый к раздаче бриф — ОДИН, и он с шапкой СТОП:**
  `.claude/plans/L3-BRIEF-DIR-2-cb200-cli-domain-errors.md`. Раздавать НЕЛЬЗЯ, пока не сделаны
  четыре правки, перечисленные в самой шапке. Номер юнита намеренно не минчен.

## 1. Первое действие, до всякой работы

**Сверяй трекер ЧЕРЕЗ КОД ЧЕКАУТА, а не через MCP.** Причина не изменилась и стоит первым пунктом
второй handoff подряд: сервер MCP поднят из устаревшего снимка `pipx`, поэтому на части запросов
отвечает по СТАРОМУ коду, и всякая проверка починки через него даёт ложный отрицательный результат.
Команда:

```
uv run python -c "import codebugs.db as db, codebugs.findings as f; c=db.connect(); r=f.query_findings(c, tag='dir-2', status='open', limit=200); print(r['total'])"
```

**Эта причина есть ровно та задача, что стоит первой в очереди** (см. §2): выпуск 0.3.0 делает
версию снимка отличимой от чекаута.

**И перед КАЖДОЙ записью в `main` применяй проверку «идёт ли чужой финиш»** — три условия вместе,
форма и доводы в артефакте (раздел смены 20), рабочий скрипт восстанавливать по ней: скрэтчпад
сессии умирает вместе с сессией. Проверено мною запуском в обе стороны, см. §4.

## 2. СОСТОЯНИЕ СТОЛА

**Долгов приёмки нет. В полёте ничего. Захватов направления нет, рабочих копий DIR-2 нет.**

**ГОЛОВА ОЧЕРЕДИ — НЕ КАРТА, А ЗАДАЧА ВЛАДЕЛЬЦА: ВЫПУСК 0.3.0.** Пришла от куратора вне очереди,
слово владельца получено. Принята мною, НЕ исполнена — заморозка пришла в тот же час. Полная
тройка П-Б записана в артефакте направления, раздел «ЗАДАЧА ВЛАДЕЛЬЦА ВНЕ ОЧЕРЕДИ», и продублирована
в реестре болей. Коротко: поднять `version` в `pyproject.toml` и `__version__` в
`src/codebugs/__init__.py`; перетитловать `## [Unreleased]` в `## [0.3.0] — <дата>` с пустым
`[Unreleased]` сверху и человеческим абзацем-хайлайтом в начале; юнит обычным порядком через
рабочую копию и финиш; **после посадки** — аннотированный тег `v0.3.0` на МЕРЖ-коммит из главного
чекаута; владельцу передать одной строкой `pipx reinstall codebugs`. Окно посадки — через преемника
куратора. По путям с соседским Т-131 (переезд текста `CLAUDE.md`) не пересекается.

**Номер: аллокатор предлагал `Т-134` на момент записи.** Спрашивай в момент, когда номер нужен: за
эту смену реестр пополнялся соседями.

**Набор на `main`:** перемерян мною дважды прогоном — **3358** на `d7d054b` и **3366** на `efe3a43`
(после посадки соседского Т-133, принёсшего восемь тестов). Линтер чист. Перемеряй на своей вершине.

**Мои коммиты смены:** `d528d98` (запись смены в артефакт), `12298d3` (бриф CB-200 с шапкой СТОП),
плюс этот и пересобранный стартовый промпт. Записей в код нет: смена ушла в замеры, решение и атаку.

## 3. Очередь — 20 открытых карт с меткой `dir-2` (было 19, я завёл CB-286)

Порядок ПО ИЗМЕРЕННОМУ ВРЕДУ, а не по метке.

0. **ВЫПУСК 0.3.0** — вне очереди, задача владельца, см. §2.
1. **CB-200** (`low`, вред перемерен мною) — бриф написан, атакован и НЕ роздан. Четыре правки в
   шапке брифа обязательны до раздачи. **Осторожно: `exit 0` из прошлого handoff'а ОПРОВЕРГНУТ**
   (см. §4) — не наследуй эту формулировку.
2. **CB-280** (`medium`) — **форма ВЫБРАНА мною: (a), объявить новую семантику правдиво.** Довод и
   замер в артефакте; довод сильнее карточного. Осталось написать бриф.
3. **CB-281** (`low`) — пять утверждений, переживших код. Естественно уезжает одним юнитом с CB-280:
   те же функции, та же правда текстов.
4. **CB-286** (`medium`, заведена мною этой сменой) — арма `except ValueError` внутри
   `import_findings` глушит порчу хранимых данных и выдаёт её за плохую строку CSV, при этом
   остальные строки коммитятся. Форма CB-99 в другой ветви дерева исключений. Ребро `found_during`
   на CB-200. **Провенанс честный: замер построен адверсарием, мною НЕ воспроизведён** — воспроизвести
   до починки.
5. **CB-276** (`medium`) — **не бери как «переписать карту»: это неисполнимо.** У `update_finding`
   нет параметра `description` вовсе (описание — вход отпечатка тождества). Общая форма — CB-265,
   и её порог УЖЕ снят составом работы. Подробности в артефакте, раздел «ПУНКТ 4 ОЧЕРЕДИ
   НЕИСПОЛНИМ».
6. **CB-265** (`low`) — поправка живёт в `meta.notes`, читается первым неизменяемое описание. Порог
   снят: юнит по CB-280 и так касается `findings.py` и поверхности чтения. Форма (a) — производный
   безусловный ключ `corrections` — карте нравится, но **решать её надо в брифе, и отдельным юнитом
   от CB-280**: смешение двух решений в одном юните лишает приёмку возможности сказать, какое
   ратифицировано (довод взят из самой CB-276).
7. **CB-184, CB-269, CB-172** (`medium`) — не трогались; состояние в артефакте прошлых смен.
8. Прочие низкие: CB-274, CB-263, CB-261, CB-260, CB-259, CB-257, CB-256, CB-251, CB-229, CB-228,
   CB-188, CB-180.

**Закрыто этой сменой:** ничего. Заведено: CB-286.

## 4. Сессионные факты, которых нет в файлах

- **«`exit 0`» ИЗ ПРОШЛОГО HANDOFF'А ОПРОВЕРГНУТ.** Прошлый handoff и стартовый промпт называли
  вред CB-200 так: «три команды дают сырой трейсбек И `exit 0`». Сплошной перебор пар «верб ×
  аргумент» (202 прогона, 68 вербов, прибор вшит приложением в бриф) даёт **10 прогонов на 6 вербах
  с трейсбеком, ноль с `rc=0`** — все `rc=1`, то есть код ошибки ввода. Устояло под атакой (432
  прогона рецензента, структурный обход, чтение `cli.main`). **Сама карта CB-200 этого никогда не
  утверждала** — формулировка появилась в передаточных текстах. Замысел очереди уцелел: вред в
  другом (23 хендлера без перехвата из 68).
- **СЕРВЕР MCP ПО-ПРЕЖНЕМУ ИЗ УСТАРЕВШЕГО СНИМКА `pipx`.** Не изменилось; требует руки владельца
  или выпуска 0.3.0.
- **Гетерогенной рецензии (Codex) в этой смене НЕ БЫЛО.** Две попытки, ни одного связного вердикта:
  релей вопреки прямому запрету запустил `codex exec` В ФОНЕ и вернулся без ответа; мой собственный
  запуск в переднем плане с суженной задачей набирал улики и не дошёл до сводки за 590 секунд.
  **Практическое:** запрет «не фоном» надо ставить релею условием возврата, а не строкой промпта —
  мой релей прочёл запрет и всё равно ушёл в фон.
- **Сторож «идёт ли чужой финиш» проверен мною двусторонне и окупился на живом состоянии.** Премисса
  условия (1) подтверждена построенным исполняющимся сценарием: интерпретатор ДЕРЖИТ файл в таблице
  дескрипторов. Четвёртый обманщик (процесс, несущий имя сценария в командной строке и не
  исполняющий его) сторожем не назван. На живом состоянии условие (3) доложило чужой прогон набора
  при СВОБОДНОМ замке — то есть по замку я прочёл бы «путь свободен» и записал бы под чужим прогоном.
- **Окно тишины уровня (1) под Т-133 исполнено обеими вещами**; Т-133 сел мержем `100ab6c`. Сторож
  события строй на базовой линии, снятой ДО наблюдения (у меня — «ветка стала предком `main`»,
  условие ложное на старте), иначе он сработает вхолостую.
- **`grep` здесь — `ugrep`,** читает `.gitignore` и молча теряет попадания. Счётные утверждения
  делать обходом синтаксического дерева.
- **Модели:** адверсарий — `opus` (окупился, см. §5), Codex/Sol — не довёл дважды. Замеры и обходы
  делал сам, потому что их результат я повторяю как факт. `Fable` не использовалась.

## 5. Что я оставляю преемнику названным — записи против себя

Форма «утверждение шире замера» сработала у меня **четырежды**, и ни разу её не поймала
бдительность — все четыре поймал МЕХАНИЗМ. Это подтверждает рабочую гипотезу предшественницы, и я
передаю её дальше усиленной.

1. **Первая редакция прибора не измерила ничего.** Заполняла только ОБЯЗАТЕЛЬНЫЕ аргументы, поэтому
   необязательные фильтры-словари — ровно семья CB-200 — не опрашивались вовсе. Вернула «2 верба»;
   число выглядело результатом и не значило ничего. Поймано сверкой ответа прибора со списком карты.
2. **Мой прибор испачкал корень главного чекаута.** Запускал вербы с рабочим каталогом = корень, и
   `export-csv` записал туда `codebugs_export.csv` по умолчанию. Поймал **алярм CB-215**, не я. Вред
   померен и оказался нулевым (имя не подпадает под образцы сторожа мусора, с путями чужих веток не
   совпадает) — но грязь я развёл в минуты чужого прогона. **Правило шире случая: у всякого прибора,
   запускающего вербы пакета, рабочий каталог обязан лежать ВНЕ главного чекаута.**
3. **Я перемерил ОПИСАНИЕ карты и унаследовал её ЗАМЕТКУ.** Заметка CB-200 несёт ПРЕДПИСАНИЕ
   исполнителю («указателей в `CHANGELOG` два»); сегодня их **один**, второй уже перевёл соседний
   юнит. Поймалось программной проверкой цитаты, затеянной ради самой цитаты. **Правило: заметка
   карты есть такой же унаследованный текст, как описание, и перемеру подлежит наравне с ним.**
4. **Моё число «шесть хендлеров со своей арматурой» ложно как факт о дереве — их десять.** «Шесть»
   было «арматура И НЕТ `domain_errors`»; пересечение из четырёх я посчитал и не показал. Именно оно
   ломает тотальность разбиения. Замер был верен для СВОЕГО вопроса; вопрос был у́же дерева. Поймал
   адверсарий, не я.
5. **И почти пятая, не состоявшаяся.** Прочитав комментарий, вокруг которого заведена CB-280, я
   решил, что приёмщик преувеличил, и собрался выпустить поправку к его находке. Перечитав фразу
   как одно предложение, признаю: его чтение по меньшей мере столь же естественно, и честный вердикт
   — «фраза допускает два чтения, и это и есть дефект». **Я чуть не выпустил поправку к ВЕРНОЙ
   находке, и форма моей поправки была бы та же самая.**

**Вывод, который я передаю усиленным.** Правило предшественницы — «правильный ответ на „я снова
спутал слои“ не „буду внимательнее“, а „где прибор, спрашивающий снизу?“» — в этой смене получило
четыре независимых подтверждения. Но у него есть уточнение, оплаченное пунктом 4: **прибор ловит
только тот вопрос, который в него заложен.** Мой прибор спрашивал снизу и всё равно дал ложное
число, потому что я задал ему два непересекающихся вопроса и не задал третий. Отсюда практическое:
**прибор, чей ответ ты собираешься повторить как факт, обязан пройти через ЧУЖУЮ атаку** — не ради
проверки числа, а ради проверки ВОПРОСА.

## 6. Хозяйство

Чисто по отслеживаемым: рабочих копий DIR-2 нет, захватов нет, моих правок в главном чекауте ноль.
Неотслеживаемое в корне на момент записи — `.claude/settings.json`,
`.claude/settings.json.bak-20260827-185712` (лежали до моей смены, не мои) и `acceptor-norm-pending/`
(появился при мне, соседский). Мой собственный `codebugs_export.csv` убран, см. §5.2.

Стартовый промпт `PROMPT-DIR-2-next-session.md` пересобран под смену 22 в этом же заходе.
