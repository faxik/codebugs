# L3-БРИФ DIR-2 / Т-67 — три гейта, которые НЕ МОГУТ ВЫСТРЕЛИТЬ (CB-166, CB-164, CB-165)

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2 «Identity находок».
**Полоса:** механическая, но с оговоркой — карта CB-166 защищает ДОСЛОВНУЮ просьбу владельца,
поэтому юнит не является чисто латентным.
**Карты:** **CB-166** (medium, `open`), **CB-164** (medium, `open`), **CB-165** (low, `open`).
**Класс по Р1: B** — оракул детерминированный и уже НАЗВАН числами: держатель прогнал мутанты и
знает, какие из них сегодня выживают. **Стартовый тир: opus, effort low**, бюджет попыток 4.
Объёмное (прогоны сьюта, гребёнки) делегируй `sonnet`/`haiku`. **Fable не использовать.**

**Ветка:** `tools/worktree-setup.sh fix/cb-166-gates-that-cannot-fire` из КОРНЯ репозитория — он же
возьмёт клейм из имени ветки (К-8; клеймит ХАРНЕС, пре-клейм направлением запрещён). Остальные две
карты назови в тексте.
**Веди заметки в `/tmp/t67-manager-notes.md` и коммить рано и часто** (К-11).

---

## 1. Общая форма всех трёх — и почему они собраны в один юнит

Все три карты — один класс: **проверка присутствует, а выстрелить не может**. Репозиторий знает этот
класс под разными именами (`TestKnownLimits` был вакуозен, потому что фикстура не проверяла
собственное существование; `additionalProperties: True` сделал golden непригодным как гейт на форму
ответа; CB-157 — «декоративный аргумент»). Здесь три новых экземпляра, найденных за одни сутки, и
собраны они вместе потому, что читаются как одна мысль, а правятся в трёх независимых местах.

**Против себя, чтобы ты понимал цену:** три правки в трёх файлах — это ровно на границе того, что
уместно в одном дереве. Не добавляй четвёртую. Если по дороге найдёшь ещё экземпляр класса — он
идёт в слот эскалаций, а не в дифф.

## 2. CB-166 — главная. Просьба владельца не запинена, и это ИЗМЕРЕНО мутантом

**Что просил владелец** (карточка CB-95, дословно, 2026-08-18): «can we have some more stable and
robust fingerprint of location than just a line in a file? **Ideally, with auto-resolution of the new
line.**» Ради этой фразы построен весь механизм BT-7: дизайн, четыре юнита нарезки, отдельный юнит
Т-56 ради видимости якоря в обычном чтении, backfill на двух живых трекерах. Карточка CB-95 закрыта
держателем 2026-08-25 — поведение поставлено и работает.

**Чего нет.** Держатель прогнал три мутанта в одноразовом отсоединённом worktree. Результат:

- **М-A, «разрешили, но номер не отдали»:** в функции `_summary` (`src/codebugs/loc.py`) поле
  `"line"` возвращает `None`, при этом `"resolved"` остаётся `True`. Целевая батарея
  `tests/test_loc_read_paths.py` — 39 passed, 0 failed. **Полный сьют — 2436 passed, 0 failed.
  МУТАНТ ВЫЖИЛ.**
- **М-B, «сдвиг на единицу»** (`resolution["line"] + 1`): убит, но СЛУЧАЙНО — три падения вызваны
  тем, что на путях с `resolution["line"] is None` выражение бросает `TypeError`, обогатитель его
  перехватывает и подменяет всю сводку на `unavailable`. На благополучном пути сдвиг не ловится
  ничем.
- **М-C, «замороженный резолвер»:** убит по существу (6 падений), но ВСЕ они — на сценарии `git mv`
  файла, то есть ловится ПЕРЕЕЗД ФАЙЛА, а не номер строки. На фикстуре без переезда тот же мутант
  прошёл бы.

**Гребёнка подтверждает:** во всём `tests/` ассерций вида `anchor["line"]` ровно четыре, все в
`tests/test_loc.py` и все на объекте, полученном ПРЯМЫМ вызовом захвата (`capture()`), то есть на
СОХРАНЁННОМ номере. В самой батарее путей чтения — **ноль**.

**Почему батарея выглядит полной.** Пин `TestAnOrdinaryGetSaysWhereTheCodeWent` хороший: он
проверяет, что карточка с переехавшим файлом отдаёт новый путь при вызове БЕЗ АРГУМЕНТОВ. Но он
ассертит `path`, `stored_path`, `loc_status`, `moved_file`, `resolved`, `channel`, `survived` — и не
`line`. И не мог бы: переезд файла номер строки не двигает.

**Гарантия держится КОМПОЗИЦИЕЙ двух элементов, а не проверкой композиции.** Элемент первый —
ядровой тест `tests/test_loc.py::test_a_line_pushed_down_by_an_edit_above_it_is_moved_with_the_new_number`;
элемент второй — структурный AST-пин `TestThereIsExactlyOneResolver`. Между ними стоит `_summary`,
и именно там М-A теряет номер, не тронув ни один из элементов. Это дословно правило `CLAUDE.md`:
«a check that validates elements cannot validate their composition».

**Задание:**
1. Поведенческий тест НА ПУТИ ЧТЕНИЯ: завести карточку с якорем, **вставить строки ВЫШЕ якоря в том
   же файле** (не переезд файла!), закоммитить, прочитать карточку обычным `get` **без аргументов**
   и сверить **КОНКРЕТНОЕ новое число**. Он обязан убивать и М-A, и М-B.
2. Утверждение, что при `resolved: true` номер присутствует и целочислен, — там, где это уместно по
   форме батареи. Оно ловит только М-A, поэтому пункт 1 им НЕ заменяется: два теста закрывают разные
   мутанты, и проба это показала числами.
3. Проверь, нужен ли аналогичный пин на `query`/`staleness_check` под флагом. Если считаешь, что не
   нужен, — скажи это в отчёте с обоснованием, а не умолчи.

**Оракул:** поставь М-A и М-B сам, покажи в отчёте, что каждый краснеет, и назови ЧИСЛА до и после.
Мутант ставится и откатывается в дереве ветки, не на main.

**Границы:** поведение `loc.py` НЕ меняй. Это юнит про батарею. Если по дороге обнаружишь, что
поведение неверно, — это эскалация и остановка, а не правка.

## 3. CB-164 — снимок провода является РЕКОНСТРУКЦИЕЙ провода

**Механизм.** Боевой сервер регистрирует тулы ЧЕРЕЗ адаптер: в `src/codebugs/server.py` строится
`registrar = _NormalizedDescriptions(server)`, и провайдеры регистрируются на нём. Генератор снимка
делает не это: `collect_tool_schemas` в `tests/_mcp_schema.py` строит ГОЛЫЙ `MCPServer(provider.name)`,
регистрирует провайдера прямо на нём, а нормализацию применяет САМ, уже к результату —
`normalize_description(t.description or "")`. То есть снимок проходит МИМО починенного кода и
повторяет его работу своими руками.

**Следствие измерено, и не держателем.** Менеджер юнита Т-63 записал в карту CB-156 дословно:
«adapter reverted to dedent-only -> caught ONLY by the new adapter-level test and the surface tests,
**golden stayed GREEN** (the golden is generated through tests/_mcp_schema, not through the adapter —
that is why the adapter-level assertion had to exist)». Покрытие держится композицией трёх катчеров,
ни один из которых не является снимком.

**Почему это больше, чем замечание о покрытии: опасность УЖЕ ОПИСАНА В ДЕРЕВЕ и охраняется одним
СОГЛАШЕНИЕМ.** Докстринг `src/codebugs/surfacegen.py`, секция «WHY `__doc__` AND NOT `description=`»,
говорит: «`server._NormalizedDescriptions` dedents a tool's docstring only when the caller passes no
`description=`, while the golden collector registers on a RAW server and dedents by itself. A
generated tool passing `description=` would therefore match the golden byte for byte and still ship
un-dedented text to clients — CB-73 resurrected behind the very gate built to catch it.» Вывод, к
которому автор пришёл, — дисциплина: эмиттер выставляет `__doc__` и не передаёт `description=`.
Соглашение соблюдено в единственном известном сайте и ничем не энфорсится. Правило репозитория на
этот счёт сформулировано без исключений: **«Prose cannot enforce prose»**.

**Задание.** Регистрируй в `collect_tool_schemas` через тот же адаптер, что и боевой сервер, и убери
ручной вызов нормализации. После этого снимок записывает то, что реально уходит клиенту: тул,
передавший `description=`, попадёт в снимок НЕ нормализованным, и существующий гейт CB-156 в
`tests/test_boundary.py` немедленно назовёт его нарушителем.

**ОЖИДАЕМАЯ ЦЕНА НАЗВАНА ЗАРАНЕЕ, и это твой главный оракул: снимок не должен сдвинуться НИ НА
БАЙТ.** Сегодня все тулы получают описания из `__doc__`, поэтому адаптер и ручной вызов обязаны
давать один результат. **Если golden сдвинулся — это НАХОДКА, а не помеха: значит где-то УЖЕ есть
тул, чьё описание уходит на провод не тем, что записано в снимке. Остановись, зафиксируй, эскалируй.
Не регенерируй поверх.**

**Второй оракул, обязательный:** поставь мутант — сними адаптер с боевой регистрации в `server.py` —
и покажи, что ТЕПЕРЬ краснеет сравнение с golden, тогда как ДО твоей правки оно оставалось зелёным.
Это и есть доказательство, что гейт научился стрелять. Мутант откати.

**Замечание, которое сэкономит тебе время:** в `collect_tool_schemas` уже стоит комментарий, честно
называющий ДРУГУЮ слепую зону этого генератора (по одному серверу на провайдера, поэтому коллизия
имён тулов между провайдерами не ловится). Не трогай её и не чини — это отдельное, объявленное
решение. Твоя — только провенанс нормализации.

## 4. CB-165 — аллоулист не самоудаляется

**Что стоит.** В `tests/test_boundary.py` тест, охраняющий результат CB-156, начинается со строки
`prose_sections_left_alone = {("codesweep_add", "Returns:")}` и внутри цикла делает безусловный
`continue` по попаданию в это множество. Ни один тест не спрашивает, существует ли ещё тул
`codesweep_add`, несёт ли он ещё секцию `Returns:` и является ли она всё ещё прозой, ради которой
исключение выписано.

**Почему это дефект.** Ряд аллоулиста — обещание, что в одном названном месте гейт намеренно не
применяется. Обещание перестаёт быть верным ровно тогда, когда особый случай исчезает, и с этой
минуты ряд освобождает от проверки то, что её прошло бы, — молча и навсегда. Правило репозитория про
`SURFACE_GAPS` формулирует вторую половину дословно: «a hole declared for an argument that is in fact
present fails the gate too, so the list cannot rot into permission to skip a surface».

**Образец с ОБЕИМИ половинами лежит в этом же дереве** — `tests/test_strict_bool_gates.py`, таблица
`DECLARED_EXCEPTIONS` и два теста при ней:
`test_every_declared_exception_carries_a_non_empty_reason` («a table without reasons is the same hole
this ratchet exists to close, one level up») и
`test_every_declared_exception_still_names_a_real_non_strict_bool_param`, чей докстринг формулирует
принцип: «Self-deleting table: once a direction fixes its parameter, the row must be REMOVED, not
left to rot.» Второй образец — свежий и из этого же направления: механизм юнита Т-61 несёт
самоудаляющуюся таблицу `ASSEMBLED_BY_THE_WRAPPER`, и она оправдалась немедленно, сама потребовав
удалить протухший ряд (5→4).

**Задание.** Приведи аллоулист к форме образца: обе половины — ряд обязан по-прежнему называть
существующий тул с существующей секцией такой формы, и ряд обязан нести непустое обоснование. Они
закрывают РАЗНЫЕ отказы, поэтому нужны обе.

**Оракул:** протухший ряд (назови несуществующий тул) валит сьют; ряд без обоснования валит сьют;
сегодняшний законный ряд сьют не краснит.

**Это ДОЛГ ЮНИТА, а не сторонняя находка** — требование самоудаления было вменено брифом Т-63 и не
исполнено, а сьют остался зелёным, потому что отсутствие самоудаления по построению ничего не
краснит. Запиши это в отчёт как факт, он принадлежит направлению.

## 5. Гейты, сериализация, окружение

**Сериализация.** Параллельно идёт юнит Т-66 (CB-62), который двигает СОДЕРЖИМОЕ обоих golden
(`tests/golden/mcp_schema.json` и `tests/golden/cli_surface.json`), добавляя значения в описания
параметров; DIR-1 двигает их тоже. Ты трогаешь ГЕНЕРАТОР одного из них, а не содержимое. Если после
форвард-мерджа main в твоё дерево golden конфликтует — регенерируй ИЗ СВОЕГО worktree:
`PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json`.
**Но помни §3: после ТВОЕЙ правки снимок обязан быть байт-в-байт равен тому, что дал бы форвард-мердж
без неё. Разделяй эти два источника изменения и умей сказать в отчёте, что именно двинулось и от
чего.** Из worktree голый `python` дотянется до main через editable-установку и снимет ЧУЖОЕ дерево —
только `PYTHONPATH=src uv run`.

**Сьют и ruff в worktree — только с `--extra dev`**, иначе там нет ни pytest, ни ruff. Пин ruff
0.15.7.

**`CHANGELOG.md`, попутная мелочь, названная вслух:** под `[Unreleased]` сегодня стоят ДВА подряд
заголовка `### Fixed`. Если твоя запись ложится в эту секцию — слей их в один. Отдельной карты у
этого нет намеренно; если Т-66 сольёт их первым, просто ничего не делай.

## 6. Границы

- **Поведение продукта не меняется ни одной строкой**, кроме двух строк генератора снимка в §3
  (`tests/_mcp_schema.py` — тестовая инфраструктура) и попутного слияния заголовков CHANGELOG.
  `src/codebugs/loc.py` не трогать вовсе.
- Файловое множество: `tests/test_loc_read_paths.py`, `tests/_mcp_schema.py`,
  `tests/test_boundary.py`, `CHANGELOG.md`. **Расширять запрещено.** Нашёл смежный дефект — в слот
  эскалаций.
- Не «заодно» чинить другие аллоулисты в `tests/`: популяция голых множеств-исключений НЕ ИЗМЕРЕНА и
  записана на CB-165 как то, что обязано предшествовать сплошной починке. Если сделаешь гребёнку —
  числа в отчёт, диффы не трогать.

## 7. Сообщение мерджа

`--merge-msg` **обязателен** (ветка почти наверняка пойдёт по TDD, а деривация субъекта берёт первый
коммит первопарентной линии, при TDD это «red tests»). Форма:
`Merge fix/cb-166-gates-that-cannot-fire: разрешённый номер строки, снимок и аллоулист теперь умеют падать (CB-166, CB-164, CB-165)`

## 8. Отчёт менеджера (≤25 строк; улики — в файл на ветке)

- **Класс задачи и модель** (§13 п.8): класс по Р1, стартовый тир, сработавшие эскалации тира с
  признаком, финальный тир. «Не пинован» — тоже факт и это дефект.
- **Эскалации** — заполнить ЯВНО; пусто — тоже явно.
- **Числа по каждому из трёх мутантов** (М-A и М-B по §2, адаптерный по §3, два по §4): красный до
  правки / красный после — с указанием, какой именно тест ловит.
- **Отдельной строкой: сдвинулся ли `tests/golden/mcp_schema.json`, и если да — от чего именно.**
- Факт своей приёмки (3)←(4) пиши в юнит-бриф **НА ВЕТКЕ**. **Уровень (3) на main не коммитит —
  никогда** (К-1).

---

## 9. Приёмка уровня (3). Т-67, 2026-08-25, НА ВЕТКЕ

Юнит выполнен менеджером напрямую, без уровня (4): три правки в трёх файлах, каждая тестовая,
ни одна не потребовала отдельного исполнителя.

**Класс по Р1: B**, как и объявлено брифом. Стартовый тир — opus, effort low. **Эскалаций тира
не было**; финальный тир — opus. Объёмное (гребёнка по `tests/`) ушло на `sonnet`, суждение и
все мутантные пробы остались на opus. Fable не использовался.

### 9.1. Числа

Отсчёт: **39** тестов в целевой батарее, **2436** во всём сьюте. После — **43** и **2442**.

**CB-166.** Мутант **М-A** (в `loc._summary` поле `line` возвращает `None`, `resolved` остаётся
`True`) до правки прошёл ВЕСЬ сьют: 39 passed в батарее, 2436 passed целиком — ровно то число,
что назвал бриф. После правки его валят пять тестов; первым —
`TestTheNewLineNumberIsAnswered::test_a_line_pushed_down_by_an_edit_above_it_is_read_back_at_its_new_number`
с сообщением `assert None == 7`.

Мутант **М-B** (`resolution["line"] + 1`) до правки валил три теста, и все три — побочно: на путях,
где `resolution["line"] is None`, выражение бросает `TypeError`, обогатитель его перехватывает и
подменяет сводку на `unavailable`; ловилось падение расширения, а не сдвиг номера. После правки
мутанта валят восемь тестов, и новые падают именно на числе: `assert 8 == 7`, `assert 7 == (1 + 5)`,
`assert 4 <= 3`.

Мутант **М-SWAP** (`line` и `end` обменяны местами в хойсте) — НАЙДЕН адверсариальной проверкой
собственного диффа и был вакуозен в первой редакции: все фикстуры батареи однострочные, поэтому
`line == end`, и мутант переживал весь сьют (2441 passed). Закрыт строкой с трёхстрочным якорем
(`meta={"line": "4-6"}`); теперь падает на ней с `assert 8 <= 6`.

**CB-164.** Снимок `tests/golden/mcp_schema.json` **НЕ сдвинулся**: md5
`25ea202974453b13f823548763a2077c` до и после, `git diff main...HEAD -- tests/golden/` пуст,
регенерация даёт байт-в-байт тот же файл. Мутант «адаптер сделан сквозным проходом» до правки
давал `13 passed` — гейт не мог выстрелить; после правки красным становится
`TestMcpWireSchema::test_schema_matches_golden`. Мутант CB-156 (адаптер откачен к одному лишь
`dedent_docstring`) — тот самый, про который менеджер Т-63 записал «golden stayed GREEN», —
после правки валит тот же тест.

Отдельно: **правка CB-164 сама себя не защищала**. Откат `tests/_mcp_schema.py` к версии main
давал 193 passed на `test_boundary` + `test_server` + `test_loc` — снимок не двигается, потому
что сегодня ни один тул не передаёт `description=`. Тот же класс внутри собственного исправления.
Закрыто тестом
`test_a_tool_passing_its_own_description_lands_in_the_snapshot_unnormalized`: с откаченным
генератором краснеет ровно он.

**CB-165.** Протухший ряд, называющий несуществующий тул, и ряд, называющий существующий тул с
секцией не той формы, валят `test_every_prose_exception_still_names_a_section_the_normalizer_left_alone`;
пустое и пробельное обоснование валят `test_every_prose_exception_carries_a_non_empty_reason`.
Половины не пересекаются — измерено, обе нужны. Законный ряд сьют не краснит. Долг направления,
вменённый брифом Т-63 и не исполненный, закрыт.

### 9.2. Гребёнка по `tests/` (числа, диффы не тронуты)

49 файлов `tests/*.py`; **9** списков-исключений; **2 голых**, **1 частичный**, остальные 6 (все в
`test_update_parity.py`) несут обе половины. Голые, оба проверены менеджером лично:
`tests/cli_surface.py:90` (`_EXCLUDED_ACTION_ATTRS = frozenset({"container"})`, потребляется на
`:145` через `continue`; причина живёт в комментарии, а не в проверяемом поле) — самый значимый; и
`tests/test_blockers.py:979` (`EXEMPT: dict[str, str] = {}` — форма «имя → причина» есть, тестов на
обе половины нет, но словарь пуст, живого риска нет). Частичный —
`tests/test_claims.py:425` (`allowed`): самоудаление есть, текстовой причины на ряд нет.

### 9.3. Эскалации (проверено менеджером лично, все — вне §6)

Правка CB-164 сделала ложными **четыре** утверждения в дереве. Все четыре лгут в БЕЗОПАСНУЮ
сторону — занижают защиту, которая теперь есть, — поэтому ни одно не ведёт к небезопасному
действию; но это ровно тот дефект, который юнит закрывает, и молча оставлять его нельзя.

1. `src/codebugs/surfacegen.py:38-44` — «the golden collector registers on a RAW server and dedents
   by itself», и вывод, что тул с `description=` «would match the golden byte for byte». Теперь
   неверно: такой тул попадает в снимок ненормализованным и гейт CB-156 его называет. Самое
   значимое из четырёх — это продакшн-докстринг, и именно его бриф §3 цитирует как мотивацию.
2. `tests/test_server.py:332-336` — «a mutant that reverted the adapter alone to `dedent_docstring`
   would leave the golden gate perfectly green». Измерено обратное: краснеет.
3. `tests/test_sweep_surface.py:174` и `tests/test_bench_surface.py:128` — «because the wire golden
   registers on a raw server and normalizes by itself», дважды одна и та же фраза. Сами тесты
   остаются нужными; протухло обоснование.
4. `src/codebugs/server.py:154-155` — «`tests/_mcp_schema` (the generator and the gate) both call
   THIS»: генератор больше не вызывает `normalize_description`, только импортирует ради пина
   CB-73; гейт вызывает.

Пятое, от кросс-модельной проверки (Codex), не связано с протуханием:
`src/codebugs/server.py:196-207` — адаптер зовёт `self._server.tool()` дважды на ветке с
`description=` (первый вызов лишь строит декоратор). Сегодня безвредно, чистота фабрики не
гарантирована API.

Шестое, наблюдение: комментарий `tests/_mcp_schema.py:68-72` утверждает, что переход к общей
топологии «would change the golden». Измерено: не изменил бы — 83 тула, `identical: True`. Ряд не
тронут, потому что бриф §3 запретил трогать эту зону; фраза требует уточнения отдельной картой.

### 9.4. Что проверено и НЕ является дефектом

Канал резолвинга новые тесты намеренно не ассертят. С заглушённым `_channel_a` краснеют пять
тестов батареи, включая `test_a_card_whose_file_moved_reports_the_new_path_with_no_arguments`, чья
фикстура есть ПЕРЕЕЗД файла: контентный канал ищет по записанному пути и `moved_file` выдать не
может. Ассерция канала в тестах про НОМЕР сделала бы законную перестановку каскада красной по
чужой причине. Запись об этом лежит в докстринге класса, чтобы вопрос не переоткрывали.

Записи в `CHANGELOG.md` юнит не добавляет: все три карты меняют батарею и тестовую
инфраструктуру, ни одна строка поведения продукта не двинулась. Два подряд идущих заголовка
`### Fixed` под `[Unreleased]` слиты, как названо в §5 брифа.
