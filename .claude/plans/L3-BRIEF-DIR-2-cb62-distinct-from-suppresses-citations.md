# L3-БРИФ DIR-2 / CB-62 остаток — `distinct_from` подавляет объединение в компонентах цитирования

**Уровень:** (3) менеджер юнита. **Направление:** DIR-2 «Identity находок».
**Полоса:** остаток карты после исполненного пункта (1); нарезка сужена уровнем (1) 2026-08-25.
**Карта:** **CB-62** (medium, `open`) — исполняется ТОЛЬКО суженный пункт (3), см. §1.
**Класс по Р1: B** — оракул детерминированный (объявленное подавление либо разрывает компоненту,
либо нет; мутанты называются точно), но путь НЕ полностью известен: связи между двумя модулями
сегодня нет вовсе, и одно решение о фейл-поведении придётся ИЗМЕРИТЬ, а не выбрать (см. §4).
**Стартовый тир: opus, effort low**, бюджет попыток 4. Объёмное делегируй `sonnet`/`haiku`.
**Fable не использовать.**

**ID юнита минтит держатель**, он назван в промпте диспетчеризации. `tools/cascade-mint.sh` не
запускай (К-1; и скрипт механически откажет вне `main`).

**Ветка:** `tools/worktree-setup.sh fix/cb-62-distinct-from-suppresses-citations` из КОРНЯ
репозитория — он же возьмёт клейм карты из имени ветки (К-8).
**Веди заметки в `/tmp/cb62-manager-notes.md` и коммить рано и часто** (К-11).

---

## 1. Что именно исполняется, и откуда взята формулировка

Карта CB-62 была нарезана на три пункта. Пункт (1) — оси группировки `group_by="tag"` и
`group_by="meta:<ключ>"` — исполнен и приземлён (юнит Т-66, мердж `ea54920`). Пункт (2) — семантическая
ось по векторам — вынесен в кандидаты на отдельную большую задачу и **сюда не входит**.

**Точная формулировка пункта (3) лежит НЕ на CB-62, а на CB-163**, в поле
`meta.cb62_residual_will_be_narrowed`, дословно:

> «distinct_from will suppress in the ONE existing consumer (grouping's citation components) with an
> explicit declaration that it does not unify the graphs — giving unification a measured demand
> signal instead of a guess»

**Скажи это прямо и не выдавай за доводку почти работающего:** `distinct_from` сегодня **не
подавляет никакую группировку нигде**. `grouping.py` не импортирует `relations` вовсе, и
единственное фактическое действие `distinct_from` в пакете — write-time отказ записать
противоречащий `duplicate_of` (`relations._guard_contradictions`). Юнит **строит связь между двумя
модулями с нуля**.

## 2. Что измерено ДЕРЖАТЕЛЕМ лично — перепроверь (К-7)

1. **Публичный аксессор для этого уже существует и объявлен именно для этого.**
   `relations.query_relations(conn, entity_id=None, rel="distinct_from")` возвращает ВСЕ живые
   строки одним запросом (`include_retracted=False` по умолчанию, то есть отозванные исключены), и
   его собственный докстринг говорит: **«`rel="distinct_from"` with no entity is the
   active-suppressions view»**. Новый аксессор строить НЕ надо.
2. **Отношение симметрично и канонизировано при записи** — пара хранится как
   `(min(src,dst), max(src,dst))`. Значит сравнивать надо канонизированную пару, а не упорядоченную.
3. **Сайтов объединения через union-find в `grouping.py` ДВА, а не один:** `citation_report`
   (`dsu.union(a, b)` на рёбрах цитирования) и `filing_report` (`dsu.union(parent, child)` на
   родословных рёбрах). Формулировка «единственный потребитель» — про НАМЕРЕНИЕ, и §3 объясняет,
   почему второй сайт трогать нельзя. **Не прочти её как «сайт в файле один» — их два.**
4. **Теста нет.** В `tests/test_grouping.py` и `tests/test_grouping_surface.py` упоминаний
   `distinct_from`/`relations` нет вовсе — потому что сегодня влияния и нет.
5. `grouping.py` уже несёт дисциплину «строки читаются только через публичный аксессор» (в его шапке
   назван `findings.grouping_candidates` как единственный дозволенный канал). Твоя правка обязана
   этой дисциплине следовать: **никакого SQL по `finding_relations` в `grouping.py`.**

## 3. РЕШЕНИЕ УРОВНЯ (2): подавляем ТОЛЬКО в `citation_report`, и вот почему

Соблазн, в который легко впасть, увидев два сайта union: подавить в обоих. **Это запрещено, и
причина не в объёме, а в смысле.**

- **Рёбра цитирования ВЫВЕДЕНЫ**: они получаются разбором текста карточки, нашедшим упоминание
  `CB-N`. Это догадка инструмента. Явное человеческое объявление «это разные дефекты, не путать»
  обязано такую догадку перебивать — ровно за этим `distinct_from` и существует.
- **Рёбра родословной ОБЪЯВЛЕНЫ**: `split_from` записан намеренно. Подавление объявленного ребра
  другим объявленным отношением — это не «догадка против объявления», а **противоречие двух
  объявлений**, и решать его значит отвечать на вопрос, каким из двух графов владеет истина. Этот
  вопрос — предмет **CB-163** (у `split_from` СЕГОДНЯ два независимых представления: тип отношения в
  `finding_relations` и ключ `meta.split_from`/`split_children`, который читает `grouping.py`), и он
  прямо объявлен решением о владении, выходящим за пределы DIR-2.

**Держи различение «выведенное против объявленного» в голове как критерий**, а не запоминай список
из одного имени функции: список — это буква, а различение — смысл, и оно само скажет тебе ответ,
если завтра появится третий сайт.

## 4. Форма

**(1) Читай подавления ОДИН РАЗ на обход, не по ребру.** Возьми все живые пары одним вызовом
`query_relations(conn, rel="distinct_from")`, сложи канонизированные пары в множество, и проверяй
членство в цикле. Построение по ребру дало бы N запросов внутри обхода — прецедент прямой:
`live_source_clause` из CB-31 отдельно оговаривает «build it ONCE per traversal», потому что
по-строчное построение делало дефект хуже.

**(2) Пропускай объединение, а не ребро.** Место — тот же цикл, где уже пропускаются пары через
хабы; форма пропуска уже есть рядом, повтори её, а не изобретай вторую.

**(3) ОДНО РЕШЕНИЕ ИЗМЕРЬ, НЕ УГАДЫВАЙ: что делать, если таблицы `finding_relations` нет.**
`relations` зарегистрирован в реестре схем с зависимостью от `findings`, поэтому через `db.connect()`
таблица есть всегда — но сырое соединение в тестах может её не иметь. Кандидаты и их цена:
- полагаться на реестр схем (ничего не делать) — измерь, правда ли всякий путь до `citation_report`
  проходит через `db.connect()`;
- проглотить `OperationalError` — **скорее всего неверно**: это форма «сторож отчитался чисто,
  потому что не смог посмотреть», по которой в этом репозитории заведено несколько карт;
- явно проверить наличие таблицы и трактовать отсутствие как «подавлений нет» — фейл-опен, и здесь
  он содержательно верен (подавление есть УТВЕРДИТЕЛЬНОЕ объявление; нет таблицы — никто ничего не
  объявлял), но обязан быть НАПИСАН как решение, а не получиться сам собой.
**Выбери, обоснуй в отчёте и запинь тестом.** Это единственное место юнита, где форма не выбрана за
тебя.

**(4) ОБЪЯВЛЕНИЕ О НЕ-УНИФИКАЦИИ — ОБЯЗАТЕЛЬНАЯ ЧАСТЬ ЮНИТА, а не комментарий по вкусу.** В коде,
рядом с подавлением, должно быть сказано: подавляются только выведенные рёбра цитирования; графы
`relations` и родословной `grouping` НЕ объединяются; вопрос унификации — CB-163, и он ждёт
измеренного спроса, а не догадки. Без этой строки следующий читатель прочтёт частичное подавление
как недоделку и «доделает» её прямо в CB-163.

## 5. Что в скоуп НЕ входит

- **`filing_report` и любой второй сайт union** — см. §3.
- **Унификация графов `split_from`** — это CB-163, и она вынесена намеренно.
- **Перенос связей и леджер предложений** — запарковано владельцем, к этому юниту отношения не имеет.
- **Новый аксессор в `relations.py`** — существующий публичный уже подходит (§2.1). Если окажется,
  что не подходит, — это эскалация, а не тихое расширение чужого модуля.
- **Пункт (2) нарезки** (семантическая ось по векторам).

## 6. Оракул — каждый пункт с мутантом

1. Две карточки, связанные ТОЛЬКО ребром цитирования и помеченные `distinct_from`, **остаются в
   разных компонентах** `citation_report`. **Мутант:** убрать проверку — тест краснеет (компоненты
   сливаются).
2. Те же две карточки БЕЗ `distinct_from` — **в одной компоненте**. Без этого пункта первый
   удовлетворяется поломкой группировки вообще.
3. **Отозванное (`retracted`) подавление не подавляет.** **Мутант:** передать
   `include_retracted=True` — тест краснеет. Это проверяет, что читается ЖИВОЕ отношение, а не любое.
4. **Подавление работает в ОБЕ стороны пары** (отношение симметрично и канонизировано при записи).
   **Мутант:** сравнивать неканонизированную упорядоченную пару — тест краснеет на одном из порядков.
5. **`filing_report` НЕ затронут:** его компоненты те же, что и до правки, даже когда на паре
   родословной висит `distinct_from`. Это оракул на НЕ-расползание, и он так же обязателен, как
   остальные.
6. Решение из §4(3) запинено тестом, каким бы оно ни было.
7. Полный сьют зелёный, `ruff` чист.

## 7. Поверхности, гейты, CHANGELOG

**Поведение тула `grouping_citations` меняется для вызывающей стороны**, поэтому его описание должно
это назвать — значит **`tests/golden/mcp_schema.json` сдвинется**. Регенерация **только из
worktree**: `PYTHONPATH=src uv run python tests/dump_schema.py > tests/golden/mcp_schema.json`.

**`tests/golden/cli_surface.json` двигать НЕ НАДО**: текст справки CLI-верба `grouping-citations` не
меняй, флагов не добавляй. Если он сдвинулся — ты тронул поверхность за пределами скоупа.

**Сьют и ruff в worktree — только с `--extra dev`.** Ruff пин 0.15.7.

**Запись в `CHANGELOG.md` ОБЯЗАТЕЛЬНА и является частью критерия приёмки** (правило уровня (1) от
2026-08-25, готовится релиз). **Пиши её пользовательским языком — что изменилось для того, кто
ВЫЗЫВАЕТ тул**, а не что изменилось в реализации: читатель не обязан знать ни про union-find, ни про
таблицу связей. По смыслу это звучит как «если вы явно объявили две карточки разными
(`relations_relate … distinct_from`), отчёт по цитированиям больше не будет сливать их в одну
единицу работы; на объявленную родословную это не влияет».

## 8. Сообщение мерджа и отчёт

`--merge-msg` обязателен (К-4). Форма:
`Merge fix/cb-62-distinct-from-suppresses-citations: объявленное distinct_from разрывает выведенное ребро цитирования, графы не объединяются (CB-62)`

Отчёт ≤25 строк. **Два раздела — обязательные поля формы, каждый проверяется своим пунктом
контракта приёмки; отсутствие любого есть FAIL:**

- **«Класс задачи и модель»** — класс по Р1, стартовый тир, каждая эскалация тира с причиной,
  финальный тир. Это **пункт 8** контракта (§13 спеки); он выпадает чаще прочих, потому что
  определение агента-приёмщика до сих пор говорит «7 пунктов», тогда как §13 требует восьми.
- **«Эскалации исполнителя»** — дословно каждая, включая отклонённые, с твоим ответом. Это **пункт
  4**. Пустой список пишется явной строкой «эскалаций не было».

Плюс: SHA мерджа, счёт тестов до и после, состояние обоих golden, **обоснование выбора по §4(3)**, и
— одной строкой — если заметишь, что описания тулов или `README` не объясняют вызывающей стороне
типовой сценарий (подать → дедуп → якорь → закрыть), **не чини, а назови**: куратор собирает картину
по вопросу владельца о «recommended workflow».

Факт своей приёмки пиши в этот бриф **НА ВЕТКЕ**; **уровень (3) на main не коммитит — никогда**
(К-1). Карту CB-62 **не закрывай** — после этого юнита у неё остаётся неисполненный пункт (2), и
закрытие решает уровень (2).
