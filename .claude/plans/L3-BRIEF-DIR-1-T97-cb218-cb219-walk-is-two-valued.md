# L3-BRIEF DIR-1 / Т-97 — CB-218 (`high`) + CB-219: обход каталогов вверх остался двузначным и решает вопрос достижимости первым

Держатель направления DIR-1, уровень (2), 2026-08-26, смена 13.
Карты: **CB-218** (`high`), **CB-219** (`medium`). Обе — находки изолированного приёмщика юнита Т-92
(мерж `1b8205a`); юнит был принят с оговоркой, и обе карты фиксируют, что дефект **был до него**.

Ветка: `fix/cb-218-cb-219-walk-is-two-valued`. Оба номера в имени намеренно — `worktree-setup.sh`
достаёт их регуляркой `cb-?[0-9]+` и клеймит **обе** карты до создания дерева (проверено чтением
скрипта, строки 66–79).

---

## §1. Замысел — одной фразой, потому что дальше будет много деталей

Юнит Т-92 научил трекер отвечать «не удалось установить» вместо ложного «ничего нет» — но научил
этому **пять мест, которые смотрят на уже найденный корень**, а не то место, которое решает, **какой
корень вообще будет найден**. Обход каталогов вверх остался двузначным, стоит **раньше** всех
исправленных мест, и на нём воспроизводится худший вред этой семьи карт: молчаливая привязка не к
тому трекеру при коде возврата 0 и совершенно чистой диагностике.

Замысел юнита: **обход остаётся тем же алгоритмом**, но перестаёт делать выводы из вопросов, на
которые не получил ответа, а диагностика перестаёт молчать о кандидате, которого не удалось
опросить.

---

## §2. ЧТО Я ИЗМЕРИЛ ЛИЧНО 2026-08-26 — не пересказ карт, а мой собственный прогон

Три состояния, каждое построено с нуля во временном каталоге, интерпретатор — `.venv` главного
чекаута, ключи возврата сняты **без конвейера** (первая моя попытка мерила код выхода `head`, а не
кода — эту ошибку записываю сразу, чтобы ты её не повторил).

**(1) Вред CB-218, вариант «выше ничего нет».** Трекер в каталоге `A`, процесс в `A/b/c`, у `A`
снято право входа уже после того, как процесс туда вошёл. `codebugs where` печатает:

```
codebugs: no .codebugs/ found in …/A/b/c or any parent; run `codebugs init` here, …
source:   directory discovery
root:     (unresolved)
```
код возврата **1**. То есть верб **утверждает отсутствие того, на что не смог посмотреть** — дословно
дефект CB-203, только на другом предикате.

**(2) Вред CB-218, вариант «выше есть посторонний трекер» — он и делает карту `high`.** То же
состояние плюс трекер в `P`, родителе `A`. `codebugs where` печатает **совершенно чистую привязку**:

```
source:   directory discovery
root:     …/P
database: …/P/.codebugs/findings.db
```
код возврата **0**, ни одного предупреждения. Читающий верб (`stats`) при этом отвечает **пустой
популяцией** и кодом 0 — про чужой трекер, честно. Собственные данные пользователя лежат уровнем
ниже и не опрошены. Это не потерянное предупреждение и не ложное обещание, а **тихая привязка не к
тому трекеру** — ровно то, ради предотвращения чего заводилась CB-8 и ради видимости чего строилась
CB-11.

**(3) Вред CB-219.** Каталог `.codebugs/` существует, пуст, права `555`. `codebugs where` печатает
привязку и строку «`(no database there yet — the next command creates one)`» при коде **0**;
следующий же читающий верб отказывает — «`cannot open findings.db … for writing`», код **1**.
Классификация здесь **верна** (файла действительно нет), ложен **вывод**: «нет файла» не влечёт
«файл будет создан».

**Все три состояния воспроизведены. Ни одно из них не является гипотезой карты.**

**И одно измерение, которого в картах НЕТ и которое расширяет юнит — это моя находка, проверь её
первой.** Двузначны в обходе не только пробы каталога трекера. `src/codebugs/db.py`:

- строка **978** — `(cur / DB_DIR).is_dir()`, это то, что называет CB-218;
- строка **981** — `git.is_dir()`, проба «здесь граница репозитория»;
- строка **983** — `git.is_file()`, проба «здесь связанное рабочее дерево»;
- и те же две в `_enclosing_worktree_root`, строки **954** и **956**.

`Path.is_dir()` глотает ошибку нижележащего системного вызова ровно так же, как `os.path.isdir`.
Значит недоступный ответ про `.git` читается как «границы здесь нет», и обход **проходит мимо
границы репозитория** — а это тот же самый вред «привязались не к тому трекеру», просто через другую
дверь. Чинить в одной функции одну пробу из трёх — это буквально то, в чём CB-218 обвиняет
`CLAUDE.md`: проверка элементов вместо проверки состава. **Поэтому в объём юнита входят все пять
проб обхода, а не одна.**

---

## §3. ФОРМА ВЫБРАНА УРОВНЕМ (2) — и её границу задал куратор

**Форма минимальная: третий исход и предупреждение, НЕ переделка резолвера.** Это указание уровня
(1), и я его сузил до проверяемого:

- **Алгоритм обхода не меняется ни в одной точке.** Порядок каналов (аргумент → флаг → переменная
  окружения → обход), правила границ (каталог `.git` останавливает, файл `.git` со ссылкой на общий
  каталог продолжает от главного чекаута), приоритет `.codebugs/` над границей в одном каталоге —
  всё остаётся дословно как есть. Ни одна из этих строк не должна поменять смысл.
- **Меняется только ПРИМИТИВ пробы:** каждая из пяти проб §2 идёт через `db._path_state`, который
  уже существует, уже принят и уже возвращает четыре ответа плюс «не удалось установить».

**Решение по границе, которое карта оставила уровню (2) — что делать при «не удалось установить» на
маршруте обхода. Решаю: ИДТИ ВЫШЕ, ЗАПОМНИВ пропущенного кандидата, и НАЗВАТЬ его в диагностике.**

Довод — цена ошибки в обе стороны. Остановка превратила бы состояние, которое сегодня работает, в
жёсткий отказ: `_path_state` отвечает «не удалось установить» не только на снятом праве входа, но и
на любой другой ошибке файловой системы — ввод-вывод, петля ссылок, слишком длинное имя, сетевой
том, ответивший не тем кодом. Одна такая ошибка на **постороннем** предке между текущим каталогом и
настоящим трекером сегодня безвредна, а после остановки стала бы отказом работать вовсе. Ложный
отказ это дерево считает худшим исходом, чем деградация, — и правильно считает. **Продолжить обход
безопасно; молчать о пропущенном кандидате — нет**, потому что именно молчание и делает вариант (2)
§2 тихой привязкой к чужому трекеру.

**Как пропущенный кандидат доезжает до человека — три звена, и ни одно не «переделка резолвера».**

1. `_find_db_root` возвращает **пару**: найденный корень (как сегодня) и **список неопрошенных
   кандидатов** — путь плюс короткая человеческая причина, ровно та, что даёт `_path_state`.
2. `_resolve_db` протаскивает этот список наружу третьим элементом кортежа. Ветки, где обход не
   выполнялся вовсе (объявленный корень, именованный каталог), возвращают **пустой список**.
   **Пустой список означает «обход прошёл и пропущенного не было», и он БЕЗУСЛОВЕН** — это ровно та
   дисциплина, которую пакет уже держит для `attention`, `stripped_meta_keys` и `exists_reason`:
   пустое значение никогда не должно читаться как «такого канала нет». `_db_path` продолжает
   возвращать только путь и не меняет ни одного своего вызывающего.
3. `describe_root` получает новый **безусловный** ключ с этим списком, а оба его потребителя —
   `codebugs where` и стартовая проверка MCP-сервера — печатают строку, **когда список непуст, и
   молчат, когда он пуст**. Это та же конвенция, по которой уже печатается `writable`: говорим
   только тогда, когда есть что сказать.

**Текст отказа на маршруте «ничего не нашли» перестаёт утверждать отсутствие**, когда по дороге был
неопрошенный кандидат: сегодня он говорит «нет `.codebugs/` ни здесь, ни у одного из предков», и это
утверждение о том, на что посмотреть не удалось.

---

## §4. ВТОРАЯ КАРТА, CB-219 — почему тем же юнитом, а не следующим

Обещание «следующая команда создаст базу» печатает **та же функция вывода**, и трогать её двумя
юнитами значит устроить конфликт на посадке ради воображаемой чистоты нарезки. Кроме того, обе карты
— один и тот же дефект мышления: **вывод, не покрытый проверкой**.

**Форма:** прежде чем обещать создание, спросить, можно ли писать **в каталог**. У пакета уже есть
готовый механизм — `_writable_probe` проверяет **и файл, и каталог, который его держит**, и вторая
половина этой проверки заработала не из осторожности, а по замеру (её докстринг это описывает).
Нужна ровно эта вторая половина, применённая к каталогу `.codebugs/`, когда `exists` равен `False`.

**Три границы, которые обязаны быть соблюдены, иначе правка сама станет новой ложью:**

- **`_writable_probe` нельзя звать на несуществующем пути** — его собственный контракт это запрещает,
  и по делу: `os.access` на отсутствующем имени отвечает `False` по постороннему поводу. Здесь
  спрашивают про **каталог**, который существует; про файл не спрашивают вовсе.
- **У пробы три исхода, и третий обязан быть виден как третий.** «Не удалось установить» не имеет
  права свернуться ни в «можно», ни в «нельзя».
- **Печатать только отрицательный ответ.** Утвердительный не печатается — `os.access` это
  «проверил-и-действуй», и диагностика, сказавшая «писать можно», а затем получившая отказ, — это
  тот же класс расхождения, ради которого заводилась CB-100, просто вывернутый. «Не удалось
  установить» тоже не печатается на этой строке: молчание — это то, как обязано выглядеть
  «не знаю», и ровно так уже устроен `writable`.
- **Существующая строка про «базы ещё нет» не должна ни исчезнуть, ни задвоиться.**

---

## §5. ТРЕТЬЯ ПОЛОВИНА — НОРМАТИВНЫЙ ДОКУМЕНТ, и она обязательна

`CLAUDE.md`, единственный маркированный абзац про `exists` и три значения (найдёшь его по строке
«**`exists` IS THREE-VALUED**»). Внутри него стоит утверждение:

> **The same predicate replaced `isfile`/`isdir` at all five sites in `db.py`, not just the one the
> card named**

**Каждое слово в нём истинно.** Ложна композиция: рядом с фразой «механизм, которого никто не
перечислял, попадает в *не удалось установить* по построению» это читается как «в `db.py` двузначного
ответа про достижимость больше нет». А он есть, и стоит **раньше** всех пяти.

**Что сделать.** Сузить утверждение до того, что оно на самом деле описывает — `_path_state` и его
пять вызывающих, смотрящих на **уже найденный** корень, — и **отдельной фразой** сказать, что обход
каталогов вверх решает вопрос **раньше** и после этого юнита тоже стал трёхзначным, назвав, что
именно он теперь делает при неопределённости (идёт выше и называет пропущенного кандидата).
Формулируй как **свойство**, а не как счёт мест: этот документ уже дважды был неправ ровно там, где
в прозе стоял счёт.

**Пиши это в `CLAUDE.md` последним коммитом ветки, после того как код зелёный.** Иначе документ
описывает состояние, которого в дереве ещё нет, — а это и есть та самая ошибка, которую он тут
чинит.

---

## §6. К-18 — ОТДЕЛЬНАЯ СТРОКА, ПРОВЕРЬ ПРЕДПОСЫЛКУ САМОЙ ПРАВКИ

Канон К-18: **правка, написанная как защита от ошибки, проверяется хуже обычной** — она приходит с
готовым оправданием («это же исправление лжи»), и её собственная предпосылка не перепроверяется.
Здесь предпосылок **две**, и обе твои:

**(а) Что `_path_state` действительно отвечает «не удалось установить» на том состоянии, ради
которого всё делается.** Не выводи это из докстринга. Построй состояние варианта (2) §2 и **сними
ответ `_path_state` напрямую** на пути `A/.codebugs`. Если он отвечает не тем, чего я жду, — мой
замер верен только на уровне верба, а внутренний механизм другой, и это **эскалация, а не повод
переписать предикат**.

**(б) Что три пробы `.git` из §2 действительно достижимы к тому же вреду.** Я вывел это из формы
кода, а **не** воспроизвёл вербом. Твоя работа — попытаться воспроизвести: недоступный ответ про
`cur/.git` должен дать проход мимо границы репозитория и привязку к трекеру за ней. **Если
воспроизвести не удастся — так и напиши**, оставь правку примитива (она всё равно верна и дешева),
но не пиши в отчёте, что закрыт вред, которого ты не видел. Честная граница дороже красивого
утверждения; на этом направлении за обратное уже платили.

---

## §7. Инварианты и границы

- **Ни один голден не двигается.** Ни `tests/golden/mcp_schema.json`, ни снимок поверхности CLI:
  новых аргументов и вербов не появляется. Если голден сдвинулся — это эскалация, а не «ну
  перегенерирую».
- **Молчание на здоровом состоянии — половина оракула.** Обычный трекер, обычный обход: ни новой
  строки в `where`, ни новой строки в стартовой проверке сервера. Тест на это обязателен и обязан
  гоняться на **непустом** множестве состояний, иначе он зелёный по построению.
- **`describe_root` не имеет права начать бросать исключения.** Его контракт «никогда не бросает» —
  несущий: стартовая проверка сервера обязана оставаться предупреждающей, а не фатальной.
- **Ключ списка безусловен.** Отсутствующий ключ и пустой список — разные вещи; потребитель никогда
  не должен читать первое как второе.
- **Сравнение с `None` — через `is`.** В этой функции уже живёт трёхзначное `exists`, и истинностное
  сравнение читает `None` как `False`. Второй такой ключ обязан следовать той же дисциплине.
- **Правило `S608` и прочие маркеры подавления не трогаем** — это соседняя карта CB-172, не твоя.
- Вне объёма: `CHANGELOG.md` трогаешь **обязательно** (см. §9), файлы соседних направлений — нет.

---

## §8. Мутанты — оракул

**Только в копии вне рабочего дерева или во временном дереве (К-5, К-15), никогда в рабочем.**
`PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, и **подтверди `grep -c`, что мутант применён** —
иначе «мутант выжил» и «мутант не применён» неразличимы.

**Ловушка, наступали шесть раз:** проба, запущенная указанием пути к тестам **без смены рабочего
каталога внутрь копии**, молча измеряет главный чекаут. Сними `inspect.getsourcefile()` у
импортированного модуля, а для тестов, шеллящих CLI подпроцессом, — **из подпроцесса**, тем же
способом запуска, каким исполняется тест.

**Обязаны краснеть после юнита (и все обязаны быть ЗЕЛЁНЫМИ до него — покажи оба состояния):**

1. Состояние (1) §2 — недоступный предок, выше ничего нет: текст отказа обязан перестать утверждать
   отсутствие и обязан назвать неопрошенного кандидата.
2. Состояние (2) §2 — недоступный предок, выше **посторонний** трекер: `where` обязан назвать
   пропущенного кандидата, оставаясь при этом привязкой к тому, что нашёл. **Это главный мутант
   юнита.**
3. То же состояние (2), но через **стартовую проверку MCP-сервера**: строка в потоке ошибок
   обязана появиться.
4. Состояние (3) §2 — пустой каталог трекера с правами `555`: обещание «следующая команда создаст
   базу» обязано перестать быть безусловным.
5. Мутант на примитив: верни `_path_state`-ветку обхода к двузначному ответу (пусть «не удалось
   установить» читается как «ничего нет») — **любой из тестов 1–3 обязан покраснеть**.
6. Мутант на безусловность ключа: сделай ключ появляющимся только когда список непуст — тест обязан
   покраснеть.
7. **То же состояние, полученное ДРУГИМ способом.** Эта строка на нашем направлении дважды приносила
   больше, чем весь остальной оракул. «Не удалось установить» достижимо не только снятым правом
   входа: попробуй ошибку ввода-вывода, петлю символических ссылок, имя длиннее предела, путь через
   сетевой или специальный том. Назови, сколько способов ты нашёл и сколько из них покрыты тестами.

**Обязано ПРОХОДИТЬ:** здоровый обход по-прежнему молчит; обычный трекер даёт ту же самую строку
привязки, что и до юнита; граница репозитория (каталог `.git`) по-прежнему **останавливает** обход, а
файл `.git` связанного дерева по-прежнему **продолжает** его от главного чекаута; существующие тесты
`tests/test_db_infra.py` про обход зелены без правок — а если какой-то пришлось править, объясни в
отчёте, почему это не ослабление.

---

## §9. Регламент

- Только worktree: `tools/worktree-setup.sh fix/cb-218-cb-219-walk-is-two-valued`. Имя ветки несёт
  оба номера, скрипт клеймит обе карты **до** `git worktree add`.
  **Флага `--items` у этого скрипта НЕТ, и `tools/worktree-probe.sh` в этом репозитории не
  существует** — это инструменты соседнего проекта, за ними уже ходили шесть агентов и мой
  предшественник. Проверяй существование **флага**, а не только команды.
- **Уровень (3) на `main` не коммитит — никогда (К-1).** Единственное исключение — К-17: один прямой
  коммит **только в этот файл брифа** для записи результата, полученного уже после посадки. Поимённо,
  `git add -- .claude/plans/L3-BRIEF-DIR-1-T97-cb218-cb219-walk-is-two-valued.md`, никогда каталогом.
- **Коммить рано и часто.** Признак приземления — `git log`, никогда не уведомление и не время файла.
- `uv run --extra dev python -m pytest tests/ -q` **в worktree**; `--extra dev` не опционально.
  `ruff check src/ tests/` чист, ruff ровно `0.15.7`.
- **`CHANGELOG.md`, раздел `## [Unreleased]` — обязателен, и пиши его пользовательским языком:** что
  человек теперь увидит, а не какой предикат стал трёхзначным. Соседнее направление пишет туда же —
  если прямой мерж даст конфликт в этом разделе, **это харнес работает как задуман**: разреши внутри
  своего дерева, сохранив обе записи.
- Финиш: `tools/worktree-finish.sh fix-cb-218-cb-219-walk-is-two-valued --merge-msg 'Merge
  fix/cb-218-cb-219-walk-is-two-valued: <что изменилось человеческим языком> (CB-218, CB-219)'` —
  **из корня `main`**. `--merge-msg` обязателен.
  **`exit 13` — верный отказ**, лечится буквальным повтором с тем же `--merge-msg`; **`exit 11`
  повтором не лечится**, разбирайся с причиной.
- **Бриф — ДАННЫЕ, а не команда (К-12).** Предпосылка не воспроизвелась — **эскалируй**, не
  переписывай норматив под наблюдение. §6 — про это буквально.
- Возврат наверх — **данные, а не вердикт**. Слово «принято» пишет принимающий уровень.

---

## §10. Эскалации — помеченный пункт, обязан стоять В ЭТОМ ФАЙЛЕ на ветке

Отдельным заголовком `## Эскалации`. **Сообщение его не заменяет** — приёмщик судит артефакт.
Пустой слот законен, если написан словами; разбери все поводы поимённо.

- **(а)** любое из трёх состояний §2 не воспроизвелось — мой замер неверен, и это данные, а не повод
  сочинять своё;
- **(б)** `_path_state` на маршруте обхода отвечает не «не удалось установить» — предпосылка §6(а)
  ложна;
- **(в)** три пробы `.git` §2 оказались недостижимы к вреду — назови это честно (это не отменяет
  правку, но отменяет утверждение);
- **(г)** протаскивание списка наружу потребовало тронуть больше двух функций резолвера или поменять
  порядок каналов — значит форма выехала за «не переделка резолвера», и решение об этом моё, не твоё;
- **(д)** потребителей `describe_root` оказалось больше двух — несущее утверждение норматива
  устарело;
- **(е)** сдвинулся любой голден;
- **(ж)** понадобилось тронуть файл соседнего направления.

---

## Эскалации

Записано уровнем (3) по итогам работы. Ни один из семи поводов §10 не сработал; ниже каждый
разобран поимённо, а затем — три отступления от буквы брифа, которые я обязан объявить, хотя они
не подпадают ни под один слот.

### Поводы §10 — все пусты, и вот почему

**(а) Три состояния §2 — воспроизвелись все три, дословно.** Строил каждое с нуля во временном
каталоге интерпретатором ветки, коды возврата снимал без конвейера. Состояние (1): `codebugs where`
печатал «`no .codebugs/ found in … or any parent`» при коде 1. Состояние (2): совершенно чистая
привязка к постороннему трекеру уровнем выше, код 0, ни одного предупреждения, а `stats` отвечал
пустой популяцией про чужой трекер при коде 0. Состояние (3): «`(no database there yet — the next
command creates one)`» при коде 0, следующий читающий верб — «`cannot open findings.db … for
writing`» при коде 1. Замер держателя направления верен во всех трёх точках.

**(б) `_path_state` на маршруте обхода — предпосылка подтвердилась.** Снял ответ напрямую на пути
`A/.codebugs` в состоянии (2): `(None, 'could not look at it (Permission denied)')`, то есть ровно
«не удалось установить». Рядом, для контраста, `os.path.isdir` и `pathlib.Path.is_dir` оба отвечают
`False`, а `_find_db_root` возвращает посторонний корень `P`. Внутренний механизм оказался в точности
тем, что описывает бриф.

**(в) Три пробы `.git` — вред достижим, и это проверено вербом, а не выведено из формы кода.**
Держатель направления пометил это как невоспроизведённое им. Воспроизвёл двумя независимыми
способами. Первый: право входа снято у самого каталога репозитория — обход прошёл мимо границы и
привязался к постороннему трекеру за ней при коде 0. Этот способ, однако, делает неопределимыми
**обе** пробы сразу (`.codebugs` и `.git`), поэтому сам по себе он не изолирует вину `.git`. Второй
способ изолирует: каталог репозитория полностью читаем, `.codebugs/` в нём отсутствует доказуемо
(`absent`), а `.git` — петля символических ссылок; единственный неотвеченный вопрос — про `.git`, и
обход всё равно проходит мимо границы. `_path_state` там отвечает `(None, 'a symbolic link is there
and does not resolve (Too many levels of symbolic links)')`. Есть и контрольный случай, который
полезно знать следующему: `.git` **с правами 000** неопределимости НЕ даёт — `lstat` на самом
`.git` требует права входа только у родителя, поэтому ответ честный `('dir', None)` и обход
правильно останавливается.

**(г) Форма не выехала за «не переделка резолвера».** Порядок каналов (аргумент → флаг → переменная
окружения → обход) не изменился ни на строку. Решающих точек в резолвере по-прежнему две:
`_walk_db_root` (это дословно прежнее тело обхода) и `_resolve_db`. Прочее — механические
приспособления к новой сигнатуре: `connect` распаковывает тройку вместо пары.

**(д) Потребителей `describe_root` ровно два** — `cli.py:113` (`codebugs where`) и `server.py:464`
(стартовая проверка). Проверено гребёнкой по всему дереву: остальные упоминания — это докстринги,
комментарии и тесты. Несущее утверждение норматива не устарело.

**(е) Ни один голден не сдвинулся.** `git diff` по `tests/golden/` против базы ветки пуст. Новых
аргументов и вербов не появилось.

**(ж) Файлы соседних направлений не трогались.** Изменены пять файлов: `src/codebugs/db.py`,
`src/codebugs/cli.py`, `src/codebugs/server.py`, `tests/test_db_infra.py`, `tests/test_server.py`,
плюс обязательные `CHANGELOG.md`, `CLAUDE.md` и этот файл.

### Три отступления от буквы брифа — объявляю, решение о них не моё

**1. `_find_db_root` НЕ стал возвращать пару; пару возвращает новая `_walk_db_root`, а
`_find_db_root` остался тонкой обёрткой над ней.** §3 звено 1 предписывает буквально «`_find_db_root`
возвращает пару». Буква столкнулась с §8, требующим, чтобы существующие тесты обхода в
`tests/test_db_infra.py` были зелены **без правок**: у `_find_db_root` около двадцати пяти мест
вызова, среди них — `tests/conftest.py` (сессионный страж герметичности набора) и
`tests/test_suite_hermeticity.py`, то есть файлы, которые ломать ради переименования сигнатуры
дороже всего. Выбранная форма — ровно та, что этот модуль уже применяет на уровень выше:
`_db_path` есть тонкая обёртка над `_resolve_db`, и бриф сам это благословляет. Замысел сохранён
полностью (обход отдаёт корень и список неопрошенных кандидатов), изменилось только, какое имя носит
функция, отдающая пару. Итог: **ни одна существующая строка тестов не удалена и не изменена** —
463 добавленных строки, 0 удалённых.

**2. `DatabaseNotFoundError` получил атрибут `unexamined`.** Это четвёртое звено сверх трёх,
названных в §3, и вот зачем. На ветке отказа (состояние (1)) `describe_root` не возвращает кортеж
вовсе — он ловит исключение. Без атрибута ключ `unexamined` пришлось бы там выставлять пустым, а
пустой список по §7 означает «обход прошёл и пропущенного не было» — то есть ключ солгал бы ровно в
том состоянии, ради которого заводился. Атрибут — самый дешёвый способ сохранить это свойство;
читается он через `isinstance`, а не через `getattr` со значением по умолчанию, потому что `OSError`
на той же ветке действительно такого ответа не несёт, и разница между «обхода не было» и «атрибута
нет» — это ровно та разница, на которой ключ и построен.

**3. Ключей добавилось два, а не один: `unexamined` (CB-218) и `dir_writable` (CB-219).** §4 задаёт
поведение — обещание проверяется, — но не говорит, каким путём ответ доезжает до обоих
потребителей. Оба печатают это обещание, значит ответ обязан лежать в `describe_root`, иначе
получились бы две копии одного правила в двух файлах. Складывать его в существующий `writable`
я отказался: тот отвечает про **файл** и определён только когда файл есть, а этот — про **каталог**
и только когда файла нет; один ключ с двумя значениями — это в точности то смешение, от которого
предостерегает CB-23. Пришлось также расщепить `_writable_probe` на неё саму и `_access_probe`:
звать `_writable_probe` на каталоге было бы неверно, потому что она спросила бы заодно и про корень
проекта, где ничего создавать не собираются, и дала бы ложную тревогу.

### Мои собственные ошибки и то, что нашлось по дороге

**Придуманное мною ограничение списка пришлось снять — его опроверг замер.** Первая редакция
печатала не более трёх пропущенных кандидатов и дальше считала. Довод был про читаемость: одна
стена прав делает неопределимым каждый уровень **ниже** себя (обход спрашивает абсолютными путями, и
каждый из них проходит через стену), поэтому трекер тремя каталогами ниже даёт **шесть** записей на
одно условие с одним лечением. Замер показал, что ограничение отрезает не то: список идёт от
глубокого к мелкому, и в первые три попадают **тени** стены (`…/b/c/.codebugs`, которых никогда не
существовало), а запись, называющая саму стену — тот каталог, где лежит трекер пользователя и чьи
права надо чинить, — оказывается за отсечкой. Короткая диагностика без собственной причины хуже
длинной. Ограничение снято, вместо него стоит тест, проверяющий, что **самая мелкая** запись
доезжает до текста.

**Мутант №1 выжил с первого прогона — мой тест был слабее собственного имени.** Тест назывался
«отказ перестал утверждать отсутствие», а проверял отрицание: что в тексте нет старого написания с
точкой с запятой. Мутант, снявший уточнение «that could be examined», но оставивший приписку про
кандидатов, эту проверку прошёл — приписка начинается с тире, и точка с запятой не возвращалась.
То есть тест пинал «оговорка присутствует», что уже покрыто мутантом на примитив, а не то, ради
чего написан. Переписан на положительное утверждение о самом уточнении; парный тест на здоровом
маршруте держит обратное направление. После правки все восемь мутантов гибнут.

**Две ошибки в моей собственной оснастке, обе оплачены прогоном, а не рассуждением.** Первая: в
каталог, у предка которого снято право входа, нельзя войти извне — `subprocess` с `cwd=` просто не
стартует, поэтому право надо снимать **после** входа, ровно как и написано в брифе; и возвращать его
между замерами, иначе второй замер падает на входе. Вторая: вложенный трекер нельзя создать под уже
существующим внешним — защита от затенения откажет; естественный порядок (сначала внутренний, потом
внешний) эту защиту не задевает вовсе и никакого `--force` не требует.

**Остаток, названный и намеренно не закрытый.** `_find_db_root` — обёртка, отбрасывающая список, —
питает два стража затенения: `init_project` и вариант с `--force` в `cli.py`. Оба по-прежнему
читают отсутствие корня как «объемлющего трекера нет», поэтому неопросимый предок позволяет `init`
создать трекер, который настоящий трекер выше затенит. Это тот же двузначный ответ в **другом**
решении, и у него должен быть свой ответ (отказать? предупредить?), а не молчаливое наследование
этого. Записано в докстринге `_find_db_root`, в `CLAUDE.md` и заведено отдельной картой — **CB-220**
(`medium`), где названы и цена отказа: он был бы ложным всякий раз, когда неопросимый предок — это
чужой каталог, в котором ничего нет.

---

## Результат посадки

Записано прямым коммитом в этот файл после интеграции (К-17); больше на `main` уровень (3) ничего
не писал.

- **Мерж: `5f49149`**, первым родителем `63f6bc1`. Ветка `fix/cb-218-cb-219-walk-is-two-valued`,
  шесть коммитов: `da93b10` (резолвер), `d3ee23a` (оракул), `92d76c1` (усиление теста после того,
  как мутант №1 выжил), `6bccaa5` (CHANGELOG), `4d512b7` (эскалации), `83b80a7` (норматив —
  последним, после зелёного кода, как требует §5). Прямой мерж прошёл без конфликта: соседнее
  направление писало в `CHANGELOG.md` в другую подсекцию.
- **Собственный прогон уже на `main`, после мержа:** `ruff check src/ tests/` — «All checks
  passed!», ruff ровно 0.15.7; `uv run --extra dev python -m pytest tests/ -q` — **2852 passed**,
  0 упавших. Голден не сдвинулся (`git diff` по `tests/golden/` против базы ветки пуст).
- **Существующие тесты не правились**: 463 добавленных строки в `tests/`, **0 удалённых**.
- **Оракул мутантов §8: восемь мутантов, все гибнут.** Прогон — во временном рабочем дереве,
  `PYTHONDONTWRITEBYTECODE=1`, чистый `__pycache__`, применение каждого подтверждено `grep -c`,
  принадлежность модуля снята через `inspect.getsourcefile()` и указывает во временное дерево.
  С первого раза выжил мутант №1; тест усилен, после чего гибнут все восемь.
- **Карты CB-218 и CB-219 намеренно ОСТАВЛЕНЫ ОТКРЫТЫМИ.** Регламент §9 закрывать их не поручал, а
  по К-1 факт приёмки пишет принимающий уровень; закрытая карта до вердикта — это самоотчёт. Клеймы
  ветка отпустила сама при финише. Заведена **CB-220** — остаток, названный выше.
