# HANDOFF — DIR-1 «Доверие и координация», уровень (2), 2026-08-25 (вытесняет: HANDOFF-DIR-1-2026-08-22.md)

Написан уходящей сессией 7. **Правил здесь нет намеренно** — только указатели, фактическое состояние
и первые действия. В вопросах НОРМЫ handoff-у не верь: сверяй с §0 и чини handoff.

## 0. Нормативные входы (указатели, НЕ пересказ)

- Роль и регламент сессий: `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — твои секции §1, §2, §7, §8.3б, §13; §17 про ротацию.
- Харнес, гейты, доктрина репо: `CLAUDE.md` (раздел **Workflow** — несущий; **Error handling** правлен
  юнитом Т-60 и теперь называет `cli.domain_errors()`).
- **Артефакт твоего уровня и источник истины:** `.claude/plans/DIR-1-trust-coordination.md`
  — §5 таблица юнитов, §6 ратифицированные расхождения, §9 неизменяемые факты приёмки, §10 эскалации,
  §11 журнал, **§13 шаблон брифа — читать целиком перед первым брифом** (накопленные поправки).
- Реестр болей (решения куратора и владельца): `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- Аллокатор ID: `tools/cascade-mint.sh --help`; реестр `.claude/plans/CASCADE-IDS.md` (шапка правлена
  Т-40 и Т-47 — описывает ЧЕТЫРЕ отказываемых состояния гейта).
- **BT-6 закрыта:** `.claude/plans/BT-6-CUT-surface-generator-pilot.md` несёт врезку об остановке
  ратификацией; арка В-5(а) закрыта, CB-66 `wont_fix`.

## 1. Фактическое состояние (по git, на момент ротации)

- `main` = `origin/main` = `fca898f`, дерево чисто. `.claude/settings.json` untracked — лежит до меня,
  permission-чувствителен, не мой, не трогал.
- **В ПОЛЁТЕ ровно один юнит: Т-64 (CB-152 + CB-155).** Ветка `fix/t64-cb-152-instrument-truth`,
  **один коммит `8eca8e4`**, worktree `.worktrees/fix-t64-cb-152-instrument-truth` **чист**
  (незакоммиченного нет). Клейм CB-152 держится веткой. **Исполнитель, скорее всего, мёртв** — ноль
  активности файлов за 10+ минут. `worktree-finish.sh` НЕ запускался. Бриф —
  `.claude/plans/L3-BRIEF-DIR-1-Т-64-cb152-cb155-instrument-truth.md`; слот эскалаций в нём НЕ заполнен.
- **Приземлено моей сессией (все с изолированной приёмкой):** `2fc840a` (Т-55/CB-153), `1e4a1dc`
  (Т-58), `7e46180` (Т-57/CB-55+CB-124), `da18056` (Т-59/CB-56+CB-80), `5adfed8` (Т-60/CB-159+CB-160),
  `603beab` (Т-62/CB-154). Ранее в сессии: Т-40, Т-42 (второй пилот), Т-44, Т-45, Т-47, Т-51, Т-53, Т-54.
- **Ветка `feature/t49-bt6-third-pilot-annotations` НЕ приземляется намеренно** (решение уровня (2),
  утверждено куратором): несёт два замера третьего пилота (`4c275d5`, `c208856`) и его полный итог.
- **Статусы хвоста территории:** CB-150 `open/medium`, **CB-152 `in_progress`** (держит Т-64),
  CB-154 `fixed`, CB-155 `open/low`, CB-158 `open/low`. Плюс новые низкие: **CB-161** `open/low`
  (`last_n` интерполируется в SQL двумя сайтами `bench.py`, не валидируется, без `# noqa: S608`) и
  **CB-162** `open/low` (`codebench_list(last_n=)` без `benchmark` молча игнорирует `last_n` —
  экземпляр CB-28, чья предписанная починка ОТКАЗ, а не молчание).
- Последний сминченный ID — **Т-64** (мой). Т-63 брала DIR-2. Хвост перечитать скриптом, номер не
  набирать руками.
- Чужое: в `/tmp/t63-probe-sut` висит detached probe-worktree соседнего направления. Не мой.
- **Скоропортящееся, перепроверить на месте:** статусы карт, живость Т-64, состав worktree, CI, хвост
  аллокатора.

## 2. Первые действия (по порядку)

1. Сверить §0 с источниками; расхождение — чинить handoff.
2. Прочитать хвост `PAIN-REGISTRY-codebugs.md` — пакет твоей сессии и последние ратификации.
3. Прочитать `DIR-1-trust-coordination.md` §13 целиком, затем §11 (журнал сессии 7 — там **семь**
   записей против себя, и они породили правила, которые ты обязан применять).
4. **Решить судьбу Т-64 по состоянию, а не по этому тексту:** `git log main..fix/t64-cb-152-instrument-truth`
   и `git -C .worktrees/... status`. Есть незакоммиченное — сперва закоммитить. Ветка несёт работу и
   клейм — **возобновляй сообщением, а не перезапускай холодным** (урок сессии: холодный старт дешевле
   только когда терять нечего). Слот эскалаций в брифе на ветке не заполнен — это обязательная часть.
5. Далее хвост по слоту: **CB-150** (12 из 52 исторических коммитов реестра были бы отказаны
   минт-гейтом — латентный класс ложных отказов), **CB-158** (`limit=0` перебивается в домене
   `reqs.query_requirements`, когда задан `--id`), затем CB-161/CB-162.
