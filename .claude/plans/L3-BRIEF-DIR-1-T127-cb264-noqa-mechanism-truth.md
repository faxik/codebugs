# L3-BRIEF DIR-1 / Т-127 — CB-264: описание механизма `# noqa: S608` привести к правде везде

Держатель направления DIR-1, уровень (2), 2026-08-31, смена 22.
Карта: **CB-264** (`medium`, `contract_mismatch`, `CLAUDE.md`). Носители — карта, этот файл и
строка Т-127 в `CASCADE-IDS.md` (заминчена сменой 20, выдаётся сейчас).

**Класс по Р1: B** (предмет назван полностью и разложен картой на три части; оракул половинный —
часть 1 машинная и детерминированная, части 2 и 3 суждение). **Стартовый тир: `opus`.
`effort`: `medium`.**

Ветка: `fix/cb-264-noqa-mechanism-truth`.

---

## §0. Предмет одной фразой, и почему он не буквоедство

В `CLAUDE.md` есть абзац, объясняющий, что маркер `# noqa: S608` **ничего не подавляет**, потому что
правило `S608` в этом репозитории никогда не включалось. Абзац верен по своему главному делу — и
**последняя его фраза утверждает ровно ту же ошибку рода, которую абзац разоблачает**: что другое
правило, `RUF100`, всплывёт само, «в тот момент, когда включат хоть что-нибудь».

Не всплывёт. `RUF100` (`unused-noqa`) само не входит в набор по умолчанию и должно быть выбрано
явно. **Ошибка «инструмент считается работающим, хотя его никто не запускал» допущена в абзаце,
который её же разоблачает, юнитом, чьим предметом она была.**

**Цена ошибается в опасную сторону, и это делает карту не косметикой.** Абзац — единственный в
дереве источник оценки стоимости карты CB-172 (включить `S608`). Взявший её напишет
`select = ["S608"]`, увидит красноту от `S608` и ни одного `RUF100` — и заключит, что мёртвых
маркеров других правил нет, раз документ обещал, что они всплывут сами. Документ занижает объём
работы примерно вдвое.

---

## §1. Что чинить — три части, закрываемые ОДНИМ решением

Решение одно: **привести описание механизма `S608`/`noqa` к правде везде, где оно встречается.**
Части разделимы, но собраны в один юнит потому, что порознь они дают три несогласованных описания
одного механизма — то есть ровно ту болезнь, от которой лечим.

### Часть 1 (главная, оракул машинный): ложь про `RUF100`

`CLAUDE.md`, абзац про интерполируемый идентификатор SQL: «…dead markers belonging to OTHER rules
that `RUF100` surfaces **the moment anything is enabled**». Приземлено юнитом Т-120 (мерж
`3e52280`). Опровергнуто ПОСТРОЕНИЕМ: приёмщик добавил в копию настоящую секцию
`[tool.ruff.lint]`, сперва `select = ["S608"]`, затем `extend-select = ["S608"]`, и оба прогона
дали срабатывания `S608` и **ноль** `RUF100`; только явное `--extend-select S608,RUF100` подняло
`RUF100`.

**Починка:** сказать правду — `RUF100` надо выбирать отдельно, и мёртвые маркеры других правил
всплывают ТОЛЬКО когда названо оно само. Оценку объёма работы для CB-172 привести в соответствие с
измеренным, **назвав разрез** (см. §2, ловушка 1).

### Часть 2 (структура, в ИСХОДНИКЕ): протухшая рамка в `entities.py`

`src/codebugs/entities.py`, докстрока `EntityKind` (около строк 24–28): «That is what lets the
`# noqa: S608` justifications on the f-string statements below be STRUCTURAL RATHER THAN A PROMISE».
Это ровно та картина мира, которую Т-120 убрал из `CLAUDE.md`, — и юнит `entities.py` не тронул.

**Хуже, чем в `CLAUDE.md`:** это первое, что читает добавляющий поле в `EntityKind`, и он поставит
маркер в уверенности, что маркер работает. **Честно и в брифе, и в починке: ядро фразы остаётся
правдой** — валидация в `__post_init__` действительно делает обоснование проверяемым. Ложна
посылка, будто маркер участвует в деле. Чини посылку, не выбрасывай ядро.

### Часть 3 (неполнота, читающаяся как полный ответ)

Абзац называет «настоящей защитой» валидацию у ОБЪЯВЛЕНИЯ (`EntityKind.__post_init__`,
`types.is_sql_identifier`). Замер приёмщика: из 52 неподавленных мест в `src/` **29 лежат в девяти
файлах, которые `is_sql_identifier` не вызывают ни разу**; в `findings.py` валидатор вызван один раз
на кортеже `_RESTORE_COLUMNS`, и тот запрос не входит ни в одно из 21 места того же файла.

Большинство держится **ТРЕТЬИМ механизмом, которого абзац не называет**: подставляемое есть либо
фиксированный литеральный фрагмент, либо член ЗАКРЫТОГО перечисления (`axis.column` через
`_resolve_group_axis`; `f"{size}_held"` через `_held_col` с проверкой принадлежности `ITEM_SIZES`).

**ЭТО НЕ ДЫРА В БЕЗОПАСНОСТИ**, и написать надо именно так: мест с неконтролируемым внешним
идентификатором приёмщик не нашёл, оговорив выборку. Дефект ОПИСАНИЯ: читатель заключит, что дерево
держится валидатором, тогда как оно держится тем, что снаружи в подстановку ничего не приходит — а
сохранять при добавлении нового места надо ВТОРУЮ гарантию.

### Две находки низкого веса — бери, если дёшево, не в ущерб трём

- **(а)** Первая фраза абзаца говорит «not where it is used», но `milestones/reconcile.py` проверяет
  `alias` именно при употреблении, и `CLAUDE.md` описывает это как правило; то же
  `entities.terminal_exists_sql` с `ref_expr`. Противоречия по существу нет — ложна универсальность.
- **(б)** «Values are always bound» — абсолют с двумя контрпримерами (`claims.py`, `findings.py`
  подставляют `prefix_len` текстом; безопасно, снаружи недостижимо). Ложно «always», как и соседнее
  «Never interpolate values into SQL».

---

## §2. ЛОВУШКИ. Читай раньше, чем начнёшь мерить

### Ловушка 1 (несущая): у «числа мёртвых маркеров» ЧЕТЫРЕ значения, и все четыре измерены

Карта называет **54**. Держатель прошлой смены, меряя сверх карты, получил **32** при включённом
`S608`, **48** при одном `RUF100` по `src/`, и **91** по `src/ tests/`. Ни одно из них не «неверно» —
это четыре разных вопроса.

**Развести обязан ты, и это часть задания, а не помеха ему.** Правило смены, принятое в реестр
болей: **если число зависит от того, где ты провёл границу, называй границу вместе с числом или не
называй числа.** В починенном тексте должно стоять число С РАЗРЕЗОМ (какие правила включены, по
каким путям прогон, какая версия `ruff`), иначе ты заменишь одно вводящее в заблуждение число другим.

### Ловушка 2: предмет юнита — «инструмент считается работающим, хотя его никто не запускал»

Значит **твой собственный оракул обязан быть ЗАПУЩЕН, а не прочитан.** Ни одного утверждения о
поведении `ruff` из чтения документации или из этого брифа. Прогон в одноразовой копии, с настоящей
секцией `[tool.ruff.lint]`, с печатью версии `ruff` и кода возврата рядом с числом. Это §13 п. 20 —
примени критерий юнита к собственным артефактам — и здесь он совпадает с предметом буквально.

### Ловушка 3: числа карты сняты на `f6e34b3` и УСТАРЕЛИ как состояние

Перемеряй всё. Замер, перенесённый через чужой мерж без пересчёта, устаревал здесь дважды.

### Ловушка 4: команда, вернувшая ноль или пустоту, НЕ ЯВЛЯЕТСЯ ЗАМЕРОМ

Пока не показано, что она выполнилась: код возврата, версия инструмента, непустой контрольный вывод.
Живые примеры этого репозитория: **`./.venv/bin/ruff` НЕ СУЩЕСТВУЕТ** (только
`uv run --extra dev ruff`, версия `0.15.7`); незакавыченный `--include=*.py` под `zsh` обрывает
`grep` целиком; прогон по именам ФАЙЛОВ вместо КЛАССОВ даёт `no tests ran`, читаемое как зелень.
**Печатай улику рядом с выводом.** Сегодня держатель построил пробу, которая молча обошла пустой
массив и выдала правдоподобный ложный ответ; поймала её только напечатанная рядом улика.

---

## §3. Чего юнит НЕ делает

- **НЕ ВКЛЮЧАЕТ `S608`.** Это карта **CB-172**, отдельная, с измеренной ценой (первая в истории
  проекта настройка правил линтера плюс пачка мёртвых маркеров чужих правил). Твоя работа — сделать
  описание правдивым, чтобы взявший CB-172 знал настоящую цену. Уход в CB-172 есть выход за предмет.
- Не правит правила по существу: найдёшь неверное — в `## Эскалации` и в карту, не в правку.
- Не трогает пинованный базлайн алярма и не ослабляет тестов.
- **Не переписывает соседние абзацы и не переформатирует `CLAUDE.md`.** Параллельно идёт юнит Т-133,
  правящий ДРУГОЕ место того же файла (таблица харнеса и словарь кодов возврата в разделе Workflow),
  и вскоре пойдёт Т-131, переносящий текст целиком. Держись своего абзаца.

---

## §4. Канон К-18 — предпосылки, которые надо ПОСТРОИТЬ

- **П1. Что фраза про `RUF100` действительно ложна** — прогоном с настоящей секцией
  `[tool.ruff.lint] select = ["S608"]` в одноразовой копии. **Если при каком-то честном прочтении
  или на текущей версии `ruff` она верна — это находка важнее выполненного задания**, немедленно в
  эскалацию. Сегодня ровно так сработала эскалация у соседнего юнита Т-132: приземлённая фраза
  оказалась не ложной, а ДВУСМЫСЛЕННОЙ, и это изменило запись приёмки.
- **П2. Что `RUF100` не входит в набор по умолчанию** — отдельной пробой, не выводом из П1.
- **П3. Свой разрез для числа мёртвых маркеров** (ловушка 1): назови вопрос, на который отвечаешь,
  и лишь потом число.
- **П4. Что докстрока `entities.py` говорит то, что карта ей приписывает** — прочти её сам.
  Номера строк карты могли уехать: соседи коммитят постоянно.
- **П5. Что набор зелен ДО правки** — числом, прогоном по именам КЛАССОВ. На `7aa9df2` это
  `3350 passed`, `ruff check src/ tests/` чист.

**Бриф — ДАННЫЕ, а не команда (К-12).** Опровергай: за двое суток исполнители и приёмщики
опровергли у держателей больше чисел, чем устояло, и каждый раз были правы.

---

## §5. Оракул

**Машинная половина — только для части 1:**

- Прогон в одноразовой копии с настоящей секцией правил: `S608` даёт срабатывания, `RUF100` даёт
  **ноль**; с явно названным `RUF100` — даёт ненулевое. Обе стороны, иначе это не различитель.
- Полный набор зелен числом; `ruff check src/ tests/` чист (ruff `0.15.7`). Часть 2 трогает
  `src/codebugs/entities.py` — докстроку, не код, но набор всё равно гоняется целиком.

**Части 2 и 3 машинного оракула НЕ ИМЕЮТ, и строковый оракул на этом направлении ЗАПРЕЩЁН**
(класс CB-227/CB-244: проверка «слово исчезло» зелена на мутанте, говорящем ту же неправду иначе).
Носитель — прямое сличение утверждения с деревом: для части 3 это пересчёт мест по файлам с
названным разрезом, для части 2 — прочтение докстроки рядом с кодом, который она описывает.

**Сквозной перечит абзаца КАК ОДНОГО ТЕКСТА, отдельным финальным действием.** Именно эта апертура
нашла карту: первый проход приёмки Т-120 её не имел и вынес вердикт по другим основаниям. Проверка
элементов не проверяет композиции — а здесь дефект родился ровно посекционным применением правки.

---

## §6. Регламент

- **Только worktree:** `tools/worktree-setup.sh fix/cb-264-noqa-mechanism-truth`. Карта назовётся в
  имени ветки, замок в трекере возьмётся автоматически.
- **В главном чекауте не создавай ни файлов, ни правок:** там параллельно работает соседнее
  направление, и любой изменённый ОТСЛЕЖИВАЕМЫЙ файл валит их финиш кодом 11. Держатель сегодня был
  в одном шаге от этого.
- **Уровень (3) на `main` не коммитит (К-1)**; исключение К-17 — только этот файл брифа, поимённо.
- **КОММИТЬ В ВЕТКУ РАНО И ЧАСТО.** Сессии рвутся сетью и упираются в лимит подписки — за прошлую
  смену трижды, у держателя сегодня дважды. Уцелевает только записанное.
- **Прогон:** `uv run --extra dev python -m pytest tests/ -q`; `--extra dev` не опционально.
  **Не через `nohup`** — он ставит `SIGHUP` в игнор, дочерние процессы наследуют диспозицию, и два
  теста сигналов дают ЛОЖНЫЙ отказ.
- Файлы `.claude/settings.json` и `.claude/settings.json.bak-*` — настройки владельца, **не
  трогать**, в индекс не добавлять. Прямой вызов `sqlite3` запрещён политикой окружения.
- **ФИНИШ ТОЛЬКО ПО ЯВНОМУ СЛОВУ ДЕРЖАТЕЛЯ.** Ветка готова и зелена — напиши и жди.
- **КАНАЛ К ДЕРЖАТЕЛЮ НЕНАДЁЖЕН СТРУКТУРНО (канон К-31):** подчинённый агент адресует только свою
  родительскую сессию, и при ротации держателя юнит становится сиротой. **Всякий возврат дублируй
  записью в ЭТОТ файл на ветке.** Если слова не будет, финиш запустит держатель сам, прочтя твой
  возврат; держать состояние ради слова не нужно.
- Три раздела — `## Отчёт по регламенту`, `## Проход /simplify`, `## Эскалации` — обязаны стоять В
  ЭТОМ ФАЙЛЕ на ветке: приёмщик судит артефакт, а не переписку.

---

## §7. Эскалации — помеченный пункт в этом файле на ветке

- **(а)** Фраза про `RUF100` при каком-то честном прочтении или на текущей версии `ruff` ВЕРНА.
- **(б)** Ядро фразы в `entities.py` тоже оказалось ложным (не только посылка про маркер) — тогда
  чинится больше, чем сказано, назови объём.
- **(в)** Третий механизм части 3 описать честно не выходит без перечисления мест, а перечисление
  протухнет — предложи форму и спроси.
- **(г)** Найдено место с НЕКОНТРОЛИРУЕМЫМ внешним идентификатором в подстановке — **это не правка,
  это немедленная эскалация и новая карта высокой тяжести**, приёмщик такого не нашёл, но оговорил
  выборку.
- **(д)** Найдено правило `CLAUDE.md`, кажущееся неверным, — в эскалацию и в карту, НЕ в правку.
- **(е)** Сдвинулся любой голден; понадобилось тронуть файл соседнего направления.

---

## Замеры (построены исполнителем Т-127, не прочитаны)

Все прогоны — в одноразовой копии дерева (`src/` и `tests/` скопированы в `mktemp -d`), с
НАСТОЯЩЕЙ секцией `[tool.ruff.lint]`, дописанной в копию `pyproject.toml`, а не флагом командной
строки. Инструмент — `ruff 0.15.7`, двоичный файл `.venv/bin/ruff` рабочего дерева. Дерево на
`adcf354`. Рядом с каждым числом напечатаны код возврата и объём вывода в stderr; скрипты проб —
`probe_ruff.sh` и `probe2.sh` в блокноте сессии.

### П5 (половина «до»): дерево зелено под тем набором правил, который у него ЕСТЬ

`ruff check src/ tests/` в копии, конфигурация не тронута: `rc=0`, ноль сообщений. В настоящем
`pyproject.toml` секций `[tool.ruff.lint]` — **0 штук** (посчитано `grep -c`), то есть работает
набор правил по умолчанию, как абзац `CLAUDE.md` и утверждает.

### П1: фраза про `RUF100` ложна, опровергнута ПОСТРОЕНИЕМ

| секция, дописанная в `pyproject.toml` | пути | `S608` | `RUF100` | rc |
|---|---|---|---|---|
| `[tool.ruff.lint]` + `select = ["S608"]` | `src/ tests/` | 56 | **0** | 1 |
| `[tool.ruff.lint]` + `extend-select = ["S608"]` | `src/ tests/` | 56 | **0** | 1 |

Ровно та правка, которую фраза описывает («включили хоть что-нибудь»), в обоих написаниях даёт
**ноль** `RUF100`. Мёртвые маркеры чужих правил не всплывают.

### П2: `RUF100` не входит в набор по умолчанию — отдельной пробой, не выводом из П1

Контрольный файл из одной строки `x = 1  # noqa: S608` — маркер мёртв ПО ПОСТРОЕНИЮ (правило не
включено и на этой строке сработать не могло). Прогон `--isolated`, то есть вообще без чьей-либо
конфигурации:

- набор по умолчанию → `All checks passed!`, `rc=0`, ноль `RUF100`;
- `--select RUF100` → `d.py:1:8: RUF100 [*] Unused noqa directive (non-enabled: S608)`, `rc=1`.

Обе стороны, иначе это не различитель. Дополнительно измерено, чем ещё `RUF100` поднимается, чтобы
починенный текст не оказался УЖЕ правды: `--select RUF` (семейство), `--select RUF1` (префикс) и
`--select ALL` поднимают его тоже (по 1 сообщению на контрольном файле), а `--select S608` — нет
(0). То есть «его надо назвать» верно, но называть можно и префиксом.

### Находка, едва не ставшая эскалацией (а): `--preview` поднимает `RUF100` без всякого `select`

Измерено на контрольном файле и независимо на настоящей копии дерева: `ruff check src/ tests/
--preview` **без единой секции `[tool.ruff.lint]`** даёт 517 сообщений, среди них `RUF100` — 58.
Без `--preview` та же команда даёт 0. То есть в `ruff 0.15.7` предварительный режим сам по себе
несёт куда более широкий набор по умолчанию.

**Эскалацию (а) это НЕ поднимает, и вот почему.** Приземлённая фраза утверждает причинную связь:
*включили правило → `RUF100` всплывает сам*. Эта связь ложна, что и показано таблицей П1.
`--preview` — не «включение правила», а другой механизм, и он вообще не требует ничего включать.
Но починенный текст обязан это учесть: формулировка «`RUF100` сработает, только если его выбрать»
была бы ЛОЖНОЙ под `--preview`, и читатель, запустивший `ruff check --preview`, увидел бы ровно то
же расхождение слова с деревом, ради которого карта заведена.

### П3: мой разрез для числа мёртвых маркеров, и почему у числа не одно значение

**Число мёртвых маркеров — не свойство дерева.** Маркер мёртв ОТНОСИТЕЛЬНО ВКЛЮЧЁННОГО НАБОРА:
`# noqa: F401` мёртв, если `F401` выключено, и жив, если включено и сработало. Поэтому `select`
(заменяет набор по умолчанию) и `extend-select` (расширяет его) отвечают разными числами на один и
тот же вопрос, а пути прогона двигают их ещё раз. Измерено, `ruff 0.15.7`, дерево `adcf354`:

| включённый набор | пути | `RUF100` |
|---|---|---|
| `extend-select = ["S608","RUF100"]` (умолчания ВКЛ) | `src/ tests/` | **56** |
| `select = ["S608","RUF100"]` (умолчания ВЫКЛ) | `src/ tests/` | 93 |
| `extend-select = ["S608","RUF100"]` (умолчания ВКЛ) | `src/` | 17 |
| `select = ["S608","RUF100"]` (умолчания ВЫКЛ) | `src/` | 32 |
| `select = ["RUF100"]` (S608 НЕ включено) | `src/` | 48 |
| `select = ["RUF100"]` (S608 НЕ включено) | `src/ tests/` | 110 |

Четыре значения из §2 брифа этим объясняются, а не опровергаются: **32** — это строка 4, **48** —
строка 5 (без `S608` мертвеют и его собственные 18 маркеров в `src/`), **91** — строка 6 на более
раннем дереве (сегодня 110: `tests/` с тех пор двигали), **54** карты — строка 1 на `f6e34b3`
(сегодня 56). Ловушка 3 подтверждена: числа карты как СОСТОЯНИЕ устарели, как СМЫСЛ — нет.

**Разрез, который я ставлю в текст, и почему именно он.** `ruff 0.15.7`;
`[tool.ruff.lint] extend-select = ["S608", "RUF100"]` — то есть набор по умолчанию СОХРАНЁН и
дополнен, а не заменён; прогон по `src/ tests/` — ровно те пути, которые линтует гейт финиша
(`tools/worktree-finish.sh:546`). Это единственный разрез, отвечающий на вопрос, который задаст
берущий CB-172: *что мне придётся привести в порядок, чтобы гейт остался зелёным*. Числа этого
разреза: **56 срабатываний `S608` и 56 мёртвых маркеров**, из них 17 в `src/` и 39 в `tests/`.

### Часть 3: пересчёт мест по файлам (разрез: `select = ["S608"]`, `src/`, `ruff 0.15.7`, `adcf354`)

52 неподавленных места в 11 файлах. **29 из них — в девяти файлах, которые `is_sql_identifier` не
вызывают ни разу:** `reqs.py` 6, `milestones/foundation.py` 5, `sweep.py` 5, `bench.py` 4,
`milestones/capacity.py` 4, `milestones/triage.py` 2, `blockers.py` 1, `embeddings.py` 1,
`merge.py` 1. Оставшиеся 23 — `findings.py` 21 и `milestones/reconcile.py` 2. Числа карты
воспроизвелись ТОЧНО.

В `findings.py` единственное употребление валидатора — импортная проверка кортежа
`_RESTORE_COLUMNS` (`findings.py:2043`), и тот запрос ни в одно из 21 места не входит. (Я насчитал
там сперва «2 вызова» — это два ТЕКСТОВЫХ вхождения в одном `if` и в его сообщении об ошибке, одна
проверка. Карта права, мой первый счёт отвечал на другой вопрос.)

Третий механизм, которого абзац не называет, проверен чтением обоих названных примеров:
`milestones/capacity.py:37` отказывает на `size not in ITEM_SIZES` перед склейкой `f"{size}_held"`;
`findings.py:3138` `_resolve_group_axis` возвращает разобранную ось, и в SQL идёт `axis.column`.
Оба — членство в ЗАКРЫТОМ перечислении, а не проверка формы идентификатора.

Низковесные находки (а) и (б) подтверждены чтением: `milestones/reconcile.py:191` проверяет
`alias` именно ПРИ УПОТРЕБЛЕНИИ; `claims.py:117` и `findings.py:205` подставляют `prefix_len`
текстом (это `len()` от константы модуля — снаружи недостижимо, безопасно, но «always bound»
как абсолют ложно).
