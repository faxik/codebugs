# Т-109 / CB-233 — заметки исполнителя, СПАСЁННЫЕ ИЗ ПОТЕРЯННОЙ СЕССИИ

> **Врезка держателя DIR-2, 2026-08-27.** Сессия менеджера Т-109 упала на сетевом сбое, не написав
> отчёта: ни слота эскалаций, ни трёх строк о пути модели, ни раздела «что тронул сверх брифа».
> Приёмщик нашёл вот этот файл во временном каталоге и назвал его в вердикте; я скопировала его в
> `.claude/plans/` дословно, ничего не правя, потому что временный каталог чистится и это
> единственное, что от работы исполнителя осталось в его собственных словах.
>
> **Чем это НЕ является: это не отчёт.** Здесь есть предпосылки, мутационный стенд и разбор
> голдена — то есть материал пункта 9 контракта приёмки, — и нет ничего из того, ради чего слот
> эскалаций существует. Пункт 4 приёмки на этом юните поэтому не пройден, а пункт 8 проверяем лишь
> наполовину: стартовый тир известен из брифа, итоговый и фактическое число эскалаций — ниоткуда.
> Долг мой, держателя, и он невосполним; запись сохранена, чтобы преемница видела границу между
> тем, что исполнитель успел зафиксировать, и тем, чего он не успел.

---

# Т-109 / CB-233 — заметки

Ветка: fix/cb-233-mcp-line-parameter
Дерево: /home/faxik/w/codebugs/.worktrees/fix-cb-233-mcp-line-parameter
Состояние: ветка зелёная (3066 тестов), линтер чист, адверсарий запущен.

## Предпосылки §7 — итог
1. ПОДТВЕРДИЛАСЬ. `add -f src/codebugs/loc.py -l 1850` → anchor.state="anchored", meta.loc полон.
2. ПОДТВЕРДИЛАСЬ С УТОЧНЕНИЕМ. Таблица `_ADD_META_FLAGS` годится; на argparse она НЕ завязана —
   первое поле было и dest, и именем параметра MCP. Правило вынесено в `_compose_add_meta`
   (findings.py:508); таблица читается ровно в одном месте.
3. ПОДТВЕРДИЛАСЬ. Элемент batch_add — dict; ключ `lines` ПОТРЕБЛЯЕТСЯ обёрткой (findings.py:4567),
   `_BATCH_MEMBER_KEYS` не расширялся, домен не тронут.
4. ПОДТВЕРДИЛАСЬ, ЧИСЛО: 51 из 51 живой карточки. По всем 233 строкам: 229 настоящих путей,
   3 путь-подобных отсутствующих, 1 свободный текст. Расширения живых: .py 42, .md 4, .sh 4, .toml 1.
5. ПОДТВЕРДИЛАСЬ. tests/test_loc.py:150, класс TestGrammar (строка 110). Файл не тронут вовсе.
6. ВЫПОЛНЕНА первым действием (см. 1).

## Мутационный стенд — 8 мутантов, все исполнились и покраснели
M1 обёртка add глотает lines; M2 сужение до int; M3 молчаливая победа meta; M4 требование пути;
M5 truthiness вместо `is None`; M6 batch_add роняет lines; M7 вторая копия правила в MCP;
M8 второй читатель `_ADD_META_FLAGS`.
M7 ловится ТОЛЬКО поведенческой таблицей BOTH_SURFACES, структурный тест его не видит — сказать честно.

## Неожиданный читатель
tests/test_declared_params_reach_the_body.py — `enumerate(findings)` механически удовлетворял
храповик и потребовал бы удаления строки таблицы ASSEMBLED_BY_THE_WRAPPER, хотя её ПРИЧИНА
осталась верной. Оставил цикл без enumerate, индекс из `len(enriched)`. findings.py:4552.
Второй: tests/golden/cli_surface.json — снимок справки CLI, сдвинулся на одну строку.

## Голден MCP — 4 участка, 18+/2-
1 строка описания add (новый пункт lines + переписанный meta); блок свойства "lines" в inputSchema
(16 строк); 1 строка описания batch_add (список ключей члена). SDK сворачивает блок Args: в
маркированный список, каждый пункт — одна длинная строка; мой многострочный докстринг виден там
одной строкой. Это переформатирование SDK, а не посторонняя правка. Убедился глазами.
