# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), 2026-08-28, смена 16
(вытесняет `HANDOFF-DIR-2-2026-08-27-s15.md`; следующий вытеснит этот)

**Правил здесь нет намеренно** — только указатели, факты и первые действия. Стартуй от НОРМАТИВНЫХ
файлов, а не от пересказа.

## 0. Нормативные входы — УКАЗАТЕЛИ; сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, **§13 (контракт приёмки — ДЕВЯТЬ пунктов)**, §16.3, §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md` — журнал фактов,
  шаблон юнит-брифа (каноны К-1…К-22), эскалации. Разделы «СОСТОЯНИЕ СМЕНЫ 16», «ДООХВАТ
  РЕТРОСПЕКТИВНОГО ПРОГОНА», «РЕШЕНИЕ УРОВНЯ (2) ПО ФОРМЕ ПОЧИНКИ CB-217» и «ОСТАТОК CB-62» — мои и
  подробнее этого handoff'а.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md` — там же каноны К-14…К-22
  и моя закрывающая строка по доохвату.
- **Правила репозитория:** `CLAUDE.md`. **Аллокатор ID:** `tools/cascade-mint.sh`.
- **Спецификация ретро-прогона:** `.claude/plans/RETRO-TASK-simplify-sweep-2026-08-27.md`.
- **ГОТОВЫЙ К ЗАПУСКУ юнит:** `.claude/plans/L3-BRIEF-DIR-2-cb217-archived-only-refusal.md` — форма
  починки уже решена уровнем (2), номер минтится в момент запуска.

## 1. Первое действие, до всякой работы

**Сверь очередь уровня (1) с `git log`.** Признак посадки — история, а не пакет. Девятая смена
подряд; две минуты против юнита.

**И второе, оплаченное мною дважды за эту смену: не объявляй ничего отсутствующим, не назвав
наблюдение, которым ты отличаешь «нет» от «есть, но отсюда не видно».** Мой читатель принёс сильное
обвинение — докстринг приписывает адверсариальному обзору числа, тогда как отчёт того же юнита
говорит, что обзор не вернулся. Обвинение снялось телом коммита, легшего через две минуты после
отчёта: **обзор вернулся, а отчёт устарел.** Это тот же механизм, на котором смена 15 ошиблась
трижды.

## 2. СОСТОЯНИЕ СТОЛА

**Долгов приёмки нет.** Юнит Т-111 приземлён (`0248624`), принят с оговоркой, обе его карты закрыты,
оговорка заведена картой CB-247 с двумя рёбрами наследования.

**Юнит Т-114 (CB-208 + CB-210) — состояние на момент записи см. в §9;** если он к твоему приходу не
принят, приёмка его — твоё первое действие, и в §9 сказано, чем именно он расширил объём и на каких
условиях расширение санкционировано. Захваты карточек CB-208 и CB-210 держит его ветка и снимет
финиш; если ветка брошена, захваты придётся снять руками (`claims_release`), потому что харнес
отпускает их только на посадке.

**Рабочие копии.** Мои живут в `.worktrees/` и снимаются финишем. Пять брошенных копий в устаревшем
каталоге `.claude/worktrees/agent-*` — **не мои и не твои**, они поручены DIR-1 куратором, одна из
них грязная.

**`main` зелен:** полный набор на `main` в эту смену — 3114 пройдено, линтер чист (замерено мною
лично после посадки Т-111, число совпало с отчётами исполнителя и приёмщика до единицы).

## 3. Что сделано в смене 16, одной фразой

**Доохват ретроспективного прогона по трём мержам, поручённым куратором, — ЗАВЕРШЁН; юнит Т-111
(CB-241, CB-242 и долг направления в `CLAUDE.md`) приземлён; заведён и запущен Т-114 (CB-208 и
CB-210).** Долг, передававшийся две смены подряд, разряжен.

## 4. Очередь на следующую смену — И Я ЕЁ ПЕРЕУПОРЯДОЧИЛА, С ПРИЧИНОЙ

Предшественница оставила порядок «CB-217, остаток CB-62, CB-236, CB-234». **Я ставлю CB-236 ВЫШЕ
CB-217** и говорю почему, чтобы порядок не выглядел произволом:

**Правило выбора, а не фиксированный порядок** (поправка независимого разбора, принятая мною):
между картой с бо́льшим вредом и картой с готовым брифом решает **цена задержки**. Если выбор формы
по старшей карте оставит исполнителя без работы дольше, чем занимает младшая с готовым брифом, —
пропусти младшую вперёд. Если решение принимается без простоя, тяжесть и ожидаемый вред важнее
готовности брифа.

0. **CB-158 (medium) — ПЕРЕДАНА КУРАТОРОМ из очереди DIR-1 решением от 2026-08-28, и она первая.**
   Явный предел строк перебивается обратно до числа переданных идентификаторов: домен поднимает
   заданный предел до `len(ids)`, то есть возвращает больше строк, чем попросил вызывающий.
   **Бриф ГОТОВ — `L3-BRIEF-DIR-2-cb158-explicit-limit-wins.md`, юнит Т-116, номер заминчен**;
   форма починки решена уровнем (2) («явный предел всегда побеждает выведенный»), замер
   воспроизведён мною лично на приземлённом `main` уже ПОСЛЕ Т-114. **Вторым пунктом того же юнита
   идёт кодовая половина оговорки приёмки Т-114** — долг сторожа предела. Юнит не запущен только
   потому, что куратор велел ставить его ПОСЛЕ Т-115; как только тот сядет, запускай.
0б. **CB-254 + CB-247 (обе low) — остатки текста двух принятых юнитов, БРИФ ГОТОВ:**
   `L3-BRIEF-DIR-2-cb247-cb254-text-remainders.md`, номер минтится при запуске. Семь пунктов в
   двух файлах, из них пользователю виден ровно один — абзац `CHANGELOG` говорит «с кодом выхода 1»
   в тексте, обращённом и к клиенту MCP, который кода выхода не получает вовсе. **В §0 брифа стои́т
   предупреждение сверить каждый пункт с деревом перед починкой:** обе карты — остатки, а эти файлы
   трогает почти каждый юнит направления, так что часть могла уехать с соседней работой.
0в. **CB-247 (low) — остаток юнита Т-111, заведён мною по оговорке приёмки.** Четыре места, где текст
   не доведён до правды, из них главное: юнит убрал одно ложное утверждение о живом корпусе и
   поставил на его место ДВА в настоящем времени, в том самом абзаце, который объясняет, почему так
   нельзя. Работа мелкая (три правки в `findings.py`, одна в `CLAUDE.md`, три строки в существующий
   тест) и естественно едет первой в любом юните, который и так касается `findings.py`, — но
   **отдельным пунктом брифа, а не «заодно»**.
1. **CB-236 (medium)** — названный вход местоположения (`-l/--lines` на CLI и `lines=` на MCP) пишет
   ключ во множественном числе, а грамматика захвата предпочитает единственное, поэтому соседний
   ключ `meta.line` молча побеждает явно названный вход. Замерено сквозной пробой. Это ядро
   территории (идентичность и якоря), тяжесть выше, а поверхность — та, которой пользуется всякий
   филер, называющий место в коде. **Форма починки картой не выбрана и есть работа уровня (2):**
   сравнивать названный вход со ВСЕМИ ключами приоритета, а не только со своим, и отказывать при
   расхождении — но решение о смене поведения CLI, стоящего с CB-129, принимается замером, а не по
   аналогии.

   **РАМКА РЕШЕНИЯ, которую я продумала, но НЕ замерила — бери как отправную точку, а не как
   вердикт.** Развилок две, и путать их нельзя. **Первая: отказ или победа названного входа?**
   Победа названного входа соблазнительна («он же явный»), но это тот же молчаливый сброс, только в
   другую сторону: соседний ключ пропадёт так же тихо, как сейчас пропадает названный вход. Правило
   репозитория по семье CB-28 велит отказывать, когда исполнить обе просьбы нельзя, — а здесь
   нельзя, потому что два ключа называют РАЗНЫЕ места. Моя склонность — отказ. **Вторая развилка
   труднее: что считать расхождением?** Ключи разной ФОРМЫ — единственное число против диапазона,
   ключ с именем файла против голого номера — могут называть одно и то же место разными словами, и
   отказ на таком входе будет ЛОЖНЫМ, а ложный отказ дороже дефекта. **Замер, который её закрывает и
   которого я не успела сделать:** прогнать грамматику захвата на всех парах таких ключей,
   встречающихся в реальных карточках обоих трекеров, и посмотреть, сколько пар приводятся к одному
   месту, а сколько расходятся по-настоящему. Нет приводимых пар в корпусе — сравнивать можно грубо,
   по нормализованному месту, и отказ безопасен. Есть — сравнивать надо ПОСЛЕ приведения и отказывать
   только на неприводимом. **Вторую развилку не решай по аналогии: она про данные, а не про принцип.**
2. **CB-217 (low)** — бриф ГОТОВ, форма решена (отказ на противоречивой комбинации), запускается без
   подготовки.
3. **Остаток CB-62 — НАВЕРХ, НЕ в слот.** Оба блокирующих замера перепроверены мною 2026-08-28:
   в этом трекере `total: 6, embedded: 0, dimensions: []`, то есть ось нечем питать; корпус, на
   котором она работает, живёт в другом репозитории. Развилка «заводить ли поставщика векторов
   внутри пакета» принадлежит владельцу, и условия на случай «да» уже ратифицированы в `CLAUDE.md`.
4. **CB-234 (medium)** — прочитана мною в эту смену, поэтому передаю не имя, а содержание:
   `resolve-trailers` не идемпотентен между прогонами. Повтор той же ревизионной полосы приписывает
   ту же заметку второй раз и заново двигает `updated_at`, потому что множество виденного
   дедуплицирует только ВНУТРИ одного прогона, а форма верба прямо приглашает перезапуск на
   расширенной полосе. Карта называет две формы и ни одной не выбирает. **Моя склонность, но не
   решение — я её не замеряла:** узкий вариант, где ключом идемпотентности служит сам текст заметки
   (перед добавлением проверить, не несёт ли карта эту заметку дословно, и тогда отчитаться
   «уже применено», ничего не записав), не требует ни новой таблицы, ни нового состояния и чинит
   ОБА вреда — и дубль текста, и сдвиг даты. Его слабость назвать честно: человек, поправивший текст
   заметки рукой, ломает ключ, и заметка приедет снова. Тяжёлый вариант с журналом применённых
   строк-подсказок точнее, но это схема ради одного верба. **Решать замером, а не по аналогии.**
5. **CSV — на дно (К-22).**

## 5. Сессионные факты, которых нет в файлах

- **Пуш `main` разрешён владельцем**, исполняется после каждой приёмки и в конце смены. Проверяй
  `git merge-base --is-ancestor <мердж> origin/main`, а не факт успеха пуша.
- **Хук `/simplify-traced` ПОДКЛЮЧЁН.** `.claude/settings.json` (не отслеживается git, файл
  владельца) несёт `PreToolUse` → `~/.claude/hooks/simplify-traced-gate.sh`. Он ОТКАЗЫВАЕТ, если
  проход в сессии не запускался. Держи это в брифе исполнителя. Рядом лежат
  `tools/install-simplify-gate.py` и резервная копия настроек — **не мои и не трогать.**
- **Пять брошенных рабочих копий в устаревшем `.claude/worktrees/agent-*` поручены DIR-1** куратором.
  Не трогать: одна из них грязная, и её разбор — отдельная задача с доказательством по каждой строке.
- **ЧИТАЙ ДОЛГОЖИВУЩУЮ КАРТУ КАК ОДИН ТЕКСТ: ОПИСАНИЕ И ЗАМЕТКИ.** Описание неизменяемо (это вход
  отпечатка `auto:v1`), заголовок — его первая строка, тоже неизменяем. Значит популяция, выросшая
  после заведения карты, физически ложится только в заметки, и инвентарь берётся оттуда. У CB-208 я
  вывела инвентарь **независимо, обходом синтаксического дерева**, и он сошёлся с заметками дословно
  (восемь сайтов одного класса и четыре другого) — это дешевле спора и надёжнее веры.
- **ЕСТЬ ОДИН НАСТОЯЩИЙ КАНАЛ ПРОГРЕССА ИСПОЛНИТЕЛЯ, И ОН НЕ ФАЙЛ ВЫВОДА АГЕНТА.** Журнал попыток
  посадки `.worktrees/landing-attempts.log` (заведён юнитом Т-105) пишет строку на КАЖДУЮ попытку
  интеграции: время, ветка, код возврата. По нему видно и то, что исполнитель дошёл до посадки, и
  сколько раз она отказала и почему — например, обе попытки Т-111 стоят там дословно, `13` и следом
  `0`. Он **не** говорит, жив ли исполнитель, который ещё не дошёл до финиша, — то есть отвечает
  только на вторую половину вопроса, — но эта половина раньше не отвечалась ничем.
- **СЕТЬ УРОНИЛА ОДНУ СЕССИЮ ИЗ ЧЕТЫРЁХ, И ЭТА СТРОКА СНАЧАЛА ГОВОРИЛА ОБРАТНОЕ.** Я написала здесь
  «сеть не уронила ни одной сессии» — и через полчаса исполнитель Т-114 оборвался на середине
  ответа с ошибкой соединения. Утверждение было верным в момент написания и протухло, то есть я
  посадила в собственную передачу ровно ту форму, за которой охотится это направление: суждение о
  живом состоянии в настоящем времени. **Как отличать эти два случая, теперь известно точно:**
  оборванный агент приходит уведомлением со статусом `failed` и текстом ошибки, тогда как
  работающий не приходит никак. Тишина по-прежнему не значит ничего, а `failed` значит ровно то,
  что написано, — и такой агент **возобновляется сообщением**, а не запускается заново: рабочая
  копия и вся его работа на месте. Правильная форма факта: *из четырёх фоновых агентов смены три
  вернулись сами, один оборван сетью и возобновлён сообщением.*
- **И ВОТ ВТОРОЙ КАНАЛ ЖИВОСТИ, КОТОРОГО ПРОШЛОЙ СМЕНЕ НЕ ХВАТИЛО, — ОН ОТВЕЧАЕТ НА ПЕРВУЮ ПОЛОВИНУ
  ВОПРОСА.** Предшественница записала, что работающий агент и умерший выглядят одинаково, потому что
  файл вывода не растёт до завершения, и «единственное наблюдение — завершение». Это неверно, и
  наблюдение стоит две команды: **время последней правки файлов в рабочей копии исполнителя**
  (`git status --short` плюс `stat` по именам) показывает, работал ли он минуту назад или стои́т
  часами. Я так и отличила живого исполнителя Т-114 от зависшего: гейт правился четыре минуты назад
  при неизменных остальных файлах. Плюс `git log main..HEAD` в его копии показывает, что уже
  закоммичено, — то есть можно узнать СОСТОЯНИЕ работы, не спрашивая исполнителя и не дожидаясь его.
  **Вместе с журналом посадок это закрывает вопрос целиком: чем занят — по времени правки, докуда
  дошёл — по коммитам ветки, как прошла посадка — по журналу, жив ли вообще — по уведомлению.**
- **Модели:** менеджеры — `opus` effort low; объёмное — `sonnet`/`haiku`. **Fable не использовать.**
- **Канал (3)→(2) через `SendMessage` не работает** — развёрнутые доводы исполнитель кладёт в файл
  `.claude/plans/L3-REPORT-DIR-2-T<номер>.md` и коммитит его на `main` по имени.

## 6. Что доложить куратору про доохват, если спросит

Три мержа, два с находками. `7750975` (CB-90) — находка есть, но она уже несёт открытую карту
CB-188: докстринг и `CHANGELOG` утверждают, что законную разметку в конце описания нельзя отличить
от настоящей утечки, и я опровергла это построением — приземлённый предикат режет фрагмент ленты
новостей, теряя 22 байта, тогда как словарный конъюнкт ленту не трогает, а утечку режет. Пара
`8f69133`/`fb008d1` (CB-150, CB-181) — **две ложные фразы в шапке `CASCADE-IDS.md`, исправлены
коммитом `4c08786`**: «те же 12 отказов на каждом прогоне» опровергнуто прогоном (125 переходов,
14 отказов), а «число растёт вместе с main» — трижды на разной длине истории. Поведение минт-гейта
не тронуто ни одной строкой ни одним из трёх мержей, проверено хешем файла со снятыми комментариями
в четырёх состояниях.

**И обязательно вторую половину, иначе число соврёт.** Тройка вопросов прохода измеряет
СООТВЕТСТВИЕ сделанного заказанному и правдивость написанного о коде. Она по построению не может
спросить, было ли САМО заказанное и принятое поведение верным, полным и безопасным. Сплошного ревью
кодовой базы у проекта по-прежнему не было ни разу.

## 7. Находка о процессе, которую стоит донести до уровня (1) повторно, если её не подхватят

Карта CB-186 несла две половины — незакреплённость записи тестом и простую фактическую ложь в шапке
нормативного файла, — а в пакете видов, который читал владелец, была описана одной строкой про
первую. Ратифицированное «да» закрыло карту целиком, вместе с половиной, которой в резюме не было.
Цена на этот раз — два предложения текста; форма повторима и опасна. Предложение записано строкой в
реестр болей: **строка вида обязана называть каждую половину, которую закроет «да».**

## 8. Ловушки преемнику, оплаченные ошибками этой смены

**Первая: совпадение, полученное из ошибки, выглядит точно как совпадение, полученное из истины.**
Проверяя главное утверждение двух юнитов («поведение гейта не изменилось ни одной исполняемой
строкой»), я сравнила хеши файла в четырёх состояниях. Все четыре совпали — потому что оболочка
`zsh` съела часть выражения как модификатор, `git show` не прочёл ни одного файла, и совпавший хеш
был хешем **пустого потока**. Это дословно форма «сторож отчитался «чисто», потому что не смог
посмотреть», и она едва не прошла у меня за улику. **Отличает истину от ошибки не факт равенства, а
предъявленное значение.**

**Вторая, того же корня и в тот же час: `zsh` НЕ разбивает неквотированную переменную на слова.**
Цикл `for args in "--limit 2" …; do cmd $args; done` передал строку одним аргументом, разбор
командной строки вернул код 2, и я получила «одинаковый результат на всех входах» — снова
совпадение из ошибки. Плюс `rc=$?` после конвейера сообщает код ПОСЛЕДНЕЙ команды конвейера, а не
той, которую ты мерил. Меряя код возврата, не пропускай его через `| tail`.

**Третья: пустая фикстура отвечает как исправленный дефект.** Первая проба CB-208 показала «recent
ничего не печатает» — потому что все пять моих `add` отказали гейтом минта категорий (нужен
`--new-category`), а трекер остался пуст. Прежде чем читать ответ пробы, спроси её саму, сколько в
ней строк.

**Четвёртая, не ошибка, а окупившийся расход: решение уровня (2) стоит записывать вместе с ценой.**
По CB-217 я выбрала форму, которой карта не называла, и записала в артефакт не только выбор, но и
чем он оплачен (объявленное на MCP сужение станет ложным и снимается вместе с починкой). Исполнителю
достаётся не приказ, а довод, который он может опровергнуть замером.

## 9. Итог смены — SHA для спот-чека

**Приземлилось за смену:**

| SHA | Что это |
|---|---|
| `0248624` | **Мерж Т-111** — CB-242, CB-241 и долг направления в `CLAUDE.md`. ПРИНЯТ С ОГОВОРКОЙ, обе карты закрыты, оговорка заведена картой CB-247. |
| `4c08786` | **Продукт доохвата прохода** — две ложные фразы в шапке `CASCADE-IDS.md` сняты, обе воспроизведены мною прогоном, поведение минт-гейта не тронуто. |
| `753ad17`, `e74c360` | Минт Т-111 и вписывание номера в бриф. |
| `4137b7e` | Таблица «мерж → карта → вердикт» доохвата в артефакте направления. |
| `dbf0ece`, `2badf84` | Закрывающая строка доохвата и находка о процессе в реестре болей. |
| `4e6254d`, `3fa068e`, `2eec594` | Брифы юнитов Т-114 и CB-217 (второй готов к запуску). |
| `f7e3380`, `fe58fa5` | Факт приёмки Т-111, кросс-модельный разбор моих суждений, решения по Т-114. |

**Юнит Т-114 (CB-208 + CB-210) ПРИЗЕМЛЁН — мерж `b2283dd`**, с четвёртой попытки (два отказа «main
уехал», один — лок интеграции, пока садилось соседнее направление; ни один не был дефектом ветки).
Исполнитель по дороге был оборван сетью и возобновлён сообщением. **Приёмка запущена; её вердикт —
первое, что тебе надо прочитать в артефакте направления, если он лёг после этой записи.**

**Мои личные замеры на приземлённом `main`, независимые от отчёта исполнителя и от приёмки:** мой
обход синтаксического дерева находит **ноль** несвалидированных сайтов против восьми до мержа;
`recent --limit -1`, `query --limit -1` и `usage --limit -1` теперь все отказывают с кодом 1 —
асимметрии двух соседних вербов над одной таблицей больше нет; `query --limit 0` печатает
«(limit was 0, so no rows were requested — 5 finding(s) match)», то есть говорит правду о ЗАПРОСЕ и
даёт число, которое раньше выбрасывалось ранним возвратом; а подлинно пустая выдача осталась
байт-в-байт прежней — «(no findings match)», как и требовало сужение брифа.

**Юнит вышел за рамки брифа: построен механизм** `tests/test_row_limit_gate.py` (816 строк, обход
синтаксического дерева, отказывающий при появлении девятого сайта), и подсказки приведены к новому
контракту на всех пяти вербах вместо одного, названного картой. **Расширение санкционировано мною по
ходу работы, с тремя условиями** — подробности и довод в артефакте направления, раздел про Т-114.

**Т-114 ПРИНЯТ С ОГОВОРКОЙ** на `b2283dd`; CB-208 и CB-210 закрыты. Вердикт вернулся куратору, а
не мне — я в тот момент была мертва, сессию рвало трижды за смену. Оговорка двойная и **обе половины
закрыты моим решением**: текстовую я закрыла сама (слот эскалаций дописан в отчёт юнита моей рукой,
с пометкой авторства — коммит `3eb6ec4`), кодовую сложила в юнит Т-116, потому что это одна семья
«предел выборки» и один файл сторожа. Подробности — в артефакте направления.

**Т-115 (CB-217) ПРИЗЕМЛЁН И ПРИНЯТ БЕЗ ОГОВОРОК — мерж `751fd23`**, со второй попытки; CB-217
закрыта, долгов не остаётся.
Его первый исполнитель был оборван сетью посреди проверки предпосылок; второй работал в ТОЙ ЖЕ
рабочей копии — она и захват карточки уже существовали, поэтому `worktree-setup.sh` заново было
запускать нельзя. **Основание моей формы починки я перепроверила сама:** три законные комбинации
дают 2, 3 и 1 строку, четвёртая отказывает, текст отказа называет обе стороны, а ложной фразы
`overrides include_archived` не осталось нигде в дереве. Расширение объёма (постоянный пин на
предпосылку «умолчания ложны») исполнитель эскалировал сам, и я его санкционировала: предпосылка,
решающая форму, обязана держаться механизмом.

**Наследница юнита — CB-251 (low)**, заведена исполнителем с ребром `found_during`: текст про
взаимоисключённость пары флагов написан рукой в трёх местах на каждую пару, и ничто не проверяет
согласия трёх копий с проверкой, которую они описывают. **У карты названный порог: браться при
появлении ТРЕТЬЕЙ такой пары** — при двух в одном файле цена ручного дублирования ниже цены нового
механизма. Это правильная форма отложенной карты, и трогать её раньше порога не надо.

**Т-116 (CB-158 + кодовая половина оговорки Т-114) ЗАПУЩЕН** сразу после посадки Т-115, как и велел
куратор.

**Моя находка о форме пакетов видов принята уровнем (1) каноном К-27.**

**Что осталось наверх, вопросами:** развилка владельца по остатку CB-62 (поставщик векторов внутри
пакета); разногласие о полномочии по моей правке шапки `CASCADE-IDS.md` (независимый разбор считает
её обходом ратифицированного решения, я не согласилась — довод записан в артефакте); предложение по
форме пакетов видов (строка вида обязана называть каждую половину, которую закроет «да»).
