# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), 2026-08-27, смена 14
(вытесняет `HANDOFF-DIR-2-2026-08-26-s13.md`; следующий вытеснит этот)

**Правил здесь нет намеренно** — только указатели, факты и первые действия. Стартуй от НОРМАТИВНЫХ
файлов, а не от пересказа.

## 0. Нормативные входы — УКАЗАТЕЛИ; сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §7, §8.3б, **§13 (контракт приёмки — ДЕВЯТЬ пунктов)**, §16.3, §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md` — журнал фактов,
  шаблон юнит-брифа (каноны К-1…К-22), эскалации. Разделы **«СОСТОЯНИЕ СМЕНЫ 14»** и следующие за
  ним мои и подробнее этого handoff'а.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md` — там же каноны К-14…К-22.
- **Правила репозитория:** `CLAUDE.md`. **Аллокатор ID:** `tools/cascade-mint.sh`.
- **Образцы формы брифа:** `L3-BRIEF-DIR-2-cb230-updated-at-service-write.md` (этой смены; в нём же
  §11 слот эскалаций и §12–13 — мои ответы и правки держателя) и
  `L3-BRIEF-DIR-2-cb222-cb223-category-merge-contract.md`. Обязательные разделы: **§0 «Класс и
  модель»** и **«К-18: предпосылки моих предписаний»**, написанный как ЗАДАНИЕ с глаголом.

## 1. Первое действие, до всякой работы

**Сверь очередь уровня (1) с `git log`.** Признак посадки — история, а не пакет. Правило действует
седьмую смену. Две минуты против юнита.

**И второе, специфичное для этой передачи: перечитай §7 про сеть.** Смена 14 прерывалась сетевыми
сбоями ТРИЖДЫ — дважды моя сессия, плюс упали сессии менеджера и адверсария Т-109. Это изменило то,
как надо работать, а не только то, что случилось.

## 2. СОСТОЯНИЕ СТОЛА

**Заполняется по итогу смены — см. §3.** Проверь сам: `git log --oneline -15`, `git worktree list`,
`codebugs claims`.

## 3. Фактическое состояние (перепроверь: `git log`, `git worktree list`, `codebugs claims`)

| юнит | мердж | что | вердикт |
|---|---|---|---|
| Т-108 | `d42aed5` | CB-230 — служебная запись перестала двигать `updated_at`, а смена статуса не может быть объявлена служебной | **ПРИНЯТ УСЛОВНО**, долг снят ДО посадки держателем |
| Т-109 | — | CB-233 — именованный вход места на поверхности MCP | см. §5 |

**Заведено мной за смену:** CB-234 (`resolve_trailers` не идемпотентен между прогонами) и CB-236
(именованный вход места зовётся во множественном числе, а грамматика предпочитает синглярные ключи).
**У обеих стоят рёбра `found_during`** — на CB-230 и CB-233 соответственно.

**Свои деревья убрала** — оба мёртвых дерева адверсариев (`t108-adversary` и заблокированное дерево
адверсария Т-109) сняты БЕЗ `--force`: обе копии были чисты, а блокировка снялась сама. Оставшиеся
деревья агентов принадлежат DIR-1, я их не трогала.

## 4. Т-108 — ПРИНЯТ УСЛОВНО, долг снят до посадки. Читать про долг обязательно

**Что построено.** Флаг `authored` (по умолчанию `True`) на `findings.update_finding` управляет
**ровно одной колонкой**: при `authored=False` присваивание `updated_at` не попадает в собираемый
`SET`, и не меняется больше НИЧЕГО. Комбинация `status=` с `authored=False` **отказывается выше
транзакции** — единственный способ, которым флаг мог бы стереть настоящую дату, сделан
непредставимым, вместо дисциплины в точке использования. Служебный вызов ровно один (обновление
якоря в `loc.py`); на поверхностях MCP и CLI флага нет.

**Ответ на второй вопрос карты — замером, и я перемерила независимо.** Порядок
`ORDER BY updated_at DESC, rowid DESC` в `_match_fingerprint` решает что-либо только когда у ОДНОГО
отпечатка ДВЕ И БОЛЕЕ терминальных строк. В codebugs таких **ноль**, в autosorter **один**, и обе
карточки там одного класса статуса — исход неразличим при любом победителе. **Второй ключ не
строится.** Триггер пересмотра назван состоянием, и рядом названа CB-209 как механизм, способный это
состояние создать.

**ДОЛГ, КОТОРЫЙ СНЯЛА Я, И ЕГО ФОРМА ВАЖНЕЕ СОДЕРЖАНИЯ.** Исполнитель вписал замеры о живом корпусе
прямо в докстринги, **в настоящем времени**. Пока юнит летел, куратор восстановил метки из архивной
копии от 23.08, и утверждения стали ложными. Пять текстов исправлены до посадки; самый дорогой —
**абзац `CHANGELOG`, единственный из пяти, который читает пользователь продукта**: он советовал
считать даты на затронутых карточках недостоверными, то есть не доверять данным, которые как раз
верны и восстановлены.

**Обе эскалации исполнителя решены, записаны в §12 брифа на ветке.** Э-1: ветка `Tightens:` в
разборе трейлеров остаётся **авторской**; различитель — не «кто выбрал момент записи» (иначе
служебным пришлось бы объявить и `Resolves:`, который карточку закрывает), а **приобретает ли
карточка содержимое, которое читатель ленты «недавних» захочет увидеть**. Э-2: доктрину в
`CLAUDE.md` в этом юните не писали.

**ПОПУТНЫЙ ДОЛГ С АДРЕСОМ, ТВОЙ:** записать служебный режим `update_finding` в `CLAUDE.md`, в абзац
про `escalate=False` и `promote_tags=False` — это третий опт-аут той же семьи, и место для него
готово. Отложено сознательно: второй юнит смены правил тот же `findings.py`, и лишний конфликт в
общем файле был дороже. **Сделай при следующем касании этого абзаца**; если касания не случится за
смену — минти отдельный юнит, не оставляй молча.

## 5. Т-109 — состояние на конец смены

**Заполняется по итогу приёмки.** Ветка `fix/cb-233-mcp-line-parameter`, вершина `0564930`.
Проверено держателем лично: 3071 тест зелен, `ruff` чист.

**Постановка задачи была ОПРОВЕРГНУТА проверкой К-21, и это главное, что надо знать о карте.**
Гипотеза куратора «грамматика захвата узка» неверна: `codebugs add -f <файл> -l 1850` уже даёт живой
якорь, грамматика приняла 55 из 55 корпусных токенов. Настоящая дыра — паритет поверхностей.

**Отчёта менеджера НЕ СУЩЕСТВУЕТ** — сессия упала на сети. Слота эскалаций нет, трёх строк о пути
модели нет. Это долг направления, а не исполнителя, и он невосполним.

## 6. Очередь на следующую смену

1. **CB-208 и CB-210 — одна семья** (вербы лимита утверждают ложное свойство корпуса). Куратор
   назвал их вместе; проверь родство сам, прежде чем брать в один юнит.
2. **CB-217.**
3. **Остаток CB-62** — наверх, НЕ в слот.
4. **CB-236** — наследник этой смены; закрытие меняет поведение CLI, стоявшее с CB-129, поэтому
   нужен собственный замер, а не правка на ходу.
5. **CB-234** — наследник этой смены, medium.
6. **CSV — на дно очереди (К-22).** Куратор ратифицировал сегодня: у CSV-путей ноль замеренных
   вызовов, и «видимый вред» без замера частоты вызова не повышает карту.

## 7. Сессионные факты, которых нет в файлах

- **СЕТЬ РОНЯЕТ СЕССИИ, И ЭТО ИЗМЕНИЛО ПОРЯДОК РАБОТЫ.** За смену упали: моя сессия дважды, сессия
  менеджера Т-109, сессия адверсария Т-109. **Пиши в журнал направления ПОСЛЕ КАЖДОГО ЮНИТА, а не в
  конце смены** — две прерванные попытки не оставили в журнале ни строки, и третий заход начинался с
  восстановления картины по дереву. Первое действие смены — запись состояния и коммит поимённо.
- **У упавшей сессии ОСМОТРИ РАБОЧУЮ КОПИЮ, прежде чем запускать замену.** Адверсарий Т-109 успел
  передать одну фразу — «три расходящиеся строки, все во множественном написании» — и она оказалась
  настоящей находкой (теперь CB-236). Копия при этом была пуста, но проверка стоила минуту.
- **Пуш `main` разрешён владельцем**, исполняется после каждой приёмки и в конце смены. Сегодня один
  пуш вернул отказ по правам (сеть), а слияние всё равно уехало на `origin` с пушем соседнего
  направления. **Проверяй `git merge-base --is-ancestor <мердж> origin/main`, а не факт успеха пуша.**
- **Ливлок посадки жив:** Т-108 сел со второй попытки, первая отказала с `exit 13` (`main` уехал).
  Различение `exit 13` (лечится ПОВТОРОМ) и `exit 11` (чужое грязное дерево main, лечится не тобой)
  клади прямо в промпт менеджера. `--skip-checks` не брать никогда.
- **ЗАМОРАЖИВАЙ СВОИ КОММИТЫ НА `main`, ПОКА ТВОЙ ЮНИТ СТОИТ НА ГЕЙТЕ.** Соблюдено в эту смену без
  единого нарушения — в отличие от предыдущей, где часть заметок в окно гейта была моя.
- **Модели:** менеджеры — `opus` effort low; объёмное — `sonnet`/`haiku`. **Fable не использовать.**
- **Адверсария — ТОЛЬКО в отдельном дереве** (`isolation: "worktree"`).
- **Классификатор безопасности временами отваливается по таймауту** («claude-sonnet-5[1m] is
  temporarily unavailable»), и тогда не проходят ни запуск агента, ни правка файла. Лечится
  повтором через минуту; чтения не затронуты.
- **Канал (3)→(2) через `SendMessage` не работает** — менеджер не может ответить, адрес не
  разрешается из его сессии. Развёрнутые доводы он кладёт в файл отчёта. **А если его сессия упадёт
  раньше, чем он это сделает, не останется ничего** — что и произошло с Т-109.

## 8. Ловушка преемнице, оплаченная ошибками этой смены

**Первая — и она не моя, но заплатили за неё мы: утверждение о живом мире, вписанное в код,
протухает, пока юнит летит.** Замеры о корпусе стояли в докстрингах в настоящем времени, а мир
изменился между замером и посадкой — данные восстановили. Никто не солгал; текст просто перестал
быть правдой. **Замер о состоянии живого корпуса пиши в ПРОШЕДШЕМ времени с датой** — «на 24 августа
было столько-то», — а не «сейчас столько-то». Прошедшее время с датой не протухает никогда.

**Вторая, и она про расположение: из пяти одинаковых по смыслу текстов один читает ПОЛЬЗОВАТЕЛЬ, и
он стоит дороже остальных четырёх вместе.** Абзац `CHANGELOG` не просто устарел — он давал
пользователю ложный СОВЕТ: не доверять данным, которые верны. Докстринг, устаревший на ту же фразу,
стоит несравнимо дешевле. **Разбирая список правок, спрашивай не «что здесь неверно», а «кто это
читает и что сделает, прочитав».**

**Третья — заимствованная гарантия.** Юнит утверждал, что закрытие карточки теперь ГАРАНТИРОВАННО
штампует дату, и опирался на один отказ в одной функции. Гарантия про ПАКЕТ не может стоять на
одной функции: второй санкционированный писатель статуса, `entities.EntityRef.set_status`, живёт
вне этого отказа. Гарантия уцелела только потому, что он штампует колонку сам, — **и это я проверила
чтением его исходника, а не приняла со слов приёмки.** Утверждение о гарантии, взятое на веру, и
есть ровно та ошибка, которую этот юнит чинил.

**Четвёртая — про то, чего НЕ надо чинить.** Найдя дыру в правиле (CB-236), я сначала потянулась
починить её внутри летящего юнита: правка на три строки, всё под рукой. Остановило одно измерение —
**та же комбинация принимается на `main` ДО ветки**, то есть юнит дыру не вносил. Починка внутри
чужого объёма выглядела бы как «заодно», а на деле изменила бы поведение CLI, стоявшее с CB-129, без
собственного замера и без приёмки. **Прежде чем чинить «заодно», измерь, вносил ли эту дыру тот
юнит, в который ты её чинишь.**

**Пятая — не ошибка, а окупившаяся привычка.** Обе побочные карты этой смены родились из проверки
замером того, что можно было принять на слово: эскалацию исполнителя я проверила запуском (и нашла
CB-234), фразу упавшего адверсария — тоже (и нашла CB-236). **Ни одна из двух не была бы найдена
рассуждением.**
