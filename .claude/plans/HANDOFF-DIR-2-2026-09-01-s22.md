# HANDOFF — держатель DIR-2 «Identity находок», уровень (2), смена 22 (2026-09-01)

(вытесняет `HANDOFF-DIR-2-2026-08-31-s21.md`; следующий вытеснит этот)

**Правил здесь нет намеренно** — только указатели, факты и первые действия. Стартуй от НОРМАТИВНЫХ
файлов, а не от пересказа.

## 0. Нормативные входы — УКАЗАТЕЛИ; сверь handoff с ними, а не наоборот

- **Спека ролей:** `~/workspace-private/autosorter/.claude/plans/SPEC-planner-cascade-2026-08-19.md`
  — §1, §2, §4 (пять проверок preflight), §7, §12 (два ОБЯЗАТЕЛЬНЫХ раздела брифа), §13 (приёмка,
  девять пунктов), §16.3 (Р1–Р7), §17 (ротация).
- **Артефакт уровня и ИСТОЧНИК ИСТИНЫ:** `.claude/plans/DIR-2-findings-identity.md`, раздел
  «СМЕНА 22» и далее — он подробнее этого handoff'а и писался по ходу.
- **Реестр болей (уровень (1)):** `.claude/plans/PAIN-REGISTRY-codebugs.md`.
- **Каноны направления** — в артефакте, раздел «Шаблон юнит-брифа DIR-2 — обязательные пункты»
  (К-1 … К-11); каноны куратора К-12 … К-33 — в реестре болей.
- **Правила репозитория:** `CLAUDE.md` — слой директив; глубина по месту загрузки в
  `src/codebugs/CLAUDE.md` и `tests/CLAUDE.md`, обоснования в `docs/claude-md-rationale/`.
- **Аллокатор номеров:** `tools/cascade-mint.sh --prefix Т --dry-run`.
- **Образец брифа, прошедшего обе рецензии:**
  `.claude/plans/L3-BRIEF-DIR-2-T134-release-0-3-0.md` — с записями против автора внутри,
  оставленными нарочно. Готовых к раздаче брифов НЕТ.

## 1. Первое действие, до всякой работы

**Сверяй трекер ЧЕРЕЗ КОД ЧЕКАУТА, а не через MCP.** Причина ИЗМЕНИЛАСЬ, но не исчезла: выпуск
0.3.0 приземлён и помечен тегом, поэтому чекаут теперь говорит `0.3.0`, а снимок `pipx` — `0.2.2`,
и стороны наконец РАЗЛИЧИМЫ. Но снимок по-прежнему устарел, пока владелец не выполнил
`pipx reinstall codebugs`; передача этой команды — за уровнем (1). Команда снятия состояния:

```
uv run python -c "import codebugs.db as db, codebugs.findings as f; c=db.connect(); r=f.query_findings(c, tag='dir-2', status='open', limit=200); print(r['total'])"
```

**И перед КАЖДОЙ записью в `main` применяй проверку «идёт ли чужой финиш»** — три условия вместе,
форма и доводы в артефакте (раздел смены 20, поправка DIR-1; уточнения смены 22 — там же). Скрипт
живёт в скрэтчпаде сессии и умирает с ней; восстанавливай по описанию.

## 2. СОСТОЯНИЕ СТОЛА

**Долгов приёмки нет. В полёте ничего. Захватов направления нет, рабочих копий DIR-2 нет.**

**ЗАДАЧА ВЛАДЕЛЬЦА ЗАКРЫТА: выпуск 0.3.0 приземлён мержем `1f4c4c3`, тег `v0.3.0` стоит на нём и
отправлен.** Правка двух лживых фраз в уже выпущенной секции села вторым мержем `bf73df4`. Тег
намеренно НЕ перенесён на второй мерж — довод уровня (1) в артефакте.

**ГОЛОВА ОЧЕРЕДИ — CB-290 (`high`), владелец оценил как важную. РАЗВИЛКА УЖЕ РЕШЕНА И УТВЕРЖДЕНА:
вторая дверь — обещание приводится к механизму.** Довод, числа и поправка к формулировке карты — в
артефакте, раздел «CB-290: РАЗВИЛКА РЕШЕНА ЗАМЕРОМ». **Не передоказывай её**; осталось написать
бриф. В тот же юнит берётся побочная находка: докстринг `resolve_findings` говорит «малая доля», а
якорь есть у 99 % (304 из 307).

**Номер: аллокатор предлагал `Т-137` на момент записи.** Спрашивай в момент, когда номер нужен.

**Набор на `main`:** 3373 passed, перемерян мною трижды за смену (базовая линия на `3a80bee`, оба
слитых дерева юнитов). Линтер чист. Перемеряй на своей вершине.

**`main` опережает `origin/main`** — выпуск приземлился локально; push за уровнем (1).

## 3. Очередь

0. **CB-290** (`high`) — голова, развилка решена, нужен бриф.
1. **CB-200** (`low`) — бриф написан, атакован и НЕ роздан, лежит с шапкой «СТОП» в
   `.claude/plans/L3-BRIEF-DIR-2-cb200-cli-domain-errors.md`; четыре правки в шапке обязательны до
   раздачи. **Не наследуй формулировку «`exit 0`» — она опровергнута замером прошлой смены.**
2. **CB-280** (`medium`) — форма выбрана предшественником: (a). Осталось написать бриф. Уезжает
   одним юнитом с **CB-281** (`low`).
3. **CB-286** (`medium`) — арма `except ValueError` внутри `import_findings` глушит порчу хранимых
   данных. **Замер строил адверсарий, держателем НЕ воспроизведён** — воспроизвести до починки.
4. **CB-265** (`low`) — решать ОТДЕЛЬНЫМ юнитом от CB-280 (довод из CB-276).
5. **CB-276** — не бери как «переписать карту», это неисполнимо через поверхность; см. артефакт.
6. **CB-308** (`low`, заведена мною) — поимённое называние неякорных строк в пакетном
   `anchor_resolve`. Заведена ОТДЕЛЬНО от CB-290 намеренно: там ложь текста, здесь нехватка
   возможности. Форма — добавочный ключ; `results` и `summary` трогать нельзя.
7. **CB-184, CB-269, CB-172** (`medium`) — не трогались.
8. Прочие низкие: CB-274, CB-263, CB-261, CB-260, CB-259, CB-257, CB-256, CB-251, CB-229, CB-228,
   CB-188, CB-180.

**Закрыто/починено этой сменой:** CB-289 и CB-307 (правки текста приземлены). **Заведено:** CB-307,
CB-308.

## 4. Сессионные факты, которых нет в файлах

- **CODEX ДОВЁЛ ОТВЕТ — впервые за три смены, и рецепт воспроизводим.** Запуск СВОЕЙ рукой в
  переднем плане (не через релей, который уходит в фон вопреки запрету), `< /dev/null`, и —
  главное — **четыре ОГРАНИЧЕННЫХ вопроса вместо открытой задачи**, с прямым «не исследуй дальше».
  Ответ за девять минут. Вывод верен, цитата неточна: сослался на голову функции вместо рабочего
  цикла. Проверять всё равно обязательно.
- **Сторож «идёт ли чужой финиш» починен ДВАЖДЫ, и второй раз — по срабатыванию, а не по
  рассуждению.** (1) «Консервативное чтение недоступной таблицы дескрипторов» срабатывало на 544
  чужих процессах, то есть всегда — сужено до своего uid, потому что чужой финиш идёт под тем же
  владельцем. (2) Условие (3) отбило мою запись из-за соседского `codex exec`, у которого слово
  `pytest` стояло внутри прозаического промпта: имя теперь ищется как ОТДЕЛЬНЫЙ элемент `argv`.
  Проверено в обе стороны на живом состоянии.
- **Гейт `/simplify-traced`: фаза 3 под приказом владельца до 2026-09-07** (корпус правил
  переполнен). Фазы 1–2 идут как обычно.
- **Наблюдение уровня (1) «якоря не разрешаются ни у одной карточки» — ОШИБКА ВЫЗОВА, перемерено.**
  Без `project_dir` разрешается 0 из 304, с ним — 51. Настоящий сигнал под этим и от вызова не
  зависящий: `no_grammar` 236 из 304. Стоит ли карты — решай сам.
- **Модели:** адверсарий — `opus` (окупился, опроверг четыре несущих места), сверки записей —
  `sonnet` (обе окупились, одна нашла CB-307), гигиена — `sonnet`. `Fable` не использовалась.

## 5. Что я оставляю названным — записи против себя

1. **Мой бриф нёс ОБЕ формы карт, на которых сам же построен.** Счёт «три ломающих починки» не
   сходился ни при каком прочтении (форма CB-205), а фраза про ключ `meta` приписывала выпуску
   отказ, вышедший ещё в 0.2.0 (форма CB-191). Обе нашла атака, не я.
2. **Я вписал в нормативный документ число из отчёта субагента, не перемерив его.** Хвост
   `CHANGELOG` назван «154 041 байт» вместо 154 868. Несущие утверждения той же атаки я проверил
   все и на двух нашёл неточность — а число пропустил, потому что оно выглядело «просто числом».
   **Число в чужом отчёте есть такое же утверждение, как вывод.** Поймал исполнитель через
   обязательный раздел «Эскалации».
3. **Я едва не воспроизвёл CB-289 внутри её же починки:** черновая правка написала «Three further
   limits», а путей потери строки четыре. Счётное слово СНЯТО, а не пересчитано.
4. **Я назначил `high` фразе, которая не меняет поведения** (CB-307). Замер верен, вес назначен
   неверно — две разные ошибки. **Точность замера не даёт права на вес.**

**Вывод смены, передаю усиленным.** Правило предшественника — «прибор, чей ответ повторишь как
факт, обязан пройти чужую атаку» — подтвердилось трижды. Но у него есть уточнение, оплаченное
пунктом 1 списка выше и находкой CB-307: **проверка на СОГЛАСИЕ двух текстов не отличает «оба
верны» от «оба лгут одинаково».** Ложь CB-307 стояла и в абзаце-хайлайте, и в теле записи, они
сходились друг с другом, и двунаправленная таблица прошла мимо. Третьей точкой обязан быть КОД.
Это ровно та граница оракула, которую я назвал вслух пунктом (д) собственного брифа, — и она
оказалась не декоративной.

## 6. Хозяйство

Чисто по отслеживаемым: рабочих копий DIR-2 нет, захватов нет, моих правок в главном чекауте ноль.
Неотслеживаемое в корне — `.claude/settings.json` и `.claude/settings.json.bak-20260827-185712`
(лежали до смены, не мои). Чужая рабочая копия DIR-1 (`fix-cb-285-return-code-oracle`) жива.

Стартовый промпт `PROMPT-DIR-2-next-session.md` пересобран под смену 23 в этом же заходе.
